\documentclass[11pt]{article}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage[hidelinks]{hyperref}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{conditionaltheorem}[theorem]{Conditional Theorem}
\title{A Categorical Green-Operator Program for the Riemann Hypothesis\\
\large Internal draft---not for submission}
\author{Authorship pending contribution audit}
\date{\today}
\begin{document}
\maketitle
\begin{abstract}
This internal manuscript organizes a source-authorized categorical and operator-theoretic program for the Riemann hypothesis. The complete RH conclusion is not currently established. Every unresolved dependency is governed by \texttt{publication/rh-proof-ledger.json}; conditional statements below may be promoted only after their ledger gates close.
\end{abstract}

\section{Claim boundary and theorem dependency map}
The release theorem depends on four mathematical gates: construction of the completed Adams edge, completion-stable constructor coherence, five uniform Green margins, and bidirectional spectral identification with the completed Xi function. Independent review and publication authorization are separate downstream gates.

\section{Arithmetic and analytic source constructors}
\subsection{Prime-power endpoint currents}
Record the primitive and square endpoint coefficients as linear typed currents before polarization. Distinguish amplitude, mixed-block, and self-energy coefficient lenses.

\subsection{Analytic carrier}
Develop the Mellin half-density and source-normalized Laguerre carriers, including the distinct even and odd skew-Jacobi completion channels.

\section{Closed derivative chain and boundary module}
State the closed minimal chain map
\[
P_+\mathsf D=\mathsf DQ_-,
\]
the two-step wall module, its Krein Green form, and the orientation-reversing reciprocal sewing. Prove the positive real boundary polarization
\[
-JM=I.
\]

\section{The completed Adams edge}
\begin{conditionaltheorem}[Gate G1]
The source-derived causal-history polarization, endpoint lifts, and prime assembly define a closed, completion-stable Adams edge with uniform auxiliary and loading margins.
\end{conditionaltheorem}
This section remains conditional until G1.1--G1.4 close.

\section{Constructor coherence}
\begin{conditionaltheorem}[Gate G2]
The arithmetic and analytic constructor systems are related by a convergent, interface-preserving rewrite whose finite coherence cells and completed transport are uniformly controlled.
\end{conditionaltheorem}

\section{Five-margin Green coercivity}
\begin{conditionaltheorem}[Gate G3]
On every compact off-seam parameter region,
\[
\delta_P,\ \delta_A,\ \delta_{\mathrm{glue}},\
\delta_{\mathrm{diag}},\ \delta_{\mathrm{mix}}>0
\]
uniformly over cutoff.
\end{conditionaltheorem}
The compact theta-sampling incidence cannot itself supply the arithmetic lower margin on the full intrinsic Euler completion: its normalized prime singular values tend to zero, and the Euler--theta graph intersection leaves that obstruction unchanged. A source-faithful two-port Euler valuation frame is available before theta sampling. Gate G3 therefore requires a domain-intertwining theorem showing that the endpoint lifts and Green quotient preserve both ports without radical leakage or terminal cancellation. The topology may not be changed merely to force coercivity.

\section{Spectral identification}
\begin{conditionaltheorem}[Gate G4]
The completed operator collision is equivalent, in both directions and with multiplicity, to a zero of the completed Xi function in the corresponding off-seam region.
\end{conditionaltheorem}

\section{Conditional RH deduction}
If Gates G1--G4 are proved exactly as stated, the spectral identification and off-seam coercivity exclude off-critical zeros. This section must not be promoted to an unconditional theorem before the proof ledger and independent reviews close.

\section{Verification and reproducibility}
The companion paper contains domain proofs, coherence cells, hostile examples, computational certificates, and the theorem-to-evidence map.

\end{document}
