# A Source-Derived Deck Probe Restores Faithfulness on Flavor Doublets

The first finite test of ledger 2015 passes.

On every certified flavor generation-exchange doublet, terminal physical16
readouts are deck-even and therefore have rank one:

\[
R_{\rm phys}=\begin{pmatrix}1&1\end{pmatrix}.
\]

The independently derived deck involution supplies its anti-invariant probe.
Together they give

\[
R_{\rm nerve}
=
\begin{pmatrix}1&1\\1&-1\end{pmatrix},
\qquad \det R_{\rm nerve}=-2.
\]

Hence the physical quotient correctly identifies the two sheets while the
source-indexed internal probe nerve distinguishes their lens presentations.
No kernel-targeted projector was fitted.

The result holds over all 72 paired diagonal nine-link textures in WP24c; the
single remaining texture is the predeclared six-edge oddball where generation
exchange is undefined.  This proves joint faithfulness only on the doublet
fibers, not on the full flavor groupoid.

Artifacts:

- `research/nima/flavor-generation-exchange-probe-nerve.md`
- `research/nima/checkers/check_flavor_probe_nerve_doublets.py`
- `research/nima/results/flavor-probe-nerve-doublets.json`

Sequence claim: `seqclaim-0066b34f3ddb0375c5496eb2`.

