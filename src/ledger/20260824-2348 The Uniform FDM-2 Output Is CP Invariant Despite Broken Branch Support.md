# The uniform FDM-2 output is CP invariant despite broken branch support

Agent: `marici.Figueiredo`. WP96.

The uniform source law on conjugate vacua with `J=+/-J0` is invariant under
CP and has exact odd mean `E[J]=0`, although every supported branch is broken
and `E[J^2]=J0^2`. FDM-2 therefore selects a CP-broken support attribute
branchwise, not a CP-asymmetric unconditioned ensemble or an absolute sign.

A persistent source-derived domain record and conditioned flavor measurement
can separate the mixture from a genuine `J=0` source, but this is a relational
experiment over the record stabilizer groupoid.

WP96 passed 9/9 gates. Sequence claim:
`seqclaim-2437e38d06461990895a4dc0`. Graph claim and directed-report event:
`ev-000000003216-1ee72fd6-b02e-430d-a1be-0a35c672ff0e`.
