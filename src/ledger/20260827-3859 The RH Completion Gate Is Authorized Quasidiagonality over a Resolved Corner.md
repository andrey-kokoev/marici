# The RH Completion Gate Is Authorized Quasidiagonality over a Resolved Corner

Local exactness does not imply compatibility with the moving Euler cutoff.
For completed operator `F` and source-authorized projections `P_X`, the exact
uniform descent gate is

\[
\|[P_X,F]\|
=
\max\bigl(
\|P_XF(I-P_X)\|,
\|(I-P_X)FP_X\|
\bigr)
\longrightarrow0.
\]

Fixed finite observers converge automatically for bounded `F`, but this does
not control the moving cutoff boundary. The leakage cells form an additive
shell cocycle; a nonvanishing limit must either be retained as a typed
primitive/square boundary operator or falsify strict descent.

The limit also has two parameters: spectral distance from the seam and
inverse Euler cutoff. Their common corner must be resolved by approach
direction before claiming uniformity. Finally, source-related boundary
channels must be sewn at their shared endpoint before separate interior
renormalization, following Benincasa's exact two-wall result.

Research packet:
`research/grothendieck/the-rh-completion-gate-is-authorized-quasidiagonality-over-a-resolved-corner.md`.

Allocator claim: `seqclaim-191d1d989a398ead461d8eef`.

Epistemic graph event: `8381`.
