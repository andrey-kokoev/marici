# Spectral-Action Moment Freedom Propagates to the Flavor Portal

WP839 fixes the primitive current (q=(1,2,3)^T), its normalized reflection
(H_q), the minimal completion, and the scale-free spectral shape. The
two-moment action on (D_m=mH_q) is

\[
S_{\alpha,\beta}=3\alpha m^2+3\beta m^4,
\]

with positive stationary minimum (m_*^2=-\alpha/(2\beta)) for
(\alpha<0<\beta). The coefficient packets ((-2,1)) and ((-4,1)) preserve
all upstream finite geometry but select (m_*^2=1) and (2).

Under the exact hostile interface (c=2+m_*^2) with (a=b=d=f=1), these
actions produce fixed-point coordinates (1/2) and (1/3), hence positive
portals (1/\sqrt2) and (1/\sqrt3). The interface is a falsifier, not a
proposed physical matching law: it proves that downstream matching cannot
restore selection authority absent upstream action-coefficient authority.

The first nonfaithful arrow is therefore the map from fixed finite spectral
geometry to the choice of spectral-action profile and moment normalization.
A successful source principle must derive that profile before it can derive
unique beta coefficients. Absolute clock, global basin, threshold survival,
and calibrated `physical16` realization remain additional gates.

Verification: 12 of 12 exact checks pass in
`research/flavor/checkers/wp839_spectral_action_coefficient_fiber.py`.

Graph admission: `ev-000000007885-ac1b545a-1d89-40af-8cd1-56387dc0208a`.
