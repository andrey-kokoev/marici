# The Euler-to-Green Incidence Has Rank One

Scope correction: the rank-one statement applies after the even scalar
evaluation quotient. The full boundary module also carries the independently
source-derived odd response \(Qf\), recorded in ledger 4002.

Primitive, prime-square, connected-tail, and archimedean currents are
genuinely distinct labelled boundary data. Their currently authorized path to
the Green zero-state nevertheless factors through one scalar control line:

\[
T_s(\nu)=R_sB_fB_f^\times(\nu)=\nu(f)G_s.
\]

For the complete filtered packet,

\[
T_s(\boldsymbol\nu)
=
\left(\sum_j\nu_j(f)\right)G_s.
\]

Hence the arithmetic-to-analytic incidence has rank at most one. Nonzero
labelled packets in the evaluation kernel retain provenance but are invisible
to the declared Green dynamics.

The filtration is necessary for correctly typed completion, but the scalar
path alone adds no RH force. The finite acceptance gate is a nonzero minor of
the typed incidence matrix before scalar aggregation; ledger 4002 exhibits
that minor after the odd relative current is restored.

Artifact:

- research/grothendieck/the-euler-to-green-incidence-has-rank-one.md
