# The Mellin Position Row Positively Observes Relative Prime Flux

The relative prime flux at scale `L` is

\[
j_L=\delta_L-\delta_{-L}.
\]

An even theta scalar annihilates it. Multiplication by logarithmic position
creates the source-derived odd probe and gives

\[
\langle j_L,Q\Phi\rangle=2L\Phi(L)>0
\]

for the positive theta profile. At `L=k log p`, the cyclic factor `1/k`
cancels the scale multiplicity and leaves the von Mangoldt weight `log p`.

Fourier exchanges position and derivative, so the minimal archimedean
observer is the phase-space graph pair `(Q,D)`, not one scalar theta value.
This transfers Nima's two-sided Mellin–de Rham leakage controls into the
relative-flux problem.

The finite pairing is exact. The remaining obstacle is uniform continuity of
the complete prime-power flux in the graph dual and its connection to the
completed zero-state domain.

Research packet:
[the-mellin-position-row-positively-observes-relative-prime-flux.md](../../research/grothendieck/the-mellin-position-row-positively-observes-relative-prime-flux.md)

Graph admission: `ev-000000008440-91853200-bcc1-44f8-b18b-1710ad3c4615`.
