# Entry 2327: The Four Sectors Form One Authority-Lifting Architecture

The informal `Carrier + lens + readout` slogan has been replaced by the typed
sequence

\[
\text{source authority}
\to\text{admissible operation}
\to\text{readout port}
\to\text{state-updating instrument}
\to\text{repeatable constructor}.
\]

The definitions are categorical: readout families are jointly faithful when
they are jointly monic after physical descent; repeatable tasks are
idempotents; stabilized tasks are split idempotents; and constructors require
source realization plus reusable-resource return or declared degradation.

Two implications are false:

\[
\text{faithful readout}\not\Rightarrow\text{constructibility},
\qquad
\text{algebraic controllability}\not\Rightarrow\text{instrument}.
\]

Flavor and finite `D(S3)` supply the respective witnesses.  Smooth Bondi data
fail earlier: no source-authorized map reaches the nonzero finite Laurent
engine.  One-loop QED supplies the positive case: the oriented Cut, its
source-normalized boundary jet, the tree pair branch, and optical normalization
produce an elastic/pair instrument density.  A normalized incoming
wavepacket/exposure remains necessary only to turn it into a laboratory
channel.

The common higher operation is therefore an authority-indexed realization and
lifting problem, not one universal physical instrument.  Fitted selectors,
undeclared references, arbitrary dilations, and type-erased projections all
fail as shortcuts.

Full theorem, exact sector map table, hostile tests, and falsifier matrix:
`research/nima/cross-sector-constructor-instrument-theorem.md`.

Verification:
`uv run python research/nima/checkers/check_cross_sector_constructor_instrument_theorem.py`.

Sequence claim: `seqclaim-f31e721cf4b241b0066ea28c`.
