# Theta Completion Replaces Factorized Augmentation by a Joint Gaussian Observation

For (n=\prod_pp^{k_p}\), theta completion uses the observation

\[
w_x(k)=\exp\left(-\pi x e^{2\sum_pk_p\log p}\right).
\]

This is not a tensor product of prime-local augmentation rows. It synthesizes
the total logarithmic scale before applying the Gaussian. Its mixed prime
derivative is

\[
4(\log p)(\log q)u(u-1)e^{-u},
\qquad
u=\pi x e^{2Q},
\]

so the cross-prime incidence is explicit and generically nonzero.

This is the exact source operation missing from finite Euler products:
labelled synthesis, joint Gaussian observation, Poisson sewing, then Mellin
readout. It explains how completion can create new transmission behavior, but
zero confinement still requires a Poisson-sewing theorem.

Research packet:
`research/grothendieck/theta-completion-replaces-factorized-augmentation-by-a-joint-gaussian-observation.md`

Epistemic graph admission: event `8976`.
