# Compact-Open Coherence Cannot See a Symmetric Divisor Escaping Through Infinity

The exact even real multiplier

\[
Q_R(w)=\frac{((w-R)^2+1)((w+R)^2+1)}{R^4}
\]

has the off-seam quartet \(\pm R\pm i\), yet converges uniformly to one on
every compact set. Multiplying a completed readout by \(Q_R\) therefore
inserts a hostile quartet that becomes invisible to every finite jet,
bounded-height packet, and compact-open coherence tower as \(R\to\infty\).

This corrects the proposed “no infinity flux” law. If RH is true and the
Newman constant is zero, off-seam defects at negative heat time may be
absorbed through infinite height as time approaches zero. The missing datum
is an oriented infinity-incidence port: it must allow absorption from the
negative-time side while forbidding emission into either open half-plane at
physical time zero.

Categorically, local coherence does not imply properness of the divisor map.
The next outer control channel must retain a nonzero boundary trace for the
family \(Q_R\), even though its compact-open limit is the identity.

Research packet:
`research/grothendieck/compact-open-coherence-cannot-see-a-symmetric-divisor-escaping-through-infinity.md`

Checker:
`research/grothendieck/checkers/check_symmetric_divisor_escape_at_infinity.py`

The dependency-free checker passes 12/12 gates.
