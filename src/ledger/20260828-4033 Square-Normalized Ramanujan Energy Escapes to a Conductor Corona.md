# Square-Normalized Ramanujan Energy Escapes to a Conductor Corona

After exact variance normalization, define

\[
v_Q
=
\frac{
(Q/\varphi(Q))\mathbf 1_{(a,Q)=1}-1
}{
\sqrt{Q/\varphi(Q)-1}
}.
\]

Then \(\|v_Q\|_2=1\). For every fixed-modulus cylinder observable \(h\),
Chinese remainder factorization gives a cutoff-independent numerator in
\(\langle v_Q,h\rangle\), divided by
\(\sqrt{Q/\varphi(Q)-1}\). Hence along primorial cutoffs

\[
v_Q\rightharpoonup0
\]

while its norm remains one. There is no strong Hilbert limit: the normalized
Ramanujan energy escapes to conductor infinity.

The square-current line fixes covariance size but does not close completion.
An independent conductor-corona state must be retained and coupled to the
archimedean logarithmic boundary.

Artifact:

- research/grothendieck/square-normalized-ramanujan-energy-escapes-to-a-conductor-corona.md

