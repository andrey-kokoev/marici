# Global Normalization-Cech Source Type Gate

## Record

Date: 2026-08-17

Status: proved construction-order/type obstruction after the expanded cellular
chain map. This is not a nonexistence theorem for the extraordinary
correspondence and does not determine its endpoint parity.

Three independent read-only low-cognition investigations were run through the
Narada worker-delegation MCP:

- `run-588f1276dd10463bb88c3b959180e68b`: source reconstruction;
- `run-949cf326f09743c285f205ff298a02bb`: bivariant obstruction audit;
- `run-42fe384880874edbab0ff50899f6251c`: finite-certificate design.

## Typed result

Entry344 constructs the finite cellular maps `Gamma_0, Gamma_1, Gamma_2`
from the minimally expanded full-log carrier. Entry143 fixes the target
object `E_{partial,Q}^{BM,Cech}`, its road inclusion, and its diagonal
comparison `kappa`.

The desired next arrow has the recorded type:

`alpha_sh^{!,Cech}: S_sh^{norm,reg} -> D_supp(E_{partial,Q}^{BM,Cech}) tensor chi_N`.

However, the repository does not instantiate `S_sh^{norm,reg}` as one
global complex. There is no checked totalization and differential
simultaneously containing:

1. both normalization branches `B_+ direct-sum B_-`;
2. reciprocal-regular support variance;
3. occurrence and independent multi-Rees filtrations;
4. both conductor Tor grades and extended Cech terms;
5. the expanded full-log carrier;
6. central-flip/conductor gluing; and
7. both endpoint restrictions.

Consequently `alpha_sh^{!,Cech}` is presently a signature with an undefined
global domain, not a morphism problem between two fully specified objects.
Writing a global matrix `L z = b` now would require inventing source rows,
degrees, or differentials.

## Existing local pieces

The following inputs are genuine and must be reused:

- entry93: normalization/conductor square, `B_+ direct-sum B_-`, and the
  alternating conductor grade;
- entry100: one-normal Koszul-Cech comparison and local Cousin traces;
- entry105: the scoped local plus-sheet object
  `K(I_+^dual)[-5] ~= D(F_0)[-2]`;
- entries129--131: occurrence trace, frozen endpoint Cech maps, and scoped
  road-face PC purity;
- entry143: target endpoint/Q Cech object, road inclusion, and closed
  conductor-point map;
- entry249: normalization-provenanced dP6 oriented connector geometry;
- entry344: expanded integral cellular carrier map.

None of these authorizes reflecting the local plus-sheet packet and declaring
the direct sum to be the global source. The conductor, central-flip,
occurrence, and endpoint gluing maps are part of the missing datum.

## Exact obstruction classification

Three statements must remain distinct.

1. Entry314 proves nonexistence only for the unexpanded direct
   label-preserving toroidal map.
2. Entries247 and 249 prove failure of ordinary stratified gluing and then
   construct the required topological/log-KN connector geometry.
3. After expansion, no nonexistence class in the bivariant/log category has
   been proved. The earliest obstruction is prior to such a class: the source
   object whose mapping obstruction would be evaluated is not globally
   defined.

The endpoint-fixed carrier mapping space is separately an unpointed
`Z/2`-torsor. That torsor becomes a pointing problem only after a global
kernel exists. It must not be promoted to a nonzero obstruction class or used
to infer physical parity before the domain and connector 2-cells are built.

## Minimal added geometry

The minimal legitimate next construction is one global two-sheet
normalization-Cech totalization `S_sh^{norm,reg}`, derived from the
normalization/conductor square and equipped with:

- reciprocal one-normal packets on both sheets;
- occurrence-resolved multi-Rees and two-grade Tor differentials;
- a normalization-provenanced central-flip gluing map;
- restriction maps to both closed conductor endpoints;
- compatibility with the expanded log carrier and dP6 connector boundary;
- strict D3 and reflection actions.

No target complex, scalar parity, fitted sign, or extra K6 face is required.

## First valid finite test afterward

Once the source differential is fixed geometrically, form the exact integral
system `L z = b` containing:

- the chain equation `d_T A = A d_S`;
- the interior equation
  `d_T a_int = A_boundary boundary[dP6]`;
- the based condition `pi_Q(a_int) = qSigma`;
- the boundary condition `pi_Q A_boundary = 0`;
- endpoint homotopies
  `d_T h_+ + h_+ d_{S,+} = A|Z_+ - ev_{v_+}` and its minus analogue.

It must also enforce occurrence/Rees support, both Tor grades,
radial/normal/mixed Cech squares, D3/reflection covariance, the central
`[2,1]` row, and ordinary-forgetting contraction.

A genuine Smith calculation then has a typed interpretation:

- incompatible transformed right-hand side: finite kernel obstruction;
- unit Smith factors and zero free kernel: unique saturated candidate;
- positive free rank: homogeneous ambiguity;
- nonunit factors: integral torsion or divisibility obstruction.

Even a unique solution would prove only a finite filtered chain candidate.
Properness, extraordinary base change, and spatial normalization provenance
would still require geometric verification.

## Consequence

The next task is not another endpoint coefficient census. It is to construct
the global domain complex from the already proved local normalization,
one-normal, occurrence, Tor, Cech, central-flip, and connector pieces.

Until that object exists, support-typed `W_03,25`, literal corestriction
squares, based `qSigma`, the pointed endpoint/Q mapping fiber, physical
`p_partial,Q`, its Bockstein, D8, and Jordan coherence remain open.

No graph admission is claimed.
