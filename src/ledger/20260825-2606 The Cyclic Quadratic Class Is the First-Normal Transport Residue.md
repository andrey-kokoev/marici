# 2606 — The Cyclic Quadratic Class Is the First-Normal Transport Residue

## Hard-to-vary claim

The unique cyclic quadratic Cayley--Menger class is the unique
second-fundamental-form direction generated by failure of the physical
first-normal packet to form a subconnection.

## Frozen source packet

For \(s_i=P_i^2\) and \(L_i=\partial_{s_i}K\), use the three physical
first-normal classes

\[
\mathsf n_i=\frac{L_i}{K}K^{-1/2}.
\]

Their derivatives use the same Gauss--Manin convention as the cyclic class:

\[
\nabla_{s_j}\mathsf n_i
=
\left(
\frac{\partial_{s_j}L_i}{K}
-\frac32\frac{L_i\partial_{s_j}K}{K^2}
\right)K^{-1/2}.
\]

## Exact finite result

Implementation audit: `normal_tower_class_rank` multiplies labelled
coefficients by \(K^{-1}\) before reduction. The corrected packet therefore
supplies bare \(L_i\) in its three labelled columns and supplies the already
localized derivatives directly. An earlier implementation supplied
\(L_i/K\) in those columns and introduced one extra inverse power. After
repair, the complete rank sequence and cyclic-line identification below
replicate unchanged at both primes.

At A, B, and HOMA, with a second-prime replication at A, the rank sequence
obtained by adjoining the nine ordered derivatives is

\[
3,4,4,4,4,4,4,4,4,4.
\]

Hence the first-normal span is not preserved, but its first transport closure
adds exactly one direction. The smallest witness is
\(\nabla_{s_1}\mathsf n_1\).

At every run, adjoining Entry 2591's cyclic quadratic class raises the normal
rank from three to four, while adjoining both the cyclic class and the smallest
transport witness leaves rank four. They therefore generate the same quotient
line.

## Consequence

The cyclic quadratic class is not an independently horizontal line. It is the
canonical residue of failed continuation of the first-normal packet. This
repairs Entries 2593 and 2597 while preserving Entry 2591's source-derived
cyclic occurrence construction.

No new Carrier support is required. No physical activation is established.

## Next falsifier

Construct the rank-four transport closure

\[
\widehat N_1
=
\langle N_1,\mathsf A_{\rm cyc}^{(2)}\rangle
\]

and test whether it is a subconnection. If it is preserved, compute its
extension class relative to \(N_1\). If another direction escapes, classify
that direction before adding any new coefficient cell.

## Artifacts

- `research/benincasa/cm-cyclic-second-fundamental-form-residue.md`
- `research/benincasa/checkers/check_cm_first_normal_subconnection.py`
- `research/benincasa/results/cm-first-normal-subconnection.json`
- `research/benincasa/marici-gm/src/bin/cm_normal_tower_rank.rs`

Ledger sequence claim: `seqclaim-2c2b0191a4bab7df89b6f7d9`.

Epistemic event: `ev-000000003828-c57991fb-5599-49b8-b4a7-9eee194d4951`.
