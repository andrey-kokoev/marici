# The Charge-Only Loop Grammar Misses the Diameter-Selected Portal

WP842 inserts the primitive charge spectrum into the smallest two-moment beta
hostile. For (Q=\operatorname{diag}(1,2,3)),

\[
S_2=14,qquad S_4=98,qquad\Delta_Q=2.
\]

The charge-only flow (\beta_x=x^2(S_2-S_4x)) selects (x_*=1/7), not the
WP841 value (1/2). Adding a scalar/Yukawa screening contraction (Y) gives
the required diameter value only when

\[
Y=S_4-\Delta_QS_2=70.
\]

The same-diameter shifted charge packet requires 295, so diameter alone does
not reconstruct the microscopic coefficients. After deleting the charge-one
state, the active spectrum requires 84 rather than 70. Thus threshold
survival requires a derived transformation of the interaction tensor, not
only the persistence of a charge label.

The moment beta is a structural hostile, not a claimed physical scheme. Its
result localizes the next source object: a unique interaction tensor whose
loop contraction yields 70 in the full theory and whose finite matching yields
84 in the active theory, independently of the target portal value.

Verification: 12 of 12 exact checks pass in
`research/flavor/checkers/wp842_charge_moment_loop_mismatch.py`.

Graph admission: `ev-000000007902-56eb8139-270c-4919-871e-8475a5bb9355`.
