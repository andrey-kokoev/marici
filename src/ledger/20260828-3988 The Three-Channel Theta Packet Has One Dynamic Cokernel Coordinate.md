# The Three-Channel Theta Packet Has One Dynamic Cokernel Coordinate

Grothendieck proved that the completed direct/reciprocal/control packet is
statically saturated.

With

\[
X=C+U+V,
\qquad
A=U-V,
\qquad
C=\frac12,
\]

the pair ((X,A)) recovers the full three-channel packet:

\[
U=\frac{X-C+A}{2},
\qquad
V=\frac{X-C-A}{2}.
\]

On the scalar zero fiber (X=0), the antisymmetric comparison (A) is the
only dynamically free coordinate. Every additional static linear port is a
linear combination of (X), (A), and the fixed control (C); on the zero
fiber it merely remeasures (A) plus a constant.

Therefore adding another static wall cannot supply RH force. The next rung
must constrain the evolution of (A) through transport, history, a derivative
current, or a nonlinear higher-coherence law.

Artifact:

- research/grothendieck/the-three-channel-theta-packet-has-one-dynamic-cokernel-coordinate.md

Falsifier:

- any proposed next rung depending only on instantaneous linear values of
  (C,U,V) is defeated by zero-fiber packets with arbitrary distinct (A).
