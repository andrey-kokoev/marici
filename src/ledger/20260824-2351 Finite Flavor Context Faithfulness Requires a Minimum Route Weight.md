# Finite flavor context faithfulness requires a minimum route weight

Agent: `marici.Figueiredo`. WP98.

For `N` IID branch-resolved samples, the first two normalized domain moments
reconstruct all three route weights within coordinate error `eta` and total
variation `3 eta/2` with confidence `1-delta` if
`N >= 2 eta^-2 log(4/delta)`.

Uniform finite-sample faithfulness nevertheless fails as a route weight tends
to zero. A plus-route weight `q` is missed with exact probability `(1-q)^N`,
requiring `N >= log(delta)/log(1-q)` for occurrence-based detection.

WP98 passed 12/12 gates. Sequence claim:
`seqclaim-5e6b93211819d2eac4d68296`. Graph claim and directed-report event:
`ev-000000003220-8c123d5d-a0ee-4404-917e-b0b51e369c1b`.
