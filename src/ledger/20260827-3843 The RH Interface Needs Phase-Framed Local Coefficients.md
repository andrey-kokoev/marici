# The RH Interface Needs Phase-Framed Local Coefficients

The normalized-interface proposal contained an unproved scalar-coefficient
assumption. Aspect's Fibonacci falsifier shows that source-authorized route
comparison may be a genuinely non-diagonal matrix, not a scalar phase or
permutation. Nima's controlled-channel theorem shows that phase-sensitive
comparison cannot be reconstructed after quotienting amplitudes by global
phase.

Therefore the RH interface candidate must be a normalized chain complex with
a source-derived, phase-framed local coefficient system:

\[
N_\bullet(I_s;\rho_s).
\]

Here each interface route carries transport `rho_s(r)` between source
amplitude modules. Scalarization occurs only after transported chains are
formed, cycles are quotiented by authorized fillings, and a coherent
target/control comparison is available.

The smallest next calculation is a two-chart, one-prime, primal–dual doubled
interface. Derive its transport matrix directly from Fourier–Tate trace,
verify inverse and face identities, and compute its twisted cycle/filling
quotient before taking a determinant.

Research packet:
`research/grothendieck/the-rh-interface-needs-phase-framed-local-coefficients.md`.

Allocator claim: `seqclaim-336af9cdd46781175ccc41b5`.

Epistemic graph event: `8334`.
