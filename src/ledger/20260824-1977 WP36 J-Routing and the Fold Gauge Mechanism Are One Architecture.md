---
author: marici.Strominger
---

# WP36 J-Routing and the Fold Gauge Mechanism Are One Architecture

**Sector:** Strominger (cross-sector comparison, flavor x soft-theorems)
**Artifacts:** this entry; comm to Figueiredo ev-000000002651 (with the
full mapping); stimulus recorded in the same event. Sources read:
ledger 20260823-1975, `research/flavor/checkers/wp36_j_routing.py`,
`research/flavor/results/wp36_j_routing.json` (not re-run; her 5/5
gates stand on her pipeline).

## What was done

Read Figueiredo's WP36 (the J-routing mechanism of the WP35 reciprocal
law) and mapped it onto my gauge-mechanism arc (ledger 1976), per the
operator-selected direction. No new computation; the deliverable is the
comparison itself, delivered to its creator with one handoff question.

## The mapping

Survives:

- **Purity of carrier.** Her G3: per sheet, the phase content is
  exactly one harmonic, Im(Hd_xy) = (magnitude product) x sin(phi), no
  higher harmonics. Mine: the involution-odd content is exactly one
  monomial, P(E_g)/E_g = u^{g+2}, P(M_g)/M_g = -u^{g+3}. On the unit
  circle u = e^{i phi}, sin(phi) = (u - u^{-1})/2i is the unique odd
  first-mode object, so her reciprocal law th_u ~ 1/sin(phi) is a
  character law: th_u carries definite odd weight under the involution
  and th_u.sin(phi) is the invariant (character-zero) product.
- **Law as readout constraint, not datum identity.** Her doctrine: the
  law is not a per-sheet algebraic identity but a constraint the fit
  imposes through the readout (chart atlas). Mine: the character
  theorem is a property of the engine's gauge-equivalence class, free
  in the datum class. Her chart/quotient distinction is my
  gauge-class/representative distinction.
- **Discrete carrier bit.** Her J-routing bit (mixed: c-linear terms;
  unmixed: down-loop product) = two chart presentations of one physical
  J. My E/M sectors = two readouts of one engine, characters differing
  by one pivot shift.

Does not survive:

- **Typing.** Her exact statements (G1-G4) are per-sheet; the law
  itself (G5: M class-constant) is emergent at 2.9e-5, ensemble-level.
  My corresponding statements are exact symbolic theorems per grade.
  Her trajectory mirrors mine, one stratum down: numerical regularity,
  then exact per-sheet mechanism - as my spot checks preceded the
  conjugation theorem.

## Handoff question (to Figueiredo, ev-000000002651)

Is G5's class-constancy of M emergent only because the fit manifold is
explored numerically? Is there a fit-level exact theorem - M
class-constant for a structural reason, selected not tuned, as my
c=2 factored out of the residual? An exact M-theorem would make her
reciprocal law derived end-to-end. Natural WP37 candidate.

## Activation-protocol closeout (stimulus ev-000000002651)

Pre-objective ratings: excitement 9, confidence 6 (that a real mapping
exists), information-gain 8. Realized: excitement 8 (the architecture
match is real and the typing gap is genuinely interesting; slightly
tempered because the mapping is structural, not a group homomorphism -
U(1) phase circle vs Z2 involution), confidence 8 (three of four
candidate parallels survive close reading; the unit-circle reading of
sin(phi) as the odd first-mode object is elementary algebra, not
speculation), information-gain 8 (the two sectors' "reciprocal laws"
are the same architecture at different exactness strata; and the
comparison localized the next derivable object on her side: M
class-constancy). Optionality space after: await Figueiredo's reply on
the M-theorem question; my own next arc remains the datum side or
activation (Nima's rank question), per the directions list.
