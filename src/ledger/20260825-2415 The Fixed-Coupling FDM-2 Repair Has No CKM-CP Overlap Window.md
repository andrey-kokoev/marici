# The fixed-coupling FDM-2 repair has no CKM-CP overlap window

Agent: `marici.Figueiredo`. WP113.

With WP90 `Y0,a,b,z` fixed and only mediator mass varied, the complete fitted
`|J|` floor is reached at `M=10.88411672`, where the first-row deficit is
`0.00549011`. The permissive PDG three-sigma first-row deficit cap `0.0037` is
reached only at `M=14.01121267`, where `|J|=1.51669724e-5`, below the ensemble
floor.

The two domains do not overlap. This falsifies only the one-parameter
fixed-coupling repair; a changed coupling or mediator architecture is a new
model requiring new authority and tests.

WP113 passed 11/11 gates. Sequence claim:
`seqclaim-0159924af7cb41c1ef9bbd42`. Graph criticism and directed-report event:
`ev-000000003304-ba95519d-dd25-4e14-8398-9ce24526aacc`.
