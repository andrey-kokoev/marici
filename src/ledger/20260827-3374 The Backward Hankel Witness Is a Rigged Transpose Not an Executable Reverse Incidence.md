# The Backward Hankel Witness Is a Rigged Transpose, Not an Executable Reverse Incidence

The earlier capability theorem correctly found no authorized forward arrow
from tail state back to forcing state. Under the `3+2+1` architecture, the
backward witness has opposite variance and does not require such feedback.

For the source Hankel history map and endpoint covector,

\[
(\mathsf H_f^\times E_0)(\varphi)
=E_0(\mathsf H_f\varphi)
=\int_0^\infty f(a)\varphi(a)\,da.
\]

Thus endpoint evaluation pulls back canonically to the source covector in the
rigged dual. Forward Evans construction and backward source evaluation are
exact mates without a chosen Hilbert metric or authority-smearing executable
reverse path.

A transform zero means the distinguished character lies in the kernel of
this nonzero pulled-back covector. The remaining RH-bearing `+1` cell must
compare that covector with the full typed arithmetic Ward defect, naturally
under cutoff and completion.

Research packet:
`research/grothendieck/the-backward-hankel-witness-is-a-rigged-transpose-not-an-executable-reverse-incidence.md`

Exact checker:
`research/grothendieck/checkers/check_hankel_endpoint_transpose_mate.py`

The checker passes 4/4 exact gates.
