# Unilateral Polarization Is the First Nonexact RH Interface Map

The injective trace sewing operator is conjugated Fourier and flat. Source
Mellin evaluation remains reciprocal-covariant until a unilateral tail
boundary condition is chosen. The first nonexact operation is the half-line
projection.

For scale reflection `S` and half-line projections `P_+`, `P_-`,

\[
SP_+S=P_-,
\qquad
[P_+,S]\ne0.
\]

The previously derived seam cocycle

\[
C_f(z)
=A(-\overline z)-\overline{A(z)}
=-2\int_0^\infty f(q)\sinh(\overline zq)\,dq
\]

is the endpoint shadow of this noncommutation.

For the bilateral completed source, a zero imposes an anti-diagonal endpoint
relation `A_+(z)+A_-(z)=0`; it does not annihilate either unilateral endpoint.
The smallest post-trace interface must therefore retain both polarized
sectors, reciprocal exchange, their common augmentation, the mismatch
cocycle, and only source-authorized modular fillings.

The next exact test is whether the primitive, prime-square, archimedean, and
mixed-seam packet has a frozen cell whose boundary equals `C_f`. A residual
after that typed comparison falsifies the conservation route.

Research packet:
`research/grothendieck/unilateral-polarization-is-the-first-nonexact-rh-interface-map.md`.

Allocator claim: `seqclaim-90c5a892b8c4051a36cba02a`.

Epistemic graph event: `8351`.
