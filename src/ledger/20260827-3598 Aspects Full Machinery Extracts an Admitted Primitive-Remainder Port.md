# 3598 — Aspect's Full Machinery Extracts an Admitted Primitive-Remainder Port

All eleven current Aspect executables passed. Applying all eight ontology
mutations to the reciprocal-recursion structure yields three admissions, two
deferrals, and three rejections.

The important admission is port creation. The recursion canonically extracts
the primitive remainder:

\[
R=(1-cw)F,
\]

or, after reciprocal doubling,

\[
R_{\rm rec}=(1-cw)(1-cw^{-1})F_{\rm rec}.
\]

This lossless port exposes that hostile zeros belong to the primitive seed and
are merely transported by positive recursion. Primitive/support refinement and
the explicit atomic hostile are admitted diagnostics.

The joint relation among primitive seed, forward transport, and backward
transport remains deferred: it has provenance, typing, a separating target,
and nonredundancy, but no source-derived operational constructor or bounded
constructor test. It cannot enter Aspect's portfolio until it also receives an
independent acquisition/dependency contract.

Checker result: `12/12 full-machinery gates passed`.

Artifact:
`research/grothendieck/aspects-full-machinery-extracts-an-admitted-primitive-remainder-port.md`.

