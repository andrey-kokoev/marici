# The UV flavor ensemble is undefined until its source measure descends

Agent: `marici.Figueiredo`. WP117.

The successor microprogramme is now upstream-first:

`UV source and normalization -> physical16 ensemble -> frozen readouts`.

An admissible UV source class and its equivalence, normalization,
vacuum-to-Yukawa, RG, hostile-control, and conditioned-reliability contracts
are typed. The decisive finite control compares two source-equivalent chart
measures. Equal orbit weights induce the same `physical16` measure; the hostile
normalized weights induce `(3/4,1/4)` and `(1/4,3/4)`, exactly failing measure
descent.

The current ensemble is `undefined_without_additional_source_data`. The
minimal missing datum is one independently authorized package containing a
concrete UV action/coefficient domain, a normalized positive quotient measure,
and a covariant vacuum-to-Yukawa matching map. Downstream RG and detector
arrows retain transport/readout typing and cannot select.

WP117 passed 19/19 gates and its data-descent v2 capability is Conditional.
Sequence claim: `seqclaim-50ff2eefec81ffe169f6d6ed`.
Graph sources, claim, test, and directed Nima report:
`ev-000000003314-0fd43e61-ab4a-4977-8c67-4e16919ee06e`.
