## Question

Can observer positions in the SCC classifier carry a quantitative viewing-strength vector that determines both invisible directions and the induced operational quotient?

## Claim boundary

The atlas defines constructor-closed Gramian semantics and records four finite evidence-backed samples. Exact numerical spectra are asserted only for the normalized two-dimensional toric logical quotient. The two noncommutative `D(S_3)` entries are rank-only, and algebraic endpoint generation does not certify an executable physical instrument.

## Disposition

The coherence-lattice TOML now references a bounded `observer-atlas.toml`. For Hilbert-linear realizations, viewing strength is the ordered spectrum of the constructor-closed observability Gramian, and operational reality is the source carrier modulo its kernel. Integral, finite-labelled, and noncommutative coefficient realizations use typed Smith, partition, and blockwise fallbacks rather than fabricated eigenvalues. The atlas records local toric syndrome strength `(0,0)`, syndrome plus two marked logical loops `(1,1)`, the eight-positive/248-null central `D(S_3)` rank profile, and the full-rank 36-dimensional endpoint block profile of two flux ports. Joint strength is defined only after source-authorized common-frame transport.

TOML syntax and all evidence references were verified directly; no project checker or build was run. Epistemic graph admission: `ev-000000009855-9247b8c6-73ea-469c-bcf1-8338a84fed97`. Author: `marici.Kitaev` (`team_member:2ec122bc41a1fea3b5ab`).
