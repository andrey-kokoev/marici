# Two-Edge Bivariant Trace and the Unlocalized Two-Flip Alignment Gate

## Record

Date: 2026-08-15

Status: the full two-edge coefficient/bivariant trace is proved; the claim
that the universal unlocalized second weighted graph canonically selects its
physical aligned residue is falsified. The symbolic local-\(Q\) collar
attachment is nonzero, but no spatial exceptional-fibre pushforward,
\(\alpha_{03}\), generic-to-special \(Q\) leg, global mapping fiber, or
parity is constructed. No graph admission is claimed.

## The two marked occurrence edges

Let the first and second marked edges be

\[
e_c:v_+\longrightarrow m_+,
\qquad
e_r:m_+\longrightarrow c,
\]

with weighted occurrence boundaries

\[
\boxed{
d e_c=X_Dm_+-x_5v_+,
}
\]

and

\[
\boxed{
d e_r=x_0c-x_1m_+.
}
\]

The shared \(m_+\) coefficient cancels in the combination

\[
\boxed{
E_{03}^{(2)}:=x_1e_c+X_De_r.
}
\]

Indeed,

\[
dE_{03}^{(2)}
=X_Dx_0c-x_1x_5v_+.
\]

The middle equality is the lcm identity

\[
x_1X_D=X_Dx_1.
\]

Since \(x_1\) and \(X_D\) are independent primitive labels, the coefficient
pair \((x_1,X_D)\) has gcd one. Thus the concatenation is primitive; it is
not obtained by fitting a rational scalar at \(m_+\).

## Both normal double-Rees packets

The first edge carries the complete independent normal kernel

\[
K(u_5,u_D),
\]

and the second carries

\[
K(u_1,u_0).
\]

Each is the saturated \(1\to4\to4\) double-Rees totalization of its two
primitive normal/occurrence relations. Their Cech realizations retain all
degree-zero, lower, and double-overlap terms, including

\[
\frac1{u_5u_D}
\qquad\text{and}\qquad
\frac1{u_1u_0}
\]

only in their named overlap summands. With the product orientations,

\[
d^2=0
\]

on both full packets. No base normal is inverted.

Deleting either double overlap or any forced lower copy destroys its chain
equation. The full lower terms are part of the trace, not auxiliary choices.

## Repeated-(u_3) excess and sign

The entry-100 repeated-normal packet is a spectator factor common to the two
edges. In its ordered middle basis, the primitive excess vector is

\[
\boxed{
\eta_{\rm mix}=(-q_3,-1).
}
\]

It is closed because

\[
u_3+q_3u_3^\vee=0.
\]

One coordinate of \(\eta_{\rm mix}\) is a unit, so the vector is primitive.
Its normalized projection \((1,-q_3)\) retains the entry-100 orientation
sign. Both conductor grades survive:

\[
\operatorname{Tor}_0\simeq\mathbf1,
\qquad
\operatorname{Tor}_1\simeq\mathbf1.
\]

The two normal double-Rees packets and this repeated-\(u_3\) factor totalize
without torsion or a fitted scalar.

## Exact shared-(m_+) lcm gluing

The gluing at \(m_+\) is coefficientwise strict. The first edge contributes
\(x_1X_D\) after multiplication by \(x_1\), and the second contributes the
same monomial \(X_Dx_1\) after multiplication by \(X_D\), with opposite
incidence signs. Therefore the shared term cancels exactly before any
specialization.

This lcm equality is independent of the normal Cech choices. It introduces
no new torsion and does not select a special-fibre component of either
weighted graph.

## Full coefficient/bivariant trace theorem

Combining the primitive occurrence concatenation, both complete normal
double-Rees resolutions, and the repeated-\(u_3\) excess produces a closed
integral two-edge coefficient/bivariant trace

\[
\Theta_{+,03}^{(2),\rm coeff}.
\]

It has:

- primitive occurrence top \(x_1e_c+X_De_r\);
- exact shared-\(m_+\) lcm cancellation;
- every lower and double-overlap normal term;
- \(d^2=0\) strictly;
- primitive \(\eta_{\rm mix}\) with both Tor grades;
- no positive coefficient Tor from the independent regular products; and
- no integer torsion or base inversion.

This is a genuine coefficient/bivariant construction, not merely a rank
shadow.

## Symbolic attachment to the certified target collar

Compose formally with entry 170's complete
\(P_{03}\otimes C_D\) target collar. The occurrence top reaches the
primitive nonzero class

\[
p_{03}\ne0.
\]

After the full peripheral completion and relative quotient, the local
entry-143 \(Q\) row is

\[
\boxed{
\text{top}\longmapsto\frac{X_D}{u_D}p_{03},
\qquad
n_D\longmapsto p_{03}.
}
\]

Thus the coefficient trace is algebraically compatible with a nonzero
\(p_{03}\) and the local \([X_D/u_D\mid1]\) collar. The statement is
symbolic and branch-local: it assumes the already certified target collar
and does not construct the spatial source-to-target map that realizes the
composition.

## The universal second weighted graph

For the second flip retain

\[
u_1=t_1x_1,
\qquad
u_0=t_0x_0.
\]

With occurrence coordinates \([G:H]=[x_1:x_0]\) and normal coordinates
\([P:Q]=[u_1:u_0]\), the projective closure is

\[
\boxed{
t_0HP-t_1GQ=0.
}
\]

Where \(t_0,t_1\) are units it is the aligned graph

\[
[G:H]\longmapsto[t_1G:t_0H]
\]

with the physical coordinate endpoints fixed.

Over the unlocalized base this graph is not a clean diagonal:

\[
t_1=0:\quad\{H=0\}\cup\{P=0\},
\]

\[
t_0=0:\quad\{G=0\}\cup\{Q=0\},
\]

and

\[
t_0=t_1=0:\quad\mathbb P^1\times\mathbb P^1.
\]

The double-zero equation vanishes and retains a rank-one excess
\(\operatorname{Tor}_1\). Projective saturation removes none of these honest
limiting components.

## Facewise legality does not select the residue

The actual middle face

\[
m_+=\{D03,x_1,x_3\}
\]

contains the mixed normal-circle state

\[
H=\{D03,x_1\}
\]

needed to totalize the two flips. The external repeated-\(u_3\)
\(\operatorname{Tor}_0/\operatorname{Tor}_1\) packet is also algebraically
available.

Nevertheless the universal graph has more special-fibre components than an
aligned component-supported residue. It therefore does not select a unique
\(\eta_{\rm mix}\) representative or endpoint transfer. Existence of the
correct face state proves legality of a possible landing; it does not choose
the extraordinary pushforward into that state.

The first universal obstruction is absence of an unlocalized,
nowhere-vanishing alignment between \(t_1\) and \(t_0\). The extra components
and their Tor line survive saturation.

## The (F_B/Q) support boundary

Every vertex and edge of the finite path

\[
v_+\longrightarrow m_+\longrightarrow c
\]

has at least one short-boundary label. Hence the entire literal two-flip path
lies in \(F_B\), and

\[
\boxed{
\pi_Q(C_{\rm two\ flip})=0
\quad\text{in}\quad Q=F_K/F_B.
}
\]

This does not negate the symbolic nonzero \(p_{03}\) collar: \(p_{03}\) is
the relative long-facet top supplied by the full target completion, not the
literal image of a path already contained in \(F_B\). A spatial
exceptional-fibre pushforward must cross these support types.

## Missing spatial map and global boundary

The coefficient theorem does not construct the exceptional-fibre
nearby-cycle/proper pushforward

\[
\alpha_{03}^{\rm supp}:
\mathcal S_{+,03}^{\rm two\ flip}
\longrightarrow
\mathcal E_{03}^{\rm BM,\check C}
\]

that would select aligned components, distribute both Tor grades, and map to
the literal \(p_{03}/Q03\) states. It also does not construct the
normalization-sheet source, polarity-conjugate endpoints, three-road gluing,
or the logarithmic Beck--Chevalley cell.

Therefore the framed endpoint/\(Q\) mapping fiber is uninstantiated and
reflection parity is undefined.

## Anti-circularity controls

- Do not infer a spatial trace from the closed coefficient trace.
- Do not omit lower or double-overlap terms from either normal kernel.
- Do not contract the repeated-\(u_3\) Tor pair.
- Do not use existence of the mixed \(m_+\) face state to choose a residue.
- Do not remove special-fibre components by saturation or invert \(t_0,t_1\)
  on the universal base.
- Do not identify the \(F_B\)-supported path with the nonzero relative
  \(p_{03}\) top.
- Do not infer \(\alpha_{03}\), generic \(Q\), the mapping fiber, parity, or
  graph admission from the symbolic collar attachment.

## Falsifiers and scope

The coefficient theorem would be falsified by a nonprimitive concatenation,
failure of the shared lcm cancellation, nonzero \(d^2\), missing lower terms,
loss of either Tor grade, incorrect \(\eta_{\rm mix}\) sign, integer torsion,
or zero symbolic \(p_{03}\) attachment.

The universal alignment gate would be falsified if either single-zero fibre
were clean and irreducible, if the double-zero product or Tor line vanished
under saturation, or if the full unlocalized closure canonically selected a
unique aligned residue.

The spatial boundary would be crossed by an independently constructed
component-supported nearby-cycle pushforward landing in the actual mixed
\(m_+\) state and nonzero \(p_{03}/Q03\) collar. No global nonexistence theorem
is asserted.

## Provenance and exact certificates

The exact checkers are

- `research/voevodsky/check_d03_two_edge_bivariant_trace.rs`; and
- `research/voevodsky/check_d03_two_flip_unlocalized_alignment_gate.rs`.

Their SHA-256 hashes are, respectively,

- `d23e79f568c7a416f8bb93cdb43b5a82934333b033940e3af043cee7f2985889`;
  and
- `075971881acf50480c0e723f49cf5231f19f8a2f08eea9777b923f26710d70b9`.

The first verifies the primitive two-edge occurrence concatenation, both
complete normal totalizations, every lower/double term, \(d^2=0\), the
repeated-\(u_3\) Tor/sign packet, exact lcm gluing, torsion freedom, and
symbolic nonzero \(p_{03}\)/local-\(Q\) attachment. The second verifies the
unit-locus second graph, every extra special fibre and Tor line, actual
mixed-face legality, nonuniqueness of the unlocalized residue, and zero
literal \(Q\) projection of the two-flip path.

## Next experiment: anti-diagonal component cone

Use entry 93's two normalization branches to form the anti-diagonal
component-supported cone with

\[
J_+=(t_1),
\qquad
J_-=(t_0).
\]

Construct the polarity-oriented anti-diagonal map

\[
J_+\oplus J_-
\xrightarrow{\ \varepsilon_+-\varepsilon_-\ }
\mathcal O_C
\]

and retain both conormal symbols \([t_1]\), \([t_0]\) and their derived
Tor grades. Then build the exceptional-fibre nearby-cycle pushforward from
this selected component cone to the actual mixed \(m_+\) state. Require its
restriction to reproduce \(\Theta_{+,03}^{(2),\rm coeff}\), its target to be
the full \(p_{03}\)/local-\(Q\) collar, and its generic source to retain
\(q_J\). Only afterward test global polarity, the mapping fiber, or parity.

## Outcome contract

~~~json
{
  "claim": "The full two-edge D03 coefficient/bivariant trace closes integrally with primitive x1*e_c+X_D*e_r, complete K(u5,u_D) and K(u1,u0) lower/overlap terms, primitive repeated-u3 eta_mix and both Tor grades, exact torsion-free m_plus lcm gluing, and symbolic nonzero p03/local-Q attachment; however the universal unlocalized second weighted graph has extra single/double-zero fibres and does not canonically select the spatial aligned residue.",
  "status": "proved",
  "scope": "proved two-edge coefficient/bivariant trace and symbolic one-road target-collar attachment, with scoped falsification of universal unlocalized alignment; no graph admission, spatial alpha03, global mapping fiber, or parity",
  "assumptions": [
    "All occurrence and normal/Rees parameters are independent regular labelled sections.",
    "Double inverses occur only in their indicated Cech overlap summands.",
    "The completed first-edge and certified P03 tensor C_D target orientations are fixed.",
    "Entry-143 face/circle labels and F_B/Q filtration remain literal."
  ],
  "factorization": {
    "occurrence_boundaries": ["X_D*m_plus-x5*v_plus", "x0*c-x1*m_plus"],
    "primitive_concatenation": "x1*e_c+X_D*e_r",
    "shared_m_lcm": "X_D*x1=x1*X_D, cancels exactly",
    "normal_kernels": ["K(u5,u_D)", "K(u1,u0)"],
    "normal_product_ranks": [[1, 4, 4], [1, 4, 4]],
    "all_lower_terms": "retained",
    "double_overlaps": ["1/(u5*u_D)", "1/(u1*u0)"],
    "d_squared": "zero",
    "eta_mix": "(-q3,-1), primitive with entry-100 sign",
    "repeated_u3_Tor": [1, 1],
    "torsion": "none",
    "base_inversion": "none",
    "p03": "nonzero primitive symbolically",
    "local_Q": ["top -> (X_D/u_D)*p03", "n_D -> p03"],
    "second_cross_relation": "t0*H*P-t1*G*Q=0",
    "second_unit_locus": "aligned weighted graph",
    "t1_zero": "two P1 components",
    "t0_zero": "two P1 components",
    "double_zero": "P1xP1 with rank-one Tor1",
    "mplus_mixed_state": "H={D03,x1} exists",
    "aligned_residue_from_full_closure": "not unique",
    "two_flip_support": "entirely in F_B",
    "Q_projection_rank": 0,
    "spatial_alpha03": "unconstructed",
    "mapping_fiber": "uninstantiated",
    "parity": "undefined"
  },
  "evidence_refs": [
    "research/voevodsky/check_d03_two_edge_bivariant_trace.rs",
    "research/voevodsky/check_d03_two_flip_unlocalized_alignment_gate.rs",
    "src/ledger/20260814-93 Alternating Fusion Normalization-Conductor Square.md",
    "src/ledger/20260814-100 Support-Directed Can-Var Packet and Three Local Cousin Traces.md",
    "src/ledger/20260815-143 Two-Endpoint Road Carrier and the Loaded Conductor Cospan Blocker.md",
    "src/ledger/20260815-170 Product-Rees F03 Square and the Forced Long-Normal Completion.md",
    "src/ledger/20260815-173 Component-Supported Semistable Node and the vplus Coefficient Counit.md"
  ],
  "checker_sha256": {
    "two_edge_bivariant_trace": "d23e79f568c7a416f8bb93cdb43b5a82934333b033940e3af043cee7f2985889",
    "two_flip_unlocalized_alignment_gate": "075971881acf50480c0e723f49cf5231f19f8a2f08eea9777b923f26710d70b9"
  },
  "counterevidence": [
    "The universal second graph retains extra single-zero components and a double-zero P1xP1 Tor fibre.",
    "The mixed m_plus face state exists but does not select a unique aligned residue.",
    "The literal two-flip path is contained in F_B and maps to zero in Q.",
    "No exceptional-fibre pushforward or spatial alpha03 has been constructed."
  ],
  "next_experiment": "Form the entry-93 anti-diagonal component cone J_plus=(t1), J_minus=(t0), retain both conormal/Tor packets, and construct its exceptional-fibre nearby-cycle pushforward to the actual mixed m_plus state and full p03/local-Q collar while retaining q_J."
}
~~~
