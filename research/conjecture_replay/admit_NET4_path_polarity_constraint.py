#!/usr/bin/env python3
import json,sys
from dataclasses import asdict
from pathlib import Path
R=Path(__file__).resolve().parents[2];sys.path.insert(0,str(R/'research/conjecture_net'));from event_log import *
prior=json.loads((R/'research/conjecture_replay/results/NET3_composite_arrow_test_admitted.json').read_text());p=prior['projection'];src=R/'research/conjecture_replay/results/degree15_path_polarity_constraint.json';x=json.loads(src.read_text());g1=digest({'parent':p['graph_digest'],'constraint':'direct=*+,factored=*+,coherent'});ev=TopologyEvent(p['graph_digest'],'dependency_change',(),(),(),('VC3 direct path -> *+','VC3 factored path -> *+','direct composite = factored composite'),g1,'freeze terminal-polarity existence condition before constituent outcomes',(f'{src.relative_to(R).as_posix()}#{digest(x)}',),'2026-09-11T00:00:30Z');log=EventLog(p['graph_digest'],p['state_id']).append(ev);out={'schema':'marici.conjecture-replay.NET4-path-polarity-constraint-admitted.v1','event':{'event_id':ev.event_id,**asdict(ev)},'projection':asdict(log.project()),'constraint_analysis':x['analysis'],'metric_projection':x['metric_projection'],'next_recommendation':'R_exact_degree15','passed':True};(R/'research/conjecture_replay/results/NET4_path_polarity_constraint_admitted.json').write_text(json.dumps(out,indent=2,default=list)+'\n');print(json.dumps({'passed':True,'status':x['analysis']['status'],'next':out['next_recommendation']}))
