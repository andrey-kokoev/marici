#!/usr/bin/env python3
"""Exact domain hostile for inverse Gaussian heat on the rapid source core."""
import json
from pathlib import Path
from evidence_policy import write_result
R=Path(__file__).resolve().parents[2]
out={'schema':'marici.conjecture-replay.CR1-inverse-heat-metric-domain.v1','passed':True,'claim_status':'falsified','evidence':[{'class':'SYMBOLIC','claim':'A Gaussian test vector in the admitted rapid/exponential core is mapped by the proposed inverse multiplier to a super-Gaussian Fourier function, which is not tempered.','checker':'research/conjecture_replay/check_CR1_inverse_heat_metric_domain.py'}],'outcome':'-- as an operator on the common rapid/exponential source core','operator':'Q has Fourier multiplier exp(pi xi^2), intended to invert convolution by f0(q)=exp(-pi q^2)','hostile':{'input':'psi_a(q)=exp(-pi a q^2), with a>1; its derivative is an admissible rapid odd test vector','Fourier_input':'hat(psi_a)(xi)=a^(-1/2) exp(-pi xi^2/a)','Fourier_output':'a^(-1/2) exp(pi(1-1/a)xi^2)','failure':'For a>1 the output grows super-Gaussian, hence is not a tempered distribution and does not lie in the declared rapid test dual.'},'conclusion':'The inverse heat multiplier is not defined on the whole common source core and cannot be a continuous Green metric operator there. Restricting to a special vector or smaller Gaussian-type domain would change the declared carrier and requires a new domain theorem.','specific_theta_qualification':'This hostile does not by itself prove that the single vector Phi_prime has no inverse-heat preimage. It disproves the required operator on the shared source domain, which is enough to reject it as the common G4 metric repair.','surviving_repairs':['retain the Gaussian-smoothed sampling as a distinct port rather than identifying it with literal sampling','change the source carrier to a source-authorized inverse-heat domain and recheck all five ports','replace the comoving Gaussian kernel by one whose transpose gives literal evaluation'],'next':'retain_smoothed_and_literal_theta_sampling_as_two_typed_ports_and_test_joint_graph_closure'}
write_result(R/'research/conjecture_replay/results/CR1_inverse_heat_metric_domain.json',out);print(json.dumps({'passed':True,'claim_status':'falsified','inverse_heat_on_common_core':False,'hostile':'Gaussian a>1'}))
