# Many-many grade-resolved forward pair incidence

Define the grade-resolved loaded pair map

$$
\mathcal B_{\rm fwd}(x)
=\sum_{p,k,q,\ell}
 c_{p,k}\overline{c_{q,\ell}}
B_{\rm rad}\bigl(x_{(p,k),(q,\ell)}\bigr).
$$

For the diagonal rank-one source `x=c tensor conjugate(c)`, this becomes

$$
\mathcal B_{\rm fwd}(c\otimes\overline c)
=\sum_{p,q}c_p\overline{c_q}\,\rho_{p,q}^{\rm graph}.
$$

Absolute convergence follows from the prime-power theta majorant. The map preserves reciprocal Hermitian symmetry and retains ordered matrix units before wall codiagonalization.

Status: grade-resolved forward pair incidence assembled under the theta loading.
