# Many-many infinite shell-mode compatibility

Choose pairwise disjoint mode bands `Omega_j` and set

$$
H_{\rm opt}=\bigoplus_{j\ge1}L^2(\Omega_j;\mathbb C^{d_j}).
$$

If the shell embeddings `iota_j` are uniformly bounded, the direct sum defines

$$
\iota=\bigoplus_{j\ge1}\iota_j.
$$

The attenuation

$$
(\widetilde V_tf)|_{\Omega_j}=t^jf|_{\Omega_j}
$$

is a contraction for every `0<t<1`, and satisfies

$$
\widetilde V_t\iota=\iota V_t.
$$

Finite-cutoff embeddings are the coordinate restrictions of `iota`, so cutoff bonding is strict. The infinite detector coupling exists when the source history coupler `C` extends continuously through `iota`; loss outcomes remain part of the normalized effect family.

Status: infinite mode-space compatibility reduced to uniform embedding bounds and a continuous detector-coupler extension.
