# Unruh–DeWitt one-mode source instrument

## Why this sector

The present Marici sectors stopped at effects and readouts. The Unruh–DeWitt
model supplies the missing physical interface explicitly: a two-level
apparatus, its preparation, a field, a switching interaction, unitary
evolution, and an energy-basis pointer.

The source interaction is

[
H_D(	au)=lambdachi(	au)mu_D(	au)otimesphi[x_D(	au)],
qquad
U=mathcal Texp!left(-iint H_I,dtight).
]

The source initializes the detector in its ground state and interprets its
excitation probability as the detector response. Source: Suryaatmadja, Cong,
and Mann, *Entanglement Harvesting of Inertially Moving Unruh–DeWitt
Detectors in Minkowski Spacetime*, arXiv:2205.14739, equations (1)–(4).

## Bounded specialization

To obtain an exact finite falsifier, retain one resonant field mode and the
single-excitation sector. Write (c=cos	heta), (s=sin	heta), and choose
the exact nontrivial point

[
c=rac35,qquad s=rac45.
]

With the detector prepared in (|gangle) and measured in
({|gangle,|eangle}), the source unitary determines

[
K_0=|0anglelangle0|+c|1anglelangle1|,
qquad
K_1=-is|0anglelangle1|.
]

This is a declared resonant one-mode specialization of the UDW coupling, not a
claim that the full continuum field is equivalent to a qubit.

## Exact results

The checker verifies

[
K_0^dagger K_0+K_1^dagger K_1=1,
]

so the two outcome maps are completely positive and their nonselective sum is
trace preserving. The click probability is

[
p_1=s^2ho_{11}.
]

The instrument descends under phase rephasing of the retained field mode:
(K_1) changes only by a scalar phase, which cancels in
(K_1ho K_1^dagger). Two sequential detector uses compose by Kraus-word
multiplication and remain trace preserving; a second click is forbidden in
the retained single-excitation sector. Forgetting the classical outcome is
exactly the sum of the two branches.

## Decisive comparison

The click effect is

[
E_1=K_1^dagger K_1=s^2|1anglelangle1|.
]

Its formal Lüders completion would use

[
L_1=sqrt{E_1}=s|1anglelangle1|.
]

Both give the same probability, but their successor states differ:

[
K_1ho K_1^dagger=s^2ho_{11}|0anglelangle0|,
qquad
L_1ho L_1^dagger=s^2ho_{11}|1anglelangle1|.
]

Thus

[
oxed{	ext{the public record does not determine the physical update}.}
]

The interaction does. In this source-derived realization the click transfers
the excitation into the detector and leaves the field mode in its vacuum;
the formal Lüders update incorrectly leaves the excitation in the field.

## Consequence for Marici

This is the first bounded physical realization of the capability-indexed
interaction surface:

[
	ext{Carrier/state}
+	ext{apparatus coupling}
longrightarrow
(	ext{classical record},	ext{successor state}).
]

It supports the conjectured separation between read-only observables and
state-changing capabilities. It does **not** yet establish that the UDW
instrument is generated by the shared Carrier, nor that the full continuum
instrument descends through all Marici transport. Those are the next tests.
