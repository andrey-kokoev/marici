# v75: derived normalization, duality, and dilation

Four unique reports were replayed successfully; the `(1)` derived-normalization
file is byte-identical to its unsuffixed copy. Assertion counts were 59,808,
18,087, 11,420, and 20,685.

`rzk/101-derived-normalization-duality-dilation.rzk.md` records that the complete
auxiliary normalization and conductor Tor towers cancel in the total conductor
kernel, with an explicit integral contraction. This does not give equivalence
with intermediate rows fixed: the row-filtered comparison has lambda-torsion
`I_occ` in degree `-1` and `B^2` in all nonnegative degrees.

The full normalization trace retains two grades with normalized matrix
`-beta [[1,1],[0,1]]`; scalar projection loses the occurrence-normal line. Both
reciprocal traces lift independently to full six-coordinate conductor support,
where an explicit relative dualizing residue distinguishes their relation-only
difference. Under separated two-branch dilation both traces remain supported at
the divisor intersection; the generic incidence vanishes where both dilation
parameters invert, and identifying the parameters is a nonflat specialization
with additional derived terms.

The combined Rzk module passes a fresh check. These are complete coefficient
comparisons, not yet a physical support-changing correspondence.
