# Candidate two should not inherit the full nine-constructor audit before G4 identifies its crossing domain

## Question

After Adams and seam--endpoint stability are reread as constructed, must
candidate two still audit every generator in the historical nine-operation
constructor domain?

## Claim boundary

Not without an exact G4 crossing declaration. Candidate two is a specific
Fourier--Poisson response-intertwining square. Its internal chain requires only
the maps that form its boundary trace, horizontal descent, sewing transition,
and contragredient response. Importing every historical constructor would be
silent scope expansion unless `conservative_green_complex` explicitly uses
those generators on this boundary domain.

## Independently verified stability cells

The repository already constructs:

### Adams source and analytic action

\[
 S_re_{p,k}=e_{p,rk},
 \qquad
 \mathcal IS_r=M_{\rho_r}U_r\mathcal I,
\]

with exact cocycle composition, projective continuity, cutoff naturality, and
Fourier-saturated contractivity.

### Seam transport

Moving-seam transport is unitary by reassembly conjugation and intertwines the
resolved history, wall, and tail maps.

### Endpoint attachment

Complete wall--jump endpoint pushforward is an exact object-indexed metric
natural transformation. Its Adams-weighted version has norm
\(|\rho_r(p,k)|\), preserves reciprocal grading, and is natural in all four
Fourier presentations.

### Response sewing

The horizontal four-presentation equalizer is closed stratum by stratum, and
the graph of the unitary Fourier--Poisson transition with contragredient dual
action is maximal isotropic.

## Historical nine-operation theorem

The general saturated-pro-Gram packet gives a sufficient theorem for an
arbitrary admitted constructor \(S\): prove an unsaturated bound and Fourier
naturality, then lift the bound to the four-presentation saturation.

That theorem does not assert that all nine historical generators occur in the
candidate-two square. It is a reusable criterion, not an automatically active
obligation set.

## Exact candidate-two interface

The square to be proved is

\[
 \mathcal O_{\partial,+}\operatorname{Tr}_+
 =W_{\rm FP}\mathcal O_{\partial,-}\operatorname{Tr}_-,
\]

with response transported by \(W_{\rm FP}^{-*}\). Its required interface data
are:

1. source and target trace domains;
2. retained response strata;
3. horizontal transition maps;
4. \(W_{\rm FP}\) and its line/metaplectic cocycle;
5. the maximal-isotropic boundary form.

Only constructors appearing in those declarations require a candidate-two
stability proof.

## Remaining authority issue

The SCC witness for `conservative_green_complex` does not expose this
interface. Therefore one cannot presently decide whether additional
constructors cross the response graph. Listing unspecified “non-Adams
constructors” as open would replace missing source identification with an
unbounded audit request.

## Revised candidate-two frontier

Within the retained boundary chain, the named analytic, topological, Adams,
seam, endpoint, and sewing cells are constructed. The remaining bounded task is
one source comparison:

- recover the canonical G4 conservative-complex declaration;
- compare its carrier, traces, transition, strata, and form with the retained
  chain;
- audit only the extra crossing maps actually found there.

## Disposition

Candidate two should not inherit the whole nine-operation audit by default.
Its unresolved status is concentrated at the missing canonical G4 interface
and any concrete mismatch revealed by that interface. Candidate one remains
independently open. No RH conclusion is authorized.
