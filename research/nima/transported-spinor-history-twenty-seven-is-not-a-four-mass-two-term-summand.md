# Transported-spinor history 27 is not a four-mass two-term summand

The source's n=9 left-nested candidate 27 has outer `(a₁,b₁)=(2,8)`, inner `(a₂,b₂)=(5,7)`, and transported chiral covector `ξ=⟨9|x₉₈x₈₂`. Its inner generalized-R fermionic delta contains the term `⟨ξ|x₂₅x₅₇|θ₇₂⟩`. On retained physical label pairs `X=(1,6)` and `Y=(2,7)` (local `(1,5),(2,6)`), let `A_p` and `B_p` be respectively the outer and inner per-flavor fermionic coefficients. The ratio of the **complete** outer×inner product is `(A₂B₇−A₇B₂)/(A₁B₆)`. Here `B₆,B₇` arise from `θ₇`, but `B₂` arises from **both** `θ₇₂` and `θ₅₂`, and `A₇` is NONZERO. A first shortcut omitted the crossed term `−A₇B₂`; it failed an all-label consistency check and was corrected. The rerun with the full crossed term still differs from both four-mass fibre ratios at each witness. The common bosonic generalized-R prefactor cancels.

The checker evaluates this **source-specific necessary pair ratio** via adjacent-momentum-twistor incidence for `x_i` and `θ_i`, with explicit two-spinor ε tensors for alternating chiral/dotted contractions. The convention is not accepted on faith: at EACH of two independent generic rational supertwistor datasets, (1) its transported `ξ` ratio for `χ₂/χ₁` agrees EXACTLY with the ordinary five-bracket `[9,1,2,7,8]`, and (2) the independently constructed `x₉₅x₅₇` ratio for `χ₇/χ₆` agrees EXACTLY with the ordinary five-bracket `[9,4,5,6,7]`. The candidate-27 ratio is invariant under a nontrivial upper-parabolic `GL(4)` change mixing λ into μ, and under replacement of deleted physical twistor 3.

At both datasets the calibrated history-27 pair ratio differs from **each** of the two source-fibre pair ratios of the complete starred four-mass ψ invariant. The earlier binary-quartic Hankel argument proves that any expression of this rank-two invariant as ONLY TWO flavor-blind products of fermionic δ⁴ factors must use exactly those two branch ratios. Thus history 27, like ordinary history 9, cannot be a summand in ANY two-history decomposition, irrespective of bosonic scalar coefficient. This does NOT rule out sums of three or more histories: their pair ratios need not coincide with the unique rank-two secant points when more terms are allowed. No full generalized-R bosonic prefactor, global form orientation or all-history amplitude equality is claimed.

Checker: `research/nima/checkers/check_nine_point_history27_transported_pair_ratio.py`; result: `research/nima/results/nine-point-history27-transported-pair-ratio.json`.
