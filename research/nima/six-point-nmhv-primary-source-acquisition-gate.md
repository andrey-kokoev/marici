# Six-point NMHV primary-source acquisition gate

## Question

Can the completed algebraic benchmark be promoted to an independent replication of the published six-point NMHV amplituhedron result?

## Verified locally

The local suite now verifies:

1. equality of the oriented boundaries of two three-simplex triangulation chains;
2. cancellation of their six internal facets and equality of their nine external facets;
3. an exact bosonic canonical-form identity for explicit positive moment-curve data;
4. the same bosonic identity generically on a projective chart;
5. the super five-bracket six-term identity at three positive rational probes;
6. the super identity generically in the chart with `Z1,...,Z4` a basis and `Z5=a`, `Z6=b`;
7. nonzero residuals under hostile orientation mutations;
8. exact exhaustion of the declared adjacent-pair momentum-twistor pole pattern by the nine external facets.

## Source acquisition result

Operator-authorized shell retrieval materialized both cited PDFs and their TeX archives with SHA-256 digests under `research/sources/nima/papers/six-point-nmhv/`.

The retrieved sources locate the tree-amplituhedron map `Y=C·Z`, the `k=1,m=4` five-coordinate cells, the triangulation pattern `(1,i,i+1,j,j+1)`, its R-invariant/BCFW interpretation, the existence of two three-term six-particle NMHV representations, and the explanation of Yangian identities as boundaries of positive-Grassmannian cells.

## Remaining source packet fields

The figure-encoded six BCFW terms still require a term-by-term map to the local five-bracket labels. The exact five-bracket Grassmann normalization, full-amplitude versus ratio-function convention, and factorization-versus-collinear classification also remain unlocated.

## Acceptance test

Decode the six source diagrams and compare every local convention and formula against the cited source. Record any global sign, MHV-prefactor, Grassmann-degree, projective-measure, or boundary-classification mismatch as a residual. Only a zero-residual comparison permits the phrase “independent replication.”

## Disposition

The external-source blocker is repaired. The benchmark is a source-supported partial replication, with the remaining label and normalization residuals recorded in `research/nima/six-point-nmhv-primary-source-audit.md`.
