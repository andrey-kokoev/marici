# The global Fourier action exists; the missing RH cell is the boundary observer lift

## Exact global action

On the additive rigging

$$
\mathcal S(\mathbb R)\subset L^2(\mathbb R)\subset\mathcal S'(\mathbb R),
$$

Fourier transport acts globally and unitarily. For the normalized Gaussian
dilation orbit,

$$
\mathcal F g_q=g_{-q},
$$

and the full lattice comb satisfies

$$
\mathcal F\Delta_{\mathbb Z}=\Delta_{\mathbb Z}.
$$

This is a source-derived, grading-noncentral, invertible action exchanging the
two scale sectors. It has no finite Euler antecedent, but it exists on the
completed additive carrier.

Therefore the global action itself must not remain listed as missing.

## The partial observer

The comb pairing gives the scalar theta readout

$$
A(q)=\langle\Delta_{\mathbb Z},g_q\rangle,
$$

and global naturality yields $A(q)=A(-q)$. This observer is natural but not
faithful for the complete theta/Tate boundary grammar. It retains none of the
separate primitive, square, connected, seam, or archimedean coordinates.

The missing map is the observer lift

$$
\mathcal J:A_{\mathrm{add}}\to B_{\mathrm{Tate}},
$$

where $B_{\mathrm{Tate}}$ is the full Fourier-saturated boundary pro-object.
It must satisfy

$$
\mathcal J\mathcal F_A=\mathcal F_B\mathcal J
$$

grade by grade.

This is a mixed action–observation square. It cannot be replaced by another
proof of Fourier reciprocity.

## Two 405 rows

The metaplectic prepared-orbit row and the complete theta/Tate row must be
kept separate.

For the additive metaplectic row:

- carrier compatibility is exact;
- the Fourier action is faithful on the global source representation;
- the comb observer is natural but not faithful;
- no RH-bearing uniform estimate follows.

For the full multiplicative boundary row:

- the boundary carrier and pro-Gram grades are typed;
- the induced boundary sewing action is not yet constructed;
- the complete observer lift is absent;
- uniform estimates are downstream.

The stronger profile of the additive row cannot be transported to the
multiplicative row without $\mathcal J$.

## Finite hostile model

A scalar invariant observer can satisfy naturality while killing the entire
odd sector. For the swap action on two coordinates, the row $(1,1)$ is
invariant and annihilates $(1,-1)$. Hence scalar modular equality coexists
with a nonzero invisible action mode.

Adding a full two-coordinate observer restores separation and determines the
boundary action. This illustrates the exact role of the missing graded lift.

## Next construction

Derive the first two grade maps directly from the additive source:

$$
\mathcal J_1:A_{\mathrm{add}}\to B_{k=1},
\qquad
\mathcal J_2:A_{\mathrm{add}}\to B_{k=2}.
$$

Then compute their typed residuals under Fourier–Poisson transport. The two
residuals must vanish separately. Cancellation after their scalar sum is a
falsifier, not evidence.

## Hostile universality

Fourier-fixed hostile Schwartz sources obey the same global metaplectic law
and may still have off-seam Mellin zeros. Therefore global action coherence
alone has no RH force. The lift must encode the actual prime/Fock source
grammar strongly enough to reject those hostiles before scalar projection.
