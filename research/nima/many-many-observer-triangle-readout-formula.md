# Many-many observer triangle readout formula

For each pair response, the Laplace-side equation gives

$$
\mathcal L_z(F_{nm})
=zR_{nm}(z)-\rho_{nm}(0)-\kappa_{nm}\partial_zR_{nm}(z).
$$

Therefore the direct prime-labelled readout must use the same three coordinates:

$$
B_{P\to B}(n,m)
=zR_{nm}(z)-\rho_{nm}(0)-\kappa_{nm}\partial_zR_{nm}(z),
$$

with the endpoint-flux convention included in the boundary coordinate.

The pair-to-boundary composite uses `R_nm` and its Laplace derivative, so the observer triangle commutes exactly whenever both routes use this formula.

This identifies the analytic content of the triangle: the pair-dependent operator coefficient becomes the derivative term in the spectral readout.

Status: triangle equality reduced to equality of one explicit Laplace-side formula; endpoint-flux convention remains to be matched.
