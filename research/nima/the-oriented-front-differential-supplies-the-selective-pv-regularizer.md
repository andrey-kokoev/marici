# The oriented front differential supplies the selective PV regularizer

## Correction

The source audit behind event 10160 stopped one theorem too early. The required odd moment-jet coherencer is already present in the two-front geometry.

With inner and outer profiles

\[
I=H-1,
\qquad
O=-H,
\]

their oriented difference is

\[
C=O-I=1-2H,
\]

and the source boundary differential is

\[
dC=2e^{-\pi u^2}\,du.
\]

This is not a fitted counterterm. It is the canonical incidence arrow from the odd front comparison to the positive interval density.

## Fourier exactness

On the nonzero-frequency subspace, the principal-value front has symbol \((i\xi)^{-1}\), while the oriented boundary differential has symbol \(i\xi\). Therefore

\[
(i\xi)(i\xi)^{-1}=1
\]

on the reduced support. The singular moment is removed before arithmetic aggregation.

The zero-frequency coordinate is not acted on by this inverse. It remains a separately typed archimedean boundary port. Hence the operation realizes exactly the selective identities sought in event 10160:

\[
T_{\mathrm{mom}}R_{\mathrm{front}}=0,
\qquad
T_{0}R_{\mathrm{front}}=T_{0},
\]

where \(T_0\) is the retained zero-frequency wall port.

Orientation is also retained rather than erased: reflection reverses \(C\), while the signed differential records the chosen direction through the positive Gaussian density.

## Arithmetic compatibility

For a prime \(p\) and valuation \(r\ge1\), primitive occupancy and valuation excess satisfy

\[
P_p(r)=1,
\qquad
Q_p(r)=r-1,
\qquad
P_p(r)+Q_p(r)=r.
\]

After the front differential, the two channels couple to one interval density with coefficient

\[
2(\log p)r e^{-\pi u^2}.
\]

Summing over prime divisors gives

\[
\sum_p v_p(n)\log p=\log n.
\]

Thus the source does not separately complete a primitive principal-value scalar series. It first converts the singular comparison lane into the oriented positive interval tower, and only then performs arithmetic aggregation.

## Effect on the Schur split

The eliminated auxiliary sector may now be typed as the nonzero-frequency front primitive together with ordinary zero-trace bulk. The retained sector contains:

- the zero-frequency wall coordinate;
- the oriented interval-density output;
- the endpoint packet.

The incidence condition needed by the Schur theorem is therefore source-authorized, not conjectural:

\[
T_{\mathrm{ret}}J_{\mathrm{elim}}=0
\]

after reduction by the front differential, while the oriented density remains a retained output.

## Remaining gates

This closes moment cancellation but not completion. The live obligations are:

1. prove the front differential and inverse-frequency port form a closed exact pair on one rigged core;
2. prove the zero-frequency splitting is continuous in that topology;
3. prove the positive interval tower has a cutoff-uniform lower bound away from the vacuum;
4. prove vacuum transversality for the doubled endpoint problem;
5. prove the zero-state bridge remains faithful after completion.

The finite hostile is now a nonzero vacuum state satisfying both endpoint conditions. The completion hostile is a sequence of nonvacuum states with unit source norm and vanishing full-interval energy.

## Verdict

The earliest missing constructor is no longer the odd moment-jet coherencer. The source already supplies it as the oriented front differential. The frontier advances to closed Fourier exactness, vacuum transversality, and completion-stable coercivity of the positive interval tower.
