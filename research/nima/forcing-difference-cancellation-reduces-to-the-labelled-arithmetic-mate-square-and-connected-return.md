# Forcing-difference cancellation reduces to the labelled arithmetic mate square and connected return

The reciprocal Green sign ledger leaves the odd forcing current

$$
F_+(w,z)-F_-(w,z).
$$

Its cancelling source channel is the typed boundary-Stokes/Wronskian linking polarization, not the positive resolved Gram.

Fresh state closes the entire analytic half-density leg. For the four-front packet

$$
b_p=U_{-2L}f_0-U_{2L}f_0-U_{-L}f_0+U_Lf_0,
\qquad L=\log p,
$$

the twisted traces satisfy

$$
M_-(b_p)=-m_p,
\qquad
M_+(b_p)=m_p,
$$

with

$$
m_p=2e^{1/(16\pi)}
\left(\sinh L-\sinh\frac L2\right).
$$

Hence the packet is purely odd and

$$
s_p^{(1/2)}=j_{1/2}(b_p)=\sqrt2m_p>0.
$$

Graph closedness, trace continuity, reciprocal reflection, prime diagonality, and cutoff naturality are already proved. They are not remaining gates.

Because `b_p` contains only the displacements `L` and `2L`, this strict cell can mate only the primitive and square Euler currents. Define

$$
\kappa_p^{(\le2)}
=\kappa_p^{(1)}+\kappa_p^{(2)}.
$$

The first remaining source square is the labelled equality that sends the same retained arithmetic generator simultaneously to

$$
b_p
$$

on the half-density window side and

$$
\kappa_p^{(\le2)}\Phi'
$$

on the theta/Wronskian side. If it commutes, its forced scalar is

$$
\lambda_{p,\le2}^{(1/2)}
=\frac{-\kappa_p^{(\le2)}}{2s_p^{(1/2)}}>0,
$$

and the oriented linking output is exactly

$$
\lambda_{p,\le2}^{(1/2)}s_p^{(1/2)}
=-\frac12\kappa_p^{(\le2)}.
$$

The connected grades cannot be inserted into this two-endpoint square. They require a separate source-authorized return

$$
\mathcal R_p^{(\ge3)}:
J_p^{(\ge3)}\longrightarrow
\text{retained odd Wronskian graph},
$$

with output `-kappa_p^(>=3)/2`, followed by the archimedean/seam mate. Therefore complete cancellation of `F_+-F_-` has the ledger:

1. primitive/square mate square — open arithmetic naturality;
2. connected `det_3` odd return — open;
3. seam/archimedean mate — open globally;
4. analytic twisted-history realization — closed.

Status: analytic half-density comparison removed from the open list; the first executable gate is the labelled primitive/square arithmetic mate square, not another Green integration.
