# The assembled observer has a complete constraint flag and an actual retention tetrahedron

## Deliverables and the two comparison contracts

The assembled old-plus-270-plus-original-plus-two-private-plus-sixteen-private-plus-matched **ideal aggregate** observer now has:

1. an explicit finite linear-constraint presentation of its full ideal-power filtration and the inherited filtration of its kernel over O_2;
2. six actual cumulative-retention comparisons, four typed strict face witnesses and a strict tetrahedral filler;
3. a separate source-level obstruction certificate for interpreting the four individual scalar recipes as interchangeable frames.

The last distinction is essential. A tetrahedron of actual retention restrictions exists. A tetrahedron of source-compatible recipe equivalences does not. The latter failure occurs already at its edges, not as a newly discovered degree-three obstruction.

The presentation is **nonminimal**. It specifies all generators, relation equations and inclusion maps. It does not assert a numerically minimized dimension for F0 or F1, and it never treats the ambient 3,591 readouts as independent. The certified top filtration dimension is 270.

## 1. Replace the abstract source ideal by finite data

The source is the declared square-free six-event packet. Its 473 relevant typed corners contain 63,360 paths with at most two retained marks and at least two events.

The recorder is evaluated independently on those paths. Rational null bases give 51,550 source-ideal generators. For each corner the verifier checks:

- the complete allowed path list;
- the recorder rank;
- zero recorder value of every proposed generator;
- independence via its distinct free path coordinate;
- equality of the number of generators to the recorder nullity.

There are no ideal components of event length zero or one. Source components with at least three retained marks are invisible to every assembled seed and remain invisible after source multiplication, which cannot remove retained marks. They are omitted only for that checked structural reason, not identified with zero in the authoritative source.

The source domain in the presentation is thus a justified quotient for this observer, not an unsupported source-reconstruction claim.

## 2. The actual evaluation and its relations

Let V be the free coordinate space on these 51,550 source-ideal generators. The exported sparse matrix

`G0: V -> K^3591`

evaluates every assembled contextual row, where K is the fixed actual analytical scalar field of the owning protocol.

All entries are supplied, using 619 shared coefficient expressions. The R and N window tests, calibration ratios and normalizers retain the semantics pinned by the assembly artifact. They are not replaced by independently sampled numerical parameters.

The observer is presented as

`O3 = V / Rel0`,

where Rel0 is the solution space of the **explicit finite row equations**

`G0*z=0`.

Thus equality of presented states is decided by the exported relation equations, not by treating V or the ambient readout space as free observer states. This is a linear-constraint presentation rather than a minimized basis presentation.

The independent verifier reconstructs the owning coefficient functionals and checks every one of the 51,550 evaluation columns.

## 3. Full filtration matrices

All admissible products of two homogeneous source-ideal basis elements are enumerated. There are 25,940 products; after removing repeated rational multiples, 24,550 generating columns remain. Their source-coordinate matrix is J1.

The verifier checks their product witnesses and exhausts all admissible basis products to establish completeness, not merely inclusion in I^2.

For I^3, event length forces each factor to be a two-event relation. All 630 products with at most two retained marks are exported as J2. An explicit matrix J21 satisfies

`J1*J21=J2`.

The flag is therefore concretely presented by

`F0 = V/ker(G0)`,

`F1 = im(G0*J1)`, with relation equations `G0*J1*c=0`,

`F2 = im(G0*J2)`, with relation equations `G0*J2*c=0`,

`F3 = 0`.

Its inclusion maps are J1 and J21 on the corresponding generator coordinates. These are finite matrices, not an instruction to later discover the source ideal or its powers.

The 270 two-feature cubic private pivots are independent. Every other top reading is checked to be the prescribed combination of them, using the actual interval-additivity and calibration identities. The 3,780 new-frame component checks establish

`dim(F2)=270`.

No generic symbolic rank argument is specialized without justification to the actual analytical calibration.

## 4. Full kernel presentation over O_2

Let P be the owning nine-coordinate observer projection and put

`H=P*G0`.

H is exported column by column. The actual old source representatives give a matrix L0 with

`H*L0=identity_9`.

Therefore a complete generating operator for the kernel is

`B0=identity-L0*H`.

The kernel is `im(G0*B0)`, with the explicit relation equations `G0*B0*c=0`.

For the inherited first level, all columns of HJ1 lie on the one-dimensional old I^2 image. The exporter supplies its coordinate row h1, index p, and a source lift L1 lying in I^2 such that

`H*J1=e_p*h1`, `H*L1=e_p`.

Define

`B1=J1-L1*h1`.

Then the complete inherited kernel flag is

`K0=im(G0*B0)`,

`K1=im(G0*B1)`,

`K2=im(G0*J2)`,

`K3=0`.

Each level has its corresponding explicit relation equations. The inclusions are represented by B1 and J21, since

`B0*B1=B1`, `B1*J21=J2`.

The proof uses the checked matrix identities and does not assume a source-bimodule splitting. L0 and L1 are auxiliary scalar-linear source lifts, not bimodule sections. These are inherited levels of the restriction kernel, not its intrinsic ideal powers.

### The unfiltered-lift trap is checked explicitly

The chosen L0 lift of the old line is not in F1. On the relevant degree-one four-event I^2 corner, private unit observations have rank 12, equal to the full source I^2 rank there. L0 has zero values on those private rows but old-line value one. It therefore cannot be a filtered lift.

The verifier detects that incompatibility. L1, not L0, is used in the inherited K1 construction. Neither is substituted for a face witness of an unrelated attachment complex.

## 5. Six actual cumulative-retention comparisons

The four retained protocols are explicitly:

- C0: core plus original detector;
- C1: C0 plus the two-private aggregate;
- C2: C1 plus the sixteen-private aggregate;
- C3: C2 plus the matched aggregate.

Their ambient presentations have 2,741, 2,763, 2,863 and 3,591 rows. Their O_1 and O_2 components are shared; only the finite O_3 component is enlarged. All four use the same source generators and filtration matrices above, with the relevant prefixes of G0.

For every i>j the comparison `p_ij:Ci->Cj` retains the first corresponding rows. All six sparse matrices are exported. They preserve source anchors, the actual source actions, and every filtration level.

The checker tests 309,300 source-generator comparison identities and 28,111 action-entry restriction conditions. No abstract permutation or freely chosen frame fixture is used.

## 6. Four faces and the filler

For each i<j<k,

`p_ji p_kj = p_ki`

as actual matrices. In filtered source bimodules viewed as complexes concentrated in degree zero, the four face witnesses are the zero degree-minus-one homotopies of these equal maps.

The alternating whiskered face boundary is zero, and the degree-minus-two tetrahedral filler is zero.

These zero witnesses express **strict equality of restriction composites**. They do not nullhomotope the source attachment, split the nonsplit restrictions, or make the old unfiltered lift filtration-preserving.

## 7. The individual-recipe interpretation is obstructed

A different question uses the vertices

`core+original`, `core+two-private`, `core+sixteen-private`, `core+matched`.

Those are not the cumulative retained towers above. On three explicit source-ideal elements invisible to the common core, the extra readings have matrix

```
original       1  0  0
two-private    0  0  0
sixteen        0  A  0
matched        B  C  D
```

A and D are nonzero by the fixed positive-window formulas. B and C need no nonvanishing assumption. The exact source witnesses and all four scalar definitions are exported.

This gives nine impossible directed source-compatible maps. For example, the matched-to-original direction is contradicted by

`h_original-(B/D)*h_matched`:

its entire incoming observer value is zero but its original-detector value is one. The other blocked directions have analogous explicit witnesses.

The only off-diagonal directed comparisons among these four recipes are the three restrictions to the redundant two-private/core view. Hence none of the six pairs supports a source-compatible equivalence, and all four face requests for an equivalence tetrahedron fail their edge typing.

This is the requested obstruction for the replacement-frame interpretation. It is not repaired by a higher filler. One must retain the richer common source/view or change the comparison contract explicitly.

## 8. Reproduction and artifacts

Rebuild the existing inputs first, then run:

```
uv run --with sympy python research/nima/checkers/complete_actual_observer_presentation.py
uv run --with sympy python research/nima/checkers/verify_actual_observer_completion.py
uv run --with sympy python research/nima/checkers/close_actual_retention_tetrahedron.py
```

Outputs:

- `research/nima/results/actual-observer-full-finite-presentation.json.gz`
- `research/nima/results/actual-observer-presentation-verification.json`
- `research/nima/results/actual-retention-tetrahedron-and-recipe-obstruction.json`
- `research/nima/results/actual-observer-completion-fresh-closure.json`

The first artifact contains the full finite generators and relation equations, not just dimensions or witness summaries. The compressed serialization is deterministic. The remaining outputs pin its digest and verify the distinct comparison contracts.

Scope remains the declared finite ideal-aggregate protocol. Raw-reading retention, optional vacuum acquisitions, rounded implementations and completed infinite-source domains are different contracts. Numerical minimization of F0/F1 is not asserted by this constraint presentation.
