# Many-many observer-vertex triangle gate

Refinement coherence is strict. The remaining triangle concerns the three analytic vertices:

$$
P_{\mathcal F}\to R_{\mathcal F}\to B_{\mathcal F}
$$

and the direct map `P_F -> B_F`. Its required equality is

$$
B_{R\to B}\,B_{P\to R}=B_{P\to B}
$$

on every source-labelled pair-response coordinate.

The pair equation, shell concatenation, and Laplace readout define the three maps. Equality requires the Laplace readout of the pair solution to equal the direct prime-labelled readout after the declared endpoint-flux subtraction.

Status: observer-vertex triangle isolated; its analytic equality is the next substantive gate.
