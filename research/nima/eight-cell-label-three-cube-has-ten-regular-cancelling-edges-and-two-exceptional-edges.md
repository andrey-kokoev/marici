# Eight-cell label-3 cube: ten regular cancelling edges, two exceptional edges

An exact target audit covers **all 12 shared source facets** of the eight strictly positive label-3 cells `(E,E_B,E_C,E_D,F,F_B,F_C,F_D)`. All paired complete boundary matrices and fermionic numerators coincide, and all calibrated source residue signs are opposite.

At positive rank-six moment-curve boundary controls:

* **Ten edges** have **rank-eight target Jacobians on both sides**, seven shared target tangent columns, and **complete nonlinear pushed simple-superpole cancellation** in **two independent transverse target directions** per edge. This includes all **four horizontal/vertical `w₂=0` edges** and the three regular square edges on each horizontal/vertical face.
* The two `t−u=0` edges **`E_B/E_D` and `F_B/F_D`** have **rank-seven target Jacobians on both sides**. Their source residues are opposite, but an ordinary pushed eight-form residue is **not defined by the regular rank-eight argument**. These two singular faces remain genuine gates.

The slope edges use the boundary-adapted normal `v=t−u` with tangent `u`; this avoids falsely treating old `t` and `u` as independent boundary coordinates. This is a **complete local cube-edge classification**, not a proof that the eight cells cover or triangulate the nine-point positive image. Other cell facets, inverse sheets, target multiplicities and an independent arbitrary-Y canonical form remain unresolved.

Checker: `research/nima/checkers/check_nine_point_label3_cube_complete_edge_rank_and_regular_pole_audit.py`; result: `research/nima/results/nine-point-label3-cube-complete-edge-rank-and-regular-pole-audit.json`.
