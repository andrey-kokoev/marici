# The Euler-to-theta arrow is a projection, not a completion rewrite

The correction at event 10248 can now be sharpened from source order.

The raw coefficient Mellin observer and the completed theta observer are not two
objects connected by a source-authorized completion map from the first to the
second. Global Fourier--Poisson sewing acts on the full restricted adelic
source before Euler factorization and prime-power seam sampling. Hence the
authorized direction is

\[
\text{full adelic source}
\longrightarrow
\text{Poisson-sewn theta source}
\longrightarrow
\text{Mellin boundary section}
\longrightarrow
\text{Euler diagnostics}.
\]

There is no reverse constructor from the raw Euler observer to the theta
observer. Indeed, prime-seam sampling has a kernel containing smooth
archimedean variations that remain visible to Poisson sewing. Thus an identity

\[
\ell_s^{\Theta}
=
\mathcal C_s\ell_s^{\mathrm{raw}}
+
\ell_s^{B}
\]

cannot be asserted on the full source unless the right side retains an
independent continuum coordinate large enough to detect that kernel. Calling
\(\mathcal C_s\) a completion operator would otherwise invent an inverse to a
nonfaithful projection.

This changes the interpretation of the logarithmic corona. The raw observer's
escaping seam mass is not something that must be transported or cancelled to
construct theta. It is a defect of a downstream diagnostic projection. The
completed theta Green form must be built upstream, and only then projected to

\[
J_{\mathrm{full}}
=
J_{\infty}+J_1+J_2+J_{\ge3}+J_{\mathrm{mixed}}.
\]

The correct comparison theorem is therefore not Euler-to-theta completion.
It is a source-level commutative diagram:

\[
\begin{array}{ccc}
\mathcal S_{\mathrm{adelic}} & \xrightarrow{\mathcal P} &
\mathcal S_{\Theta}\\
\downarrow \mathcal G_{\mathrm{full}} && \downarrow \mathcal M_s\\
\mathcal B_{\mathrm{full}} & \xrightarrow{\mathrm{readout}} &
F_{\Theta}(s),
\end{array}
\]

followed by a nonfaithful Euler diagnostic projection
\(\Pi_{\mathrm E}\). On the Euler chamber, its scalar readout agrees with the
Fock/unique-factorization reconstruction, but scalar agreement does not
identify the upstream Green form.

The next executable theorem is:

> Construct the doubled Green boundary form on the undecomposed restricted
> adelic theta source, prove Poisson covariance there, and only afterward
> compute its archimedean, primitive, square, connected, and mixed
> projections.

This theorem must exhibit the external boundary carrier independently; it
cannot derive the continuum seam coordinate from prime samples.

The minimal hostile consists of two full source states \(u\) and \(u+b\) with

\[
\Pi_{\mathrm E}b=0,
\qquad
\mathcal P b\ne0.
\]

They have identical raw Euler diagnostics and therefore identical raw
log-corona data, but different completed theta Green flux. Any proposed
Euler-to-theta rewrite identifies them and is nonfaithful.

Consequently the earliest missing constructor has moved one level upstream:
it is the source-level doubled adelic Green correspondence compatible with
Poisson sewing, not a renormalization of the raw Mellin observer.
