# Many-many G4 identification generator test

The analytic pair-to-bordered crossing is already canonical. To identify the G4 forward map, it suffices to verify on a dense generating family of elementary tensors

$$
 f\otimes g\in\mathscr T_\theta\otimes\mathscr T_\theta
$$

that the four retained coordinates agree:

$$
U_{\rm G4}(f\otimes g)
=\bigl(\rho_{f,g}(0),E_{f,g},W_{f,g},R_{f,g}\bigr).
$$

Continuity then extends equality to the completed projective pair carrier. Contragredient uniqueness automatically identifies the return map. The G4 comparison must preserve the variance lane: analytic-transpose and Hermitian lanes are tested separately.

The finite test also checks reciprocal exchange and shell restriction on generators. Passing these tests supplies the forward source square needed for the conservative promotion target.

Status: G4 identification reduced to dense-generator coordinate equality.
