# The actual private core has source-compatible actions and an eight-dimensional coupled vacuum increment

## Result and exact protocol

The observer generated by the original two seeds and the 270 first-seam private seeds now has an explicit source-evaluation presentation and a verified restriction to the actual nine-dimensional O_2.

The export uses **2,720 ambient readout coordinates**. This is NOT an asserted observer dimension: the state space is the image of the actual source ideal under these rows, with its dependencies retained.

For this precisely specified core, adjoining the background-two vacuum seed has an exactly eight-dimensional restriction kernel. Its inherited filtration has dimensions `(8,6,1,0)`, while its own intrinsic ideal powers have dimensions `(8,2,0)`. The restriction is nonsplit.

The full historical stage-three protocol retains additional cubic frames/corrections. They are NOT silently identified with this core. The exact increment result below is for the core only; the full-union bounds in Voevodsky's acquisition note remain unchanged.

## 1. An evaluation image, not a guessed free state space

The source is the actual background-two packet with event primes `(2,3,5,7,11,13)`. Its ideal is the kernel of the ordered potential-word terminal recorder.

Each private seed selects one canonical marked path: flatten its three ordered pairs, put each pair's retained mark on its first event, and leave the second forgotten. Its vacuum buffers force precisely that path. Dividing the entire contextual family by its fixed positive analytical factor yields this exact source coefficient functional.

The original unit-vacuum and four-event residual seeds are retained. The latter uses the fixed symbolic ratio rho from the preceding O_2 export, not a sampled numerical calibration.

All nonzero contexts are prefixes and suffixes of a supported seed word. Enumerating and deduplicating the resulting functionals gives the 2,720 rows. Contexts with fewer than two remaining events vanish on the source ideal and are omitted.

The presentation defines

`E_core = image(epsilon_core:I -> Q(rho)^2720)`.

It does not replace E_core by the entire coordinate space. In particular the export makes no assumption that contextual readings are independent, acquired, or freely realizable.

Some contextual scalar factors are divided out when deduplicating rows. These are explicit invertible coordinate normalizations with transported actions, not new positive-gain numerical acquisition claims. The only parameter denominators involved come from the nonzero fixed rho.

## 2. Complete contextual source actions

Precomposing a row with a prime edge removes a matching first or last event from its source coefficient word. The remaining functional is another exported contextual row, with an explicit scalar factor.

The exporter checks 4,598 formal coefficient identities of this kind. These are equalities of source functionals, not tests on selected cubic witnesses.

It also checks 2,725 left/right context-commutation identities. Vertex idempotents project onto the labelled initial and terminal corners. Unlisted prime actions are zero. All path actions follow by composition.

Because epsilon_core intertwines source multiplication with these actions, its actual ideal image is stable. This constructs a source-bimodule presentation without requiring a potentially misleading choice of an independent ambient readout basis.

## 3. The actual transition to O_2

The nine O_2 coordinate functionals are retained contextual rows, up to their explicit scalar normalizations. They determine the exported restriction pi.

The checker verifies source-action compatibility of pi and uses all nine previously exported actual source representatives to prove its surjectivity. It also checks those representatives remain in the terminal-recorder kernel.

For this presentation the transition identities agree already as coefficient functions; no extra quotient relation was needed in the tested action equations. This stronger equality is checked, not inferred from the source representatives alone.

The old source lift is retained explicitly:

`pi((1-rho) epsilon_core(x))=epsilon_2(v2)`.

The checker also verifies

`P_y(epsilon_core(v2*c1))=1`,

`epsilon_core(x*c1)=0`,

`pi(epsilon_core(v2*c1))=0`.

The initial ideal corner is exactly two-dimensional and already fully observed by O_2. A hypothetical section would therefore have to lift the initial mixed diamond by its actual source evaluation. Acting by the remaining forgotten/mixed diamonds gives the displayed contradiction. Thus the core restriction to O_2 is nonsplit.

The chosen lift of the old graded line remains an unfiltered lift. It does not split this whole restriction and does not become a filtered face witness merely because its normalization is explicit.

## 4. Exact background-two vacuum increment for the core

The complete vacuum saturation has the fifteen canonical contiguous-subword coefficient functionals indexed by `(i,j)` with `j-i>=2`.

Exactly these seven are already core coordinates:

`(0,2), (1,3), (1,4), (2,4), (3,5), (3,6), (4,6)`.

At every other canonical interval corner, all core rows have positive retained degree. Consequently the core vanishes on the all-forgotten source sector there. No linear combination of these positive-degree rows can recover the missing vacuum functional on the source ideal, which is homogeneous in retained degree.

The missing intervals are

`(0,3), (0,4), (0,5), (0,6), (1,5), (1,6), (2,5), (2,6)`.

For each, the canonical forgotten subword minus its first-two-event swap is an actual ideal source. It evaluates to zero in the core and to one in the corresponding new vacuum coordinate. Different corners give independent directions.

Thus the old/vacuum intersection has dimension seven and the restriction kernel has dimension eight. The artifact includes these source representatives and the kernel's sparse prime actions.

This refines the general bound only for the declared core. Additional retained detector families can enlarge the intersection; their absence must not be hidden when reporting this exact number.

## 5. Inherited filtration is not the kernel's own ideal-power filtration

The kernel's inherited levels from the enlarged observer are

`D, D intersect I Eplus, D intersect I^2 Eplus, 0`.

Products of two forgotten relations realize all six missing intervals of length at least four. Three relations realize the single full interval. The source length bound gives the opposite inclusions, so these dimensions are

`(8,6,1,0)`.

By contrast, ideal action ON D itself can reach only the directions `(0,5)` and `(0,6)`. Left and right images agree and have dimension two; another ideal factor kills them. Its intrinsic ideal-power dimensions are therefore

`(8,2,0)`.

Confusing these filtrations would mis-type an acquisition comparison. The equality seen for the split background-three/four blocks cannot be transferred to this nonsplit kernel.

The acquisition is nonsplit by the already forced initial forgotten diamond: its forgotten cubic product is invisible in the core and detected by the new vacuum row. This is the actual-source mechanism in `../voevodsky/the-background-two-vacuum-acquisition-is-a-nonsplit-observer-enlargement.md`, now accompanied by an exact core-only increment presentation.

## 6. What remains before actual four-tower coherence

We now have source-labelled evaluation rows, complete contextual actions and the O_2 restriction for a concrete stage-three core. The former gap is no longer just a list of unnamed maps.

Two boundaries remain explicit:

1. The additional historical cubic frames, particularly aggregated or memory-block detectors, need their full source functionals compiled and compared with the core. Agreement on the 270 top-layer columns is insufficient to identify their saturated observers.
2. The actual four-tower comparisons and chosen face witnesses must be extracted from that completed protocol, with unfiltered lifts distinguished from filtered ones.

No ideal-equivariant map has been assigned to an unacquired scalar tail. No abstract tetrahedral fixture has been substituted for these actual objects.

## Verification

Generate/recheck the lower observer, then the core:

`uv run --with sympy python research/nima/checkers/export_actual_old_observer.py`

`uv run --with sympy python research/nima/checkers/export_actual_private_observer_core.py`

Artifact:

`research/nima/results/actual-private-observer-core.json`

The run checks 272 seeds, 2,720 ambient rows, 4,598 coefficient action identities, 2,725 commuting-context identities, and nine source witnesses for surjectivity. It also checks the exact core vacuum increment and its two distinct filtrations.

This is a symbolic exporter with source checks, not yet a separate portable verifier for arbitrary submitted JSON. Fixed analytical normalization, actual calibration identification and the owning source-category hypotheses remain explicit assumptions.
