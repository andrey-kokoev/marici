# v169: soft-D1 nearby-cycle candidate disposition

An independent Rees physical-extension certificate now narrows which geometric
construction can realize the synthetic Bockstein cells.

All 508 ordinary crossing comparisons send the conductor Tor1 generator to
zero, so no ordinary fibre-boundary map of that form can retain the required
excess class. A proper two-chart blowup with relative dualizing line does give
an equivalence `Rp_* O_Y(E) -> S`, repairing the scalar coefficient trace, but
it explicitly does not identify the logarithmic endpoint functor.

The same certificate records 59 coefficient-model checks that the selected
physical readout is a chain map. Thus neither the scalar trace nor chain-map
algebra is the remaining issue. The required construction is specifically a
logarithmic/nearby-cycle excess comparison retaining the conductor Tor1 class.

Evidence is `results/soft-D1-nearby-cycle-candidate-disposition.json` from
`checkers/check_soft_D1_nearby_cycle_candidate_disposition.py`.
`rzk/197-soft-d1-nearby-cycle-candidate-disposition.rzk.md` passes all eight
declarations without assumptions. The source certificate itself states that
its tests are not proof-assistant verification.
