# v207: normalization comparison is a mixed-variance kernel, not a ring map

The failed generatorwise specialization in v206 is not the correct categorical gate. Ledger entries 434, 435, 544, and 624 construct the required left leg as a mixed-variance constructible kernel in the fs/Kato trivial-inertia sector.

At each of the 215 loaded PC stalks it uses

`K_L = [B_L[z_+] plus B_L[z_-] --(epsilon_+ - epsilon_-)--> B_L]`.

The primitive conductor-difference row commutes with all 522 face localizations and all 840 two-step routes, and its three-chart Cech twist vanishes. After tensoring with the logarithmic relative-dualizing and external Cartier packets, normalized blowdown and Cech descent give

`Phi(M) = Rq_!(K(M) tensor^L omega_q^log tensor^L C_Cartier)`.

This is deliberately not induced by assignments between radial-occurrence, Rees, branch, and soft-energy generators.

On the distinguished normalization-sheet object, the audited frozen signature is:

- generic Q roof `+1`;
- generic Rees factor `x_D`;
- closed Cartier residue `+1`;
- endpoint matrix `[[0,1],[1,0]]`;
- Tor-one orientation `+1`;
- Cech residual, Cartier commutator, reflection defect, and Jordan associator all zero.

The uniqueness audit identifies this image with the unique global framed filtered connector. Therefore module 226's request for a labelled coefficient-ring map is superseded: branch-to-endpoint transport is supplied by the mixed-variance kernel and its unimodular endpoint swap.

This closes the spatial comparison only in the fs/Kato sector. The remaining physical gate is the derived pullback/butterfly along the fixed road inclusion, proving that the selected physical Gysin class lands on `a*s+b*W+c*v`. Once that class equality is available, v205 and v206 give the pointwise residue `-beta*(a+(b+c))` without another Jacobian.
