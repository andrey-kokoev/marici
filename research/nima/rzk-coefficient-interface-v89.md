# v89: comparison-fibre supported amplitude

`rzk/117-comparison-fibre-amplitude.rzk.md` specializes supported-trace
evaluation to coherent coefficient/physical comparison fillings.

The amplitude is the supported trace of the physical coordinate. It computes
judgmentally to that trace and is therefore independent of both the chosen
coherence proof and the coefficient lift whenever the physical coordinate is
fixed. This isolates the intended variance: coefficient data establishes that
a physical point lies in the comparison fibre, while the amplitude reads the
physical supported class.

A fresh Rzk check passes all 11 declarations. The symbol closure contains one
file and no `#assume` declarations.

The remaining concrete gate is to supply an inhabited physical comparison
fibre and identify its supported trace with the existing occurrence/conductor
residue channel.
