# WITHDRAWN — no canonical incidence filtration was constructed

> **Status correction.** The proposed (15\subset25\subset26) filtration was
> computed from images of chosen quotient representatives.  Since the
> branch-to-union formulas fail to send 852 of 5,780 tested exact relations to
> zero, those images are not cohomologically defined.  The rank arithmetic is
> not an admissible filtration theorem.

The two branch inclusions and their intersection define a filtration without
choosing any vector-space splitting:

\[
F_0=V_{23}\cap V_{31},
\qquad
F_1=V_{23}+V_{31},
\qquad
F_2=V_{\cup}.
\]

The established ranks are

\[
\dim F_0=15,
\qquad
\dim F_1=25,
\qquad
\dim F_2=26.
\]

Each branch contributes five directions beyond the common core, and the union
contributes the marked-crossing Kato line beyond their sum.  Hence the
associated graded has the source-labelled form

\[
\boxed{
\operatorname{gr}V_{\cup}
\simeq_{\rm ranks}
15_{\rm common}
\oplus5_{23}
\oplus5_{31}
\oplus1_{23\cap31}.
}
\]

The notation records graded ranks only; no canonical direct-sum splitting is
asserted.  The final line is generated locally by

\[
d\log q_{\mathfrak g_{23}}\wedge d\log q_{\mathfrak g_{31}}.
\]

This is the complete incidence anatomy of the physical five-mark residue.  It
is precisely the pattern predicted by a Kato/localization coefficient calculus:

* a common lower object;
* one boundary quotient for each alternative mark;
* one coherent double-boundary class when both marks are retained.

Thus the apparent rank anomaly was not a hidden coefficient sector.  It was
the failure to carry the marked-divisor filtration far enough to include its
double incidence.
