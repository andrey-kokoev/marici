# The ordered Fourier--Tate current must enter a boundary Evans promotion, not an ordinary seam Fredholm determinant

## Spectral-coordinate correction

The centered theta Laplace section is

\[
\tau(z)=\int_{\mathbb R}e^{-zq}\Phi(q)\,dq,
\]

and, after the declared completion normalization, it is the Xi section. The
bilateral history generator is

\[
A=\partial_q,
\qquad
\operatorname{spec}(A)=i\mathbb R.
\]

The critical seam is exactly `z in iR`. Hence the Xi divisor lies on the
essential spectrum of the history block.

For the theta-deleted paired pencil

\[
P(z)=
\begin{pmatrix}
A-z&-B\\
-B^\dagger&D_U(z)
\end{pmatrix},
\]

the incidence `B` is Hilbert--Schmidt. If `D_U` is an invertible arithmetic
complement, Schur elimination changes `A-z` only by a compact operator.
Compact incidence cannot remove zero from its essential spectrum. Therefore
`P(z)` is not an ordinary Fredholm family on the Xi seam in the unweighted
bilateral history carrier.

The proposed identification

\[
\det_F P(z)=c(z)\Xi(z)
\]

is consequently invalid there without a different weighted or scattering
construction.

## Exact boundary characteristic

The two stable histories are

\[
u_-(q;z)=
\int_{-\infty}^q e^{z(q-r)}\Phi(r)\,dr,
\]

\[
u_+(q;z)=
-\int_q^\infty e^{z(q-r)}\Phi(r)\,dr.
\]

Their seam mismatch is

\[
E_{\mathrm{hist}}(z)
=u_-(0;z)-u_+(0;z)
=\tau(z).
\]

Thus the boundary Evans mismatch already equals Xi, including every local
zero multiplicity. At a zero, the stable histories glue to a two-ended `H1`
state even though the parameter lies in continuous spectrum.

## Role of the ordered current

The scalar seam match is not yet the arithmetic Green boundary condition.
The ordered port retains the missing relative coordinate:

\[
S_{\mathrm{ord}}=V_+-V_-=-2H_{\mathrm{jump}}.
\]

Its Fourier image is the reciprocal-odd Hardy principal-value channel, and
its pairing is the integrated Green relative response. Therefore the proper
promotion problem is to show that the glued Evans history satisfies the
lower arithmetic equation

\[
B^\dagger u_z+D_U(z)x_z=0
\]

with the ordered endpoint/Hardy current retained. This is a boundary or
scattering comparison, not an ordinary full-line Fredholm determinant.

## Arithmetic complement warning

The strict prime-delay Cayley network supplies a passive, cutoff-natural
complement. It does not have the late-shell normalization required of the
full diagonal reciprocal response. It may serve as the zero-free complement
inside a boundary promotion, but it cannot be substituted for the complete
graded `D_U` merely because it is invertible.

## Revised frontier

The next theorem should construct a source-derived map from the Xi Evans seam
state into the transverse arithmetic-Green boundary relation and prove:

1. the lower arithmetic equation on the retained graph;
2. preservation of primitive, square, connected, and archimedean grades;
3. equality of the ordered Green current under the two stable-history charts;
4. cutoff-independent limiting-absorption or scattering closure;
5. no use of the identically zero canonical Stokes codiagonal as detector.

The Xi boundary characteristic itself is already exact. What remains is its
nontrivial arithmetic promotion, not another scalar determinant formula.
