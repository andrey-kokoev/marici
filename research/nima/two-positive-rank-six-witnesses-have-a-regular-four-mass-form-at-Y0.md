# Two positive rank-six witnesses have a regular four-mass form at Y0

The previously used target gauge `Y=[I2|B]` does not include the source plane `Y0=[0_(2x4)|I2]`. That chart obstruction does not by itself imply an intrinsic pole. Starting with each of two positive rational target/source pairs, construct an exact rational `G ∈ SL(6)` by choosing four columns in the target plane's kernel and two right-inverse columns, scaling one kernel column so `det G=1`. Then `Y G=Y0` and `Z'=Z G` retains rank six and every ordered six-bracket (hence positivity).

For EACH transformed target, solve the original paired-cell quadratic and retain both sheets. On each sheet the transformed source matrix maps to the same plane Y0, though its row representative has a sheet-dependent last-two-column gauge `H`. Use the correct local coordinates `U=H^-1 Y'_(first four)`; thus each source-to-U Jacobian uses `H^-1 dY'_(first four)`, rather than incorrectly assuming `H=I` on both source gauges. Both exact sheet Jacobians are nonzero and their full oriented trace is nonzero. Independently, the old B coefficient transforms by the ambient chart-change determinant `det(A)^4 det(G_(last four,first four))^2`, with `A=Y_(first two)` at the old representative. This equality holds on each sheet and on their complete trace for both targets.

**Boundary:** This establishes two bona fide rank-six positive-data REGULAR witnesses at the source plane, and audits the gauge-normalized eight-form orientation. It does not establish regularity for generic external data or for the source's rank-four-body NILPOTENT bosonization. In particular it is not permission to substitute `Z=(z,φη)` into a fixed positive-data two-kernel algorithm. The global rational target numerator, nilpotent pullback, and Berezin component/residue comparison remain open.

Checker: `research/nima/checkers/check_four_mass_rank_six_Y0_regular_witness.py`; result: `research/nima/results/four-mass-rank-six-Y0-regular-witness.json`.
