# Source extraction types the constant-delta wall as an external port, not a dynamic state

## Decisive source evidence

The source artifact *The External Five-Cell Controller Closes Both Resolvent-Face Squares* explicitly freezes

\[
B=\langle1,\delta_0,K,V\rangle
\]

as a boundary representation in graded packets

\[
P_i=E_i\oplus B.
\]

It states that only the Fourier-fixed theta line enters the bulk and that the four coordinates of \(B\) remain external boundary data. The resolvent faces act as identity transport on \(B\); no constant or delta distribution is inserted into a bulk graph domain.

Therefore the current source authority selects the boundary-port branch of the wall trichotomy.

## Consequence for canonicalization

The constant-delta pair may contribute:

- boundary flux;
- endpoint storage;
- Green-form feedthrough;
- reciprocal orientation;
- trace observability.

It does not currently contribute an independent dynamic block to the bulk spectral generator \(A_1\).

Hence the proposed four-dimensional dynamic state

\[
(G_+,G_-,w_{\mathrm{const}},w_\delta)
\]

is a useful hostile/candidate model but is not source-authorized by the existing five-cell controller.

Most importantly, external wall energy cannot by itself remove the real reciprocal eigenpair from the bulk spectral symbol. It may close the total Green inequality without producing a positive bulk canonical generator.

## Revised branch status

The present source architecture is:

\[
\text{indefinite reciprocal bulk}
\quad\oplus\quad
\text{external Fourier boundary controller}.
\]

Three possibilities remain:

1. Boundary Green theorem: the combined bulk-plus-port Lagrange identity is positive on the zero-trace module even though the bulk is not a de Branges canonical system.
2. Coisotropic reduction: the external trace constraint removes the real bulk modes and leaves a positive canonical quotient.
3. Krein obstruction: the real modes survive and only indefinite-system machinery remains valid.

A dynamic four-dimensional canonical enlargement would require a new source theorem retyping some boundary coordinates as states. It cannot be inferred from Fourier closure.

## New earliest calculation

The source itself identifies the next theorem: combine the two face Green forms and the homotopy Green form with the external boundary representation, then test the anti-diagonal completed zero trace.

Let

\[
\mathfrak G_{\mathrm{tot}}
=
\mathfrak G_{+}
+
\mathfrak G_{0}
+
\mathfrak G_{-}
+
\mathfrak F_{B}.
\]

The required finite statement is:

\[
\operatorname{Tr}_{\mathrm{anti}}Y=0
\quad\Longrightarrow\quad
\mathfrak G_{\mathrm{tot}}(Y)
=
2\operatorname{Re}(z)\,\mathcal E_{\mathrm{rel}}(Y),
\]

with \(\mathcal E_{\mathrm{rel}}\ge0\) and radical controlled on the source module, subject to the frozen centered convention.

If this identity holds, off-seam exclusion may proceed through a boundary-bearing Green system without first forcing a positive canonical bulk Hamiltonian.

## Correction to the frontier

The earliest missing constructor is no longer a \(4\times4\) dynamic wall Hamiltonian. It is the complete three-sector Green identity with external five-cell flux and anti-diagonal trace.

Only its resulting reduced form can decide whether a positive canonical quotient exists. This returns the programme to the source-defined architecture rather than continuing along an invented dynamic-wall branch.
