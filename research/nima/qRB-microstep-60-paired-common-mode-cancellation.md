# qRB microstep 60: paired common-mode cancellation

The failed absolute-summability test identifies the needed structure. For prime and prime′ families, write the paired local packet as

$$
Y_p^{+}-Y_p^{-}
=
\bigl(p^{-1/2}\phi+r_p^{+}\bigr)
-
\bigl(p^{-1/2}\phi+r_p^{-}\bigr).
$$

The common translated mode cancels exactly if the two half-density coefficients and source placements agree. The remaining relative packet is

$$
\delta_p=r_p^{+}-r_p^{-},
$$

which is the object to estimate in the wall or graph norm.

The required proof has two independent parts:

1. exact equality of the common modes;
2. summability or bounded synthesis of the coupled residuals `delta_p`.

Neither follows from positivity of either family separately.

Status: paired cancellation mechanism isolated; equality of the actual prime/prime′ source common modes remains to be checked.
