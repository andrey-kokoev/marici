# v82: collar control audit and matching Hom

The Branch A matching-Hom checker independently passed 29,530 assertions. The
collar-control audit has a supplied certificate but no standalone audit checker
in the repository, so its new calculations could not be independently replayed.

`rzk/108-collar-control-and-matching-hom.rzk.md` records the audit's native
bar-cochain operations, 56 canonical seed images per ideal endpoint, forward
quotient test, and abstract graded/dihedral control structure. It also records
the decisive data gap: the requested physical control complex cannot be
instantiated without complete Branch C framed endpoint matrices, Branch A's
marked target models, and cross-branch comparison maps.

For the available Branch A coefficient problem, the degree-zero Hom from bare
seven-state Q to the nonsplit dualizing object is computed, and every such map
lifts to D35 because its conormal obstruction already vanishes on Q. The derived
sheet targets have unbounded conductor Ext. Neither the six-state enlargement
nor the ordered mixed cofiber is equivalent to D35, as their low-degree probe
Hom groups differ.

The combined Rzk module passes a fresh check. The known `(1,1)` class therefore
has no computed image in a physical matching-fibre H1, and no physical
existence/nonexistence conclusion follows yet.
