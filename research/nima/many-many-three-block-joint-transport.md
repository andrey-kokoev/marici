# Many-many three-block joint transport

Use the typed carrier

$$
\mathscr X_{3}
=
\mathscr H_{\rm FP}
\oplus
(\mathscr H_{\rm link}^{+}\oplus\mathscr H_{\rm link}^{-})
\oplus
\mathcal L_{\rm ac},
$$

where `H_FP` carries the stratified Fourier–Poisson response transport, the doubled linking space carries the completed polarized metric, and `L_ac` is the archimedean/anomaly-normalized determinant line.

The joint reciprocal transport is typed sectorwise. On `H_FP` it is `T_FP`. On the linking output pair it is the source reciprocal slot exchange `S_link`, together with the transported orbit action in each slot. On `L_ac` it is the line isomorphism `chi_ac(s)`; its endpoint presentation uses

$$
R_\infty(s)=
\begin{pmatrix}0&\gamma_\infty(s)^{-1}\\
\gamma_\infty(s)&0\end{pmatrix}.
$$

Here `chi_ac` combines the Tate transition with the anomaly-normalized determinant character. No global square-root frame is required.

The Hilbert-sector form is

$$
G_3^{\rm Hilb}
=G_{\rm FP}\oplus
\begin{pmatrix}0&K_{\rm link}\\-K_{\rm link}&0\end{pmatrix},
\qquad
K_{\rm link}=-\frac12J_{\rm link}.
$$

The determinant line is transported coherently rather than inserted as a scalar Hilbert metric. The valid sectorwise laws are

$$
\mathbb T_{\rm FP}^*G_{\rm FP}\mathbb T_{\rm FP}=G_{\rm FP},
$$

$$
S_{\rm link}^*G_{\rm link}S_{\rm link}=-G_{\rm link},
$$

and

$$
\mathbb U_{3,{\rm line}}:\mathcal L_{\rm ac}(s)\simeq\mathcal L_{\rm ac}(1-s).
$$

A single scalar matrix equation across all three sectors would incorrectly trivialize the determinant line.

Status: typed three-block transport assembled; G4 identification and relative-trace determinant comparison remain open.
