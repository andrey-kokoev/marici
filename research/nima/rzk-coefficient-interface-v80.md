# v80: physical endpoint pullback candidate

Independent replay passed 824,081 checks over 24,891 complete Hom columns and
eight endpoint variants.

`rzk/106-physical-endpoint-pullback-candidate.rzk.md` records the typed
endpoint-facing Gysin candidate and its obstruction theory. The unchanged
bare-Q problem has a contractible lift space once the generic class is fixed.
Three-normal endpoint duality remains acyclic. Retaining the endpoint conormal
frame creates a primitive endpoint class but removes the degree-four generic
class from that source frame.

The line-retaining counit exists, but Euler evaluation gives a nullhomotopic
source comparison. Prescribing primitive endpoint differences independently
then obstructs the same-target pullback by `(1,1)` in the two endpoint lines.
Each endpoint carries nine independent nonzero mixed-operation actions. Hence a
unit-preserving exterior-factorized augmentation-only source action cannot
intertwine them. The reverse endpoint-to-scalar augmentation is compatible, so
these facts alone do not prohibit every possible collar.

The Rzk module passes a fresh check. This is a tested candidate and obstruction,
not an identification with the independently prescribed physical collar.
