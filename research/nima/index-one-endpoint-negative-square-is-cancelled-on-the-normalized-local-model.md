# Index-one endpoint negative square is cancelled on the normalized local model

The odd endpoint block has rank-one negative contribution

$$
c_Lv_Lv_L^*.
$$

The normalized shifted-Laplacian Poisson vector `d_L` satisfies

$$
C_L^{1/2}d_L=\sqrt{c_L}\,v_L
$$

and

$$
\|d_L\|^2
=\frac{\sinh L-L}{2\sinh^2(L/2)}<1
$$

for every finite `L>0`. By the Douglas lemma,

$$
C_L-c_Lv_Lv_L^*\succeq0.
$$

Thus the local generalized Nevanlinna kernel compresses from index one to index zero:

$$
N_1\longrightarrow N_0.
$$

The successor maps carry `d_L` coherently between normalized harmonic endpoint lines. On an unbounded labelled completion, the leverage tends to one but never attains it; the completed residual remains nonnegative and injective, though noncoercive.

This closes the local endpoint positivity problem under the shifted-Laplacian boundary normalization. It does not yet identify the local bulk operator `C_L` with the full arithmetic Euler conservative block on the pair-to-Euler joint graph. That identification is exactly the remaining boundary-Weyl/pair-to-Euler source comparison.

Status: normalized endpoint negative square cancelled locally and injectively at completion; full arithmetic-carrier identification and forcing balance remain open.
