# v112: soft D1 quartic-tail gate

The minus-quartic-restoration and plus-defect calculations were freshly replayed.
The minus residue `2 a^7` is exact and creates no new boundary class. On the
plus side, multiplication by `a^2` does not preserve the exact image and has a
stable rank-one defect across cutoffs 12,16,20,24,28. A Smith module over
`A_+` remains undefined. See `rzk/140-soft-d1-quartic-tail-gate.rzk.md`, which
passes all six declarations without assumptions.
