# v54: normalization endpoint extension and Stacks enrichment

`rzk/71-normalization-endpoint-extension.rzk.md` records the normalization
pullback sources with twelve and fourteen channels. The fourteen-channel source
maps to the full 215-state target; the twelve-channel source maps after endpoint
quotient. The two added labels are the positive and negative endpoint triple
poles.

The endpoint-forgetting extension is nonsplit. Its annihilator consists of both
occurrence tails and the nine products of one positive with one negative Rees
normal. Every such normal-product multiple has an endpoint-preserving lift, but
the two ordered first-pole detectors retain primitive nonzero values. The source
is explicitly classified as target-residue-selected, not as the native physical
source.

Independent certificate replay passed 120,909 assertions. A fresh 73-file Rzk
closure passed in 26.72 seconds; see
`results/71-normalization-endpoint-extension.typecheck.json`.

## Stacks Project enrichment located with `stacks:search`

- tags `02FQ` and `0B8R`: additive torsors, their `H^1` classes, and the Cech
  description for a specified trivializing cover;
- tags `062F`, `0913`, and `0G6L`: regular implies Koszul-regular, extended
  alternating Cech complexes as Koszul colimits, and their vanishing pattern;
- tag `0954`: local-cohomology/Ext comparison;
- tags `0633` and `0690`: affine conormal modules and conormal exactness for
  regular immersions;
- tag `0A71`: derived `RHom`/Ext;
- tag `06YD`: bounded-above flat complexes are K-flat.

These results enrich the interpretation of the explicit Cech, local-cohomology,
conormal-determinant, and finite-free derived-Hom calculations. They do not
supply physical provenance, spatial endpoint connector cells, reflection
parity, or `Delta_J`.
