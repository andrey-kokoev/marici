# Fixed dual-rung control authorizes strong primitive sewing

## Projective exponential test space

Let

[
mathcal A_{exp}
=
igcap_{delta>0}
ell^1(mathcal M,e^{deltaell}),
]

with seminorms

[
q_delta(x)=sum_m |x_m|e^{deltaell(m)}.
]

For prime labels, (ell(p)=log p). A coefficient row (a=(a_p)) belongs
to the weighted dual rung

[
E_{-delta}
=
left{
a:|a|_{-delta}
=
sup_p|a_p|p^{-delta}<infty
ight}.
]

If (a_p=O(p^r(log p)^m)), then for every (delta>r),

[
|a-P_X'a|_{-delta}
=
sup_{p>X}|a_p|p^{-delta}
longrightarrow0.
]

Thus every fixed finite-order row has norm-convergent prime truncations in a
slightly larger dual rung.

## Source-current audit

The current rows have the following order budgets:

[
a_p=1:
quad r=0,
]

[
a_p=(log p)p^{-1/2}:
quad r=-rac12,
]

[
a_p=(log p)p^{-1}:
quad r=-1,
]

[
a_p=p^{-1/2}sqrt{log p}:
quad r=-rac12.
]

Hence each has truncation convergence in every rung (E_{-delta}) with
(delta>r). In particular any positive (delta) controls all four rows.
The generic (ell^infty) hostile therefore does not apply to these specific
currents once the projective exponential rigging and its step inclusions are
authorized.

## Strong-dual theorem still needed

Norm convergence in one weighted rung implies strong-dual convergence only
after proving:

1. (E_{-delta}	omathcal A_{exp,eta}') is continuous;
2. bounded sets of (mathcal A_{exp}) are controlled by the corresponding
   weighted pairing;
3. the chosen countable projective presentation is regular enough that the
   strong dual is the authorized inductive union of these rungs.

This is automatic in some nuclear Köthe settings, but is not a consequence of
the coefficient estimate alone.

## Adams order transport

Let (A_2) be the grade-doubling Adams transport. If the output length is
(2ell), the test seminorm transforms by

[
e^{delta(2ell)}=e^{(2delta)ell}.
]

Consequently the transpose order budget must be recorded with the opposite
index change. Schematically,

[
A_2':E_{-delta}^{Q}longrightarrow E_{-2delta}^{P},
]

or the inverse relation, depending on the frozen direction of the Adams arrow.
The factor two is structural. It cannot be hidden inside an unspecified
continuity constant.

Arithmetic half-density factors improve the coefficient order, while endpoint
trace or sewing factors may worsen it. The complete map needs an explicit
budget function

[
ho_S(delta)
]

such that

[
S':E_{-delta}longrightarrow E_{-ho_S(delta)}
]

continuously.

## Fixed-rung completion criterion

For each source current (u), a completed sewing family (S_X'u_X) is
authorized if there exists one (delta), independent of cutoff, such that

[
sup_X|S_X'u_X|_{-delta}<infty
]

and

[
S_X'u_X
]

is Cauchy in (E_{-delta}). This yields a limit in a fixed dual step and,
through the continuous step inclusion, in the strong dual.

Merely finding (delta_X) separately for every cutoff proves only finite
existence.

## Sharp hostile

Choose rows (u_X) supported below (X), and sewing maps (S_X) whose outputs
have polynomial orders (r_X	oinfty). Every (S_X'u_X) lies in some
weighted dual rung, but no fixed (E_{-delta}) contains the family. The
finite maps and all finite prime labels exist; the inductive-limit image
escapes every bounded step, so strong-dual completion fails.

This is cell-frame loss expressed as runaway exponential order.

## Next source calculation

For wall subtraction, local Green propagation, reciprocal sewing, endpoint
attachment, and archimedean sewing, calculate:

[
ho_S(delta),
qquad
|S'|_{E_{-delta}	o E_{-ho_S(delta)}}.
]

Then compose the budget functions in constructor order. The first Adams edge
closes strongly exactly when the composite sends every source-current order
into one cutoff-independent finite rung.

This replaces the generic dual hostile with a source-specific order-budget
theorem.
