# Fourier saturation closes the wall-tail Green cross block in the canonical boundary topology

## Existing constructor

The source already defines the Fourier-saturated boundary Gram

\[
Q_{\mathrm{sat}}
=
\sum_{j=0}^{3}
(F^j)^{*}QF^j.
\]

Because

\[
F^4=I,
\]

this Gram satisfies the exact invariance law

\[
F^{*}Q_{\mathrm{sat}}F
=
Q_{\mathrm{sat}}.
\]

It is source-authorized: every summand is a Fourier transport of an already authorized observer. No arbitrary averaging group has been introduced.

## Cross-block consequence

Exact \(F\)-invariance implies exact \(F^2\)-invariance. Since

\[
F^2=+I
\]

on the constant–delta wall plane and

\[
F^2=-I
\]

on the centered-tail/principal-value plane, these planes are orthogonal in \(Q_{\mathrm{sat}}\).

Therefore

\[
C_{\mathrm{wt}}^{\mathrm{sat}}=0.
\]

The canonical saturated boundary topology has no wall-tail Green loading at all. Its joint boundary form is

\[
Q_{\mathrm{sat}}
=
Q_{\mathrm{wall}}
\oplus
Q_{\mathrm{tail}}.
\]

Thus the symmetry gate proposed at event 10169 is already closed for the canonical boundary topology.

## Scope

This conclusion concerns the source-generated saturated observer Gram. It does not claim that every unsaturated analytic Green form is wall-tail orthogonal.

To use the conclusion in the RH constructor, one must prove that the completed arithmetic operations act continuously on the saturated topology and that the eventual Green reduction uses this source topology, or a uniformly equivalent one respecting the parity splitting.

The programme should not silently switch back to an unsaturated metric after using saturation to authorize sewing.

## Remaining topology gates

The original saturation theorem left five questions. Recent work closes part of them:

- Fourier sewing is isometric by construction;
- translated half-density histories preserve exact frame constants;
- prime-labelled local histories are closed and cutoff-natural;
- the local incidence energy is uniformly equivalent on compact off-seam sets.

The live completion questions are now:

1. do all arithmetic constructor words preserve the saturated pro-Gram domain;
2. do cutoff bonding maps intertwine the saturated four-orbit packet;
3. is the completed feature map injective with a noncollapsing lower bound;
4. does the final scalar Riemann readout extend continuously from this packet.

## Terminal mixed cancellation

Even though the saturated Green form is block diagonal, the scalar readout can still combine wall and tail outputs:

\[
r_s(w,t)
=
r_{\mathrm{wall},s}(w)
+
r_{\mathrm{tail},s}(t).
\]

A nonzero pair can lie in \(\ker r_s\) through cancellation. This is the genuine terminal mixed margin and cannot be removed by Green orthogonality.

The correct order is therefore

\[
\text{Fourier-saturated orthogonal packet}
\longrightarrow
\text{constructor-stable completion}
\longrightarrow
\text{scalar readout angle}.
\]

## Hostiles

An unsaturated metric with a nonzero wall-tail block cannot be called canonical merely because it is equivalent at each finite cutoff.

A saturated packet may remain perfectly coercive while the scalar row approaches a wall-tail cancellation angle of zero.

Arithmetic transport may preserve each original seminorm separately but fail to preserve the finite Fourier orbit as one compatible family.

## Frontier

The wall-tail Green assembly obstruction is closed in the canonical saturated topology. The next theorem is:

> Prove that the arithmetic constructor semigroup and cutoff bonding maps preserve the Fourier-saturated pro-Gram completion with uniform lower observability.

After that, the only wall-tail interaction left is the terminal scalar mixed-cancellation gate.
