# Local boundary authority exists; physical activation remains pending

The candidate now incorporates the source-derived Bunch--Davies prescription

\[
p\mapsto p-i(\epsilon_1+\epsilon_2+3\epsilon_3).
\]

This is a source-authorized local boundary-value germ. Subsequent scaled
monodromy testing closes its local activation route: the punctured family is
trivial and generates no \(\mu_2\)-odd thimble. It is not a global contour and
does not activate the triple-incidence nearby line.

The contour route remains closed, but the internal coefficient programme is no
longer exhausted. The source normal, auxiliary \(K_p\), and minimal marked-wall
Čech carrier are now typed. That carrier is exact in the special degrees, so
its relative Bockstein is zero. Only a separately source-derived bulk/face
augmentation remains open.

Still missing:

- a source-derived bulk/face augmentation beyond the exact minimal Čech nerve;
- a lift \(dH_p=p\Xi_p\) and two-prime survival witness for \([\Xi_p]\);
- deck and wall/Čech compatibility;
- a source-normalized \(\mu_2\)-odd Betti/coefficient pairing;
- continued physical period covector;
- alternatively, independently sourced global contour coupling to remote
  singularities.

Artifact:

- `research/nima/checkers/check_cosmology_post_candidate_authority_status.py`
- `research/nima/results/cosmology_post_candidate_authority_status.json`
