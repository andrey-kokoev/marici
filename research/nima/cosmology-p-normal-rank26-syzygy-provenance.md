# Seven p-normal syzygy images now have replayable source provenance

At ambient degree eight, paired reduction produced seven independent syzygy
image candidates over both `F_32003` and `F_32009`. Their labelled pivot
coordinates and source-support counts agree across primes. Every emitted
source coefficient packet replays to an exact dependency among the 9,780
special relation rows.

The seven pivot labels and full sparse provenance are stored in bounded
receipts plus content-digested sidecars. Source coefficient counts are
`21,21,66,70,86,111,172`.

Family classification shows an interface obstruction:

- candidates 0 and 1 use only IBP and Cayley–Menger multiplication relations;
- candidates 2 through 6 use all five marked families
  `(g1,g2,g3,g23,g31)`.

None is already a source-local cell in the ordered principal three-wall cone
`(g1,g2,g3)`. A comparison must account for the `g23` and `g31` contributions
rather than deleting them. The prior normal-versus-tangent test still places
the entire seven-dimensional image in the p-tangent span. No tau map or period
is claimed.
