# v78: source-relative excess trace

Independent replay passed 423,648 checks.

`rzk/104-source-relative-excess-trace.rzk.md` records a corrected trace on the
entire supported source-target Hom complex. All eight complete supported maps
have primitive images in the bare seven-state generic quotient, with no
remaining raw-normal/excess target tensor. A naive partial exterior trace is
not a chain map; the existing occurrence-Koszul multiplication homotopy gives
the required correction.

After determinant signs, every output is the same 45-term complementary-face
matrix on 18 source columns, while the source Cartier and excess degrees remain
distinct. Both endpoint composites are exact. Two endpoint-supported terms
correct the 43-term unclosed map to the 45-term closed cap, and the pair/triple
endpoint coherences transport. The prescribed Hom complexes to the short and
endpoint supports are acyclic, so fixing the generic class yields a
contractible relative lift space.

The Rzk module passes a fresh check. This source-relative operation is not yet
identified with an independently prescribed physical collar source.
