# v204: ramified source collar has an explicit retraction

On the ramified base `x=h^2`, the qg2 strict-transform prism defines a collar inclusion at a fixed nonzero `h=epsilon` and specialization retraction `h -> 0`. On the endpoint/conductor relative-chain sector their composite is the identity: the retraction leaves the chain parameter and its endpoint labels unchanged.

This supplies the split datum required by module 163 within the ramified normalization source. Hence the odd branch-difference class cannot vanish under transport from the special wall fibre to the nonzero-h collar fibre. Composing its primitive detector with the retraction also gives the detector-preserving source readout of module 164.

The result does not solve the coefficient-Gysin-to-supported-physical stage of module 166. The retraction is defined on the ramified qg2 source collar, while the target endpoint/Q class W is defined in the K6 Rees complex. Identifying these two W sectors still requires the blocked labelled specialization from v200.
