# Amplitude–cosmology factorization: first interface theorem

## Question

What is the first source-typed interface on which scattering-amplitude and cosmological-wavefunction factorization can be compared with the shared Carrier calculus?

## Claim boundary

The frozen three-site source audit identifies the following chain:

\[
\text{graph coefficient}
\longrightarrow
\text{site-energy integral of a flat-space wavefunction}
\longrightarrow
\text{twisted-period integrand with divisors }q_g=0.
\]

The same source separately provides sequential residues on compatible connected-subgraph divisors and a labelled deletion/partial-fraction decomposition. Therefore the earliest common factorization object is not a numerical amplitude, master-space rank, or canonical-form label. It is the labelled incidence category whose objects are denominator divisors `q_g=0` and whose admitted compositions are source-compatible sequential residues.

A Carrier comparison requires a map

\[
F_{\rm div}:\mathcal D_{q}
\longrightarrow
\mathcal W_{C},
\]

from this divisor/residue category to declared Carrier walls and wall intersections. It must preserve graph labels, compatibility of connected subgraphs, residue order up to a proved coherence, and the source normalization relating the site-energy integral to the flat-space wavefunction.

No such map is present in the audited packets. Consequently neither a Carrier boundary nor a positive-geometry face can presently be identified with a physical factorization pole merely from matching incidence.

## Strongest falsification attempt

The two-site bubble audit supplies both physical factorization residues and a one-dimensional spurious Čech class supported on three parameter-space walls. Identifying them would give a candidate common boundary object. The audit rejects this identification:

- integrand facets are not the spurious parameter walls;
- no source-defined map sends physical residues to the spurious Čech class;
- no source-defined pair-overlap functional is available.

The residual is typed: the codomain and comparison map are absent, not merely uncomputed. Equal cohomological dimension cannot repair it.

## Conjecture and rivals

Conjecture: source-compatible residue incidence is the minimal common architecture for amplitude and cosmological-wavefunction factorization, and a valid Carrier realization factors through `F_div`.

Rivals are: direct identification by pole names; identification by equal master-space rank; promotion of a spurious Čech class to a physical factorization face; or use of canonical-form language without a source map. Each rival fails the typed-domain or source-normalization gate before any physical conclusion.

## Acceptance test

For one smallest connected graph, supply:

1. every labelled divisor `q_g` and compatible connected-subgraph pair;
2. exact single and sequential residues with normalization;
3. a declared Carrier wall for each divisor;
4. commuting residue/restriction squares, including both admissible orders;
5. one incompatible-pair deliberate failure;
6. a proof that any proposed canonical-form face map preserves the same labels and residues.

The first missing datum is the owner-derived finite divisor/residue table needed to instantiate this test, followed by `F_div` itself.

## Disposition

The programme has a typed starting interface and an exact blocker. Factorization is source-defined on the divisor/residue side but has not descended to Carrier walls, positive-geometry faces, or physical readouts. The next executable step is a low-point divisor/residue census; geometric realization remains coordinated with `marici.Benincasa`.

## Evidence

- `research/benincasa/three-site-keldysh-source-provenance-audit.md`
- `research/benincasa/bubble-residue-spurious-cech-typing.json`
