# Prior research splits the prolate–Tate/Sonin seam into global form convergence and positive endpoint coupling

## Question

What prior constructions and corrections identify the actual remaining seam obligations in the two-stage/co-contra/four-chart system?

This is a source audit, not a new proof or checker run. The principal status authority is `research/voevodsky/positive-coherence-authoritative-frontier-index.md`, read completely. That index explicitly uses correction/dependency links rather than commit chronology. Its later Sonin/endpoint entries revise the older broad seam-blocker language used in our last packet.

## Status correction

The earlier statement 'the physical prolate-to-Tate/Sonin seam is missing' is too broad. The recorded constructions separate:

- exact volume successor and finite-regulator placement transport;
- signed asymptotic comparison;
- a global source-derived Tate phase-energy boundary;
- fixed coercive-packet common-edge removal, subject to alignment inputs;
- a source-natural signed Sonin Green form;
- its closed absolute-connection graph realization;
- its reflected-contour identification with completed endpoint coupling.

The remaining gates are positive/global, not absence of every signed seam map.

## 1. Volume and placement already have a source law

`research/voevodsky/moving-prolate-placement-volume-splits-into-an-exact-source-cocycle-and-an-asymptotically-natural-boundary.md` derives

\[
\mathcal V_{L'}(h)-\mathcal V_L(h)
=2(L'-L)\tau_S(\lambda_S(h)).
\]

The ordered cutoff is transported exactly at finite regulator. Its relative boundary becomes stationary in the observer-localized trace-norm asymptotic quotient. Therefore the fourth chart must retain the pair (volume, relative boundary); collapsing them to one finite-part scalar erases a genuine affine successor coordinate.

This channel does not need to be reconstructed by another cube calculation.

## 2. Prolate-to-Tate: positive mass is additional information

The source residual R_alpha has two distinct forms:

\[
K_\alpha=R_\alpha^*R_\alpha,\qquad
C_\alpha=R_\alpha^*J_\alpha R_\alpha.
\]

Signed comparison identifies the target connection A through C_alpha. It does not determine K_alpha. The exact one-dimensional hostile in `prolate-to-tate-bulk-removal-is-exactly-the-absolute-gram-gate-and-is-not-closed-by-signed-sewing.md` has two legs (sqrt(1+d),sqrt(d)): signed value 1, positive norm squared 1+2d.

Thus the two covariance/polarity legs cannot be replaced by their signed difference. The positive target feature has Gram |A|, possibly augmented by independently retained endpoint/Sonin data.

### Fixed packets versus global completion

`common-widom-edge-removal-closes-the-absolute-gram-gate-on-every-coercive-finite-packet.md` constructs an exact common positive edge when the common leading edge is coercive and the centered difference remains bounded. Removing that edge yields the packet absolute value |D_alpha| and its limit |W_E|.

This is a real finite-packet construction. It does not license replacing |P_E A P_E| by P_E|A|P_E; compression and absolute value generally do not commute. Packet-natural assembly remains part of the global comparison, not a consequence of matrix square-root continuity.

The authoritative index's current global target is direct convergence of closed centered relative forms on the source-derived phase-energy space:

\[
q_\alpha^{\rm relative}\xrightarrow{\rm Mosco}q_{|\mathcal A_S|}.
\]

Required inputs include a regulator-independent common core, pointwise convergence, uniform graph bounds, a weak liminf theorem, recovery sequences, and endpoint/radical stability. The exact candidate family of physical relative forms must be defined before invoking this completion theorem.

### Excluded shortcut

`correction-the-tate-hankel-map-is-bounded-on-phase-energy-but-not-filtration-compact.md` proves the exact norm identity

\[
\|[P,M_\gamma]M_m\|_{HS}^2
=\int\kappa_\gamma(t)|m(t)|^2dt.
\]

Disjoint observer supports yield orthogonal images, so uniform finite-rank tail removal on the energy unit ball is generally false. Pointwise graph-core recovery remains available; it does not prove the Mosco lower bound.

Likewise exact signed alignment does not by itself permit a common positive edge: the two-dimensional hostile in `noncommuting-near-null-modes-are-the-exact-obstruction-to-global-common-edge-removal.md` violates D_+ <= G^T. Finite coercivity can repair a fixed packet without supplying a uniform global estimate.

## 3. Sonin is the complementary intersection

`correction-the-eigenvalue-one-atom-of-pqp-is-the-inside-inside-intersection-not-the-sonin-space.md` fixes a critical source-type error:

\[
H_{11}=\operatorname{ran}P\cap\operatorname{ran}Q,
\qquad H_{00}=\ker P\cap\ker Q.
\]

The eigenvalue-one atom of PQP is H_11. Standard Sonin is H_00, seen by the complementary contraction (I-P)(I-Q)(I-P). A near-one prolate cloud is neither automatically an exact intersection nor the Sonin sector. A complete packet must keep the inside and outside towers separately.

The older balanced-pair proposal was conditional on signed-null Sonin readout. It is not a license to delete or balance the actual endpoint-coupled Sonin contribution without its source form.

## 4. Signed Sonin and endpoint coupling are already constructed

The progression is explicit:

1. `the-euler-connection-restricts-to-a-source-natural-signed-sonin-green-form.md` restricts the gamma–prime connection to the source Sonin form core and records prime-successor naturality.
2. `the-sonin-green-form-is-closed-in-the-absolute-connection-graph-norm.md` realizes the signed form on the complete absolute-connection graph domain. This is not a theorem that the literal compressed operator is self-adjoint or positive.
3. `the-reflected-contour-green-identity-identifies-the-two-source-pullbacks-of-the-augmented-coupling.md` identifies spectral and graph boundary forms through one reflected differential, including endpoint evaluations at plus/minus i/2, under its admitted contour and graph-domain hypotheses.

The recorded augmented signed operator is

\[
\mathbb G_S=\begin{pmatrix}\mathcal A_S&B_S^*\\B_S&J_{end}\end{pmatrix}.
\]

Existence and source naturality are not the active missing arrows according to the frontier index. Positivity after the declared parity reduction is still open. This audit has not independently re-proved the analytic contour/domain premises.

## 5. Most concrete positive seam candidate

`prior-research-reduces-the-sonin-endpoint-gate-to-a-rank-one-leverage-bound-in-the-two-space-paired-metric.md` reduces the remaining parity block to

\[
C_k\succeq b_kb_k^*=c_kv_kv_k^*.
\]

For a declared positive C_k this requires both

\[
v_k\in\operatorname{ran}C_k^{1/2},\qquad
c_k\|C_k^{\dagger/2}v_k\|^2\le1.
\]

Equivalently construct a successor-compatible vector d_k with C_k^(1/2)d_k=sqrt(c_k)v_k and norm at most one. A small endpoint norm without the range condition is insufficient.

The odd endpoint vector is source-known: on the finite Paley–Wiener interval it is the odd combination of exp(x/2) and exp(-x/2), with the packet's normalization giving squared norm 2(sinh L-L). Its cross term is a Robin/Wronskian trace from Green's identity, not a fit parameter.

### Why the two directions matter analytically

`the-semilocal-sonin-weight-is-a-two-space-unitary-pairing-not-a-similarity-renormalization-of-the-reflected-multiplier.md` supplies distinct canonical and dual weighted spaces, paired through their common semilocal source. Inverse Euler factors on one side and conjugate Euler factors on the other preserve the pairing; differentiation retains the prime current.

This gives actual source content to the co/contra axis. A scalar weight acting by similarity on one multiplier space commutes with that multiplier and cannot repair its expansive norm. Finite-rank endpoint removal also cannot change its essential norm.

The recorded candidate construction is an intertwiner from the differentiated two-space connection into the shifted-Laplacian energy complex, with P=partial_x^2-1/4. Its Green boundary trace would represent the odd endpoint vector in the bulk range. A contractive source factorization could then establish the leverage bound. That intertwiner is not constructed in the inspected packets, and the resulting global inequality carries arithmetic positivity content rather than formal coherence alone.

## Consequence for the proposed window

The window should retain: affine volume, signed and absolute boundary forms, both cutoff towers, cycle coordinates, and the signed endpoint coupling. The signed chart/successor cells can use the recorded source maps. The two nonformal tests are now:

1. a packet-independent relative positive-form family with the required Mosco limit;
2. the successor-natural endpoint range factorization and leverage inequality in the genuine two-space metric.

These tests occupy different comparison layers. Solving a signed cube or choosing a metric pulled back from source coordinates does not solve either positive comparison. The source two-space pairing is a stronger clue than any earlier arbitrary scalar residual.

## Disposition

The search narrows and corrects the missing-seam diagnosis. Existing signed Sonin/endpoint construction should be reused, not reinvented. The next substantive work must address one of the two positive/global gates above. No new checker was run, no earlier -4/5 interpretation is inferred, and no positivity or completed determinant theorem is claimed by this audit.
