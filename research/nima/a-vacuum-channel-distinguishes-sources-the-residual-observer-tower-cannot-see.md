# A vacuum channel distinguishes sources the residual observer tower cannot see

## Result

There are two genuine sources in BOTH declared summable domains, within the SAME certified all-radius source budgets, whose observations agree at every stage of the specified residual observer tower and its admitted cubic frame corrections.

They are distinguished by one additional existing three-seam VACUUM record coordinate. It requires no tiny theta-amplitude division and no new arithmetic response operator.

This is an actual test of an invisible source direction, not another internal frame-consistency identity. It shows what the present observer selection fails to determine and supplies an independently acquired coordinate that resolves that particular ambiguity. It does not prove that the enlarged tower is faithful on all sources.

## 1. The blind direction is an actual source relation product

In the same initial six-event packet let a_0,b_0,c_0 be the forgotten diamond relations on (2,3),(5,7),(11,13), respectively. Put

`k=a_0 b_0 c_0`.

This is a nonzero element of I^3, consisting of eight distinct entirely forgotten paths of length six with coefficients +/-1. It has no retained features. In its finite corner I^4=0.

Its actual-letter weights are all Gamma=1, so exactly

`Q_R(k)=8*6!*R^6=5760 R^6`.

It is an admissible finite source in every actual-letter and common-path seminorm and in every fixed corner-height moment class. No raw arithmetic kernel is substituted for the terminal-record ideal.

## 2. Why the entire specified tower misses it

For every original detector ell_r and every source context p,q,

`ell_r(p k q)=0`.

If r<=2, the detector's outer event length is less than six, so no nonnegative-length contexts can fit k into its support.

For r>=3, a contributing detector begins at the same initial arithmetic vertex as k. Typing and acyclicity force a left context to be the vertex identity. Its first required retained seam is in the initial (2,3) diamond. Every path of k has both these events forgotten. A right context starts after the entire six-event packet and cannot create that missing retained seam. The selected feature/vacuum sectors remain distinct under the actual balancing operation, so the required retained slot is absent.

This proves invisibility under ALL source-action translates, not just the single positive witness test. Thus Obs_m(k)=0 for every m.

The same conclusion holds for the currently admitted cubic recalibrations, their private-coordinate additions and their source-action-saturated differences. Each is supported at length six and retained degree two. On k only zero-length contexts fit, and its retained degree is zero. This includes the larger cubic block-norming representative when it is extended by its declared homogeneous degree-two test. It does not assert invisibility under arbitrary future observer choices.

Corrected frame transitions and consistency observers cannot recover information annihilated by all these rows. Their discrepancy outputs remain zero on this common invisible perturbation.

## 3. Two sources inside the SAME worked certificate budgets

Use the explicitly realized source x from `arb-certifies-a-minimum-source-lift-and-an-all-depth-realizable-tail.md`, with its private normalized readings (1,1/2), base x_0=v_0+(1/2)v_x, and prescribed r>=4 tail. Let w=w_seam>0 and define

`x'=x+10^(-10) w k`.

Both lie in J_3 and in the actual-letter/common-path intersection. They have the same private normalized readings, the same saturated residual-tower states at every depth, and the same corrected-frame consistency results.

They also fit the SAME all-radius upper budgets of the worked certificate, not merely some larger unspecified domain. Here is the quantitative slack argument.

The freshly certified complete forcing bound is

`||1_[log 2,infinity)Phi||_H4^2<1/4`.

Thus the original tail estimate can be sharpened by a factor 2^(-(r-1)) on its r-th term. Writing

`S_R=sum_(r>=4) R^(2r)2^(-r^2)`,

we have

`Q_R(x)/w <=10^(-170)R^6+S_R/8`.

The additional source cost is 5760*10^(-10)R^6. For every R>=1,

`5760*10^(-10)R^6
 <=(7/8) R^8 2^(-16)
 <=(7/8)S_R`.

Consequently x' satisfies the old budget 10^(-170)R^6+S_R, and hence also its certified finite-sum/geometric majorant. In particular both sources satisfy Q_1<10^(-4)w. Their common-path membership follows from the same convergent tail plus a finite perturbation.

Source realizability and internal observer consistency therefore do not select between these two explanations.

## 4. A new observation that does distinguish them

Retain the EXISTING all-vacuum balanced D3 row whose seams are

`2->4`, `12->60`, `420->4620`,

all forgotten, and whose four coefficient buffers are vacuum. Evaluate each prescribed vacuum slot by its unit observation. Call this finite record functional chi.

The actual ordered derivative gives

`chi(D3(k))=1`.

This follows from D3(k)=D(a_0) tensor_bal D(b_0) tensor_bal D(c_0), with coefficient +1 on the three chosen first seams. The checker also constructs D3 directly from cut positions.

The old source x has zero value in this row. Its cubic components have retained degree two; its later tail components belong to I^4 or higher and are killed by D3. Hence

`chi(D3(x))=0`,

`chi(D3(x'))=10^(-10)w`.

This functional is the original unit-vacuum observation on an existing labelled record, not a freely postulated tensor-dual element. It does not use an even/residual feature port in place of a vacuum, nor adjoin a new root state.

## 5. A simple forward noise threshold

In the UNscaled labelled record l1 norm, the selected unit vacuum coordinate has functional norm one. If its acquired scalar error has magnitude at most epsilon, the two predictions give disjoint error balls whenever

`epsilon < (1/2)10^(-10)w`.

For example epsilon<=2.5*10^(-11)w leaves a strict separation. Any included graph/shape normalization must be converted with its actual fixed multiplier; a source norm is not silently used as a response error norm.

This coordinate has no retained arithmetic feature, so it requires neither a theta-amplitude normalization nor a prime-field truncation estimate. It is nevertheless additional LABELLED RECORD data. A protocol recording only the previous arithmetic feature fields does not gain it by calculation. It must be acquired or otherwise independently justified.

The statement supplies two distinguishable admissible predictions, not an assertion that a physical measurement has been performed or that this precision is available experimentally.

## 6. The observer tower must actually be enlarged

The new readout chi D3 kills J_4 and is continuous with finite support. It may be adjoined, together with its source-action saturation, at stage three and retained at all later stages. This gives a valid enlarged observer tower by the established construction.

It is NOT a frame change of the old tower: chi D3(k)!=0 while every old saturated row annihilates k. Its reverse descent through the old observation map fails the kernel criterion. The new information cannot be supplied by coherence or recalibration of the old observations alone.

Only this blind direction has been resolved. Other kernels may remain, and uniform all-depth norm bounds do not follow from one finite addition. In particular the theorem is not a claim that every pair of competing source explanations can be distinguished by the previously selected records.

## Interpretation

The old frames can agree perfectly and admit a summable source while leaving a real source ambiguity. The appropriate response is an observation with a different sensitivity, not another comparison among the same readings.

Here that observation is concrete and already available in the prescribed carrier. It separates the two sources while preserving the source, pairing and all previous results. It demonstrates a falsifiable difference between explanations; it does not turn consistency itself into truth.

## Verification

`uv run --with python-flint --with sympy python research/nima/checkers/check_invisible_source_probe.py`

The checker freshly reruns the worked source certificate, verifies the stronger complete-theta bound, constructs the actual eight-term forgotten product, checks the independent D3 row and lower derivatives, audits finite right contexts, and verifies the exact common-budget slack and separation threshold. The all-depth invisibility proof is the support/mark argument above, not extrapolation from those finite contexts.
