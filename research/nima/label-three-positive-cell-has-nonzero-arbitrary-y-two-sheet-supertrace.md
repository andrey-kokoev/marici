# Label-3-sensitive cell has a nonzero arbitrary-Y two-sheet supertrace

The positive eight-cell E with **physical column 2 zero** is exactly the four-pair source chart D after retaining physical labels `(1,3,4,5,6,7,8,9)` rather than `(1,2,4,5,6,7,8,9)`. Thus the independent `SL(6)` recentering construction gives an **exact arbitrary-Y two-sheet rational trace** for E: after deleting external/fermionic row 2, its fibre ideal, eight-parameter Jacobian and complete fermionic numerator equal the corresponding eight-point expressions **for every first-pivot target**. Its calibrated oriented source density is the opposite of the original zero-column A density. E is blind to `(Z₂,χ₂)` but genuinely sensitive to `(Z₃,χ₃)`.

The earlier local E `χ₃⁴χ₅⁴` component is **not merely a single-sheet artefact**. At **two exact positive rank-six moment-curve external targets**, both algebraic inverse sheets were reconstructed and their complete `χ₃⁴χ₅⁴` pushed coefficients were traced. The **full two-sheet traces are nonzero** at both targets. Since this trace is rational on the simple-fibre open set, the component is generically nonzero on a nonempty algebraic open.

A's arbitrary-Y trace is identically `χ₃`-independent. This establishes a concrete, exact **arbitrary-Y form-branch distinction between two positive source-cell candidates**, not just a boundary-normal comparison. It still does **not** determine whether E belongs to the global amplitude contour: additional positive cells or sheets could cancel its label-3 component, and no complete nine-point image canonical form is asserted.

Checker: `research/nima/checkers/check_nine_point_label3_cell_arbitrary_y_two_sheet_trace.py`; result: `research/nima/results/nine-point-label3-cell-arbitrary-y-two-sheet-trace.json`.
