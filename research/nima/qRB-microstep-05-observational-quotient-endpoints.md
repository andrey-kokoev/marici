# qRB microstep 05: observational quotient and endpoints

The correct infinite-dimensional faithfulness statement is not that one observer is faithful. It is

$$
\bigcap_{\omega\in\mathcal O}\ker\omega=\operatorname{Rad}_{\mathcal O},
$$

where the observer family contains all source-derived translates and endpoint functionals.

The finite stacked-observer checker passes: a jointly monic observer separates the coherencer. The endpoint checker also passes on finite graph rungs and their projective system.

Therefore the admitted construction is:

1. form the positive relative carrier;
2. quotient by the observational radical;
3. retain endpoint observations projectively;
4. only then discuss completion.

A bare uniform endpoint Hilbert bound is explicitly false, so endpoint evaluation must not be used as a norm-continuous functional on the unregulated spectral carrier.

Status: finite/projective quotient compatibility closed; all-translate density in the infinite carrier remains open.
