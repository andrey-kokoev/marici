# Correction: the linear Stokes/Wronskian descent is complete, and the live five-port gate is external G4 quadratic authority

## Closed linear comparison

The completed ordered linking row is already constructed. Its source-derived
coefficient is

\[
\lambda_p=rac{-\kappa_p}{2s_p}>0.
\]

The diagonal Stokes-to-Wronskian map is nuclear on the Hilbert rung,
continuous and regularizing on the projective source, cutoff diagonal, and has
a defined transpose. Thus completed linear polarized descent is not an open
map.

The rung-five primitive adds a source-derived endpoint component to the same
eligible port architecture; it does not repair a missing linear topology.

## Live quadratic cell

For source and target two-output cells

\[
G_S=
\begin{pmatrix}
a_S&r_S+i\ell_S\\
r_S-i\ell_S&d_S
\end{pmatrix},
\qquad
G_W=
\begin{pmatrix}
a_W&r_W+i\ell_W\\
r_W-i\ell_W&d_W
\end{pmatrix},
\]

and

\[
T_p=\operatorname{diag}(\alpha_p,\lambda_p),
\]

the required source identity is

\[
G_S=T_p^*G_WT_p.
\]

Equivalently,

\[
a_S=\alpha_p^2a_W,
\qquad
d_S=\lambda_p^2d_W,
\]

\[
r_S+i\ell_S
=\alpha_p\lambda_p(r_W+i\ell_W).
\]

The odd coefficient `lambda_p` is fixed. The external G4 interface does not
materialize the two matrices or the even coefficient `alpha_p`.

## Authority boundary

The retained four-port graph and its internal constructor action are
constructed. The current materialized G4 contract still uses a different
three-port/Hilbert interface and does not admit the retained rigged four-port
carrier as its conservative Green complex.

A proposed G4-v3 descriptor exists, but it explicitly leaves open:

- canonical arithmetic loading;
- external G4 sewing identification.

Those are owner-authority cells. They cannot be filled by redefining the G4
target from the retained source packet.

## Consequence for the attempted divisibility test

The final five-port adjoint cannot yet be assembled because its quadratic
linking metric and external response action are not exposed by the owning G4
surface. The rung-five primitive is an eligible new source column, but its
coefficient in the final adjoint is not authorized until the matrix identity
above is evaluated in the owner-supplied target cell.

## Disposition

The next operation is not another internal analytic construction. It is an
interface admission/evaluation task:

1. admit the retained four-port rigged carrier under a successor G4 contract;
2. expose `G_W` and `alpha_p` primewise;
3. run the exact three-equation polarized matrix test;
4. only then assemble the five-port adjoint and test Xi divisibility.