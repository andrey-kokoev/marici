# v170: local logarithmic nearby-cycle realization exists

A fresh audit of the logarithmic excess certificate changes the geometric
frontier: the needed kind of soft-D1 cell is already constructed locally.

The primitive relative log generator is `-r_D+r_1`; normal Tor maps to this log
link integrally and reflection reverses it. The full endpoint pairing and
generic trace-times-road maps are chain maps, and an explicit endpoint
comparison homotopy identifies all polynomial coefficient cycle pairings with
the road readout. The full log packet has an integral contraction away from its
primitive class while retaining both normal grades.

What remains is not discovery of a nearby-cycle model. It is comparison: map
the global L2 integral relation/Bockstein system into this log normal-link
packet, then promote the local coefficient comparison to one ringed filtered
correspondence into the literal Q target. The certificate explicitly does not
claim a nonzero derived Q morphism or full physical correspondence.

Evidence is `results/log-nearby-cycle-local-realization.json` from
`checkers/check_log_nearby_cycle_local_realization.py`.
`rzk/198-log-nearby-cycle-local-realization.rzk.md` passes all eight declarations
without assumptions; the source certificate is executable algebra, not a
proof-assistant verification.
