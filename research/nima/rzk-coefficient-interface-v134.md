# v134: W-survival transport split

Fresh extraction from the full comparison-fibre replay shows that W-survival is
already proved through the endpoint stage. The decomposable term is nonzero in
the native algebra, reflection acts nontrivially at the endpoint, the relative
endpoint class is retained in the comparison fibre, and integral detector and
orientation checks pass.

The replay scope simultaneously states that no physical collar/Verdier
identification or Branch A conormal map is supplied. Thus the nonvacuity gate
for odd reflection is reduced to one map-level statement: the endpoint-to-
physical conormal readout must not annihilate W. Native nonzero status alone is
not promoted across that absent map.

Evidence is `results/W-survival-transport-split.json` from
`checkers/check_W_survival_transport_split.py`.
`rzk/162-W-survival-physical-transport-split.rzk.md` passes all eight
declarations without assumptions.
