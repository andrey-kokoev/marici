# v208: the physical derived pullback is a unique primitive integral line

Ledger entry 436 and `check_physical_derived_pullback_after_transform.py` complete the first half of the module-228 gate. With the mixed-variance connector of v207 and the established road inclusion fixed, the unsplit conductor/road homotopy pullback has chain ranks

`1 -> 4 -> 5 -> 1`

and differential ranks `(1,3,1)`. Unit minors make every image lattice saturated. Its integral homology is

`H1 = Z`, with every other group zero and no torsion.

A primitive cycle is `z=(1,0,1,0,0)` in the rank-five degree-one term. Its road augmentation is `+1`. The independent physical checks give even loaded endpoint parity, reflection sign `(-1)*(-1)=+1`, generic Q coefficient `+1`, and Cartier edge residue `+1`; both ordinary-forgetting shadows vanish.

Thus the six-point positive-sheet physical Gysin source is no longer missing: it is the uniquely oriented primitive line generated by `z`. What remains from module 228 is narrower. One must compare the three selected supported detector functionals with this line and prove that their evaluations are the declared coordinates `(a,b,c)`, equivalently that the supported image is `a*s+b*W+c*v`. Entry 436 does not itself identify that three-line packet or a numerical amplitude.
