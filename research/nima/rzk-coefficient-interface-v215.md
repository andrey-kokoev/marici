# v215: five-direction normalized physical witness assembled

Module 237 combines the existing road/group-corrected five-direction lift with the primitive mixed-kernel pullback and the detector calculation. The witness retains all five vanishing directions through its `FiveLift` component, while its physical projection has supported image

`s + W + v`.

The pointwise packet law then gives

`residue = -beta * (1 + (1 + 1))`.

Thus the normalized positive-sheet six-point case is now packaged as one five-direction physical amplitude witness rather than as separate lift, Gysin, and scalar statements. The constructor requires the already established lift, image equality, and residue equality as explicitly typed inputs; it does not infer them from scalar signatures.

The remaining scope boundary is the independent symbolic `(a,b,c)` family. The concrete witness realizes `(1,1,1)`; extending it to arbitrary independent coefficients requires a coefficient-enriched physical source map, not another proof about this normalized integral generator.
