# v60: normalization-ideal descent and supported reverse endpoints

Independent replays passed 301,266 and 49,961 assertions.

`rzk/80-normalization-ideal-descent.rzk.md` records the complete derived mapping
lattice from the actual conductor ideal: rank 40, with rank 34 generator values,
six relation-only classes, and rank 16 strict maps. Its equivariant rank is 13;
the scalar alternating conductor element sees eleven directions and misses two
relation-homotopy directions.

`rzk/81-reverse-endpoint-gysin.rzk.md` records a globally nonzero reverse
endpoint connector which is zero on every cohomology sheaf. On each opposite
triple-normal support, extraordinary restriction admits a supported Gysin lift.
The ungraded lift space is a nonempty discrete torsor with seven coefficient
families per polarity. Supported splitting does not split the global endpoint
extension.

Both Rzk modules pass fresh checks. The supported constructions improve the
endpoint side but remain attached to target-residue-selected sheaves; neither
they nor the thirteen normalization-ideal candidates select the native physical
conductor--Morse or spatial connector map.
