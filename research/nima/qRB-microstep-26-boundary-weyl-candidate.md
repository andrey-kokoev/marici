# qRB microstep 26: identify the boundary Weyl candidate

The source-level boundary candidate is the reflected multiplier kernel

$$
K_S(z,w)=
\frac{1-\Theta_S(z)\overline{\Theta_S(w)}}{2\pi i(\bar w-z)}.
$$

Its boundary diagonal supplies the phase current, while endpoint lines carry the pole/residue data. Thus the `B`-interface has an explicit candidate kernel.

The remaining condition is contractive/positive compression of this kernel. That is equivalent to the Schur or Hermite–Biehler inequality for `Theta_S`, and is therefore RH-strength on the unrestricted completed carrier.

Status: candidate boundary kernel identified; positive compression remains open. This is not a failure of relative geometry, which retains the boundary form as signed cross-readout.
