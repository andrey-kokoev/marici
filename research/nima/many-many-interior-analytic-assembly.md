# Many-many interior analytic assembly

For a finite pair family whose shells satisfy `a >= a_0 > 0`, the source-labelled incidence map, pair-response solution, Laplace readout, and relative linking coordinates assemble into one continuous finite-family graph.

The triangle

$$
P_{\mathcal F}\to R_{\mathcal F}\to B_{\mathcal F}
$$

commutes on the source core because each pair equation and each shell concatenation identity is preserved.

Taking the projective family over finite interior packets yields a many-many relative analytic object on the interior source domain.

The endpoint sector remains an attached projective boundary object. A global physical completion requires endpoint graph control and compatibility of the boundary interface with the interior family.

Status: interior many-many relative analytic assembly established under the Gaussian decay and finite-family graph hypotheses.
