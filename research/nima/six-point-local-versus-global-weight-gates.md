# Local versus global six-point weight gates

## Question

Do rank-one coefficient tests on every six-point facet imply one global system of channel scales?

## Claim boundary

The result compares exact relation spaces for six-point triangulation coefficients. It does not provide source coefficients or embedding provenance.

The three long diagonals split the hexagon into two quadrilaterals. Each gives one `2 by 2` regional coefficient matrix and one logarithmic cross-ratio relation. The resulting local relation matrix has rank three.

Global absorption into nine channel scales is controlled by the left kernel of the `14 by 9` triangulation-channel incidence matrix, which has rank five. Every local cross-ratio lies in this kernel, but the local span has codimension two. Therefore

\[
\text{global channel factorability}\Longrightarrow
\text{all facet rank-one tests},
\]

while the converse fails.

The checker constructs two explicit complementary global relations. It also gives positive weights of the form `w_T=2^(ell_T)` whose logarithmic exponent vector annihilates all three local relations but has nonzero pairing with a complementary relation. Thus every nontrivial local facet matrix has rank one while no global channel-scale assignment exists.

Short-cut facets have `1 by 5` arrays and impose no cross-ratio at six points. This explains why local residue factorization misses the two global compatibility cycles.

## Disposition

The owner acceptance test must retain both layers. Facet rank-one tests localize gluing defects, but the complete five-binomial incidence gate is required to detect two additional nonlocal compatibility obstructions.
