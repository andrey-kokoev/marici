# v70: completed toric descent and W03 excess

Independent replays passed 107,071 and 20,918 checks.

`rzk/96-completed-toric-descent-and-w03-excess.rzk.md` records that proper
duality extends to the whole nonperfect normal-dual inverse system in `D_qc`.
The proper trace is an equivalence and descends the complete Cartier comparison,
including its endpoint pair. At the original long-Rees center the completed
generic dual is four free lines in degree three with no extra `Rlim^1`; its
normalized generic trace is still zero, while all six upper endpoint terms
persist.

The W03 opposite-edge costalk is
`Lambda(-2)<P> + Lambda(-3)<E,R>`. Its given map is `beta*P + E`: the excess
term is nonzero with zero primary and survives the correctly shifted X03
Cartier operation. Identifying the native 35-circle with `beta` times the
separate occurrence-35 partner deletes this genuine class rather than producing
a homotopy.

The combined Rzk module passes a fresh check. Proper completed descent and local
excess are now compatible coefficient constructions, but their identification
with the full physical supported comparison remains open.
