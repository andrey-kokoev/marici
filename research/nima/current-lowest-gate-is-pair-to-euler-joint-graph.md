# Current lowest gate is conservative trace identification on the pair-to-Euler joint graph

The earlier prerequisites are now available:

- complete polarized Stokes/Wronskian metric;
- full stratified Fourier–Poisson contragredient transport;
- canonical G4 pair-to-bordered crossing;
- pointed Evans-to-pair lift with completion injectivity and Xi multiplicity;
- canonical codiagonal injectivity after the interval-cycle quotient.

Prior research constructs the variance-correct bridge

$$
Q_{\rm tr}:P_{\rm pair}\longrightarrow
\mathcal I_1(E)\widehat\otimes H_{\rm sep},
\qquad
g_p\longmapsto P_p\otimes\rho_g,
$$

and its faithful retained graph. It preserves ordered pair labels, shell, ratio orientation, reciprocal slot, source projection, and all parameter jets. Adams grade two acts through cyclic evaluation

$$
A\longmapsto\operatorname{Tr}_E(L(s)^2A),
$$

rather than as a new Euler state coordinate. The bordered response square commutes entrywise for `rho(0),E,W,R`.

The first remaining source object is a conservative response functional on this trace ideal whose four bordered entries equal the independently constructed `C_FP` arithmetic/Green return. Pulling the Euler conservative operators to this graph must produce the arithmetic boundary identity

$$
2\operatorname{Re}z\,E_{\rm ar}(Q_X\Delta_\Phi u_z)
=2\operatorname{Re}\langle\Phi,u_z\rangle.
$$

Together with the theta Green identity

$$
2\operatorname{Re}z\,\|u_z\|^2
=-2\operatorname{Re}\langle\Phi,u_z\rangle,
$$

this would give

$$
2\operatorname{Re}z
\bigl(\|u_z\|^2+E_{\rm ar}(Q_X\Delta_\Phi u_z)\bigr)=0.
$$

If the combined energy is positive and nonzero on Xi kernel states, this forces `Re z=0`.

The functional equality cannot be inferred from shell asymptotics, determinant equality, or the scalar combination `R+2E`. Its first finite test is one prime shell: compare the conservative response and cyclic trace separately on `rho(0)`, `E`, `W`, and `R`, with the determinant-line normalization retained.

Status: pair-to-Euler cyclic joint graph and bordered response compatibility closed; conservative trace identification is the lowest missing arithmetic coherence.
