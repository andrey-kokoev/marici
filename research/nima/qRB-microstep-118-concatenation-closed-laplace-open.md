# qRB microstep 118: concatenation closed, Laplace gate open

The shell identity is exact from interval additivity:

$$
\int_a^c F_{nm}(t)\,dt
=\int_a^b F_{nm}(t)\,dt+\int_b^c F_{nm}(t)\,dt.
$$

Therefore the pair response concatenates before any wall comparison.

The only nontrivial remaining interface condition is the Laplace readout identity between the pair-response graph and the scalar wall port. This requires the source-authorized flux subtraction and equality on an open parameter set.

Status: shell concatenation closed; pair-to-wall Laplace intertwining remains the live gate.
