# The local window-to-odd orientation is already fixed by boundary Stokes

## Reconciliation

The resolved two-window Gram and the charge-window Stokes pairing contain two
different real scalars that have both been called \(\beta_p\). They must not be
identified by notation.

Write

\[
g_p:=\langle BW_L,BW_{2L}\rangle
\]

for the unresolved positive resolved-graph tail entry, and write

\[
s_p:=\langle O,D(W_{2L}-W_L)\rangle
\]

for the oriented cross-character Stokes entry. The existing boundary
calculation gives, for \(L=\log p\),

\[
s_p=4\bigl(H(L)-H(2L)\bigr)>0.
\]

Thus evaluating \(g_p\) is still needed to complete the positive window block,
but it is **not** needed to manufacture the odd orientation.

## Source of the reciprocal sign

The orientation is fixed before any Hermitian Gram completion by

\[
O(q)=\operatorname{sgn}(q),\qquad DO=2\delta_0,
\]

and distributional integration by parts:

\[
\langle O,DV_L\rangle
=-\langle DO,V_L\rangle
=-2\langle\delta_0,V_L\rangle,
\qquad V_L=W_{2L}-W_L.
\]

This is the required ordered sign. Reversing the two slots gives the reciprocal
Green/adjoint entry with the opposite orientation; replacing the ordered
pairing by an ordinary real symmetric Gram would erase precisely this datum.
The hostile same-level pairing confirms the typing requirement:

\[
\langle\delta_0,DV_L\rangle=V_L'(0)=0.
\]

Hence one slot must remain at return level and the other at incidence level.

## Compatibility with the quarter-turn port

The independently established weighted Hadamard/Wronskian comparison fixes the
odd endpoint coordinate with no adjustable normalization:

\[
j_\theta=\frac14S_{\mathrm{ord}},
\qquad
Bj_\theta=-\frac12H_K.
\]

It identifies endpoint jump, normalized Wronskian quarter-column, and
ordered/tail odd history as frames of the same reciprocal-odd line. The Fourier
quarter-turn supplies the imaginary unit used when this real oriented Stokes
scalar is inserted into the complex Schur cell; the Fourier transform does not
choose or fit the scalar's sign.

Consequently the local off-diagonal pair has the forced form

\[
G^{\mathrm{link}}_{p,12}=i\,c_p s_p,
\qquad
G^{\mathrm{link}}_{p,21}=-i\,c_p s_p,
\]

where \(c_p\) denotes only the already source-declared Hadamard/Wronskian and
Euler loading. It is not a free calibration parameter.

## What is closed

On the declared distributional Stokes core:

- the local window-to-odd pairing is nonzero;
- its sign is source-fixed;
- its reciprocal entry has the forced opposite orientation;
- prime diagonality and cutoff naturality hold;
- Euler-loaded upper convergence through the seam is already available.

Therefore the previous statement that the earliest local gate is
"construction of a source-authorized mixed window-to-odd linking block" is
superseded. That local block already exists.

## Remaining gate

What is not yet proved is the comparison needed to insert this local Stokes
block into the selected resolved first-Adams quadratic form. The next exact
identity must show that the source-declared maps from

\[
(W_L,W_{2L})
\]

through the resolved graph and weighted Hadamard/Wronskian lift carry the
ordered Stokes entry to the frozen arithmetic Euler odd coefficient with the
same normalization \(c_p\).

Separately, the positive scalar

\[
g_p=\langle BW_L,BW_{2L}\rangle
\]

still has to be evaluated. Neither that real calculation nor positivity can
replace the arithmetic calibration of \(s_p\).

Radical descent, continuity on the common completion, full-pushout closed
range, and prime-uniform lower bounds also remain open. No RH conclusion is
authorized.
