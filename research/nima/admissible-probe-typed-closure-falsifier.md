# Admissible Probes Require Full Typed Closure

The first attack falsifies the transport-only reading. Over
\(\mathbf F_2^2\), swap generates \(p_1\) from \(p_0\), while the additive
coefficient structure also generates \(p_0+p_1\).

Calling the sum probe fitted was a typing error. The lawful probe family is
closure under all operations carried by the source category and coefficient
enrichment, not merely a symmetry orbit.

The surviving conjecture is:

> An observable is admissible only when it belongs to the minimal fully typed
> closure of primitive source probes. Whether that closure is faithful is a
> theorem or falsifier, never an admission rule.

The next hostile test is sectoral: find a reproducible physical readout absent
from the closure generated by the frozen source, coefficients, supports, and
coherences.

Sequence claim: seqclaim-2a6f524abff60ac63c6398ba.
