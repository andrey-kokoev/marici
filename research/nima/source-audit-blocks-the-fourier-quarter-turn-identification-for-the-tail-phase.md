# Source audit blocks the Fourier quarter-turn identification for the tail phase

## Retraction boundary

The preceding Schur algebra is valid conditionally: if a source-derived skew carrier \(T\) exists, then \(D=S+iT\), its reciprocal resolvent polarization, and the nested contraction gates are correct.

However, the available source audit does not authorize identifying that \(T\) with one theta Fourier quarter-turn.

Grothendieck's source packet establishes two decisive facts:

1. In the normalized theta matrix-coefficient frame, the completed Gaussian source and arithmetic comb are Fourier-fixed. Fourier acts by dilation reversal and does not generate a relative scalar quadrature phase.
2. The adjoint of the full seam-history synthesis is produced by causal reflection, equivalently a Fourier half-turn, not by one Fourier quarter-turn.

Therefore the arrow

\[
\text{theta Fourier quarter-turn}
\longrightarrow
T_{\mathrm{tail/PV}}
\]

is presently undefined.

## What remains valid

The following results remain exact conditional operator theorems:

- Hermitian auxiliary typing \(D=S+iT\);
- reciprocal positivity through \(K=S^{-1/2}(iT)S^{-1/2}\);
- odd/even resolvent polarization;
- endpoint loading through \(L_\pm\);
- triangular shear control;
- the minimal area-energy identity when a two-dimensional oriented \(T\) is independently supplied.

What is withdrawn is the source claim that the theta Fourier transform itself supplies the required \(T\).

## Three distinct operations

The source packets force separation of:

- Fourier quarter-turn: spectral presentation and metaplectic polarization;
- causal reflection or Fourier half-turn: history adjoint and reciprocal variance reversal;
- determinant-line Maslov phase: oscillator half-form orientation.

Equal angles or common metaplectic language do not provide comparison maps among these levels.

In particular, importing the oscillator phase into the theta tail would violate source typing unless a determinant/Pfaffian functor from the tail boundary system is constructed and shown to intertwine Fourier transport.

## Revised candidates for the skew carrier

The odd auxiliary operator \(T\) may still arise from one of three authorized routes:

1. an intrinsic antisymmetric Green boundary form on the two-sector tail/PV system;
2. a causal commutator between forward history and reflected adjoint history;
3. a determinant-line or Pfaffian lift with a proved comparison back to the endpoint Schur block.

Each route must independently prove the reciprocal sign law and Euler-current matching.

## Immediate source-native test

Let \(H_+\) be the anti-causal history synthesis and

\[
H_- = H_+^{*}=R H_+ R
\]

its reflected causal adjoint. The first available skew candidate is the anti-Hermitian history difference

\[
T_{\mathrm{hist}}
=
\frac{H_+-H_+^{*}}{2}.
\]

Then

\[
T_{\mathrm{hist}}^{*}=-T_{\mathrm{hist}},
\qquad
RT_{\mathrm{hist}}R=-T_{\mathrm{hist}}.
\]

This has exactly the required reciprocal character without attributing it to a quarter-turn. But it is only a candidate. The source must still show:

- a common closed domain for \(H_+\) and \(H_+^*\);
- compatibility with the tail/PV even block \(S\);
- prime-label preservation;
- radical descent;
- compression to the scoped Euler odd current.

## New hostile

Take a Fourier-fixed theta source. Any construction claiming that one Fourier application creates a nonzero relative tail quadrature is false in that frame, even if its abstract \(2\times2\) Schur algebra is correct.

A second hostile has a valid skew history commutator but zero Euler compression. Reciprocal oddness alone does not identify arithmetic orientation.

## Revised frontier

The earliest missing constructor is now:

\[
\text{closed forward history}
+
\text{reflected adjoint history}
\longrightarrow
\text{source-derived skew tail/PV form }T
\longrightarrow
\text{Euler odd compression}.
\]

Until this arrow is proved, all quarter-turn contraction margins are conditional templates rather than instantiated Adams-edge data.
