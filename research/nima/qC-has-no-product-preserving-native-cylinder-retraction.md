# The q-C cylinder has no product-preserving native retraction

A structured retraction of the universal q-C mapping cylinder into the native historical chart would produce a homotopy through product-preserving character presentations.

The canonical Pontryagin kernel is

$$
K_m(u)=e^{-2\pi imu}.
$$

It satisfies the character law

$$
K_m(u+v)=K_m(u)K_m(v).
$$

The radialized additive-Fourier kernel has phase

$$
W_m(u)=e^{-2\pi i e^{m+u}}.
$$

Its character law would require

$$
e^{m+u+v}
\equiv
e^{m+u}+e^{m+v}
\pmod{\mathbb Z}.
$$

Set

$$
e^u=\frac32,
\qquad
e^v=\frac54,
\qquad m=0.
$$

The combined phase is

$$
e^{u+v}=\frac{15}{8},
$$

while the product phase is

$$
e^u+e^v=\frac{11}{4}.
$$

Their difference is

$$
-\frac78,
$$

which is nonintegral. The two unit complex phases differ. The historical radial endpoint therefore lies outside the character-preserving mapping space containing the canonical Pontryagin chart.

Consequently the universal cylinder admits no retraction into the native historical carrier that preserves the additive-log character product throughout the homotopy.

The q-C comparison survives in two forms:

1. a graph correspondence through the universal mapping cylinder;
2. an A-infinity comparison whose first higher component records the multiplicativity defect.

The defect cochain is

$$
\mu_2(u,v)
=
W(u+v)-W(u)W(v).
$$

Higher q-C cells must carry the coherence of this defect under arity, polarity, successor, observation, and regulator transport. The six previously registered q-C cubes are the natural locations for those higher components.
