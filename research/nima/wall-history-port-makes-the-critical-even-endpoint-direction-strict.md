# Wall-history port makes the critical even endpoint direction strict

The fixed even endpoint column has energy one. The resolved theta-history graph retains the additional wall-history coordinate

$$
w_\theta\longmapsto M_\Phi w_\theta,
$$

so its complete diagonal energy is

$$
1+M_\Phi^2.
$$

The source normalization identifies

$$
M_\Phi=\int_0^\infty\Phi(r)\,dr=\xi(1/2),
$$

and prior theta estimates give

$$
0<M_\Phi<\frac12.
$$

Therefore the normalized even loading eigenvalue is

$$
\lambda_{\rm even}
=\frac1{1+M_\Phi^2}<1,
$$

with strict margin

$$
\delta_{\rm even}
=1-\lambda_{\rm even}
=\frac{M_\Phi^2}{1+M_\Phi^2}>0.
$$

The odd endpoint column already has energy `1/4`, and its derivative/history incidence has the separate completion-graph bound

$$
\|\partial_qG_C^{-1/2}\|
\le\sqrt{\frac{\sqrt{17}-1}{8}}<1.
$$

Hence both local parity directions possess source-derived strict margins when the resolved wall-history port and completion graph are retained. Codiagonalizing away the wall-history coordinate restores the even eigenvalue one and destroys this conclusion.

This closes local diagonal strictness. The remaining full-row theorem must include reciprocal/linking cross terms, prime/grade synthesis, and prove a uniform largest-eigenvalue bound after their joint normalization.

Status: local even and odd strict margins constructed; global joint loading contraction remains open.
