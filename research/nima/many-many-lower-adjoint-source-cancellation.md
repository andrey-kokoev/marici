# Many-many lower-adjoint source-cancellation gate

For the Xi-derived history state `u_s` with forcing amplitude `c_s`, the Green readback has the exact form

$$
2\left(\operatorname{Re}s-\frac12\right)\|u_s\|^2
=-2\operatorname{Re}\bigl(c_s\langle u_s,\Phi\rangle\bigr)
-\Sigma(\operatorname{Tr}_\partial u_s,\operatorname{Tr}_\partial u_s).
$$

Maximal isotropy cancels the boundary term. The remaining forcing term is cancelled by the lower adjoint source equation represented by `B^dagger`. The required identity is therefore a paired cancellation

$$
2\operatorname{Re}\bigl(c_s\langle u_s,\Phi\rangle\bigr)
+
\Sigma_{\rm adj}(u_s)=0
$$

on every characteristic state, where `Sigma_adj` is the lower adjoint source contribution. The separate forcing term need not vanish. With nonzero radial mass

$$
\mathcal N_s(u_s)=\|u_s\|^2>0.
$$

Then

$$
\left(\operatorname{Re}s-\frac12\right)\mathcal N_s(u_s)=0.
$$

Status: paired lower-adjoint cancellation target isolated; Xi-history verification remains open.
