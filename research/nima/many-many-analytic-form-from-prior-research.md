# Many-many analytic form from prior research

Prior research supplies the analytic pattern for a finite observer family. For each source coordinate include all multiplier and observed-multiplier coordinates in one diagonal graph:

$$
J_{\mathcal F}x
=\bigl(x,M_1x,\ldots,M_rx,\mathcal O_1x,\ldots,\mathcal O_rM_rx\bigr).
$$

Complete the common source core in the product graph norm. Separate closability of the finitely many coordinates implies joint closability, so the finite-family graph is closed.

For an infinite family, take the projective product of these finite-family graph domains. This gives coordinatewise continuity and source-labelled faithfulness. A uniform record norm requires additional estimates.

The observer-family refinement result supplies the compatible arrows: rank restriction is principal compression, base-point translation is a shift, and mesh coarsening is telescoping. Successive coarsenings compose by multiplication of mesh factors.

Therefore the analytic many-many form is:

$$
\text{source-labelled finite diagonal graphs}
\longrightarrow
\text{closed projective family}
\longrightarrow
\text{relative boundary/spectral readout}.
$$

The remaining incidence gate is continuous landing of the arithmetic pair family in this joint graph. This is the correct analytic target above the scalar RH positivity gate.
