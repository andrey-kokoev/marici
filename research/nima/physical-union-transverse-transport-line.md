# WITHDRAWN — the proposed branch maps do not descend

> **Status correction.** The calculation below used images of chosen quotient
> basis representatives.  A complete relation audit found that 852 of 5,780
> domain pivot relations map nontrivially in the cutoff-seven union
> presentation.  Thus the apparent branch images are not well-defined maps of
> the presented cohomology objects, and the claimed transverse line is
> untyped.  The text is retained only as superseded history.

Let (V_{23}) and (V_{31}) be the two rank-20 four-mark residue objects,
mapped into the stabilized five-mark union (V_{\cup}) by multiplying by the
missing labelled denominator before passing to the common product-pole
presentation.  At the calibrated stabilized packet,

\[
\dim V_{23}=\dim V_{31}=20,
\]

\[
\dim(V_{23}\cap V_{31})=15,
\qquad
\dim(V_{23}+V_{31})=25,
\]

while

\[
\dim V_{\cup}=26.
\]

The literal combined numerator lies in (V_{23}+V_{31}), as it must.  Its two
first Gauss--Manin derivatives enlarge that sum by exactly one dimension:

\[
\dim\left(V_{23}+V_{31}+\langle\nabla_1s,\nabla_2s\rangle\right)=26.
\]

Consequently there is a canonical one-dimensional quotient

\[
\boxed{
T_{\rm mix}=V_{\cup}/(V_{23}+V_{31})
}
\]

which is activated by transport, not by either branch at a fixed fiber.  This
is the smallest source-derived object that the earlier rank-20 and rank-35
branchwise analyses necessarily missed.

The line is not yet identified with \(\mathcal Q\).  The admissible next test is
to transport (T_{\rm mix}) over the external base and compute the intrinsic
divisor of its induced second-fundamental-form/extension class.  A
\(\mathcal Q\) factor there would be typed evidence; its absence would remove
the last algebraic extension-level home presently visible in the literal
marked source.
