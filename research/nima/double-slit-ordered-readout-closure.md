# Double-Slit Readout Closure Is Ordered

The frozen QND instrument supplies a Hilbert-space inner product, adjoint,
record projectors, and pointer forgetting. These generate visibility and the
record-difference operator

\[
\Delta=|e_L\rangle\langle e_L|-|e_R\rangle\langle e_R|.
\]

At the exact source point,

\[
\Delta^2=\frac{16}{25}I.
\]

The unordered algebraic relations cannot distinguish \(\Delta\) from
\(-\Delta\). Physical distinguishability is instead the positive norm

\[
D=\|\Delta\|=\frac45.
\]

Thus the sector's readout API requires two independently typed enrichments:

\[
\text{star pairing}\longrightarrow\text{intensity},\qquad
\text{positive order and norm}\longrightarrow\text{distinguishability}.
\]

Both are already part of the frozen finite-dimensional quantum coefficient
object, so the sectoral DPC test passes. It does not yet pass at Carrier level:
the shared Carrier has not been shown to generate either the QND interaction
or its ordered coefficient lens.

Sequence claim: seqclaim-8f4769d9d40b356819b1eea1.
