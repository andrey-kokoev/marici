# qRB microstep 98: Gaussian spectral target interface

The explicit source formula supplies an independent target for Gaussian probes:

$$
\Theta(t,\xi)=K_{\rm endpoint}(t,\xi)+K_{\rm gamma}(t,\xi)+K_{\rm prime}(t,\xi).
$$

This gives concrete target values without using zero locations. The qRB identification test can therefore compare the assembled relative observer against `Theta(t,xi)` on Gaussian parameters.

The comparison must now place the regular-plus-wall linking operator as a separate bounded skew response block alongside the source identity. It must verify compatible action on the same Gaussian parameters, including the oscillatory endpoint factor and the damped prime-power sum, without identifying the two forms.

Status: independent Gaussian target available; compatible skew-block placement is the remaining comparison.
