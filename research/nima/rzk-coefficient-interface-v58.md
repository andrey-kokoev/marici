# v58: reverse cone, conductor source relations, and joint endpoint dual

Independent replays passed 51,635, 258,267, and 13,203 checks respectively.

Modules `76`--`78` record three complementary advances. The complete reverse
normalization object retains its 15-by-16 attaching map, sixteen branch-volume
lines, fifteen conductor lines, and both nonsplit triple-normal endpoint blocks.
Using the actual conductor ideal as source yields rank 16 strict and rank 40
resolved maps; six resolved classes live only on source relations, and two of
the thirteen equivariant classes are likewise invisible on generators. Finally,
the actual node dual has branch Ext degree three and a coupled conductor Ext
degree five whose gluing pairing is the primitive ordered sixfold residue.

The natural node pullback of the earlier fivefold Gysin construction is a useful
negative control: its supported morphism is nonzero, but its support counit and
derived conductor specialization are zero. Thus it cannot supply the desired
endpoint transport through that source map.

All three Rzk modules pass fresh checks. The source-side normalization diagrams
and reflection homotopy are now explicit, but no spatial collar identification,
physical parity, or unique conductor--Morse choice has been established.
