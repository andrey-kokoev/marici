# Rank-26 prior art opens a full relation-module p-normal test

Earlier rank-26 work contains a source-derived construction pattern absent
from the minimal Čech carrier: differentiate the complete labelled exact
relation module in a normal direction and reduce the derivative modulo the
special exact image. The conductor-gamma computation produced a canonical
rank-one Bockstein, moving-relation coherence controlled differentiated IBP,
`K`, and marked-pole relations, and the cyclic atlas transported that line
with unit edge product.

Only the algorithm may be reused. Neither the gamma-normal vector nor the
total-energy Rees class is a p-normal class.

A clean p-normal test point is `(x,y,z)=(3,6,-3)`: `p=x+y+3z=0` while total
energy is `6`. Both `nx=(1,0,0)` and `ny=(0,1,0)` satisfy `dp=1`; their
difference `(1,-1,0)` is p-tangent. Compute the first derivative of every
source-generated rank-26 relation along both normals, reduce modulo the
special relation image and tangent-derived span, then map any surviving line
to the ordered wall/excptional-face total complex.

Success requires normal-choice independence, an integral unit `(1,1)` column
to `(Xi_log,-sigma123)`, total `d^2=0`, Rees-shear invariance, and two-prime
survival. No Bockstein or period is presently claimed.
