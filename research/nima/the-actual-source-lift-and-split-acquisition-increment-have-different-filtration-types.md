# The actual source lift and split acquisition increment have different filtration types

## Result

The actual chosen lower-filtration source lift is now reconstructed and checked directly on the original six-event packet. Its source, old sector values and all 2160 private-row/context coefficients are explicit.

The calculation exposes a necessary type distinction: the old source nullhomotopy escapes the inherited ideal filtration. It cannot simply become a filtration-preserving face witness of the earlier tetrahedral fixture.

Voevodsky's new `../voevodsky/the-two-acquired-vacuum-rows-add-a-canonical-split-thirty-dimensional-increment.md` resolves a separate part of the identification gate. The background-three/four acquisitions have an actual complete saturated presentation and a canonical filtered splitting. Their new adjacent components are filtered-nullhomotopic, unlike the old component's chosen unfiltered nullhomotopy.

## 1. Reconstruct the actual source lift

Keep the old background-two observer and its 270 first-seam private rows. Write

`x=path(2_forgotten,3_retained) forgotten(5,7)`,

`v2=mixed(2,3) forgotten(5,7)`,

`c1=mixed(11,13)`.

The checker reconstructs these actual labelled path sums using the independently verified source relations. It checks:

- x has path cost 2 and zero terminal recorder;
- its full first jet is nonzero;
- the full first jet of v2 is zero;
- the old early/late sector coefficients of x are `(0,1)`;
- those of v2 are `(1,1)`.

The full first jet retains nonvacuum potential-word buffers. Its vacuum projection alone vanishes here and would miss the membership distinction. Since the first jet is a recorder-relative derivation, it kills I^2; its nonzero value on x witnesses the filtration escape in the source.

## 2. Preserve the actual ideal normalization symbolically

After removing the common positive analytical prefactor, write the fixed old functional as

`ell=-A*early+B*late`,

where `A=mu_A1-L` and `B=mu_A2-L` are the actual fixed ideal coefficients.

Then

`ell(v2)=B-A`, `ell(x)=B`.

The checker verifies the exact Laurent-polynomial identity

`c=1-A/B`, `c*B=B-A`.

No midpoint, sample calibration setting or fixture score is substituted for A or B. Nonvanishing of B and identification with the owning physical calibration remain explicit hypotheses; the existing calibration audit supplies their owning justification.

Set

`u=Obs_2(v2)`, `z=c Obs_3(x)`, `j(u)=z`.

The normalization gives `pi(z)=u`.

## 3. Full contributing-context support for the declared 270 rows

The checker reconstructs all 270 private pivot rows from the original source certificate. It evaluates x times all eight marked right contexts filling the remaining `(11,13)` events.

Every one of the 2160 private coefficients is zero. There is no nonidentity left context before the packet's initial vertex, and every contributing total right context is one of those eight. The old row's positive-length contexts vanish by endpoint support.

Thus, under the owning source-evaluation hypotheses, the chosen j is source-equivariant for the declared old-plus-270 observer. This audit does not claim the larger 715-block detector protocol; that has its separate owning support audit.

The resulting source-module nullhomotopy is

`H=B_ev-j*w*q`,

with `pi H=0` and `H i=f`, using the owning source-extension identities. These are symbolic consequences of the retained source lift and compatible evaluations, not a newly exported full observer matrix.

## 4. The filtration mismatch is real

The checker verifies

`P_y(x*c1)=0`, `P_y(v2*c1)=1`.

The owning complete-prefix argument gives `P_y(m*c1)=1` for every m in I E over u. In contrast z*c1 is zero. Therefore z lies outside I E.

Consequently H is an ordinary source-equivariant nullhomotopy into K, not a filtered extension into N=K intersect I E. Its existence is compatible with the nonzero filtered attachment.

This prevents a mistaken identification of H with a filtration-preserving face witness of our previous three-term module fixture. The actual comparison must explicitly retain which category and filtration each map belongs to.

A whole-row gain freezing the old detector leaves x and c unchanged. Source-compatible evaluation then transports j and H naturally. This is the owning symbolic gain theorem, not an identification of the full actual observer with a rational toy module.

## 5. The acquired increment is now supplied, not an unknown module

The newly audited acquisition theorem provides

`Eplus = E direct_sum V3 direct_sum V4`.

Each V_A is the dual of the complete contiguous-subword saturation of the actual selected vacuum coefficient. It has dimension 15, actual source representatives, sparse prime-edge actions and ideal-depth dimensions `(15,6,1,0)`.

The restriction kernel D therefore has dimensions

`(dim D, dim I D, dim I^2 D, dim I^3 D)=(30,12,2,0)`.

These are powers of the actual source ideal. They are not the joint-action filtration of the dual-number fixture.

The source-corner exclusion makes the splitting canonical and filtered. The projection to the old observer is a source-bimodule map, but its section is NOT compatible with the enlarged evaluation of every same source. New sources can be invisible to the old observer and visible in V3 or V4. Hence this acquisition is not an invertible gain re-expression.

Only two raw scalar readings were acquired. Their formal saturation has thirty directions; the two readings do not determine all thirty coordinates.

## 6. The two kinds of nullhomotopy must remain separate

The new block maps admit filtered extensions by source evaluation:

`H_A:A -> I V_A`, `H_A i=f_A`.

The enlarged adjacent filtered class is therefore

`tauplus=(tau,0,0)`.

The old nonzero filtered component remains. Its unfiltered vanishing still uses the lower-filtration lift j. The new components vanish even in the filtered category and need no such escape.

This is useful identification progress: the acquisition increment is now concretely source-typed. The remaining old-component gate is its saturated evaluation presentation and the typed comparison of its chosen lift with the productization construction. The six actual tower comparisons and their four face witnesses are still not supplied by merely adjoining the split blocks.

No unacquired tail has been turned into a detector or an equivariant aggregation map.

## Verification

New source-lift audit:

`python research/nima/checkers/check_actual_private_source_lift.py`

Artifact:

`research/nima/results/actual-private-source-lift.json`

Freshly rerun acquisition check:

`python research/voevodsky/checkers/check_vacuum_acquisition_increment.py`

Both pass. The source-lift audit checks actual finite coefficients and a symbolic normalization identity; it is not a standalone verifier for all categorical hypotheses or a four-tower coherence certificate.
