# Final source relation is Xi kernel to unitary-feedback kernel

The one-sided sum/difference factorization constructs a passive colligation on each stable tail. Their reciprocal direct sum gives a doubled passive return

$$
G(\lambda):\mathcal B\to\mathcal B.
$$

The stratified Fourier–Poisson reciprocal sewing supplies a boundary unitary

$$
C:\mathcal B\to\mathcal B
$$

in the transported Green metric. Off the lossless seam, the transfer-defect identity gives strict pointwise contraction on every nonzero source-generated state:

$$
\|G(\lambda)b\|<\|b\|.
$$

Therefore

$$
\ker(I-CG(\lambda))=0
$$

off seam.

The independent Xi pencil has

$$
\ker\mathcal R_\Xi(z)\ne0
\quad\Longleftrightarrow\quad
\tau(z)=0.
$$

The exact remaining source coherence is a multiplicity-preserving chain map

$$
J_{\rm fb}(z):
\ker\mathcal R_\Xi(z)
\longrightarrow
\ker(I-CG(\lambda(z)))
$$

that is injective on nonzero Xi states and commutes with parameter jets. More strongly, one may seek an isomorphism of local root complexes.

This is the operator form of the two-height energy-cycle identity. If the map exists, an off-seam Xi zero would produce a nonzero feedback kernel vector, contradicting strict passivity.

The map must be derived from the fixed-forcing one-leg pair lift, bordered crossing, and complete sum/difference ports. Defining it only on known Xi zeros or through the desired kernel equality is circular.

Status: passive doubled feedback system constructed at the source-factorization level; Xi-kernel-to-feedback-kernel comparison remains the RH-bearing source relation.
