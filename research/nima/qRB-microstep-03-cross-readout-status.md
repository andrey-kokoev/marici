# qRB microstep 03: cross-readout status

The finite aligned identity now has its correct analytic disposition.

For the completed semilocal filler,

$$
\mathfrak F_{34,S}=(C_S,D_S,J_2,\operatorname{Tr}_{rel}),
$$

we require:

$$
D_S\rho(g)\in\mathcal S_2,
\qquad
\rho(h)^*(C_S^*J_2D_S+D_S^*J_2C_S)\rho(g)\in\mathcal S_1.
$$

The status ledger records these properties as constructed on the Bruhat–Schwartz core, with iterated regulator order. The common row is bounded as a module map but its raw Hilbert–Schmidt norm diverges; this is an exact obstruction to an ordinary uniform completion.

Thus microstep 03 is **closed in the relative category**: the readout is trace-class and regulator-relative. It is not closed for simultaneous all-regulator convergence or an ordinary finite-norm limit.

No scalar Weil positivity or RH assertion follows.
