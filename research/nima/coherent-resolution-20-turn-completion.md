# Coherent Resolution execution-cycle completion

## Completed locally

1. **Finite n=8 canonical boundary:** all 166 facet incidences covered; all 40 shared residues cancel explicitly; external pushforward and physical completion pass.
2. **Arbitrary-rank combinatorics:** finite `CR_m` is contractible; supported histories plus boundary completions biject onto every `A_m` facet; all codimension-two tests are indexed with closed `PP`, `PN`, and `NN` counts.
3. **Weighted transport:** the positive rational line is a diagonal gauge twist with zero augmented homology. Direct binary descent is invariantly obstructed on 17 of 21 A3 mutation edges.
4. **Formalization:** polygon diagonals now use correct modular adjacency and canonical ordering. Lean proves square and pentagon vertex-ratio holonomy and their nonunit obstructions; the build passes without placeholders.
5. **Flavor:** the maximal free presentation is a chain isomorphism, but no Carrier-to-Yukawa map exists.
6. **Control:** boundary-plus-curvature reconstruction has an explicit integral inverse for every finite rank; it remains a static coordinate theorem.
7. **BCJ/radiative bridge:** the present six-point CR assignment has a certified rank-four chain defect. Its minimal cone repair is free rather than source-derived.
8. **Independent G4/RH:** the target interface has 64 slots and zero independent entries. Separately, unchanged-Evans membership is certified false at the first zero under the tested criterion; this does not decide RH.
9. **SCC semantics:** literal Grassmannian equality, decorated quotient equivalence, and positive-domain incidence are represented separately.

## Reopening witnesses requiring new source data

- arbitrary-rank canonical-form/positroid representatives and oriented residue maps;
- a Carrier-to-weak-basis-Yukawa chain map;
- a source-derived physical error alphabet and syndrome map over a declared integral coefficient model;
- a typed state evolution, actuator, output family, and physical-time map;
- cubic-graph Jacobi numerators on CR generators with generalized-gauge and differential data;
- a CR-derived arithmetic `J_r` functor and `G_{r,s}` forms populating the independent G4 interface;
- a sourced divisor-preserving modified Evans state and new membership theorem.

None of these witnesses is present in the current repository or inbox. Continuing locally without one would repeat an accepted audit, freely adjoin missing structure, or cross an explicit claim boundary.

## Rebuild

Run:

```bash
python research/nima/run_coherent_resolution_frontier.py
```

The latest dependency-ordered rebuild passes all 23 checks. The authoritative integrated status is `research/nima/results/coherent-resolution-programme-frontier.json`.
