# Many-many quotient-derivative wall coordinate

Let `N_E` denote the endpoint-value radical in the retained wall carrier. Define the quotient boundary space

$$
\mathcal J_E=\operatorname{Dom}(\partial_z)/\mathcal N_E
$$

and the derivative coordinate

$$
\eta[f]=[\partial_z f]_{\mathcal N_E}.
$$

For the pair response, use the weighted class

$$
\eta_{nm}[R]
=\left[\kappa_{nm}\partial_zR_{nm}\right]_{\mathcal N_E}.
$$

The endpoint package becomes

$$
(\gamma_0,\eta)_{nm}
=\left(\rho_{nm}(0),\eta_{nm}[R]\right).
$$

The pair-to-wall map is well-defined precisely when the endpoint radical is invariant under the weighted derivative coordinate. The source equation then supplies the readout relation

$$
\mathcal L_z(F_{nm})
=zR_{nm}-\gamma_0(R_{nm})-\eta_{nm}[R].
$$

This construction retains derivative information in the relative quotient and supports the oriented antisymmetric sector.

Status: quotient-derivative interface defined; radical invariance and agreement with the original linking form are the verification gates.
