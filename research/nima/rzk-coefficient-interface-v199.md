# v199: existing partial Rees specialization found

The v198 statement that no labelled coefficient specialization exists was too strong. Earlier source work supplies the graph

`u03=lambda*X03`

and a reduced occurrence-tensored Q packet with ordered exponents

`(X03,X14,X25,u03,u14,u25;X35)`.

Under that source-prescribed specialization, the primary `Omega` is the boundary of `S`; the comparison class `Z` remains a cycle with primitive detector. The supported Cartier cone has differential

`D(a,b)=(d a, X03*a-d b)`

and explicit identity `D(S,0)=(Omega',X03*S)`.

Thus the 18-variable target has an established restriction to a seven-variable labelled sector and one nontrivial graph relation. What remains absent is the map from these seven variables, including the external unit lambda and independent occurrence variable X35, to the ramified physical parameters `(p,kappa,x,d)`. In particular no existing source formula identifies X03 with the deformation coordinate x or realizes d as a Laurent expression in this seven-variable ring.

The next compatibility test must start from this seven-variable packet, not the unconstrained 18-variable ring.
