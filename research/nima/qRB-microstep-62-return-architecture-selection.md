# qRB microstep 62: return architecture selection

Prior research identifies three possible placements of the prime return:

1. labelled return before codiagonalization;
2. common-history return with a proved label-orthogonal metric;
3. common-history return with explicit cross-prime blocks and a Schur/form bound.

For the present microstep, select architecture 1. It preserves source prime diagonality and avoids importing undeclared cross-prime metric coupling.

The resulting order is

$$
\text{prime-labelled feature}
\longrightarrow
\text{labelled return}
\longrightarrow
\text{relative quotient}
\longrightarrow
\text{codiagonal physical readout}.
$$

This is the minimal noncircular route. Architectures 2 and 3 remain optional upgrades requiring additional metric data.

Status: return placement fixed for the next calculation; no cross-prime estimate is claimed.
