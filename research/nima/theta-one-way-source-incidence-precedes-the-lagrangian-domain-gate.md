# One-way theta source incidence precedes the Lagrangian-domain gate

> **Status update.** The successor packet
> `the-retained-history-metric-now-constructs-the-adjoint-incidence-but-not-its-xi-characteristic.md`
> constructs the reverse metric incidence on the retained history/seam
> carrier. The triangular no-go below remains correct for the original
> one-way system. The paired system now exists, but its characteristic divisor
> has not been identified with Xi.

## Status

Exact bulk-adjointness obstruction. The source-derived tail system cannot be
made selfadjoint merely by choosing a maximal Lagrangian boundary domain,
because its source coupling is triangular. Reciprocal sewing must first derive
the missing adjoint incidence.

## Abstract triangular system

Let \(\mathcal H\) be the tail state space and \(\mathcal U\) the source-channel
space. Consider

\[
\mathcal A_{\mathrm{tri}}
=
\begin{pmatrix}
A_0&B\\
0&C
\end{pmatrix}
\]

on

\[
\mathcal H\oplus\mathcal U.
\]

Assume the two source types carry the declared orthogonal direct-sum inner
product. For \(g\in\operatorname{Dom}A_0\) and \(u\in\operatorname{Dom}C\),

\[
\left\langle
\mathcal A_{\mathrm{tri}}inom0u,
\binom g0
\right\rangle
=
\langle Bu,g\rangle,
\]

whereas

\[
\left\langle
\binom0u,
\mathcal A_{\mathrm{tri}}inom g0
\right\rangle
=
0.
\]

Symmetry therefore requires

\[
B=0.
\]

A nonzero one-way source incidence is incompatible with selfadjointness.

## Application to the tail system

The homogeneous source-tail operator is

\[
\mathcal D_z
=
\begin{pmatrix}
\partial_q+z&f(q)\\
0&\partial_q
\end{pmatrix}.
\]

Its upper incidence is multiplication by the source forcing:

\[
Bc=f(q)c.
\]

The lower adjoint incidence is absent. Hence the augmented rank-two system is
not a selfadjoint spectral realization. It is a controlled system with a
one-way input.

## Boundary domains cannot repair bulk asymmetry

A Lagrangian boundary domain cancels the Green form produced by integration
by parts in the differential block \(A_0\). The residual

\[
\langle Bu,g\rangle
\]

is an interior pairing. It remains for compactly supported \(g\), where every
endpoint trace vanishes. Therefore no endpoint boundary condition, no
coorientation, and no maximal isotropic relation can remove it.

The construction order is forced:

1. derive a reciprocal lower incidence;
2. prove bulk symmetry or adjointness;
3. derive the Green boundary form;
4. select a maximal Lagrangian domain;
5. prove selfadjointness after completion;
6. identify the characteristic determinant.

Starting at step three silently assumes the missing bulk constructor.

## Minimal symmetric completion

The formal symmetric block has the form

\[
\mathcal A_{\mathrm{sym}}
=
\begin{pmatrix}
A_0&B\\
B^*&C
\end{pmatrix},
\]

with \(A_0\) and \(C\) symmetric on compatible domains.

The new equation

\[
c'=B^*G+C c
\]

is backreaction. It cannot be inferred from the original condition \(c'=0\).
It must be generated by the reciprocal Fourier–Tate source, a boundary
reservoir, or another independently authorized channel.

## Schur-complement warning

Eliminating the source/reservoir block of a selfadjoint realization produces a
Weyl or transfer function of the schematic form

\[
M(z)
=
C-z-B^*(A_0-z)^{-1}B.
\]

Its half-plane orientation follows from selfadjointness. But choosing \(B^*\),
\(C\), or the domain so that \(M\) matches the completed logarithmic derivative
after scalar inspection would merely rebuild the RH-equivalent Herglotz
criterion.

The blocks must be derived before the scalar comparison.

## Reciprocal sewing test

At cutoff \(X\), let

\[
B_{+,X}:\mathcal U_X\longrightarrow\mathcal H_X
\]

be the direct source incidence, and let

\[
B_{-,X}:\mathcal H_X\longrightarrow\mathcal U_X
\]

be the candidate reciprocal incidence. The first decisive residual is

\[
R_X^{\mathrm{adj}}
=
B_{-,X}-B_{+,X}^*.
\]

This residual must vanish as a typed operator identity, including domains and
source pairings. Equality after scalar trace or determinant aggregation is
insufficient.

If the source spaces use nontrivial Gram matrices \(H_X\) and \(K_X\), the
correct condition is

\[
K_XB_{-,X}=B_{+,X}^*H_X.
\]

The metric data may not be fitted to make this equation true.

## Relation to the forcing obstruction

The uncancelled polarization

\[
\operatorname{Re}(fc\overline G)
\]

is the quadratic shadow of the missing adjoint incidence. Cayley wave variables
retype it as boundary supply but do not create the backreaction operator.

Grothendieck's oscillatory autocorrelation invariant is therefore compatible
with the same diagnosis: the source has supplied forward forcing and
reflection metadata, but not yet a global adjoint coupling that turns the
controlled evolution into a conservative spectral generator.

## Factorization coherence

Primitive, square, seam, and archimedean channels may assemble the reciprocal
incidence through several logical factorizations. Equal endpoint operators do
not identify their intermediate kernel flags. Any two factorizations require
a comparison cell preserving:

- the source domain;
- the adjoint pairing;
- the boundary form;
- the residue filtration;
- completion topology.

This is the operator relevance of Buzzard's composite-residue hostile.

## Finite falsifiers

The route fails at the first cutoff where any of the following occurs:

1. \(R_X^{\mathrm{adj}}\ne0\);
2. the candidate lower incidence has the wrong domain;
3. adjointness holds only after scalar projection;
4. a source metric is chosen retrospectively to force adjointness;
5. two source factorizations have equal endpoints but incompatible residue
   flags;
6. bulk symmetry holds finitely but fails in graph completion.

Only after these tests pass should one compute

\[
B_X^*J_XB_X
\]

for a candidate Lagrangian boundary domain.

## Verdict

The immediate RH frontier is not yet domain selection. It is the construction
of the reciprocal adjoint source incidence. The current tail system is an
input-driven triangular system, not a selfadjoint spectral operator. A
source-derived lower block matching the adjoint of the forward forcing is the
first constructor that could convert provenance and passivity into a genuine
spectral explanation.
