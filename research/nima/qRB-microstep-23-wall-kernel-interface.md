# qRB microstep 23: wall-kernel comparison interface

The affine source packet is now fixed. The remaining comparison is an interface, not yet an identity:

$$
\mathcal A_{a,b}\longrightarrow \mathcal W_{a,b},
$$

where `\mathcal A_{a,b}` is generated by `D_a`, `T_b`, and their semidirect relation, while `\mathcal W_{a,b}` is the source-derived wall-kernel coordinate.

A valid interface must specify:

1. the wall kernel as an explicit bilinear functional;
2. its domain on the affine Gaussian packet;
3. covariance under `D_aT_bD_a^{-1}=T_{b/a}`;
4. the relative trace/readout map;
5. endpoint and regulator typing.

Until these data are supplied, no claim may identify the affine residual with the wall kernel or declare the comparison cell positive.

Status: interface isolated; source-derived wall-kernel formula remains the next substantive input.
