# The tenth theta quarter-turn rotates defect memory into a canonical model state

## Ordered-port correction

The canonical model state represents zeros of its chosen inner transfer
symbol. It represents theta zeros only if that symbol is derived from the
ordered source-to-endpoint channel. The current unframed Weyl characteristic
function does not satisfy that gate. This packet remains a generic hostile
model and no-go theorem.

Exact finite Blaschke/model-space no-go theorem. Fix the causal Hardy factor and
its Toeplitz memory defect from the ninth rotation. The defect space carries a
canonical compressed-shift dynamics and a positive de Branges--Rovnyak kernel.
Every finite collection of hostile half-plane zeros admits such a causal,
lossless, controllable, and observable realization.

Therefore passive minimal realization does not supply the missing RH
orientation law. It realizes the unwanted zeros as legitimate internal states.

## Disk normalization

Apply a Cayley map from the selected open half-plane to the unit disk. A finite
inner factor becomes a finite Blaschke product

\[
B(z)
=
e^{i\theta}
\prod_{j=1}^{d}
\frac{z-a_j}{1-\overline{a_j}z},
\qquad
|a_j|<1.
\]

Its Hardy model space is

\[
\mathcal K_B
=
H^2\ominus BH^2,
\]

with

\[
\dim\mathcal K_B=d
\]

when zeros are counted with multiplicity.

## The tenth quarter-turn

The memory defect becomes a state space. Let \(S\) be the unilateral shift on
\(H^2\). The canonical internal dynamics is the compressed backward shift

\[
A_B
=
S^*\vert_{\mathcal K_B}.
\]

For \(a\in\mathbb D\), write the Hardy reproducing kernel as

\[
k_a(z)
=
\frac{1}{1-\overline a z}.
\]

If \(B(a)=0\), then for every \(h\in H^2\),

\[
\langle Bh,k_a\rangle
=
B(a)h(a)
=
0.
\]

Hence

\[
k_a\in\mathcal K_B.
\]

Moreover,

\[
A_Bk_a
=
\overline a k_a.
\]

The zero has become an eigenstate of the canonical memory dynamics.

## Automatic positive kernel

The model space has reproducing kernel

\[
K_B(z,w)
=
\frac{1-B(z)\overline{B(w)}}{1-z\overline w}.
\]

This kernel is positive semidefinite for every Schur function \(B\), and for a
finite Blaschke product it has rank \(d\). At the zeros,

\[
K_B(a_i,a_j)
=
\frac{1}{1-a_i\overline{a_j}},
\]

which is the positive Gram matrix of their canonical defect states.

Thus kernel positivity does not reject hostile zeros. It supplies them with a
well-behaved Hilbert geometry.

## Minimal conservative realization is also automatic

A finite Blaschke product admits a finite-dimensional unitary colligation with
transfer function \(B\). Its canonical model realization on \(\mathcal K_B\)
is simple: the state space is generated by its input and output orbits, so the
realization is both controllable and observable after removal of redundant
coordinates.

Consequently the following properties can all hold together:

- one-sided causal support;
- unitary boundary values;
- positive signed delay;
- zero wrong-sector Hankel leakage;
- finite memory defect;
- positive model-space kernel;
- controllability;
- observability;
- and zeros anywhere in the selected open half-plane.

None of these properties is an RH-selective orientation law.

## Exact hostile family

Choose arbitrary points \(a_1,\ldots,a_d\) in the disk and form the associated
finite Blaschke product. The construction gives

\[
\operatorname{spec}(A_B)
=
\{\overline{a_1},\ldots,\overline{a_d}\}
\]

with algebraic multiplicity encoded by the corresponding root vectors.

Moving the \(a_j\) changes the zero locations while preserving the entire
generic architecture listed above. Reciprocal or conjugate pairing can be
enforced by adjoining the reflected Blaschke factors, again without forcing
the points to the sewing seam.

This is the finite hostile model for any proposed theorem based only on causal
passive minimal realization.

## What the rotation teaches

The successive objects are related by information-restoring lifts:

\[
\mathcal Q
\longleftarrow
B
\longleftarrow
\mathcal K_B
\longleftarrow
A_B.
\]

The positive delay density forgets the causal factor. The factor forgets a
chosen state realization. The canonical model restores that realization, but
the restored states are exactly where hostile zeros live.

The new object is therefore not an automatic solution. It is a precise carrier
for the obstruction.

## Stronger source law required

An RH-bearing condition must constrain \(A_B\) using information not shared by
arbitrary inner functions. Candidate types include:

- a source-derived pairing that cancels every open-sector defect state;
- a grading under which all such states are forbidden or exactly paired;
- an arithmetic incidence law incompatible with arbitrary Blaschke factors;
- a completion theorem that leaves no finite-energy open-sector model state;
- or a global relation tying the defect spectrum to primitive, square,
  archimedean, and seam currents.

Calling the realization minimal, passive, lossless, or observable cannot serve
as that condition.

## Finite falsifier

For any proposed law expressed solely through generic model-space positivity,
causality, passivity, controllability, or observability:

1. choose one point \(a\) in the forbidden open sector;
2. use the degree-one Blaschke factor with zero \(a\);
3. construct its one-dimensional model space;
4. verify the proposed law;
5. observe the eigenstate \(k_a\) of \(A_B\).

If all gates pass, the law cannot exclude the off-seam zero.

## Decisive conclusion

The tenth quarter-turn converts the causal memory defect into a canonical
internal state machine. This does not remove the hostile zero; it makes the
zero a legitimate, observable, controllable state with a positive kernel.
The generic passive-control route is therefore closed as an RH explanation.
Only an arithmetic source law acting on the model states themselves can move
the programme forward.
