# Many-many pro-object triangle coherence

The associator and unitors are identity maps on source-labelled coordinates. Therefore the triangle coherence diagram reduces to

$$
R_{\beta\alpha}
=R_{\beta\alpha}I_{\mathcal E_\alpha}
=I_{\mathcal E_\beta}R_{\beta\alpha}.
$$

This holds for each finite family and passes coordinatewise to the projective pro-limit. The three observer maps `P`, `R`, and `B` inherit the same triangle law through their typed compositions.

Status: associator–unitor triangle coherence closed for the many-many relative pro-system.
