# The q-C face has a universal mapping-cylinder homotopy

Let

$$
f_i=C_{i+1}q_{\mathrm{can}}
$$

and

$$
g_i=q_{\mathrm{hist}}C_i
$$

be the two q-C routes from the canonical chart carrier to the next historical chart carrier.

Form the double mapping cylinder

$$
M_i
=
Y_{i+1}\sqcup(X_i\times[0,1])ig/
\bigl((x,0)\sim f_i(x),\ (x,1)\sim g_i(x)\bigr).
$$

Inside this enlarged comparison carrier, the cylinder coordinate gives a canonical homotopy

$$
\alpha_i:
f_i\Rightarrow g_i.
$$

Its cellular boundary is

$$
\partial\alpha_i=f_i-g_i.
$$

This constructs the q-C face universally as a correspondence-level homotopy.

## Six secondary cells

For every third axis

$$
a\in\{H,V,D,L,O,R\},
$$

the q-C-a cube has six square faces. Their oriented sum is a closed cellular 2-cycle because

$$
\partial^2=0.
$$

Adjoining a 3-cell

$$
K_{qCa}
$$

with that boundary gives a homotopy of homotopies. The six secondary fillers are

$$
K_{qCH},
\quad
K_{qCV},
\quad
K_{qCD},
\quad
K_{qCL},
\quad
K_{qCO},
\quad
K_{qCR}.
$$

The checker verifies the q-C square cycle and all six cubical 2-cycle boundaries.

## Native realization gate

The mapping cylinder enlarges the comparison carrier. A q-C homotopy inside the native historical retained graph requires a structure-preserving map

$$
r_i:M_i\to Y_{i+1}
$$

whose restriction to the historical summand is the identity and which preserves the graph, product, dagger, endpoint, and regulator structures.

Such a map is equivalent to realizing the universal cylinder homotopy in the native structured mapping space. The kernel mismatch blocks a direct chart conjugacy and leaves this structured retraction as the relevant test.

The positive geometry therefore supports the full q-C hierarchy at the correspondence level:

$$
\alpha_{qC}
$$

on the square and

$$
K_{qCa}
$$

on each adjacent cube. Native historical promotion is governed by the structured cylinder retraction.
