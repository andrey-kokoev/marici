# Irrational four-mass sheets and rational algebraic trace at exact rank-six targets

The earlier source-generated rational targets had two **rational** inverse sheets. That fact is nongeneric: at TWO independently constructed rational rank-six targets, with strictly positive moment-curve external data, the sourced four-pair inverse equation is an **irreducible real quadratic over Q**. The test uses a positive-determinant `GL(6)` presentation with the first six retained twistors equal to `I₆`, then perturbs a target coordinate by exactly `1/1000` away from the original source-selected target. The defining four pair-minor equations solve `T` linearly in `q=det(T)`, leaving `P(q)=Aq²+Bq+C` with positive **nonsquare** rational discriminant. Both algebraic sheets satisfy the exact bosonic target and four source pair constraints, and their source gauge minor is nonzero.

The relevant per-flavor ratio between `(local 2,6)` and `(local 1,5)` Plücker coordinates is reduced explicitly in the quotient field `Q[q]/(P)`. At **both** targets it has the form `r(q)=r₀+r₁q` with `r₁≠0`. Thus each individual sheet ratio is genuinely irrational, and the two sheets are exchanged by the quadratic Galois involution. Nonetheless their sum is the exact rational number `2r₀−r₁ B/A` recorded by the checker.

More generally, the complete oriented four-mass source density, target Jacobian, and full fermionic numerator are *rational functions* of the reconstructed source coordinates, themselves rational functions of `q` on this chart. Wherever their denominators remain nonzero, the **sum of the two algebraic sheet residues** is therefore a rational field trace, even when either sheet alone contains a square root. This is an algebraic rationality argument, not an expanded closed rational expression for the arbitrary-`Y` form. It also does not identify this two-sheet complex contour with the full positive-image fibre cycle or prove global image coverage. Positivity of the externally supplied rank-six data is certified; strict interior positivity of each perturbed target's inferred source sheet is not independently certified here.

Checker: `research/nima/checkers/check_nine_point_genuinely_quadratic_four_mass_fibre.py`; result: `research/nima/results/nine-point-genuinely-quadratic-four-mass-fibre.json`.
