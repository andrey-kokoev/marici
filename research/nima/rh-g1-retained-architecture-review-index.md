# RH G1 retained-architecture review index

## Scope

This index records the current review boundary for G1.  It does not change the
publication ledger, certify G1.4, establish later global margins, or authorize
an RH conclusion.

## Selected constructor

The source-authorized completed object is the retained saturated graph

\[
\mathcal Jx=(x,Ax,Cx),
\]

with the grade decomposition

\[
\Gamma_{12}\oplus\Gamma_{\ge3}.
\]

The source coordinate is not auxiliary bookkeeping: it provides the closed
embedding, preserves labels through completion, and prevents compact analytic
outputs from determining the source topology.

The alternative output-only codiagonal has dense nonclosed range in the
declared Köthe topology even with the correct half-density coefficient.  It is
therefore a later readout at most, not the completed G1 carrier.

## G1.1 review object

Status: closure candidate.

Established statements:

\[
Q_\pm x=\frac1{\sqrt2}(x\pm iHx),
\]

\[
Q_\pm^*Q_\pm
=\frac12(I\pm iH)^*(I\pm iH)
\ge\frac18I,
\]

and, on the exact twisted-theta incidence frame,

\[
V_{\rm src}^*iTV_{\rm src}
=i\tau
\begin{pmatrix}0&-1\\1&0\end{pmatrix},
\qquad
\tau=-\|\Phi\|_2^2<0.
\]

Review packets:

- `the-retained-history-graph-realizes-the-g1-1-shifted-squares-with-the-theta-mass-bound.md`;
- `the-exact-half-density-columns-fix-the-source-incidence-and-give-tau-minus-theta-l2-mass.md`;
- `rh-g1-1-closure-candidate-and-g1-3-boundary.md`.

Numerical scout only:

- `checkers/check_rh_theta_tau_compression_numeric.py`.

## G1.2 review object

Status: closure candidate on the selected retained architecture.

Established statements:

- strict primitive-square Stieltjes endpoint lifts;
- closed half-density histories and continuous endpoint traces;
- reciprocal exchange and finite-cutoff naturality;
- compatible zero-trace radical quotient;
- zero radical and closed range of the retained completed graph;
- dense nonclosed range of the corrected output-only codiagonal.

Review packets:

- `the-stieltjes-endpoint-lift-transports-the-euler-pauli-frame-with-a-uniform-lower-bound.md`;
- `the-retained-theta-endpoint-metric-makes-wall-jump-radical-compatibility-automatic.md`;
- `the-analytic-half-density-window-to-wronskian-leg-is-now-closed.md`;
- `retaining-the-source-coordinate-closes-the-global-rigged-joint-graph.md`;
- `the-correct-half-density-output-codiagonal-also-has-dense-nonclosed-kothe-range.md`;
- `rh-g1-2-is-closed-on-the-retained-graph-but-not-on-an-output-only-pushforward.md`.

## G1.3 review object

Status: closure candidate on the selected retained architecture.

The strict low-grade carrier retains

\[
S_{12}=\operatorname{diag}(-1,+1)
\]

and the source loading

\[
\kappa_p^{(k)}
=2(\log p)p^{-k/2}\Phi'(k\log p).
\]

The conservative low-grade loading margin satisfies

\[
\Delta_p^{\rm res}-\frac{\kappa_p^2}{4}>0.244.
\]

The connected grades enter through their separate nuclear return

\[
R_{\ge3}e_{p,k}=\kappa_p^{(k)}j_p,
\qquad k\ge3.
\]

Review packets:

- `the-local-primitive-square-mate-is-the-source-joint-graph-not-a-ratio-map.md`;
- `the-primitive-square-linking-margin-survives-both-saturated-and-codiagonal-assembly.md`;
- `the-euler-theta-incidence-is-uniformly-too-small-to-exhaust-the-window-area.md`;
- `the-connected-prime-power-tail-has-a-separate-nuclear-wronskian-return.md`;
- `rh-g1-3-reduces-to-the-separate-connected-tail-return.md`.

Numerical hostile/cross-check:

- `checkers/check_rh_first_adams_oriented_margin_numeric.py`.

## G1.4 review object

Status: closure candidate; independent owner review requested.

The operator explicitly authorized `marici.Nima` to perform the G1.4 audit.
On the retained labelled carrier, every component commutes with source prime
idempotents and

\[
P_qGP_p=0
\qquad(p\ne q).
\]

The connected return targets the prime-labelled generator \(j_p\), and the
internal Wronskian codiagonal aggregates grades only within fixed \(p\).
Cross-prime products appear only if a later terminal scalar evaluator is moved
before polarization; that operation is outside G1.

Review packet:

- `rh-g1-4-prime-diagonality-is-exact-on-the-retained-labelled-carrier.md`.

Initial handoff event:

`ev-000000010461-66207c11-940e-4f2f-9593-acb6ea05fac2`.

Result and independent-review request:

`ev-000000010462-f65beefd-e4a9-448e-a5e6-e3aa8cbedeac`.

## Ledger action boundary

G1.1--G1.4 are closure candidates on one retained labelled architecture.
Do not change their ledger statuses until the cross-packet constructor order,
closure domains, source idempotents, and terminal-evaluator boundary receive a
coherent review. G2--G4 and RH remain open regardless of G1 closure.
