# G4 hard blocker after five independent constructor rechecks

## Current minimal architecture

The source-authorized completed candidate now has:

- one unlabelled theta source port \(V:\mathbb C_\theta\to H_\theta\);
- the seam-weighted Hilbert--Schmidt incidence
  \(B_\Sigma:U_{\rm ar}\to H_\theta\);
- zero theta bulk diagonal \(D_\theta=0\);
- no primitive direct theta--arithmetic block;
- an arithmetic complement law \(D_U\), with the prime-delay Cayley network as
  a viable complement candidate;
- the two-sided Evans mismatch \(\tau\), equal to the Xi section;
- a maximal-isotropic endpoint domain.

On an off-seam history-resolvent chart, define

\[
Q_U
=
D_U+B_\Sigma^\dagger R_HB_\Sigma
\]

and

\[
F_\theta
=
V^\dagger R_HV
-
V^\dagger R_HB_\Sigma
Q_U^{-1}
B_\Sigma^\dagger R_HV.
\]

At finite cutoff and wherever the blocks are invertible,

\[
\det M_{\theta U}
=
\det Q_U\,F_\theta.
\]

## Closed constructor facts

1. The Xi section is exactly the mismatch of two stable theta histories.
2. A zero of \(\tau\) gives a nonzero two-ended triangular Evans state.
3. The reverse seam incidence \(B_\Sigma^\dagger\) exists and is bounded.
4. The arithmetic return through \(B_\Sigma\) is trace class on the centered
   seam and has the declared relative ideal charts off it.
5. The strict prime-delay Cayley law gives cutoff-uniform inverse control for
   the arithmetic complement, not for the theta defect coordinate.
6. G3 margins may control \(Q_U\) only after the theta defect line is split
   off.
7. The ordinary unweighted diagonal theta packet is not an object of the
   retained labelled carrier; G4 instead uses the distinct bounded summed
   incidence \(B_\Sigma\).

## Unresolved source theorems

### Complement-energy transfer

The successor packet
`the-theta-common-mode-lies-in-the-closure-of-arithmetic-incidence-but-only-at-diverging-source-cost.md`
proves

\[
\Phi\in\overline{\operatorname{ran}B_\Sigma}.
\]

Thus the former positive range-angle target is false. The approximating source
vectors have norm squared \(p\log p\), so the correct gate is instead a
complement-energy estimate for

\[
Q_U^{-1/2}B_\Sigma^\dagger R_HV.
\]

This weighted transfer, not unweighted closed-range separation, determines
whether arithmetic dressing can cancel the theta Weyl value.

### Seam boundary value

The formulas use \(R_H=(A-z)^{-1}\). At the critical seam, the skew history
generator may meet continuous spectrum. No completed limiting-absorption
result currently places the dressed state and Schur scalar in the declared
maximal-isotropic domain.

### Divisor identity

The decisive comparison remains

\[
F_\theta(z)=E_\theta(z)\tau(z),
\qquad
E_\theta(z)\ne0.
\]

No source identity derives this equality. Defining \(E_\theta\) by the quotient
is circular, and scalar analytic continuation does not supply a kernel map.

### Multiplicity completion

Even after scalar equality, preservation of local module length requires
holomorphic complement elimination through each zero and exclusion of cutoff
spectral pollution.

## Rejected shortcuts

- ordinary ungraded cone determinant: wrong inverse-Euler variance;
- holomorphic similarity to a positive G3 Gram: impossible for a nonconstant
  analytic family;
- Riesz lift of the Mellin covector: vanishes at the Xi divisor;
- tensor-unit projection: wrong kernel and erases higher-label cancellation;
- same-sign square passive pencil as the whole characteristic: zero-free on
  the seam;
- direct dynamic block \(V^\dagger B\): only a Gram compression;
- trace-class Fock completion: fails at the Euler boundary;
- imposing the adjoint source equation on the natural Evans state: already
  contains the RH confinement.

## Stopping condition

The present source corpus contains no additional typed identity from which the
range angle, seam limiting absorption, or dressed-scalar/Xi equality follows.
Further rearrangement of the same determinant and Green formulas would be
semantic repetition rather than a new constructor.

G4 remains open. G1--G3 ledger statuses remain unchanged, G5--G7 remain open,
and no RH conclusion is authorized.
