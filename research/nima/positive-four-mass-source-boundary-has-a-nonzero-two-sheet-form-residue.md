# A positive four-mass source boundary carries a nonzero pushed-form residue

On the exact rational rank-six target slice `Y[0,4]↦Y[0,4]+ε`, the first **positive-cell** boundary event is different from the previously detected nonpositive `w₈` pole. At

```
ε = 485/12278,
```
one exact inverse sheet (`q=−13/3374`) reaches **`w₄=0`**, while its other six source weights, `u`, and `t−u` are all strictly positive. Since `dw₄/dε≠0` on the inverse branch, it is approached from the positive four-pair source region on one side of this target value. The conjugate inverse sheet is distinct and has all source dlog denominator factors nonzero.

The boundary sheet has a regular row gauge and an exactly **nonzero full `8×8` source-to-target chart Jacobian**. Dividing the intrinsic oriented dlog density by this Jacobian gives a genuine **simple pole** in the algebraic two-sheet trace; its exact scalar residue is nonzero. The independently tested four-SU(4)-flavor `XXXX` coefficient also has a nonzero residue. The regular second sheet cannot cancel either one. This supplies a sourced *positive-cell pushforward* boundary pole, not merely a resultant candidate or a discriminant square-root artifact.

**Crucial distinction:** the cell is a boundary in the *eight-dimensional sourced four-mass contour*. This computation does **not** prove that its target is a boundary of the **full** nine-point positive top-cell image, nor that the full nine-point image canonical form or amplitude has this pole. Determining whether other positive top-cell preimages reach the same target remains a separate image-coverage problem.

Checker: `research/nima/checkers/check_nine_point_four_mass_positive_source_boundary_pole_slice.py`; certificate: `research/nima/results/nine-point-four-mass-positive-source-boundary-pole-slice.json`.
