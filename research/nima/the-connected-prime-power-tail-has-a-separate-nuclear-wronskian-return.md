# The connected prime-power tail has a separate nuclear Wronskian return

## Connected source summand

For each prime, retain the connected grade space

\[
E_{p,\ge3}
=\bigoplus_{k\ge3}\mathbb Ce_{p,k}
\]

inside the projective exponential Köthe source.  Set

\[
L_{p,k}=k\log p,
\qquad
w_{p,k}=\frac1k p^{-k/2}.
\]

No vector in this summand is identified with either primitive or square
endpoint coordinate.

## Cut-atom incidence

Let \(u_{p,k}\) be the exact theta tail-seam cut atom at displacement
\(L_{p,k}\).  Its analytic norm is label independent:

\[
\|u_{p,k}\|=\|\Phi\|_2.
\]

Define

\[
C_{\ge3}c
=\sum_p\sum_{k\ge3}c_{p,k}w_{p,k}u_{p,k}.
\]

The rank-one expansion is nuclear because

\[
\sum_p\sum_{k\ge3}
\|w_{p,k}u_{p,k}\|
=
\|\Phi\|_2
\sum_p\sum_{k\ge3}\frac1k p^{-k/2}
<\infty.
\]

It is also continuous on every projective exponential rung, prime/grade
diagonal, reflection equivariant, and cutoff natural.

## Wronskian return

The closed cut-atom graph has continuous normal evaluation

\[
N_0(u_{p,k})=\Phi'(k\log p).
\]

The Euler boundary derivative contributes \(2k\log p\).  Therefore define the
connected return

\[
R_{\ge3}e_{p,k}
=2k(\log p)w_{p,k}N_0(u_{p,k})\,j
\]

on the prime-labelled reciprocal-odd Wronskian generator \(j_p\). Explicitly,

\[
R_{\ge3}e_{p,k}
=\kappa_p^{(k)}j_p,
\qquad
\kappa_p^{(k)}
=2(\log p)p^{-k/2}\Phi'(k\log p).
\]

Hence the codiagonal only after the grade-labelled return, and only within one
prime fibre, is

\[
\sum_{k\ge3}R_{\ge3}e_{p,k}
=\kappa_p^{(\ge3)}j_p.
\]

The completed Wronskian target is \(\bigoplus_p\mathbb Cj_p\), not one common
unlabelled line. This realizes the connected Euler incidence without inserting
it into the two-endpoint source cell or creating cross-prime quadratic terms.

## Absolute convergence and nuclearity

Completed theta decay is faster than every exponential.  In particular, for
each fixed positive \(\delta\),

\[
|\Phi'(L)|\le C_\delta e^{-\delta L}.
\]

Thus

\[
|\kappa_p^{(k)}|
\le2C_\delta(\log p)
 p^{-k(1/2+\delta)}.
\]

The double series over \(p\) and \(k\ge3\) converges after choosing any
\(\delta>1/6\).  Therefore \(R_{\ge3}\) is nuclear into the prime-labelled Wronskian direct
sum and its prime/grade cutoffs converge absolutely.

## Retained attachment

Attach the connected return through its own graph:

\[
\Gamma_{\ge3}
=
\{(x,C_{\ge3}x,R_{\ge3}x):x\in E_{\ge3}\}.
\]

The source coordinate is retained.  Consequently this graph is a topological
embedding with closed range in the declared product rigging.  Its positive
form

\[
\|x\|_{E_{\ge3}}^2
+\|C_{\ge3}x\|_{\rm cut,G}^2
+|R_{\ge3}x|^2
\]

has zero radical.

The full first-Adams retained source graph is the typed direct sum

\[
\Gamma_{12}\oplus\Gamma_{\ge3}.
\]

Only after forming this direct sum may the fibrewise Wronskian codiagonal add
\(\kappa_p^{(\le2)}j_p\) and \(\kappa_p^{(\ge3)}j_p\). That output operation
does not identify source grades or prime labels. A later scalar evaluator that
sums over \(p\) is a separate global readout and is not part of the G1
quadratic carrier.

## Disposition

The connected \(k\ge3\) tail now has:

- a source-derived cut-atom incidence;
- a continuous normal-evaluation return;
- the exact Euler coefficient \(\kappa_p^{(k)}\);
- nuclear completed synthesis;
- prime/grade cutoff naturality;
- a closed retained graph with zero radical;
- a lawful later Wronskian codiagonal.

This closes the missing connected-tail construction on the retained saturated
architecture.  It does not prove closed range after forgetting the source
coordinate, nor does it alter the primitive-square determinant margin.  No RH
conclusion is authorized.
