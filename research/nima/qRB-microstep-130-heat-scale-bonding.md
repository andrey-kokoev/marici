# qRB microstep 130: heat-scale bonding

For `0<t_1<t_2`, the heat weights satisfy

$$
0<w_{nm}(t_2)\le w_{nm}(t_1).
$$

Thus the heat-regularized endpoint rows form a directed family under inclusion of retained pair coordinates. At every positive scale the endpoint sum is absolutely convergent, while decreasing the heat scale exposes more of the projective boundary mass.

The bonding maps preserve source labels and reciprocal pair orientation. They do not identify the numerical endpoint rows at different heat scales.

Status: positive-heat endpoint family is coherently bonded; the zero-heat projective limit remains unbounded in the naive pair norm.
