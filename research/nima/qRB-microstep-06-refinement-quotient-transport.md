# qRB microstep 06: refinement descends to the observational quotient

Let `R_XY` be a refinement map and let `Rad_X` be the observer radical. The required descent condition is

$$
R_{XY}(\operatorname{Rad}_X)\subseteq\operatorname{Rad}_Y.
$$

Equivalently, every observer at level `Y` pulled back along refinement must remain an admitted observer at level `X`.

The finite higher-coherence control test confirms the logical order: positivity alone does not identify coherencers. A jointly faithful observer is required before higher equality can be treated as proposition-valued.

Thus the next qRB rule is:

$$
\bar R_{XY}:\mathcal H_X/\operatorname{Rad}_X
\longrightarrow
\mathcal H_Y/\operatorname{Rad}_Y.
$$

Status: the finite mechanism and typed requirement are established. Infinite source-observer faithfulness and uniqueness of compatible fillers remain open; no premature contractibility claim is made.
