# Hermitian modulus comparison is closed at relative-Haar form level

Let

$$
V_s(p)=p^{-s}U_a(p)
$$

act on the relative-Haar form domain. The source-derived energy satisfies

$$
\mathcal E_{a|m}(V_s(p)\Psi)
=p^{1-2\operatorname{Re}s}\mathcal E_{a|m}(\Psi).
$$

In centered coordinates `s=1/2+z`, this is

$$
\mathcal E_z(T_p\Psi)
=p^{-2\operatorname{Re}z}\mathcal E_z(\Psi).
$$

Thus the positive modulus component of the cyclic character is already constructed. No Schur-return eigenvector equation is required. The corresponding oriented defect is

$$
D_p(z;\Psi)
=(1-p^{-2\operatorname{Re}z})\mathcal E_z(\Psi).
$$

For nonzero `Psi`, this vanishes exactly when

$$
\operatorname{Re}z=0.
$$

The analytic phase remains on the cyclic/Tate line and combines with this modulus to recover `p^(-2s)`.

The unresolved square is now purely one of state placement. For the pointed Xi lift `Delta_Phi(u_z)`, one must prove that the two routes whose difference is `D_p` are exactly the two source routes identified by the Xi-relative pair equality

$$
\Delta_-(z)-\Delta_+(z)
=\tau(z)(\Phi\otimes e^{zq}).
$$

At `tau(z)=0`, this would close the relative-Haar energy cycle on the same nonzero lifted state and force `Re z=0`. Equality modulo Xi in the abstract pair module does not alone identify the routes with the additive/multiplicative Haar square.

Status: Hermitian modulus and strict off-seam sign closed; the sole remaining gate is the typed intertwiner from the Xi pointed pair lift to the relative-Haar coherence square.
