# The local Green authority square is closed on each incidence plane; cross-plane polarization is global

## Reconciliation

The recent audit isolated a missing four-entry Green polarization. Existing source results already close that problem on each two-dimensional theta incidence plane.

Let \(V_{p,L}\) be generated by the even completed forcing and its reciprocal-odd derivative. The authorized incidence map \(\mathcal I_{p,L}\) pulls the jump-energy form back to

\[
P_{p,L}(\sigma)
=
\mathcal I_{p,L}^{*}
E_{\sigma,L}
\mathcal I_{p,L}.
\]

Mellin translation identifies this comparison with one base seam fiber. The exact endpoint trace matrix is invertible, so \(P_{p,L}(\sigma)\) is positive on \(V_{p,L}\). Graph continuity then gives

\[
c_-G_L
\le
P_{p,L}(\sigma)
\le
c_+G_L
\]

uniformly on compact off-seam parameter sets.

The rapid twisted core theorem proves density with both endpoint traces fixed, and the first-order boundary identity extends uniquely through closure. Thus the local pullback form and its represented Green boundary are already linked on the incidence plane.

## What remains outside that theorem

The full coefficient boundary quotient has two invariant planes:

\[
W_{\mathrm{wall}}
=
\operatorname{span}\{1,\delta_0\},
\qquad
W_{\mathrm{tail}}
=
\operatorname{span}\{K,V\}.
\]

The direct-sum observer keeps these planes separate. Their individual positive forms are authorized by the trace and history theorems. What is not determined locally is a cross-plane polarization

\[
C_{\mathrm{wt}}:
W_{\mathrm{tail}}
\longrightarrow
W_{\mathrm{wall}}.
\]

Declaring \(C_{\mathrm{wt}}=0\) is legitimate for the observer target norm, which was deliberately defined as a typed direct sum. It is not automatically legitimate for the joint Green system, where the source may supply wall-tail interaction.

Therefore the unresolved four-entry problem is not local Adams-edge existence. It is global assembly of the two already valid planes.

## Block theorem

Write the joint boundary Green form as

\[
G_{\partial}
=
\begin{pmatrix}
G_{\mathrm{wall}}&C_{\mathrm{wt}}\\
C_{\mathrm{wt}}^{*}&G_{\mathrm{tail}}
\end{pmatrix}.
\]

The local work establishes coercive diagonal blocks on compact off-seam sets. Positivity of the joint form is equivalent, on reduced supports, to

\[
\left\|
G_{\mathrm{wall}}^{-1/2}
C_{\mathrm{wt}}
G_{\mathrm{tail}}^{-1/2}
\right\|
\le1.
\]

Strict completion-stable assembly requires a uniform bound below \(1\).

This is precisely a mixed-cancellation or gluing-angle gate. It should not be counted again as a missing local Green polarization.

## Consequences

The first Adams edge now has:

- an explicit coefficient history;
- closed reciprocal analytic histories;
- exact endpoint traces;
- a source-authorized local pullback energy;
- closure of the first-order boundary factorization;
- compact-off-seam metric equivalence.

The next source calculation is to derive \(C_{\mathrm{wt}}\) from the five-cell assembly and test its normalized Schur contraction. If the source direct sum is orthogonal, prove that orthogonality. If not, retain the derived interaction.

## Hostiles

Two individually coercive planes can have

\[
\|G_{\mathrm{wall}}^{-1/2}C_{\mathrm{wt}}G_{\mathrm{tail}}^{-1/2}\|\to1,
\]

so the joint boundary form loses coercivity despite perfect local frames.

Conversely, forcing \(C_{\mathrm{wt}}=0\) can make the observer norm positive while deleting a source-authorized Green current needed by the Riemann scalar readout.

## Frontier

The local quadratic representation gate is closed on the declared incidence range. The programme advances to one concrete global constructor:

> Derive the wall-tail cross polarization of the five-cell joint Green form and prove its uniform normalized Schur margin.

This feeds the existing global mixed margin rather than creating a new local obstruction.
