# The Clark-defect primitive supplies the missing pointed Fourier coaction for the independent Evans bridge

## Corrected setting

Version 17 retracts the nonexistent legacy G4. The admitted analytical G4 is

\[
T_{PB}=T_{\rm pair\to border}
\]

with its unique contragredient return. Its canonical Stokes cancellation is
definitional and cannot itself be an independent Xi criterion.

The remaining typed problem is to place the independently constructed linear
Evans state in the ordered-pair source without using the desired cancellation.

## Two source-derived points

The Evans differential already supplies the fixed forcing point `Phi`. The
rung-five Clark comparison independently supplies

\[
K_1=-\frac12|q|+\delta_0.
\]

Both are defined before Xi-zero specialization. Therefore define the pointed
one-leg map, chartwise, by

\[
\epsilon_{\rm Ev}^{(j)}(v)
=
(v\otimes\Phi)\oplus(v\otimes F^jK_1),
\qquad j\in\mathbb Z/4.
\]

The first summand is the forcing-reservoir channel. The second is the
endpoint/tail channel. The map is linear in `v`; no forbidden quadratic
`v tensor conjugate(v)` occurs.

## Why one defect point spans the four missing cells

Use the already declared Fourier module

\[
F\delta_0=\mathbf1,
\qquad
F\mathbf1=\delta_0,
\qquad
FK=V,
\qquad
FV=-K,
\]

where `K=|q|` up to the fixed normalization convention. Then

\[
F^jK_1
\]

has the four chart values generated by

\[
\delta_0-\frac12K,
\quad
\mathbf1-\frac12V,
\quad
\delta_0+\frac12K,
\quad
\mathbf1+\frac12V.
\]

Their span is exactly

\[
\operatorname{span}(\mathbf1,\delta_0,K,V).
\]

Thus the fixed forcing point plus the orbit of `K_1` reaches all five cells

\[
\operatorname{span}(\Phi,\mathbf1,\delta_0,K,V)
\]

without choosing five unrelated coefficients. The relative endpoint/tail
coefficient `-1/2` is forced by the exact Clark boundary identity

\[
\partial_qK_1
=-\frac12\operatorname{sgn}(q)+\delta_0'.
\]

## Structural laws

The map is Xi-linear:

\[
\epsilon_{\rm Ev}^{(j)}(\tau v)
=
\tau\epsilon_{\rm Ev}^{(j)}(v),
\]

so multiplicities and all divisor jets are preserved. Chart transport obeys

\[
(F\otimes F)\epsilon_{\rm Ev}^{(j)}(v)
=
\epsilon_{\rm Ev}^{(j+1)}(Fv)
\]

with the declared Tate/metaplectic scalar retained when `F^4` is line-valued.
Translation gives the moving-seam point `K_(1,a)`, and the previously proved
prime majorant gives cutoff-natural projective completion.

## Independence

`K_1` was obtained from the moment/history Clark defect, not from canonical
Stokes cancellation and not from the Xi divisor. Consequently this bridge is
not the tautological reuse of the canonical relative response.

## Remaining decisive test

Compute

\[
T_{PB}\epsilon_{\rm Ev}^{(j)}(u_z)
\]

on the two stable Evans histories and compare its transverse boundary defect
with `tau(z)`. Admission requires:

1. defect divisible by `tau` but not identically zero;
2. nonzero cone energy for at least one `z` with `tau(z) != 0`;
3. the normalized wall/jump columns agree without rescaling `K_1`;
4. completion remains injective on the pointed Evans ray.

This constructs the previously missing source-derived coaugmentation candidate;
it does not yet prove its boundary comparison or RH.