# qRB microstep 45: faithful joint graph

Prior research closes the linking and radical gates on the faithful joint graph

$$
x\longmapsto(x,Ax,Cx).
$$

The source coordinate must be retained. Then the graph is closed, the source seminorm is a positive direct summand, and the common radical is zero.

The continuous ordered linking form has a unique bounded skew-adjoint operator `J_link` on the orbit carrier, with

$$
\omega_{\rm link}(f,g)=\langle J_{\rm link}f,g\rangle.
$$

Therefore the relative qRB observer can be assembled before any source-forgetting codiagonalization.

The dangerous later map is

$$
D(x,Ax,Cx)=Ax+TCx.
$$

Its kernel and fiberwise constancy of the positive joint energy must be tested separately. Positivity does not automatically descend through `D`.

Status: faithful graph realization closed; source-forgetting pushout remains the next gate.
