# Many-many conservative promotion target

The terminal RH-bearing construction is a holomorphic chain map from the Evans/Koszul residue to the conservative Green pencil:

$$
C_{\rm FP}(z)i(z)=w_{\rm FP}(z)\tau(z).
$$

At a zero `tau(z_0)=0`, injectivity of `i(z_0)` produces a nonzero state in the maximal-isotropic Green kernel. Its zero-flux Green identity and lower adjoint source equation give

$$
2a\|u_{z_0}\|^2=0.
$$

For a nonzero promoted state, this forces `a=0`, hence critical-seam confinement.

The construction requires:

1. the exact Evans/Koszul lift `i`;
2. the Green target domain and maximal-isotropic boundary relation;
3. the source-preserving lower adjoint equation;
4. holomorphic complement splitting for determinant multiplicity.

Status: terminal promotion target fixed; its construction is the RH-bearing open gate.
