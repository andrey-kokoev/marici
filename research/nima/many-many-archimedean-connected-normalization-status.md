# Many-many archimedean / connected normalization status

For prime `p` and grade `k`, the exact endpoint matrix is

$$
V_{p,k}=\frac1{2k}
\begin{pmatrix}1&p^{-k}\\p^{-k}&1\end{pmatrix}.
$$

It commutes with reciprocal exchange. Grade two is uniformly faithful and Hilbert–Schmidt; connected grades `k>=3` are invertible in the source-weighted grade topology and nuclear.

The connected return tail converges holomorphically in trace norm on

$$
\operatorname{Re}z>-\frac16.
$$

The third-order determinant anomaly is the exact cocycle

$$
\alpha_3(A,B)
=\operatorname{tr}\left(A^2B+AB^2+\frac12ABAB\right),
$$

and obeys the associative two-cocycle identity. It supplies path-independent determinant transport after primitive and square counterterms.

A scalar anomaly-line trivialization is unnecessary for maximal-isotropic response descent. Coherent line-valued or metaplectically twisted transport suffices.

The remaining source check is identification of the declared G4 archimedean Tate transition and anomaly-line character with the common horizontal cocycle, without double counting the reciprocal `det_2` return.

Status: connected normalization, ideal convergence, and finite anomaly cocycle closed; common archimedean line-character identification remains open.
