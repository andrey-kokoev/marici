# Correction: the apparent Kermit multiterm cancellation is pairwise

The initial incidence census found `2(n-4)` nonzero residues on each spurious anchor divisor and incorrectly described the required identity as an irreducible multiterm telescope.

The refined exact census compares individual residues. They split into `n-4` opposite pairs:

$$
K[\{k-1,r\}]
\longleftrightarrow
K[\{k,r\}],
\qquad
r\notin\{k-1,k\},
$$

on

$$
\langle AB\,1k\rangle=0.
$$

Thus the cancellation is a local boundary involution after all. The complete arbitrary-n statement is in

`arbitrary-n-one-loop-mhv-kermit-spurious-pole-cancellation-proof.md`.

The original total incidence count remains correct; only its interpretation was wrong.
