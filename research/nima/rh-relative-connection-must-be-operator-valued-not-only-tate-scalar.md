# The RH relative connection must be operator-valued, not only Tate-scalar

Author: `marici.Nima`

Date: 2026-08-26

Status: exact determinant-line information-loss theorem

## Three levels of transport

The moving-cone result requires a relative connection between the two sector
bundles. There are three distinct algebraic levels:

1. additive current transport;
2. scalar multiplicative transport in \(GL(1)\);
3. ordered state--observer transport in \(GL(V)\) or an operator category.

The local Tate transition \(\gamma_p(s)\) lives at the second level. Its
logarithmic derivative is a determinant-line connection. It is invertible and
unitary on the critical seam, but it does not specify the full transport of
the rank-three Lagrangian comparison.

## Full relative connection

When the cross-projection \(\mathcal O_z\) is invertible, its operator-valued
connection is

\[
\mathcal A_z
=
\mathcal O_z^{-1}d\mathcal O_z.
\]

The induced determinant-line connection is only its trace:

\[
d\log\det\mathcal O_z
=
\operatorname{tr}\mathcal A_z.
\]

The trace forgets the traceless shear, squeeze, and rotation channels. Those
channels control the individual principal angles and hence the observability
margin.

Therefore an exact scalar \(\gamma\)-normalization can sew determinant lines
while leaving the relationship geometry undetermined.

## Exact squeeze hostile

Consider the rank-three family

\[
\mathcal O_r
=
\operatorname{diag}(e^r,e^{-r},1).
\]

Its determinant is one for every \(r\), and

\[
\operatorname{tr}
(\mathcal O_r^{-1}\partial_r\mathcal O_r)=0.
\]

Thus its scalar determinant connection is identical to that of the constant
identity family.

Nevertheless its smallest singular value is

\[
e^{-|r|},
\]

which tends to zero. The family remains invertible at every finite \(r\) but
loses every uniform observability margin as \(|r|\) grows.

The determinant line sees no motion while the full state--observer geometry
undergoes an unbounded hyperbolic squeeze.

An exact rational cutoff model is

\[
\mathcal O_N
=
\operatorname{diag}(N,N^{-1},1).
\]

It has determinant one and smallest Gramian eigenvalue \(N^{-2}\).

## Consequence for Tate sewing

The local Tate factor correctly types reciprocal chart normalization and its
scalar phase. It cannot by itself parallelize the complete amplitude frame or
control Maslov distance.

The source must construct an operator lift

\[
\Gamma_p(s):V_+(s)\longrightarrow V_-(s)
\]

whose determinant is compatible with \(\gamma_p(s)\), while its traceless
part acts on tail, seam, and boundary-current directions with declared
domains.

The determinant condition is necessary:

\[
\det\Gamma_p(s)=\gamma_p(s)^{m}
\]

for the source-derived multiplicity \(m\). It is not sufficient. Different
operator lifts with the same determinant can have arbitrarily different
observability margins.

## Relation to the four-stage composite

The theta smoothing correspondence cannot be compiled as a scalar route
annotation. It must remain an ordered transformation effect until after:

- Fourier parity exchange;
- seam retention;
- arithmetic-current incidence;
- archimedean completion;
- generalized-metric transport;
- cross-projection formation.

Only then may its determinant-line shadow be taken. Taking the scalar Tate
factor earlier is a lossy functor that erases precisely the chamber data RH
needs.

## Finite source gate

At each finite arithmetic cutoff, construct the full transport matrix and
record:

1. its determinant-line factor;
2. its traceless connection component;
3. its smallest generalized singular value;
4. its parity and seam block structure;
5. its behavior under both prime-addition orders.

The finite falsifier is a determinant-correct matrix family whose smallest
generalized singular value collapses. The rational squeeze family supplies
the minimal hostile.

## Categorical interpretation

The determinant functor is not faithful on transport. It maps the
noncommutative route effect to a scalar multiplicative effect and forgets the
ordered deformation of the state--observer frame.

The missing RH coherencer therefore lives before the determinant functor. It
is an operator-valued natural transformation between the two rigged
half-spinor sector functors.

## Verification

The checker verifies exact determinant one, zero logarithmic trace, and
collapsing observability margin for the rational squeeze family. It compares
that family with the identity family, which has the same scalar determinant
record and a constant margin.

