# Prior art isolates one internal tau-p lift test

The sourced blow-up exceptional face supplies only the column `(0,1)` in rows
`(Xi_log,-sigma123)`. The universal target is `(1,1)`. Residues uniquely fix
the missing Xi leg after orientation, but do not lift it before residue.

The strongest relevant earlier construction is Benincasa's physical
normal-adapter Čech class. Three gradient-pivot charts glue by Cartan
primitives `i_W(omega)` on overlaps, and the second-covariant packet supplies
explicit triple homotopies. This suggests a bounded constructor:

> form the mapping cone of the source gradient-pivot normal-adapter Čech
> complex restricted to the ordered blow-up exceptional wall face.

It must construct the comparison of covers, strict-transform the overlap
primitives, produce integral column `(1,1)`, survive Rees shears, and pass the
full total-square and two-prime tests.

Hostiles are decisive: an ordinary Laurent Cartan contraction has zero double
residue; the exceptional face alone is `(0,1)`; raw Rees coefficients vary
continuously under allowed shears; and nonunit multiples do not kill the
primitive integral class. No tau-p lift or physical period is claimed.
