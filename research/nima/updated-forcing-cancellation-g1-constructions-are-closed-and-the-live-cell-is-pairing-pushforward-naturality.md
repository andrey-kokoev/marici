# Updated forcing cancellation: G1 constructions are closed and the live cell is pairing/pushforward naturality

Newer retained-architecture results supersede the earlier open-construction ledger.

The primitive/square sector is already realized by the closed source joint graph

$$
\Gamma_{p,12}
=\{(A_{p,12}x,C_{p,12}x):x\in E_{p,12}\},
$$

with exact grade coefficients and zero radical. The connected sector already has the nuclear return

$$
R_{\ge3}e_{p,k}=\kappa_p^{(k)}j_p,
$$

and a closed retained graph. Prime diagonality and cutoff naturality are exact before terminal scalar evaluation.

Therefore the missing forcing-difference cancellation is not construction of the low-grade mate or connected return. It is the naturality of the oriented mixed pairing under typed prime pushforward.

Let the local two-chart cell have complete block

$$
M_p=
\begin{pmatrix}
G_P&B_p^*\\
B_p&G_Q
\end{pmatrix},
$$

where `B_p` retains primitive-to-square orientation. Let `Pi_p` be the typed prime incidence acting on all blocks. The required source cell is

$$
\boxed{
\Pi_p\bigl(\operatorname{Pair}(\mathscr C_p)\bigr)
\Longrightarrow
\operatorname{Pair}\bigl(\Pi_p\mathscr C_p\bigr).
}
$$

At matrix level, its decisive component is

$$
\Pi_{Q,p}B_p\Pi_{P,p}^*
=B_p^{\rm arith},
$$

with the reciprocal mate carried separately rather than silently replacing `B_p` by `B_p^*`.

After evaluation on the retained Xi/Green source state, the boundary of this cell must be

$$
-\bigl(F_{+,p}(w,z)-F_{-,p}(w,z)\bigr).
$$

Summing is allowed only after this primewise typed equality and after grade codiagonalization within each fixed prime. A scalar agreement of Euler coefficients or Wronskian traces cannot establish the cell because it cannot distinguish `B_p` from `B_p^*`.

Thus the exact finite residual is the oriented mixed block

$$
\mathcal N_{p,X}
=B_p^{\rm arith}
-
\Pi_{Q,p}B_p\Pi_{P,p}^*.
$$

It must vanish in both analytic-transpose and Hermitian lanes before cyclic trace. This is the current source-derived two-height coherence gate coupling the positive Green system to arithmetic transport.

Status: retained primitive/square/connected constructions closed as candidates; pairing-versus-prime-pushforward naturality remains open.
