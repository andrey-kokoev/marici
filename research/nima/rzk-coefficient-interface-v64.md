# v64: mixed-edge attachment and conductor costalk

Independent replays passed 72,862 checks and 349,317 new plus 337,571 inherited
checks.

`rzk/88-mixed-short-edge-generic-attachment.rzk.md` records the three degree-one
mixed-edge classes, their common native degree-two connecting image, and the
primitive row `(1,1,1)`. Individual edges have primitive short-support
transgressions and cannot lift to the endpoint quotient, while the complete
native cap lifts with both endpoint maps and has three endpoint-preserving short
presentations.

`rzk/89-milnor-totalization-conductor-costalk.rzk.md` records the rank-34 affine
costalk mapping lattice. Its counit has saturated rank-three image and rank-31
kernel, exactly the surviving normalization-extension obstruction lattice.
Invariant ranks are 11, 1, and 10. The structure-module Milnor totalization is
equivalent to the base node and has no same-degree map, so it removes no further
obstruction.

Both Rzk modules pass fresh checks. The outstanding physical step remains the
Rees/Gysin transport of the complete coupled triangle and selection of a
costalk primary plus residual-kernel position.
