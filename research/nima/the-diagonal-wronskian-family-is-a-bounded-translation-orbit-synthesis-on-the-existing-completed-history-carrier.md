# The diagonal Wronskian family is a bounded translation-orbit synthesis on the existing completed history carrier

## Question

Does the infinite rank of the diagonal Wronskian family require an unrelated
new carrier, or can it be realized inside an existing source-authorized history
module?

## Claim boundary

It is realized canonically in the translation-cyclic history module generated
by the completed theta atom and its derivative. The shell Wronskian is a
Bochner integral of translated copies of these two seeds, with an explicit
norm bound. This constructs the radial carrier and source synthesis map. It
does not identify that carrier with the scalar G4 wall/incidence port or prove
the final arithmetic codiagonal.

## Translation form of the local current

Let translation on the separation coordinate be

\[
 (T_uf)(t)=f(t+u).
\]

For one theta label \(n\), the local Wronskian density is

\[
 w_{n,u}(t)
 =\Phi_n'(u)\Phi_n(u+t)
 -\Phi_n(u)\Phi_n'(u+t).
\]

Therefore

\[
 w_{n,u}
 =\Phi_n'(u)T_u\Phi_n
 -\Phi_n(u)T_u\Phi_n'.
\]

The infinite shell-location family is thus a translation orbit of only two
source seeds, not an arbitrary collection of response functions.

## Shell synthesis

Integrating over a shell gives

\[
 \mathcal W_n^{[a,b]}
 =\int_a^b
 \left[
 \Phi_n'(u)T_u\Phi_n
 -\Phi_n(u)T_u\Phi_n'
 \right]du.
\]

Define the radial history module

\[
 \mathcal H_{n,\rm rad}
 =\overline{\operatorname{span}}
 \left\{
 T_u\Phi_n,
 T_u\Phi_n':u\in\mathbb R
 \right\}
 \subset L^2(\mathbb R,dt).
\]

Then every shell current belongs to \(\mathcal H_{n,\rm rad}\).

## Boundedness

Translations are unitary on the full-line history carrier. Minkowski's
inequality gives

\[
 \|\mathcal W_n^{[a,b]}\|_2
 \le
 \|\Phi_n\|_2\int_a^b|\Phi_n'(u)|\,du
 +
 \|\Phi_n'\|_2\int_a^b|\Phi_n(u)|\,du.
\]

The completed atoms and all their derivatives decay superexponentially on the
positive shell side, so the right side is finite. It is additive under shell
concatenation.

More generally, for a finite signed shell measure \(\mu\),

\[
 \mathcal J_{n,\rm rad}\mu
 =\int
 \left[
 \Phi_n'(u)T_u\Phi_n
 -\Phi_n(u)T_u\Phi_n'
 \right]d\mu(u)
\]

defines a bounded map from the weighted variation norm

\[
 \int\left(|\Phi_n'(u)|+|\Phi_n(u)|\right)d|\mu|(u)
\]

into the radial history module.

## Compatibility with existing history transport

The repository's completed theta history is translation convolution and
commutes with every logarithmic translation. Graph closure and reciprocal
half-density transport also preserve the translation action. Therefore
\(\mathcal H_{n,\rm rad}\) is not an externally appended function space: it is
a cyclic submodule of the already retained completed history carrier.

Theta-label translation gives

\[
 \Phi_n(u)=n^{-1/2}\Phi_1(u+\log n),
\]

so all \(\mathcal H_{n,\rm rad}\) are unitarily transported copies of the
first-label radial module, with the source half-density coefficient retained.

## Orientation

Slot reversal sends

\[
 \Phi_n'(u)T_u\Phi_n
 -\Phi_n(u)T_u\Phi_n'
\]

to its negative. Thus the synthesis carries the analytic-transpose orientation
before any scalar readout. No fitted phase is required.

## Laplace rigging

The shell currents decay faster than every exponential on the positive
separation ray. Hence

\[
 W_n(z)
 =\int_0^\infty e^{-zt}\mathcal W_n^{[a,b]}(t)\,dt
\]

is entire, and parameter differentiation is obtained by inserting powers of
\(-t\). The radial synthesis therefore retains the full Evans multiplicity-jet
family.

This entire readout uses the rapid radial submodule, not arbitrary \(L^2\)
evaluation.

## Remaining interface

The construction supplies the first missing arrow

\[
 \mathcal G_{\theta,\rm pair}^{[a,b]}
 \xrightarrow{\mathcal J_{\rm rad}}
 \mathcal H_{\rm rad}.
\]

The unresolved arrow is

\[
 \mathcal H_{\rm rad}
 \longrightarrow
 \mathcal G_{\rm wall,inc}^{\rm enlarged},
\]

including the frozen G4 metric, prime/grade loading, and physical arithmetic
codiagonal. A scalar wall/incidence plane remains too small unless it is the
boundary trace of this retained history graph.

## Hostile

A proposed enlargement fails if it introduces a radial function space without
showing that the Wronskian family is generated by source translations, or if
it scalarizes the translation orbit before Laplace jets are formed.

## Disposition

The required infinite-dimensional diagonal carrier already exists as a
translation-cyclic submodule of the completed theta history. The shell-to-radial
synthesis is explicit, bounded, orientation-preserving, and compatible with
label transport. Candidate one is narrowed to attaching this retained radial
history graph to the G4 wall/incidence boundary and arithmetic codiagonal. No
RH conclusion is authorized.
