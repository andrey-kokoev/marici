# The Literal Cosmology Terminal Readouts Are Not Jointly Conservative

After the canonical residue and infinity-Gysin stages, the residual domain is
the rank-seven algebraic kernel \(T_7\).

Entry 289 proves that generic nonsoft total-energy nearby monodromy is
identity on \(T_7\), so its nilpotent readout has rank zero there. The frozen
literal Bunch--Davies contour is one relative chain and therefore defines at
most one scalar functional on \(T_7\).

Consequently,

\[
\operatorname{rank}
\begin{pmatrix}
N_{E=0}|_{T_7}\\
\langle\Gamma_{\rm BD},-\rangle|_{T_7}
\end{pmatrix}
\le1,
\]

and hence

\[
\dim\ker\ge7-1=6.
\]

Thus the literal terminal pair fails the filtered joint-conservativity test
without requiring its unknown matrix.

This does not falsify the sequential carrier architecture. It falsifies the
stronger claim that the already named total-energy nearby cycle and one
physical chamber recover the full algebraic coefficient sector. A rescue
requires a larger family of physical cycles or a different terminal readout,
declared from source geometry before inspecting this residual kernel.
