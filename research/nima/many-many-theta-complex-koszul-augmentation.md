# Many-many theta-complex Koszul augmentation

Start with the source-authorized two-term complex

$$
K_\tau:
0\to\mathcal L_\theta\xrightarrow{\tau}\mathcal O_U\to0,
$$

where `tau` has scalar coordinate `xi(s)`.

Augment it by the labelled analytic blocks through a quasi-isomorphic resolution:

$$
T_\theta^\bullet
\twoheadrightarrow
K_\tau.
$$

The augmentation must preserve:

1. the determinant line and its section `tau`;
2. the Xi divisor and local multiplicities;
3. reciprocal involution;
4. prime, grade, shell, and theta labels;
5. the four-block ordered polarization;
6. graph-domain closure.

The kernel of the augmentation is required to be acyclic. This makes the additional many-many history data homological resolution data while the canonical Xi divisor remains unchanged.

The first concrete extension is the two-term Koszul complex together with the six-coordinate labelled prime carrier and its chain-return map. Higher primitive/square/connected blocks are attached only when their adjacent compositions satisfy the differential-square relation.

Status: theta-complex construction anchored to the canonical Koszul core; acyclic augmentation remains to be constructed.
