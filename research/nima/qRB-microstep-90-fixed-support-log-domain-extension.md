# qRB microstep 90: fixed-support logarithmic-domain extension

The compact smooth source identity extends to the fixed-support logarithmic domain

$$
\mathcal H_{\log,L}
=\left\{f:\operatorname{supp}f\subset[-L,L],\;
\int\log(2+|u|)|\mathcal Ff(u)|^2du<\infty\right\}.
$$

The gamma multiplier is controlled by the logarithmic weight, translations are unitary, and endpoint functionals are bounded on fixed support. Interior smooth approximation therefore preserves the endpoint, gamma, and finite prime terms.

Consequently the explicit identity `Q_L=E+G+P_L` holds on the completed fixed-support logarithmic domain, not only on `C_c^infty`.

Status: fixed-support graph-domain extension closed internally; the global noncompact wall completion and external normalization audit remain separate.
