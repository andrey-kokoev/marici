# The missing prism sides share one codomain change from relative Green proxies to the independent global Green system

## Local side inventory

The available degree-three cells do not fail independently.

- The corrected pair trace cell supplies the internal `SAC` comparison.
- The polarized `C34` route supplies an `ACG` comparison only with the retained
  Wronskian/relative Green packet as its `G` vertex.
- The Stieltjes endpoint lift and Pauli frame supply an `SAG` comparison only
  in the reduced cyclic Green cell.

The unresolved `SCG` face asks directly for the independent arithmetic Green
bulk.

Thus the last three faces use different objects bearing the name `G`:

\[
G_{\rm Wr},
\qquad
G_{\rm cyc},
\qquad
G_{\rm ind}.
\]

They cannot be inserted into one prism until their codomains are compared.

## One higher comparison

The economical missing datum is a natural Green-system comparison

\[
\eta_G:
\{G_{\rm Wr},G_{\rm cyc},G_{\theta}\}
\Longrightarrow
G_{\rm ind}
\]

that preserves:

- the retained base state;
- prime labels and cutoffs;
- reciprocal orientation;
- endpoint and Wronskian traces;
- the polarized Green form;
- radical quotients;
- the forward/backward phase action.

Whiskering the existing local triangle cells by `eta_G` would place them in one
common target:

\[
H_{ACG}^{\rm rel}
\xRightarrow{\eta_G}
H_{ACG}^{\rm ind},
\qquad
H_{SAG}^{\rm cyc}
\xRightarrow{\eta_G}
H_{SAG}^{\rm ind}.
\]

Naturality of `eta_G` against the source and characteristic routes would also
supply the missing `H_SCG` face.

## Higher-homotopy meaning

This is an equivalence between systems governing the lower rungs, rather than
another signal inside one system. Its coherence law is a modification between
triangle homotopies. In the two-phase picture it occupies the side structure
of

\[
\Delta^1\times\Delta^3.
\]

For the complete rung-one-through-rung-four construction laws it promotes to
the five-dimensional comparison

\[
\Delta^1\times\Delta^4.
\]

## Obstruction class

Before `eta_G` is constructed, the oriented prism boundary lands in unmatched
Green codomains. After transport to a common codomain, its top component is the
candidate obstruction

\[
\mathcal R_p(b_z)
=(1-p^{-2\operatorname{Re}z})E_p(b_z).
\]

Thus the residual can be interpreted as the naturality defect of the proposed
Green-system equivalence. This interpretation does not make it vanish; it
identifies the higher cell whose boundary would contain it.

## Concrete finite test

On the rank-two endpoint plane, `eta_G` must first identify the complete four
polarized matrix units of the cyclic/relative and independent Green forms and
intertwine valuation transport. This combines the previously separate Gram and
star gates into one naturality test. If it fails at one matrix unit or at
`p=2,3` trace-square, no higher prism filler exists.