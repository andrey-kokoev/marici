# v111: soft D1 labelled derived fibre

The labelled total-complex and derived-fibre Rust checkers were freshly replayed.

`rzk/139-soft-d1-labelled-derived-fibre.rzk.md` records that the full local
gradient lift has square-zero total differential when the principal cell and
source labels are retained. The even resonant object is a Tor object of Cartier
length two; the odd object is a cokernel of Cartier length one. Their total local
length is three and reduced rank is two.

This types the complete local labelled derived fibre and rules out using the
singularity category alone. The global quartic-tail transport remains
uncomputed, so local square-zero does not yet provide the global road-Cech map.

The Rzk module passes all eight declarations with no assumptions.
