# Correction: global port Gram identity must retain the bulk state leg

The unaugmented proposal

$$
A^*A=B^*B
$$

for endpoint-plus-difference and input-sum ports is false on the full history carrier. The exact source identity is

$$
\langle Au_w,Au_z\rangle
-
\langle Bu_w,Bu_z\rangle
=(z+\overline w)\langle u_w,u_z\rangle.
$$

Thus the port Gram operators differ by the bulk state kernel. Erasing that term would erase the spectral normal coordinate that confinement must detect.

After a Cayley transform from the half-plane to the disk, the correct lurking-isometry identity has the augmented form

$$
I-\Theta(w)^*\Theta(z)
=(1-\overline{\lambda(w)}\lambda(z))F(w)^*F(z).
$$

Equivalently, the isometry acts on vectors containing both a boundary port and a spectral-state leg, schematically

$$
\binom{\lambda(z)F(z)e}{e}
\longmapsto
\binom{F(z)e}{\Theta(z)e}.
$$

The noncircular source theorem is therefore equality of these augmented Gram matrices for arbitrary heights. The Xi stable-history relation may then be inserted into the augmented identity; it cannot justify an equality of unaugmented boundary ports.

For the doubled theta system, every primitive, square, seam, archimedean, reciprocal, and linking feature must occur in `F(z)` or `Theta(z)` with its declared variance. The first finite falsifier is the residual

$$
\mathcal R_X(w,z)
=I-\Theta_X(w)^*\Theta_X(z)
-(1-\overline{\lambda(w)}\lambda(z))F_X(w)^*F_X(z).
$$

One nonzero labelled matrix entry rejects the proposed colligation.

Status: unaugmented Gram equality retracted; augmented two-height colligation identity is the valid source target.
