# Many-many fixed-shell source faithfulness

For every nontrivial compact shell `[a,b]`, the ordered theta-pair synthesis has trivial intersection with the radial codiagonal kernel:

$$
\ker(C_{\rm radial})\cap\operatorname{im}(\mathcal I_\theta)=\{0\}.
$$

The proof extracts the least shifted label by endpoint asymptotics. Gaussian separation removes higher shifted labels, analyticity removes the corresponding row, and completed-atom independence removes every ordered coefficient.

Thus the balanced endpoint–Wronskian channel is absent from the fixed-shell theta source. The retained many-many graph is codiagonally faithful on each fixed shell.

The global extension requires compatibility across shell refinement and control of the noncompact tail.

Status: fixed-shell source intersection gate closed.
