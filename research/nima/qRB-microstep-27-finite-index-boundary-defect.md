# qRB microstep 27: finite-index boundary defect

The boundary kernel need not be positive on the unrestricted carrier to define the relative geometry. Its natural status is a finite-index Krein/Pontryagin boundary object.

The relative construction keeps the decomposition

$$
K_S=K_+-K_-+K_{\rm rel},
$$

where `K_+` and `K_-` are positive carrier contributions and `K_rel` is the signed boundary readout. The negative boundary index is recorded rather than silently absorbed.

A positive Hilbert boundary realization would require a contractive source-derived absorption of `K_-`. That is a separate RH-strength upgrade.

Status: finite-index defect retained as admissible relative data; no positive compression is claimed.
