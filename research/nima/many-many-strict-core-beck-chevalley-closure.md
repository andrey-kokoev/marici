# Many-many strict-core Beck–Chevalley closure

On the transverse retained source core, both operation orders use the same canonical factor identification. Define

$$
\beta_{\rm strict}=I_{HV}=I_{VH}.
$$

Then

$$
\beta_{\rm strict}HV=VH,
$$

and every side-face restriction commutes with `beta_strict`. Consequently

$$
\Omega_{ij}=0
$$

for affine pullback, multiplicative shell refinement, reciprocal exchange, and finite source restriction.

This closes the strict-core order-exchange cell. Loaded, nontransverse, endpoint, and physical-mode comparisons remain in the curved layer and require explicit comparison cells.

Status: strict-core Beck–Chevalley coherence closed.
