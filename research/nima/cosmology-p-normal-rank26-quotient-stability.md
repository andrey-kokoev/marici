# Raw p-normal relation derivatives vanish through degree fourteen

The rank-26 quotient test was independently extended from ambient degree 8 to
10, 12, and 14 over both `F_32003` and `F_32009`. In every run the special
exact image plus the derivative span along the p-tangent direction
`(1,-1,0)` absorbs all derivatives along both unit p-normals.

The normal extension rank is zero in all eight degree/prime runs. The tangent
over-special ranks are respectively `97,101,107,113` at degrees
`8,10,12,14`.

This closes the raw-row adapter through degree fourteen: it supplies no line
to the tau horn. It does not negate the separate rank-13 Rees support census.
Those thirteen supported summands, including seven length-one summands, are
cokernel structure and have not been explicitly extracted. Their connecting
maps therefore remain uncomputed.

The next legitimate alternatives are explicit extraction of those seven Rees
generators, or an unbounded moving-relation/Euler coherence theorem proving
normal absorption. No tau lift or physical period is claimed.
