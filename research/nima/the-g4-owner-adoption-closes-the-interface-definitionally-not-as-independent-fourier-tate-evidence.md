# The G4 owner adoption closes the interface definitionally, not as independent Fourier--Tate evidence

## Contract update

Earlier retained-graph audits treated an authoritative G4 forward map as
absent. That status is superseded by:

- `research/aspect/contracts/u-g4-owner-strict-authorization.v1.json`;
- `research/aspect/contracts/u-g4-owner-construction-packet.v5.json`;
- `research/aspect/contracts/theta-rh-interaction-net-state.v17.json`;
- `research/aspect/contracts/theta-rh-interaction-net-state.v18.json`.

The admitted analytical G4 forward constructor is now

\[
U_{G4}:=T_{\mathrm{pair\to border}}
\]

on the rapid projective ordered-pair carrier, with the unique contragredient
return on the declared pairings.

There is no independently existing legacy G4 operator to compare against.
Expressions involving `U_G4^old-T_PB` are therefore undefined rather than
open comparison defects.

## Consequence for the ordered port

The ordered port is already the retained odd bilateral-history coordinate

\[
S_{\mathrm{ord}}=-2H_{\mathrm{jump}}.
\]

The adopted G4 source is the oriented half-density pair, and its forward map
is the constructed pair-to-bordered radial crossing. Hence the ordered
Fourier--Tate naturality cell enters the admitted G4 packet through the same
retained pair carrier. No additional external sewing identification is
required for this adopted analytical object.

The bordered target retains

\[
(\rho_0,E,W,R),
\qquad
zR-\rho_0=E-\frac12W,
\]

so the endpoint and full response coordinates are not reduced to value
sampling. The lower row is the contragredient of the same forward map, not an
independently chosen adjoint loading.

## What is closed

For the adopted analytical packet, the following are closed by construction
and the retained-graph theorems:

1. the source-retaining common domain;
2. ordered odd-history incidence;
3. reciprocal and Fourier transport;
4. primitive, square, connected, and archimedean typed attachments;
5. projective completion and all finite parameter jets;
6. the forward G4 square and contragredient return.

Thus the former obstruction pair `{iota,Q_G4}` is not the current frontier in
this packet. The rapid test carrier is the observer/source-pair rigging, while
the actual retained states live in the jointly closable source-pulled graph.

## Independence boundary

The strict identity

\[
U_{G4}=T_{\mathrm{pair\to border}}
\]

is an owner-authorized definition. It is not independent evidence that a
separately derived physical G4 realization equals the retained crossing.
Accordingly, canonical cancellation in this packet is definitional.

Version 18 records the resulting no-go: the canonical Stokes section lies in
the G4 kernel for every parameter, so the unreduced determinant is identically
zero and cannot detect the Xi divisor. The live spectral candidate is a
normalized Grushin reduction transverse to this global kernel line.

## Corrected frontier

The Fourier--Tate transport and completed boundary augmentation are closed for
the admitted analytical G4 packet. The remaining research questions are now:

- construct a source-derived transverse left-kernel line and Grushin
  normalization;
- prove fixed Fredholm index and determinant-class control;
- prove locally uniform finite-shell convergence without spectral pollution;
- identify the reduced determinant with a nowhere-zero factor times Xi;
- or independently link an external physical G4 realization, if one is later
  declared.

None of these follows from the definitional forward square, and no RH
implication is obtained from the canonical cancellation.
