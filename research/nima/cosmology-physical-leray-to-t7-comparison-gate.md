# The Leray Germ Does Not Directly Restrict to T7

The full bivariate connection proves that two scalar covectors can be jointly
conservative on

\[
T_7=\langle e_1,\ldots,e_6,v_{\rm alg}\rangle,
\]

although one constant covector has replicated ceiling six in the bounded
census.  This does not yet establish physical complementarity.

Entries 365--366 construct three cyclic boundary-value Leray germs and prove
their occurrence-level naturality.  Their durable artifacts do not provide a
comparison map into the absolute algebraic kernel dual.  Moreover, the literal
positive real chain has zero incidence with every marked Cut support.  Hence
neither the Leray germs nor the algebraically found conservative pair may be
assigned coordinates in \(T_7^\vee\) by inspection.

The tempting direct map

\[
R_{\rm Leray}:\operatorname{span}
\{\gamma_{12},\gamma_{23},\gamma_{31}\}
\longrightarrow T_7^\vee,
\]

is mistyped.  The Leray germs are dual to the wall quotient

\[
W_3=M_{12}/M_9.
\]

Their pullbacks to \(M_{12}\) annihilate \(M_9\), and therefore annihilate
\(T_7\subset M_9\).  Assigning them nonzero absolute \(T_7\)-coordinates
would choose a splitting of the localization sequence.

The admissible missing datum is instead the supported connecting operation of
the full rank-twelve extension: its extension block, nearby-cycle boundary, or
Gysin comparison may land in \(T_7\) without a splitting.  One such operation
is already source-derived at the soft node and selects \(e_6\); its full
bivariate covector orbit has rank two.

The corrected finite falsifier is to derive further supported connecting rows
from the rank-twelve localization complex and stack their bivariate orbits with
the \(e_6\) row.  Joint rank seven establishes physical complementarity; lower
rank falsifies the declared family.  Direct Leray restriction, fitted
coordinates, or a split localization sequence are inadmissible substitutes.
