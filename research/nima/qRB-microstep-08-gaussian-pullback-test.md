# qRB microstep 08: Gaussian observer pullback

For refinement `R_XY`, the concrete descent test is

$$
\omega_{Y,t}\circ R_{XY}=\omega_{X,t'}
$$

(or a finite admitted linear combination), for every translated Gaussian observer and endpoint functional.

This is the smallest source-level test of

$$
R_{XY}(\operatorname{Rad}_X)\subseteq\operatorname{Rad}_Y.
$$

The existing faithfulness result says that retaining all fixed-width Gaussian translates separates tempered distributions. It does not by itself prove that every refinement pulls those observers back into the same admitted family.

Status: contract formulated; analytic pullback identity remains the next open microstep. No completion or positivity upgrade is inferred.
