# v72: native overlap and W03 relative interval

Independent replays passed 25 structural assertions and 11,724 chain-level
checks.

`rzk/98-native-overlap-relative-interval.rzk.md` records that the native
normalization branch has three independent conormal directions per sheet,
whereas the one-coordinate universal stalk branch has only one. Equivariantly
that one direction reaches only the cyclic diagonal. Consequently the native
six-term first symbol and two-dimensional punctured overlap residue cannot
factor through the one-coordinate model without additional comparison data.

For the W03 relative interval, reciprocal evaluation followed by endpoint
quotient preserves two excess classes via `-beta I_2`. Projection to scalar
homology has row `(1,1)`, identifying those classes and killing their primitive
relation-only difference. The complete occurrence complex retains a primitive
nonsplit two-extension with Postnikov class one.

The Rzk module passes a fresh check. The relative coefficient projection is not
yet the physical tangential PC trace, and the universal one-coordinate stalk
cannot substitute for the native three-occurrence normalization geometry.
