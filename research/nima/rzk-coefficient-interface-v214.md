# v214: normalized physical amplitude witness

The mixed-variance connector, primitive physical pullback class, and explicit target detector rows now produce the normalized six-point packet image

`s + W + v`.

Module 236 packages this image equality together with the already established packet trace law. By path functoriality, the physical residue is

`-beta * (1 + (1 + 1))`,

that is `-3 beta` in an ordinary ring interpretation. This is a concrete selected physical amplitude witness in the normalized C3-invariant positive-sheet frame.

The construction does not turn the one physical line into a three-parameter source. The more general formula `-beta*(a+(b+c))` remains established on the selected coefficient packet and applies to any future physical class whose detector row evaluation is `(a,b,c)`. The concrete currently inhabited detector triple is `(1,1,1)`.
