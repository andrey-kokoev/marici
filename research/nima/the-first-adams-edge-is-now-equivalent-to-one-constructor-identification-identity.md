# The first Adams edge is now equivalent to one constructor-identification identity

## Two objects now available

There are two independently constructed analytic objects on the
prime-labelled disagreement sector.

### Topological relation

The window disagreement vectors

\[
d_p=W_{2\log p}-W_{\log p}
\]

define a trace-class strict contraction

\[
J_{\mathrm{top}}:
\ell^2(\mathbb P)
\longrightarrow
\mathcal W_{\mathrm{dis}},
\qquad
J_{\mathrm{top}}e_p=d_p.
\]

Its inverse graph is a closed densely defined relation.

### Constructor transport

The window-to-theta chain produces the exact finite translation defect

\[
\mathcal R_p
=
U_L
\left[
-2L(A+1)D+L^2D^2
\right],
\qquad
L=\log p.
\]

The remaining declared constructors are:

- half-density transport \(\mathcal M_n\);
- theta label synthesis;
- the Wronskian wall and odd columns;
- ordered-port resolution;
- tail convolution \(H_K\);
- the completed history graph resolvent.

Their composition defines a source candidate

\[
J_{\mathrm{src}}
:
\ell^2(\mathbb P)
\dashrightarrow
\mathcal W_{\mathrm{dis}}
\]

once all domains and coefficient placements are frozen.

## The exact missing identity

The first Adams relation is source-authorized precisely when

\[
J_{\mathrm{src}}
=
J_{\mathrm{top}}
\]

on the finite prime core, with the reciprocal sign and Euler coefficients in
their declared ports.

Equivalently, for every finite prime packet \(c\),

\[
J_{\mathrm{src}}c
=
\sum_p c_p
\left(
W_{2\log p}-W_{\log p}
\right).
\]

This is the irreducible constructor-identification theorem.

## Quadratic equivalent

Let \(D_\pm\) be the reciprocal theta-history auxiliary blocks. The
equivalent mixed Green statement is

\[
b_{\mathrm{window}}(c,d)
=
\left\langle
J_{\mathrm{src}}c,
D_\pm^{-1}J_{\mathrm{src}}d
\right\rangle
\]

with the conventionally correct source and target placements, and with the
same form obtained by pulling back the Stieltjes Green/Stokes boundary.

Once

\[
J_{\mathrm{src}}=J_{\mathrm{top}},
\]

all analytic consequences already proved apply:

- strict contraction of the bounded mate;
- closed inverse relation;
- trace-class completion;
- bounded reciprocal resolvent;
- trace-class Schur return;
- Fredholm determinant availability.

## What equality cannot mean

The identity is not equality of raw carrier formulas. The left side passes
through ray splitting, theta completion, and tail history; the right side is a
window boundary vector.

Equality means equality after the declared comparison maps into the common
relative Green quotient.

It must include:

1. moving-cut trace cancellation;
2. wall factor-two calibration;
3. odd Wronskian quarter normalization;
4. the reciprocal-even \(L^2D^2\) correction;
5. theta label summation;
6. prime support idempotents;
7. Euler endpoint weights.

## First-prime test

At \(p=2\), with \(L=\log2\), the smallest executable equality is

\[
J_{\mathrm{src}}e_2
=
W_{2L}-W_L.
\]

The left side must be computed from

\[
U_L
\left[
-2L(A+1)D+L^2D^2
\right]f_0
\]

through half-density, Wronskian, and tail-history maps.

Testing only its scalar endpoint value is insufficient. The equality must
hold in the relative Green quotient and preserve its even and odd
polarizations.

## Minimal falsifiers

### Correct boundary, wrong curvature

Drop the \(L^2D^2\) term. The first-order endpoint orientation is correct,
but the finite transport packet differs.

### Correct curvature, wrong wall

Use the constant wall instead of the half-amplitude Wronskian wall. The
quadratic identity is off by a factor two or four.

### Correct finite primes, incompatible completion

Fit one comparison scalar at every \(p\). The required scalars grow
super-polynomially, so the topological relation is not recovered.

### Correct scalar shadow, wrong carrier

The label-summed theta output has the same integral as \(d_p\) but differs
by a nonzero Green-quotient vector.

## Status theorem

Every generic analytic question upstream of this identity has been separated:

- local propagation is explicit;
- domains are closed;
- finite radicals are absent;
- raw inverse uniformity is correctly rejected;
- the bounded mate is trace class;
- auxiliary inversion is uniformly bounded conditionally on the frozen theta
  normalization.

Therefore another abstract positivity estimate cannot advance the first edge.
The next work must evaluate the source constructor at the first prime and test
the identity above.

## Frontier

The first Adams edge has contracted to one falsifiable equation:

\[
J_{\mathrm{src}}e_p
=
W_{2\log p}-W_{\log p}
\qquad
\text{for every prime }p,
\]

read in the common relative Green quotient with complete typing.

Until this equation is proved, the closed trace-class relation is an analytic
candidate, not yet the arithmetic Adams constructor.
