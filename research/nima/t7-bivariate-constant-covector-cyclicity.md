# Full Shape Transport Still Leaves One T7 Direction Unseen

The exact flat bivariate nine-master connection was restricted to the frozen
kernel

\[
T_7=\langle e_1,\ldots,e_6,v_{\rm alg}\rangle.
\]

For 42 constant source-frame covectors, all covariant derivatives of total
degree at most six were evaluated at three generic rational points.  The
maximum observability rank is

\[
\boxed{6}
\]

over each of two independent finite fields.  The mixed derivative base jets
commute, as required by the certified flat connection.  Thus the independent
shape direction repairs one of the two missing directions in the earlier
scaling-only census, but it does not make one constant scalar germ jointly
conservative on rank-seven \(T_7\).

For the first maximal seed, the remaining blind vector is supported entirely
on \(\langle e_2,e_3,e_4,e_5\rangle\).  It has no component along \(e_1\) or
the source-selected algebraic plane \(\langle e_6,v_{\rm alg}\rangle\).

This is a bounded constant-covector theorem.  It neither identifies the
physical Bunch--Davies covector nor excludes a source-derived rationally
varying covector or a second independent physical readout.

Within the same bounded census, two constant readouts are sufficient for full
rank seven.  The first replicated conservative pair consists of the coordinate
functional dual to \(e_2\) and the all-ones functional.  This is an existence
statement in the frozen source frame, not evidence that either functional is
physical.

The individual coordinate-orbit ranks are

\[
(1,1,2,1,2,2,1)
\]

in the ordered basis \((e_1,e_2,e_3,e_4,e_5,e_6,v_{\rm alg})\).  In
particular, the independently source-selected supported \(e_6\) pairing has
bivariate orbit rank two.  It is a legitimate terminal readout, but it cannot
by itself observe the other five algebraic-kernel directions.
