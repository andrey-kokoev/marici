# Many-many forward G4 pair-incidence equality

The source-selected forward G4 map must agree with the grade-resolved pair incidence on elementary matrix units:

$$
U_{G4}\bigl(e_{p,k}\otimes\overline{e_{q,\ell}}\bigr)
=
 c_{p,k}\overline{c_{q,\ell}}
\mathcal B_{p,k;q,\ell}^{\rm graph}.
$$

After summing grades, the diagonal source reads

$$
U_{G4}(c\otimes\overline c)
=\sum_{p,q}c_p\overline{c_q}\,\mathcal B_{p,q}^{\rm graph}.
$$

The equality must hold separately in the analytic-transpose and Hermitian variance lanes, before scalar wall codiagonalization. It also must commute with prime-shell restriction and reciprocal exchange.

Status: forward G4 pair-incidence equality specified as the next source witness.
