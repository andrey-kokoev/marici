# qRB microstep 100: two-output polarized metric

Prior research supplies the missing Gaussian block object abstractly. With bounded skew operator `J_link` and `K_link=-J_link/2`, define

$$
G_{\rm link}
=
\begin{pmatrix}
0&K_{\rm link}\\
-K_{\rm link}&0
\end{pmatrix}.
$$

Because `K_link` is skew-adjoint, `G_link` is Hermitian self-adjoint and bounded. It is the forward/reciprocal polarized linking metric, not a diagonal positive norm.

Its slot-swap law is

$$
S^*G_{\rm link}S=-G_{\rm link},
$$

which records the orientation reversal. Regular and weighted wall terms are combined inside `K_link` and cannot be independently renormalized.

Status: complete bounded two-output linking metric constructed analytically; identification with the older conservative port remains open.
