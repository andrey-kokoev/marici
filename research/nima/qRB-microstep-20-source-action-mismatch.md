# qRB microstep 20: source-action mismatch

The concrete source audit distinguishes two actions:

- additive window translation `T_b`;
- multiplicative theta dilation, transported by the half-density map `U`.

They satisfy

$$
UD_aU^{-1}=\tau_{\log a},
$$

but generally

$$
UT_b\ne\tau_LU.
$$

Hence an additive sharp-window observer cannot be identified with a single logarithmic theta observer. Its pullback must be retained as a separate lax regulator channel, or expanded in the admitted varying-width/Schwartz observer family.

Status: physical source-action mismatch established. This prevents an invalid strict qRB refinement identification and confirms that the next estimate must be made on the explicit residual `UT_b-\tau_LU`.
