# A source-authorized n=9 positive boundary contour beyond the 50 tree histories

The complete starred four-mass ψ invariant comes from the sourced eight-point four-pair positroid cell. There is an **exact** nine-point embedding that does not require rewriting ψ in the 50 standard n=9 BCFW/nested-R histories: insert a ZERO column at physical label 3 into the eight-point source matrix `D` on the retained labels `(1,2,4,5,6,7,8,9)`. Denote the resulting `2×9` matrix by `D₉`.

* For source coordinates `w₂,w₄,w₅,w₆,w₇,w₈,u,v=t−u>0`, all 36 ordered `2×2` minors of `D₉` have nonnegative coefficients. Exactly **24 are strictly positive** and 12 vanish identically (eight from the zero column and four original parallel column pairs). Thus `D₉` is a positive-boundary positroid cell with label 3 a **loop**, not an interior n=9 BCFW history.
* For **arbitrary** inserted bosonic supertwistor `(Z₃,χ₃)`, the identities `D₉Z₉=DZ₈` and `D₉χ₉=Dχ₈` hold symbolically. Every source-coordinate derivative is the same; hence the two-sheet fibre, full fermionic numerator, pushforward Jacobians, and oriented intrinsic source dlog form are inherited unchanged. The two exact generic rank-four witnesses recover both regular fibre sheets; changing `Z₃` does nothing.
* This is genuinely compatible with **strictly positive n=9 rank-six external data**. Insert the moment-curve row at label 3 into the earlier positive 8-row rank-six external data, and apply the SAME source-tested `SL(6)` frame change. All **84 ordered six-brackets** remain positive by the Vandermonde formula. At each of two exact data sets, the embedded source maps to the same target `Y₀` and has the same TWO regular source sheets. The target is compared as a Grassmannian two-plane, not incorrectly as an equal gauge matrix.

The sourced eight-point four-mass contour therefore furnishes a concrete n=9 **positive boundary residue** whose complete ψ is label-3 independent and has the prescribed two-sheet form. This resolves the *representation* alternative left by the 50-history scalar-span no-go: the contour lies outside that specified tree-history span; there is no contradiction with the sourced full n=9 tree-amplitude formula, which depends on label 3. This does **not** establish that this boundary is a face of any proposed complete n=9 amplituhedron triangulation, show global image coverage, or identify a full n=9 amplitude coefficient.

Checker: `research/nima/checkers/check_nine_point_four_mass_loop_embedding.py`; certificate: `research/nima/results/nine-point-four-mass-loop-embedding.json`. Source cell and two-sheet geometry: `research/nima/checkers/check_nine_point_paired_trace_samples.py` and `research/nima/checkers/check_four_mass_rank_six_Y0_regular_witness.py`.
