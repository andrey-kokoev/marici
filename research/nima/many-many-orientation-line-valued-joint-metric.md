# Many-many orientation-line-valued joint metric

As a formal covariance package, let `Or` be the sign representation of the ordered forward/reciprocal output pair. This does not assert that `Or` is an independently source-authorized carrier coordinate. Reciprocal slot exchange acts on it by

$$
S_{\rm Or}\epsilon=-\epsilon.
$$

Because

$$
S_{\rm link}^*G_{\rm link}S_{\rm link}=-G_{\rm link},
$$

the tensor-valued form

$$
\widetilde G_{\rm link}
=\epsilon\otimes G_{\rm link}
$$

is reciprocal-invariant:

$$
(S_{\rm Or}\otimes S_{\rm link})^*
\widetilde G_{\rm link}
(S_{\rm Or}\otimes S_{\rm link})
=\widetilde G_{\rm link}.
$$

Hence, after this formal sign-representation packaging, the Hilbert part of the joint metric may be written as

$$
\widetilde G_3^{\rm Hilb}
=G_{\rm FP}\oplus\widetilde G_{\rm link},
$$

with exact reciprocal covariance. The determinant/Tate factor remains a separate line isomorphism on `L_ac`; it is not used to absorb the orientation sign.

This construction distinguishes two independent lines: `Or` records output orientation, while `L_ac` records archimedean and determinant descent. Identifying them would conflate parity with anomaly normalization.

Status: reciprocal-invariant repackaging constructed formally. The source object remains the orientation-odd `G_link`; no additional orientation-line carrier is claimed. Determinant-line comparison remains separate.
