# v74: occurrence bridge and short-Rees defects

Independent replays passed 81,667, 96,832, and 38,418 checks.

`rzk/100-occurrence-bridge-short-rees-defects.rzk.md` records a correction to
the earlier one-coordinate comparison: Entry 434 already has the six-occurrence
coefficient base, so its auxiliary tangent need not carry all native occurrence
information. The actual augmentation comparison preserves those occurrences.
It is a quotient rather than an equivalence, with rank-nine degree-two cokernel
and non-Tor-independent auxiliary-normalization pullback.

All eight higher-wedge short-Rees defects have supported morphisms from their
complete defect resolutions and primitive central native-extension values. The
six first-conormal channels have zero supported defect. Under the existing
native cap, however, all six quadratic defect channels map to zero; only the two
cubic channels produce corrected primitive short-support cycles, and every
defect channel has zero generic-Q projection.

The Rzk module passes a fresh check. Resolving the short-Rees defects therefore
does not by itself construct the physical generic/collar comparison.
