# qRB microstep 134: all-character completion gate

The smoothed prime adjacency has multiplier

$$
a_t(\xi)=2\sum_{n\ge2}\frac{\Lambda(n)}{\sqrt n}e^{-(\log n)^2/(4t)}\cos(\xi\log n).
$$

The scalar zero-character target sees only `a_t(0)`. An operator-level positive completion requires an archimedean/endpoint symbol `h_t(xi)` satisfying

$$
h_t(\xi)-a_t(\xi)\ge0
$$

for every real `xi` and every positive smoothing time `t`, with the source normalization fixed at `xi=0`.

The nonzero-character negative witness shows that positivity of the prime part alone cannot supply this inequality. The endpoint and gamma completion must be coupled before testing all characters.

Status: full-character positivity gate isolated; no all-character completion theorem is claimed.
