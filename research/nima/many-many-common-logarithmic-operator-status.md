# Many-many common logarithmic operator status

At finite prime cutoff, define

$$
Q_p=p^{-1/2}S_{\log p},
\qquad
H_X=\sum_{p\le X}\sum_{k\ge1}\frac1kQ_p^k.
$$

The exact split

$$
H_X=H_X^{(1)}+H_X^{(2)}+H_X^{(\ge3)}
$$

supplies primitive, square, and connected endpoint currents through the single endpoint coboundary

$$
(\partial E_0)_G(H_X).
$$

Thus all three strata already have one source operator provenance. The Gaussian Mellin line acts on the same translation generators, giving finite arithmetic–archimedean compatibility.

The unresolved comparison is a relative trace map identifying representation traces of `Q_p^k` with endpoint coboundary currents. It is not an ordinary trace identity. Completion retains `H^(1)` and `H^(2)` as anomaly-line data and completes `H^(>=3)` in the connected ideal.

Status: common logarithmic operator and three-grade provenance closed; relative trace comparison and completed seam/archimedean trivialization remain open.
