# Gaussian vacuum–prime dilation is the first real distributive square

## Status

Research packet. It derives a source-local compatibility between arithmetic
label multiplication and the archimedean oscillator vacuum. It rejects the
known Gaussian-polynomial hostile but does not yet orient the completed zeta
zeros.

## Labelled Gaussian atoms

For a positive integer label (n), define

\[
\phi_n(x)=e^{-\pi n^2x^2}.
\]

Let (T_p) multiply the arithmetic label by a prime (p), and let (R_p)
act on archimedean functions by dilation:

\[
T_pe_n=e_{pn},
\qquad
(R_pf)(x)=f(px).
\]

For the labelled synthesis (Ue_n=\phi_n), one has the exact source square

\[
UT_p=R_pU.
\]

This is the basic distributive law between multiplicative label transport and
archimedean scale transport. Mellin transformation turns (R_p) into the
usual scalar weight (p^{-s}), but the operator square exists before that
compression.

## Gaussian vacuum equation

Each atom is the vacuum of its labelled annihilation operator

\[
a_n=\partial_x+2\pi n^2x,
\qquad
a_n\phi_n=0.
\]

This distinguishes the standard Gaussian from the Fourier-fixed
Gaussian-polynomial hostile. The hostile preserves the finite Euler/Fock
grammar and additive Poisson architecture, but it is not annihilated by this
first-order vacuum operator.

## Exact covariance cell

The annihilation operators and prime dilations satisfy

\[
a_{pn}R_p=pR_pa_n.
\]

Indeed,

\[
\begin{aligned}
a_{pn}R_pf(x)
&=p f'(px)+2\pi p^2n^2x f(px)\\
&=pR_p\bigl(f'+2\pi n^2x f\bigr)(x).
\end{aligned}
\]

Thus prime multiplication, archimedean dilation, and the Gaussian vacuum form
one typed coherence cell. Applying it to \(\phi_n\) shows that the prime orbit stays
inside the labelled vacuum family.

This is stronger than the universal scale square (UT_p=R_pU): an arbitrary
profile of the form (f(nx)) satisfies the scale square, while only the
Gaussian vacuum satisfies the annihilation law.

## Mellin shadow

For the Gaussian Mellin factor

\[
M(s)=\int_0^\infty e^{-\pi x^2}x^{s-1}\,dx,
\]

integration by parts in the vacuum equation gives

\[
2\pi M(s+2)=sM(s).
\]

This is a scalar shadow of the operator covariance. It determines the gamma
recurrence when combined with the source normalization and analytic growth
conditions. The operator equation is prior; the recurrence alone must not be
used as source authority.

## What the theorem does and does not reject

The exact adelic hostile with standard finite components and a modified
Fourier-fixed real component retains:

- every finite Euler factor;
- the prime-power/Fock grammar;
- finite primitive and square currents;
- Fourier–Poisson continuation;
- and off-critical zeros inherited from its archimedean Mellin factor.

It fails the Gaussian vacuum equation. Therefore that equation is the first
named source operation distinguishing the standard adelic source from this
hostile.

However, the Gaussian equation only controls the archimedean factor directly.
It does not by itself exclude hypothetical off-critical zeros in the finite
arithmetic factor. The RH-bearing theorem must use the covariance cell to
couple the oscillator energy to primitive, square, seam, tail, and completion
currents.

## Positive operator available from the source

The annihilation law supplies the nonnegative oscillator operator

\[
H_n=a_n^{*}a_n.
\]

Its positivity is independent of zero locations. The next calculation is not
to assert that (H_n\ge0) proves RH, but to insert the labelled sum of these
operators into the doubled Green storage identity and compute the exact
boundary residual.

The desired finite identity equates doubled storage change plus labelled
oscillator energy with the combined primitive, square, seam, and
archimedean supply.

Every term must be an operator or quadratic form on the same labelled carrier
before scalar Poisson aggregation.

## Finite falsifiers

At a finite label cutoff, reject the route if any of the following occurs:

1. (UT_p-R_pU\ne0) for an admitted prime constructor;
2. (a_{pn}R_p-pR_pa_n\ne0);
3. the proposed boundary currents do not act on the oscillator graph domain;
4. the summed oscillator form leaves an undeclared bulk residual;
5. the hostile Gaussian-polynomial source satisfies the claimed vacuum law;
6. the finite identity holds but its lower bound collapses under completion.

## Interpretation

The first meaningful multiplicative–additive lift is not an arbitrary large
operator. It is the metaplectic oscillator vacuum together with its discrete
prime-dilation orbit. Prime multiplication becomes archimedean squeezing, and
the labelled annihilation operator intertwines the two exactly.

This returns to the moving-seam/metaplectic intuition with a concrete source
equation and a hostile that it rejects. The remaining question is whether its
positive oscillator energy closes with the full arithmetic boundary supply,
or whether it controls only the zero-free gamma factor and leaves RH
untouched.
