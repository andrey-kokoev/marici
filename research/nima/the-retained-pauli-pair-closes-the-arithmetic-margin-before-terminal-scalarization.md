# The retained Pauli pair closes the arithmetic margin before terminal scalarization

## Arithmetic observer

On the primitive-square source fibre, let

\[
X=\begin{pmatrix}0&1\\1&0\end{pmatrix},
\qquad
Y=\begin{pmatrix}0&i\\-i&0\end{pmatrix},
\]

and let \(J_p\) be the source Stieltjes endpoint lift.  Retain both typed
outputs:

\[
\mathcal O_pv=(J_pXv,J_pYv).
\]

This is the arithmetic counterpart of retaining both reciprocal history
sheets.  Neither Pauli output is scalarized or added to the other inside the
G3 observer.

## Exact twirl

Write

\[
A_p=J_p^*J_p
=\begin{pmatrix}a_p&z_p\\\overline z_p&b_p\end{pmatrix}.
\]

Then

\[
\mathcal O_p^*\mathcal O_p
=XA_pX+YA_pY
=2\begin{pmatrix}b_p&0\\0&a_p\end{pmatrix}.
\]

The endpoint cross-correlation cancels exactly.  Since

\[
m_\nu^2\le a_p,b_p\le1,
\]

one obtains

\[
2m_\nu^2I
\le\mathcal O_p^*\mathcal O_p
\le2I
\]

uniformly in \(p\).

## Completed prime sum

The G1.4 source idempotents make

\[
\mathcal O=\bigoplus_p\mathcal O_p
\]

prime diagonal.  Hence

\[
2m_\nu^2I
\le\mathcal O^*\mathcal O
\le2I
\]

on the completed primitive-square source.  Finite prime cutoffs commute with
\(\mathcal O\), and no off-diagonal accumulation changes the bound.

The connected grades are a separate nuclear return.  They do not replace or
quotient either low-grade Pauli port, so their attachment cannot reduce this
positive low-grade margin in the saturated direct-sum observer.

## Terminal-scalarization hostile

A later evaluator may form a scalar combination

\[
\ell_1J_pXv+\ell_2J_pYv.
\]

One scalar row can have a nontrivial kernel even though the retained pair is
uniformly faithful.  Therefore the arithmetic G3 margin belongs before such a
terminal scalarization.

This is not an optional strengthening.  The selected constructor order keeps
typed outputs until the global evaluator, and G3 tests the completed Green
observer before the separate spectral readout used in G4.

## Arithmetic margin

On the declared retained low-grade arithmetic support,

\[
\delta_P=2m_\nu^2
\]

in squared-norm normalization.  Equivalently, the analysis lower bound is
\(\sqrt2m_\nu\).

This closes the arithmetic G3 margin provided the G3 observer is the retained
Pauli pair specified by the source constructor.  Any manuscript using a
scalarized arithmetic row must prove a new lower bound and cannot inherit this
one.

The glue, coherent-diagonal, and global mixed margins remain open.  No RH
conclusion is authorized.
