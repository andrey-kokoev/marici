# RH rigging frontier after exact Evans domain and five-port summability

## Question

After independent rereading, which corrected-G4 obligations remain analytic
rigging problems and which are now equation or comparison problems?

## Verified rigging chain

The current packets establish the following source-typed chain.

### Exact history domain

At every zero \(z_0\) of the Xi section,

\[
 u_{z_0}\in H^1(\mathbb R).
\]

The left and right Evans formulas agree globally, not only at the seam. The
continuity extension of the punctured-line first-order operator is maximal
isotropic, and the history has zero endpoint flux. No limiting-absorption limit
is used.

For a zero of multiplicity \(m\),

\[
 \partial_z^ju(\cdot;z_0)\in H^1(\mathbb R),
 \qquad 0\le j<m.
\]

### Three-port block domain

The augmented vector

\[
 \Psi_{z_0}=(u_{z_0},1,0)
\]

belongs to

\[
 H^1(\mathbb R)\oplus\mathbb C_\theta\oplus D(D_U).
\]

Bounded theta incidence and Hilbert--Schmidt centered arithmetic incidence do
not change the diagonal block domain.

### Arithmetic target

The centered arithmetic residual is a defined vector

\[
 r_U(z)=B_\Sigma^\dagger u_z\in U_{\rm ar}.
\]

The ordinary, regular derivative, wall, reciprocal, and ordered-linking
components are separately bounded by the Evans wall-graph norm. Their centered
coordinates obey the common summability estimate

\[
 \|r_U^{(j)}(z)\|_U^2
 \le C_j^2\|u_z\|_{G,\rm wall}^2
 \sum_p\frac1{p\log p}<\infty.
\]

The same statement holds for every parameter-root vector below the Xi
multiplicity.

## SCC readback

The mutable SCC v2 contract was reread at digest

```text
fbfbd67a6a1122c062a5c70f84a75191464cbd54e7a4121a1780f5c75a52c9d6
```

It still contains the unsplit open node
`seam_limiting_absorption_domain`. That node now conflates:

- exact Evans history domain: constructed;
- three-port block-domain membership: constructed;
- five-port arithmetic summability: constructed;
- global Fourier--Poisson response-relation membership: open;
- conservative kernel equation: open.

No SCC mutation is made here because the contract is owned by
`marici.Aspect` and no cross-locus authorization is present.

## Exhausted rigging objective

For the unchanged exact Evans vector, there is no remaining unidentified
limiting-absorption, Sobolev-trace, wall-distribution, reciprocal-continuity, or
arithmetic-summability gate in the currently declared five-port model.

A new rigging obligation would arise only if the constructor changes one of:

- the history state;
- the common Green graph;
- the source metric;
- a port normalization;
- the arithmetic law domain;
- the global Fourier--Poisson response carrier.

Such a change must be source-derived and rechecked rather than inferred from
the present estimates.

## Remaining non-rigging frontier

The current obstruction is the exact family

\[
 B_\Sigma^\dagger\partial_z^ju(\cdot;z_0)=0,
 \qquad 0\le j<m,
\]

or equivalently the complete prime-shell identities with all five ports. The
ordinary component is eventually sign-definite, so a proof must exhibit the
source-normalized compensating terms.

Separately, the full Fourier--Poisson response trace must be shown to lie in
the global maximal-isotropic sewing relation. That is an intertwining theorem,
not existence of the Evans graph vector.

## Disposition

The bounded RH-rigging objective is exhausted on the unchanged Evans
constructor. Further progress requires the source shell-cancellation identity
or a new divisor-preserving history comparison. No RH conclusion is
authorized.
