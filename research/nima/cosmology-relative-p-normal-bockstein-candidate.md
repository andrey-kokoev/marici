# Relative p-normal Bockstein candidate stops at the unresolved carrier

The source identities determine the principal normal

\[
p=x+y+3z
\]

and the exact logarithmic divisibility relation

\[
\omega_{23}-\omega_{13}+\omega_{12}=p\,\eta.
\]

This authorizes the auxiliary normal Koszul factor

\[
K_p=[R\langle h_p\rangle\xrightarrow{p}R\langle e_p\rangle],
\qquad R=\mathbb Z[x,y,z].
\]

The checker verifies its square and its generic/special rank jump over
\(\mathbf F_{101}\) and \(\mathbf F_{103}\). This does **not** yet define a
Bockstein. The current source packets explicitly report that neither the
triple-Čech nearby-cycle term nor a principal chain-level homotopy has been
constructed.

The required carrier remains

\[
B_{\rm cos}=(C_{\rm log/face}/E_{\rm CM})/J_{\rm circuit}.
\]

A subsequent support audit shows that \(K_{\rm CM}\) is a unit on the generic
incidence stratum. Hence \(E_{\rm CM}=0\) there, canonically and with automatic
differential stability. The unrelated all-soft weighted chart must not be
imported.

The minimal source-ordered marked-wall Čech complex is now materialized. Its
sewing subcomplex is stable, but the special pair/triple complex is exact over
both \(\mathbf F_{101}\) and \(\mathbf F_{103}\). Thus the minimal carrier
kills the rank-one nearby quotient and supports no nonzero \(\Xi_p\).

Voevodsky's independent denominator audit further proves that \(p\eta\) has
double residue one on a punctured bidisc, whereas every exact Laurent
one-form differential has double residue zero. Thus increasing ordinary pole
depth cannot supply the missing primitive.

A connecting class can now arise only after a separately source-derived
bulk/face or exceptional mapping cone whose compensating face residue changes
the target complex. Such an augmentation must
construct \(H_p,\Xi_p\) satisfying

\[
dH_p=p\Xi_p
\]

in its independently defined quotient. Survival of \([\Xi_p]\) must then be
checked by boundary-rank comparison after \(p\mapsto0\) over at least two
finite fields; it cannot inherit survival from the failed minimal carrier.

No division by \(p\), ideal-dual evaluation, all-soft \(\mathbb Z/3\)
substitution, monodromy inference, global contour, or physical-period covector
is admitted by this candidate gate.

Artifact:

- `research/nima/checkers/check_cosmology_relative_p_normal_bockstein_candidate.py`
- `research/nima/results/cosmology_relative_p_normal_bockstein_candidate.json`
