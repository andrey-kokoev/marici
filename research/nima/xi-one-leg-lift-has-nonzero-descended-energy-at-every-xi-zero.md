# Xi one-leg lift has nonzero descended energy at every Xi zero

Let `z_0` be an Xi zero and let `v_(z_0)` be the nonzero glued Evans history state. The pointed lift is

$$
\Delta_\Phi(v_{z_0})=v_{z_0}\otimes\Phi.
$$

Because `Phi` is nonzero and the Hilbert tensor embedding is bounded below,

$$
\Delta_\Phi(v_{z_0})\ne0.
$$

The completed-theta source ray meets the interval-cycle kernel trivially. Hence its class remains nonzero in

$$
\overline P=P_{\rm pair}/Z_1(G).
$$

Canonical source recovery proves that

$$
D T_{\rm PB}:\overline P\to Y_{\rm can}
$$

is injective. Therefore

$$
D T_{\rm PB}\Delta_\Phi(v_{z_0})\ne0.
$$

The positive energy transported to the canonical codiagonal range by the source-pulled topology is consequently nonzero on every lifted Xi kernel state. Parameter derivatives are preserved by `Delta_Phi` and `T_PB`, so the same argument retains nonzero root-chain classes through the Xi multiplicity order.

This closes completion noncollapse and positive-energy nonvanishing on the Xi divisor. It does not prove the off-seam generalized-eigenvalue exclusion or the exact arithmetic return balance.

Status: Xi-zero one-leg lift survives cycle quotient and canonical codiagonal with nonzero descended energy; arithmetic balance/confinement remains open.
