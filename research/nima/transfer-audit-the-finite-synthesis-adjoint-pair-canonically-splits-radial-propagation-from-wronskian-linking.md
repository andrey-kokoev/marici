# Transfer audit: the finite synthesis-adjoint pair canonically splits radial propagation from Wronskian linking

## Question

Can prior finite reciprocal-linking research supply a non-fitted split of the
fixed combined diagonal response into reciprocal and linking mechanisms?

## Claim boundary

Yes at finite cutoff and on the source-generated radial range. The radial shell
synthesis is the forward link; its declared adjoint or analytic transpose is
the reciprocal return; the Wronskian term belongs to the interior skew part of
the radial first-order graph. This fixes the architectural split without
assigning arbitrary scalar shares. The exact equality with the G4 block still
requires the independently declared source metric, analytic-transpose
comparison, and endpoint border.

## Prior transferable theorem

For finite source synthesis

\[
 U_X:V_X\to H_X,
\]

prior research constructs the canonical reciprocal pair

\[
 M_X=U_X,
 \qquad
 N_X=U_X^*.
\]

On the source-generated range, polar normalization is available, but the
unnormalized pair already defines the Schur feedback

\[
 U_X^*A_X(z)^{-1}U_X.
\]

No fitted inverse is needed.

## Radial instantiation

Let \(V_X^{\rm shell}\) be the finite labelled shell-pair carrier and let
\(H_X^{\rm rad}\) be the finite source-generated subspace of the rapid radial
history graph. The explicit translation synthesis gives

\[
 U_X^{\rm rad}:
 V_X^{\rm shell}\longrightarrow H_X^{\rm rad}.
\]

Its columns are the shell autocorrelation, endpoint, and Wronskian graph
sections. Finite label and shell faithfulness is retained before scalar theta
summation.

The source-derived return candidate is therefore

\[
 N_X^{\rm rad}=(U_X^{\rm rad})^*
\]

in the Hermitian Green lane, and

\[
 N_{X,\rm an}^{\rm rad}=(U_X^{\rm rad})^\top
\]

in the analytic-transpose Evans lane. These returns must not be identified
without the declared real-structure comparison.

## Location of the Wronskian link

The radial state equation is

\[
 D_t\rho=e-\frac12w.
\]

The ordered Wronskian \(w\) is the skew interior incidence of this first-order
graph. It belongs inside the radial sector operator or its boundary form; it is
not a freely weighted part of the reciprocal return.

Thus the architectural split is:

- reciprocal port: adjoint or transpose return of radial propagation;
- linking port: the fixed \(-1/2\) Wronskian incidence inside the propagated
  graph;
- endpoint border: the initial value and two moving endpoint coordinates.

## Finite bordered block

The minimal finite operator has the schematic form

\[
 \mathbb T_X(z)
 =\begin{pmatrix}
 D_t-z & U_X^{\rm rad}\\
 N_X^{\rm rad} & B_X(z)
 \end{pmatrix}
\]

with the initial and endpoint traces retained as a border. Its Schur return is

\[
 B_X(z)-N_X^{\rm rad}(D_t-z)^{-1}U_X^{\rm rad}.
\]

The exact radial identity fixes what this bordered return must evaluate to on
each diagonal shell:

\[
 R(z)+2E(z)
 =\frac{\rho(0)+E(z)-\frac12W(z)}{z}+2E(z).
\]

This is a comparison target for the block, not permission to define
\(B_X\) from the desired scalar.

## Why polar normalization is unnecessary here

Candidate one's adjoint residual requires the forward synthesis and its return,
not a bounded inverse from analytic output to the whole source. The known
collapse of the finite Gram lower bound therefore does not obstruct the
retained graph construction.

Polar normalization would move Gram factors into diagonal blocks and require
anomaly-line bookkeeping. The unnormalized retained-source graph avoids that
relocation.

## Required finite comparison

For each finite labelled packet, the transfer is complete only after proving:

1. the G4 radial sector operator equals the first-order graph operator;
2. the declared reciprocal return equals \((U_X^{\rm rad})^*\) or its typed
   analytic transpose;
3. the skew block pulls back to the Wronskian coefficient \(-1/2\);
4. the endpoint border gives \(\rho(0)\) and the additional \(2E\) term;
5. the complete polarized pullback matrix agrees, not only its scalar Schur
   value.

## Completion

The radial shell sections are superexponentially summable in the retained
source graph. This supplies compact-local convergence of the forward and
transpose probes. It does not imply Morita fullness or determinant-line
invertibility, neither of which is needed merely to state the adjoint shell
identity.

## Hostiles

The transfer fails if:

- a return arrow is chosen independently of radial synthesis;
- Hermitian adjoint and analytic transpose are silently identified;
- the Wronskian coefficient is moved into an arbitrary reciprocal scalar;
- endpoint border data are eliminated before the Schur comparison;
- polar normalization is used without tracking its Gram determinant.

## Disposition

Prior finite synthesis-adjoint research supplies the canonical architectural
split of the diagonal response. The remaining blocker is a finite polarized
identification of this radial bordered block with G4's actual conservative
complex, followed by cutoff completion. No RH conclusion is authorized.
