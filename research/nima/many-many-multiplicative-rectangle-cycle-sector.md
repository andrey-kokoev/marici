# Many-many multiplicative-rectangle cycle sector

For consecutive-prime pairs `p<q` and `r<s`, define the corrected four-term cycle

$$
Z_{p,q;r,s}
=r\rho_{rr}^{[\log p,\log q]}
+q\rho_{qq}^{[\log r,\log s]}
-p\rho_{pp}^{[\log r,\log s]}
-s\rho_{ss}^{[\log p,\log q]}.
$$

Interval additivity gives

$$
Z_{p,q;r,s}=0
$$

after shell and theta labels are erased. Define the collision sector by

$$
\mathcal Z_{\rm coll}
=\operatorname{span}\{Z_{p,q;r,s}\}.
$$

The labelled carrier retains the four terms and their source provenance. The unlabelled scalar codiagonal sees their exact cancellation. Refinement and reciprocal transport act on the cycle generators; their closure determines whether scalar quotienting is functorial.

Status: explicit collision-sector generators fixed.
