# All local linear Adams data are fixed; the remaining gate is quadratic functoriality

## Completion qualification

The required quadratic theorem cannot be a uniformly bi-bounded equivalence
between the raw two-vector Stieltjes endpoint Gram and the uniformly coercive
theta-history Gram. The raw Stieltjes determinant satisfies

\[
\det G_p^{\mathrm{win}}\longrightarrow0.
\]

Accordingly, the four matrix-unit comparison below is a typed dilation into
the two-output Pauli observer and completed Green quotient, not a uniform
isomorphism of the raw endpoint planes. The Pauli pair cancels the collapsing
cross-correlation while retaining the two endpoint norms.

## Fixed even normalization

The completed theta wall is

\[
w_\theta=
\begin{pmatrix}
\frac12\\
\frac12
\end{pmatrix}.
\]

The relative coefficient wall satisfies

\[
-\mathbf1_\partial=-2w_\theta.
\]

With endpoint metric \(2I\),

\[
\|w_\theta\|_{2I}^2=1.
\]

Thus the even wall incidence has no free amplitude or metric parameter.

## Fixed odd normalization

The completed odd column is

\[
j_\theta=
\begin{pmatrix}
\frac14\\
-\frac14
\end{pmatrix}.
\]

The bilateral ordered history gives

\[
j_\theta=\frac14S_{\mathrm{ord}}.
\]

Together with the curvature identity

\[
S_{\mathrm{ord}}\Omega=4A,
\]

this yields

\[
j_\theta\Omega=A.
\]

For the derivative-tail operator \(B=H_KD\),

\[
BS_{\mathrm{ord}}=-2H_K,
\]

so

\[
Bj_\theta=-\frac12H_K.
\]

The odd magnitude, sign, curvature return, and bounded tail propagation are
therefore all source-fixed.

## Fixed bivariate comparison

Two-ray restriction, half-density transport, theta label summation, and the
completion differential define

\[
Q_p^{\mathrm{pre}}
=
P_u\Sigma_\theta\mathcal T.
\]

On the rapid bivariate core,

\[
Q_p^{\mathrm{pre}}
\partial_t^{\mathrm{cell}}
=
\partial_t^{\mathrm{cell}}
Q_p^{\mathrm{pre}}.
\]

The source Stokes identity is preserved with full polarization before the
final completed-boundary representation.

## No remaining linear parameter

Combining the fixed wall and odd maps gives a unique local linear candidate

\[
Q_p^{\mathrm{lin}}:
\mathcal B_p^{\mathrm{bi}}
\longrightarrow
\mathcal B_p^{\mathrm{theta}},
\]

with:

- wall column \(w_\theta\);
- odd column \(j_\theta=\frac14S_{\mathrm{ord}}\);
- derivative-tail propagation \(-\frac12H_K\);
- reciprocal sign fixed by the endpoint order;
- prime label retained externally.

Any alternative rescaling violates at least one source identity.

## Quadratic representation theorem

Let

\[
\mathfrak G_p^{\mathrm{St}}
\]

be the polarized Stieltjes window form and let

\[
\mathfrak G_p^{\theta}
\]

be the completed theta-history graph form after wall splitting and tail
resolution.

The first Adams edge exists exactly when

\[
\mathfrak G_p^{\mathrm{St}}(x,y)
=
\mathfrak G_p^\theta
\bigl(
Q_p^{\mathrm{lin}}x,
Q_p^{\mathrm{lin}}y
\bigr)
\]

for all \(x,y\) in the common rapid prime core, after the declared radical
quotients.

This is now a theorem with no adjustable constants.

## Four matrix-unit test

On the endpoint plane generated by

\[
e_1\mapsto W_L,
\qquad
e_2\mapsto W_{2L},
\]

it suffices locally to test the four polarized matrix units

\[
(e_j,e_k),
\qquad
j,k\in\{1,2\}.
\]

The diagonal tests determine the endpoint energies. The two off-diagonal
tests determine:

- real cross-correlation;
- reciprocal imaginary orientation;
- adjoint ordering;
- compatibility with the odd tail return.

Testing only the difference vector \(e_2-e_1\) leaves one polarization
coordinate invisible.

## Representation form

Let \(\pi_{\mathrm{St}}\) represent coefficient windows by multiplication and
let \(\pi_\theta\) represent the completed wall and tail history.

The required commuting square is quadratic:

\[
\Gamma_{\pi_\theta}
\bigl(
Q_p^{\mathrm{lin}}
|e_j\rangle\langle e_k|
(Q_p^{\mathrm{lin}})^\sharp
\bigr)
=
\Gamma_{\pi_{\mathrm{St}}}
\bigl(
|W_{jL}\rangle\langle W_{kL}|
\bigr).
\]

It must hold on the common core and survive closure.

Linear equality of endpoint vectors does not imply this identity.

## Completion consequence

If the four matrix-unit identities hold primewise with uniform form bounds,
then twisted Mellin equivariance makes the global comparison prime diagonal.
The Euler coefficient product gives the convergent majorant

\[
\sum_p
\frac{(\log p)^m}{p^{3/2+\sigma}}.
\]

Therefore the global mixed form closes through the seam for every fixed finite
jet order.

The later trace-class Schur return and Fredholm determinant constructions then
become source-authorized rather than merely analytic candidates.

## Minimal hostiles

1. All linear identities hold, but one off-diagonal matrix unit has the wrong
   sign.
2. The difference-vector energy agrees while the endpoint cross-correlation
   differs.
3. The four identities hold on a core but one representation is not closable.
4. Finite-prime equality holds with comparison norms diverging.
5. The quadratic representation collapses the fourth Gaussian grade.
6. Equality appears only after scalar Euler aggregation.

## Verdict

There is no remaining local normalization problem. Wall scale, odd scale,
orientation, curvature return, tail propagation, and bivariate linear
transport are all fixed by source identities.

The first Adams edge now depends on one parameter-free statement: the four
fully polarized endpoint matrix units must have the same Green
representation in the Stieltjes and completed theta-history carriers.
