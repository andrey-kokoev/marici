# Transfer audit: prior results constrain the radial conservative interface

## Question and claim boundary

Which prior results transfer into the missing comparison between the radial bordered block and G4's canonical conservative complex?

Prior work fixes substantial finite architecture and exact conformance tests. It does not expose G4's canonical carrier, construct the full polarized comparison, or prove the RH-bearing chain map.

## 1. Logical and domain transfers

The triangular divisor compiler \(K_\tau\oplus Q_U\) and the maximal-isotropic conservative pencil remain distinct. A scalar determinant or Schur-section match cannot identify them; a separately typed chain map is required.

The exact Evans history and every root vector below Xi multiplicity already lie in the closed centered block domain:

\[
u_{z_0},\ \partial_z^ju(\cdot;z_0)\in H^1(\mathbb R),
\qquad 0\le j<m(z_0).
\]

No limiting-absorption theorem is needed merely to evaluate the candidate state. The comparison must instead preserve the complete residual family

\[
B_\Sigma^\dagger\partial_z^ju(\cdot;z_0).
\]

Domain membership does not imply kernel membership. A chain map carrying every Xi residue into the conservative kernel, combined with the Green identity, already contains the RH confinement step and remains open.

## 2. Finite two-sided and polarized architecture

On a finite typed source-generated carrier, forward synthesis and its Hermitian return are

\[
M_X=U_X^{\rm rad},\qquad N_X=(U_X^{\rm rad})^*.
\]

This is conditional on the declared arithmetic and radial inner products. The analytic transpose remains distinct. Its identification requires source conjugations \(J_V,J_H\) and a proved comparison such as

\[
(U_X^{\rm rad})^\top=J_V(U_X^{\rm rad})^*J_H.
\]

Holomorphic reciprocity \(s\mapsto1-s\) is not the seam-fixing real structure; the critical seam is fixed by \(s\mapsto1-\overline s\). If linear and antilinear exchanges define \(K(z)=c\overline z\), the exact real-structure gate is \(K^2=I\), equivalently \(|c|=1\).

The finite target must be one two-sided linking block. A triangular synthesis graph has determinant independent of its off-diagonal incidence. Polar normalization is valid on each finite source-generated range but does not descend without a source-authorized cutoff-uniform Gram bound.

Conformance is a full matrix identity, not a scalar Wronskian normalization. It must separately test radial/history energy, boundary energy, reciprocal-even mixing, and oriented Wronskian mixing. Determinant equality and the correct coefficient \(-1/2\) are necessary but insufficient.

## 3. Boundary, endpoint, and real polarization transfers

Moving-seam transport, complete endpoint pushforward, cutoff commutation, reciprocal-grade preservation, and four-presentation Fourier naturality already exist on the source-generated bundle. Their remaining burden is identification of the preserved metric and typed outputs with the radial direct-sum ports before codiagonalization.

On the real two-dimensional wall frame, reciprocal chain reversal and outward-minus-inward orientation give the fixed positive polarization

\[
-JM=I.
\]

Its Hermitian complexification is authorized only after retaining that source real frame. This fixes wall scale and orientation but not the radial odd numerator.

The endpoint-current border remains additive boundary provenance, not the completed multiplicative readout. The radial terms \(\rho(0)\), \(E\), and \(2E\) may enter as typed border data, but their additive zeros have no Xi authority. Endpoint projection before reciprocal linking is inadmissible.

Existing local linear arrows include

\[
-\mathbf1_\partial=-2w_\theta,
\qquad
(H_\Phi-M_\Phi I)S_{\rm ord}=-2H_K.
\]

They should be reused without fitted renormalization. The surviving local burden is their polarized Green-form identification with the label-dependent window path.

## 4. Explicit prime-labelled incidence and path transfers

The first endpoint incidence arrow is explicit in the coefficient-valued cell feature space:

\[
e_{p,k}\longmapsto[W_{k\log p}],\qquad k=1,2.
\]

The comoving kernel

\[
(\mathcal K\mu)(q)=\int W_t(q)\,d\mu(t)
\]

is contractive, preserves prime labels and cutoff restriction, respects the projective source seminorms, and has the reciprocal odd scale character. Arithmetic coefficients remain external to this kernel.

For \(L=\log p\), the primitive-to-square interior is the oriented scale-path homotopy

\[
W_{2L}-W_L=\int_L^{2L}\partial_tW_t\,dt.
\]

Thus the endpoint embedding and finite path constructor exist. Promotion still requires a closed rigged first-order history with distinct primitive and square traces. Before a common Hilbert Green metric is proved, its natural return is the test-dual transpose, not automatically the Hilbert adjoint.

Universal wall and Wronskian columns constrain this map but cannot carry the prime-dependent window Gram. The complete bulk incidence \(C_p\) must remain label-dependent and source-generated; a fitted Cholesky congruence is inadmissible.

## 5. Complete Green feature and boundary-triple gates

If the assembled Green form factors as

\[
b_z[h,h]=\|J_zh\|^2,
\]

then arithmetic loading must have one coherent feature lift

\[
S_z=J_z^*\Lambda_z.
\]

The lift must annihilate the joint Green radical, remain compact-locally bounded, commute with cutoff inclusions, and intertwine Real transport. Portwise lifts and bounds are insufficient because cross terms can create a joint radical invisible on every coordinate axis.

Conservative realization additionally requires a complete boundary triple. Unitary sewing alone yields skew symmetry. Skew-adjointness requires joint trace surjectivity onto the complete deficiency boundary, kernel equal to the minimal domain, and a jointly bounded right inverse. Completion needs a compact-local positive trace-surjectivity margin.

The boundary characteristic kernel must be identified through the Poisson solution operator. Determinant equality cannot replace this kernel isomorphism.

## 6. Completion and reduction gates

Local positive and reciprocal valuation chains are acyclic throughout the open critical strip. They transfer as source-local restriction and prime-deletion tests, not as global invertibility. Any Xi-bearing kernel must occur in declared global boundary, cross-prime, renormalization, archimedean, or completion coupling.

The diagonal pullback metric is unique once the source map, orthogonal prime basis, and target metric are fixed. This does not identify the pullback completion with G4's source topology. Output-only seam comparison has zero minimum modulus, so injectivity supplies no bounded inverse or positive glue margin.

Reachable restriction and Green quotient agree uniformly only under a positive Friedrichs angle

\[
s_X(z)=\inf_{\substack{r\in R_X\ominus(R_X\cap N_X)\\\|r\|=1}}
\operatorname{dist}(r,N_X).
\]

The comparison inverse has norm \(s_X(z)^{-1}\). This margin, the synthesis Gram margin, and the trace-surjectivity margin can collapse independently. Finite injectivity, trivial intersection, and equal finite spectra do not authorize completion.

## 7. Conformance target, hostiles, and disposition

The finite witness must compare the full block

\[
\mathbb T_X(z)=
\begin{pmatrix}
D_t-z&U_X^{\rm rad}\\
N_X^{\rm rad}&B_X(z)
\end{pmatrix}
\]

with G4 before scalar Schur elimination, retaining prime labels, the endpoint border, the Wronskian coefficient \(-1/2\), both return types, and every parameter jet. Its diagonal readout target is

\[
R(z)+2E(z)
=\frac{\rho(0)+E(z)-\frac12W(z)}{z}+2E(z).
\]

Mandatory hostiles must reject:

1. zero or reversed Wronskian incidence;
2. Hermitian adjoint silently substituted for analytic transpose;
3. nontrivial mixed exchange square;
4. scalar Schur equality with unequal polarized matrices;
5. endpoint scalarization before reciprocal linking;
6. triangular synthesis invisible to the determinant;
7. portwise arithmetic bounds failing on a joint radical;
8. unitary sewing on an incomplete deficiency boundary;
9. coordinatewise traces without a joint bounded right inverse;
10. collapsing Gram, Friedrichs-angle, or trace margins;
11. prime-independent incidence fitted to prime-dependent window Grams;
12. local-chain acyclicity misused as global invertibility.

The prior-transfer review is exhausted at the current repository state. It fixes the candidate arrows and exact conformance obligations but leaves the owner-reviewed G4 interface unavailable. Further progress now requires materialization of G4's canonical carrier and a finite polarized two-sided boundary-triple witness. No RH conclusion is authorized.
