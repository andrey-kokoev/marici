# The positive Green obstruction is exactly the quadratic causal-odd compression

The actual stable-history sign ledger leaves

$$
F_+(w,z)-F_-(w,z)
=
R(z)-R(-z)
+
\overline{R(w)-R(-w)}.
$$

Define the causal convolution operator on the completed source line by

$$
(H_zf)(q)=\int_q^\infty e^{-z(r-q)}f(r)\,dr,
$$

with its reciprocal anti-causal adjoint defined on the common form domain. Then

$$
R(z)=\langle\Phi,H_z\Phi\rangle,
$$

and reciprocal reflection gives

$$
R(-z)=\langle\Phi,H_z^*\Phi\rangle
$$

with the declared analytic-transpose or Hermitian parameter convention. Therefore

$$
R(z)-R(-z)
=
\langle\Phi,(H_z-H_z^*)\Phi\rangle.
$$

Equivalently, for `z=a+it`, its diagonal Hermitian real part is the previously computed oscillatory separation current

$$
-4\int_0^\infty\sinh(ar)\cos(tr)\,d\mu_\Phi(r).
$$

Thus the obstruction to the positive doubled Green sum is not a new scalar current. It is exactly the quadratic form of the source-native odd history operator

$$
T_{\rm hist}(z)=\frac{H_z-H_z^*}{2}.
$$

Prior work constructs the linear primewise odd compression with forced normalization

$$
d_p=H_{\rm jump}b_p=-2j_\theta b_p.
$$

What is still missing is precisely its Green-form functoriality:

$$
G_{{\rm win},p}
=
K_p^{\rm odd}
G_{\theta,p}
(K_p^{\rm odd})^*
$$

on the same reduced support and common closed form domain. Summed over the primitive, square, connected, seam, and archimedean channels, this equality would identify the relative-Haar/linking boundary current with

$$
-\bigl(F_+-F_-\bigr),
$$

closing the positive Green cycle.

The exact finite residual is

$$
\mathcal E_{p,X}
=
G_{{\rm win},p,X}
-
K_{p,X}^{\rm odd}G_{\theta,p,X}(K_{p,X}^{\rm odd})^*.
$$

It must vanish as a labelled quadratic form before cyclic trace or Xi specialization. Linear equality of endpoint evaluations does not imply this quadratic identity.

Status: forcing-difference obstruction identified exactly with the known causal odd compression; the sole remaining local gate is quadratic Green-form functoriality, followed by cutoff-compatible arithmetic summation.
