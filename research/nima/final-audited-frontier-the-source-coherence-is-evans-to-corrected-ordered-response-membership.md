# Final audited frontier: the source coherence is Evans-to-corrected ordered-response membership

The audit eliminates several apparent open constructions:

- complete Fourier/Tate response sewing is constructed;
- the response Fourier graph is unitary and maximal isotropic;
- the two stable-history Evans lift is constructed and multiplicity-preserving;
- one-sided polarized Green identities are exact;
- reciprocal sign bookkeeping is fixed;
- external anti-diagonal boundary flux is cancelled;
- primitive/square retained graphs and connected nuclear returns are constructed;
- moving-seam and endpoint transport are natural;
- direct positive-exponential admission of the completed theta source is impossible.

The unchanged Evans state is not thereby admitted to the positive response graph. Existing high-precision evidence is hostile to that membership.

The exact remaining source relation is

$$
\boxed{
\Delta^\top|_{\mathcal E_{\rm Ev}}=0,
\qquad
\Delta=U_{\rm G4}-T_{\rm PB},
}
$$

where `T_PB` is the constructed pair-to-bordered crossing and `U_G4` is the corrected arithmetic forward map on the same polarized pairings.

Locally, this requires all four ordered prime-cell identities

$$
\mathfrak G_p^{\rm St}(e_j,e_k)
=
\mathfrak G_p^\theta
(Q_p^{\rm lin}e_j,Q_p^{\rm lin}e_k),
\qquad j,k\in\{1,2\}.
$$

The off-diagonal entries must reproduce the ordered bulk flux, not merely the positive face sum. At first moment-jet order that flux has the explicitly computed signed symmetric/skew tensors on `(M_1,M_2,M_3)`, but full covariance requires the infinite reciprocal moment ladder and must keep moment order separate from Euler grade.

Two honest continuations remain:

1. **Constructive:** materialize the complete theta-history sesquilinear coefficients and radical quotient on `Q_p^lin e_1,Q_p^lin e_2`, then evaluate the four identities before Xi specialization.
2. **Falsifying:** install a validated complex-ball backend and interval-certify the nonzero unchanged-Evans Hilbert residual using the recorded certificate contract.

Neither input is presently available in the repository/runtime: the theta-history mixed coefficients are not declared, and no Arb/Sage/python-flint interval backend is installed.

No further scalar manipulation of Xi, Fourier sewing, determinant equality, endpoint traces, or positive Gram sums can determine the missing ordered entries. Doing so would restate or fit the membership condition.

Status: objective not proved; exact source coherence isolated; current executable environment has no nonredundant continuation beyond supplying one of the two missing inputs above.
