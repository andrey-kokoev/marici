# v59: native conductor dual endpoint attachment

Independent replay passed 61,514 assertions. The actual normalization source
dual has branch cohomology in degree three and conductor cohomology in degree
five, connected by a mandatory nonsplit source-defined attachment.

`rzk/79-native-conductor-dual-endpoint-attachment.rzk.md` records both native
three-coordinate attachment factors. On the minus sheet the old `(X2,X4)`
occurrence Gysin map is only a factor: the source and the labelled collar both
force the missing `X0` factor. The plus attachment is ordered
`(X1,X3,X5)`.

Two complete fixed-frame Hom tests are negative. The five-variable packet has an
injective degree-zero differential with 28 unit pivots, so it admits no primitive
map into the native dual. The isolated conductor line has 49 unit pivots and no
primitive derived section. Thus the nonsplit native attachment cannot be
replaced by agreement on its degree-five cohomology class.

The Rzk module passes a fresh check. Spatial collars have the expected labelled
threefold weights but raw conductor specialization zero; their identification
with the native Gysin attachment and physical connector 2-cells remains open.
