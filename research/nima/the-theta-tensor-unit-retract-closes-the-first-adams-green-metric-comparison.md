# The theta tensor-unit retract closes the first Adams Green-metric comparison

## Canonical counit already exists

The retained positive-integer theta labels form a multiplicative monoid with
unit \(1\). On the diagonal Mellin orbit,

\[
\Delta f=(\mathcal M_nf)_{n\ge1},
\]

define

\[
\varepsilon_1
=
\mathcal M_1^{-1}\operatorname{ev}_1.
\]

Then

\[
\varepsilon_1\Delta f=f.
\]

Thus the labelled constructor has a canonical counit before scalar theta
augmentation. It is unit evaluation, not inversion of the theta sum.

## Corrected first-edge architecture

The exact labelwise Green square gives

\[
\mathcal C^{-1}\mathcal M_ns_p
=
\mathcal M_nb_p
\]

for every \(n\). At the tensor unit,

\[
\mathcal C^{-1}\mathcal M_1s_p
=
\mathcal M_1b_p.
\]

Applying \(\varepsilon_1\) yields

\[
b_p
=
\mathcal M_1^{-1}
\mathcal C^{-1}
\mathcal M_1s_p.
\]

The ordered primitive then gives

\[
d_p
=
-\frac12S_{\mathrm{ord}}b_p.
\]

Therefore the first Adams constructor descends canonically to one Stieltjes
boundary copy without any fitted label functional.

## Metric comparison

The half-density chart

\[
(\mathcal M_1f)(u)
=
e^{u/2}f(e^u)
\]

is unitary. Hence

\[
\|\mathcal M_1d_p\|
=
\|d_p\|.
\]

Let \(D_{p,\pm}^{(1)}\) be the reciprocal shifted-history block on the
unit-label fiber. If the source intertwining identifies it with the master
auxiliary block by

\[
D_{p,\pm}^{(1)}
=
\mathcal M_1D_{p,\pm}^{\mathrm{St}}\mathcal M_1^{-1},
\]

then

\[
\left\langle
\mathcal M_1d_p,
(D_{p,\pm}^{(1)})^{-1}
\mathcal M_1d_p
\right\rangle
=
\left\langle
d_p,
(D_{p,\pm}^{\mathrm{St}})^{-1}d_p
\right\rangle.
\]

Thus the labelled Green-metric comparison constant is exactly one on the
tensor-unit summand.

## Schur consequence

Use the odd incidence

\[
c_p^{(1)}=\mathcal M_1d_p
\]

for the first Adams Schur cell. The Wronskian endpoint factor remains

\[
j_\theta
=
\begin{pmatrix}
\frac14\\
-\frac14
\end{pmatrix}.
\]

The previously proved theta-mass bounds then apply without an unknown label
comparison constant. In particular,

\[
q_{p,\pm}^{(1)}
<
16a_p
\]

for every prime, conditional only on the unit-fiber shifted-history
intertwining above.

## Full theta labels remain downstream

Selecting the tensor unit for the constructor does not delete the completed
theta observer. The architecture is

\[
\text{labelwise Adams square}
\xrightarrow{\operatorname{ev}_1}
\text{unit-fiber Adams edge},
\]

while independently

\[
\text{labelwise Adams square}
\xrightarrow{\Sigma_\theta}
\text{completed theta/xi observer}.
\]

The first map is a counit on the diagonal orbit. The second is arithmetic
synthesis. They have different authority and need not coincide.

## Quotient descent

The tensor-unit retract survives a completed quotient

\[
q:\mathcal H_{\mathrm{lab}}\to\mathcal H_{\mathrm{comp}}
\]

exactly when

\[
\ker q
\subseteq
\ker(
\mathcal M_1^{-1}\operatorname{ev}_1
).
\]

A safer order is to apply unit evaluation before any quotient that merges or
symmetrizes labels. Then no descent theorem is required.

The source-authorized constructor order should therefore retain \(n=1\)
through Green resolution and evaluate it before scalar theta synthesis.

## Remaining gate

The only local metric statement still requiring explicit source typing is
that the reciprocal shifted-history block restricts covariantly to the
unit-label half-density fiber. This is much smaller than comparing the full
renormalized label metric.

It follows if every history and Green operation acts identically in the
analytic factor and diagonally in \(n\), as the labelwise square asserts.

## Minimal hostiles

1. Apply scalar theta synthesis before unit evaluation.
2. Quotient away the \(n=1\) coordinate and attempt to reconstruct it.
3. Use a label-dependent shifted-history normalization on the unit fiber.
4. Treat \(\varepsilon_1\) as faithful on arbitrary label packets rather
   than only the diagonal orbit.
5. Merge theta and valuation-prime labels before taking the unit retract.

## Verdict

The missing theta counit and labelled metric comparison both collapse on the
multiplicative tensor unit. Unit evaluation followed by the inverse
half-density chart is canonical, and \(\mathcal M_1\) is unitary.

Accordingly, the first Adams edge can be formed on the \(n=1\) Green fiber
with the already proved prime-uniform Schur margin. The full theta-label sum
remains a downstream completed-xi observer, not part of the constructor
inverse.
