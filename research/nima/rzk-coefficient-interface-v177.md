# v177: selected L2-to-log road-cell interface

The road realization target is now reduced from an unnecessarily strong cell
for every integral A-relation to the one rho0-detected relation needed by the
physical amplitude.

For a distinguished source relation `A(source)=0`, a selected realization is
one road cell whose boundary is the Cech image of `B(source)`. Given a local log
primitive cell and that boundary comparison, the realization is inhabited
constructively and exposes both its cell and boundary law.

The selected coefficient datum is `v=a^3+a^3b`, with `rho0(v)=1` and tested
order six. The remaining nonformal equality is literal and geometric:

`roadBoundary(gamma) = coefficientToCech(v)`.

It must be proved in the physical ringed filtered Q comparison. Neither the
rank calculation nor the abstract log packet supplies this equality by itself.
The full all-relation realizer of module 192 is not required for the rank-one
physical witness.

`rzk/205-selected-l2-log-road-cell.rzk.md` passes all eight declarations without
assumptions.
