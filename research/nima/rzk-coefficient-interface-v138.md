# v138: endpoint-to-Gysin W transport stage

The W comparison was decomposed using the detailed adjunction replay. All 16
line-retaining endpoint/Gysin cases were checked: plus and minus endpoints,
three pair choices and one triple, each on central and noncentral faces.

In every case the line and Gysin complexes have normalized H0 rank one, the
comparison cone is acyclic, and the comparison fibre retains H0 rank one. The
recorded reductions use integral unit pivots. Thus endpoint-to-coefficient-
Gysin transport is already an integral quasi-isomorphism throughout the tested
atlas.

The W-survival comparison of module 165 is consequently reduced to its final
stage only: identify the coefficient Gysin class with the physical supported
class/residue. Re-proving endpoint transport or native nonvanishing is
unnecessary.

Evidence is `results/endpoint-gysin-W-transport-stage.json` from
`checkers/check_endpoint_gysin_W_transport_stage.py`.
`rzk/166-endpoint-to-gysin-W-transport-stage.rzk.md` passes all six declarations
without assumptions.
