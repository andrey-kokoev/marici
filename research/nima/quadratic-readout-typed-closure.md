# The Born Readout Is Generated Only after Star-Pairing Closure

The linear admissible-probe calculus cannot contain intensity. For an
amplitude \(z\), every linear readout scales by two under \(z\mapsto2z\), while

\[
|2z|^2=4|z|^2.
\]

The readout becomes source-generated when the coefficient object supplies its
full \(*\)-algebra structure. For two path amplitudes,

\[
I(a,b)=(a+b)(\bar a+\bar b),
\]

and the interference term is forced by polarization:

\[
I(a,b)-I(a,0)-I(0,b)=a\bar b+\bar a b.
\]

Thus the physical readout is neither a primitive linear probe nor an arbitrary
UI choice. It is a derived probe generated by amplitude addition, real
structure, and positive pairing.

The attack therefore does not falsify the corrected DPC. It exposes its main
burden: the source must independently provide the coefficient enrichment that
generates the readout. Saying “close under everything needed” would be
circular.

The next falsifier is sharper: for each sector, freeze the declared enrichment
before inspecting the observed readout, then test membership in its generated
probe closure.

Sequence claim: seqclaim-e9092fe62acb283c60ef226e.
