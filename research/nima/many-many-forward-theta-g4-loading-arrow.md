# Many-many forward theta–G4 loading arrow

For forward radial shell synthesis, select

$$
 c_p=2(\log p)\sum_{k\ge1}p^{-k/2}\Phi'(k\log p).
$$

The comparison arrow has type

$$
C_{\rm FP}:X_{\rm FP}^{\rm ret}\to X_{G4},
$$

where the retained source coordinates include prime, grade, shell, ordered pair, endpoint, Wronskian, and Laplace-jet data.

Its local target readout is the ordered theta-shell section

$$
I_{nm}^{[a,b]}(z)=-\int_0^\infty e^{-zt}\rho_{nm}^{[a,b]}(t)\,dt,
$$

with diagonal shell specialization

$$
R(z)+2E(z)
=\frac{\rho(0)+E(z)-\frac12W(z)}{z}+2E(z).
$$

The arrow must intertwine:

$$
C_{\rm FP}R_{\rm shell}=R_{G4}C_{\rm FP},
$$

and the reciprocal contragredient response. It must also preserve the Xi-adic filtration and the corrected Hadamard four-block decomposition.

The coefficient `c_p` is selected by constructor role for forward theta-shell incidence. The determinant counterterm retains the separate mixed loading `omega_p^mix`.

Status: forward loading arrow typed and role-selected; G4 source authorization and full carrier comparison remain open.
