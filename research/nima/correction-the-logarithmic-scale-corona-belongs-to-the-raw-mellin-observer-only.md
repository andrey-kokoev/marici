# Correction: the logarithmic scale corona belongs to the raw Mellin observer only

## Scope correction

The calculations

\[
\|\ell_s\|_{\mathcal E^{*}}^{2}
=
\sum_{n\ge2}
\frac{n^{-2\sigma}}{2\log n}
\]

and

\[
\|\ell_{\frac12+\varepsilon}\|_{\mathcal E^{*}}^{2}
\sim
\frac12\log\frac1\varepsilon
\]

are exact for the raw coefficient-space Mellin aggregation observer paired with the interval energy.

They do not yet describe the completed theta observer on the seam.

The source explicitly warns that outside the Euler chamber the observer is a relative theta-heat distribution, not the termwise covector

\[
(n^{-s})_{n\ge1}.
\]

Analytic continuation cannot be implemented by continuing the divergent Riesz representative coefficientwise.

## What remains valid

The raw calculation proves:

- bounded mixed incidence in the open half-sector \(\sigma>1/2\);
- failure of the raw observer to extend boundedly to the seam in the interval-energy Hilbert norm;
- logarithmic scale escape for that raw Riesz family;
- the impossibility of obtaining seam boundedness by termwise completion without changing topology.

It supplies a hostile and an interface defect.

## What is not yet authorized

It does not prove that the completed theta observer has:

- logarithmic norm divergence;
- a uniform scale-corona defect measure;
- no finite-dimensional boundary residue;
- or the same Riesz representatives near the seam.

Theta-heat transport, Poisson sewing, and relative finite-part subtraction may reorganize the raw scale escape into the external boundary ports. That reorganization is precisely the missing comparison theorem.

## Required comparison map

Let

\[
\ell_s^{\mathrm{raw}}
\]

denote the Euler/Mellin coefficient observer and let

\[
\ell_s^{\Theta}
\]

denote the completed relative theta observer.

The source must construct an interface rewrite

\[
\mathcal C_s:
\ell_s^{\mathrm{raw}}
\Longrightarrow
\ell_s^{\Theta}
\oplus
b_s^{\mathrm{ext}}
\]

in their common chamber, where \(b_s^{\mathrm{ext}}\) records the constant-delta and archimedean boundary terms.

Then prove that \(\mathcal C_s\) extends, in the declared rigged topology, to the seam.

Only this theorem can decide whether the raw logarithmic corona:

- cancels internally;
- becomes a finite external port;
- survives as a continuum defect;
- or splits among several boundary types.

## High-information invariant

In the common chamber, the rewrite must preserve the scalar observer while retaining the mixed Green form:

\[
\ell_s^{\mathrm{raw}}(c)
=
\ell_s^{\Theta}(\mathcal T_sc)
+
b_s^{\mathrm{ext}}(\mathcal B_sc).
\]

Equality of scalar values alone is insufficient. The comparison must preserve cutoff restriction, phases, radicals, and boundary orientation.

## Revised frontier

The next task is not to build the scale-corona compactification as if it were already the completed boundary. It is to use the raw corona as the hostile input for the Euler-to-theta interface rewrite.

A successful rewrite must explain where every unit of the logarithmic dual-energy mass goes. Failure to account for it is a precise completion leak.
