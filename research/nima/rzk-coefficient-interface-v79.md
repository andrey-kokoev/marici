# v79: conormal trace extension and mixed obstruction

Independent replay passed 13,549 checks.

`rzk/105-conormal-trace-extension-obstruction.rzk.md` records that the
difference of the two fully normalized trace diagrams has first-conormal
readout `beta*eta35` tensored with the separately retained `35` normal line.
It is represented by the explicit rank-two extension with
`X35*u = beta*v`, `X35*v = 0`; this extension splits on the `beta=0` face.

The conductor Ext ranks in degrees one through three are `(6,24,92)`. Right
multiplication by `eta35` has rank five and kernel exactly `A*eta35`.
Consequently an ordinary rank-one-successive-quotient continuation can use only
the same normal direction. Composition with the reflected `04` normal carries
a nonzero mixed-relation obstruction.

The Rzk module passes a fresh check. The obstruction applies to the stated
ordinary three-layer module continuation and is not asserted as a universal
obstruction to every higher physical comparison.
