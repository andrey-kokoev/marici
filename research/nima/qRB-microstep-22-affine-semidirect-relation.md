# qRB microstep 22: affine semidirect relation

With

$$
(D_af)(x)=a^{1/2}f(ax),
\qquad
(T_bf)(x)=f(x-b),
$$

one has the exact relation

$$
D_aT_bD_a^{-1}=T_{b/a}.
$$

This is the minimal covariance law for the combined observer packet. It shows that the correct comparison object is not a single translation or dilation orbit, but the affine semidirect action generated by both.

The relation is algebraic and independent of positivity. It supplies the source-side coherence law that any later boundary or relative-positive filler must respect.

Status: affine covariance law recorded; comparison with the arithmetic wall-kernel coordinate remains open.
