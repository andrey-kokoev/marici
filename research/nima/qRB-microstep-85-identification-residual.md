# qRB microstep 85: identification residual

The former residual tested an equality that is now withdrawn. Define instead the placement residual

$$
\mathcal E_{\rm place}
=\text{implemented linking block}+\frac12J_{\rm link}.
$$

The valid target is `E_place=0` on the common core, with the old Green boundary form retained separately. A finite packet tests block placement and skew cancellation; it must not identify the linking form with the endpoint–gamma–prime source form.

Status: corrected placement residual isolated; owner adoption and source normalization remain open.
