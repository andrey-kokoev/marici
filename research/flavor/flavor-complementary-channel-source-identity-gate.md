# Complementary channel source-identity gate

> Superseded target: WP989 shows that the individual mixed-vertex and pole
> coefficients are not physical quotient coordinates until kinetic
> normalization is included. The source-mismatch audit remains correct, but
> those three records are no longer the minimal physical reopening.

Work package: WP988  
Owner: marici.Figueiredo

## Question

Do the existing quartic and pole instruments actually measure the source
coordinates used by WP986?

## Typed source coordinates

WP981--WP985 use

\[
\widehat\rho=\frac{\Gamma^2A^4}{BC^5}.
\]

These symbols are dimensionless factors in the WP977 mediator source:

- \(\Gamma\) normalizes the mixed quartic vertex \(s\det A\);
- \(A\) normalizes the cubic entrance \(A[X,Y]\);
- \(B\) normalizes the CP-odd scalar mass squared;
- \(C\) normalizes the Hermitian-adjoint mass squared.

An instrument matches a complementary row only if its measured operator or pole
is generated by that exact source term on the same witness.

## Exact source-identity audit

WP560 measures the self-quartic coefficient in

\[
V_s=\frac{\lambda_s}{4}s^4.
\]

WP977's \(\Gamma\) instead belongs to \(-\gamma s\det A\). No admitted
arrow equates \(\lambda_s\) and \(\gamma\), so WP560 has zero authority
for the \(\Gamma\) row.

WP534's complex poles are gauge-sector poles of the frozen WP516/WP527 source
witness. WP977's \(B,C\) are the masses of its newly introduced scalar
\(s\) and adjoint \(A\). No admitted embedding identifies either pair, so
WP534 has zero authority for the \(B\) and \(C\) rows.

The source-identity incidence matrix from existing candidate instruments to
the three complementary coordinates is the zero matrix. Its rank is zero.
This obstruction precedes calibration, detector resolution, and covariance.

## Consequence

WP987's negative conclusion remains, but its row-wise-fragment language is
superseded. Existing packets concern different operators and source witnesses.
The minimal reopening is to construct records for the mixed \(s\det A\)
vertex and the same source's \(s\) and \(A\) poles, prove weak-basis
descent, and only then construct their shared-provenance likelihood.

This could identify a realized constructor. It would not select its values.

## Smallest falsifier

The smallest falsifier is one explicit source-preserving operator map from a
current measured record to one of \(s\det A,m_s^2,m_A^2\). A numerical
resemblance or shared engineering name is not such a map.

## Reproduction

Run:

    python research/flavor/checkers/wp988_complementary_channel_source_identity_gate.py

The generated result is
research/flavor/results/wp988_complementary_channel_source_identity_gate.json.
