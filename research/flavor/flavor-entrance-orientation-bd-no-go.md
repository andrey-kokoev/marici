# Entrance-defined orientation and B-current no-go

## Source-derived nonaligned candidate

WP514 tests the smallest nonaligned law already available in the declared
geometry. The isotropic connector identifies the entrance row frame with the
principal flavor frame. The two noncollinear entrance amplitudes then define
the real rotation

\[
U_E={1\over\sqrt{a^2+b^2}}
\begin{pmatrix}
a&0&b\\
0&\sqrt{a^2+b^2}&0\\
-b&0&a
\end{pmatrix}.
\]

This orientation is fixed by the source ray \((a,b)\) before any flavor-current
instrument is evaluated. It therefore satisfies the typing requirement that a
nonaligned candidate not be chosen by fitting the desired suppression.

## Exact current response

Conjugating the quark generators by \(U_E\) and contracting them with the full
WP508 zero-momentum kernel gives a strictly positive \(bd\) shape,

\[
C_{bd}=
{2a^2\left(3b^2\mu^2+b^2s^2+3\mu^2s^2\right)
\over
3\mu^2s^2(a^2+b^2)^2}.
\]

The generated result also records the exact \(sd\) and \(bs\) functions. Their
signs and possible cancellations are irrelevant to the present falsifier
because \(C_{bd}\) has no zero on the positive source domain.

After tree elimination and electroweak matching,

\[
|x_d|v_{\mathrm{phys}}^2=C_{bd}(a^2+b^2)
>{2a^2\over a^2+b^2}.
\]

The difference is the manifestly positive term

\[
{2a^2b^2(3\mu^2+s^2)
\over3\mu^2s^2(a^2+b^2)}.
\]

## Closure-induced floor

WP513's exact pole-product argument applies unchanged. Complete two-heavy-vector
closure requires \(b/a<8\), hence

\[
{a^2\over a^2+b^2}>{1\over65}.
\]

At the observed electroweak norm this forces

\[
|x_d|>{2\over65v_{\mathrm{phys}}^2}
={1\over1966770}\ \mathrm{GeV}^{-2}.
\]

This lower bound is independent of all three gauge couplings and cannot be
removed by changing \(\mu\), \(s\), or the common source scale.

## Executable instrument test

The squared \(\Delta M_d\) response is again quadratic along the fixed real
correlated ray. WP514 verifies the quadratic at twice the boundary and checks
that its minimum lies outside the closure-allowed negative ray. At the
least-magnitude boundary,

\[
\Delta M_d^{\mathrm{boundary}}
=2.4039901219021345\mathbin{\cdot}10^{-7},
\]

while the bundled measurement is

\[
\Delta M_d^{\mathrm{exp}}=3.334\mathbin{\cdot}10^{-13}.
\]

The discrepancy exceeds 10.8 million frozen standard deviations after the
WP511 correlated theory variance is included.

## Disposition

The connector-mediated entrance-ray orientation cannot simultaneously supply
the observed electroweak normalization, quark-only total-width closure, and
calibrated \(B_d\) viability. Together with WP513, this closes the two minimal
source-derived orientations available without adding new geometry.

This is not an orientation-independent no-go. A more elaborate nonaligned law
would require a separately declared source object and must be tested against
all \((K,B_d,B_s)\) ports before it gains authority. Otherwise the honest route
is to abandon quark-only closure and calculate the open-channel widths.

The smallest exact falsifier is

\[
|x_d|>{1\over1966770}\ \mathrm{GeV}^{-2}.
\]
