# Existing source ratios do not yet map to the threshold ratio: WP1027

## Question

Does a ratio already selected elsewhere in the flavor programme supply the
missing upstream value of the WP1025 coordinate \(r=f/M\)?

## Admitted source family

The bounded inventory contains only ratios derived before this comparison:

- reciprocal and exchange symmetry: \(1\);
- the lower unit-flux branch: \(\sqrt2-1\);
- the Higgs-flavon amplitude ratio: \(1/\sqrt2\);
- minimal oriented tadpole fibers: \(2\) and \(3\).

These are genuine selectors only in their declared source domains. None of
their packets declares a covariant dimensionless interface into \(f/M\).

## Exact contextual partition

The WP1025 interval is

\[
\frac{1061950037427}{4398046511104}
<r<
\frac{539770202345}{2199023255552}.
\]

Under the identity interface, zero of the six source presentations lies in
this interval. More importantly, the admitted source-to-threshold interface
family is empty. Equality cannot be asserted merely because both objects are
dimensionless.

If a free positive scale \(s\) is introduced, every nonzero candidate \(x\)
can be sent to any target \(r_0\) by \(s=r_0/x\). This deliberate-failure
family proves that post-hoc numerical alignment is normalization authority, not
a source-derived selector.

## Classification and instrument

The programme contains source ratio selectors, but no admitted arrow from them
to the threshold coordinate. The contextual partition therefore has six
upstream presentations, five exact value classes, and zero typed \(f/M\)
interfaces. A rescaled identification would rigidify a common presentation
while leaving its scale freely chosen.

No existing source-ratio instrument is calibrated as an \(f/M\) measurement.
The CKM/Jarlskog instrument remains a downstream inverse readout only.

## Smallest exact falsifier

WP309 and WP312 both select the number one in different source experiments.
That equality does not identify either physical ratio with \(f/M\). The
missing interface, not another stationary calculation, is already visible in
this two-object pair.

## Claim boundary

This inventory is bounded to the explicitly listed admitted selectors and
tests their declared and identity interfaces. It does not exclude a new source
constructor that derives both a ratio and its map into the mediator threshold.

## Disposition

Negative but progressive: construct a source-covariant dimensionless interface
into \(f/M\) before comparing numerical values.

Verification: uv run --with sympy python
research/flavor/checkers/wp1027_source_ratio_interface_inventory.py
