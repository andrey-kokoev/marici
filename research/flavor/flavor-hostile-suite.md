# Uniform hostile suite for surviving flavor routes (WP69, move 10/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

Seven hostile tests are applied uniformly: measured-ten collision, same-orbit
weak-basis rotation, RG reversibility, threshold kernel versus image,
frozen-versus-fitted boundary normalization, fixed-versus-covariant reference,
and positive-channel authority/data gates.

The suite leaves no ambiguous survivor. `physical16` probes separate but do
not select; RG transports; thresholds lose UV data but remain surjective; a UV
boundary selects only as an absent independently normalized template;
expectations have unauthorized false loci; a reference changes the groupoid;
and chart/spurion phases fail descent.

Verification: `python research/flavor/checkers/wp69_hostile_suite.py`.
