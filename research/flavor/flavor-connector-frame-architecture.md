# Dynamical connector-frame architecture

## Renormalizable relational repair

WP483 adds a real connector frame `S_{alpha i}` between three cyclically
related singlet entrances and the oriented adjoint triplet. For each up/down
charge sector, use two vectorlike messenger stages `A_alpha` and `B_i` with

\[
y_H\bar QH A_{R\alpha}
+y_S\bar A_{L\alpha}S_{\alpha i}B_{Ri}
+y_X\bar B_{Li}X_i u_R
+M_A\bar A_{L\alpha}A_{R\alpha}
+M_B\bar B_{Li}B_{Ri}.
\]

Every displayed interaction has canonical dimension four. A cyclic row
symmetry enforces equal entrance couplings, while the right index transforms
with the oriented triplet.

## Positive frame vacuum

The source potential

\[
V_S=\lambda_S
\left\lVert S^TS-s^2I_3\right\rVert_F^2,
\qquad \lambda_S>0,
\]

is positive and renormalizable. At unit normalization, `S=I_3` is a global
zero-energy stationary point. Its exact Hessian spectrum is

\[
0^{\times3},
\qquad8^{\times6}.
\]

The six positive modes deform lengths and orthogonality. The three zero modes
rotate the frame orientation.

For equal entrance routes, the loop pairing is the row Gram

\[
W=S^TS.
\]

Consequently the frame vacuum gives `W=I_3` exactly and repairs WP482's
isotropic-portal descent at the algebraic source level.

## Changed relational experiment

This is not recovery of an absolute triplet orientation. The original object
was an unframed oriented adjoint triplet. The new object contains a dynamical
frame and is quotiented by the joint stabilizer of `S` and `X`. At the unit
frame, the declared discrete stabilizer is the diagonal cyclic subgroup inside
the row and right actions.

The three orientation zero modes show that the reference construction is not
yet a viable particle spectrum. They must be gauged or lifted by an additional
source term whose effect on the Gram and all residues is recomputed.

## Matter and fixed-point cost

Two messenger stages, three ports, two Standard Model charge sectors, and
three colors give thirty-six messenger Dirac fundamentals. Together with the
ordinary quarks, the active `SU(3)_F` theory has forty-two fundamentals. In the
WP464 convention,

\[
b_0=-{37\over2},
\qquad b_1=-493,
\qquad g_{F,\rm formal}^2=-{296\pi^2\over493}.
\]

Thus the connector repairs the Gram but worsens the gauge-only fixed-point
obstruction.

## Disposition

WP483 is a renormalizable relational connector architecture and an interaction
rigidifier. It is not a numerical selector and has no detector instrument.
Its smallest exact falsifier is the coexistence of exact Gram repair with three
physical orientation zeros and a negative gauge-only root.

The next gate is to lift or gauge the orientation modes without spoiling
`S^T S` isotropy, derive the enlarged coupled fixed ray, place both messenger
stages above the flavor-vector poles, and recompute the full scalar/vector
poles, residues, and widths.
