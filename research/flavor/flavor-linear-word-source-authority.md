# Linear-word source authority: WP647

## Question

Can the independently declared oriented (SO(3)) triplet source authorize the
sixteen real coefficients whose WP646 response has rank nine on the faithful
ten-dimensional quark quotient?

## Exact descent test

Write a degree-one coefficient as (c_iJ_i). Descent under the original source
groupoid requires

\[
L_a c=0\qquad(a=1,2,3),
\]

where (L_a) are the real triplet generators. The stacked infinitesimal system
has rank three. Its common kernel is zero. Hence the source-authorized linear
coefficient family is

\[
c=0.
\]

For a degree-two coefficient tensor, the simultaneous commutant of the three
generators is one-dimensional and generated by (delta_{ij}). The spin-one
Casimir then gives

\[
\sum_iJ_i^2=2I.
\]

The original symmetric source therefore supplies only a flavor-universal
quadratic response. It does not authorize WP646's rank-nine linear window.

## Hostile reference-port repair

Adding one oriented triplet (n_i) permits the relative word (J_n=n_iJ_i),
but changes the experiment from the (SO(3)) groupoid to the stabilizer
(SO(2)_n). At WP646's exact nondegenerate CP witness, the family

\[
\{I,J_n\}_{u,d}
\]

has eight real controls and exact response rank six. Thus one reference port
does not recover the rank-nine window. It is a new relational experiment, not
descent of an absolute orientation from the original source.

## Classification

- Original source: neither selector nor rank-nine carrier.
- Arbitrary three-component coefficients: universal fitting inputs with no
  source authority.
- One oriented reference: relational presentation rigidifier and rank-six
  probe, not a rank-nine selector.
- Source-derived selection remains conditional on a dynamical
  symmetry-breaking tensor, its action, vacuum, and physical instrument.

The smallest exact falsifier is a nonzero vector fixed by all three triplet
generators. The checker proves that no such vector exists.

## Reproduction

Run:

```powershell
uv run --with sympy python research/flavor/checkers/wp647_linear_word_source_authority.py
```

The generated result is
`results/wp647_linear_word_source_authority.json`.
