# Contextual partitions on the complete fitted ensemble (WP68, move 9/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

Exact accounting over the stored complete 1,210-sheet viable ensemble keeps
four partitions separate:

- fit provenance: 370 exact-target and 840 extra viable valleys;
- stationary selector: 0 accepted within one sigma, 1,210 rejected;
- physical readout on a generation-exchange doublet: one two-element class;
- physical plus deck-odd probe: two presentation singletons.

On the 73 diagonal-texture reference domain, 72 carry the certified deck
partner and one predeclared oddball lies outside the involution's domain.

The sheet-wise stationarity prediction and observation ranges are disjoint;
zero of 1,210 sheets passes. Therefore stationarity does not survive the
complete fitted ensemble. The deck probe does separate presentations but does
not refine physical points. Viability, physical equivalence, stationarity, and
presentation equivalence are genuinely different contextual partitions.

Verification:
`python research/flavor/checkers/wp68_fitted_ensemble_contextual_partitions.py`.
