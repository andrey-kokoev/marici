# UV ensemble hostile controls (WP117)

Each control is a first-failure test, not a model-building suggestion.

1. **Output-fitted UV coefficients:** reject at source authority. A function
   centered on a desired `physical16` output is a target encoder.
2. **Post-readout normalization:** reject at normalization. Choosing the prior
   after inspecting flavor constants reverses the pipeline.
3. **Chart-dependent weights:** reject at descent. Source-equivalent
   presentations must assign equal orbit measure.
4. **Measured-ten injectivity:** reject at quotient faithfulness. The WP57
   hostile doublet is split by `I_11=Tr(H_u H_d)` by exactly
   `9*sqrt(10)/250`.
5. **RG as selector:** reject at arrow typing. A locally invertible flow is
   transport.
6. **Detector as selector:** reject at arrow typing. Detector inversion may
   recover route weights but cannot prepare the source ensemble.

The exact finite test uses four presentation labels `a0,b0,a1,b1`, with
`ai~bi`. An invariant map sends each orbit to `pi`. Equal within-orbit weights
descend. Changing only `w(a0)` while preserving normalization makes the two
representatives of orbit zero induce different quotient weights, realizing
the decisive falsifier.
