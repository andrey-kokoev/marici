# Resolvent bilocal authority split

## Constructive repair after WP521

WP521 proves that no finite ordinary polynomial-moment tower determines the
WP520 convolution. WP522 instead uses functions generated by the source pole
denominator itself.

The exact normalized WP520 kernel has the form

\[
F(q^2)=\frac{n_0}{D(q^2)},
\]

where \(D\) has degree six and factors into two distinct cubics. These are the
two WP508 mixed sectors carrying \(F_6\) and \(F_7\); their quintet factors
cancel from the aligned current difference. At the frozen witness,

\[
n_0=2400050384732160000.
\]

Therefore the exact bilocal response requires only

\[
H_0[\mu]=\int\frac{d\mu(q^2)}{D(q^2)},
\qquad
\mathcal B_F[\mu]=n_0 H_0[\mu].
\]

This is the smallest source-shaped target for testing the one frozen WP520
kernel. It replaces the infinite ordinary-moment tower by one direct resolvent
weight, but it is not yet physically implemented.

## Algebraic six-coordinate completion

For the enlarged rational family

\[
\frac{N(q^2)}{D(q^2)},\qquad \deg N<6,
\]

define

\[
H_k[\mu]=\int\frac{(q^2)^k}{D(q^2)}\,d\mu(q^2),
\qquad k=0,\ldots,5.
\]

The checker reconstructs every member exactly as

\[
\mathcal B_N[\mu]=\sum_{k=0}^5 n_kH_k[\mu].
\]

Evaluation at six rational spacelike nodes gives a weighted Vandermonde matrix
with nonzero exact determinant. The full basis has rank six, and deleting any
one coordinate leaves rank five. Thus six coordinates are generically minimal
on this algebraically enlarged numerator family.

That is an algebraic statement, not source or control authority. The current
flavor action has not shown that arbitrary numerator directions are
independently preparable. Treating the six-coordinate span as six executable
probes would violate the distinction between algebraic span and control.

## Typing

- Frozen source domain: the single WP520 aligned kernel.
- Frozen source-derived target: one direct \(H_0\) bilocal functional.
- Algebraic completion: six rational coordinates for arbitrary numerators of
  degree below six.
- Classification: exact finite representation and authority split; neither a
  selector nor a realized instrument.
- Smallest falsifier: the denominator does not factor into two simple cubics,
  the frozen numerator is nonconstant, or the rational basis loses rank.

The physical gate is now narrow: calculate \(H_0\) for the neutral-\(B\)
system with calibrated normalization, threshold running, and uncertainties.
The six-coordinate completion requires additional source authorization before
it can be called a probe family.
