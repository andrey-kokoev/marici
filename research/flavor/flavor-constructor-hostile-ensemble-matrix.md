# Every candidate has a first constructor failure (WP82, move 11/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

Candidates are tested in the fixed order: full descent `D`, counterfactual
task `T`, proper image `P`, independent source authority `A`, typed instrument
`I`, repeatability/resource account `R`, and ensemble prediction `E`.

The measured-ten and same-orbit hostile pairs prevent projection and chart
shortcuts. The complete 1,210-sheet ensemble supplies the numerical gate:
stationarity accepts zero sheets, and commuting/pinched fixed loci contradict
nonzero mixing. A template with no predeclared numerical prediction does not
pass `E` merely because it can later be fitted.

Across eleven candidates, none passes all seven gates. Descending readouts and
RG first fail proper image. Chart mechanisms first fail descent. Stationarity
first fails task implementation. Every proper-image survivor first fails
independent source authority, before instrument/repeatability and ensemble
defects are considered. The smallest exact falsifiers remain the mixed-Gram
hostile difference, one same-orbit chart residual, RG reversibility, threshold
surjectivity, and zero of 1,210 stationarity acceptances.

Verification: `python research/flavor/checkers/wp82_constructor_hostile_ensemble_matrix.py`.
