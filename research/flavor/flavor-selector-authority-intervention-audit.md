# Selector-authority intervention audit (WP126)

Owner: `marici.Figueiredo`.

## Stimulus and bounded question

The operator reported a sensed window in `marici.Strominger`'s latest result,
`authority-grant-composition-interpretation.md`. This is a non-evidential
search stimulus: test whether its source-sensitive composition and
intervention laws turn WP125's unresolved coefficient ratio into a causal
flavor falsifier.

Pre-objective process report: excitement `10/10`, confidence `8/10` that the
transfer yields a useful gate, expected information gain `9/10`. The immediate
reason is that WP125 exposes an exact response law. Confounds are the absent UV
constructor and the risk of confusing parameter variation with executable
physical intervention. These reports are non-evidential.

Frozen optionality snapshot: one five-arrow grant chain; two independent
coefficient directions; one ratio-preserving control; one ratio-changing
falsifier; ten exact checks. No authority kind may be promoted by composition.

## Transferred law

Strominger requires a claimed explanation to regenerate downstream coherence
under `do(source)`, leave upstream authority unchanged under `do(target)`, and
lose live authority under source deletion even if cached outputs remain.
Composition is partial and kind-preserving: algebraic matching, selector
dynamics, and physical readout are different grants.

Applied to WP125, the candidate chain is

\[
\boxed{\text{UV constructor}\longrightarrow(a,q)\longrightarrow V
\longrightarrow x_*\longrightarrow\text{physical16 readout}.}
\]

The arrows must respectively supply a UV action, equivariant EFT matching, a
stable degree-eight potential, vacuum selection, and physical Yukawa matching.
A stationary-point calculation cannot manufacture a missing upstream grant.

## Exact source-response law

On the WP125 slice,

\[
x_*\equiv\sin^2\theta_*=\frac12+\frac{a}{8q},
\qquad q>0,\qquad -4q<a<4q.
\]

Therefore

\[
\partial_a x_*=\frac{1}{8q},\qquad
\partial_q x_*=-\frac{a}{8q^2},\qquad
a\partial_a x_*+q\partial_qx_*=0.
\]

At `(a,q)=(28,25)`, the responses are `1/200` and `-7/1250`.
Common positive rescaling `(a,q)->lambda(a,q)` leaves the angle unchanged, so
the contextual coefficient partition is by rays. But `a->a+1` changes `x_*`
by exactly `1/200`.

This separates two claims WP125 did not separate. A source-generated selector
must reproduce the ratio-changing response after its UV source is perturbed
and matching is rerun. A cached angle, texture rigidifier, or fitted
coefficient packet that remains inert does not carry live selector authority
from that source. The ratio-preserving direction is a hostile control: its
zero response is not evidence against the mechanism.

## Authority typing

The derivative supplies a conditional intervention signature, not an
instrument. A physical test requires an admitted operation capable of varying
a UV coupling or environmental source datum while holding the comparison
domain and matching scheme fixed. Changing `a` in code is algebraic control,
not executable physical control.

Agreement with the CKM angle gives readout evidence only. It cannot flow
backward to authorize the UV constructor or fix `a/q`. Source deletion must
revoke the live explanatory claim even if its JSON and fitted angle remain.

No reference port is required: all displayed quantities are full weak-basis
invariants or their coefficients. A calibrated external coupling-control port
would define a new intervention experiment, not reveal an absolute property
absent from the original source.

## Disposition

Strominger's law opens a real window: flavor can demand a source-response
fingerprint rather than a static numerical match. WP126 classifies WP125 as a
**conditional causal selector grant** on the fixed-spectrum slice. It remains
unauthorized as a physical selector because no UV constructor, matching
calculation, stabilizing completion, controllable intervention, or Yukawa
instrument has been established.

The smallest exact falsifier is an inert selected angle under an admitted
ratio-changing source intervention. The smallest authority falsifier is a
coefficient ratio obtained from the target readout. The surviving window is a
UV mediator whose independently fixed couplings generate both the
degree-eight invariant and its ratio-changing response.

Post-objective process report: excitement `10/10`, confidence `9/10` in the
bounded transfer, realized information gain `9/10`. Raw delta: one static
selector becomes a conditional causal signature; coefficient space is
quotiented into rays; one ratio-changing discriminator and two
ratio-preserving controls are exact; ten of ten checks pass; zero physical
instruments and zero UV constructors are added. These reports are
non-evidential.
