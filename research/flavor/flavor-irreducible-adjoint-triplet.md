# Irreducible adjoint-triplet flavon vacuum: WP447

## Question

Can an independently stated source geometry remove WP446's exact (2\oplus1) vacuum-algebra obstruction while retaining a source-free global minimum and complete stability?

## Source action

Introduce three dynamical traceless Hermitian adjoints (X_i) of gauged diagonal (SU(3)_F), with the already admitted covariant kinetic normalization and

\[
V_{447}=\lambda\sum_{i<j}
\left\lVert[X_i,X_j]-i\mu\epsilon_{ijk}X_k\right\rVert_F^2
+\rho\left(\sum_i\operatorname{Tr}X_i^2-6\mu^2\right)^2,
\]

where \(\lambda>0\), \(\rho>0\), and \(\mu>0\). This is gauge invariant, invariant under oriented rotations of the adjoint triplet, power-counting renormalizable, and contains no Yukawa, CKM, texture, or detector coordinate.

The action is a positive relational representation constraint. It does not insert three numerical flavor matrices as external sources.

## Exact global vacuum

The zero-energy conditions require an (su(2)) representation and fix its quadratic trace Casimir to six. In complex dimension three, the unitary representation possibilities are spin one, spin one-half plus a singlet, or three singlets. Their trace Casimirs are respectively (6), (3/2), and (0). Only the irreducible spin-one representation reaches zero energy:

\[
X_i=\mu J_i,
\qquad [J_i,J_j]=i\epsilon_{ijk}J_k,
\qquad \sum_i\operatorname{Tr}J_i^2=6.
\]

At unit normalization the reducible (2\oplus1) hostile representation has energy (81/4), while the origin has energy (36).

## Stability and irreducibility

The words of length at most two in the three vacuum matrices span all nine complex dimensions of (M_3(\mathbb C)). Thus there is no invariant generation subspace, repairing WP446's structural obstruction without fitting a CKM entry.

The complete 24-real-component Hessian at the unit benchmark has spectrum

\[
0^{\times8},\qquad16^{\times8},\qquad36^{\times7},\qquad100.
\]

The eight zeros exactly equal the simultaneous (SU(3)_F) conjugation orbit. All 16 physical modes are positive, and the continuous gauge-mass rank is eight.

## Claim boundary

WP447 supplies a source-free, globally minimizing, irreducible dynamical flavon vacuum. Its word algebra has the capacity to generate a general three-generation Yukawa through messenger insertions, but capacity is not a numerical selector. A messenger word grammar and its coefficients must be frozen independently before testing the map to `physical16`.

The action fixes (\sum_i\operatorname{Tr}X_i^2=6\mu^2), not \(\mu/v\). It therefore still does not select (g_Ff/v). No current constraint, pole width, residue, or detector instrument is claimed.

## Smallest exact falsifier

A zero-energy reducible (2\oplus1) representation, word-algebra dimension below nine, or any nongauge Hessian zero or negative mode falsifies the result.
