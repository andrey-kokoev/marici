# Common-source closure of the complete tree-level heavy widths

## One source witness

WP510 extends the common singlet-clock architecture to the complete WP508
vacuum.  A single real singlet \(\sigma\) fixes all four amplitudes through
positive renormalizable radial squares.  The exact witness is

\[
\sigma=1,
\qquad
\mu=1,
\qquad
s=16,
\qquad
a=55,
\qquad
b=54,
\]

with

\[
g_F=\sqrt2,
\qquad
g_P={1\over10},
\qquad
g_E={1\over50},
\qquad
z_A=z_B=2.
\]

The four radial-square coefficients are 100 and the singlet lift coefficient
is \(10^9\).  These numbers are a source-domain existence witness fixed before
the spectral test; they are not inferred from a desired pole or clock value.

The same source fixes

\[
\left({g_Ff_{\rm phys}\over v_{\rm phys}}\right)^2
={6\over5941}.
\]

This is a conditional relational selection by one action.  It is not a
numerical prediction because the admitted symmetries do not select the listed
dimensionless coefficients.

## Exact vector closure interval

Every one of the nine cubic poles is isolated by exact root counting in

\[
{19\over10}<m^2<{31\over5}.
\]

All three cubic discriminants are positive.  The five quintet poles have
\(m^2=6\) and lie in the same interval.  Since

\[
4{\cdot}{19\over10}-{31\over5}={7\over5}>0,
\]

twice the lightest possible heavy-vector mass exceeds the heaviest possible
parent mass.  Every two-heavy-vector decay is therefore kinematically closed.

The photon is not included as a heavy daughter.  The direct-product gauge
kinetic action and the neutral vacuum supply no tree-level vertex with one
flavor–port–row vector and two electromagnetic vectors.  The massive
electroweak vectors are far above the heavy flavor spectrum at this witness.

## Common-source nonvector thresholds

The canonical five-coordinate radial Hessian is tested against
\((31/20)K\), where \(K=\operatorname{diag}(6,3,2,2,1)\) is the radial kinetic
metric.  All shifted leading minors are strictly positive.  Hence every radial
physical scalar has mass squared above \(31/20\).

The remaining exact lower thresholds are:

- adjoint physical scalars: \(m^2\ge16\);
- connector physical scalars: \(m^2\ge1024\);
- entrance physical scalars: \(m^2\ge11882\);
- both messenger stages: \(M^2=4\);
- massive electroweak vectors: \(M_W^2=5941/2\) and \(M_Z^2=5941\).

The entrance Hessian has exactly six zeros, all belonging to the broken gauge
orbit.  Positive zero-residual source squares make the radial and nonradial
blocks additive; adding them cannot lower a physical Hessian eigenvalue.

Every declared nonquark two-body daughter is consequently heavier than half
the heaviest WP508 pole.

## Width and authority disposition

At this witness the WP509 six-quark partial widths are the complete tree-level
two-body widths.  Their pole residues and fractional widths are independently
frozen by the source action and the spectral projectors, not by a detector fit.

The result does not cover loop-induced decays, off-shell tails, finite detector
resolution, or additional fields outside the declared source census.  It also
does not supply the coefficient dynamics needed to predict \(6/5941\), nor a
calibrated common-frame instrument capable of measuring the matrix residues.

The smallest exact falsifier is a heavy pole outside the certified interval or
a source-derived nonquark daughter with mass squared at most \(31/20\) and a
nonzero decay vertex.  A symmetry-preserving coefficient deformation that
changes \(6/5941\) falsifies numerical selector authority while leaving the
existence theorem intact.
