# Source restriction repairs faithfulness only after independent derivation (WP79, move 8/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

The Z4 angular potential has two minimum orbits distinguished by its
coefficient sign. Symmetry alone does not choose that sign, a vacuum history,
or a covariant flavon-to-Yukawa map. The illustrative spurion relation also
changes along one weak-basis orbit. Thus the declared spontaneous-CP picture
supplies neither a `physical16` task nor a selected source domain.

There is an exact honest repair pattern. Locally write
`physical16=(o,k)` with measured `o in Q^10` and hidden `k in Q^6`. A source
law fixed independently as

\[
k=L o+b
\]

defines a proper ten-dimensional affine subdomain on which measured ten is
injective: equal `o` forces equal `k`. This repairs faithfulness by removing
hostile directions, not adding a probe. It is itself a selector law and cannot
be inferred from the same fit without circularity.

The source declares no quotient-level `L,b`. Reopening requires a complete
potential, chosen vacuum orbit, covariant joint flavon-Yukawa quotient map,
and a pre-fit law whose restricted projection is proved injective.

Verification: `python
research/flavor/checkers/wp79_spontaneous_cp_source_domain_restriction.py`.
