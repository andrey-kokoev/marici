# Flavor-current orientation fiber: WP453

## Question

Does fitting WP450's universal messenger grammar to Standard Model flavor data determine the WP448 flavor-current response?

## Exact hostile pair

Fix the WP447 flavon vacuum and take

\[
Y_u=\operatorname{diag}(1,2,3),\qquad
Y_d=\operatorname{diag}(4,5,6).
\]

Let (W) be a (\pi/4) rotation of generation coordinates one and three, and form

\[
Y'_u=WY_uW^\dagger,\qquad
Y'_d=WY_dW^\dagger.
\]

The two pairs have identical up and down spectra and identical CKM matrix (I). They are the same Standard Model flavor point under common weak-basis conjugation. WP450's degree-two word grammar realizes both literal pairs.

They are not the same point of the enlarged source experiment when the WP447 vacuum is held fixed. Rotating the Yukawas without rotating the flavon reference changes their relative orientation.

## Current separation

Contract the mass-basis (1\leftrightarrow2) transition generators with WP448's exact (K^{-1}). Before the common factor (-1/(2\mu^2)), the two (\Delta F=2) coefficients are

\[
C_{12}=0,\qquad C'_{12}=-\frac16.
\]

Thus the source-derived current probe strictly refines the Standard Model flavor projection. Equality of measured masses and CKM data does not calibrate the flavor-gauge current orientation.

## Interpretation

This is another nonfaithful readout functor, now inside the completed dynamical theory: the map from an extended flavon-plus-messenger point to its Standard Model flavor record has a nontrivial orientation fiber. Neutral-meson mixing is the relevant physical instrument class for resolving that fiber, but applying a numerical constraint requires flavor labels, operator-basis RG evolution, hadronic matrix elements, and uncertainties in one common frame.

The current probe distinguishes; it does not select. A source law must choose the coefficient-field orientation, or experiments must measure it as an additional theory parameter.

## Smallest exact falsifier

Equality of the two exact (\Delta F=2) coefficients for the hostile pair falsifies the result.
