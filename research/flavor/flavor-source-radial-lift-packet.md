# Source radial lift and updated residue packet

## Source-derived lift

WP469 lifts WP468's physical common dilaton without remixing the already frozen
massive poles. Define the gauge-invariant canonical radial norm

\[
Q={1\over4}\sum_i\operatorname{Tr}X_i^2+H^\dagger H+{1\over2}\sigma^2.
\]

On the WP467 zero-energy family,

\[
Q=D\sigma^2,
\qquad D={3\over2}y^2+a+{1\over2}.
\]

Add the independently declared positive renormalizable source term

\[
V_{\rm lift}=\kappa\left(Q-Dw^2\right)^2,
\qquad \kappa>0,\quad w>0.
\]

It intersects the old vacuum ray at `sigma=w` and preserves
`g_F f/v=g_F y sqrt(3/a)`. The parameter `w` fixes the absolute common scale;
it does not enter the selected dimensionless ratio.

## Rank-one Hessian theorem

At the unit WP468 benchmark, the canonically normalized common-dilation vector
in `(flavor radial, Higgs radial, singlet)` coordinates is

\[
d=(\sqrt3,\sqrt2,1)^T,
\qquad d^Td=6.
\]

The old radial Hessian obeys `R d=0`. For `kappa=1`, the lift adds exactly
`2 d d^T`. Hence it changes only the dilation eigenvalue:

\[
m_{\rm dilaton}^2=2d^Td=12.
\]

The updated full 29-field spectrum is

\[
0^{\times11},\quad12,\quad16^{\times8},\quad36^{\times7},
\quad206-2\sqrt{10009},\quad206+2\sqrt{10009}.
\]

All eleven remaining zeros are gauge Goldstones. There is no physical flat
direction.

## Updated poles and residues

The lifted pole projector is `d d^T/6`, with exact diagonal residues

\[
\left({1\over2},{1\over3},{1\over6}\right)
\]

in the flavor-radial, Higgs-radial, and singlet channels. Orthogonality implies
that both pre-existing massive radial projectors and their residues are exactly
unchanged. This is an independently frozen tree-level pole-and-residue update,
not a coordinate interpolation.

## Authority and instrument boundary

The lift is a genuine source operation and repairs the massless-pole defect.
It does not derive the numerical inputs `(g_F,y,a,kappa,w)`. In particular,
`w` is an admitted relevant source scale, not a prediction of the dimensionless
fixed point.

No width follows from the Hessian. The next packet must derive mass-eigenstate
vertices and enumerate all open Standard Model, flavon, Higgs, singlet, and
messenger channels at a common scale. A detector likelihood is admitted only
after those widths and production residues are frozen.

The smallest exact falsifier is any nonzero overlap between the lift vector and
a pre-existing massive radial eigenspace, which would invalidate the claim that
their residues are preserved.

