# Gauged connector-frame orientation lift

## Source operation

WP484 gauges the right oriented port symmetry of WP483. The enlarged gauge
group is

\[
SU(3)_F\mathbin\times SO(3)_P.
\]

The adjoints `X_i` and second-stage messengers `B_i` transform as
`SO(3)_P` vectors. The connector transforms on its right index. The frame
potential depends only on `S^T S`, so gauging preserves its isotropic vacuum.
All fermions charged under the new factor remain vectorlike and in the real
integer-spin vector representation.

## Exact joint orbit theorem

At

\[
X_i=J_i,
\qquad S=I_3,
\]

construct the tangent matrix of all eight `SU(3)_F` and three `SO(3)_P` gauge
generators on the 33 real frame-plus-adjoint coordinates. Its exact rank is

\[
8+3=11,
\]

and its Gram determinant is strictly positive. There is no continuous joint
stabilizer. The `S` components prevent a simultaneous port rotation and flavor
conjugation from remaining unbroken.

## Scalar spectrum

Before gauge fixing, the block-separated tree scalar spectrum is

\[
0^{\times11},
\quad8^{\times6},
\quad16^{\times8},
\quad36^{\times7},
\quad100.
\]

The three connector null vectors are exactly the `SO(3)_P` frame tangents, and
the eight adjoint zeros are the original flavor-gauge tangents. Hence all
eleven zeros are eaten. The physical tree scalar spectrum is strictly positive:

\[
8^{\times6},
\quad16^{\times8},
\quad36^{\times7},
\quad100.
\]

This repairs WP483's massless-frame defect without changing `S^T S=I_3`.

## Relational interpretation and boundary

The new experiment is quotiented by the joint gauge group. Connector
orientation has become gauge; it has not become an absolute observable. The
reference frame still changes the physical groupoid relative to the unframed
flavor theory.

WP484 is a source-derived reference-port rigidifier, not a numerical selector.
No physical detector instrument is attached. The mixed gauge-boson spectrum,
new gauge coupling, connector thresholds, and all new decay channels remain
uncomputed. Consequently WP475-WP476's residues and widths cannot yet be
transported into this enlarged theory.

The smallest exact falsifier is the rank-eleven joint tangent Gram exhausting
all eleven scalar zeros. The next packet must compute the complete mixed gauge
mass Gram and threshold ordering before any spectral reuse.
