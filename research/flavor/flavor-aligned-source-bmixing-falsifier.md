# Electroweak-calibrated aligned-source B-mixing falsifier

## Common source-to-instrument frame

WP512 composes WP510 with WP511 rather than treating their compatible-looking
coordinates as authority. The tested messenger completion restricts the down
Yukawa to a polynomial in \(J_3\). Its mass basis is therefore aligned with the
WP447 principal \(SU(2)\) frame. The up sector may still carry the observed CKM
mixing through WP450's degree-two word grammar.

This is the smallest source-authorized orientation completion. It is not
claimed to be selected uniquely by the current action.

## Exact zero-momentum source map

WP512 reconstructs the complete eight-generator zero-momentum current kernel
from the three WP508 matrix resolvents and the two singleton quintet poles.
Contracting it with the aligned transition generators gives, before the common
tree-elimination factor,

\[
C_{sd}=C_{bs}={729\over17971525},
\qquad
C_{bd}=0.
\]

The \(bd\) cancellation is exact: the two corresponding Gell-Mann generators
remain degenerate quintet directions. The entrance vacuum splits the generator
pairs relevant to \(sd\) and \(bs\), so those coefficients do not cancel.

Consequently the aligned source image in WP511's abstract \((x_d,x_s)\) packet
has rank one even though the detector response has rank two. The first
nonfaithful arrow is the source-orientation map, not the neutral-B instrument.

## Electroweak normalization and matched coefficient

The same entrance doublets contribute

\[
v_{\mathrm{phys}}^2=11882w^2.
\]

Calibrating them to \(v_{\mathrm{phys}}=246\ \mathrm{GeV}\) fixes

\[
w={123\sqrt{11882}\over5941}\ \mathrm{GeV}
\approx2.25679\ \mathrm{GeV}.
\]

The common tree-elimination factor is \(-1/(2w^2)\). The exact WET coordinates
are therefore

\[
x_d=0,
\qquad
x_s=-{81\over20340100}\ \mathrm{GeV}^{-2}.
\]

No scale remains available to suppress this coefficient without destroying
the same-source electroweak calibration.

## Executable falsification

Passing the exact \(x_s(1,1,2)\) ray through the pinned WP511 likelihood gives

\[
\Delta M_s^{\mathrm{source}}=2.6943792100627607\mathbin{\cdot}10^{-6},
\]

whereas the bundled measurement is

\[
\Delta M_s^{\mathrm{exp}}=1.1688\mathbin{\cdot}10^{-11}.
\]

Using WP511's frozen experimental-plus-theory standard deviation gives an
absolute discrepancy of approximately \(5.37\) million standard deviations.
The full executable prediction is used; this conclusion does not extrapolate
only the local Fisher response.

Thus the simplest aligned messenger completion of the WP510 closure witness is
physically excluded once source and detector share the measured electroweak
frame.

## Scope and successor

This result does not falsify every WP450 messenger orientation. The universal
word grammar leaves a continuous relative-orientation fiber, and a different
orientation can redistribute or cancel the \(sd\), \(bd\), and \(bs\)
currents. Such a rotation cannot be chosen after reading the mixing bounds.

A progressive successor must derive an orientation law independently, then
test its complete \((K,B_d,B_s)\) current vector and physical16 record against
the calibrated instruments. The WP509 residue and WP510 tree-width theorems
remain valid as conditional spectral statements, but the aligned numerical
source point has no physical prediction authority.

The smallest exact falsifier is the pair

\[
x_s=-{81\over20340100}\ \mathrm{GeV}^{-2},
\qquad
\Delta M_s^{\mathrm{source}}\approx2.69\mathbin{\cdot}10^{-6}.
\]
