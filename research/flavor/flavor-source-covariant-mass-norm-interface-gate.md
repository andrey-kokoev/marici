# Source-covariant mass-norm-interface gate: WP1236

## Question

Can a source-covariant mass-norm interface realize the conditional
\(N=17\) pole?

## DPC resolution

- **Problem:** derive a source-covariant interface realizing \(N=17\) as an
  equal-norm mass projector with radiative closure.
- **Bold conjecture:** a rank-17 equal-mass frame, minimal single-pole
  symmetry, protected \(D/F\)-term completion, or existing fixed point
  realizes \(M^2=17f^2\).
- **Named rivals:** rank-17 Schur denominator; \(O(N)\) radial or
  spectator constructor; \(D\)-term and \(F\)-term protection; existing
  fixed-point/topological archive; direct WP802 interface; integer Clebsch
  coefficient.
- **Risky consequences:** normalized ports give Schur response \(1/m\),
  not \(1/(\sqrt{17}m)\); \(O(N)\) curvature is independent of \(N\) while
  spectator sums retain free \(g\); protected classes retain a continuous
  \(G\) or \(y\) fiber; no archived candidate passes all five gates; the
  WP802 \(h=y^2\) interface is below threshold; integer \(C=11\) and
  \(C=12\) straddle the fitted interval.
- **Strongest falsification attempt:** every proposed interface changes
  the pole type, leaves continuous normalization, lacks a typed map, or
  gives the wrong magnitude.
- **Exact residual:** a named source theorem mapping a controlled fixed
  coupling to the CP-even single-pole mass operator with threshold survival
  and instrument rank.
- **Disposition:** reject the tested interfaces; select the discrete
  source-actuator typing rival.

Checker: `research/flavor/checkers/wp1236_source_covariant_mass_norm_interface_gate.py`

Result: `results/wp1236_source_covariant_mass_norm_interface_gate.json`
