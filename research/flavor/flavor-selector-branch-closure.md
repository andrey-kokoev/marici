# Source-generated selector branch closure: WP660

## Decisive question

Does the admitted flavor source select a proper numerical subfamily or a
distinguished point of the faithful physical quotient?

The current answer is no.

WP661 subsequently repairs the two-triplet frame's minimal one-loop support
closure. The frame conclusion survives with shifted normalization; the
selector verdict below is unchanged.

## What was constructed

The positive two-triplet action constructs an ordered orthonormal frame with
trivial \(SO(3)\) stabilizer. This is a genuine source-generated presentation
rigidifier. Its allowed two-sector word family

\[
\{I,J_n,J_m\}_{u,d}
\]

is a constrained carrier. At the exact nondegenerate CP witness, twelve real
scalar controls have rank-eight intrinsic response and a four-dimensional
local kernel.

WP435-type messenger chains transport those coefficients into executable
low-energy operators, but reciprocal vertex rescaling leaves a
six-complex-dimensional constructor kernel. An ideal two-width detector
architecture can identify the two vertex magnitudes when template contrast,
two efficiency controls, and a background sideband all remain nondegenerate.

## What was not constructed

The frame action supplies no equation for the scalar sector coefficients.
They are \(SO(3)\) singlets, and legal choices produce different weak-basis
invariants. Matching transports their values but does not derive them.
Identification by a detector would read those values but would still not
explain or select them.

The two closest CMS searches establish real Higgs and new-scalar decay ports,
but do not fit both WP651 widths simultaneously in one constructor frame with
the required controls and covariance.

## Typed verdict

- Admitted domain: the local exact nondegenerate CP witness; no
  ensemble-wide selector claim.
- Faithful coordinate: physical16, as a faithful sixteen-entry embedding of
  the ten-dimensional quark quotient.
- Source-authorized family: complex coefficients of
  \(\{I,J_n,J_m\}_{u,d}\) over the two-triplet frame.
- Contextual partition: rank-eight physical image with a four-dimensional
  local control kernel.
- Classification: faithful rigidifier plus constrained carrier; neither is a
  selector.
- Smallest exact falsifier: a coefficient-fixing equation derived from the
  admitted source without another source object.
- Instrument gate: a common-frame, simultaneous two-width likelihood with
  independent controls, covariance, and stable finite-width response.

Any future coefficient law must also survive all 1,210 fitted sheets before
receiving numerical authority.

Reproduce with: uv run python research/flavor/checkers/wp660_selector_branch_closure.py

Generated result: results/wp660_selector_branch_closure.json.
