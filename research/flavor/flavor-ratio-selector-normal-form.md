# Scale-free flavor-ratio selector normal form

## Sharp selector coordinate

The WP467 common-clock vacuum gives

\[
R={g_Ff\over v}={\sqrt3g_Fy\over\sqrt a}.
\]

Define the dimensionless source coordinate

\[
c={a\over g_F^2y^2}.
\]

Then a source law `a=c g_F^2 y^2` reduces the target to

\[
R=\sqrt{3\over c}.
\]

This is the minimal scale-free normal form for the missing numerical selector.
It does not require selection of the common dilation, and it cleanly separates
the source question from the later pole and current readouts.

## What the five-TeV benchmark would require

Using the corrected electroweak clock and WP476's conditional triplet pole,

\[
v={12311\over50000}\ {m TeV},
\qquad m_1=5\ {m TeV},
\]

the readout is

\[
R_*={\sqrt6m_1\over v}={250000\sqrt6\over12311}.
\]

The corresponding selector coefficient would be

\[
c_*={3\over R_*^2}
={151560721\over125000000000}.
\]

This equation is diagnostic only. Since `c_*` was obtained from the frozen
pole and measured electroweak clock, inserting it into the action would fit the
desired answer and has no source authority.

## Hostile source pair

The WP467 source class admits every positive `a`, `g_F`, and `y`. Consequently
the two relations

\[
a=cg_F^2y^2,
\qquad a=4cg_F^2y^2
\]

both preserve the same fields, symmetries, positivity, renormalizability, and
common-clock vacuum geometry. Their predictions are respectively

\[
R=\sqrt{3/c},
\qquad R={1\over2}\sqrt{3/c}.
\]

This is the smallest exact falsifier of numerical selection by the currently
admitted source grammar.

## RG and threshold gate

For the selector coordinate to descend along the complete trajectory, it must
obey

\[
{\beta_c\over c}={\beta_a\over a}
-2{\beta_g\over g_F}-2{\beta_y\over y}=0.
\]

WP465 computes only the gauge-subsystem root and proves that it is not
preserved after messenger decoupling. The beta functions for `a` and `y`, their
messenger-Yukawa dependence, and finite threshold matching are not currently
defined. Algebraic compatibility of the coefficients therefore cannot be
promoted to an RG-invariant source relation.

## Disposition

WP477 finds the exact selector architecture but not its missing constructor.
The operation is not a texture rigidifier and introduces no reference port.
Kaon, Higgs, and pole records may falsify a source-fixed `c`; they may not
define it.

Progress now requires a complete coupled gauge-messenger-scalar action that
derives `c` independently, followed by proof of the RG and threshold condition
above. Only then may its numerical prediction be compared with the already
frozen current, pole, width, and residue instruments.
