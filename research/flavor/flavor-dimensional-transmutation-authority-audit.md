# Dimensional-transmutation authority audit (WP134)

Owner: `marici.Figueiredo`.

## Bounded question

Can asymptotically free RG dimensional transmutation lift WP132's dilation
zero mode and select the absolute flavor scale without importing a new
authority-bearing boundary packet?

Pre-objective process report: excitement `8/10`, confidence `9/10` that RG
invariance and source-free scale selection separate cleanly, expected
information gain `8/10`. The immediate reason is that dimensional
transmutation is often described as generating a scale, while its numerical
value remains a function of boundary data. Confounds are the one-coupling
one-loop truncation and possible UV fixed-point boundary conditions. These
reports are non-evidential.

Frozen optionality snapshot: one asymptotically free coupling; two
renormalization presentations of one trajectory; one physically distinct
boundary trajectory; one exact logarithmic scale residual; ten exact checks.

## Exact transmutation map

Take

\[
\beta(g)=-b g^3,\qquad b>0.
\]

One-loop integration gives the RG-invariant scale

\[
\Lambda=\mu_0\exp\!\left[-\frac{1}{2b g^2(\mu_0)}\right],
\qquad
\log\Lambda=\log\mu_0-\frac{1}{2b g^2(\mu_0)}.
\]

This map genuinely lifts a classical flat direction once a running trajectory
and matching potential are admitted. It is a conditional dynamical scale
selector, not merely a chart rigidifier.

## Presentation invariance versus source uniqueness

For the exact packet

\[
b=\frac12,\qquad \log\mu_0=0,
\qquad \frac1{g^2(\mu_0)}=4,
\]

one obtains `log Lambda=-4`. Change renormalization presentation to
`log mu_1=2`. Running on the same trajectory gives

\[
\frac1{g^2(\mu_1)}=4+2b(2)=6,
\]

and hence `log Lambda=2-6=-4`. The generated scale descends under the RG
presentation groupoid exactly.

That invariance does not select a unique trajectory. Declare the same
dimensionless coupling value at a physically shifted reference boundary
`log mu=3`; then `log Lambda=-1`. The two sources differ by the exact physical
scale residual

\[
\Delta\log\Lambda=3.
\]

They are not two renormalization charts of one trajectory because the coupling
has not been run coherently between the boundary scales. They are distinct
source packets collapsed by the dimensionless low-energy flavor geometry.

## Authority and physical-instrument boundary

Dimensional transmutation therefore improves WP132 in one respect: given an
admitted boundary pair `(mu_0,g(mu_0))`, it selects a stable relational scale
and transports it coherently across RG charts. It does not explain why that
trajectory or its dimensionful reference is realized.

A UV fixed point, asymptotic-safety trajectory, gravitational matching scale,
or cosmological boundary could close this gate only if independently derived
and matched to the flavor sector before threshold data are inspected.
Choosing `mu_0` or `g(mu_0)` to place WP128's poles inside detector reach is
target fitting.

Classification: **RG-invariant conditional absolute-scale selector with an
unresolved boundary-authority gate**. It is a genuine selector once the
boundary is admitted, but not a source-free selector. No texture chart or
measured-ten uniqueness enters. No physical threshold instrument is thereby
established.

The smallest descent falsifier is unequal `Lambda` from two coherently run
representations of one trajectory; the checker finds zero residual. The
smallest authority falsifier is the hostile boundary pair, which changes
`log Lambda` by exactly three while preserving the same declared local
coupling value.

Post-objective process report: excitement `8/10`, confidence `10/10` in the
bounded distinction, realized information gain `9/10`. Raw delta: the
classical flat direction is conditionally lifted; two RG presentations merge
with zero scale defect; one independent boundary trajectory remains distinct;
one exact log-scale residual survives; ten of ten checks pass; no boundary
source or physical instrument is added. These reports are non-evidential.
