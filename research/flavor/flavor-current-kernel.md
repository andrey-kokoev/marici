# Flavor-current kernel: WP445

## Question

What exact low-energy quark-current interaction follows from the fully Higgsed WP443 vacuum, before importing any experimental constraint or fitted mass-basis orientation?

## Current and normalization

Use canonical generators (T_a=\lambda_a/2), with

\[
\operatorname{Tr}(T_aT_b)=\frac12\delta_{ab}.
\]

The anomaly-free diagonal flavor theory couples its eight gauge fields to

\[
J_a^\mu=\sum_{q=Q_L,u_R,d_R}\bar q\gamma^\mu T_aq.
\]

This is a vectorlike generation current. It is a physical interaction in the enlarged gauged theory, not a weak-basis coordinate operation.

## Exact mass and Wilson kernels

At the WP443 normalized vacuum, the gauge-mass law is

\[
M_F^2=g_F^2f^2K,
\]

where

\[
K=\operatorname{diag}\left(
\frac14,\frac12,\frac14,
\frac38,\frac38,\frac38,\frac38,\frac13
\right).
\]

It is positive definite, so all eight flavor gauge bosons are massive. Eliminating them at zero momentum gives

\[
\mathcal L_{\rm eff}
=-\frac{1}{2f^2}J_a^\mu(K^{-1})_{ab}J_{b\mu},
\]

with

\[
K^{-1}=\operatorname{diag}\left(
4,2,4,\frac83,\frac83,\frac83,\frac83,3
\right).
\]

The factor (g_F) cancels exactly between the two current vertices and the inverse mass matrix. Low-energy four-quark bounds therefore constrain the flavon scale (f), while resolved poles separately depend on (g_Ff).

## Typing boundary

This kernel is algebraically faithful on the eight labelled gauge-current components. It is not yet a calibrated physical probe on the observed quark domain. WP443's simple Pauli/fundamental vacuum has not been matched through WP435's messenger chain to a viable `physical16` Yukawa pair. Without the left- and right-handed mass-basis rotations, one cannot identify which entries contribute to kaon, charm, bottom, or top observables.

Consequently no meson or collider limit is applied in this packet. Doing so would combine a valid source current and a valid experimental readout without the missing common-frame constructor.

## Classification

The operation is a source-derived current kernel. It neither selects (g_Ff/v) nor yet has an admitted flavor-specific physical instrument. Its smallest exact falsifier is a zero eigenvalue of (K) or residual (g_F) dependence in (g_F^2(M_F^2)^{-1}).

The next gate is a source-derived messenger-to-`physical16` mass-basis map. Only after it passes may experimental flavor-current constraints be attached.
