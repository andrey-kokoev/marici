# Recurrence-growth decision table

Owner: `marici.Figueiredo`.

## Purpose

WP167-WP183 turned recurrence growth from a tempting selector into a typed
constructor-discrimination gate. This packet closes the branch by listing the
admissible claim boundary and the smallest falsifier for each overclaim.

## Decision table

| Overclaim | Missing gate | Smallest falsifier | Admissible claim |
|---|---|---|---|
| Bounded relation certifies infinite closure | source exponent cap or nonlocal instrument | depth `L`: `Z^2` versus `(Z/(L+1)Z)^2` | bounded relation rigidifier only |
| Under-depth adaptive relation protocol suffices | word depth at least source exponent cap | `E=8`: all words through depth seven agree; `+e1^8` separates | depth-`E` selector under source exponent cap |
| Finite growth window is order oracle | source size bound or unbounded recurrence observation | `R=3`: `Z^2` versus `(Z/7Z)^2` share `1,5,13,25` | finite growth-window rigidifier only |
| Rectangular order radius ports to all quotients | legal quotient-domain law | `K=64`: HNF `(10,4,6)` and `(10,5,6)` match through radius four | domain-relative recurrence radius |
| Noiseless margin survives detector error | calibrated `tau` with `d_min(R)>2 tau` | HNF `K=64,R=5,tau=1`: HNF `(10,5,6)` deficit `1` | error-robust recurrence radius |
| Compiled gate is physical authority | all four source/instrument fields | `15` incomplete WP178 authorization patterns | formal gate until fields are authorized |
| Low-energy packet explains gate tuple | hard-to-vary constructor law | `C_rect_exact` versus `C_hnf_noisy` | constructor-sensitive discriminator |
| Single noisy channel compares exact and noisy constructors | vector instrument or degradation | `I_hnf_R6_tau1` tests `C_hnf_noisy` but not `C_rect_exact` | vector-channel constructor comparison |
| Exact channel automatically covers noisy contract | authorized degradation map | exact HNF `R=6` does not test `C_hnf_noisy` without degradation | compressed single channel only with degradation |

## Disposition

The recurrence-growth branch has not produced an unconditional flavor selector.
It has produced a precise gate:

`source K + source quotient-domain law + calibrated tau + executable compiled radius + optional degradation -> constructor-discrimination selector`.

The next frontier is no longer recurrence arithmetic. It is a source
constructor law that entails the whole gate tuple before low-energy readout.

## Exact checker

- Checker: `checkers/wp184_recurrence_growth_decision_table.py`
- Result: `results/wp184_recurrence_growth_decision_table.json`

The checker verifies that each row has a unique overclaim, missing gate,
falsifier, and admissible claim, and that the branch's key governing packets
are represented.

## Calibration

- Pre excitement: `8/10`.
- Pre confidence: `10/10`.
- Pre expected information gain: `8/10`.
- Post excitement: `8/10`.
- Post confidence: `10/10`.
- Post information gain: `8/10`.
- Frozen optionality: stop extending recurrence-growth mechanics until a
  source constructor law is proposed.

## Report to `marici.Nima`

- Admitted state domain: recurrence-growth branch claims WP167-WP183.
- Faithful quotient coordinate: overclaim/missing-gate/falsifier table.
- Source-authorized probe family: conditional constructor-discrimination
  recurrence probes only.
- Contextual partition: every overclaim maps to an exact hostile class.
- Classification: branch closeout and admission table.
- Smallest exact falsifier: row-specific; see decision table.
- Remaining physical-instrument gate: derive the constructor law tying the
  gate tuple together.
