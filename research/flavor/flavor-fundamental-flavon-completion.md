# Fundamental flavon completion: WP443

## Question

What is the smallest independently typed dynamical representation that repairs WP438's residual (U(1)) while preserving a source-free global vacuum and complete stability?

## Minimal representation

One complex scalar fundamental (phi\in\mathbf 3) is sufficient. The WP438 residual generator (H=\operatorname{diag}(1,1,-2)) has no zero eigenvalue on the fundamental, so every nonzero fundamental VEV breaks that continuous stabilizer. Scalars add no chiral gauge anomaly.

The sign-even, exchange-symmetric, renormalizable completion is

\[
V_{443}=V_{437}
-\mu^2\phi^\dagger\phi
+\kappa(\phi^\dagger\phi)^2
+\tau\phi^\dagger(A^2+D^2)\phi,
\]

with \(mu^2>0\), \(kappa>0\), and \(	au>0\). The portal is the positive Gram quantity

\[
\phi^\dagger(A^2+D^2)\phi
=\lVert A\phi\rVert^2+\lVert D\phi\rVert^2.
\]

It therefore orients the fundamental into the common kernel of the embedded-Pauli adjoints without importing a flavor vector.

## Global vacuum

At the exact unit benchmark for all six coefficients,

\[
A_0=\frac{\lambda_3}{\sqrt8},\qquad
D_0=\frac{\lambda_1}{\sqrt8},\qquad
\phi_0=\frac{e_3}{\sqrt2}.
\]

The adjoint and fundamental radial sectors separately saturate their global lower bounds, while the positive portal vanishes. Hence the combined energy \(-3/8) is a global minimum.

The simultaneous gauge-tangent matrix has rank eight. The complete Hessian over 16 adjoint and six fundamental real components has spectrum

\[
0^{\times8},\qquad
(3/2)^{\times4},\qquad
4^{\times8},\qquad
10^{\times2}.
\]

Thus its kernel is exactly the fully broken gauge orbit and all 14 physical modes are strictly positive. The positive spectral gap and strict coefficient inequalities give an open stability neighborhood around the exact benchmark.

## Classification and boundary

WP443 supplies a source-free, globally minimizing, fully Higgsed dynamical flavon vacuum. It does not yet select a `physical16` point: the potential contains no quark-messenger matching that maps this vacuum to the measured Yukawa pair. It also does not select the absolute ratio (g_Ff/v); that ratio still depends on independent Lagrangian masses and couplings.

No flavor-current constraint, width, residue, detector normalization, or physical instrument is inferred from the algebraic gauge-mass rank.

## Smallest exact falsifier

At the unit benchmark, any ninth Hessian zero, any negative physical eigenvalue, or any continuous gauge kernel falsifies the completion.
