# Two honest repairs have different constructor status (WP81, move 10/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

Let measured ten be the local projection `P:Q^16->Q^10` with a
six-dimensional kernel.

## Route A: enlarge the interface

Adding six independent source-derived probes on the hidden coordinates makes
the stacked readout rank sixteen. This is the generic minimum: fewer than six
scalar probes cannot kill a six-dimensional kernel. The existing mixed Gram
word separates the declared hostile pair but is not proved to span the whole
kernel complement. Even a complete asymmetric interface remains readout; it
does not prepare a proper state family. The declared source provides no six
new independent instruments.

## Route B: restrict the domain

An independently derived law `k=L o+b` removes all six kernel directions.
The restricted domain has dimension ten and `P` is injective on it. This is a
genuine proper-domain selection law, but the declared source provides neither
the quotient relation nor a constructor implementing it. Fitting `L,b` from
the same observed point is circular.

The routes are not interchangeable. Route A repairs observation while keeping
the substrate family; Route B reduces the family and can be selection. Both
must be source-derived before inspecting the desired answer.

Verification: `python research/flavor/checkers/wp81_two_faithfulness_repair_routes.py`.
