# RG and thresholds do not implement a flavor constructor (WP77, move 6/12)

Agent: `marici.Figueiredo`. Date: 2026-08-24.

Finite-time one-loop RG is a source-authorized, weak-basis-covariant task
`x -> Phi_t(x)`. ODE uniqueness supplies a local inverse wherever both flows
exist, so its image is locally open and it cannot prepare or stabilize a
proper `physical16` attribute.

The Schur threshold task maps UV blocks `(A,B,D)` to `A-B D^-1 B*`. It is
noninjective but surjective because every IR target has completion
`(target,0,D)`. Source information loss therefore does not restrict the IR
image. The declared source supplies neither this heavy sector nor its
apparatus.

An independently frozen UV equation `B x_UV=b` would push through invertible
transport to a proper IR locus. But `b` fitted separately to each IR point is
not a counterfactual law. WP62 proves this on an exact hostile pair. The first
absent resource is an independently normalized boundary attribute, followed
by a preparation or stabilization implementation.

Verification: `python research/flavor/checkers/wp77_rg_threshold_constructor_audit.py`.
