# Oriented-adjoint-direction gate: WP1249

## Question

Can admitted normalization packets derive an oriented adjoint direction?

## DPC resolution

- **Problem:** derive an oriented adjoint direction in the residual SU(2)
  sector from a source packet; then derive ordered lines, coherent cyclic
  preparation, physical history, volume reference, and Physical16 descent.
- **Bold conjecture:** existing determinant, Wilson, history, coset,
  finite-scheme, contact, or lattice packets might select the missing
  oriented source direction.
- **Named rivals:** reciprocal determinant \(\rho\); natural
  negative-weight scalar; Wilson production kernel; history-dilation gain;
  absolute boundary coset lift; Wilson integer clock lift; finite-scheme
  ports; contact counterterm; bare integral lattice.
- **Risky consequences:** \(1/D\) has weight \(-3\) only on the cyclic
  domain and is singular on eigenlines; natural \(D\)-denominator
  candidates reduce to \(1/D\); conditional B flags and three history slots
  contribute zero production rows; integer CS lifts preserve the coset but
  change channel evaluation; Wilson phase is modulo one and the quadratic
  clock is sign blind; three finite-scheme ports are faithful but constant
  on event weights; contact shifts have zero projection to the seven-channel
  lift; a rank-one lattice leaves \(n\) and \(-n\) degenerate.
- **Strongest falsification attempt:** all admitted candidates fail to
  supply independent \(\rho\), integer lift, orientation, production
  kernel, gain \(3/2\), or Physical16 descent.
- **Exact residual:** obtain a source-authorized normalized dual cycle or
  oriented generator selecting \(n\) and \(\sigma\), then derive the
  adjoint flag, \(\rho\), kernel, gain, and descent.
- **Disposition:** accept the authority audit as a set of exact no-gos;
  select the normalized-dual-cycle/oriented-generator branch.

Checker: `research/flavor/checkers/wp1249_oriented_adjoint_direction_gate.py`

Result: `results/wp1249_oriented_adjoint_direction_gate.json`
