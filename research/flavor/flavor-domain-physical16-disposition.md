# Domain-to-physical16 disposition (WP300)

## Exact dependency closure

WP284–WP293 establish what biased domain dynamics can do. With topology,
local wall budgets, uncertainty support, and update dynamics declared, it can
conditionally select a domain branch and certify its failure probability.
Complete Boolean coincidence towers can faithfully reconstruct the declared
finite risk packet, relative to the admitted label groupoid.

None of those results supplies a flavor image for the branch.

## First missing and nonfaithful arrows

The first missing arrow is `conditional CP-domain branch -> source-derived
physical16 image`. No source action currently derives that interface.

If the interface is granted as a map to $\operatorname{sign}(J)$, the first
nonfaithful arrow is `physical16 -> sign(J)`. WP294 gives an exact hostile pair.
Adding magnitude, first-row moduli, and one lower-sector modulus progressively
shrinks the fiber but does not remove it until the cosine-sensitive
$|V_{cd}|^2$ probe is added.

WP298 then reconstructs `physical16` on its declared nondegenerate standard
chart. WP299 proves why that success is readout faithfulness, not source
selection.

## Disposition

- Domain coarsening: conditional selector inside an added domain experiment.
- Branch-to-flavor map: missing source interface.
- Signed-$J$ family: invariant partial readout with a CP-even kernel.
- WP298 family: faithful separator/readout on its chart.
- Texture atlas: presentation rigidifier evidence only.
- Overall: no source-generated proper `physical16` selector is established.

The next progressive constructor must derive an executable common-frame map
from flavor-source dynamics to a proper `physical16` image, including CP-even
response. The already faithful readout may verify that image but cannot grant
it authority.

Run `uv run --with sympy python
research/flavor/checkers/wp300_domain_physical16_disposition.py` to verify all
WP284–WP299 dependencies and regenerate the disposition.
