# The clock second moment separates occupation but does not select charge

Work package: WP625  
Owner: marici.Figueiredo

## Bounded question

Does the smallest permutation-invariant occupation probe repair WP624's
five-orbit ambiguity and independently select the target?

Freeze
\[
S=\sum_i x_i,\qquad Q=\sum_i x_i^2.
\]
No target-centered coefficient is admitted.

## Exact result

On \(S=6\), the five permutation orbits have distinct values
\[
Q\in\{8,10,12,14,20\}.
\]
Thus \((S,Q)\) is faithful on the target quotient. The hostile packets
\((-2,2,2,2,2)\) and \((1,1,1,1,2)\) have \(Q=20\) and \(Q=8\).

Positive \(Q\) uniquely minimizes the balanced orbit after imposing \(S=6\).
Without that constraint it selects the all-zero state with \(S=0\).
The pipeline is a missing charge selector, followed by the constraint
\(S=6\), followed by the occupancy rigidifier.

The second moment supplies Nima's requested occupation-sensitive operation,
but not the missing first arrow.

## Instrument and disposition

A physical realization must measure total clock power or calibrated
second-harmonic intensity together with the mean and the \(H\)-referenced
flavor record. Algebraic availability is not executable control. The flavor
coupling remains a relational stabilizer-groupoid experiment.

WP625 repairs source identification inside the target fiber and rigidifies
one occupation class conditionally. It does not select total charge six.
The next question is whether a conservation law, Gauss constraint, or anomaly
equation derives \(S=6\) independently.

All ten exact checks are generated by
research/flavor/checkers/wp625_clock_second_moment_rigidifier.py.
