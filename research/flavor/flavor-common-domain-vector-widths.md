# Common-domain vector thresholds, widths, and response rank

## Admitted conditional domain

WP476 composes the WP474-WP475 hierarchical scalar endpoint with WP448-WP449's
flavor-vector spectrum. It retains the five-TeV triplet pole as a conditional
reach benchmark and uses the corrected electroweak clock

\[
v=0.24622\ {m TeV}.
\]

The common-clock relations are

\[
f=\sqrt6\,\mu,
\qquad m_1=g_F\mu=5\ {m TeV},
\qquad m_3=\sqrt3m_1.
\]

To close WP449's messenger support condition, freeze the positive
renormalizable messenger masses as `M_U=M_D=mu`. This is a declared unit
common-clock benchmark, not a selected coefficient.

## Threshold closure

The lowest physical `SU(3)_F`-charged scalar curvature in the frozen flavor
block is `16 mu^2`, so its mass is `4 mu`. The observed Higgs and all three
radial poles are flavor neutral. At the kaon-conditioned endpoint,

\[
2M_U=2M_D\gg m_3,
\qquad 2m_{\rm charged}=8\mu\gg m_3.
\]

Messenger-pair and physical-flavon-pair decays are therefore closed. This
removes those two threshold assumptions from the quark-only WP449 width packet
on the declared benchmark. The exact leading widths remain

\[
{\Gamma_1\over m_1}={\Gamma_3\over m_3}={g_F^2\over4\pi},
\qquad {\Gamma_3\over\Gamma_1}=\sqrt3.
\]

The approximation still treats all six quarks as massless and remains a
tree-level width. It is not silently promoted to an all-orders prediction.

## Complementary source response

The low-energy current kernel is proportional to

\[
C_0={1\over f^2}={1\over6\mu^2},
\]

while the pole clock reads `m_1=g_F mu`. In logarithmic source coordinates
`(log g_F,log mu)`, their response is

\[
J_{\log}=\begin{pmatrix}1&1\\0&-2\end{pmatrix},
\qquad \det J_{\log}=-2.
\]

Thus the source-generated pole-plus-current family is exactly rank two on the
positive `(g_F,mu)` domain. Intrinsic width is not needed algebraically to
separate the two parameters. Once a pole mass is observed, the requested ratio
is the relational readout

\[
{g_Ff\over v}={\sqrt6m_1\over v}
={250000\sqrt6\over12311}.
\]

This is still not a source selection: the five-TeV pole was frozen as a reach
benchmark.

## Instrument and selector disposition

The rank-two map does not become an executable rank-two experiment on current
data. The CP-even kaon likelihood remains provisional, and WP463 proves that
the entire Run-2 pole channel contains less than one expected source event even
before acceptance. The first nonfaithful arrow is finite collider
production/counting support, not the source response.

The operation is neither a selector nor a presentation rigidifier. It adds no
reference port. Its smallest exact falsifier is an open messenger or charged
scalar pair threshold below the quintet, or a zero pole-plus-current response
determinant.

The remaining instrument gate is a controlled CP-even kaon likelihood plus an
actually populated flavor-tagged pole experiment on the same domain. The
remaining selector gate is sharper: derive the pole-to-electroweak-clock ratio
from the source action instead of freezing it.
