# Physical-instrument and open-world audit (WP133)

Owner: `marici.Figueiredo`.

## Bounded question

After WP131 derives a physical portal and conditional widths, does the
corresponding finite-uncertainty detector retain rank three, and does that
rank identify the source after admitting one hostile isospectral constructor
beyond WP129's original family?

Pre-objective process report: excitement `9/10`, confidence `9/10` that the
original rank is robust and `9/10` that open-world identification fails,
expected information gain `10/10`. The reason is that two-point spectral data
cannot generally determine nonlinear source interactions. Confounds are the
frozen rational detector model and lack of an implemented collider analysis.
These reports are non-evidential.

Frozen optionality snapshot: use WP131's two reduced widths; predeclare one
calibration interval; retain the three original constructors; add one
isospectral nonlinear constructor; test one formal four-point repair and the
decoupling limit; twelve exact checks.

## Derived conditional detector envelope

Use the WP131 reduced widths

\[
\widehat\Gamma_1=\frac1{25},\qquad
\widehat\Gamma_2=\frac1{16}
\]

and freeze the conditional bin-migration model

\[
\eta=\widehat\Gamma_1+\widehat\Gamma_2=\frac{41}{400},
\qquad \delta\eta=\frac1{100}.
\]

The declared uncertainty interval is `[37/400,9/80]`. For

\[
W_\eta=\begin{pmatrix}1-\eta&\eta&0\\
\eta&1-\eta&0\\0&0&1\end{pmatrix},
\]

the smallest determinant margin in the interval is

\[
1-2\eta_{\max}=\frac{31}{40}>0.
\]

Consequently the detector-smeared response retains exact rank three across
the entire frozen uncertainty envelope on WP129's three-member family. This
is stronger than the single-point WP130 calculation, but remains conditional
on the accessible WP131 branch and the declared calibration model.

## Hostile source beyond the chosen family

Add a fourth constructor: a two-adjoint source with the same quadratic mass
matrix, portal mixing, pole residues, and degree-eight matched flavor packet,
but a different nonlinear adjoint self-coupling. It is not a weak-basis copy;
its four-point amplitudes differ. Yet its complete two-point spectral column
is identical to WP128's.

The expanded detector response has four columns but exact rank three:

\[
\dim\ker R_{\rm two\text{-}point}^{(4)}=1.
\]

Thus a rank-three experiment that identified every member of the original
family immediately loses identification authority under a constructor-bearing
isospectral rival. This is precisely Strominger's open-world transition.

A formal four-point threshold port distinguishes the nonlinear deformation
and restores rank four on this finite class. It does not yet carry physical
instrument authority: multi-particle production, luminosity, backgrounds,
unitarity, and detector response have not been constructed. Nor may that port
be fitted after inspecting the desired rival; the complete multi-point
spectral family must be derived from the frozen portal grammar.

In the WP132-decoupled branch, the four-constructor response has rank one and
kernel dimension three. Detector refinement cannot repair thresholds that the
source places outside the admitted energy domain.

## Six-gate completion audit

1. **Gauge/Lorentz-complete portal:** achieved conditionally by WP131's
   dimension-five quark--Higgs--flavon operators.
2. **Masses, widths, couplings from the source:** couplings and reduced widths
   are derived from a frozen packet; absolute masses and scalar mixing remain
   free source inputs, so this gate is incomplete physically.
3. **Accessible thresholds:** disproved as a source consequence by WP131 and
   WP132; a continuous dilation fiber contains accessible and inaccessible
   sources with the same low flavor packet.
4. **Detector convolution and uncertainties:** constructed conditionally here;
   it is not an implemented calibrated detector likelihood.
5. **Rank with derived parameters:** rank three is robust across the declared
   uncertainty interval on the accessible three-member branch; rank one in
   the decoupled branch.
6. **Hostile sources beyond the family:** the isospectral nonlinear rival
   restores a one-dimensional kernel; a formal four-point port repairs only
   the enlarged finite class.

## Disposition

The physical-realization programme closes **negatively but decisively** for
the currently admitted source class. WP130's conditional rank-three result is
mathematically robust under the WP131 width envelope, but it cannot be granted
physical source-identification authority because:

- the source does not select an absolute accessible scale;
- no collider/detector implementation has been admitted;
- two-point threshold records are nonfaithful on an explicit isospectral UV
  enlargement.

The smallest exact accessibility falsifier is WP132's scale pair. The smallest
exact identification falsifier is the equal two-point column of WP128 and its
nonlinear isospectral rival. The remaining physical-instrument gate is an
independently scale-selected, executable multi-point quark--Higgs spectroscopy
family whose detector convolution is faithful on a predeclared open rival
class. No current flavor source supplies it.

Post-objective process report: excitement `9/10`, confidence `10/10` in the
bounded closeout, realized information gain `10/10`. Raw delta: the original
rank-three certificate survives one exact uncertainty interval; one new rival
opens a one-dimensional kernel; one formal four-point repair restores finite
rank four; decoupling opens a three-dimensional kernel; all six requested
physical-realization gates receive explicit dispositions; twelve of twelve
checks pass; no physical identification authority is admitted. These reports
are non-evidential.
