# Flavor programme index

Owner: `marici.Figueiredo`.

## Hard core

- Physical carrier: quark-Yukawa pairs modulo the full weak-basis group.
- Faithful tested coordinate: `physical16`; measured ten is only a projection.
- Sparse nine-link phases and incidence kernels are chart data unless descent is proved.
- Finite fiber does not imply singleton fiber; selector and rigidifier are independent.

## Current architecture

Carrier -> admissible flavor lenses -> source selector -> presentation
rigidifier -> physical readout.

## Live frontier

No complete physical selector is presently established. The next admissible
advance must independently derive viable UV parameters and normalization before
flavor readouts, then close the common-domain physical-instrument gates.

## Current bounded packets

- `flavor-nine-link-conventions.md`: source and equivalence conventions.
- `flavor-chart-transition-packet.md`: chart phase fails full weak-basis descent.
- `flavor-positive-gram-carrier-packet.md`: sparse carrier/incidence programme.
- `flavor-h2lr-typing-packet.md`: quotient/lens/readout typing.
- `flavor-source-selector-audit.md`: WP52 hostile selector and instrument audit.
- `flavor-rg-transport-selector-gate.md`: WP53 proves the source one-loop RG
  operation descends but is invertible transport, not selection.
- `flavor-spectral-conditional-expectation.md`: WP54 finds a canonical
  quotient-level mathematical selector whose commuting fixed locus is
  physically unauthorized and experimentally false.
- `flavor-relational-reference-port.md`: WP55 proves that a reference-assisted
  readout descends only on an enlarged relational state space over the
  reference stabilizer groupoid.
- `flavor-spontaneous-cp-selector-gate.md`: WP56 certifies the source's exact
  Z4/Z8 chart-phase algebra but records the deferred UV dynamics and failed
  full-quotient descent.
- `flavor-invariant-word-complement.md`: WP57 adds one source-derived mixed
  Gram word that exactly separates the measured-ten hostile pair without
  selecting either physical point.
- `flavor-commutator-score-selector-gate.md`: WP58 separates an instrumented
  positive invariant discriminator from the unauthorized extremization arrow.
- `flavor-selector-frontier-matrix.md`: WP59 closes the current candidate set
  under five exact gates and identifies the missing source-authorized proper
  reduction as the sharp frontier.
- `flavor-source-authority-inventory.md`: WP60 (move 1/12) freezes the declared
  source mechanisms into derived, deferred, and presentation-only classes.
- `flavor-full-rg-local-diffeomorphism.md`: WP61 (move 2/12) removes the
  leading-flow approximation and proves local nonselection for the complete
  coupled one-loop RG field.
- `flavor-uv-boundary-normalization-gate.md`: WP62 (move 3/12) proves exactly
  how an independently normalized UV equation would turn transport into a
  proper selector and why IR-fitted normalization is circular.
- `flavor-threshold-schur-selector-gate.md`: WP63 (move 4/12) proves that a
  covariant noninjective threshold reduction can remain surjective and hence
  need not select the IR quotient.
- `flavor-z4-vacuum-orbit-gate.md`: WP64 (move 5/12) proves that Z4 symmetry
  does not choose the real/imaginary vacuum orbit without an independently
  fixed potential sign and a covariant Yukawa map.
- `flavor-spurion-relation-descent.md`: WP65 (move 6/12) gives an exact
  weak-basis counterexample to the illustrative scalar Z8 component relation.
- `flavor-positive-channel-inventory.md`: WP66 (move 7/12) separates positive
  invariant readouts from proper-image mathematical channels lacking physical
  authority and ensemble survival.
- `flavor-experimental-probe-algebra.md`: WP67 (move 8/12) identifies the
  physical16-generated algebra as the maximal probe family with a declared
  experimental typing.
- `flavor-fitted-ensemble-contextual-partitions.md`: WP68 (move 9/12) gives
  exact partition counts on all 1,210 stored viable sheets and records the
  empty stationarity-selected class.
- `flavor-hostile-suite.md`: WP69 (move 10/12) applies seven hostile tests
  uniformly to every surviving route.
- `flavor-first-failure-matrix.md`: WP70 (move 11/12) records the exact first
  failed D/P/A/I/E gate for twelve candidates, including ensemble survival.
- `flavor-declared-source-selector-no-go.md`: WP71 (move 12/12) proves the
  bounded declared-source no-go and freezes the eight-field minimal extension
  contract required to reopen selector search.

## Current disposition

The twelve-move selector programme is complete. No genuine physical selector
is generated by the declared source class. The programme reopens only when a
new source package satisfies the WP71 extension contract before fitting.

## Constructor-theoretic successor programme

The successor programme asks which flavor transformations are physically
possible or impossible, with counterfactual action and repeatable resource
typing rather than numerical preference alone.

- `flavor-constructor-substrate-task-algebra.md`: WP72 (move 1/12) defines
  `physical16` as the substrate, quotient attributes, resource kinds, task
  composition, and the measured-ten hostile kernel.
- `flavor-constructor-task-classification.md`: WP73 (move 2/12) separates
  preparation, discrimination, filtering, stabilization, copying, and readout
  by their substrate, record, proper-image, and repeatability signatures.
- `flavor-counterfactual-mechanism-tasks.md`: WP74 (move 3/12) translates the
  complete WP71 mechanism closure into total or explicitly partial
  counterfactual tasks and finds no authorized proper-image constructor.
- `flavor-constructor-descent-hostile-pairs.md`: WP75 (move 4/12) orders full
  weak-basis descent before image tests and reuses the exact measured-ten
  hostile pair to separate discrimination from selection.
- `flavor-constructor-repeatability-error.md`: WP76 (move 5/12) defines reuse,
  apparatus degradation, reset/catalytic resources, and multi-use error;
  absent implementation data are untyped rather than zero error.
- `flavor-rg-threshold-constructor-audit.md`: WP77 (move 6/12) proves RG is
  authorized nonproper transport, threshold reduction is surjective, and an
  absent independently normalized boundary is required for a proper locus.
- `flavor-positive-randomized-constructor-resources.md`: WP78 (move 7/12)
  compiles exact three-branch pinching and inventories the bath, randomness,
  compiler, timing, reset, and degradation resources still absent.
- `flavor-spontaneous-cp-source-domain-restriction.md`: WP79 (move 8/12)
  rejects the chart CP rules but proves the exact independent source-domain
  pattern that would make measured ten faithful on a proper subdomain.
- `flavor-probe-reference-constructor-instruments.md`: WP80 (move 9/12)
  separates faithful quotient readout from a reference-assisted relational
  experiment; neither selects original `physical16`.
- `flavor-two-faithfulness-repair-routes.md`: WP81 (move 10/12) proves that
  six generic asymmetric scalar probes repair local readout rank without
  selection, whereas an independent six-constraint domain law can select.
- `flavor-constructor-hostile-ensemble-matrix.md`: WP82 (move 11/12) gives the
  uniform D/T/P/A/I/R/E first-failure matrix for eleven candidates and retains
  the zero-of-1,210 ensemble falsifier.
- `flavor-constructor-resource-bounded-impossibility.md`: WP83 (move 12/12)
  proves no repeatable proper-image flavor constructor exists in the declared
  resource theory and freezes the eight-resource reopening bundle.

## Constructor-theoretic disposition

The twelve-move successor programme is complete; WP72-WP83 pass their full
exact dependency audit. Declared resources generate reversible transport and
faithful records, but no independently normalized, repeatable proper-image
substrate task. Six new probes would repair generic local readout faithfulness
without selection; selection requires an independently sourced quotient law
and its physical constructor implementation.

## Post-programme sharpening

- `flavor-constructor-certificate-noninheritance.md`: WP84 proves that Boolean
  gate distance is not physical resource distance. Mutating faithful readout
  into a proper-image task changes the task identity and invalidates inherited
  authority, instrument, repeatability, and ensemble certificates.
- `flavor-pinching-coherent-task-package.md`: WP85 keeps one exact pinching
  task identity across every package field. Proper image, group-average
  normalization, and algebraic idempotence pass; physical authority,
  implementation/degradation, and ensemble survival fail.
- `flavor-parameter-versus-dynamical-substrate.md`: WP86 identifies the prior
  operational typing boundary: in the declared SM, `physical16` labels fixed
  theory parameters. A physical selector first requires a dynamical
  flavon/modulus substrate and covariant vacuum-to-`physical16` map.
- `flavor-dynamical-cp-breaking-constructor.md`: WP87 uses explicit operator
  authorization to introduce `FDM-1`, a CP-symmetric dynamical modulus with a
  task-specific annealing bath. It selects the proper CP-broken attribute
  `J != 0`, has exact convergence and degradation accounting, and its
  sign-independent prediction survives all 1,210 sheets.
- `flavor-stochastic-cp-constructor-repair.md`: WP88 detects the deterministic
  symmetry-fixed input omitted by WP87 and supersedes its totality claim with
  `FDM-2`: a uniform CP-symmetric orientation coin prepares the two-vacuum
  attribute from every input, with exact coin-bias and degradation bounds.
- `flavor-covariant-cp-vacuum-to-yukawa-map.md`: WP89 replaces the symbolic
  `J=s j(q)` declaration by an explicit quotient map. The two vacua give
  CP-conjugate Gram pairs with equal spectra, CKM moduli, and CP-even words but
  opposite nonzero Jarlskog invariants; the symmetric input has `J=0`.
- `flavor-renormalizable-fdm2-mediator-completion.md`: WP90 supplies a
  renormalizable CP-symmetric complex-singlet potential and vectorlike-quark
  mediator. Its two exact vacua generate conjugate effective Yukawas with
  commutator determinants `+/-1152 i`; the orientation coin becomes the
  symmetric quench outcome.
- `flavor-fdm2-gauge-radiative-closure.md`: WP91 verifies the mediator's gauge
  typing and vectorlike anomaly cancellation. The CP-broken selector is
  locally stable, but an allowed linear-`x` counterterm proves that the exact
  vacuum coordinates and CP magnitude are not symmetry-protected predictions.
- `flavor-fdm2-perturbative-decoupling-gate.md`: WP92 restores the threshold
  ratio `r=|<S>|/M`. The exact CP-odd determinant is `1152 i r^3`: every finite
  positive threshold selects CP breaking, while the perturbative decoupling
  limit has no uniform nonzero invariant margin.
- `flavor-fdm2-canonical-matching-correction.md`: WP93 gives an exact `2x2`
  falsifier to finite-mass Schur-complement readout: canonical light singular
  values differ until controlled decoupling.
- `flavor-fdm2-thermal-phase-gate.md`: WP94 derives the exact pitchfork at
  `tau_c=36/25` for a minimal CP-even thermal-mass ansatz, while retaining the
  source-derived thermal action and rate as open instrument gates.
- `flavor-fdm2-general-thermal-descent.md`: WP95 separates CP pairing from
  thermal selection. A source-derived sign change of transverse curvature is
  additionally necessary; the allowed `+kappa y^2` term is the smallest exact
  counterexample to selection by CP symmetry alone.
- `flavor-fdm2-mixture-versus-attribute.md`: WP96 distinguishes a branchwise
  CP-broken support attribute from the CP-invariant uniform source mixture.
  Odd means vanish; separation requires a persistent source-derived domain
  record and a relational, branch-conditioned instrument.
- `flavor-domain-moment-context-closure.md`: WP97 transfers finite contextual
  inversion to the three-route flavor packet. The first two normalized moments
  reconstruct every mixture weight exactly, repairing readout faithfulness
  without selecting or rigidifying anything.
- `flavor-domain-moment-sampling-budget.md`: WP98 adds a finite-confidence
  instrument certificate and exact rare-route obstruction. IID samples obey
  `N >= 2 eta^-2 log(4/delta)`, while no finite `N` is uniformly faithful as
  an admitted route weight tends to zero.
- `flavor-domain-detector-confusion-kernel.md`: WP99 factors the first detector
  kernel exactly. A CP-covariant three-outcome channel is faithful iff
  `(1-l-2e)(1-b-l) != 0`; its two singular surfaces erase orientation and
  broken-versus-symmetric support separately.
- `flavor-detector-modal-stability-budget.md`: WP100 diagonalizes detector
  error on the physical zero-sum plane. The exact amplification is the larger
  reciprocal orientation/support margin, so calibrated drift must stay
  strictly inside both kernel distances.
- `flavor-fdm2-end-to-end-certificate.md`: WP101 factors the complete proposed
  experiment and gives the conditional route-error bound
  `(epsilon_s+epsilon_d)/gamma+epsilon_c+epsilon_r`. The only selector arrow
  is the thermal source; all later faithful arrows are readout/context repair.
- `flavor-fdm2-higgs-portal-closure.md`: WP102 finds that WP90 omitted three
  gauge- and CP-allowed Higgs-singlet portals. In particular
  `lambda_y(Hdagger H)y^2` shifts the selector curvature and can erase the
  broken branch, so the full renormalized scalar source must be declared
  before thermal-selection authority is available.
- `flavor-fdm2-portal-copositivity.md`: WP103 derives the exact portal-quartic
  copositivity condition and exhibits a nonempty strictly stable CP-broken
  slice. Global boundedness and local CP selection are independent gates; the
  stable parameter region does not select its own coefficients.
- `flavor-fdm2-flat-ray-cubic-obstruction.md`: WP104 closes the non-strict
  boundary caveat. At a saturated copositive ray, the allowed cubic portal
  `mu_x(Hdagger H)x` has opposite signs on the two flat rays and makes one
  unbounded below unless its projection vanishes exactly.
- `flavor-fdm2-rg-stability-margin.md`: WP105 promotes strict portal stability
  to a scale-domain certificate. If `|dDelta/dlog(mu)|<=B`, then
  `Delta(mu0)>B L` preserves coercivity across log-radius `L`; pointwise
  stability or endpoint equality does not.
- `flavor-fdm2-two-margin-thermal-corridor.md`: WP106 requires simultaneous
  global-stability and CP-breaking margins along the source path. It derives
  the exact generalized commutator determinant and a conservative nonzero
  physical16 margin throughout any certified finite-threshold corridor.
- `flavor-fdm2-invariant-resolvability.md`: WP107 converts the WP106 invariant
  margin into a finite-resolution decision rule. Broken and symmetric branches
  are instrument-separable iff `C_min>2 epsilon_C`; downstream context closure
  cannot repair failure of this canonical-readout gap.
- `flavor-fdm2-ensemble-certificate-boundary.md`: WP108 verifies that the
  qualitative prediction `J!=0` survives all 1,210 stored sheets, while
  refusing to inherit WP107's witness-local determinant margin without a
  common canonical source map and normalization on every sheet.
- `flavor-jarlskog-normalized-margin.md`: WP109 supplies the exact common
  normalization `|det[Hu,Hd]|/(2|Delta_u Delta_d|)=|J|`. The observed ensemble
  minimum is now a valid normalized external target, but a source prediction
  still needs an independently derived spectral-factor bound.
- `flavor-fdm2-spectral-discriminant-bound.md`: WP110 derives the sharp
  three-level discriminant bound and converts bounded mediator-source norms
  into a normalized source lower bound for `|J|`, independent of the fitted
  ensemble minimum.
- `flavor-fdm2-benchmark-ensemble-gate.md`: WP111 evaluates the fixed WP90
  benchmark consequence on all 1,210 sheets. Its conservative algebraic
  `|J|` bound passes the ensemble, but a physical instrument still requires
  canonical matching error below half that bound.
- `flavor-fdm2-full-mediator-benchmark.md`: WP112 performs that full canonical
  benchmark test and falsifies the physical instrument: the light charged-
  current block has nonunitarity `0.9821` and the Schur quartet error is
  `0.0120`. CP covariance survives, but the unit-mass mediator is not decoupled.
- `flavor-fdm2-decoupling-phenomenology-window.md`: WP113 varies only the
  mediator mass and finds no overlap between a permissive PDG first-row
  three-sigma deficit cap and the complete-ensemble `|J|` floor. This falsifies
  the smallest fixed-coupling repair, not all vectorlike architectures.
- `flavor-fdm2-two-parameter-repair.md`: WP114 shows that scaling `a` as well
  as `M` reopens a loose phenomenological overlap. The rational witness passes
  the bounded checks, but it was found from those readouts and therefore has
  no source-selector authority.
- `flavor-fitted-uv-potential-noninheritance.md`: WP115 proves that centering a
  positive UV potential on the fitted repair is only a universal target
  encoder. Its exact minimum does not confer independent source authority or
  fix the required mass reference.
- `flavor-source-selector-final-disposition.md`: WP116 closes the bounded
  objective. The declared texture atlas is a rigidifier, the declared
  `physical16` algebra is a faithful readout, and the FDM-2 thermal arrow is a
  conditional algebraic selector whose physical realization has not survived
  the instrument and independent-source-authority gates.

## Superseded or prohibited branches

- Bare loop phase as physical invariant.
- Uniqueness inferred from measured ten.
- Texture clustering as a UV phase law.
- Preferred-example stationarity as ensemble selection.
- Reference-assisted relative phase described as an absolute phase recovery.
