# Detector-smeared threshold-faithfulness audit (WP130)

Owner: `marici.Figueiredo`.

## Bounded question and frozen grammar

Can a predeclared threshold-sensitive probe family kill WP129's
two-dimensional source kernel after widths, bin migration, detector
resolution, and decoupling are included?

Pre-objective process report: excitement `9/10`, confidence `8/10` that
resolved spectral records separate the frozen class and `3/10` that a current
physical instrument realizes them, expected information gain `9/10`.
Confounds are normalized toy residues, hypothetical mediator portals, and a
three-member rival class. These reports are non-evidential.

Frozen optionality snapshot: retain the three WP129 constructors; declare two
absorptive pole bins and one subtracted contact channel before calculating
responses; test ideal, finite-overlap, merged, and decoupled regimes; ten exact
checks. No bespoke post-response discriminator may be added.

The grammar contains either two adjoint poles, one auxiliary-adjoint pole, or
a direct contact interaction. A candidate physical realization would measure
an invariant forward-scattering spectral density of a calibrated external
current coupled to the mediator/flavon sector. Its records are integrated
absorptive weights in two fixed energy bins plus a dispersively subtracted
contact coefficient. The complete gauge- and Lorentz-invariant SM portal is
not supplied here; consequently this is a physically typed conditional
instrument, not an established one.

## Ideal and finite-width response

With rows `(low pole, high pole, contact)` and columns `(two adjoints, one
auxiliary adjoint, direct contact)`, freeze the normalized ideal response

\[
R_0=\begin{pmatrix}1&0&0\\1&2&0\\0&0&1\end{pmatrix}.
\]

It has exact rank three. Finite widths, mediator mixing, and detector bin
migration are represented by a positive response kernel fixed independently
of the rival outputs,

\[
W_\eta=\begin{pmatrix}1-\eta&\eta&0\\
\eta&1-\eta&0\\0&0&1\end{pmatrix},
\qquad \eta=\frac14.
\]

Since `det(W_eta)=1-2 eta=1/2`, the smeared response `W_eta R_0` retains rank
three. Thus finite nonzero widths do not automatically destroy identification;
what matters is whether the detector convolution remains injective on the
declared source-response span.

## Hostile resolution and decoupling limits

When the two pole bins are fully merged, the detector applies

\[
W_{\rm merge}=\begin{pmatrix}1&1&0\\1&1&0\\0&0&1\end{pmatrix}.
\]

The two-adjoint and single-adjoint columns then become exactly equal. The rank
drops to two and a one-dimensional kernel returns. The direct contact remains
separated only because the subtracted contact channel survives.

Below every mediator threshold, all three constructors were matched to the
same low-energy contact record. The response rank is then one and the full
WP129 two-dimensional kernel returns. This is decoupling, not experimental
failure: inaccessible heavy constructors are supposed to become equivalent
under the admitted low-energy experiment.

Mixing also does not by itself imply blindness. An invertible source-derived
mixing/convolution kernel preserves rank. Exact degeneracy plus insufficient
resolution is the hostile case because the measurement kernel becomes
singular on the rival difference direction.

## Groupoid and instrument typing

The threshold apparatus is a new relational experiment. Its calibrated
energy bins, external current, subtraction prescription, luminosity, and
resolution kernel reduce the physical groupoid to their stabilizer. It does
not recover an absolute label hidden in the original low-energy flavor
experiment.

Identification authority requires all of the following:

- a gauge- and Lorentz-complete coupling from physical SM external states to
  the mediator/flavon sector;
- reachable center-of-mass energy and nonzero production residues;
- widths and mediator mixing derived from the same frozen action;
- a calibrated detector response whose restriction to the rival span is
  injective;
- stability under admitted nuisance parameters and RG/threshold scheme
  changes.

The current exact matrix establishes only the resolution condition. Algebraic
span is not executable control, and normalized pole coordinates are not an
instrument.

## Disposition

- admitted domain: the three WP129 constructors with frozen ideal spectral
  packets and a predeclared detector kernel;
- faithful quotient coordinate: detector records modulo calibrated apparatus
  stabilizers, not measured-ten flavor coordinates;
- contextual partition at `eta=1/4`: three singleton classes;
- contextual partition after full pole merging: `{two adjoints, one
  auxiliary}` plus `{contact}`;
- decoupled partition: one three-member class;
- selector: unchanged from WP128;
- source identification: conditional on resolved threshold instrumentation;
- smallest exact falsifier: singular detector convolution making the first
  two response columns equal;
- remaining gate: no physical SM portal, accessible scale, derived widths, or
  calibrated detector has been established.

Post-objective process report: excitement `9/10`, confidence `10/10` in the
resolution-relative rank theorem and `2/10` in present instrument
availability, realized information gain `10/10`. Raw delta: the ideal and
finite-overlap kernels vanish; full merging opens a one-dimensional kernel;
decoupling restores the two-dimensional kernel; one changed relational
groupoid is stated; ten of ten checks pass; zero executable instruments are
added. These reports are non-evidential.
