# Hierarchical nonquark threshold closure

## Constructive question

Does the hierarchical vector witness admit a common-source completion closing
every declared nonquark two-body pair channel while preserving its poles and
residues?

Yes. WP527 constructs such a witness exactly. It still withholds total-width
authority because WP516 classified vector vertices with a floating cutoff
rather than exact zero tests.

## Exact vector bound

At the WP516 parameters, Sturm counting gives three positive roots in
\((0,25)\) for each WP508 cubic. The five quintet poles have mass squared six.
Hence every vector pole obeys

\[
M_V^2<25.
\]

Squared masses here are measured in GeV squared. Every daughter in a
nonquark pair is therefore safely closed if its mass squared exceeds \(25/4\).

## Common-source closure witness

Retain the WP516 VEVs and gauge couplings, set the common clock to one GeV,
and freeze:

- \(z_A=z_B=3\);
- all nine WP506 entrance-potential coefficients to five;
- four radial quartics to 100;
- the singlet lift to \(10^9\);
- the admitted unit adjoint and connector shape coefficients.

The exact lower bounds include

\[
m_E^2=\frac{1210320}{160001}>\frac{25}{4},
\]

\[
m_A^2=16,\qquad m_C^2=1024,\qquad m_M^2=9.
\]

The electroweak minimum is 15129 GeV squared. The shifted radial Hessian has
five positive leading principal minors. The entrance Hessian retains exactly
six gauge zeros, and all eighteen physical modes exceed the pair threshold.
Thus every nonquark pair channel in the declared tree census is closed.

This repairs WP526's messenger ambiguity on one source witness. It does not
dynamically predict the chosen coefficients.

## Remaining exactness defect

WP516 retained an open mass-basis channel only when

\[
|G_{ijk}|>10^{-7}.
\]

That is a numerical resolution convention, not a proof that every omitted
above-threshold vertex vanishes. WP517 assigned widths only to the retained
list, and WP518 correctly calls the sums lower bounds.

The first remaining nonfaithful arrow is therefore the projection from the
exact Yang--Mills tensor to the cutoff channel list. Total-width authority
requires exact algebraic pole projectors, or certified isolating intervals
that decide every threshold and every zero or nonzero vertex.

## Disposition

- Domain: one frozen common-source completion of the WP516 witness.
- Selected coordinate: conditional relational source point, not a numerical
  prediction from coefficient dynamics.
- Residues: WP509/WP520 algebra preserved.
- Widths: nonquark pairs closed; vector sums remain lower bounds.
- Classification: threshold rigidifier plus negative total-width gate.
- Smallest falsifier: one omitted above-threshold coupling is nonzero.
- Remaining gate: enumerate every vector channel exactly, sum the complete
  tree self-energy, and transport it through WP525.

WP527 narrows the width problem to one finite algebraic enumeration.
