# Three colours retain the marked-pair orbit

Refine each occurrence axis into singleton-complement (`S`), uppercase same-pair
(`P`), and lowercase marked-pair (`L`) colours. The nine-axis relabelling is
bijective and preserves all 512 masks. Forgetting colour has kernel rank six.

The shadow `(0,-1,1)` has nine minimal coloured lifts and three minimum cyclic
splittings: `all-S`, `all-P`, and `all-L`. Literal boundary support isolates the
`all-L` candidate on its two nonzero components because those terms use
`q_g23,q_g31`.

This remains conditional. The literal signs are `(-1,-1)` while the oriented
shadow signs are `(-1,+1)`, no boundary-to-occurrence chain map supplies the
orientation change, and cyclic closure additionally requires `q_g12`, absent
from the five-mark word.
