# No weighted map sends the norm correction to Xi_log

Conjecture: an existing chain-level specialization sends the norm correction to
twice the `Xi_log` generator.

The strongest interface artifacts provide weighted boundary initial forms,
regularity, a first correction, and a separate Gysin blowup algorithm. They do
not provide a chain map into the Gysin coefficient block, a relative
homology–de Rham pairing, `Xi_log` label identification, comparison to the
blowup face, or projection to `(Xi_log,-sigma123)`. No field accepts the norm
correction as input.

The conjecture is falsified under the active source envelope. The norm-doubling
and principal `2Z` obstructions remain parallel rather than identified.

The next test compares the norm residual's pole divisor with the weighted Gysin
singular support. Support incompatibility would obstruct even a future
specialization map.
