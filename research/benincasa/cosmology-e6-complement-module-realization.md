# The complement is primitive on the established A2 grade

Correction: the recorded common weight `-2` is the matching source/target
homogeneity grade. It is not scalar multiplication of the morphism by `-2`.
The established A2 realization matrix is the identity, and its cyclic transport
matrix is exactly the required complement permutation `C`.

The three source A2 roots are primitive, and `C` permutes them integrally.
Therefore the opposite-pair complement is already realized primitively on the
A2 occurrence-difference grade; no division by two is required there.

This does not produce the full lift. The literal complex lacks the complete
`q_g12` edge object, and the A2 associated grade does not determine the
off-diagonal shear, full rank-twelve connection, or connector naturality
components.
