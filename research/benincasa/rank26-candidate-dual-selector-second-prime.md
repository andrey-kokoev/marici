# The seven generic-twist candidates are dual-annihilated over the second prime

An independent paired module/provenance reduction over `F_32009` verifies
`t*g=0` for all seven materialized degree-eight generators. Every annihilation
witness replays exactly against the 19,560 source-labelled dual rows.

The witness source-row counts are

`215, 235, 188, 182, 517, 540, 598`.

They agree with the `F_32003` receipt. This establishes two-prime finite-field
authority for the dual length-one classification. It does not construct the
missing five-mark to principal-three-wall comparison, an intrinsic p-normal
class, a tau map, or a physical period.

The reusable Aspect reducer currently fixes its arithmetic modulus internally
to 32003. Therefore a caller's `--prime 32009` setting does not parameterize
that reducer. This checker uses an independently parameterized reducer; the
Aspect-owned implementation requires repair before its second-prime mode is
itself authoritative.
