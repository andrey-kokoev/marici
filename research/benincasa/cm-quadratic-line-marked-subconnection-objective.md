# CM quadratic line marked-subconnection objective

## Objective

Determine first whether Entry 2563's conditional \(P_3\)-normal projective
line \(L\) is reached by any source-derived lift of the scalar total-energy
conormal. A marked-relative subconnection test is permitted only after that
typing gate passes.

For a local generator \(v\) of \(L\), compute the invariant obstruction

\[
\Theta_L=[\nabla v]\in(E/L)\otimes\Omega^1.
\]

The calculation must not choose a conormal splitting. If a source-derived
lift exists, the later transport calculation must use the complete
source-normalized marked-relative connection. The uncertified modular
characteristic-zero candidate is not authority.

## Predeclared outcomes

1. (Theta_L=0): the line descends as a projective subconnection;
2. (Theta_L\ne0): report the smallest labelled off-line component;
3. only a quotient/derived line is horizontal: construct its typed cone;
4. the exact connection data needed for (Theta_L) are absent: record the
   comparison as currently untyped.

## Acceptance contract

- preserve the marked basis
  ((\Omega_{111},\Omega_{101},\Omega_{110},e_1,\ldots,e_9));
- derive every connection coordinate used from the common four-stratum
  reduction;
- quotient only by source-declared triangular gauge;
- verify invariance under (v\mapsto fv);
- test `A`, `B`, `HOMA`, and `SOFT1` separately and use two primes for
  discovery checks;
- require characteristic-zero source identities before a theorem claim;
- keep divisor reconstruction and physical activation blocked.

## Pre-objective optionality snapshot

- live outcomes: 4;
- certified CM input: one projective quadratic line;
- certified marked input: rank-117 reduction with seven fixed master
  coordinates;
- uncertified input: full characteristic-zero extension block;
- decisive missing datum: enough exact connection coordinates to evaluate
  ((E/L)\nabla v).

## Pre-activation observation

- excitement: 9/10;
- confidence that the line is preserved: 5/10;
- expected information gain: 10/10;
- primary confound: the CM line and the marked quotient may not share a
  canonical affine frame;
- epistemic status: process observation, not evidence.

## Result

Outcome 4 occurs before the connection calculation. The tested deformation
\(P_3\mapsto P_3+\tau\) is a chosen labelled conormal lift. The frozen source
does not identify it with the scalar total-energy conormal. On the physical
homogeneous locus, simultaneous source variation \(P_i=X_i\) keeps
\(\nu_i=P_i^2-X_i^2\) identically zero; obtaining a nonzero quadratic normal
class requires additional relative second-jet data.

Consequently \(\Theta_L\) is not yet a typed total-energy obstruction. The
marked-relative connection—exact or modular—must not be applied to manufacture
that missing source map.

The conditional \(P_3\)-normal result remains exact, including the physical
\(K^{-1/2}\) coefficient normalization.

## Post-objective optionality snapshot

- surviving outcomes: 1, namely the comparison is currently untyped;
- certified conditional input: one projective \(P_3\)-normal line;
- certified scalar-to-labelled conormal maps: 0;
- marked subconnection computations authorized: 0;
- next admissible input: an independently derived second-jet or conormal
  comparison from the frozen source.

## Post-activation observation

- excitement: 8/10;
- confidence in the narrow closure: 10/10;
- realized information gain: 10/10;
- interpretation: the apparent computational blockage was actually a missing
  typed map;
- epistemic status: process observation, not theorem evidence.
