# Existing-map inventory at the kinematic coordinate origins

## Question

Entry 2583 leaves only

\[
V(P_1,P_2,P_3)
\cup
\bigcup_iV(X_i,P_i)
\]

as kinematic rank-drop loci. Does an already constructed support map carry a
scalar total-energy class from one of these loci into the cyclic quadratic
line of Entry 2573?

## Type audit

No existing route passes all gates.

1. The finite-sextic coordinate-boundary Gysin maps live on fiber boundaries
   \(a=0\) and \(b=0\). They are not maps on the kinematic origins
   \((X_i,P_i)=(0,0)\).
2. The homogeneous site-soft Cut--nearby maps land in the conductor
   occurrence lattice. They do not target the labelled module \(N_2\).
3. Generic five-pole localization commutes with homogeneous specialization
   and preserves each \(N_2\) label, but supplies no scalar source map into
   those labels.
4. The contact-product construction supplies a rank-one mixed line at
   \(\nu_i=\nu_j=0\), but it has character \((+1,+1)\), whereas the lower
   Kummer line has character \((-1,-1)\). The source residue and all audited
   deeper soft/triangle normalizations supply no missing half-Kummer twist.
5. Jacobian rank drop itself is not a morphism.

## Narrow result

\[
\boxed{
\text{No already-constructed support map types the missing scalar-to-}N_2
\text{ comparison.}
}
\]

This closes the existing-map inventory only. It does not exclude a newly
derived coefficient Hessian/connection map or a physical relative-cycle
map. Such a map must be constructed from its source before its image or
normalization is tested.

## Frontier

Return to the frozen generic five-pole coefficient object. Test whether its
derived Gauss--Manin base-change cone contains a scalar conormal source whose
second-normal symbol lands on the cyclic line. If the source does not define
that cone or scalar port, record the comparison as untyped rather than using
the deeper rank-drop census to manufacture it.
