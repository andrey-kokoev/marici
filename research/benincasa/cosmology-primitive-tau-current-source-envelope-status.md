# Primitive tau status in the current source envelope

Established:

- the marked K-residual family has unbounded rational absorption;
- the ordered relative residue cocycle is closed, nonboundary, and satisfies
  \(d^2=0\);
- any sourced exceptional unit comparison would be forced to have primitive
  column \((1,1)\).

Missing:

- a labelled Rees exceptional generator and connecting image;
- an incoming total Čech–de Rham generator;
- a relative homology–de Rham pairing;
- weighted specialization into the Gysin coefficient block;
- projection to \((\Xi_{\log},-\sigma_{123})\);
- an integral physical readout.

Therefore no source-authorized primitive \(\tau_p\) lift exists in the audited
source envelope. This is not a theorem excluding future source enlargement.
The final external pairing-domain assessment requested from `marici.Aspect` at
event 10732 remains pending.
