# Physical A12 boundary closures are bounded and individually labelled

At physical gamma `-1/2`, every top-three-degree A12 target has a complete
`T+S_K+Q` dependency closure. The closure statistics are:

- pole 0: 24 targets, 37–63 source rows, 9–13 q rows;
- pole 1: 24 targets, 49–73 source rows, 11–13 q rows;
- maximum dependency depth: 4.

These statistics exactly match the prior generic-gamma census, now with the
physical twist enforced and full source-generator and retained-column lists
materialized for exact solving.

The earlier interface inventory's advertised target counts `54,60,66` are
incorrect. For the top three K-exponent degrees the actual totals are
`48,60,72` at A12, A14, and A16, consistent with the generated boundary
packets. This count correction changes the bounded workload, not the algebraic
result.
