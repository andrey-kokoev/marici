# The degree-ten image is three descended directions plus a four-dimensional complement

Replayable degree-ten physical half-twist provenance was materialized over
`F_32003` and `F_32009`. The seven labelled pivots and source support counts
agree across primes.

Embedding the degree-eight image into the degree-ten special quotient retains
only degree-eight candidates `5,6,7`, giving rank three. Greedy extension by
the degree-ten provenance basis selects local candidates `0,1,2,4` as a
four-dimensional complement. Together they recover the complete rank-seven
degree-ten image over both primes.

The selected complement labels include new monomials `(1,9)` and `(0,10)` and
higher-K-pole directions. This is a finite envelope transition, not a semantic
identification of candidate ordinals: every index is bound to its source and
column digests and to the declared greedy order.

The transition constructs no intrinsic p-normal class, tau map, or physical
period; the full image remains p-tangent.
