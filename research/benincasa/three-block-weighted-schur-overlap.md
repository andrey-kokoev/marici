# Three-block weighted Schur complements glue on the generic exceptional chart

Take

\[
\Sigma(t)=
\begin{pmatrix}
a&tx&ty\\
tx&t^2b&t^2z\\
ty&t^2z&t^2d
\end{pmatrix}.
\]

On the chart (bd-z^2\ne0), joint conditioning on the last two variables is

\[
a-rac{dx^2-2zxy+by^2}{bd-z^2}.
\]

Conditioning first on the (b)-block and then on the residual (d)-block, or
in the reverse order, gives the same rational function. Thus the weighted
covariance coordinates of Entry 1670 glue without a secondary overlap class on
the generic exceptional chart.

The exact checker verifies 93,638 positive covariance families across 854
invertible exceptional blocks and 187,276 sequential-order comparisons.

The only remaining locus is the pre-existing exceptional determinant

\[
bd-z^2=0.
\]

