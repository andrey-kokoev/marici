#!/usr/bin/env python3
"""DPC completion audit for the formerly partial overlap cocycle."""
import json,math
from pathlib import Path
P=Path(__file__).resolve();ROOT=P.parents[3];R=ROOT/'research'/'benincasa'/'results'
p=json.loads((R/'cosmology_horn_full_overlap_cocycle.json').read_text());c=json.loads((R/'cosmology_horn_missing_cyclic_transitions.json').read_text());assert not p['full_three_chart_cocycle_materialized'];assert len(c['transitions'])==3;assert math.prod(x['determinant'] for x in c['transitions'])==1;assert c['triple_K2_correction_sum']==[0,0] and c['integral_K2_cocycle_trivial'];out={'schema':'marici.benincasa.cosmology-full-overlap-completion-dpc.v1','problem':'Does the partial G12-G31 two-torsion correction prevent completion of the full overlap cocycle?','bold_conjecture':'At least one missing cyclic transition fails to exist integrally or leaves a nonzero triple-overlap correction.','named_rivals':['both cyclic transitions exist and telescope','orientation signs fail to close','a residual two-torsion K2 class survives'],'risky_consequences':['fewer than three projective transitions materialize','the determinant product differs from one','the correction-vector sum is nonzero'],'strongest_falsification_attempt':{'transition_count':len(c['transitions']),'determinant_product':math.prod(x['determinant'] for x in c['transitions']),'K2_correction_sum':c['triple_K2_correction_sum']},'disposition':{'status':'falsified','surviving_scope':'the full universal-horn three-chart cocycle is materialized and strictly integral','limitation':c['rank26_relation_transport_scope']},'passed':True};(R/'cosmology_full_overlap_completion_dpc.json').write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2))
