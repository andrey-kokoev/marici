#!/usr/bin/env python3
"""DPC audit of the cyclic transition composition."""
import json
from pathlib import Path
P=Path(__file__).resolve();ROOT=P.parents[3];R=ROOT/'research'/'benincasa'/'results';c=json.loads((R/'cosmology_horn_missing_cyclic_transitions.json').read_text())
def mul(a,b): return [[sum(a[i][k]*b[k][j] for k in range(2)) for j in range(2)] for i in range(2)]
M=[x['log_matrix'] for x in c['transitions']];product=mul(M[2],mul(M[1],M[0]));assert product==[[1,0],[0,1]];assert c['triple_K2_correction_sum']==[0,0];out={'schema':'marici.benincasa.cosmology-cyclic-transition-dpc.v1','problem':'Can the two newly materialized chart maps close pairwise yet fail cyclic coherence?','bold_conjecture':'The ordered product of the three logarithmic transition matrices is nonidentity or carries a residual K2 correction.','named_rivals':['matrix composition is identity and unit corrections telescope','determinants multiply correctly but matrices retain monodromy','matrices close but K2 units do not'],'risky_consequences':['the ordered matrix product differs from identity','the correction-vector sum is nonzero'],'strongest_falsification_attempt':{'ordered_matrices':M,'ordered_product':product,'K2_correction_sum':c['triple_K2_correction_sum']},'disposition':{'status':'falsified','surviving_scope':'cyclic coherence holds simultaneously at logarithmic matrix and integral K2 levels for the projective horn cover','limitation':c['rank26_relation_transport_scope']},'passed':True};(R/'cosmology_cyclic_transition_dpc.json').write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2))
