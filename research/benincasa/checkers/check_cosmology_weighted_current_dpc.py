#!/usr/bin/env python3
"""DPC underdetermination audit for extracting a chain complex from scalar data."""
import json
from pathlib import Path
P=Path(__file__).resolve();ROOT=P.parents[3];R=ROOT/'research'/'benincasa'/'results';x=json.loads((R/'cosmology_weighted_current_chain_complex_extraction.json').read_text());req=x['required_chain_data'];assert x['available_source_data']['scalar_initial_forms'];assert not any(req.values());assert not x['domain_chain_complex_materialized'];# Two inequivalent one-step complexes satisfy all available scalar statements because no incidence constraint is given.
models=[{'C1_rank':1,'C0_rank':1,'boundary':[[0]],'H1_rank':1},{'C1_rank':1,'C0_rank':1,'boundary':[[2]],'H1_rank':0}]
assert models[0]['H1_rank']!=models[1]['H1_rank'];out={'schema':'marici.benincasa.cosmology-weighted-current-dpc.v1','problem':'Do weighted scalar initial forms determine the relative-current chain complex?','bold_conjecture':'The weighted chart, normal order, and first scalar correction uniquely determine labelled current generators and their boundary matrix.','named_rivals':['scalar data carry no incidence information','different chain complexes share the same scalar initial forms','geometric horn data provide a different complex'],'risky_consequences':['all chain-complex realizations compatible with the scalar data must have the same boundary and homology','a specialization matrix must be derivable'],'strongest_falsification_attempt':{'compatible_models':models,'same_available_scalar_constraints':True,'different_H1_ranks':[1,0],'specialization_map_materialized':req['specialization_map']},'disposition':{'status':'falsified','surviving_scope':'the weighted scalar chart and corrections only','missing_object':x['first_missing_typed_object'],'reopening_test':x['reopening_test']},'passed':True};(R/'cosmology_weighted_current_dpc.json').write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2))
