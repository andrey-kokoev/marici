#!/usr/bin/env python3
"""DPC audit of canonical base-point selectors in the rank-26 torsor."""
import json
from pathlib import Path
P=Path(__file__).resolve();ROOT=P.parents[3];R=ROOT/'research'/'benincasa'/'results';x=json.loads((R/'cosmology_rank26_selector_source_search.json').read_text());p=json.loads((R/'cosmology_rank26_pairing_dpc.json').read_text());assert len(x['candidates'])==4 and all(not c['selector'] for c in x['candidates']);assert not x['kernel_sensitive_selector_found'];assert p['disposition']['status']=='falsified';out={'schema':'marici.benincasa.cosmology-rank26-selector-dpc.v1','problem':'Can an invariant condition select a canonical base point in the contraction fiber C_t?','bold_conjecture':'Symmetry invariance, evaluation normalization, dual annihilation, or pairing orthogonality uniquely selects a rank-26 contraction.','named_rivals':['kernel translations preserve every available normalization','dual annihilation is constant on all candidates','a sourced pairing breaks the shear symmetry'],'risky_consequences':['at least one audited condition must have a unique fixed point','a pairing-selected point must remain omega-compatible with the horn'],'strongest_falsification_attempt':{'candidate_results':{c['name']:c['result'] for c in x['candidates']},'selector_count':sum(bool(c['selector']) for c in x['candidates']),'omega_pairing_comparison':'independently falsified because rank26 has omega zero and Gamma has omega one'},'disposition':{'status':'falsified','surviving_scope':'C_t remains a K=ker(E) torsor without canonical rank-26 base point','qualification':'geometric Gamma sources the external rank-27 quotient section, not a base point of this rank-26 contraction torsor'},'passed':True};(R/'cosmology_rank26_selector_dpc.json').write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2))
