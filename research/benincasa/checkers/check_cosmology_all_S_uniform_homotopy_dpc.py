#!/usr/bin/env python3
"""DPC audit of the degree-uniform tangent homotopy."""
import json
from pathlib import Path
P=Path(__file__).resolve();ROOT=P.parents[3];R=ROOT/'research'/'benincasa'/'results'
u=json.loads((R/'cosmology_all_S_uniform_tangent_homotopy.json').read_text());q=json.loads((R/'cosmology_all_S_tangent_killing_exact_lift.json').read_text());assert q['exact_reconstruction_verified'] and q['field']=='Q';assert u['source_rows']==q['nonzero_exact_sources']==230;assert all(x['all_source_rows_included'] and x['exact_embedded_reconstruction'] and x['target_index']==0 for x in u['tested_embeddings']);assert u['degree_uniform_presentation_theorem'];out={'schema':'marici.benincasa.cosmology-all-S-uniform-homotopy-dpc.v1','problem':'Does adding higher ambient generators invalidate the degree-6 tangent homotopy?','bold_conjecture':'At some A>6, ambient enlargement destroys the degree-6 exact reconstruction even though its labels remain present.','named_rivals':['old coordinate block embeds unchanged','new relations alter old coefficients','target column index drifts'],'risky_consequences':['some source row label disappears','the target ceases to occupy the preserved index','replaying the same rational coefficients gives nonzero residual'],'strongest_falsification_attempt':{'test':'embed the labelled degree-6 source block and replay its exact rational coefficients at A=8,10,12, then prove exponent-range inclusion for arbitrary A>=6','tested_residuals':[0,0,0],'symbolic_residual':0},'disposition':{'status':'falsified','surviving_scope':'the 230-row rational homotopy is natural under the declared ambient inclusions for every A>=6','not_asserted':'physical readout or canonical colimit comparison'},'passed':True};(R/'cosmology_all_S_uniform_homotopy_dpc.json').write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2))
