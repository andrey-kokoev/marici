# The exceptional difference has a replayable depth-three connection-word identity

The finite physical connection orbit was rerun with paired provenance over
`F_32003`, `F_32009`, and `F_32027`. In each field the exceptional difference
is an exact combination of twelve connection words in the sourced sum, with
maximum word depth three. The same word support occurs at all three primes and
every identity replays to zero.

The modular coefficients differ by prime and three-prime rational
reconstruction does not produce a complete bounded rational identity. The
packet therefore certifies finite-field orbit membership, not an integral or
characteristic-zero source map. It also leaves the principal coefficient
lattice unchanged: the sum contributes `-2` and the difference contributes
zero.

No unit principal selector, tau map, or physical period is constructed.
