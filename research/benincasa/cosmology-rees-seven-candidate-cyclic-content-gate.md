# The seven dual candidates have no classifiable cyclic content

Both primes return seven candidate ordinals with identical provenance-support
counts: `215,235,188,182,517,540,598`. Each candidate is dual-annihilated with
zero replay residual.

The records contain no source-id coefficients, labelled generator coordinates,
cyclic permutation images, normal-image coordinates, or p-tangent coordinates.
Equal support cardinalities and deterministic ordinals do not define a cyclic
action or semantic cross-prime identity.

Therefore the seven candidates cannot be partitioned into source-labelled
cyclic orbits, and they place no constraint on the symmetry type of the missing
eighth factor. Such a classification requires the contracted production
output.
