# Boundary representatives are compatible modulo the base module

The boundary correction-cell reductions were independently replayed at prime
32003 for both adjacent ambient inclusions.

- degree 12 to 14: all 48 translated differences are exact modulo `T + S_K`;
- degree 14 to 16: all 60 translated differences are exact modulo `T + S_K`;
- raw difference supports range from 30 to 45 columns in both transitions.

Thus literal representative transport fails, but the representatives define
compatible quotient classes at the two tested transitions. The reducer does
not retain base-module coefficients, so it supplies existence rather than an
explicit correction cell. The result remains single-prime, finite, and
pivot-dependent; it is not yet a source-natural boundary operator.
