# The antisymmetric connection word has no typed ordered Čech boundary

The provenance-preserved word is the difference of the `g31` and `g23`
source cells. It has principal coefficient zero and exact two-prime orbit
replay.

Its occurrence-label shadow is \((0,-1,1)\), whose double is the required
correction \((0,-2,2)\). This shadow is not an ordered Čech boundary: the word
lives in the q-cell/master-row module, and the existing occurrence projector
does not map that module to the ordered deletion complex. The matching labels
do not supply the missing variance or chain-map law.

Therefore the boundary calculation is blocked at its domain map, not
numerically falsified. The required constructor is a source-derived map from
the q-cell occurrence module to the ordered pair-face Čech chain module,
compatible with both the connection action and deletion variance.
