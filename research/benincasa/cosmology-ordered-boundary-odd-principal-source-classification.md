# Ordered boundary of an odd-principal incoming source cell

The known ordered Čech obstruction is \((0,2,-2)\). Literal cancellation
therefore uniquely requires

\[
(0,-2,2).
\]

This correction is closed under the downstream augmentation, has support two,
\(\ell^1\)-norm four, and content gcd two. It is not the boundary vector of one
ordinary ordered simplex with unit incidence coefficients.

A minimal source enlargement must consequently combine an odd
principal/logarithmic leg with even exceptional-face legs of opposite ordered
sign. The latter require either two coherently oriented incidence occurrences
on each supported face or a degree-two attaching coefficient. Doubling the
entire source generator is excluded because it also doubles the principal leg
and restores even parity.

The statement classifies literal minimal cancellation. Additional closed
boundary cycles require independently sourced kernel classes and are not
inserted by the classification.
