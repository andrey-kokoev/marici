# Connected-route coherent-control apparatus

The source row is read directly from Benincasa's exact lift-independence packet. Route A uses its balanced integer lift. Route B uses its shifted integer lift. Each route:

1. embeds the integer row in the real characteristic-zero carrier;
2. normalizes it to a unit analyzer row;
3. constructs a Householder unitary sending the first optical mode to that analyzer;
4. exposes the compiler matrix, source provenance, normalization, environment dimension, and reset label.

The coherent-control interferometer prepares a control path in equal superposition, applies route A on one arm and route B on the other, and reads control X and Y. For input x, the ideal complex fringe is the inner product of U_A x and U_B x. Bypass transmission and norm closure are recorded separately.

The acquisition packet contains the first source mode, the exact hostile witness, and source-support basis probes. It records ideal deterministic means, not fabricated detector counts. Its status is synthetic_reference. A physical packet must replace ideal means with raw trial counts while retaining route, compiler, phase, bypass, environment, and reset fields.

