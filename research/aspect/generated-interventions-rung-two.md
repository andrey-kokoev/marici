# Generated interventions promoted into rung two

The falsifier compiler exposed eight directions invisible to the original
tester. Each synthesized dual row is now an executable rung-two gate:

1. a coherence-amplitude cross sweep detects phase curvature hidden at one amplitude;
2. a three-back Hankel probe detects memory beyond the old two-state projection;
3. a context-by-order factorial crossing detects a pure interaction with null marginals;
4. a period-31 spectrum detects a mode orthogonal to periods 2, 4, 8, 16, and 32;
5. spanning flux with a dual gain detects saturation hidden in the low-flux range;
6. sealed controller transcript replay exposes adaptive predecessor side channels;
7. environment-port coherent tomography exposes which-path information absent from populations;
8. a joint signed residual vector exposes multifault cancellation in a scalar verdict.

For every gate the checker constructs an exact hostile that passes the old
projection and is separated by the new measurement. These are calibration and
process gates, so the frozen 96 primary science proportions do not change.

This closes the eight-dimensional prototype kernel as executable decision
logic. It does not certify hardware: calibrated raw records from the apparatus
must instantiate every record and gate.
