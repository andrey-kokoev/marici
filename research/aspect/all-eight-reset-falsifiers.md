# Eight high-entropy reset falsifiers

The full reset architecture now distinguishes memory erasure from destruction of the scientific carrier. All eight gates are conjunctive.

| Index | Falsifier | Exact hostile | Required repair |
|---:|---|---|---|
| 1 | Blinding reset | predecessor spread zero with target-transfer rank one | full-rank target reconstruction with frozen condition bound |
| 2 | Coherence preservation | count outputs unchanged while process rank falls from four to two and `000–111` coherence vanishes | informationally complete coherent-process qualification |
| 3 | Reset insertion | associator stays zero while the direct ternary curve flips sign | matched direct curve with and without reset |
| 4 | Higher history | one-step marginals equal while controlled-history Hankel rank is three | retain two-back histories and bound predictive rank |
| 5 | Order reversal | equal operation multisets give outputs `(2,1)` and `(1,1)` | cyclic, reversed, and independent order-family agreement |
| 6 | Reset depth | a second decay mode violates the `16/25` recurrence | simultaneous depth `0…10` law test |
| 7 | Cross-context transport | one of sixteen route-coherence-detector contexts leaves residual `1/5` | one reset identity transported through every context |
| 8 | Schedule synchrony | periods `2,4,8,16,32` create cell spread one despite marginal balance | permutation-family phase balance plus residual spectral tests |

The master contract is `contracts/reset-full-falsifier-suite.v1.json`. The combined checker executes all eight witnesses afresh, validates their decisive outputs, and verifies that the corresponding gates occur in the three-wing preregistration.

This closes the requested exact falsifier architecture. It does not claim that a physical reset has passed: no apparatus record was supplied. A realized reset must pass the raw acquisition contract and all eight process-level gates before the associator experiment is admissible.
