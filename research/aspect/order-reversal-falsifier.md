# Balanced operation multisets do not imply order-independent implementation

Two apparatus updates can have identical counts and noncommuting physical action. The hostile uses the exact shear maps

\[
A=\begin{pmatrix}1&1\\0&1\end{pmatrix},\qquad
B=\begin{pmatrix}1&0\\1&1\end{pmatrix}.
\]

Their forward and reversed products differ. Acting on the same state gives outputs `(2,1)` and `(1,1)`. Every scalar treatment count is identical; only order distinguishes the carriers.

The acquisition contract must compare matched cyclic, reversed, and independently permuted rotation superblocks. Target, reset-monitor, and associator laws must be invariant under order family. Commutativity of label counts does not authorize commutativity of optical implementation.

Executable witness: `checkers/check_order_reversal_falsifier.py`.
