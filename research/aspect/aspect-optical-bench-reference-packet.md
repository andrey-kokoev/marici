# Aspect optical bench reference packet

The executable reference packet drives all ten wishlist rungs from one contract.

- Metric cell: tetrahedral diagonal fidelities 1 and off-diagonal fidelities 1/3.
- Orientation cell: mirror pair has identical pairwise record and opposite cyclic Y.
- Bell cell: four correlators are computed from the standard singlet analyzer angles; no-click is an explicit outcome.
- Loophole cell: fair-sampling removal, predictable settings, scanned coincidence windows, memory, and drift each block promotion.
- Randomness cell: only the admitted lower confidence bound on CHSH enters the guessing-probability map.
- Network cell: I and J enter the bilocal witness sqrt(abs(I)) + sqrt(abs(J)); source covariance blocks the independence premise.
- Context cell: the KCBS sum is paired with order-randomized compatibility diagnostics.
- Composition cell: source, phase, reset, and covariance certificates are mandatory.
- Lift cell: the finite-field barcode export and characteristic-zero lift are distinct authority objects.

The packet is a deterministic wiring oracle. Physical status remains not run.

