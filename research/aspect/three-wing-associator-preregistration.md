# Three-wing associator preregistration

## Frozen experiment

Prepare a heralded three-wing `000`--`111` coherent carrier. Randomize every
heralded trial among direct ternary analysis, left-bracketed coherent sewing,
right-bracketed coherent sewing, and destructive pairwise completion. Use
source coherence settings `3/5` and zero and four summed-phase quarter turns.

Retain the complete three-wing outcome alphabet, including every single,
multiple, and no-click pattern and every complementary vacuum-port result.
Bind all route, phase, detector assignment, ABBA block, transfer-matrix, delay,
and source-coherence calibrations to one immutable epoch.

## Primary laws

All six proper phase-sensitive estimands `A,B,C,AB,AC,BC` are zero. Direct,
left-bracketed, and right-bracketed ternary routes obey

\[
C(\Phi)=\gamma\cos\Phi.
\]

The destructive pairwise route is zero. The left-minus-right associator
residual is zero. Every route is zero at source coherence zero.

## Statistical contract

There are two coherence settings, four phases, six proper marginals, direct and
destructive routes, and four crossed logical-bracketing/hardware cells:
ninety-six primary proportions. Each requires one million effective
trials with absolute tolerance one twentieth.

Chebyshev followed by the union bound gives

\[
\frac{96}{4(1000000)(1/20)^2}=\frac6{625}.
\]

The conservative familywise ceiling is therefore ninety-six hundredths of one
percent.

## Thirty-four acceptance gates

The run passes only when:

1. the complete full-outcome record is present;
2. all routes share one calibration epoch;
3. left and right full transfer matrices match;
4. left and right delays match;
5. left and right complex Bargmann triad invariants match;
6. left and right native three-body process phases match;
7. logical bracketing is crossed with physical hardware identity;
8. every ordered pair of crossed treatments is balanced within the epoch;
9. the memory-erasing reset predecessor is independently certified;
10. reset target transfer is full-rank and boundedly reconstructible;
11. the reset preserves an informationally complete coherent process;
12. the direct ternary curve is invariant under reset insertion;
13. controlled-history rank does not exceed the admitted memory model;
14. reset laws are invariant under cyclic, reversed, and independent order families;
15. reset depths zero through ten obey the frozen recurrence;
16. one reset identity transports across every route, coherence, and detector context;
17. schedule-synchronous residual spectra are null at periods 2, 4, 8, 16, and 32;
18. coherence-amplitude cross-sweep curvature is null;
19. three-back controlled-history rank does not exceed the admitted model;
20. the context-by-order interaction is null;
21. the prime-period-31 residual is null;
22. spanning-flux primary and dual-gain transfers agree;
23. sealed reset-controller transcript replay is invariant;
24. environment-port coherent tomography is reconstructible;
25. the joint signed-mutation residual direction is null;
26. all proper phase marginals are null;
27. the direct ternary curve is present;
28. the left-bracket curve matches direct;
29. the right-bracket curve matches direct;
30. the associator residual is null;
31. destructive pairwise completion is null;
32. the zero-coherence control is null;
33. the ABBA ancillary phase gate passes;
34. counts and the familywise budget pass.

## Deliberate failures

The checker rejects pairwise phase leakage, a quarter-turn bracketing offset,
surviving fringe after destructive pairwise completion, missing no-click
records, a stale route epoch, unequal left/right transfer matrices, and equal
pairwise HOM visibilities with unequal complex triad phases, and identical
lower-excitation behavior with unequal native three-body process phases, and a
fixed logical-to-hardware assignment that confounds associator with circuit
phase, equal treatment counts with unbalanced first-order route memory, and
transition-specific memory without an independently certified reset. Eight
additional fixtures independently reject blindness, dephasing, reset-native
three-body phase, higher-history rank, order dependence, a non-geometric depth
law, context-specific transport, and schedule-synchronous resonance.
Eight compiler-generated fixtures additionally reject amplitude-dependent
phase, three-back memory, context-order interaction, period-31 response,
detector saturation, an adaptive reset side channel, coherent environment
leakage, and correlated signed-fault cancellation.

## Discovery rule

A nonzero associator claim is opened only after the other thirty-three gates pass and
the residual survives circuit exchange, global phase reversal, source-branch
reversal, detector reassignment, and sign-reversed ABBA blocks. The raw
left-minus-right curve is reported; it is not fitted away as a phase offset.

## Disposition

This is an executable finite preregistration for the new higher-relational
null. It turns the theorem-generated channel into a laboratory decision object.

## Verification

Run:

```text
python research/aspect/checkers/check_three_wing_associator_preregistration.py
```
