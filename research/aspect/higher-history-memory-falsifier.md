# One-step reset qualification can hide predictive rank three

The reset test currently conditions on one predecessor. A hidden apparatus state can depend on the two-back treatment, average to identical one-step laws, and survive the reset.

The exact hostile retains controlled histories `a → c → reset → b`. Averaging over the unrecorded label `a` gives the same suffix response `1/3` for every one-step condition. The controlled-history Hankel matrix is the three-by-three identity and has rank three. A ready/dead detector model has predictive rank at most two, so the history law forces another state.

Qualification must retain at least two predecessor labels and calculate predictive Hankel ranks across controlled suffix probes. Rank above the admitted memory model rejects the reset and enlarges the physical state carrier. Passing a one-step marginal is not evidence that the reset has erased longer memory.

Executable witness: `checkers/check_higher_history_memory_falsifier.py`.
