# Pro-Gram cutoff telescope

## Cutoff typing

This instrument uses admitted finite label or Gram truncations of the full
adelic source after global Fourier--Poisson sewing. It must never use a finite
Euler prime exhaustion.

A finite Euler product is an analytic determinant diagnostic, not the Tate
integral of a finite modification of the restricted source. Consequently it
has no source-authorized finite Poisson operator. Writing an adjacent-prime
commutator for such a packet would manufacture the operation being tested.

## Two completion observables

Grothendieck's remaining completion obstruction separates into two quantities
at every cutoff.

For every declared seminorm `q_j`, the Fourier compatibility defect is

`delta_(N,j)(x) = q_j((C_N F_N - F_(N+1) C_N)x)`,

where `C_N` is an admitted label/Gram cutoff transition. If source theory supplies a summable
envelope in every grade, telescoping makes transported Fourier sewing Cauchy
in the locally convex topology.

Finite separation is the algebraic condition

`intersection_j ker(A_(N,j)) = {0}`,

where the maps retain their primitive, square, tail, seam, and archimedean
types. Stacking may compute this kernel at a finite cutoff, but its singular
values have no invariant meaning across inequivalent grades.

## Optical extension

Run the same preregistered probe frame at adjacent admitted label/Gram cutoffs. Process it in both
orders—cutoff then Fourier, and Fourier then cutoff—and tomograph their vector
difference. This measures `delta_N`.

Record the coherent outputs of every declared packet channel and compute the
intersection of their finite kernels while preserving their types. Do not
assign the stacked array a master Euclidean norm.

Each cutoff returns pass, falsify, or abstain against source bounds fixed
before acquisition.

## What this can prove

Given externally proved source bounds:

- summability of `b_N`;
- uniform sewing bounds in every declared seminorm;
- continuity of every boundary current on its own grade;
- a common graph domain carrying the dual Green pairing;

the measured packet tests every finite premise of the completion theorem. A
resolved violation falsifies the proposed sewing or separation law at that
cutoff.

Finite measurements alone cannot establish that every per-grade defect tail
is summable or construct the common dual Green domain. Those are source
theorems. The apparatus identifies their exact finite observables and supplies
hostile witnesses when they fail.

No sequence of finite Euler products may be substituted for this truncation
system. Euler projections may be inspected only after global sewing and may
not be used to reconstruct it.

## Surprise

The graph/anti-graph seam floor is uniformly large within its Hilbert chart.
It cannot be compared numerically with the primitive dual grade or the
absolute-summable tail. The telescope instead tells Grothendieck which typed
seminorm and cutoff first loses Fourier compatibility.

## Verification

```text
uv run --with sympy python research/aspect/checkers/check_pro_gram_cutoff_telescope.py
```
