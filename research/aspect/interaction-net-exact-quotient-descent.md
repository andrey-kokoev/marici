# Exact quotient descent changes the optical architecture

Benincasa's source-reachable packets supply the exact finite linear relation
image that the first interaction-net pilot left unknown.

At depth three, both charts and both tested primes agree:

- generated rank: 4800;
- quotient rank: 53;
- relation-kernel rank: 4747;
- cross-grade reductions: 4763.

The agreement is strong replication evidence for the finite calculation. It is
not a characteristic-zero proof and it is not a complete categorical
presentation.

The decisive fact is nondescent. Exact reduction mixes the declared resource
grades, and the associated Rees table contains negative multiplicities. Thus a
context that reads one prequotient coordinate or one capped grade does not
automatically annihilate the relation kernel. Our earlier pro-context tower is
object-faithful only on the prequotient state set.

An admissible quotient-level optical port must be a linear functional on the
generated carrier that annihilates the exact relation kernel. Equivalently, its
row belongs to the dual quotient image. Physically, this points to coherent
interference among labelled bins, with weights fixed by quotient normal forms,
rather than independent intensity readout of each resource coordinate.

Depth four also prevents premature stabilization: the tested G12 packet grows
from quotient rank 53 to 1386. More range alone does not solve the problem.
Every new level needs a compatible quotient-dual basis and restriction map.

The remaining missing object is the complete categorical relation module.
The finite linear quotient closes rank and descent questions for the tested
carrier, but it cannot by itself identify path equalities or exclude silent
parallel operations.
