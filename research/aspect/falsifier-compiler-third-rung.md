# The third rung is a falsifier compiler

The physical reset and three-wing process are rung one. The original twenty-six-gate tester is rung two. Rung three treats that tester as an observation map and searches for mutation pairs that it cannot distinguish even though the scientific claim differs.

The exact prototype uses a sixteen-dimensional mutation carrier. Eight coordinates are the hostile families already recognized by the tester. Eight are frozen prototype holdouts: amplitude-dependent phase, three-back memory, context-order interaction, period-31 schedule response, detector saturation, adaptive reset side channel, coherent environment leakage, and correlated multifault cancellation.

The present twenty-six-gate observation map has diagnostic rank eight. It kills all eight primitive mutations, all twenty-eight unsigned primitive pairs, and all 112 signed primitive pairs, but its unresolved kernel has dimension eight. The smallest surviving pair differs only along amplitude-dependent phase while producing the all-zero current observation.

The compiler calculates an exact nullspace basis and synthesizes one dual intervention for each unresolved direction. Adding those rows raises detected and diagnostically reconstructed dimension from eight to sixteen and reduces the declared unresolved kernel to zero.

Two independent analyzers verify closure. Rational Gaussian elimination computes kernel dimension. Exhaustive enumeration checks all `65535` nonzero binary mutations: `255` survive before repair, exactly the nonzero vectors in an eight-dimensional binary kernel, and none survive afterward.

The third rung also rejects an always-reject meta-tester because its diagnostic rank is one, checks eight cyclic relabelings plus reversal, and retains an explicit open-world boundary. Closure is relative to the declared carrier. The holdout packet is a frozen prototype fixture, not an independently administered blind challenge.

The operational cycle is now:

`tester observation map → unresolved kernel → smallest hostile pair → synthesized separating intervention → repaired observation map`.

The synthesized rows have now been promoted into rung two as eight exact hostile-witness gates, producing a thirty-four-gate preregistration. Each new row separates its corresponding old-kernel mutation. Rung two remains physically incomplete until those interventions are constructed, calibrated, and admitted as raw apparatus records.

Master checker: `checkers/check_falsifier_compiler.py`.

Rung four now audits the mutation language itself. See
`fourth-rung-ontology-falsifier.md` and
`checkers/check_four_rung_tower.py`.
