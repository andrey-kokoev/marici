# Typed resource-enriched interaction-net kernel pilot

## Boundary

This pilot presents only SCC's local constructor and rewrite kernel as an
interaction net. Authority transitions, source derivation, completion, and
higher-coherence certification remain external typed gates. A rewrite cannot
create authority or promote a candidate to evidence.

Objects are typed open nets with ordered external interfaces. An admitted
rewrite is an active-pair replacement preserving that ordered interface. Every
rule carries an authority root, support delta, resource equation,
fault/modality grade, and coherence class. Duplication and deletion are agents,
not implicit meta-operations.

## Entry 4013 falsifier

Before relations, the source-generated carrier has three chart seeds, five
independent Boolean occurrence resources, and fiber labels

[
0\leq u\leq k,\qquad 0\leq v\leq 2k.
]

The fiber rank is ((k+1)(2k+1)), giving coefficients
[
1,6,15,28,45.
]
Summing through depth four and all occurrence grades gives
[
3\cdot 2^5\cdot(1+6+15+28+45)=9120.
]

The checker reproduces this separately in G12 and G31. For every tested finite
truncation it exhibits the first K edge leaving the truncation. Thus any
finite-closure interpretation fails.

## Three noninterchangeable outputs

At every bidegree the checker reports generated reachability, exact-relation
image rank, and quotient rank. Only the first is presently computed. The
relation module has not been supplied, so the other two remain typed unknowns.
Neither zero nor equality with generated reachability is inserted.

## Critical pairs

The bounded local system checks occurrence/occurrence,
occurrence/fiber-successor, duplication/constructor, and
deletion/constructor overlaps. Their declared normal forms join while
preserving the external interface. This establishes local confluence only for
the enumerated pilot rules, not for SCC as a whole.

## Semantic gate and verdict

The candidate executable context basis contains identity, five occurrence
ports, the fiber-successor port, and explicit duplication/deletion ports. Its
source generation is declared. Physical realizability and faithfulness of the
restricted enriched nerve are not yet certified. Full-Yoneda separation by
arbitrary nonexecutable probes is rejected.

The verdict is useful-but-incomplete presentation: Entry 4013 reachability and
the bounded rewrite kernel fit, but the exact relation module and faithful
executable semantic context basis remain missing.
