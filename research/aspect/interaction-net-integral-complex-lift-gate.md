# Integral complex lift gate

The finite-field barcode skeleton is stable, but its coefficients do not admit a canonical coefficientwise CRT lift. The accepted constructor must lift the entire source complex.

The packet begins with free integral modules, integral relation matrices, and integral depth transitions. Kernels and images are saturated before quotient formation. Smith and Hermite invariants certify the integral lattice structure. Comparison maps reduce the same integral complex to each good-prime packet. Quotient-dual covectors are then derived, checked primitive, paired with the quotient basis, and source-normalized.

Only after that sequence may a covector enter the optical analyzer compiler.

The gate includes four hostile fixtures:

- a non-saturated relation lattice;
- a coefficientwise representative masquerading as a complex lift;
- a covector that does not annihilate the relation lattice;
- a nonprimitive covector whose optical normalization hides an integral factor.

The current candidate locator is intentionally absent. The checker validates the ingestion path and hostile separation while reporting awaiting_source_integral_complex.

