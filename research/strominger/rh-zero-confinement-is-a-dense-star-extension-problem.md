# RH zero confinement through an ordered star-extension candidate

Status: architectural reduction with explicit no-go boundaries; no
zero-confinement theorem is claimed.

## Separation of the two source loci

The completed theta boundary packet generates the polarization coordinate

\[
z=s-\frac12
\]

and therefore the critical seam.  Independently, the reciprocal prime
boundary current changes completion class at a scalar zero.  These explain
two marginal loci:

1. the fixed/equal-share seam selected by reciprocal reflection;
2. the completion-upgrade locus selected by the scalar readout.

RH is not either marginal statement.  It is the incidence assertion that the
second locus is contained in the first.

## Common-source gate

The global adelic Tate operation closes this gate at scalar/determinant-line
level.  Poisson sewing and Euler factorization are source-derived
presentations of one distinguished completed section.  Thus the seam and the
prime scalar presentation do have one rung-1 source.

What is not constructed is a common star-bearing state realization.  The
additive theta system and multiplicative Euler loop meet on the determinant
line; prime powers are cyclic traces of one loop, not independent state
ports.  A stronger source object or source category \(\mathcal S\), a single
state realization \(F(\mathcal S)\), and natural readouts would be needed:

\[
F(\mathcal S)\longrightarrow\mathsf{BoundaryPolarization},
\qquad
F(\mathcal S)\longrightarrow\mathsf{PrimeCompletionPacket}.
\]

Their source-variation squares must commute before zero incidence is used.
Without this state-level gate, an extension map could be a fitted lift of a
valid scalar comparison.  In particular, cyclic prime traces must not be
reified as Gram-vector ports merely because such a lift would support a
positivity argument.

## Zero-to-state attachment is already exact

The unitary modulus action of the adelic source has spectral parameter

\[
s=\frac12+it.
\]

This explains the seam as the unitary dual selected by the half-density
normalization.  The global Tate integral also constructs the scalar completed
section.  Grothendieck's bordered doubled-tail operator closes the
zero-to-state bridge without inspecting the divisor:

\[
S_\partial(z)=0
\quad\Longleftrightarrow\quad
\ker
\begin{pmatrix}A_z&B\\C&D\end{pmatrix}
\ne0.
\]

The bulk block \(A_z\) remains invertible; the nonzero state belongs to the
closed bulk-plus-boundary system.  Thus scalar vanishing already has a
source-derived state preimage.  What is missing is a law that forces this
state to satisfy the star-compatible or coercive seam condition.

## Minimal coercive completion theorem

Use the normal spectral coordinate

\[
u=-i\left(s-\frac12\right),
\]

so the critical seam is the real \(u\)-axis.  The existing bordered pencil
already supplies determinant/kernel faithfulness.  Its Green identity,
however, has a mixed forcing residual.  In centered \(z=s-1/2\) coordinates
it has the form

\[
2\operatorname{Re}(z)E(v)
=
-2\operatorname{Re}\mathcal K(z;v),
\]

where \(E(v)>0\) for a nonzero zero-state and \(\mathcal K\) is the
source-derived mixed-sheet forcing.  Consequently the remaining sufficient
theorem is either

\[
\mathcal K(z;v)=0
\]

on every admissible zero-state, or the weaker oriented inequality

\[
\operatorname{Re}(z)\operatorname{Re}\mathcal K(z;v)\ge0
\]

for every off-seam zero-state.  Either condition contradicts the Green
identity when \(\operatorname{Re}z\ne0\).

The forcing is itself the odd transform of a positive theta autocorrelation
measure.  Positivity of that measure is insufficient because its cosine
transform oscillates.  The missing force must therefore be a joint
theta--arithmetic incidence law evaluated on the already constructed
zero-state, not another zero-to-state lift.

The hostile failures are explicit: a positive two-shell source with an
off-seam scalar zero and incorrectly oriented forcing, a completion domain on
which the Green identity fails, or an arithmetic correction defined as the
negative of the observed residual.

## Arithmetic generator subgroup

Prime translations generate, after reciprocal group completion,

\[
G_{\rm arith}
=
\left\{\sum_p n_p\log p:n_p\in\mathbb Z
\text{ and finitely many }n_p\ne0\right\}
=\log\mathbb Q_{>0}.
\]

This is a dense subgroup of the source translation group \(\mathbb R\).
Finite prime cocycle diamonds already prove coherence on finite words.  A
scalar zero supplies the separate completion-class upgrade that makes the
exceptional primitive and square packets summable.

## Restriction and essential image

Conditionally, let \(\mathsf{Coc}^{\rm cont}_*(\mathbb R)\) denote the source-continuous,
star-compatible boundary cocycles on the full translation group, and let
\(\mathsf{Pack}^{\rm sum}(G_{\rm arith})\) denote labelled summable arithmetic
packets.  Restriction gives

\[
\operatorname{Res}:
\mathsf{Coc}^{\rm cont}_*(\mathbb R)
\longrightarrow
\mathsf{Pack}^{\rm sum}(G_{\rm arith}).
\]

Grothendieck's continuous-cocycle theorem shows that reciprocal transport can
agree with Hilbert adjunction only on the critical seam.  Consequently a
zero-confinement theorem would follow from the source-derived extension law

> Every arithmetic packet produced by a nontrivial zero lies in the
> essential image of \(\operatorname{Res}\).

This is stronger than summability.  An off-seam zero can upgrade the discrete
completion class while failing star-compatible continuous extension.

## Exact extension gates

For a labelled packet on the dense subgroup, the useful sufficient gates are:

1. exact group and cocycle coherence on \(G_{\rm arith}\);
2. positivity of every source-derived finite Gram matrix;
3. continuity at the identity in the source topology, uniformly as
   \(\log q\to0\);
4. compatibility of reciprocal transport with the involution on the complete
   labelled packet, not only after scalar aggregation.

The first gate is finite algebra.  The second and third are the analytic
completion problem.  Once they hold, the dense arithmetic action has a unique
continuous star-compatible extension to the real translation group.  The
continuous adjointness characterization then forces the zero state onto the
critical seam.

## Quotient defect

Define the extension defect schematically by

\[
\mathcal Q_{\rm RH}
=
\mathsf{Pack}^{\rm zero}_{\rm arith}
\big/
\operatorname{EssIm}(\operatorname{Res}).
\]

This is the RH analogue of the unresolved Jacobian quotient in the magnetic
boundary problem.  A nonzero class is a summable arithmetic zero packet that
cannot be promoted to a source-continuous star representation.  RH asks that
the actual zeta zero packets have zero extension defect.

The notation is classificatory until the packet category and quotient are
constructed; it does not itself prove confinement.

## Hostile deformation

An even real quartet factor preserves functional reflection, boundary
reality, boundary sign, and the critical seam while inserting off-seam zeros.
It therefore preserves the marginal fixed-locus explanation.  A successful
source theory must reject it at the attachment or extension gate: the factor
must fail to arise from the fixed arithmetic generator packet, fail a finite
Gram condition, or fail the source-topology continuity estimate.

This supplies the decisive falsifier.  If an inserted off-seam quartet also
produces a packet in the essential image of the same source restriction
functor, dense star extension does not imply RH.

## Rung interpretation

1. Source atom: the global adelic Tate operation with self-dual measure,
   Fourier/Poisson sewing, and the modulus action.
2. Authorized action: the labelled prime translations and their relations.
3. Local construction: the bordered doubled-tail system, whose Schur
   complement turns a scalar zero into a nonzero bulk-plus-boundary state.
4. Global attachment: the theta autocorrelation forcing and the arithmetic
   prime current must attach to that same state through one source identity.
5. Completion: either an oriented forcing law on the bordered state or a
   continuous star extension from \(\log\mathbb Q_{>0}\) to \(\mathbb R\).

This separates three gates:

1. line generation is closed by half-density normalization and Poisson
   reflection;
2. zero-to-state attachment is closed by the bordered Schur realization;
3. state-to-seam confinement remains open because the mixed forcing has not
   been oriented on the exact zero-state.

The missing theorem is therefore not another determinant realization and not
positivity in isolation.  It is a source-derived compatibility law between
the Gaussian heat-flow current and the arithmetic staircase current before
either is scalarized.

## Closed-loop normal form

The bordered realization gives a sharper equivalent target.  Write its
boundary Schur complement as

\[
S_\partial(z)=D(z)-C(z)A(z)^{-1}B(z).
\]

After an authorized normalization of the direct boundary channel, define

\[
L(z)=C(z)A(z)^{-1}B(z),
\qquad
S_\partial(z)=I-L(z).
\]

A scalar zero therefore supplies a nonzero closed-loop state satisfying

\[
L(z)v=v.
\]

The state-to-seam theorem is exactly the assertion that the source-defined
loop has no eigenvalue \(1\) in either open half-plane.  The mixed forcing in
the Green identity is the feedback cross term of this same equation, not an
independent defect.

There are three noncircular sufficient mechanisms:

1. a strict small-gain estimate for the complete loop;
2. strict dissipativity of \(I-L(z)\) on its declared graph domain;
3. a well-founded source grading that is strictly raised by \(L(z)\).

Each mechanism must include the seam, endpoint, and arithmetic incidence
channels.  A contraction of the invertible Volterra bulk \(A(z)\) proves only
open-loop solvability and says nothing about the boundary divisor.

The global pointwise orientation of the forward-minus-backward Poisson field
is not an admissible substitute: both evolutions conserve total mass, so a
nontrivial integrable difference cannot have one sign everywhere.  The
orientation claim is meaningful only after restriction to the closed-loop
kernel relation.  Likewise, no finite tower of source antiderivatives can
close the mixed residual away from \(z=0\); a successful local-current proof
must retain the full shift state or derive an independent finite closure law
from the theta source.

## Source-typing obstruction to a larger Schur matrix

The arithmetic refinement cannot be obtained by adjoining one boundary
coordinate for each prime-power grade.  There is one labelled primitive
operator

\[
L_s e_p=p^{-s}e_p,
\]

and its prime-power contributions are cyclic readouts

\[
\tau_k(s)=\frac1k\operatorname{Tr}(L_s^k).
\]

Thus \(k=1\), \(k=2\), and \(k\ge3\) are return lengths of one loop, not
independent state ports.  Reifying them as a direct sum introduces
source-absent cross terms and falsely turns cyclic degree into a
well-founded grade.

The determinant-three unit removes every \(k\ge3\) contribution without
moving the divisor.  The remaining scalar connection has precisely the
primitive current, the square current, and the archimedean line.  These
coordinates may be retained as a residual determinant-line packet, but they
are not three Hilbert coordinates of the theta Schur state.

The legitimate lift is consequently a nonlinear, variance-aware comparison

\[
\alpha_s:
\mathcal L_\theta(s)
\longrightarrow
\mathcal L_\infty(s)\otimes
\operatorname{Det}_{\rm rel}(I-L_s),
\]

constructed before scalar evaluation.  Small gain and grading on the
one-dimensional theta Schur complement are explanation-inert: there they
are merely reformulations of zero-freeness.  Any new force must arise in the
ordered primitive loop, the reciprocal comparison, or their global
attachment—not from enlarging the scalar boundary space by hand.

## First source-specific intervention law

The primitive theta profile now supplies a genuine ordered response that is
invisible in the scalar endpoint alone.  With a moving observation wall
\(a\), its moments satisfy

\[
M_k'(a)=-kM_{k-1}(a),
\]

and the primitive cubic flux obeys

\[
\Omega'(a)=-h_4(a)+3h_6(a)-3h_8(a)+h_{10}(a).
\]

Exact score elimination identifies the pointwise sufficient sign with a
fourth-order memorylessness-defect balance between adjacent residual-power
laws.  The exponential reference cancels exactly; the theta carrier has two
negative repair terms competing with a positive skew-drift term.  This is
source-specific intervention information, not generic positivity.

It is nevertheless only one vertical side of the required comparison
square.  Let wall displacement act vertically on the additive theta
realization and let \(\alpha_s\) denote the horizontal theta--Euler
comparison.  A completed explanatory cell would require an independently
defined Euler-side intervention \(W_s\) and a source coherence

\[
\alpha_s\circ W_\theta
\;\Longrightarrow\;
W_s\circ\alpha_s.
\]

The certified primitive flux supplies \(W_\theta\).  It does not yet define
\(W_s\), transport prime labels, or prove the square.  Equal scalar fluxes
after determinant projection would not suffice, because the comparison is
required in the ordered lens.

This locates a smaller missing constructor than a common Hilbert state:
an Euler-side response to the same source intervention, together with a
typed coherence cell whose scalar shadow agrees with the known completed
section.

## Principal-idèle square and its boundary

A positive rational displacement \(q\) does supply one canonical global
intervention.  Its placewise charges

\[
\lambda_v(q)=\log|q|_v
\]

obey

\[
\sum_v\lambda_v(q)=0.
\]

Thus principal-idèle transport couples an archimedean dilation to the finite
valuation shifts and closes the degree-one comparison square.  This explains
why \(\log\mathbb Q_{>0}\) is the natural dense transport subgroup rather
than a fitted arithmetic lattice.

The product formula supplies neutrality only.  It does not select a
quadratic form on the charge-zero hyperplane, much less the order-four form
detected by the primitive moving-wall flux.  Weighted positive metrics share
the same neutral locus, so positivity cannot authorize one of them.

There is also an exact separation no-go.  For an admissible even real test
state \(h\), the dilation matrix coefficient has Mellin transform

\[
\mathcal X_h(s)=2\zeta(s)\mathcal M[h](s).
\]

When \(\mathcal M[h]\) is zero-free in the open strip, archimedean
interventions change only the local factor and leave every nontrivial zeta
zero fixed.  Consequently the certified order-four wall flux explains an
archimedean coherence gate but cannot by itself orient the arithmetic
divisor.

The missing cell must act internally on the arithmetic amplitude after the
principal-idèle degree-one square has closed.  Equivalently, it must derive a
metric, incidence form, or scattering law on the labelled finite-place
boundary that is not determined by product-formula neutrality.

## Smallest remaining constructor

The canonical dilation generator is self-adjoint, but the completed section
is a Mellin-transformed matrix coefficient of that generator, the theta
vacuum, and the rational comb.  Self-adjointness of the carrier does not
constrain complex zeros of an arbitrary matrix coefficient.

Several incidence ingredients are already exact:

1. rational translation and modulation invariance select the comb line
   uniquely up to normalization;
2. the product-formula charge defines a maximal isotropic relation on the
   finite-place and algebraic all-place hyperbolic doubles;
3. reciprocal Fourier transport is correctly typed through two opposite
   rigged Clifford charts;
4. the standard adelic vacuum supplies the canonical nuclear bridge

   \[
   K_f=P_{\mathbb Z}e^{-\pi N^2}P_{\mathbb Z};
   \]

5. the admitted integral sector is canonically the arithmetic phase circle
   \(L^2(\mathbb R/\mathbb Z)\).

The kernel of \(K_f\) records nonintegral inadmissibility.  It has no Riemann
zero inside the surviving integral sector, because the heat multiplier is
strictly positive there.  Thus the bridge closes source selection and
smoothing, not zero confinement.

The smallest remaining constructor must retain the entire dilated circle
heat family and turn its modular comparison into spectral incidence.  It
must derive two Green boundary relations before the scalar trace and Mellin
readout:

\[
\mathcal L_z,\qquad \mathcal L_{\rm arith},
\]

such that

\[
\Xi(z)=0
\quad\Longleftrightarrow\quad
\mathcal L_z\cap\mathcal L_{\rm arith}\ne0.
\]

Its contract must also derive the Green form, prove the arithmetic relation
maximal self-adjoint or Lagrangian, and identify the corresponding relative
determinant with \(\Xi\) up to a source-derived nowhere-zero unit.  The
bordered Evans state proves that a zero-state exists; this stronger
constructor would type that state as a loss of transversality in a geometry
whose admissible parameter locus can potentially be confined.

The algebraic pure-spinor maximality does not automatically prove maximal
self-adjointness in this Green topology.  Likewise, projective overlap
incidence is automatic but insufficient, and the raw moving-seam projection
is non-Fredholm on continuum \(L^2\).  The missing arrow is therefore a
source-selected passage from the dilated arithmetic-circle correspondence to
a Green-compatible Fredholm boundary family.

The decisive hostile model keeps the same self-adjoint dilation carrier and
rational comb but changes the Fourier-stable vacuum within the common core.
If its matrix coefficient acquires off-seam zeros, the incidence constructor
must reject that vacuum by a condition visible before its zeros are located.
Otherwise it is merely a repackaging of the scalar divisor.

## Next falsifiable objective

The one-sided tail channels and their endpoint traces are already controlled.
Writing their invertible generators as \(A_+(s)\) and \(A_-(s)\), the
completed two-sector problem Schur-reduces to the seam return

\[
R_s=A_-(s)^{-1}C_sA_+(s)^{-1}B_s.
\]

A scalar zero must be identified with a nonzero interface state satisfying

\[
R_sv=v.
\]

The source has constructed the global Tate scalar sewing and a
regulator-universal relative Euler detector, but not an operator-valued lift
of that sewing to the incidence maps \(B_s,C_s\).  Ordinary additive Hilbert
restriction cannot supply them because the idèles form an additive-null
boundary.  The maps must be rigged multiplicative traces defined before
additive Hilbert completion.

The next exact contract is therefore:

1. construct \(B_s,C_s\) from the labelled global Tate correspondence;
2. prove their covariance under finite-place bonding and reciprocal exchange;
3. prove the completed return map is closed and belongs to a declared
   determinant class;
4. derive

   \[
   \Xi(s)=u(s)\det_{\rm rel}(I-R_s),
   \qquad u(s)\ne0,
   \]

   without using the divisor;
5. identify the return-map Green form with the already derived mixed forcing
   current.

Finite-stage injectivity is insufficient: every finite Euler detector is
nonzero, while the nontrivial divisor appears only after restricted-product
completion.  Rank-one endpoint incidence is also insufficient because it has
an explicit two-label kernel.  A valid determinant--kernel bridge must act on
the distinguished completed Fock state variety, not on arbitrary sparse
coefficient packets.

The decisive hostile witness is a source-admissible lift for which the scalar
Tate finite part is correct but \(I-R_s\) remains invertible at one of its
zeros, or becomes singular where the scalar section is nonzero.  Either
witness proves that scalar sewing and operator incidence have not been
faithfully coupled.

## Bounded evidence

The deterministic ordered-theta--Euler-correspondence gate verifies finite
cyclic cancellation through length ten on four labelled prime modes.  It also
contains:

- a determinant-one shear showing that scalar determinant data do not recover
  ordered reflection variance;
- an exact local Tate fixture whose defect changes sign within one open
  half-sector;
- explicit rejection of independent prime-power state ports.

The checker is
research/strominger/checkers/ordered_theta_euler_correspondence_gate.py and
its machine-readable result is
research/strominger/results/ordered_theta_euler_correspondence_gate.json.
The result declares the comparison-cell constructor missing and RH untested.
A passing gate is therefore evidence for the typing reduction and its no-go
boundaries only.
