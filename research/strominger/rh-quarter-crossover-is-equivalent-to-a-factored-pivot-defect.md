# Quarter crossover is equivalent to a factored pivot defect

## Problem

The curvature conjecture must be translated into the local normalized pivot variable governed by condensation.

## Bold conjecture

For \(t=\kappa n\), the normalized pivot ratio tends to

\[
A(\kappa)=
1-rac{\kappa^2(2\kappa^2+2\kappa+1)}
{2(\kappa+2)(\kappa+1)^3}.
\]

## Named rivals

The rivals are an unfactored defect with a different denominator power, a curvature fit that does not descend to pivots, and finite-degree bias.

## Risky consequences

Exact condensation must recover the proposed curvature from \(A\), and the pivot defect must remain positive at every tested positive ratio.

## Strongest falsification attempt

Using

\[
1-A(\kappa)=
\left(\frac{\kappa}{\kappa+1}\right)^4F(\kappa),
\]

exact algebra gives the displayed factorization. The polynomial identity

\[
2\kappa^3+4\kappa^2+3\kappa+1
=(\kappa+1)(2\kappa^2+2\kappa+1)
\]

produces the cubic denominator. Seven rational test ratios have zero residual; replacing that denominator by the fourth power fails deliberately. All five gates passed.

## Disposition

Resolve the equivalence between the crossover and the factored pivot defect. The next leaf is `quarter-pivot-defect-leading-balance`: derive this rational defect independently from the large-degree LU or equilibrium balance.

## Claim boundary

The exact transformation proves equivalence, not the limit. The pivot formula inherits the unproved crossover conjecture.
