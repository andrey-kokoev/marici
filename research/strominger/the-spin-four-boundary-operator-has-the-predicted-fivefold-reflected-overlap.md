# The spin-four boundary operator has the predicted fivefold reflected overlap

## Independent matrix hostile

The reflection falsifier predicted that spin four is the first exceptional
member of the general-spin affine family.  Here

\[
s=4,qquad n=4s-1=15.
\]

Rather than testing only the affine frame, reconstruct the full sparse
boundary blocks from the covariant spin-four Laurent fold

\[
c_j=\binom gj(-1)^{g-j}
a^{\overline{g-j}}(8-a)^{\overline j}.
\]

On the chart locus

\[
q=2g+16,
\qquad
k=\frac g2+8,
\]

the exact matrices at even grades \(2\le g\le10\) each have a unique
one-dimensional left cocircuit.  In every case the full source matrix obeys

\[
(2g+15)R_1-(3g+15)R_0=0.
\]

Thus the constant fifteen is recovered from the sparse operator, not inserted
only as the determinant of a proposed two-by-two frame.

## Reflected joint germ

The two oriented chart coordinates are

\[
\ell(x,y)=3x-2y,
\qquad
\ell_X(x,y)=3y-2x
\pmod{15}.
\]

Enumerating all 225 input pairs gives only 45 joint outputs.  Every output has
exactly five preimages.  The common zero fiber contains the nonzero class

\[
(3,12),
\]

and its cyclic multiples.

Therefore the reflected chart packet is not the naive square
\((\mathbb Z/15)^2\).  It is a 45-class image with a five-element invisible
fiber.

## Meaning

This confirms the arithmetic prediction generated by the failure of direct
reflection descent:

\[
\frac{15^2}{\gcd(15,5)}=45.
\]

The oriented cyclic-resolution theorem still gives one \(\mathbb Z/15\)
character packet.  Sewing it to its reflected chart introduces a new
five-primary relation.  Hence orientation-preserving and reflection-completed
resolution are genuinely different constructors.

## Scope

The calculation is exact for the covariant spin-four Laurent operator and is
checked on five growing sparse blocks.  Treating that operator as a physical
higher-spin gravitational source remains conditional: the existing Bondi
source derives spin two, not a propagating spin-four field.  The result proves
the internal general-spin prediction, not the existence of such a physical
sector in Einstein gravity.
