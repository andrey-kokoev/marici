# The Fox fiber controls the square class of snake torsion

## Question

After commutator decoration, does the magnetic index merely acquire an additional square multiplier?

If so, the twice-square law would fail numerically while retaining the same underlying rational square class.

It does not.

## Exact square-class test

For positive integers \(m,n\),

\[
m/n\in (\mathbb Q^\times)^2
\]

if and only if

\[
mn
\]

is an integer square.

This permits exact comparison in

\[
\mathbb Q_{>0}^\times/(\mathbb Q_{>0}^\times)^2
\]

without factoring the large integers.

The four tested indices—the atomic baseline and three commutator decorations—are pairwise inequivalent in this quotient.

## Consequence

The nonabelian Fox fiber changes more than the size of the snake torsion. It changes its rational square class.

Thus a proposed factorization

\[
I(w)=2K(w)^2
\]

cannot be repaired off the atomic stratum merely by replacing \(K\) with a larger integer conductor. A genuinely new factor is present.

The natural refined form is

\[
I(w)=\delta(w)K(w)^2,
\]

where \(\delta(w)\) is square-class data derived from the source cocycle. On the atomic baseline,

\[
\delta=2.
\]

The present calculation shows that the three decorated words have three different values of \(\delta\), all distinct from \(2\).

This introduces a new invariant without yet identifying its closed source formula.

## Structural picture

The magnetic C mechanism now separates into three layers:

1. reflection geometry forces the rank-one diagonal snake;
2. an integral conductor controls square magnitude;
3. a Fox discriminant controls rational square class.

The earlier twice-square theorem is the special locus where the discriminant is the constant class of \(2\).

## Next falsifier

The decisive next question is whether \(\delta(w)\) is determined by a finite Fox-Jacobian invariant—such as a determinant, reduced norm, or torsion pairing—or requires the full group-ring cocycle.

A finite formula would turn the enormous indices into a tractable source discriminant. Failure would show that no fixed finite abelian quotient classifies the snake arithmetic.

## Evidence

Four indices produce six pairwise comparisons. Every product is tested by exact integer square root. All four aggregate gates pass.
