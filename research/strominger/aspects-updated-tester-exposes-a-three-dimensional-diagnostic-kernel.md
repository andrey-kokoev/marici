# Aspect's Updated Tester Exposes a Three-Dimensional Diagnostic Kernel

## Tester application

Aspect's current machinery has six levels:

```text
formal realization
hostile tester
falsifier compiler
ontology falsifier
admissibility governor
quotient-aware portfolio controller
```

The metaplectic sign instrument passes only the first two.

The four-capability packet is formally sufficient, and every single-capability
deletion destroys sign distinguishability. That proves detection minimality.
It does not prove diagnostic completeness.

## The hidden kernel

Suppose the four primitive failure coordinates are:

```text
phase lift absent
coherent preparation absent
conditional coupling absent
complementary readout absent
```

The live instrument exposes one terminal predicate: sign contrast present or
blind. Every single failure produces the same blind result. Its linearized
observation matrix is therefore

\[
M=\begin{pmatrix}1&1&1&1\end{pmatrix},
\]

with rank one and kernel dimension three.

Our deletion experiments knew which constructor was deleted because the test
harness performed the mutation. A live observer seeing only the final blind
result cannot diagnose which capability failed. This is precisely Aspect's
tester-versus-falsifier-compiler distinction.

## Minimal declared-carrier repair

Retain the terminal contrast and add three independent health/tomography ports,
for example:

```text
linear-lift/path witness
selector-coherence tomography
conditional-process tomography
```

The fourth component is then inferred at the linearized declared-carrier level,
and the repaired four-by-four observation matrix has full rank. This does not
give exact diagnosis of arbitrary simultaneous binary failures. Entry 3733
shows that four additional binary monitors are required for that stronger
task.

This is only closure relative to the declared four-dimensional failure carrier.
Aspect's ontology rung immediately supplies fresh challenges:

- correlated two-port failure;
- spectator-grade leakage;
- control-phase drift;
- context-dependent branch swap.

No finite diagnostic vocabulary is absolutely closed under fresh mutation
constructors.

## Six-rung disposition

```text
rung 1 formal realization: pass
rung 2 hostile deletion tester: pass
rung 3 diagnostic falsifier compiler: fail, rank 1 of 4
rung 4 absolute ontology closure: reject
rung 5 physical admissibility: reject
rung 6 apparatus portfolio: reject
```

The correct statement is therefore:

> The four-capability packet is a minimal formal detector, not a
> self-diagnosing or physically admitted instrument.

This is a material strengthening of the result because it identifies the next
three independent observation directions rather than merely repeating that
physical authority is absent.

## Evidence replay

```powershell
C:\Users\andrey\.local\bin\python.exe research/strominger/checkers/aspect_metaplectic_instrument_six_rung_audit.py
```

The audit replays Aspect's current full falsifier and six-rung checkers before
applying their rank, ontology, governance, and apparatus gates to the endpoint
instrument.
