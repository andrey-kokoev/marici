# Compatibility notice: superseded physical-applicability theorem

The coordinate-Green version of this packet is withdrawn. The canonical
corrected theorem is `completed-physical-engine-master-theorem.md`.

The correction replaces the coordinate kernel by `D_z^2(S log S)`, removes
the spurious `xi^2/z^2` cocycle, moves the exterior onset from depth two to
depth four, replaces the geometric/quartic cutoff calculation by the
negative-binomial/sextic theorem, and distinguishes complete local ports from
ordinary contour periods.
