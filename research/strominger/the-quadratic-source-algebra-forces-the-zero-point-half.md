# The Quadratic Source Algebra Forces the Zero-Point Half

## Source commutator

The one-mode quadratic constructors are

\[
K_+=\frac12u^2,
\qquad
K_- = \frac12\partial_u^2.
\]

Their commutator on every monomial is

\[
[K_-,K_+]u^n
=
\left(n+\frac12\right)u^n.
\]

Therefore

\[
[K_-,K_+]=N_u+\frac12I.
\]

If the Cartan generator were written as \(H_c=N_u+cI\), exact closure with
the declared raising and lowering constructors would require

\[
H_c=[K_-,K_+],
\]

and hence uniquely \(c=\tfrac12\).

## Two different quotients

Channel dynamics cannot distinguish any value of \(c\), because scalar shifts
drop out of \([H_c,\rho]\). Source-algebra closure distinguishes exactly one.
Thus the maps are

```text
quadratic source constructors
  derive c = 1/2
  define the linear metaplectic lift
  project to channel dynamics
  erase c completely
```

The metaplectic phase is not an arbitrary normalization chosen after the fact.
It is already encoded in the incompatibility between quadratic raising and
lowering at the vacuum boundary.

## Boundary localization

For \(n>0\), the number contribution \(n\) looks like ordinary degree. The
half-unit is visible most sharply on the vacuum:

\[
[K_-,K_+]1=\frac12.
\]

So the global double-cover sign is generated by a local boundary correction at
the bottom of the grade tower. Exponentiation transports that correction into
the global \(2\pi\) holonomy.

This is the endpoint instance of the recurring pattern:

```text
boundary defect in local constructor algebra
  -> central affine correction
  -> global compositional residue
```

## Authority boundary

The source algebra now authorizes the mathematical scalar lift. What remains
unproved is physical realization of that scalar relative to an inactive
control branch. Entries 3707 and 3709 therefore remain operative at the
execution interface, not at the invariant-construction interface.

## Evidence replay

```powershell
C:\Users\andrey\.local\bin\python.exe research/strominger/checkers/source_forces_zero_point_half_checks.py
```

The exact checker verifies monomial degrees zero through one hundred and
contrasts unique source closure with channel-level scalar freedom.
