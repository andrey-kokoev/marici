# The magnetic snake sees the nonabelian Fox fiber

## Result

The equal-charge hostile was inserted into the actual magnetic source construction.

The baseline tail \(x\) was replaced by:

\[
x[x,y],
\qquad
[x,y]x,
\qquad
[x,y]x[x,y]^{-1}.
\]

Every replacement has the same exponent charge as \(x\):

\[
(1,0,0).
\]

The response parameter remained primitive, and the opposite right tail remained fixed.

The magnetic snake readout changed in every decorated case.

## Exact observations

The baseline cyclic target index is

\[
1568=2\cdot 28^2.
\]

The three decorated indices are:

\[
7306071077228271850849931638,
\]

\[
7865258181583837570522477026,
\]

and

\[
175728406342577936249240427957411658506347000.
\]

None is twice a square.

For both response signs:

- the relational determinant remains zero;
- a primitive relational kernel still exists;
- its lift remains diagonal;
- the full matrix content remains \(768\);
- the cyclic target index changes;
- the twice-square law disappears.

Thus the decoration does not destroy the snake mechanism. It changes the arithmetic coefficient carried by that mechanism.

## Interpretation

The magnetic readout does not factor through abelian tail charge.

It sees information inside the augmentation fiber of the Fox cocycle. In particular, the twice-square law is not a theorem for all words with primitive exponent charge. It belongs to the smaller source stratum where the relevant Fox data are atomic in the full nonabelian sense.

The distinction between the last hostile and a presentation change is important. Replacing only one tail by a conjugate while leaving the rest of the source fixed is a new source configuration, not a simultaneous inner change of basis of the whole construction. Its altered readout is therefore not a violation of presentation invariance.

## Refined square criterion

The evidence now supports:

1. primitive response content;
2. source-atomic Fox cocycle data, not merely primitive augmentation;
3. endpoint anti-invariance.

The integer charge is an efficient classifier only on the previously tested pure-power slice.

## Structural consequence

The relational rank defect is more robust than the square conductor law.

This separates two layers that had looked identical:

- reflection geometry forces a diagonal snake target;
- nonabelian source data determine the index of that target.

The first survives all tested commutator decorations. The second changes dramatically.

So the square is not the existence theorem for the snake. It is the arithmetic signature of the atomic Fox stratum inside the persistent snake family.

## Evidence boundary

The checker evaluates four exact tails and both primitive response signs using the faithful Artin action. All six aggregate gates pass. The calculation establishes the bounded hostile family, not a classification of every augmentation fiber.
