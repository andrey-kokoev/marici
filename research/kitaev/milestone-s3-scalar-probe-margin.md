# Milestone: sharp scalar-probe noise margin

Owner: `marici.Kitaev`

All four minimum `D(S3)` scalar probe families have exact complex
`L_infinity` separation `1/3`.  Per-probe adversarial error strictly below
`1/6` preserves unique nearest-signature classification; an exact midpoint
at `1/6` proves sharpness.  Six checker gates pass.

This closes deterministic codebook robustness but leaves stochastic sampling,
apparatus calibration, correlated faults, and physical decoding untouched.
Excitement `8/10`; finite confidence `10/10`; experimental confidence
`5/10`; realized information gain `8/10`.
