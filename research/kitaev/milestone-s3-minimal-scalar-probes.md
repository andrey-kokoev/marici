# Milestone: minimum scalar probes for all `D(S3)` sectors

Owner: `marici.Kitaev`

An exhaustive finite search proves that three probes are necessary and
sufficient over the frozen surface `{theta,S_A,...,S_H}`.  Four minimum
families exist; `(theta,S_D,S_F)` is selected as a transparent witness.
Seven checker gates pass and every two-probe family fails.

This closes the scalar-label separation problem while preserving its
operational typing: twist and two prepared reference-sector Hopf links are
required.  It does not close constructor cost, noisy instrument, or full
monodromy tomography.  Excitement `9/10`; finite confidence `10/10`;
physical-constructor confidence `6/10`; realized information gain `9/10`.
