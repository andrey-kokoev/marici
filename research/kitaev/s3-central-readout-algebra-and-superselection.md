# Central readout algebra and its ambient kernel in `D(S_3)`

Owner: `marici.Kitaev`

## Bounded question

What operator algebra is generated by the frozen twist and normalized
monodromy probes, and what information can it recover beyond classical sector
weights?

## Frozen representation space

For the ordered simple sectors `(A,B,C,D,E,F,G,H)`, use the induced-module
dimensions

\[
(d_a)=(1,1,2,3,3,2,2,2),\qquad \sum_a d_a=16.
\]

This gives the ambient direct sum

\[
\mathcal H=\bigoplus_a V_a,
\]

while the block-diagonal algebra has complex dimension
`sum_a d_a^2=36`.  Every frozen scalar probe acts as a sector scalar,

\[
O_f=\bigoplus_a f(a)I_{V_a}.
\]

Hence the probes commute and lie in the eight-dimensional center spanned by
the sector projectors `P_a`.

## Exact theorem

The normalization operator together with

\[
(\mu_B,\mu_C,\mu_D,\mu_E,\mu_F,\mu_G,\mu_H)
\]

has exact rank eight on the sector-value space.  It therefore spans the full
central readout algebra and reconstructs precisely the eight classical sector
weights.

It does not perform tomography on the ambient 16-dimensional Hilbert space.
The real Hermitian operator space has dimension 256, so the central readout
map has a 248-dimensional kernel.  The checker splits it canonically as

\[
248=220+28,
\]

where 220 dimensions are off-block inter-sector coherences and

\[
28=\sum_a(d_a^2-1)
\]

are traceless operators internal to the simple-sector blocks.  Explicit
invisible witnesses are an `A`--`B` Hermitian off-diagonal operator and
`diag(1,-1)` inside the `C` block.  Sector dephasing preserves every central
probe expectation.

## Superselection boundary

The 220-dimensional coherence kernel is an ambient algebraic statement, not
a claim that coherent superpositions of inequivalent anyon charges are
physically preparable.  A superselection rule may remove those states from
the admitted domain.  It does not remove the 28-dimensional within-sector
kernel, nor does it enlarge this central instrument into a noncentral one.
Conversely, the checker does not derive a superselection rule from the probe
algebra.

## Carrier versus coefficient lens

The direct-sum decomposition, block support, dephasing map, and distinction
between central and block-diagonal observables are reusable Carrier geometry.
The dimensions `(1,1,2,3,3,2,2,2)`, modularly derived values `mu_ab`, and
interpretation as anyon charge sectors require the quantum-double coefficient
lens.

## Assumptions and falsifiers

Assumptions: the earlier induced irreps and normalized monodromy matrix are
frozen; scalar probes act by their simple-sector eigenvalues; tomography is
linear expectation-value tomography on the stated operator domain.

Falsifiers: any selected central basis with rank below eight; any noncommuting
pair among the frozen probes; a central probe with nonzero trace pairing
against either explicit witness; or failure of sector dephasing to preserve a
probe expectation.  All are tested exactly.

## Verification and activation

Run:

```text
uv run --with sympy python -u research/kitaev/checkers/check_s3_central_readout_algebra.py
```

The saved result is
`research/kitaev/results/s3-central-readout-algebra.json`; seven aggregate
gates pass.  Post-objective excitement 10/10, confidence 10/10, realized
information gain 9/10.  The optionality delta is: central-sector tomography
is closed exactly; ambient state tomography is eliminated for this probe
algebra; superselection admissibility remains a separately typed branch.
