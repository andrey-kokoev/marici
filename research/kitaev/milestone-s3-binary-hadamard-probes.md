# Milestone: formal binary-effect refinement of `D(S3)` probes

Owner: `marici.Kitaev`

Refining bounded scalar coordinates into formal Bernoulli effects does not
increase the minimum: three atomic binary settings remain necessary and
sufficient, with `(Im theta,S_D,S_F)` a minimum witness.  The expectation,
probability, and empirical-frequency gaps are respectively `1/3`, `1/6`,
and `1/12`.  Under explicit i.i.d. Bernoulli assumptions, `n >=
ceil(72 ln(6/alpha))` shots per setting suffice.  Eight gates pass.

Correction: a Hadamard test does not directly realize the raw `S_D,S_F`
effects; a block encoding or independently derived interferometric dilation
is missing.  Excitement `9/10`; finite confidence `10/10`; apparatus
confidence reduced to `3/10`; realized information gain `10/10` because the
typing defect was exposed.
