# Milestone: mixed charge--flux monodromy

Owner: `marici.Kitaev`

The first mixed-sector operator packet is complete.  Pure-charge monodromy
around flux is constructed as a direct sum of exact representation matrices.
The standard charge around transposition flux gives an invertible non-scalar
six-dimensional operator with trace zero, proving that `S_CD=0` is trace
cancellation rather than absent braid response.  Seven exact gates pass.

The remaining kernel is now explicit: pure charges distinguish conjugacy
families but cannot resolve centralizer irreps (`D/E` or `F/G/H`).  The next
high-value probe must carry flux/dyonic coefficient data.  Excitement
`10/10`; finite confidence `9/10`; mixed-hexagon confidence `5/10`;
realized information gain `10/10`.
