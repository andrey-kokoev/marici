# The Deutschian RH obstruction is a two-polarization signature-order transport

## Question

What source-derived conserved distinction would make normalized off-seam scalar invisibility an impossible constructor task rather than a restatement of injectivity?

The current programme gives an exact answer to what an off-seam zero would construct, and isolates the missing explanation one layer earlier than the scalar resolvent.

## What is already exact

For a framed transfer

\[
F(z)=c^*(A-zI)^{-1}b,
\qquad
A=A^*,
\]

suppose a Hermitian invertible modular metric \(K\) satisfies

\[
AK=KA,
\qquad
Kb=\alpha c,
\qquad
\alpha\ne0.
\]

An off-axis zero produces a nonzero endpoint-reachable state \(x\) satisfying

\[
(A-zI)x=-b,
\qquad
c^*x=0,
\qquad
\langle x,Kx\rangle=0.
\]

Thus the impossible task has a concrete form: construct endpoint-reachable, source-invisible, modularly neutral memory that survives the arithmetic boundary and completion laws.

## Why neutrality is not yet a prohibition

Indefinite metrics contain nonzero neutral vectors. The three-pole signed measure

\[
d\mu=\delta_{-1}-\delta_0+\delta_1
\]

gives

\[
F(z)=\frac1{-1-z}-\frac1{-z}+\frac1{1-z}
\]

with zeros at \(z=\pm i\). The carrier is selfadjoint, the modular metric is exact, and the zero state is neutral.

Therefore conserved indefinite charge explains what the zero is but not why it is impossible.

## Two-moment collision theorem

Write the signed spectral measure as

\[
d\mu=d\mu_+-d\mu_-.
\]

For \(z=x+iy\) with \(y\ne0\), an off-axis zero requires equality of the positive and negative Cauchy-windowed masses and barycenters. With

\[
p_{x,y}(t)=\frac1{(t-x)^2+y^2},
\]

the required collisions are

\[
\int p_{x,y}\,d\mu_+=\int p_{x,y}\,d\mu_-
\]

and

\[
\int t p_{x,y}\,d\mu_+=\int t p_{x,y}\,d\mu_-.
\]

Charge neutrality is the first equality. Source invisibility supplies the second conjugate moment.

## A genuinely explanatory sufficient mechanism

If the positive and negative signature measures occupy ordered disjoint spectral regions, their positive Cauchy reweightings retain disjoint barycenter intervals. The second collision is impossible.

This is weaker than global positivity. Both signs may occur. What is forbidden is their interlacing into a pattern capable of neutral mass-and-barycenter matching.

The mechanism has counterfactual reach:

- changing magnitudes within either ordered region does not enable collision;
- changing readout coordinates does not change carrier order when the comparison is source-covariant;
- completion remains safe if stochastic order or a uniform separation survives;
- inserting a positive-negative-positive signature pattern predicts the first finite hostile.

This is the first candidate that qualifies as a Deutschian explanation rather than a zero reformulation.

## Why the candidate is not yet derived

The theta source has a natural ordered radial or Laplace presentation. Its exponential incidence kernel is sign-regular and variation diminishing after one order reversal.

The selfadjoint spectral carrier lives after a Fourier quarter-turn. Fourier minors have oscillating phase and no fixed sign. Ordinary variation diminution is not transported through that change of polarization.

Moreover, the actual theta source enters locally negative two-point Krein corridors near the seam at high frequency. No proof can orient the global signature by independently positive adjacent source pairs.

Thus the implication from an ordered positive theta source to an ordered endpoint spectral signature is unsupported.

The two sides live in different polarizations, and the transform between them does not preserve the required order by itself.

## The missing constructor

The needed object is a source-derived comparison cell retaining simultaneously:

- the ordered Laplace source flag;
- the Fourier/selfadjoint carrier;
- the modular signature decomposition;
- the endpoint/source incidence;
- seam and archimedean boundary flow;
- primitive and square-current completion data.

Its law must control the sign-transition pattern or a weaker stochastic order of the transported signed spectral measure without inspecting zeros or assuming the Herglotz quotient.

This is a composition capability, not another scalar observable. Neither polarization separately contains the required theorem.

## Deutschian conjecture

The hard-to-vary candidate is:

> Labelled theta/Tate incidence carries the ordered radial source flag into an endpoint-cyclic modular spectral measure whose opposite signatures cannot undergo a Cauchy-windowed mass-and-barycenter collision off the unitary seam, and this noncollision law survives the full boundary-bearing completion.

The strongest simple version is a single ordered sign transition. Weaker admissible forms include stochastic ordering, a monotone likelihood ratio, typed noncancellation at touching support, or a constrained variation-diminishing law on the zero-dynamics class.

## Exact hostile tests

1. A positive-negative-positive spectral triple compatible with all declared source labels falsifies single-transition ordering.
2. A Fourier paired minor whose phase changes under an authorized source perturbation falsifies fixed complex orientation.
3. Ordered finite cutoffs with a collapsing separation or stochastic margin falsify completion stability.
4. A theorem proved only after forming \(F\), its resolvent, or its zeros is circular.
5. Removing the seam or boundary current must predict which opposite-signature mass can escape or return.
6. Replacing constructor incidence by scalar functional-equation equality must restore an invisible neutral sector.

## What would count as a proper explanation

A proper explanation would derive the signature-order law from labelled source incidence before scalar aggregation, prove it closed under every authorized constructor and completion arrow, and use the two-moment theorem to show why neutral invisible memory cannot be constructed off seam.

A checker showing finite ordered weights would be confirmation. The explanation is the source law that makes alternating signature unreachable under all allowed counterfactual compositions.

## Current bottom line

The programme has reached the following irreducible frontier:

- off-seam zero equals neutral invisible reachable memory;
- neutrality is exact but not prohibitive;
- ordered signature would prohibit the required two-moment collision;
- source order exists in Laplace polarization;
- spectral charge exists in Fourier polarization;
- their orientation-preserving comparison is not constructed.

The missing conserved distinction is therefore not a scalar norm. It is the order type of modular signature across a source-authorized polarization crossing, including its boundary resource flow and completion margin.
