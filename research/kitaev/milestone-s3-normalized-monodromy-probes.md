# Milestone: normalized-monodromy probe repair

Owner: `marici.Kitaev`

The raw-`S` apparatus defect now has a constructive replacement.  Controlled
trace estimation measures `mu_ab=6S_ab/(d_a d_b)`; three settings remain
necessary and sufficient, with expectation gap `1/2`, probability gap
`1/4`, empirical radius `1/8`, and conditional shot bound `ceil(32
ln(6/alpha))`.  Seven gates pass.

The unresolved hardware data are local fault-tolerant controlled ribbons and
sector-uniform maximally mixed internal preparation.  Excitement `10/10`;
finite confidence `10/10`; apparatus confidence `7/10`; realized information
gain `10/10`.
