# Exact frame-coboundary audit

Sources:

- `research/grothendieck/prime-reciprocal-noncommutativity-is-an-exact-frame-coboundary.md`
- `research/grothendieck/primitive-and-square-euler-jet-currents-have-flat-triangle-transport.md`

## Formal theorem boundary

`MariciFormal/ExactFrameCoboundary.lean` works with an arbitrary label type
and group. A one-label frame potential `R` induces
`endpointTransition R a b = (R a)⁻¹ * R b`. Lean proves identity transitions,
the exact cocycle law, and trivial triangle holonomy. It also proves that a
local operator obtained by conjugating one universal operator normalizes back
to that operator in its local frame.

The hostile triangle uses `ℚˣ`: edge values `-1,1,1` have nonidentity product
and therefore cannot all be generated by endpoint frames. Pairwise group
elements alone do not supply an endpoint potential.

This group theorem is shared with the authority/descent transport vocabulary.
It formalizes the exact-coboundary implication, not the analytic claim that
Grothendieck's displayed prime matrices have the stated exponential entries.
It also does not construct a source-derived nonfactorizable pair-incidence
law; the source packet says that object remains missing.

## Verification

Run from `research/buzzard/marici_formal`:

```powershell
lake env lean MariciFormal/ExactFrameCoboundary.lean
lake build MariciFormal
```

No Git command or Marici site build is part of this audit.

Both commands were executed successfully. The targeted file exited zero with
no output, and the project build completed with 8795 jobs. No placeholder or
active-conjecture conclusion is present.
