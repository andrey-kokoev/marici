# Correction: the unitary Weil normalization halves the Carleman boundary budget

The exact half-line identity is written for the principal multiplier
`log|u|` with coefficient one. Its cosine--sine boundary defect is the
Carleman operator of norm `pi`.

The compact-window matrix used in the two-prime certificate has unitary
archimedean multiplier

\[
\frac{\Re\psi(1/4+iu/2)-\log\pi}{2}.
\]

Its principal logarithmic coefficient is therefore `1/2`. Linearity of the
boundary identity gives the normalized boundary norm

\[
\boxed{C_{\partial}=\pi/2.}
\]

The previously registered threshold `1,362,986` used `pi` together with the
half-normalized gamma multiplier and is overestimated by a normalization
mismatch.

Using the proved coarse digamma comparison gives

\[
C_{\Gamma}=\frac{4+\tfrac12\log20+\log\pi}{2},
\quad C_{\partial}=\frac\pi2,
\quad C_{\rm prime}=\frac{\log2}{\sqrt2}+\frac{\log3}{\sqrt3}.
\]

A publication threshold must still add the interval localization commutator
`C_loc(.55)`. The half-line normalization is now fixed; the remaining
constant is the directed localization budget, not another factor-of-two
choice.
