# Construct supported rank-26 comparison

## DPC conjecture

The exact rank-26 row-module artifacts and exceptional-triangle localization determine a canonical supported chain map.

Rivals are that row modules lack geometric generator semantics, that the exceptional complex is an independent three-edge model, or that absorption supplies only an abstract zero quotient map.

Risky consequences are shared source and target bases, typed differentials, generator images, support localization, and a checked zero chain residual.

## Falsification

The rank-26 aggregate exposes counts, seed families, and absorption classes. The exceptional checker exposes a `3x3` incidence boundary. They share no basis identifiers, differential contract, support map, or generator images. A deliberately proposed zero map is rejected because no source-derived comparison says that it represents the principal-wall first jet.

The conjecture is rejected. Dimension counts and zero quotient classes cannot define the missing map.

## Claim boundary

Relation absorption, exceptional localization, and the geometric horn remain separate typed results. Restoring a comparison requires either geometrizing every rank-26 source generator or admitting a separately sourced bridge with a complete chain contract.

## Disposition

The next leaf specifies the minimal source-geometrization contract and tests whether existing seed data can populate any of it.

## Verification

- `research/voevodsky/check_cosmology_construct_supported_rank26_comparison.py` — exit 0
- `research/voevodsky/results/cosmology_construct_supported_rank26_comparison.json` — all seven interface fields absent; deliberate unsourced zero map rejected
