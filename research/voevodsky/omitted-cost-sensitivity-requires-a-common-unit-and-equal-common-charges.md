# Omitted-cost sensitivity requires a common unit and equal common charges

Independent `verify_bundle_cost_sensitivity.py` recomputes the 180-byte receipt gap from the separately recorded finite provider menu; exact rational controls confirm strict preference below 180, tie at 180 and reversal above. The formula `cheap iff u_C-u_B<180` presumes both omitted provider charges are in explicitly normalized receipt-byte-equivalent units AND any omitted charge on shared restricted provider A is identical in the two scenarios. A different charge on A can reverse the ranking even at `u_C=u_B=0`; the checker tests this hostile. Trying to add CPU seconds to receipt bytes without a conversion is rejected as an incommensurate account.

Fresh `python research/voevodsky/checkers/verify_bundle_cost_sensitivity.py` passed. A stale C handle is an eligibility loss, not an infinite price: no finite scalarization restores its old full-domain service; this live fact is exercised by the separate session producer and cannot be reconstructed from the verifier's packet. No measured heap, owner-time, or archival-risk conversion is available; the correct result is a conditional sensitivity region, not a complete-cost ranking.

With the finite resource and typed operation branches now scoped, the remaining objective can be assessed honestly as two targets: a locally integrated observation/retention/continuation calculus on one owner migration, which has executable evidence, and analytic five-role identification, which lacks a source-derived role-to-polyhedral graded functor. The latter is an explicit typed obstruction/reopening gate, not a license to report the entire architecture proved or to halt local work.
