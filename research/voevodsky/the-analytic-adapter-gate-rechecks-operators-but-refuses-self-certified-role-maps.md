# Analytic adapter gate checks historical operators but refuses self-certified role maps

A new finite gate `check_analytic_adapter_verifier_contract.py` independently imports the already established historical four-edge fixture, computes exact ranks and compares every candidate matrix against the trusted local operator source. It rejects a stale fixture digest, changed matrix entry, omitted role/base map, and a forged self-certified role/base assignment even when all four historical operators are correct. Fresh command `uv run --with sympy python research/voevodsky/checkers/check_analytic_adapter_verifier_contract.py` passed. Result: `results/analytic-adapter-verifier-contract.json`.

This is an intentionally NON-promoting verifier boundary. Importing four matrices and reproducing their alternating defects does not construct a functor from S,A,C,G roles into constraint/provider presentations, let alone the residue R vertex, corrected base b_z, ten interval cofibers, reciprocal mates or redundant-row quotient. A purported owner signature or source derivation inside an untrusted candidate field cannot authorize itself. There is no positive acceptance path for analytic identification in the current finite gate.

The missing authority is scientific rather than a file permission: an authoritative owner-supplied role-to-closed-operator/base assignment and independently checkable maps to the Farkas/provider target. The exact requested packet can refer to the already verified four-edge fixture, but must specify which operator realizes which role edge, how its closed domain and nonzero graded interval defects transport, and how B matches b_z under redundant-row comparisons. On receipt, build a separate checker for that exact supplied construction; until then dispose this adapter gate as an obstruction/handoff if no local candidate exists. Do not change Nima's files or infer the missing map from matching simplex dimensions.
