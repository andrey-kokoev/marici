# Conditional global carrier theorem

## Theorem

Suppose the characteristic-zero carrier is smooth near a regular codimension-three center `C`, carries a common line `L`, an ordered isomorphism

`L plus L plus L -> I_C/I_C^2`,

labeled wall lifts, and the declared DNC identification. Then its blowup DNC carries a base-change-natural integral star `Gamma` satisfying

`d Phi(Gamma)=(Xi_rel,-sigma123)`.

Its tame tuple is `(v^-1,u,-v/u,1)`, and all radial and codimension-two residuals vanish. This is a geometric `HomotopyLift` in the relative comparison cone.

The construction is a strict source enlargement. The complete rank-26 p-normal relation source has zero `Xi` coefficient, while `Gamma` has coefficient one. Therefore no chain map preserving the `Xi` projection can make `Gamma` the image of a rank-26 degree-one element.

The local `A3` slice satisfies the hypotheses. No global carrier classifying tuple is materialized, so the intended global instance remains conditional.

## Disposition

Geometric existence follows from the six-field classifying map; rank-26 membership is excluded by absorption. The next leaf defines the minimal extension adjoining the geometric `HomotopyLift` while preserving the absorbed relation subcomplex.

## Verification

- `research/voevodsky/check_cosmology_conditional_global_carrier_theorem.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
