# Evaluating the relative C34 feature on the Xi pair state is ill-typed without a source-retaining tail-to-idelic span

The comparison written in the previous iteration,

\[
q_{\mathfrak F_{34}}(g_z,g_z)=P(z),
\qquad g_z=\Phi\otimes u_z,
\]

is not yet typed.  The relative `C34` feature acts on a Schwartz--Bruhat/idelic
observer source, while `g_z` currently belongs to the ordered tail/pair
carrier.  No theorem identifies those carriers or embeds the latter into the
former with every retained label.

A direct map from tail boundary records to the idelic trace would require an
unproved inverse and could erase source information.  The valid interface must
retain the common source.  Let

\[
\tau:\mathcal S(\mathbb A)\to\operatorname{Ran}(\tau)
\]

be the injective idelic trace and let

\[
\kappa_z:\mathcal S(\mathbb A)\to\mathcal S_z^{\rm tail}
\]

be a source-derived reciprocal tail constructor.  The required object is the
graph

\[
\Gamma_z=
\{(\phi,\kappa_z\phi):\phi\in\mathcal S(\mathbb A)\},
\]

which yields the span

\[
\mathcal B_{\rm tail}
\longleftarrow\Gamma_z
\longrightarrow\operatorname{Ran}(\tau).
\]

The idelic Fourier core is already defined on the right leg by

\[
J_0(\tau\phi)=\tau(\mathcal F\phi).
\]

What is missing is a `phi_z` whose tail leg is the corrected Xi pair response
and whose idelic leg supplies the `C34` observer, with primitive, square, seam,
endpoint, connected, and archimedean labels preserved.

Therefore the external bulk equality must be stated on `Gamma_z`, not by
inserting `Phi tensor u_z` directly into `q_F34`.  The current first missing
edge is the source-retaining tail-to-idelic span (or an equivalent explicit
`kappa_z`), followed by the equality of its two transported observations.
