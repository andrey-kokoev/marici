# Support-four raw probe: DPC gate

## Conjecture

The chosen A14 target can be separated from the grade-eight relation span by a coefficient functional supported on its target column plus three companion columns.

## Exact reduction

The target column occurs in six relation rows. Restrict every other column to the complementary relation rows. A support-four probe exists only if three complementary restrictions are linearly dependent. For each dependent triple, its one-dimensional cancellation combination must then reproduce the negative target-column vector on the six retained rows.

The preceding support-three search found 5,840 distinct projective complementary restrictions. Therefore no two are dependent, but this does not decide whether a dependent triple exists. An exhaustive support-four test requires examining the spans generated by 17,049,880 column pairs, using a modular circuit sieve followed by exact rational reconstruction for candidates.

## DPC disposition

Support four is not established and not falsified. The exact lower bound remains four. Pairwise projective distinctness is evidence only against support at most three; treating it as a support-four obstruction would be invalid.

Surviving alternatives are:

- an exact three-column circuit yielding minimum support four;
- no such circuit, raising the minimum to five;
- a larger or structured extraction probe.

## Next discriminating test

Construct a multi-prime modular index of two-column spans on the complementary rows, detect third columns lying in those spans, then reconstruct and verify every candidate over the rationals on all relation rows. A modular hit is only a candidate; exact full-row reconstruction is the admission gate.

## Evidence

- `research/voevodsky/results/cosmology_three_coordinate_raw_probe_DPC.json`
