# An actual noninvertible observer square preserves fibers and history-relative retention

## Result

The source-anchored tower prototype now has a concrete noninvertible comparison attached to actual reading contracts:

    seven-reading reconstruction -> two separately retained vacuum readings.

On the complete declared ideal slice this map has a five-dimensional kernel, with inherited filtration dimensions (5,2,0). Its coordinate comparisons commute, its full affine source fibers agree across realizations, and five explicitly retained residual coordinates reconstruct the input.

There is a second, history-relative result. Under Nima's prescribed history of appending P-Q twice, the reachable final source has dimension two. The discarded kernel intersects that reachable subspace trivially. The same two vacuum readings then reconstruct the entire compatible history.

Thus required retention depends on the admitted source AND history domain. A kernel on an ambient carrier need not represent independent ambiguity on a constrained reachable history.

## 1. Actual anchors and the square

Use the seven-dimensional forgotten ideal slice at background two and the exact reading contract from `seven-labelled-readings-reconstruct-the-entire-forgotten-ideal-slice.md`.

Let U be this source slice, Y its seven-reading realization, and V the pair

    (u,v)=(canonical vacuum, reversed vacuum).

The source anchor E:U->Y is rebuilt from the actual ordered-cut recorder and is invertible. The coarse anchor is W=P E, where P selects the first two physical readings.

For each of the four realization matrices from `../nima/a-source-anchored-finite-tower-of-observer-towers.md`, restrict to the ideal condition m_empty=0 and form its source anchor E_i. The actual noninvertible comparison is

    Q_i=W E_i^(-1).

For the frame comparison F_ji=E_j E_i^(-1), the checked square is

    realization i --F_ji--> realization j
          | Q_i                  | Q_j
          v                      v
          V --------id---------> V.

It commutes because Q_j F_ji=Q_i. The projection is NOT assumed to be the same coordinate deletion in every frame. What remains fixed is the actual physical reading contract on the source.

Sixteen full-depth squares and 64 composition triangles pass.

## 2. The lost information and its inherited flag

The rank of W is two and dim U=7. Therefore K=ker W has dimension five.

The inherited dimensions are:

| Ideal level | Rich carrier | Coarse carrier | Kernel |
|---|---:|---:|---:|
| I | 7 | 2 | 5 |
| I^2 | 4 | 2 | 2 |
| I^3 | 1 | 1 | 0 |

The checker supplies actual source generators adapted to this flag. Writing e_T for the P/H interaction mode, with H=Q-P, they are

    e_T - (-1)^|T| e_{12},
    T={1},{2},{3},{1,3},{2,3}.

The first three have ideal order one; the last two have order two. Every generator has both vacuum readings zero. Exact Fox evaluations establish their orders.

This lower-dimensional observer retains the entire pure top interaction line while losing lower-order information. Carrier dimension alone does not determine observation depth.

## 3. Compatible jet truncation requires a new coarse tower

The rich ideal-jet dimensions at orders 0,1,2,3 are

    0,3,6,7.

The actual coarse row space must be intersected with the available jet row space at each order. The resulting coarse dimensions are

    0,0,1,2.

At order two only u+v is available. Individually u and v depend on the hidden third-order interaction. At orders zero and one no nonzero combination of these two rows descends.

Thus the coarse order-three to order-two transition is

    (u,v) -> u+v,

not retention of the same two values at every depth.

The checker verifies the row-space intersections, 64 frame squares across the four depths, and 40 depth/comparison squares. This connects the noninvertible quotient to both indices of the tower rather than attaching an unrelated quotient at each level.

## 4. Residual retention without a source archive

In the public seven-reading order y0,...,y6, retain u=y0, v=y1 and

    r=(y2, y3-u-v, y4, y5+u+v, y6).

Then reconstruct

    y=(u, v, r0, r1+u+v, r2, r3-u-v, r4).

The existing standalone decoder reconstructs the source from this recovered y. No original source or original seven-reading array is hidden in the envelope.

This uses an explicitly declared filtered LINEAR section

    (u,v) -> (u+v)e_{12}+v e_{123}.

It is a choice on this finite slice, not a source-bimodule section of the globally nonsplit acquisition extension. Its identity is retained in the wire contract. Losing a residual coordinate or changing the section without tracking it is rejected.

For an unknown source, the correct coarse state is instead the entire affine fiber W^(-1)(u,v). Selecting the section's point does not establish that the unknown residual is zero. Sixteen comparisons of complete rational affine fibers verify that changing frames does not change this ambiguity.

## 5. The history note changes the effective retention requirement

Now use the actual prescribed history in `../nima/observer-coherence-is-natural-along-a-source-derived-history.md`. Its convention is H=P-Q; this is the negative of the interaction-basis convention above. Both prescribed future factors use that convention, so the signs cancel in the final reversed reading.

Start with

    x0=a_P P+a_Q Q,

and append H in block two and then H in block three. The stage carriers have ambient dimensions 2,4,7, but the full compatible final image remains two-dimensional.

The two final vacuum readings are exactly

    (u,v)=(a_P,a_Q).

The checker verifies this from actual source multiplication and the physical row anchors:

    W T20 = identity_2.

Consequently

    K intersect image(T20)=0.

On this admitted reachable source domain, the coarse receiver is lossless. The five residual coordinates used for the ambient carrier are no longer free parameters: they equal

    (0,-u-v,0,u+v,0).

Those values follow from the prescribed dynamics. They must not be substituted for arbitrary final ideal sources.

This history also selects its own compatible linear section, namely the source produced from the initial pair (u,v). It differs from the illustrative section above by a kernel-valued map. Knowing the dynamics removes the freedom to choose an arbitrary point in the ambient affine fiber.

## 6. Evidence refines the history without replacing earlier facts

The induced order-two coarse value u+v is exactly the terminal coefficient a_P+a_Q of the initial source. This explains how the new coarse depth tower fits Nima's derivative-order shift.

With the initial terminal fact a_P+a_Q=1, the compatible 13-coordinate history fiber has dimension one. A later canonical vacuum reading u=1 reduces it to the unique history starting at P. The checker retains every transition equation and the old terminal fact.

After that refinement, a contradictory repeat u=0 gives an empty fiber. It does not overwrite the previous reading. All 16 tested observer/history routes give the same two-vacuum prediction map on the initial source.

These are exact synthetic histories, not a claim that the source dynamics or measurements have been established physically.

## 7. Negative controls and boundaries

An invertible rich-coordinate change y0'=y0+y2 does not descend to the old two-vacuum view. A hidden vector with y2=1 and y0=y1=0 would acquire a nonzero first visible value. No comparison defined only on the old visible pair can produce it.

Corrupting a coarse comparison also breaks the source-anchor square even though every carrier dimension is unchanged.

The test rejects both failures, as well as missing residuals and untracked section changes.

This comparison is between the new seven-reading reconstruction and two actual vacuum rows on the stated slice. It is NOT the outstanding full raw-449 versus scalar-plus-row-76 observer comparison: those old degree-two detectors vanish on this entirely forgotten full-corner slice. Nor does any local linear section here split the globally nonsplit source-bimodule acquisition extension.

## Verification

    python research/voevodsky/checkers/check_source_anchored_noninvertible_square.py

Artifacts:

- `results/source-anchored-noninvertible-square.json`
- `results/vacuum-pair-with-residual-example.json`

All checks pass. The report exports anchors, frame comparisons, noninvertible projections, filtration-adapted kernel sources, affine fibers, residual extraction/reconstruction matrices and the history transitions.
