# Three-observer coherence retyped as a stratified two-leg residue prism

## Question

What are the three observers, what does the internal \(3+2+1\) count, and what must be preserved under one-grade extension?

## Claim boundary

This packet reconciles the operator's proposed observer count with prior source architecture. It identifies the typed diagram and its extension obligations. It does not prove the analytic completion maps, physical acquisition, or any RH conclusion.

## Correction: observer multiplicity is not grade multiplicity

Prior research identifies the three observer channels as the function-valued state channels

\[
P,\qquad Q,\qquad M,
\]

where \(P\) and \(Q\) are reciprocal tail instruments and \(M\) is the shared seam instrument. Reciprocal exchange swaps \(P,Q\) and fixes \(M\).

These are not the arithmetic regularity grades. Each complete observer presentation must separately retain the three completion strata

\[
B^{(1)}\oplus B^{(2)}\oplus B^{(3)},
\]

corresponding to primitive-distributional, square-Hilbert, and connected trace-class data. The connected stratum retains its individual prime-power grades before any authorized aggregation.

Thus there are two independent threes:

1. three observer channels \((P,Q,M)\);
2. three completion modalities inside each channel.

Conflating them would turn a function-valued instrument into three scalar grade readings.

## The sourced \(3(3+2+1)+2+1\) architecture

Prior work gives the useful categorical reading

\[
3(3+2+1)+2+1.
\]

The outer three are complete presentations

\[
\mathcal S_+,
\qquad
\mathcal S_-,
\qquad
\mathcal M.
\]

Inside each presentation, \(3+2+1\) records:

- three retained strata or vertices;
- two composable adjacent comparison legs;
- one coherence comparison between the composite route and a sourced direct route, when that direct route exists.

The additional two are the incidence functors

\[
L_+:\mathcal S_+\to\mathcal M,
\qquad
L_-:\mathcal S_-\to\mathcal M.
\]

The final one is the residue constructor

\[
h:\mathcal R\to\mathcal M,
\]

whose required boundary property is

\[
\operatorname{im}h=\ker\rho
\]

for the declared scalar readout \(\rho:\mathcal M\to\mathcal O\). The controlled equation is therefore

\[
L_+(x_+)-L_-(x_-)-h(r)=0,
\]

not the unsupported strict equality \(L_+(x_+)=L_-(x_-)\).

## Extension from grade cutoff \(k\) to \(k+1\)

Let \(i_k\) denote the prefix inclusion of the retained grade carrier. The correct extension requirement is not a second copy of the observer ontology. It is naturality of the entire typed diagram:

\[
i_k^{\mathcal M}L_{\pm,k}
=
L_{\pm,k+1}i_k^{\mathcal S_\pm},
\]

and

\[
i_k^{\mathcal M}h_k
=
h_{k+1}i_k^{\mathcal R}.
\]

Each internal stratum comparison has its own analogous square. Their triangle cells must be compatible with these vertical squares. This produces a prism coherence condition only where direct and composite comparison routes are both source-defined.

The finite shift-defect calculations sharpen the boundary behavior. Extension adds a unary observation residue on the new cell while activating ternary coherence on the former terminal cell. The signed pair is the interval boundary

\[
d(e_k)=P_{k+1}-P_k.
\]

This gives an exact local model for the vertical face of the prism. It does not construct the horizontal sector maps \(L_+,L_-\) or residue classifier \(h\).

## Function-valued and lineage requirements

The observer channels cannot be replaced by their current scalar values. The scalar and seam rows have kernel containing the reciprocal-odd direction

\[
(P,Q,M)=(1,-1,0).
\]

No fixed finite moment list is faithful on the completed odd channel without a source recurrence. Each observer must therefore retain a record-labelled update

\[
\mathcal I_\alpha:x\longmapsto(r_\alpha,x'_\alpha),
\]

and terminal sewing must compare both record and update lineage. In the present non-temporal vocabulary, lineage means constructor provenance under composition and extension, not history in physical time.

## What is already available

Prior packets supply:

- the three function-valued channel types \(P,Q,M\);
- reciprocal exchange typing;
- the three incompatible completion strata;
- the abstract two-leg residue architecture;
- a finite transport-triangle hostile showing that scalar characters can miss holonomy;
- local shift-defect extension naturality and its signed residue boundary.

## First unresolved typed comparison

The current material does not yet show that the three internal \(3+2+1\) triangles, the two sector-to-mixed legs, and the residue constructor form one source-derived prism. In particular, a scalar zero only proves

\[
\rho\bigl(L_+(x_+)-L_-(x_-)\bigr)=0;
\]

it does not supply a residue state \(r\).

The first missing datum is the source-derived factorization

\[
L_+(x_+)-L_-(x_-)=h(r)
\]

on the full function-valued, three-stratum carrier, followed by its naturality under \(k\to k+1\). A finite scalar or grade-counting check cannot manufacture this map.

## Disposition

The proposed coherence architecture is supported after retyping:

> Three function-valued observers each retain a \(3+2+1\) stratified coherence packet; two source legs compare the reciprocal presentations in the mixed seam carrier; one residue constructor classifies their unresolved difference; and the whole diagram must be natural under grade extension.

The local interval-boundary calculation supplies the vertical extension template. The global prism and residue classifier remain unconstructed.
