# Historical four-edge fixture exposes alternating relative defects

The local executable `check_degree_four_reciprocal_waldhausen_realization.py` DOES provide explicit historical edge operators; the earlier bounded search for an *edge-to-polyhedral role assignment* must not be read as absence of the historical analytic edges themselves. Four Fplus matrices run through dimensions `2->3->2->3->2`; their exact ranks are all 2. Hence their `(cokernel dimension, kernel dimension)` pairs alternate `(1,0),(0,1),(1,0),(0,1)`. They are bounded operators on entire finite-dimensional closed graph domains. Reciprocal mates Fminus and the ten interval cones, ten triple homotopies and five strict fourfold parenthesizations are checked in that fixture. Fresh `uv run --with sympy python research/voevodsky/checkers/check_degree_four_reciprocal_waldhausen_realization.py` passed, and a separate import computed the four exact rank tuples above.

These explicit classes provide the FIRST concrete noncollapse invariant for an attempted analytic-to-polyhedral edge assignment: the image of edge 0 must preserve a one-dimensional cokernel defect, edge 1 a one-dimensional kernel defect, and reciprocal transport must respect the alternating sequence. A cone of a face inclusion between contractible simplices has zero relative homology and fails already at edge 0. An affine Farkas slack is a rational inequality residual, not a chain complex with this graded homology unless an additional source-derived realization creates one. Numerically matching `1` is no substitute for a typed homology map.

The owner handoff can now be sharpened: Nima need not supply an edge matrix—we have one. The missing input is an authoritative assignment of the analytic S/A/C/G (and ultimately R) role objects, their closed edge operators and retained corrected base to specified constraint/provider objects and maps, with a proof/counterexample of preservation of these alternating kernel/cokernel defects and reciprocal mates. No such assignment is manufactured here. This active branch continues by testing any proposed map, not by waiting for a new matrix. The audit's role-map obstruction remains, but its language about the first unassigned edge should be read as UNASSIGNED TO THE POLYHEDRAL TARGET, not nonexistent analytic source operator.
