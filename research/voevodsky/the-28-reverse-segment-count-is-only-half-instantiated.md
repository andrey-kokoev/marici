# The 28 reverse-segment count is only half instantiated

## Counting convention

The proposed denominator 28 means seven fine segments in each of four principal directions. A reverse ledger is meaningful only after every forward direction has eight typed nodes and seven standalone arrows.

## Inventory

### Source-to-spectral direction

The packet `the-source-to-spectral-edge-has-an-eight-node-factorization-from-convolution-polarization-to-the-dual-canonical-connection.md` explicitly supplies all eight nodes and seven arrows. Status: **7/7 forward segments enumerated**.

### Geometry-to-trace direction

The packet `the-semilocal-geometry-to-trace-edge-has-an-eight-node-factorization-following-connes-theorem-4.md` explicitly supplies all eight nodes and seven arrows. Status: **7/7 forward segments enumerated**.

### Source-to-geometry direction

`c13-segmentwise-c12-triangulation-audit.md` supplies seven source-supported transfers adjacent to the source-to-spectral segments. These are face comparisons, not an independent eight-node factorization of the source-to-geometry edge. Status: **0/7 standalone edge segments enumerated; 7/7 adjacent face transfers available**.

### Spectral-to-trace direction

`c34-positive-semilocal-filler-status-ledger.md` supplies a relative-positive feature construction and its regulator qualifications. It explicitly warns against interpreting feature-slot depth as an edgewise simplex count. Status: **0/7 canonical standalone edge segments enumerated**.

Therefore the current strict forward-edge inventory is

\[
14/28
\]

standalone fine segments. The other fourteen positions are not yet legitimate rows of a reverse-arrow ledger.

## Common-graph consequence

The anchored common-graph construction gives an algebraic opposite for every fine node map once that map is admitted as a coordinate of the common graph. It does not manufacture missing forward node factorizations. Nor has the existing joint-closability theorem explicitly listed every Mellin/Euler, orbit-kernel, regulator-family, and asymptotic-error coordinate appearing in the two enumerated paths.

Thus the correct reverse status is:

- **14 candidate opposites** generated formally by anchored graph-chart exchange, conditional on adding all enumerated coordinates to the joint graph;
- **14 positions undefined**, because their forward standalone segmentations have not been constructed;
- **0 opposites independently verified against pre-existing unanchored target topologies**.

## Next construction

Extend the common graph coordinate family by the sixteen node readouts on the two explicit paths, prove joint closability/continuity on one source-derived core, and separately decide whether the source-to-geometry and spectral-to-trace directions require their own seven-node factorizations or are only face-transfer directions.

## Disposition

The earlier phrase “all 28 opposite fine arrows” was premature. The common graph solves reversal uniformly for admitted coordinates, but only 14 of the proposed 28 forward segments currently have standalone eight-node source formulas.