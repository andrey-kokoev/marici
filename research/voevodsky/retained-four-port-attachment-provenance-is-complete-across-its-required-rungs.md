# Retained four-port attachment provenance is complete across its required rungs

## Question

After construction of the retained common graph domain, does a current-by-current transpose-range gate remain open internally?

## Claim boundary

No for the named retained attachments, once each attachment is typed on its actual rung rather than forced into one Hilbert transpose range. The remaining problems are the global determinant-line comparison and identification with an external G4 interface.

## Boundary traces

The four traces

$$
(P,Q,M_a,J_a)
=
(f(-\infty),f(+\infty),f(a),f'(a))
$$

are jointly continuous on the weighted second-order relative graph \(\mathcal H_{{\rm rel},a}^2\). The Volterra source reaches this graph continuously, and reflection and seam transport preserve it.

## Primitive row

The primitive coefficient row \((\log p)p^{-1/2}\) is not in the fixed Hilbert Green transpose range. It is, however, the pullback of the centered first-moment functional on the second-position graph:

$$
L_{\rm prim}(c_a)=a,
\qquad
(K'L_{\rm prim})_p=(\log p)p^{-1/2}.
$$

Thus its provenance is distributional and continuous on the correct graph dual.

## Square rows

Every retained square convention

$$
P(\log p)p^{-1}
$$

for fixed polynomial \(P\) is the pullback of a finite combination of exponentially twisted moments. Hence both the constant and logarithmic square conventions have explicit transpose provenance on their half-density position graphs.

## Connected and archimedean attachments

The connected tail is an operator-ideal coordinate, not a transpose-range current. Its correct provenance question is ideal continuity. The archimedean attachment is an explicit Mellin-line functional satisfying

$$
\mathcal M_sR_a=a^{-s}\mathcal M_s,
$$

not a prime-labelled interpolation current. Its local provenance and covariance are complete.

## Consequence

Together with joint closability of the finite retained coordinate family, these results close internal attachment typing on the multi-rung common domain \(D_{\rm ret}\). No single Hilbert Riesz representation is required or authorized.

The unresolved comparison is global rather than local:

1. join the Mellin line, primitive and square counterterms, and connected \(\det_3\) coordinate into the declared relative determinant line;
2. identify the external G4 Fourier--Poisson sewing with retained-chain sewing.

## Disposition

All named retained four-port attachments have source-side analytic provenance on their correct graph, ideal, or Mellin rungs. The earlier generic “current-by-current transpose-range” item is no longer an internal open gate. Global determinant comparison and external G4 identification remain open.