# Carrier classifying map

## Question

What is the minimal datum by which the intended carrier inherits the universal integral horn?

## Claim boundary

It consists of six fields:

1. a geometric carrier `X`;
2. a codimension-three regular center `C`;
3. a common line bundle `L` on `C`;
4. an ordered isomorphism `L plus L plus L -> I_C/I_C^2`;
5. labeled wall equations lifting that basis and satisfying the additive wall relation;
6. identification of the carrier DNC with the corresponding blowup/normal model.

Any such datum pulls back the universal `P2`-bundle, star `Gamma`, ratios `u,v`, Milnor symbol, tame tuple, and equation

`d Phi(Gamma)=(Xi_rel,-sigma123)`.

The local `A3` slice supplies all six fields with `L=O` and determinant-one matrix. No complete global tuple is materialized.

A map from the rank-26 relation presentation to this geometric DNC is unnecessary for geometric existence, but necessary to claim membership in rather than enlargement of the rank-26 source.

## Disposition

The carrier problem is reduced to an explicit classifying map. The next leaf freezes the conditional global theorem and separates geometric existence from rank-26-source membership.

## Verification

- `research/voevodsky/check_cosmology_carrier_classifying_map.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
