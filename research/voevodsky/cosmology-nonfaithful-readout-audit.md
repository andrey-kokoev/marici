# Nonfaithful readout audit

## Question

Can a correspondence that kills the primitive triangle class nevertheless produce the required independently sourced readout?

## Claim boundary

No readout of that class can factor through the face-filled complex. The comparison from the old triangle to the filled simplex sends `sigma123` to zero in homology. Every subsequent homological, regulator-compatible, or state-effect record map therefore also sends it to zero.

The old faithful edge coordinate detects `sigma123`, whereas every coordinate factoring through the filled quotient returns zero. The correspondence loses exactly the capability that a readout of `(Xi_log,-sigma123)` must preserve.

No source, admitted effect, positive pairing, or physical record map has been supplied that bypasses this factorization. Assigning a number to a chain representative without homological invariance would not define a readout of the cohomology class and would not construct a horn boundary.

## Disposition

Nonfaithful higher-dimensional correspondences may carry unrelated observables, but they cannot produce a nonzero readout of `Xi_log` or `sigma123` without a new independently sourced map that avoids the filled quotient. The next leaf freezes the characteristic-zero horn frontier, separating the proved obstruction from eliminated and still-open constructor classes.

## Verification

- `research/voevodsky/check_cosmology_nonfaithful_readout_audit.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
