# Two residual gaps detect the adjacent cubic attachment

## Result and relation to the existing two-stage theorem

An actual six-event product, with TWO retained features and THREE seam factors, detects a nonzero adjacent connecting class for

    0 -> I^3/I^4 -> I^2/I^4 -> I^2/I^3 -> 0.

Its four-sector observer evaluates to a strictly positive product of two actual window-moment gaps. The real arithmetic multiplier cancels. The balanced three-seam cycle is independent of the two binary parenthesizations, and its old two-seam observation vanishes.

The completed roofs and their two-stage compatibility were already constructed in `the-first-two-completed-filtered-attachments-form-a-compatible-comparison.md`. The new result is their explicit RESIDUAL-sensitive, two-feature adjacent witness and its nonzero transport, not a claim that the cubic extension machinery was previously absent.

Other inputs:
- `../nima/the-residual-port-restores-the-labelled-two-seam-comparison.md`
- `../nima/an-explicit-residual-observer-detects-the-nonzero-attachment-class.md`
- `../nima/theta-tail-dominance-controls-actual-letter-factorization-lifts.md`
- `../nima/convex-source-heights-preserve-endpoint-budgets-through-attachment-operations.md`

## 1. A genuine six-event source product

Start at the arithmetic vertex 2. Take

- a: the singly retained diamond on primes 2,3, ending at 12;
- b: the singly retained diamond on primes 5,7, ending at 420;
- c: the forgotten diamond on primes 11,13, ending at 60060.

Put v=abc. The source expansion contains 32 distinct marked paths, all with coefficient magnitude one. It belongs to I^3. In this six-event finite packet I^4=0.

Let D3 be the EXISTING ordered three-seam derivative. On this product,

    j3(v)=D3(abc)=D(a) tensor_balanced D(b) tensor_balanced D(c).

The finite checker constructs D3 independently from its event-position formula and verifies this equality before any pairing. It also verifies that the balanced boundary is zero.

Select the first forgotten seam 420->4620 in the third diamond. In the first retained diamond select in turn 2->4 and 4->12, with window forcings

    A_1=1_[log 2,log 4] Phi,
    A_2=1_[log 4,log 12] Phi.

In the second retained diamond select in turn 12->60 and 60->420, with

    B_1=1_[log 12,log 60] Phi,
    B_2=1_[log 60,log 420] Phi.

These choices give FOUR distinct balanced sectors. Each has coefficient +1, both retained features in their respective seams, and vacuum buffers and third seam. The feature labels are (Psi(A_i),Psi(B_j)) in their original order.

## 2. The prescribed one-slot residual test

Fix z_0=i y in an admitted compact Euler interior, y>gamma>1/2, and use

    e_0(u)=exp(-y u),  L_0=L(1/2+y),
    o_0=(e_0,-2L_0 e_0).

For any of the four positive windows F, write

    X_F=X_F(i y)>0,
    mu_F=iX_F'(i y)/X_F(i y).

The already proved residual-port pairing gives

    Q_res(Psi(F)(i y),o_0)
      =sqrt(2) X_F(mu_F-L_0)/(2y).

All pairings here are the prescribed UNWEIGHTED half-line pairings. The observer changes neither the arithmetic form nor its signature.

## 3. Four sectors isolate the product of two gaps

Set sign_1=-1 and sign_2=+1. In selected sector (i,j), place

    sign_i sign_j (o_0/X_(A_i)) tensor (o_0/X_(B_j))

and the unit forgotten/vacuum observations. Set every other observer coordinate to zero. Call this observer eta3. These divisions are fixed positive scalar normalizations for this witness only.

Slotwise tensor pairing yields

    Q_joint,3(j3(v),eta3)
      =w_seam^2 (mu_(A_2)-mu_(A_1))(mu_(B_2)-mu_(B_1))/(2y^2).

Both gaps are strictly positive. Each mu_F is the cosh(yx)Phi(x)-weighted mean of h_y(x)=x tanh(yx). This function is strictly increasing for x>0. Each pair consists of successive positive windows, so its means lie on opposite sides of the common boundary value.

The resulting observation is therefore strictly positive, regardless of the value or sign of L_0. The arithmetic multiplier cancels separately in the two differences. The observer detects two actual residual differences, rather than an arithmetic-only even-port replacement.

No calibrated numerical margin is claimed for these much later windows. Their amplitudes are positive but can be extremely small. The previously calibrated four-event constants are not reused at this new witness.

## 4. Continuity and the two parenthesizations

At the fixed spectral point,

    ||o_0|| <= C_o=[sqrt(1+y^2)+2|L_0|]/sqrt(2(y-gamma)).

The projective norm of the finite observer is controlled by C_o^2 times

    (1/X_(A_1)+1/X_(A_2))(1/X_(B_1)+1/X_(B_2)),

with the original finite shape and physical seam multipliers. Its event length, seam count and feature count are fixed at 6,3,2, so it belongs to every declared endpoint/depth/feature control seminorm. Evaluation at z_0 is bounded on compact-spectral continuous families.

For a balanced record with seams and intervening buffers, binary composition concatenates the touching buffers. Therefore

    (D(a) tensor_balanced D(b)) tensor_balanced D(c)
      =D(a) tensor_balanced (D(b) tensor_balanced D(c))

under the canonical labelled reassociation. The checker compares BOTH successive binary constructions to the independently computed D3 and the existing three-factor normalization. This is word-level balancing with actual marks and endpoints, not merely equality of a scalar product.

The same reassociation holds on projective completions because the letter maps and finite normalization operations are continuous with the supplied radius bounds. Three seams do not entail three retained features: the available two-feature rigged test/dual comparison, with its adjoined unit vacuum, applies here without asserting a new three-feature estimate.

## 5. Nonzero adjacent derived morphism

Use the existing adjacent model

    K_adj=[G3 -> F23],
    G3=J_3/J_4, F23=J_2/J_4,

in degrees -1,0, strictly quasi-isomorphic to G2=J_2/J_3. Let T3=J_(Psi,3)[-2], with the supplied convention placing its bottom cycles in degree -1. The connecting observation is zero in degree zero and D3 in degree -1.

Restrict to the finite six-event path source. Then I^4=0 and the model is [I^3 -> I^2]. Both terms are one-sided projective modules over the finite hereditary path algebra. This is the essential finite projective input; no completed ideal is declared projective.

If the adjacent observation were source-equivariantly nullhomotopic, a homotopy would give H:I^2->T3^(-1) with

    D3(abc)=H(abc)=a H(bc)=0.

Here bc belongs to I^2, while a belongs to I and the target's outer coefficient actions factor through the terminal recorder, so I acts trivially. There is no target degree -2 to provide another homotopy term. The strictly positive eta3 observation contradicts the displayed equality.

Since the finite source model is bounded projective, this rules out zero in its derived category, not just a chain-level homotopy. The admitted finite restriction preserves the strict quasi-isomorphism comparisons. A zero completed adjacent roof would restrict to a zero finite derived map, which is impossible.

Thus the adjacent connecting class is genuinely nonzero after residual substitution.

## 6. Compatibility with the earlier attachment

The existing full cubic model is H=[G3 -> E3], E3=J_1/J_4. The inclusion K_adj->H is the identity on G3. Consequently the full and adjacent cubic connecting observations agree on this witness before pairing.

The older refined model [F23->E3] maps to [G2->E2], E2=J_1/J_3, with kernel [G3 --identity--> G3]. This kernel remains contractible under the residual letter substitution. Also

    D2(I^3)=0,

which the checker verifies on the actual retained-retained-forgotten product. Thus the new cubic observation is not a relabelling of the old two-seam value: the old derivative kills this new layer.

Keep D3 on G3, where its cycle and equivariance properties hold. We do NOT extend it to all of F23 and assert another equivariant bottom component of the old roof; that would be the grade error already excluded by the two-stage theorem.

## 7. Completions, rigged outputs and height cutoffs

The ordered derivative has at most binomial(n,3) terms. Combined with the residual forcing-letter bound, the existing path-radius scale and actual-letter factorial scale absorb its polynomial/event and feature costs. The theta-tail factorization theorem identifies the inherited and native presentation intersections on their declared all-radius domains. The finite strict extension sequence and observation therefore transport to those domains.

If the common-path requirement is imposed as well, retain it separately or work on the stated intersection; actual-letter membership alone does not supply it.

The newly supplied convex source height can contain the entire six-event vertex interval. It preserves total outer corner under cuts, lifts and balancing. Charging height once per total corner retains the same moment exponent. The finite convex projection then commutes with these operations over the projected source. No factorwise product of three height weights is silently treated as a single height weight.

The rigged Tate/residual paired substitution retains the same positive value by its two-feature tensor identity. It keeps the negative-side leakage, endpoint and window-residual labels separate. No ordinary L2 limit or perfect duality is inferred.

## Verification

`uv run --with sympy python research/voevodsky/checkers/check_residual_adjacent_third_attachment.py`

Passes the actual 32-term source product, four selected feature sectors, both binary normalizations, the three-seam cycle, D2 vanishing, fourth-power D3 vanishings, exact residual-gap product, and the adjacent source-equivariant nullhomotopy obstruction.

The supplied convex-height checker also passes. Strict localization and completion follow from the finite projective restriction and the established source-domain theorems, not from finite scalar samples alone.
