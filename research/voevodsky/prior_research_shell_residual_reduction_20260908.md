# Prior-research reduction of the Evans prime-shell residual

Date: 2026-09-08

## Port domains are closed

Prior research proves that every component of the centered Evans adjoint residual is a bounded, arithmetically summable map on the declared whole-line wall graph:

- ordinary history overlap;
- regular derivative overlap;
- wall response;
- reciprocal history;
- ordered Stokes/Wronskian linking polarization.

For Xi zeros on compact parameter sets, each residual sequence belongs to the arithmetic Hilbert space with majorant

\[
\sum_p\frac1{p\log p}<\infty.
\]

Thus the failure is no longer attributable to limiting absorption, trace domains, or undefined infinite sums.

## Derivative and wall totalization

For consecutive primes `p<q`, with `a=log p`, `b=log q`, the regular derivative and wall ports combine exactly into

\[
I_{p,q}^{(1)}(z)+I_{p,q}^{(\mathrm{wall})}(z)
=
\Phi(b)\overline{u_z(b)}-
\Phi(a)\overline{u_z(a)}
\]

in the Hermitian Green lane.  In the analytic-transpose lane the conjugates are removed, giving a holomorphic expression whose parameter derivatives provide multiplicity jets.

This leaves the shell residual

\[
\mathcal S_{p,q}
=I_{p,q}^{(0)}+I_{p,q}^{(\mathrm{end})}
 +I_{p,q}^{(\mathrm{recip})}+I_{p,q}^{(\mathrm{link})}.
\]

## Ordinary term is a localized odd-autocorrelation obstruction

For the shell `[a,b]`, define

\[
\rho_{a,b}(t)=\int_a^b\Phi(x)\Phi(x+t)\,dx.
\]

Then

\[
I_{a,b}^{(0)}(z)
=-\int_0^\infty e^{-zt}\rho_{a,b}(t)\,dt.
\]

The densities add over shell partitions and refine the known global odd-autocorrelation obstruction.  On theta coefficients they are linear only after passing to the ordered-pair arithmetic module

\[
\mathcal C_{\rm ar}\otimes\overline{\mathcal C_{\rm ar}}.
\]

## First missing map

The source-defined polarized synthesis is

\[
\widetilde B_{a,b}
(e_n\otimes\overline{e_m})(t)
=
\int_a^b
\phi_n(x)\overline{\phi_m(x+t)}\,dx.
\]

What remains absent is the labelled reciprocal/linking current

\[
\widetilde B_{a,b}(e_n\otimes\overline{e_m})
\longmapsto
J_{a,b;n,m}
\]

whose shell response cancels the ordinary localized density after the explicit endpoint term is removed.  It must retain ordered labels and both product and ratio logarithmic degrees and must fail on the existing generic positive two-shell hostile.

## Disposition

Prior research closes all port riggings and computes the endpoint and ordinary-density contributions.  The residual is one source-constructor problem: build the labelled polarized correlation current `J`.  No repository formula located in this search supplies it.  Without `J`, numerical evaluation of the full shell residual would require inventing the reciprocal/linking term.

## Evidence

- `research/nima/reflection-trace-and-graph-differentiation-close-the-remaining-evans-residual-port-riggings.md`
- `research/nima/the-regular-derivative-and-wall-shell-ports-totalize-to-one-explicit-endpoint-green-difference.md`
- `research/nima/the-prime-shell-residual-is-the-localized-form-of-the-known-odd-autocorrelation-obstruction.md`
- `research/nima/the-ordered-linking-port-is-continuous-on-the-centered-evans-rigging-but-its-shell-value-remains-open.md`
