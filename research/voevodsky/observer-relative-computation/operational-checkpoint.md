# Internal observer checkpoint: retained information, signed bounds and finite adaptive limits

## Fresh canonical audit

Reproduce with:

`python research/voevodsky/observer-relative-computation/check_operational_checkpoint.py`

The current run passed with57 entry modules:51 observer research modules and6 foundational bridge modules, plus the aggregate root. The checker rejects missing, extra or duplicate root imports and requires every entry to appear in fresh compilation output. It uses --safe --cubical --guardedness --ignore-interfaces --transliterate -Werror.

All200 inventoried source/local-checker files remained unchanged during the138.815-second run. Two independently compiled negative controls were rejected with UnequalTerms:

1. true=false with proof refl;
2. the signed up comparison has integer winding0 with proof refl (its winding is1).

The negative modules are retained as text, not broken live Agda sources. Commands, source hashes, compiler/Python versions, exact entry coverage and positive/negative log hashes are in `results/operational-checkpoint.json`.

The previous24-entry receipt and its associated logs/source control are archived under `results/before-internal-observer-checkpoint/`. `results/foundation-audit.json` remains historical. Earlier transport-gate receipts are not silently relabelled after source changes; this canonical receipt is the current audit evidence. This is proof-checking/source-integrity evidence, not a compiler/OS correctness proof or scientific confirmation of the foundational interpretation.

## Checked research boundaries

### Retained observations

Support, participation, coherent sections and distinguishability remain distinct. Restriction and gluing retain comparison evidence. Set-valued and groupoid-valued image descent have their scoped constructive results; groupoid factors and coherent fibre data have checked full-data roundtrips. Comparison-sensitive readouts can distinguish identical visible postprocessors. The historical older encoder is not separately aligned with the certified encoder.

### Execution and history

Actual machine runs embed into static comparison support, but reverse comparisons need not be admitted reverse runs. Opposite admissibility policies can share a value observer. Mere existence supports faithful recovery exactly when the witness type is a proposition. A two-history fixed-policy fixture retains distinct rule witnesses despite identical results. Transport-compatible general eliminators replaced the formerly warning-producing specialized history proofs; the whole current aggregate now passes -Werror.

### Internal finite interfaces

Observer access, structural restrictions and finite comparison expressions are first-order data, not arbitrary evaluator callbacks. In the two-history access language, admissible images normalize to Unit or History/Bool. Repeating access to one history adds no independent content. Restriction exists exactly at the checked capability boundary and is coherent on admissible set-valued images, not arbitrary raw replies or all higher comparison witnesses.

### Dependent records and comparison information

Distinct comparison paths at a fixed reply do not imply distinct total certificates when the reply may move. Dependent reply/path/value records normalize faithfully relative to an anchor. Reanchoring is coherent and normalization equivariant, but normalization cannot forget the comparison path. Exactly result-only codes descend in the tested finite language.

Parity suffices for Boolean readback but not semantic path recovery. Binary and ternary observations jointly realize six readings and are individually incomparable; another finite cover detects their residual loss. Uniform finite cyclic covers support forward/backward transport. Actual fixed-base loops of this circle-index model are equivalent to integers; signed expression evaluation preserves composition and reconstructs the original semantic paths, not the raw syntax.

### Bounds versus fixed observers

A supplied numerical signed bound constructs an interval certificate and a calibrated finite observer with faithful path recovery. The sum of two winding magnitudes supplies a common bound, hence an input-dependent finite probe separating any unequal pair of loops in this model.

Nevertheless every FIXED finite family of cyclic probes, even with arbitrary retained phases, misses a nontrivial common-period path. The calibrated probe representation is explicitly included; there is no phase loophole.

### Adaptive transcripts

Finite first-order binary equality-test protocols retain probe identities, typed replies and terminal labels. A nontrivial alternative comparison has the identical full zero-loop transcript. The construction needs only periods actually queried on that branch, with a checked transcript-extraction equality. No transcript-only certificate can both accept that zero transcript and certify zero uniquely over all loops without a domain hypothesis.

This is a finite-tree theorem, not a theorem about arbitrary recursive or merely terminating controllers. Bounded sufficiency does not discover its own bound from unbounded cyclic observations.

## RRC / DSC integration frontier

Terminology is documented in `research/voevodsky/rrc-dsc-terminology.md`:

- RRC: Retained Resolution Calculus, whole-package witnessed construction;
- DSC: Dependent Substitution Calculus, dependent composition and transport.

The observer results above are standalone checked models, NOT yet a theorem internalizing all their interfaces inside a mutually faithful RRC/DSC representation. The new modules check one-layer application retention and a recursive signature-relative representation with seed substitution/flattening compatibility; see `rrc-dsc-representation-bridge.md` and `rrc-dsc-recursive-substitution.md`. The latter adds explicit generator-call syntax and retains the full RRC signature: it is not a reduction to bare DSC composition. The source/signature ledger now distinguishes all twelve generator tags, their supplied evidence and derived outputs (`rrc-dsc-source-signature.md`). Rule-policy preservation under substitution is checked, including the impossibility of unchecked flattening under a no-rules policy. A finite Boolean identity/swap fragment now has a total witness-producing decision procedure, with actual RRC derivations and checked impossibility/completeness boundaries (`rrc-dsc-witness-synthesis.md`). This is not general source-dependent synthesis. Finite comparison solutions now transfer with full dependent roundtrips and preserved negative certificates. Distinct compare-rule/identity-rule histories at the same endpoint remain distinct after translation (`rrc-dsc-comparison-coverage.md`). All five upstream Structural.Generated constructors now transfer with exact interpreted-witness preservation via a dependent boundary square, relative to the supplied law interpretation. Reflection now preserves exact soundness witnesses, and source Completeness implies exact-witness completeness on encoded inputs. The source premise itself is not proved; a concrete source-instance obligation remains open. A two-path-code solver now constructs actual dependent transport witnesses in the circle Boolean cover, full total-space RRC comparisons and exact index-path recovery, while rejecting a reflexive true-to-false query (`rrc-dsc-dependent-synthesis.md`). This remains a finite specified language, not arbitrary dependent synthesis. A concrete source instance now refutes automatic Completeness: unit-valued semantics identify two same-endpoint histories that all admitted tag-preserving laws keep apart. The obstruction survives reflection through the bridge (`rrc-dsc-source-completeness-boundary.md`). Adequacy for the intended semantic algebra/law basis remains separate. Finite observer combinations now compile to native Pi derivations relative to explicit primitive source accesses; reply roundtrips, faithful rule recovery and result-only noninterference are checked. That noninterference applies to the declared reply view, not arbitrary access to retained provenance. Structural restrictions now execute on native replies through DSC composition, preserving canonical outputs and reply-image certificates. Coherence holds on admissible images; an inconsistent raw pair remains a checked counterexample. Comparison-sensitive observation now has an explicit retained-path/fibre package and DSC dependent execution interface with substitution compatibility. Fixed-path distinctions coexist with equality of moving-reply packages and dependent reading coherence; a global constant Bool reader extending both fixed readings is impossible. Native RRC transport generation/admission and bound/adaptive integration remain open (`rrc-dsc-observer-internalization.md`). Witness construction, comparison coverage, operational realization, observer internalization and resources/effects remain distinct existing branches; do not duplicate them or presume other owners' results.

No geometric observer area, physical causal law, thermal cost, global commitment authority, universal evaluator or universal computation-as-observation theorem has been established. The programme remains active after this checkpoint.
