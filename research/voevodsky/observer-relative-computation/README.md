# Observer-relative mathematical computation

Dedicated epistemic-graph tree: `issue-tree:b860b514b76f8592b7c7773d`.

Explicitly authorized by the user's request to create a new tree. Linked prior programme: `issue-tree:eaac890b531559a26d4086ec` (Resolution Net v1), left unchanged.

## Objective

Investigate fundamental alignment of computation and mathematics as existing witness-bearing structure. Execution/evaluation as observer-relative manifestation, rather than primitive directed evolution, is a foundational hypothesis to investigate, not an established theorem.

Define observer support and meaningful positivity, restriction, witnessed comparison and compatible gluing. Distinguish checked models from claims about physical time, geometric area, effective computation and external authority.

## Registered decomposition

Node IDs below have prefix `issue-tree:b860b514b76f8592b7c7773d:`.

- `root:v2`: initialization/decomposition resolved; programme remains open.
- `observation-baseline:v1`: resolved for the existing restriction/transport model only.
- `gluing-baseline:v1`: resolved for existing two-probe pushout gluing and obstruction only.
- `observer-positivity:v1`: resolved for formal separation of support, participation, coherence and distinguishability; see `observer-positivity.md` and `results/positivity.json`. Interpretation as execution remains a hypothesis.
- `support-invariance:v2`: resolved for probe-equivalence invariance and the finite duplicated-access/descent test; see `support-invariance.md`.
- `higher-overlap-coherence:v2`: resolved for triangle transport, witness-compatibility formulation and filling obstruction; see `higher-overlap-coherence.md`. General descent is not yet proved.
- `triangle-descent:v2`: resolved for explicit dependent filled-triangle descent equivalence and obstruction regression; see `triangle-descent.md`.
- `direction-and-records:v2`: resolved for reversible comparison, strict information loss, factorization and full-source record refinement; see `direction-and-records.md`.
- `restriction-information-order:v2`: resolved for actual restriction factorization, coordinate loss, neutral repeated access and the unused-output counterexample; see `restriction-information-order.md`.
- `admissible-observation-images:v1`: resolved for images versus records, strict loss and removal of empty-source ghost outputs; see `admissible-observation-images.md`.
- `set-valued-image-descent:v2`: resolved for a constructive collision-preservation criterion and uniqueness with set-valued target; see `set-valued-image-descent.md`.
- `higher-witness-descent:v1`: resolved for an explicit factor with a non-retainable chosen comparison family; see `higher-witness-descent.md`.
- `foundation-checkpoint-audit:v2`: resolved. Fresh15-module audit passed with96 inventoried sources unchanged; see `foundation-checkpoint.md` and `results/foundation-audit.json`. Reproduce with `python research/voevodsky/observer-relative-computation/check_foundation.py`.
- `coherent-image-descent:v2`: resolved for native action identity/composition and preservation of specified triangles without a set-target assumption; see `coherent-image-action.md`.
- `endpoint-action-coherence:v2`: resolved for conjugation coherence and the explicit link to the original named-endpoint collision action; see `endpoint-action-coherence.md`.
- `higher-descent-sufficiency:v2`: resolved for a constructive groupoid-valued existence criterion with retained links and triangle witnesses; see `groupoid-image-descent.md`.
- `groupoid-descent-roundtrips:v2`: resolved for full-data descent equivalence and original-constructor alignment; the older encoder remains separately unaligned. See `groupoid-descent-equivalence.md`.
- `groupoid-witness-regression:v1`: resolved. Identical postprocessors support distinct coherent witnesses and different comparison-sensitive Bool readouts; concrete groupoid and distinct encoded coherence records checked. See `groupoid-witness-regression.md`.
- `execution-observation-bridge:v2`: resolved for the finite run-to-observation construction and a proved converse obstruction; see `execution-observation-bridge.md`.
- `orientation-policy:v1`: resolved. Opposing admissibility policies share the same value observer; full retained views recover histories. See `orientation-policy.md`.
- `policy-local-reconstruction:v2`: resolved for the faithful-recovery criterion and actual two-step fixture uniqueness; see `policy-local-reconstruction.md`.
- `nonunique-history-obstruction:v1`: resolved. Fixed-policy equal-result histories remain distinct; rule-label access gives History≃Bool while mere admissibility does not faithfully recover the supplied history. See `nonunique-history-obstruction.md`.
- `operational-observation-checkpoint:v2`: historical24-entry audit with159 stable sources and a rejected false-equality control. Its receipt/logs are preserved in `results/before-internal-observer-checkpoint/`; the current canonical audit is described below.
- `internal-observer-interface:v1`: resolved for first-order access codes packaged with histories, result-only noninterference and rule-access recovery. Fresh typecheck has inherited transported-index computation warnings; see `internal-observer-interface.md`.
- `transport-computation-gate:v2`: resolved for strict gate establishment and local observer repair. Transported rule readouts pass -Werror, but the whole-programme gate correctly remains failed; see `transport-computation-gate.md` and `results/transport-gate.json`.
- `machine-transport-eliminators:v1`: resolved. General eliminators preserve the actual Run and uniqueness statements; transported execution regressions and the whole -Werror gate pass. See `machine-transport-eliminators.md` and current `results/transport-gate.json`; preceding failed gate evidence is archived under `results/before-machine-repair/`.
- `internal-access-classification:v2`: resolved. Every code's admissible image is equivalent to Unit or History/Bool; repeated rule access adds no independent content, and inconsistent replies are inadmissible. See `internal-access-classification.md`; fresh whole -Werror gate passes.
- `internal-restriction-calculus:v1`: resolved. Structural restrictions preserve shared origins; admissible image maps obey identity/composition and parallel agreement. Capability implication characterizes availability. See `internal-restriction-calculus.md`; whole strict gate passes.
- `internal-comparison-witnesses:v2`: resolved. Actual comparison paths differ at fixed reply, but their total certificates are connected; valid readout extension is dependent, not constant Bool. See `internal-comparison-witnesses.md`; whole strict gate passes.
- `anchored-dependent-records:v1`: resolved. Full dependent records are equivalent to the anchored Bool with both roundtrips; coherent value changes and genuinely changed anchored content are distinguished. See `anchored-dependent-records.md`; whole strict gate passes.
- `coherent-anchor-change:v2`: resolved. Reanchoring has identity/composition and normalization-equivariance laws; identical forgotten records can have different anchored values, so Boolean normalization cannot descend. See `coherent-anchor-change.md`; whole strict gate passes.
- `coded-anchor-descent:v1`: resolved. Exactly result-only coded observations descend; the decision is constructive and descent is preserved by structural restriction. See `coded-anchor-descent.md`; whole strict gate passes.
- `comparison-action-compression:v2`: resolved. Parity suffices for Boolean readback/normalization but cannot recover actual comparison paths; a three-state cover distinguishes equal-parity examples. See `comparison-action-compression.md`; whole strict gate passes.
- `joint-observer-refinement:v1`: resolved. Joint image has six realized readings; individual observers are incomparable, while a four-state cover detects semantic paths lost jointly. See `joint-observer-refinement.md`; whole strict gate passes.
- `finite-cyclic-family-boundary:v2`: resolved for positive-power absolute-winding residue probes. Every finite positive-period family misses a semantically nontrivial common-period path. Signed cyclic monodromy is NOT inferred; see `finite-cyclic-family-boundary.md`. Whole strict gate passes.
- `signed-cyclic-cover-bridge:v1`: resolved. Uniform Fin-state permutation covers support signed transport; the residue bridge transfers finite-family blindness to genuine finite-state observations. See `signed-cyclic-cover-bridge.md`; whole strict gate passes.
- `bounded-cyclic-sufficiency:v2`: resolved. Period N+1 faithfully recovers bounded positive-power paths; pair-dependent finite probes separate distinct powers, without a fixed unbounded completeness claim. See `bounded-cyclic-sufficiency.md`; whole strict gate passes.
- `signed-comparison-normalization:v1`: resolved. Actual fixed-base loops are equivalent to integers; signed evaluation normalizes their paths, with composition and cancellation checked. Syntax is not identified. See `signed-comparison-normalization.md`; whole strict gate passes.
- `signed-bounded-probes:v2`: resolved for interval-certified signed paths. A calibrated period-2B+1 probe faithfully recovers paths, including inverse endpoints; admission from numerical inequalities remains separate. See `signed-bounded-probes.md`; whole strict gate passes.
- `signed-bound-admission:v1`: resolved. Numerical bounds construct interval witnesses; a common magnitude bound gives finite pairwise separation for arbitrary loops in this model. See `signed-bound-admission.md`; whole strict gate passes.
- `phase-bearing-family-boundary:v2`: resolved. Actual transport restores every phase at the common period; fixed finite families remain incomplete, including the calibrated probe representation. See `phase-bearing-family-boundary.md`; whole strict gate passes.
- `finite-adaptive-observer:v1`: resolved. First-order adaptive finite trees have identical complete transcripts on a nontrivial common-period path and zero turns; no faithful transcript decoder exists. See `finite-adaptive-observer.md`; whole strict gate passes.
- `trace-local-observer-boundary:v2`: resolved. Actually queried periods yield a nontrivial alternative with identical full transcript; no unrestricted sound zero certificate can accept that transcript. Scope remains finite trees. See `trace-local-observer-boundary.md`; whole strict gate passes.
- `internal-observer-checkpoint:v2`: resolved. Fresh46-entry strict aggregate audit passed with186 inventoried source/checker files unchanged and two false-theorem controls rejected. See `operational-checkpoint.md` and `results/operational-checkpoint.json`.
- `rrc-dsc-representation-bridge:v3`: resolved for one native application only. `ObserverRRCDSCBridge.agda` checks full boundary/application roundtrips, substitution compatibility and an endpoint-only nonfaithfulness witness; see `rrc-dsc-representation-bridge.md`. Fresh whole transport gate passes including this new module; the earlier46-entry canonical audit remains historical for its recorded snapshot.
- `rrc-dsc-recursive-substitution:v2`: scoped signature-relative result resolved. Recursive roundtrips and seed substitution/flattening compatibility checked in `ObserverRRCRecursiveBridge.agda`; see `rrc-dsc-recursive-substitution.md`. This syntax extension retains RRC's Rule signature; it does not derive those generators from bare DSC. Fresh canonical audit now passes48 entries,189 stable sources/checkers and two negative controls.
- `rrc-dsc-source-signature:v3`: resolved for the current declared signature. `rrc-dsc-source-signature.md` audits eight Code constructors and twelve Rule tags. `ObserverRRCSourceAdmission.agda` checks policy-preserving substitution and refutes unchecked flattening. Fresh canonical aggregate:49 entries,190 stable sources/checkers, two rejected false-theorem controls.
- `rrc-dsc-witness-construction:v3`: finite identity/swap Boolean fragment resolved. `ObserverRRCWitnessSynthesis.agda` produces actual boundary witnesses and RRC histories or certified impossibility, with scoped completeness. See `rrc-dsc-witness-synthesis.md`. Canonical aggregate:50 entries,191 stable sources/checkers, two negative controls.
- `rrc-dsc-comparison-coverage:v5`: relative bridge resolved. All five generated constructors transfer and reflect with exact interpreted-witness preservation. Source Completeness conditionally yields exact-witness completeness on encoded inputs; its premise and derivation-level inverse laws are not proved. See `rrc-dsc-comparison-coverage.md`. Latest canonical aggregate:52 entries,195 stable sources/checkers, two negative controls.
- `rrc-dsc-dependent-witness-synthesis:v3`: resolved for two canonical index-path codes in the nontrivial circle Boolean cover. Actual transport witnesses and total-space RRC histories are constructed, with exact index recovery and reflexive-query obstruction. See `rrc-dsc-dependent-synthesis.md`. Latest canonical audit:53 entries,196 stable source/checker files, two negative controls.
- `rrc-dsc-source-completeness-instance:v3`: concrete countermodel resolved. Unit-valued semantics plus tag-preserving laws fail Generated completeness; the obstruction survives the bridge. See `rrc-dsc-source-completeness-boundary.md`. Canonical audit:54 entries,197 stable source/checker files, two negative controls.
- `rrc-dsc-intended-law-adequacy:v1`: open; specify and test the intended semantics/law basis rather than extrapolating from the countermodel.
- `rrc-dsc-observer-internalization:v5`: selected. Native finite access/restrictions and a comparison-sensitive retained-path/fibre package with actual dependent DSC execution are checked. Fixed-path distinctions and moving-reply coherence are preserved; no global constant Bool extension exists. Native Resolve transport generation/admission and bound/adaptive integration remain open. See `rrc-dsc-observer-internalization.md`. Latest canonical audit:57 entries,200 stable source/checker files, two negative controls. Use fresh graph resume for current selection.

Creation event: `ev-000000015449-fd9d2826-62f5-4bf2-a40f-a3c9fde8bc05`.
Initial decomposition: graph admission `ep_72405881-ac86-4395-bcf9-065dec0734f8`. Admission certifies coordination, not mathematical truth. This registration turn did not rerun proof checks.

## Existing artifacts retained in place

Under `../resolution-net-v1/`:

- `observer-relative-existence.md`, `agda/ResolutionNetObservation.agda`, `results/agda-observation.log`.
- `observation-gluing.md`, `agda/ResolutionNetObservationGluing.agda`, `results/agda-observation-gluing.log`.
- Earlier context: `foundational-alignment.md`, `dependent-interface-foundation.md`, `dependent-machine.md` and their referenced Agda modules.

The completed baseline nodes link the preceding turns' checks and their precise scope. No files were relocated, and no old whole-prototype audit receipt is asserted current for the later foundational modules.
