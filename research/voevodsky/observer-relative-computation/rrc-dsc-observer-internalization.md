# Observer internalization checkpoint: native composition with explicit base access

`agda/ObserverRRCInternalInterface.agda` compiles the existing finite observer language from ObserverInternalInterface: result, rule and recursively combined both.

## What is internalized

Each both node becomes an actual native RRC Pi-rule over Bool, with both recursively built child derivations supplied as premises. Its Complete output retains the whole child package family. This is not merely wrapping the entire interpreter in atom or supplying an arbitrary evaluator callback.

The two leaf probes remain explicit Source constructors. result-access and rule-access retain the actual observed History and supply their fixed primitive readings. The rule label is extracted by the existing checked fixed history observer; it is NOT claimed to be derived from Pi or bare DSC composition.

The native output view is decoded structurally. The reply encoder/decoder have both roundtrips for all native reply values, giving a checked reply-type equivalence. The observed history parameter indexes the retained package types; the decoder's computational clauses inspect the reply structure, not the history data.

## Preservation theorems

- `view-agrees`: compiled native output reads exactly as the original observer code.
- `faithful`: a supplied RuleAccess location still reconstructs the original fixture history.
- `noninterference`: all result-only observer combinations give equal declared reply views for any two histories.
- `no-result-only-recovery`: no recovery function on those replies can faithfully reconstruct every history.
- `history-roundtrip`: recursive translation into the DSC signature-relative Term language and decoding retains the actual constructed native derivation, including every child and source capability.

The finite fixture and its two possible histories are the same as in the original observer model; no universal information theorem follows.

## Critical access boundary

Noninterference concerns the declared Reply view. It is NOT secrecy against arbitrary inspection of the full retained package/history: the source constructors deliberately retain the original History as provenance. An observer granted that raw data would have a different access interface. No OS/process isolation, effect authority or physical observer has been proved.

Similarly, this is composition internalization RELATIVE TO admitted primitive accesses. It does not establish that an arbitrary observer operation is generated by the current RRC signature. Those base capabilities and their admission remain visible in Source instead of being concealed in an evaluator argument.

## Native restriction compilation

`agda/ObserverRRCInternalRestriction.agda` now compiles every existing Restrict constructor into native reply operations: identity, discard, projections, pairing and composition. Composition uses the actual DSC compose operation. The syntax has no arbitrary callback field; correctness against the original interpreter and preservation of canonical native values are checked structurally.

This is execution on native reply representations through DSC's semantic interface, not a claim that each projection is a primitive RRC Resolve rule or that an autonomous object machine has been supplied.

Native Image certificates retain mere origin of the decoded reply in the original observer's readout image. They certify reply admissibility, not every retained provenance field or authority to inspect that provenance. Image maps preserve these certificates. Parallel restrictions are coherent on these images, and image identity/composition are checked.

The scope is essential: the native raw reply representing `(true,false)` for `both rule rule` makes left and right projections disagree. It has no admissible reply-origin certificate. The structural restriction language still cannot create rule-sensitive access from result-only access. No global coherence of arbitrary raw replies is claimed.

## Comparison-sensitive dependent interface

`agda/ObserverRRCDependentObserver.agda` now emits an explicit RRC expression `retain (paths index-code base reply) comparison (fibre reply)` and its transported value. The retained path package is recovered exactly through the existing Code recover operation; the reading agrees with the original dependent observer.

The actual DSC execute operation takes the reply and its comparison as dependent inputs and returns a value in that reply's fibre. Execution agrees with the emitted packet and commutes with environment substitution. The transport operation is fixed and transparent, not a caller-supplied evaluator callback.

At a fixed reply, stationary and turn paths are distinct and yield true/false readings. If the reply is allowed to move, their whole emitted Complete packages are connected by a checked path and their readings by a dependent path along the varying expression. Consequently no constant Bool-valued reader on all Complete packages can extend both chosen fixed-boundary readings. This preserves the earlier distinction between fixed-path information and moving-reply total certificates; retaining data does not license a type-incorrect constant-codomain readout.

This is a package/DSC semantic-interface result. It does NOT yet construct a native RRC Resolve derivation implementing transport from source premises alone, nor synthesize arbitrary comparison paths. That generation/admission boundary must remain explicit.

## Active continuation

The broader observer-internalization branch remains selected. Finite access composition, image-relative restrictions and the dependent package/interface are now checked. Next settle the native transport generation/admission boundary, then integrate supplied recovery bounds and adaptive transcripts with their exact hypotheses. The present results do not internalize all observers automatically.

## Verification

Fresh canonical safe Cubical Agda --ignore-interfaces -Werror passes57 entries,200 source/checker files unchanged, and two rejected false-theorem controls in138.815 seconds. Evidence: results/operational-checkpoint.json and results/agda-operational-checkpoint.log. No Nima-owned source changed.
