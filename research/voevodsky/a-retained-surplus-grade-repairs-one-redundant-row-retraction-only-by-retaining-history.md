# A retained surplus grade repairs one redundant-row retraction only by retaining history

For the fixed interval `0<=x<=1`, adjoin a redundant row `x<=1+k`, k>=0. Under the fixed add multiplier M the third bound equals the duplicated old bound plus Farkas surplus k. The ordinary drop N forgets k: fine presentations k=0 and k=1 have the same old interval and identical plain dropped bound but distinct proof presentations. A DECORATED drop retaining `(old bound,k)` admits an exact inverse `restore(old bound,k)` over this restricted redundant-row family. Both arrows preserve nonnegative surplus only because k>=0 is a declared domain premise. Fresh exact-rational `check_retained_surplus_grade.py` passes five k-values, refuses negative surplus and explicitly excludes the genuine new fine constraint `x<=1/2` (which would need k=-1/2 in this parametrization).

The extra grade is retained HISTORY/PROOF data, not an invariant of the coarse feasible relation. Making the proof retraction invertible requires storing precisely what ordinary forgetting discards. This is analogous to archive/re-exposure costs but does not identify Farkas surplus with analytic residue R or build a chain complex. The construction's inverse is not a lawful generic transition from an old state with no retained k; inventing k after erasure would fabricate evidence.

This resolves a narrow yes/no: a minimal displayed one-coordinate surplus decoration repairs the one-row redundant proof retraction, while unadorned quotienting cannot. Extension to multiple rows and changing source charts would need independently checked composition and positivity laws. The ten-interval analytic graded defect correspondence and owner-derived role functor remain absent.
