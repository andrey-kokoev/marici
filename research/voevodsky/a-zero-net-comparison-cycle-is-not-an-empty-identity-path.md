# A zero-net comparison cycle is not an empty identity path

On unit-square rows, P=(0,1,0,0),surplus1 and Q=(1,2,0,0),surplus0 separately prove x<=2. Fresh `check_nonempty_comparison_cycle.py` models typed comparison edges P->Q and Q->P; their signed multiplier differences sum to zero and the path starts and ends at P. Yet this synthetic cycle has TWO edges, while identity at P has none. Endpoint and net mathematical equality do not erase distinct audit occurrences.

No actual events or signed source issuer are evidenced; analytic S,A,R,C,G remains deferred.

Next test CYCLE REDUCTION with a branch: removing P->Q->P from a larger path may preserve endpoint math but must produce a new derived view referencing the deleted-segment digest, not destructively rewrite the original history.
