# Exact Hardy transport places the observer at one boundary, not across the expanding prolate window

## Placement supplied by the physical transport

The exact characterwise transport contains translation by the cutoff boundary

\[
L=\log\Lambda.
\]

After transport, the physical cutoff is fixed:

\[
\mathcal U_\chi P_\Lambda\mathcal U_\chi^*
=\Pi.
\]

The reference and Tate Fourier cutoffs are

\[
Q_L^0
=M_{e^{2iLs}}\Pi M_{e^{2iLs}}^*,
\]

and

\[
Q_{L,\chi}^T
=M_{e^{2iLs}\gamma_\chi}
\Pi
M_{e^{2iLs}\gamma_\chi}^*.
\]

The observer becomes the fixed multiplier

\[
\mathcal U_\chi A_g\mathcal U_\chi^*
=M_{m_{g,\chi}}.
\]

Thus the observer is recentered at the fixed Hardy boundary \(\Pi\). It is not spread uniformly across the expanding physical window.

## Consequence for local edge density

The symmetric unweighted prolate trace integrates transition density over the entire expanding interval. That integration produces logarithmic Landau--Widom mass.

A fixed Schwartz multiplier in the recentered Hardy coordinate probes one boundary chart. It does not test the whole expanding interval with approximately constant weight.

Therefore the unweighted law

\[
\operatorname{Tr}\eta(B_c)
\sim
2C_W(\log2)\log c
\]

does not imply an observer-weighted logarithmic Gram on the transported source.

The previously proposed leading form

\[
2C_W(\log2)(\log c)G_{Pl}
\]

used the wrong observer scaling.

## Common translation removal

Write

\[
U_L=M_{e^{2iLs}}.
\]

Then

\[
Q_L^0=U_L\Pi U_L^*,
\]

and

\[
Q_L^T=U_LQ_0^TU_L^*
\]

with the stationary scattered projection

\[
Q_0^T=M_\gamma\Pi M_\gamma^*.
\]

Hence

\[
Q_L^T-Q_L^0
=U_L
(Q_0^T-\Pi)
U_L^*.
\]

Because observer multipliers commute with \(U_L\), the relative difference row is stationary after common recentering.

Its observer-localized Hilbert--Schmidt norm is independent of \(L\):

\[
\|
(Q_L^T-Q_L^0)M_m
\|_2
=
\|
(Q_0^T-\Pi)M_m
\|_2.
\]

Thus the Tate/reference relative generic-angle energy is finite on the phase-energy observer core and carries no logarithmic growth in \(L\).

## Reference geometry

The pure pair \((\Pi,Q_L^0)\) is nested in the chosen orientation. Its extensive mass belongs to the exact mismatch strip, not to a generic principal-angle sector.

Consequently the reference pair has no generic \(\eta(B)\) plunge obstruction in this Hardy chart. Its growing contribution is the already identified mismatch-strip bulk.

The Tate phase perturbs the moving projection, but after common recentering the perturbation is the fixed relative projection pair \((Q_0^T,\Pi)\).

## Two edge notions

There are therefore two different objects that had been conflated.

### Global unweighted prolate edge

This integrates generic-angle density over the whole expanding window and has logarithmic Widom mass.

### Observer-localized relative Hardy edge

This follows one recentered boundary and compares Tate with reference after removing their common translation. Its difference row is stationary and Hilbert--Schmidt on the observer core.

Only the second object enters the finite Tate boundary form.

## Placement commutator

The left Hardy placement leaves the commutator term

\[
\mathcal C_L(g,h)
=
\operatorname{Tr}
\left(
[M_{m_h}^*,\Pi_L]
\Delta Q_0M_{m_g}
\right).
\]

Its factors have cutoff-independent Hilbert--Schmidt norms. Its scalar pairing tends to zero by oscillation.

This confirms the boundary-following interpretation: the remaining \(L\)-dependence is relative translation of one localized boundary kernel, not growth of a uniform observer-weighted edge density.

## Corrected positive boundary target

On the Bruhat--Schwartz observer core, one should not introduce a second logarithmically divergent edge subtraction merely from the bare prolate Widom law.

The positive relative feature already has:

1. an extensive common mismatch row;
2. a stationary observer-localized difference row;
3. a finite cross readout;
4. a placement commutator vanishing weakly.

The absolute completion problem is to minimalize this finite stationary relative feature in the phase-energy metric. It is not to subtract a universal \(\sqrt{\log c}\) edge from each localized observer leg.

## Remaining positive issue

The fixed Krein realization of the stationary relative pair can still contain balanced positive mass. The relevant obstruction is now the finite phase-energy Gram of the stationary difference feature, not the unweighted logarithmic trace of the full prolate window.

Thus the next candidate must be built directly from

\[
(Q_0^T-\Pi)M_m
\]

and the normalized multiplier

\[
\mathcal A_S=M_{w_S/e_S}.
\]

The comparison should be made on the phase-energy source, where both are bounded.

## Disposition

Exact transport answers the observer-placement question. The observer is fixed at one Hardy boundary after recentering. The unweighted logarithmic prolate edge does not become a logarithmic Plancherel Gram on this observer core.

The proposed observer-weighted edge law proportional to \((\log c)G_{Pl}\) is withdrawn. The regular completion gate returns to finite stationary relative-feature minimalization on \(\mathscr E_S\).
