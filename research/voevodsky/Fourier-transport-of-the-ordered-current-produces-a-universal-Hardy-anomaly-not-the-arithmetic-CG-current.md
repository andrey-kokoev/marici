# Fourier transport of the ordered current produces a universal Hardy anomaly, not the arithmetic CG current

The naive Fourier--Tate naturality square from the previous iteration is false
without a boundary correction.  The order operator

\[
(Sg)(q)=\int_{\mathbb R}\operatorname{sgn}(v-q)g(v)\,dv
\]

has the exact reciprocal-odd Fourier multiplier

\[
\widehat{Sg}(\xi)
=2i\,\operatorname{pv}\!\left(\frac1\xi\right)\widehat g(\xi).
\]

For the ordered theta tail, however, the source is the positive-half
restriction `f=P_+ Phi`.  Fourier transform does not commute with this
restriction:

\[
\widehat{P_+\Phi}
=\frac12\widehat\Phi
+\frac{1}{2\pi i}\operatorname{pv}
 \int\frac{\widehat\Phi(\eta)}{\xi-\eta}\,d\eta.
\]

The defect is the universal Hardy/Hilbert boundary operator.  It is the native
archimedean channel of the ordered current.

This calculation contains no prime labels, valuation multiplicities, or Adams
grades.  Therefore Fourier conjugation of the ordered current cannot by itself
produce the primitive or prime-square arithmetic currents.  The equality

\[
FT_{\rm bdry}(C_f)=C_{FT_{\rm src}f}
\]

must be replaced by an anomaly-corrected square retaining the commutator of
Fourier transport with half-line restriction.

A full external `CG` current now requires an additional labelled adelic
incidence

\[
A_{\rm ad}:U_{\rm labelled}\longrightarrow
\mathcal H_{\rm Hardy/PV}
\]

that couples the primitive, square, and connected source strata to this
universal Hardy boundary channel.  It must preserve reciprocal oddness,
exclude the reflection-even constant mode, and be defined before scalar
cumulant evaluation.

Thus the ordered current remains the correct archimedean factor of the
external candidate, but not the complete arithmetic current.  The missing
mixed polarization is precisely the source-derived joining of labelled adelic
sampling with the Hardy principal-value port.
