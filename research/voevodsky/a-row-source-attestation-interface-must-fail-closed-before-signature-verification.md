# A row-source attestation interface must fail closed before signature verification

The frozen row-attestation request still has `requested_owner=null`, `owner_event_id=null` and status `owner_unassigned_no_live_grant`. Its disclosed ordered-row manifest DOES hash to its recorded SHA-256 under the request's canonical JSON encoding. Fresh `check_row_attestation_interface.py` first verifies that local binding, then requires a claimed issuer and source event, an INDEPENDENT matching issuer/source grant, live generation and revocation route, matching event/manifest binding, and an actual trusted cryptographic verifier. The real request fails immediately as `ISSUER_OR_EVENT_UNASSIGNED`. A graph-actor self-claim without a separate grant fails `INDEPENDENT_ISSUER_GRANT_MISSING`; wrong scope, generation, manifest and event are refused. Even fully populated FICTIONAL test fields fail `CRYPTOGRAPHIC_VERIFIER_UNAVAILABLE` rather than producing an approval.

This is an interface specification and negative local test, not a cryptographic implementation, issuer designation, or authorized owner handoff. A production verifier would additionally need an admitted trust root, validated signed canonical event bytes, current revocation information and event/generation freshness. No source-owner recipient is authenticated in the currently inspected evidence, so no owner message is fabricated. Mathematical row identity and graph transition admission remain strictly weaker than publication authority.

Next test EVENT REPLAY across two generations: the same manifest and nominal issuer/event ID paired with a superseded generation should be refused absent live freshness and nonrevocation evidence. This remains a local hypothetical hostile fixture until an authorized issuer surface exists; analytic S,A,R,C,G correspondence is deferred.
