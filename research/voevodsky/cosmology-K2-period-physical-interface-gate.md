# K2 period physical-interface gate

## Question

Does the programme currently supply the interfaces needed to promote the normalized K2 period to a physical readout?

## Claim boundary

No. The mathematical chain is complete: ordered units `(u,v)`, the compact Betti torus, the class `Xi_log`, normalized period one, and matching Parshin residue. The physical chain is absent.

The contour `|u|=|v|=1` uses the compact involutions `u -> 1/conjugate(u)` and `v -> 1/conjugate(v)` and selects their torus generator. The algebraic symbol `{u,v}` does not select this contour as physical. Its sign is fixed only after ordering the units and orienting both circles; no cosmological boundary condition supplies that orientation authority.

Across the rank-26 source, exceptional-triangle, K2, flag, and period artifacts, there is no typed kinematic map to `G_m^2`, source-derived real form, contour prescription, state/effect pairing, amplitude normalization, detector record, or physical record map.

## Disposition

The normalized value one is a mathematical period and residue invariant, not a cosmological observable or contour contribution. This is a bounded nonconstruction result, not a proof that an external physical model cannot provide the missing interfaces. The next leaf inspects actual kinematic variables and boundary prescriptions for a sourced map to the compact torus and rejects coordinate analogy as authority.

## Verification

- `research/voevodsky/check_cosmology_K2_period_physical_interface_gate.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
