# Bounded verification of the weak relational source contract

The contract of `a-common-relational-contract-does-not-identify-source-structures.md` admits three typed interpretations, not a structure-preserving equivalence. Finite controls were freshly rerun on the declared input on this turn:

- `python research/nima/checkers/verify_source_evidence_coupling.py`: passed, 638 reachable states, 1268 independently replayed labelled edges, 461 enhanced classes; current/source-only class has opposite issuance admissions. Owner-produced coupling SHA-256: `30e53bb33d131561ebde2a5d167ade86e1ceab39ad70454062eeee2fdd50eade`.
- `python research/voevodsky/checkers/check_observation_refinement_laws.py`: passed on a 27-point source grid, 147 coarse-query comparisons; fine audit lost after coarse saturation and row deletion differs from projection.
- `python research/aspect/scc/scc.py check relational-continuation-ports`: passed; labelled enhanced successor congruence and source-only issue-label hostile, conditional on owner graph. SCC validates this finite stratum only.

The protocol graph verifier imports the source `record` constructor and checks owner contract hashes before replay. This establishes the finite declared graph against its typed guards; it does not establish physical evidence provenance. The independent SCC test additionally reports an issue-labelled collision rather than relying on the first arbitrary collision. The finite set control demonstrates a different carrier: the whole possible subset, not an individual source word. Neither test proves that a module-linear source action is the same operation as evidence acquisition or fine constraint addition.

**Scoped disposition.** The weak common signature (typed carriers, labelled partial successor relations, output maps) and the compressed-successor criterion have direct set-theoretic proofs and reproducible bounded instances; a universal source-preserving comparison, arbitrary fine-audit descent, and analytic S,A,R,C,G realization are explicitly withheld. The structural side contracts required for future instances are (i) source corners, coefficient and filtration data for linear word actions, (ii) evidence fields and labelled rejection for coupled protocol, and (iii) exact constraint graph, fine witness incidence and owner grant for authorized rebase. Replacing any of them by equality of public images is a typed error. This resolves the selected source-contract question only in this weak sense; new cross-class functor or analytic map claims must be separate successor research.
