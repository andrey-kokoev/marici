# Transport-compatible carrier system

## Question

What geometric data realize the algebraic maps `T_A direct_sum id` across even ambient degree?

## Claim boundary

For every even `A>=12`, one needs a six-field carrier datum `D_A` and a carrier morphism to `D_(A+2)` satisfying six squares:

1. the centers form a Cartesian square;
2. the common line pulls back;
3. the ordered conormal isomorphism commutes with pullback;
4. every labeled wall pulls back to the same label;
5. the DNC identification commutes with base change;
6. orientation is preserved, or coefficients use the sign local system.

These squares force preservation of `Gamma`, its boundary, `{u,v}`, the tame tuple, and the comparison-cone equation. They geometrically realize `T_A direct_sum id`.

The existing `T`, `S_K`, and `Q` transports act on relation modules. They supply none of the carrier objects or naturality squares. Therefore algebraic colimit coherence is proved, while geometric unbounded realization remains unmaterialized. Finite universal common-line realizations remain valid.

## Disposition

The missing object is one compatible carrier system, not another rank computation. The next leaf consolidates the result-strength boundary and residual gate.

## Verification

- `research/voevodsky/check_cosmology_transport_compatible_carrier_system.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
