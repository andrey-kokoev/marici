# The retained common domain exists but does not supply Hilbert observability

## Question

After the labelled/radial interface correction, is the common domain itself still missing?

## Claim boundary

No for the internally retained four-port chain. Prior research proves joint closability and constructs its common graph completion. What remains missing is external G4 identification and uniform Hilbert recovery from the analytic record after forgetting the source graph coordinate.

## Retained common domain

Let \(D_0\) be the typed finite-support source and let \(A_1,\ldots,A_m\) be the finite family consisting of retained bilateral histories, grade action, Green-summable prime assembly, traces, currents, and reciprocal/Fourier sewing. The diagonal graph map is

$$
Jx=(x,A_1x,\ldots,A_mx).
$$

Prior work proves this family jointly closable. Hence

$$
D_{\mathrm{ret}}=\overline{J(D_0)}
$$

is a faithful graph completion over the source: it contains no nonzero vertical vector over source zero.

On the graph presentation that retains the source coordinate, projection

$$
\pi_1:D_{\mathrm{ret}}\longrightarrow \mathcal A_{\exp}
$$

is continuous and injective. Thus the fourth-to-source comparison is analytically meaningful in the locally convex graph category on this restricted domain.

## Why this is not the desired Hilbert C41

The analytic four-port synthesis obtained after forgetting the source coordinate still has zero lower margin on the labelled Hilbert rung:

$$
\inf_{\|c\|_{\mathcal H_0}=1}\|U_4c\|_{\mathrm{an}}=0.
$$

Therefore the graph projection cannot be promoted to a bounded inverse of the source-forgetting Hilbert record. The radial defect multiplier repairs radial-field observability but does not change this Euler-weighted labelled-source obstruction.

## Remaining gates

A subsequent rung-by-rung audit shows that internal attachment provenance is complete: traces share the second-order graph, the primitive and square rows have explicit graph-dual functionals, the connected tail is an ideal coordinate, and the archimedean attachment is a Mellin-line functional. The remaining gates are therefore:

1. construct the global determinant-line comparison joining those separately typed attachments;
2. identify external G4 Fourier--Poisson sewing with the retained length-preserving sewing;
3. if Hilbert \(C_{41}\) is required, add a labelled bulk channel with a lower margin before Euler synthesis, or change the source norm explicitly.

## Disposition

The common-domain gate is closed internally by \(D_{\mathrm{ret}}\). Continuous graph-level fourth-to-source projection is available when the source coordinate is retained. Bounded Hilbert recovery from the source-forgetting four-port record remains impossible with the currently declared norm evidence.