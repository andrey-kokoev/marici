# Branching weakening shortcuts require a selected path ID

Two FICTIONAL audit paths have common x<=1 start and x<=3 end. Each passes through a mathematically identical x<=2 intermediate packet, but one uses `middle-A` occurrence and the other `middle-B`. Fresh `check_branching_weakening_shortcut.py` verifies exact surplus increments along BOTH paths. Endpoint-only lookup rejects `AMBIGUOUS_PATH`; each separately derived direct arithmetic shortcut must retain its particular intermediate occurrence and full parent-path digest. Neither is an observed direct edge.

This closes the bounded target-weakening branch: typed weakening is mathematically composable, but terminal packet equivalence does not identify the historical route. All fixtures are synthetic. The real row issuer, source-publication grant and analytic S,A,R,C,G correspondence remain unestablished. Do not treat graph admission as any of those authorizations.

Future work, only if newly tasked: identify an authorized source issuer and an actually observed path with an explicit signed contract before proposing publication; otherwise leave the analytic correspondence deferred and the exact local mathematical results scoped to these fixtures.
