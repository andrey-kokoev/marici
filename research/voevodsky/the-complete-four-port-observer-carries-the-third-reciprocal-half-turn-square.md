# The complete four-port observer carries the third reciprocal half-turn square

## Question

Does the complete fourth presentation carry an analytic reciprocal action compatible with source reflection?

## Claim boundary

Yes when the fourth presentation is the retained four-port boundary graph and the constructor domain is the bounded observer coalgebra already declared. This does not identify that graph with the separate completed history-plus-cycle image on every topology.

Let \(\mathcal B_4\) be the retained boundary graph with ports \((P,Q,M,J)\). Prior research constructs reciprocal Fourier--Poisson sewing with contragredient dual action as a unitary graph automorphism. Denote this involutive half-turn by

$$
\rho_4:\mathcal B_4\longrightarrow\mathcal B_4.
$$

Let

$$
q_4:\mathsf{Obs}_S\longrightarrow\mathcal B_4
$$

be the complete four-port observer before scalar readout, on the bounded source constructor category. Reciprocal naturality of the observer coalgebra gives

$$
\rho_4q_4=q_4\iota,
$$

where

$$
(\iota h)(a)=h(a^{-1}).
$$

Since reciprocal sewing is involutive,

$$
\rho_4^2=\operatorname{id}_{\mathcal B_4}.
$$

This is the third source-rooted mixed half-turn square, complementing

$$
\operatorname{Ad}_{F_S}C_{12}=C_{12}\iota
$$

and

$$
\rho_3C_{13}=C_{13}\iota.
$$

On observer-generated essential images, the other three half-turn squares follow by composing these source-rooted squares with the proved presentation inverses.

## Interface boundary

The symbol \(q_4\) here denotes the complete four-port boundary record, not the scalar Weil functional. The repository also has a completed history-plus-cycle observer \(\mathcal O_D\). No packet identifies \(\mathcal B_4\) and \(\operatorname{im}\mathcal O_D\) as topological presentation objects. That comparison remains separate.

## Disposition

All three source-rooted reciprocal half-turn squares are analytically constructed on their declared observer-generated carriers. Therefore all six presentation edges inherit half-turn equivariance by composition. Quarter-turn equivariance still belongs to the larger additive Fourier carrier and requires its own projection interfaces.