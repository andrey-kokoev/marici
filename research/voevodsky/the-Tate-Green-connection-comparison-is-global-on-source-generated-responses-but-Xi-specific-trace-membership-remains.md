# The Tate--Green connection comparison is global on source-generated responses, but Xi-specific trace membership remains

The complete response carrier already has a canonical unitary quarter-turn
sewing relation

\[
\Lambda_{\mathbb T}=\{(x,\mathbb T x)angle\},
\]

which is maximal isotropic.  For every admitted source test vector `g`,

\[
(\mathscr R(g),\mathscr R(\mathcal Fg))
=(\mathscr R(g),\mathbb T\mathscr R(g))
\in\Lambda_{\mathbb T}.
\]

The response record includes values, Fourier values, moving-seam histories,
principal-value responses, endpoint traces, and their jets.  Placewise Tate
factors with their logarithmic connection give the equivalent spectral
presentation.  Therefore the connection-preserving Tate--Green comparison is
already established on the generic source-generated response graph.

This does not identify a particular characteristic trace with a graph vector.
For the Xi problem one still needs a declared source vector `g_z` and the full
identity

\[
\operatorname{Tr}_{\rm Evans}(z)
=
(\mathscr R(g_z),\mathbb T\mathscr R(g_z))
\]

including every prime shell and all retained value, connection, endpoint,
ordered, and archimedean ports.

The fixed-forcing pair lift supplies a natural candidate

\[
g_z=\Phi\otimes u_z
\]

in the corrected source lane.  Its noncollapse and Xi-multiplicity properties
are established.  What remains is to prove that its complete response record
is the independently declared Evans/Tate trace, rather than merely the
internally generated Stokes response.

Hence the frontier is not a generic connection or sewing theorem.  It is an
Xi-specific source-membership equality.  On the unchanged Evans lane this
membership is refuted by the first-zero residual; on the corrected pair lane it
is open.
