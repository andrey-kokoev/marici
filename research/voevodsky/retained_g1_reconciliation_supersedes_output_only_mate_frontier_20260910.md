# Retained G1 reconciliation supersedes the output-only mate frontier

## Question

How do the recent finite Green-defect calculations compose with the already selected first-Adams architecture?

## Claim boundary

The transported valuation defects give a valid finite resolution of the retained grade source. They do not reopen the stronger output-only comparison between window and cut/Wronskian outputs. This packet reconciles constructor order and identifies the next research boundary; it does not mutate any ledger status, establish G2--G4, or prove RH.

## Selected object

For each prime, the selected source is

\[
E_p=E_{p,12}\oplus E_{p,\ge3},
\]

and the completed first-Adams carrier retains the source coordinate:

\[
\mathcal Jx=(x,Ax,Cx).
\]

The local primitive/square graph is

\[
\Gamma_{p,12}
=\{(A_{p,12}x,C_{p,12}x):x\in E_{p,12}\},
\]

while connected grades attach through their separate retained graph \(\Gamma_{p,\ge3}\). No bounded map from one analytic output to the other is part of this constructor.

## Reconciliation with transported Green defects

For the finite valuation shift, define

\[
P_j=S^jP_0(S^*)^j.
\]

The exact label comparison is

\[
P_j\longleftrightarrow e_{p,j+1}.
\]

Consequently,

\[
P_0,P_1\longleftrightarrow E_{p,12},
\qquad
P_j\longleftrightarrow E_{p,\ge3}\quad(j\ge2).
\]

The cumulative Ward filtration remains

\[
I-S^k(S^*)^k=\sum_{j<k}P_j.
\]

This identity is a resolution of the retained source grading. It is not an equality obtained after window or Wronskian scalarization.

At depth \(N\), the finite terminal projector \(P_N\) is the highest currently retained grade \(e_{p,N+1}\). Under extension to depth \(N+1\), that coordinate persists unchanged and ceases to be terminal; \(P_{N+1}\) becomes the new terminal coordinate. Terminal status is cutoff-relative, whereas valuation grade is durable.

## Constructor order

The compatible order is:

1. retain prime and grade source idempotents;
2. construct window, cut, seam, wall, and Wronskian graph coordinates;
3. form the polarized direct-sum Green form while the source coordinate remains present;
4. aggregate grades only inside one fixed-prime Wronskian fibre;
5. apply any all-prime scalar evaluator only as a terminal readout.

This order prevents compact theta incidence from being mistaken for an inverse reconstruction map and prevents cross-prime products from entering the G1 Green carrier.

## Disposition of recent finite artifacts

The following survive as components or audits of the selected architecture:

- `transported_green_defect_filtration_mate.v1.json`: exact atomic-to-cumulative valuation resolution;
- `transported_defect_register_to_retained_g1_graph.v1.json`: exact grade reindexing into \(\Gamma_{12}\oplus\Gamma_{\ge3}\);
- `prime_two_gradewise_euler_theta_incidence_mate.v1.json`: finite nonzero labelled incidence;
- `green_seam_bilinear_linking_operator.v1.json`: even seam observable on the shifted-tail carrier;
- `archimedean_wall_odd_endpoint_incidence_relation.v1.json`: external wall/odd endpoint relation;
- `full_log_jet_tower_recovers_finite_valuation_register.v1.json`: alternative finite moment coordinates for the same atomic register.

The following are hostile or nonselected branches rather than active gates:

- output-only endpoint comparison and bounded inverse requirements;
- universal prime-power quadrature of continuous seam accumulation;
- Gaussian-ray selection of the Riemann channel;
- internalization of the constant--delta orbit as an ordinary bulk state;
- any fixed finite logarithmic-jet truncation at unbounded valuation depth.

The four-front target and quadratic translation-defect checks remain valid local identities, but prior retained-graph closure does not require proving an output-only equality \(J_{\rm src}=J_{\rm top}\) as a separate constructor.

## Current status

Prior cross-packet review supports G1.1--G1.4 as closure candidates on the retained saturated architecture. This reconciliation adds no independent confirmation of every cited analytic bound and grants no ledger authority.

The active global frontier is downstream of G1. The current model audit locates the RH-bearing missing chain map at

\[
K_\tau\longrightarrow C_{\rm FP},
\]

whose unchanged-Evans component requires the prime-shell adjoint residual family culminating in

\[
B_\Sigma^\dagger u_z=0
\]

on the Xi divisor. That vector identity, together with completion and source identification, belongs to G2--G4 rather than to the resolved finite G1 grade mate.

## Disposition

The finite Green-defect work is retained as a source-grade resolution inside G1. The output-only mate frontier is superseded. Subsequent work should inspect the prime-shell adjoint residual and Evans-to-conservative-Green chain-map packets, not add further scalar or finite-frame repairs to G1.
