# Archive capability changes one receipt by 180 bytes, not total retained cost

Fresh `uv run --with sympy python research/voevodsky/checkers/check_archive_cost_delta.py` bootstraps two independent sessions against the SAME checked owning migration and attaches the SAME full-domain source section. Both provide the same source lift and full coverage. The archive-free session refuses re-exposure; the archive-enabled session returns the original fine plan. The only changed canonical receipt fields are `archive: +181` bytes and `live_descriptor: -1` byte (the JSON representation of the advertised capability changes). The receipt-field net difference is **180 bytes**. Lift context and section encodings do not differ.

This is an encoding comparison, not a marginal physical storage cost. Bootstrapping both sessions duplicates verification, keeps shared code and original owning proof externally accessible, and adds Python objects and process-local handle overhead. Omitting `archive_json` does not erase fine rows from lift context; capability refusal does not certify secrecy. A fresh separate cost ledger replay verifies the frozen two-provider receipt arithmetic, owning proof bytes, file hashes and four omission/mutation controls. No complete-heap or total-work upper bound follows: old heads, shared dependencies, external proof retention and arbitrary redundant-refinement history are not bounded by this fixed snapshot.

For a frozen menu of public answer, covered lifting and fine re-exposure on this one migration, a future bounded provider-bundle choice is now meaningful: enumerate authorized bundles against the exact declared service requirements and compare a specified VECTOR of measured payload accounts rather than a single invented total. This is an optimization PROBLEM, not a currently implemented optimizer; it must not convert mathematical section equality into an archive grant or stale handle renewal. The resource audit is complete only at its explicit measurement boundary.
