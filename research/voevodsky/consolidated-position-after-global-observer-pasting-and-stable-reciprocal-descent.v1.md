# Consolidated position after global observer pasting and stable–reciprocal descent

## Constructed analytic architecture

The physical response carrier is the horizontal carrier tensored with an independent two-polarity factor.

The physical quarter-turn is separate from Fourier presentation transport.

The reflected source complex structure constructs the bounded two-polarity trace on the source-retaining response graph.

The reciprocal prime-wall graph is closed, reflection is unitary, and the positive and negative arithmetic source channels are independently constructed.

The central analytic interface separates endpoint detectability, Hilbert–Schmidt attenuation, square-summable response, and faithful source recovery.

Primitive endpoint polarity extends to every prime-power grade through

\[
V_{p,k}
=
\frac{1}{2k}
\begin{pmatrix}
1&p^{-k}\\
p^{-k}&1
\end{pmatrix}.
\]

Square incidence is Hilbert–Schmidt and the connected tail is nuclear.

The reciprocal augmented extension is covariant under the reciprocal-odd Euler coordinate, reciprocal-odd jet shift, and reciprocal-even endpoint evaluation.

APCT, EPCT, HSPF, the central analytic interface, the augmented extension, and GSAC form a typed bicategorical pasting complex on the source-generated response carrier.

The linking operator remains a separate bounded skew response block and does not replace the old first-order Green boundary form.

## Exact Xi constructions

The two stable theta histories satisfy

\[
u_-(q;z)-u_+(q;z)=e^{zq}\tau(z).
\]

At every Xi zero, the two histories glue into a nonzero global state in \(H^1(\mathbb R)\).

Parameter derivatives below the zero multiplicity lie in the same maximal-isotropic continuity domain.

The triangular chain square is exact and preserves the local Xi multiplicity.

The arithmetic Evans residual is an entire \(U_{\mathrm{ar}}\)-valued section.

Multiplicity-preserving conservative promotion is equivalent to the vector-valued division condition

\[
r_U=\tau h_U.
\]

## Faithful joint-graph semantics

The adopted global carrier retains the source and all typed observer outputs in one faithful joint graph.

The first-Adams Stokes and Wronskian realizations coexist without a universal Riesz or single-metric identification.

The four matrix-unit metric identities are optional single-metric descent data under this semantics.

The linking response does not enter the arithmetic codiagonal through an unidentified metric comparison.

## Stable–reciprocal symmetry

The stable and reciprocal binary factors are independent.

The correct finite carrier is

\[
B_{\mathrm{fb}}
=
B_{\mathrm{st}}\otimes B_{\mathrm{rec}}
\cong
\mathbb C^4.
\]

In the basis \((L,z),(R,z),(L,-z),(R,-z)\), the Evans mismatch kernel equals the stable-plus subspace.

The normalized inclusion and retraction are

\[
\iota_+
=
\frac{1}{\sqrt2}
\begin{pmatrix}
1&0\\
1&0\\
0&1\\
0&1
\end{pmatrix},
\qquad
\pi_+=\iota_+^*.
\]

Simultaneous reciprocal Evans matching gives the canonical reduction

\[
B_{\mathrm{fb}}
\longrightarrow
\Lambda_{\mathrm{st}}^+\otimes B_{\mathrm{rec}}
\cong
B_{\mathrm{rec}}.
\]

Reciprocal Tate transport and the reciprocal quarter-turn descend through this retract.

The stable quarter-turn remains transverse because it exchanges the stable matching and mismatch chiralities.

The complete quarter-turn orbit restores all four chirality combinations.

The Xi reduction is therefore a matched slice of the orbit rather than an invariant orbit representation.

## Universal two-involution pattern

For commuting involutions \(S_1\) and \(S_2\), the canonical projectors are

\[
P_{\epsilon_1\epsilon_2}
=
\frac14
(I+\epsilon_1S_1)
(I+\epsilon_2S_2).
\]

The four-character decomposition localizes residuals into orthogonal sectors.

A first equalizer reduces four sectors to two, and the intersection of two equalizers reduces two sectors to one.

This pattern is proved for the stable–reciprocal boundary carrier.

Applications to Fourier, augmented-extension, endpoint, and pair-response carriers require separate verification of the two commuting involutions and their source domains.

## Corrections retained

The four-sector kinematic involution is not the complete tail-feedback operator.

The complete one-sided input and output ports have different internal types and belong to a state-space colligation.

The valid full-tail Green-balance identity is already constructed on the source-generated span.

Unchanged-Evans membership in the conservative graph remains a separate source-placement theorem.

The scalar endpoint-only positive repair is a positivity majorant rather than a normalized conservative realization.

The ordinary Fredholm realization on the Xi seam is replaced by the exact Evans boundary mismatch because the bilateral history generator has essential spectrum there.

## Numerical status

The unchanged-Evans arithmetic residual at the first Xi zero is numerically stable near

\[
-0.150851219058739200687179708292.
\]

The computation is a high-precision scout without interval certification or a completed infinite-tail bound.

The unchanged-Evans promotion remains formally open and numerically disfavored.

## Open theorem-strength gates

The authoritative G4 forward crossing \(U_{\mathrm{G4}}\) has not been exposed on the same source and target pairings as the constructed pair-to-bordered crossing \(T_{\mathrm{PB}}\).

The exact unchanged-Evans gate is

\[
\Delta^\top|_{\mathcal E_{\mathrm{Ev}}}=0,
\qquad
\Delta=U_{\mathrm{G4}}-T_{\mathrm{PB}}.
\]

The equivalent arithmetic gate is the Xi divisibility of the full residual and all jets below each zero multiplicity.

A divisor-preserving modified-history chain map remains unconstructed.

The three-port dressed route still requires seam boundary values and a source-derived factorization

\[
F_\theta=E_\theta\tau
\]

with \(E_\theta\) nowhere zero.

Conservative feedback admission for every Xi state is RH-bearing because the established positive Green identity then forces critical-line confinement.

## Current frontier

The analytic response and coherence architecture is constructed on its source-generated carriers.

The triangular Xi divisor-to-history map is constructed with multiplicity.

The unresolved theorem is the passage from that triangular state to an independently sourced conservative positive state.

The two viable directions are interval-certified rejection followed by a divisor-preserving modified-history construction, or exposure of the authoritative G4 crossing followed by a direct Evans-cyclic defect test.
