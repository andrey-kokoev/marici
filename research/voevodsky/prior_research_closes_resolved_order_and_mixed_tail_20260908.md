# Prior research closes the resolved order and mixed tail

Date: 2026-09-08

## Source-selected order

`the-source-authorized-joint-graph-selects-the-resolved-saturated-order.md` closes the order ambiguity found in the DAG search.  The source-authorized quadratic object is the joint comparison graph, which retains each typed coordinate with the direct-sum metric.  Therefore the wall/tail sector is

\[
R f=(f,Bf,M_\Phi f),
\qquad
G_{\mathrm{joint,res}}
=(1+M_\Phi^2)I+B^*B,
\]

before the history codiagonal.  The nonzero term `M_Phi(B+B*)` belongs to the downstream history observer and is not part of this resolved joint boundary Gram.

Thus the DAG preference for component retention agrees with independently recorded source authority; it is no longer merely an architectural suggestion.

## Mixed tail already evaluated

`the-resolved-four-matrix-unit-test-reduces-to-one-mixed-tail-pairing.md` is itself superseded by a later evaluation.  For `L=log p`,

\[
\beta_p
=\langle BW_L,BW_{2L}\rangle
=2\bigl(C_{K_+*\rho}(L)-C_{K_+*\rho}(3L)\bigr)>0.
\]

The ordinary window pairings also have the closed form

\[
\langle W_a,W_b\rangle
=2\bigl(R(a+b)-R(|a-b|)\bigr).
\]

Hence the complete positive resolved two-column Gram is analytically explicit:

\[
G^{\mathrm{res}}_{ij}
=(1+M_\Phi^2)\langle q_i,q_j\rangle
+\langle Bq_i,Bq_j\rangle.
\]

Its imaginary reciprocal orientation does not come from `beta_p`; that scalar is real and positive.  Orientation remains in the separately typed boundary-Stokes/Wronskian linking form.

## Existing seam/endpoint theorem

Prior research also proves that weighted Adams incidence, moving-seam transport, and complete wall--jump endpoint pushforward form an exact metric natural transformation with cutoff and four-Fourier-presentation compatibility.

The residual is one identification rather than a new continuity theorem: prove that the metric preserved by that natural transformation is exactly the resolved direct-sum metric on the three outputs

\[
f,\qquad B_Nf,\qquad M_\Phi f.
\]

Equivalently, write the typed intertwining diagram

\[
\mathcal R_NA_N
=
\bigl(A_N^{(0)}\oplus A_N^{(B)}\oplus A_N^{(W)}\bigr)\mathcal R_N.
\]

If this is isometric on the source-generated pre-Hilbert space, radical descent and extension to its completion follow automatically.  Closed range of the full source pushout remains separate.

## Updated frontier

The following earlier residuals are closed by prior work:

- choice between saturated and history forms;
- construction of the third wall port;
- cutoff preservation along the Adams grade ray;
- analytic evaluation and sign of the mixed resolved-tail entry;
- linear seam/endpoint naturality.

The first local missing theorem is now the exact three-output metric identification for the already constructed seam--endpoint word.  After it, the unresolved work is coefficient-side reproduction of the explicit mixed Green entry, boundary-Stokes/Wronskian normalization inside the same representation, fourth-grade compatibility, and full-pushout closed range.

## Evidence

- `research/nima/the-source-authorized-joint-graph-selects-the-resolved-saturated-order.md`
- `research/nima/the-resolved-four-matrix-unit-test-reduces-to-one-mixed-tail-pairing.md`
- `research/nima/the-existing-seam-endpoint-theorem-reduces-the-resolved-graph-gate-to-one-metric-identification.md`
- `research/nima/prime-diagonal-return-is-exact-before-codiagonalization-and-conditional-after-it.md`
