# Correction: the external Green comparison is a boundary-triple problem, not a direct intertwiner

## Withdrawn route

A proposed next step sought a nonzero intertwiner between the Euler phase connection and the shifted endpoint Laplacian.

Prior research already proves that this route is impossible.

The two operators have different differential orders and incompatible essential spectra. A Fourier-multiplier relation

\[
(\partial_x^2-	frac14)T_S
=T_S(-i\partial_t+V_{loc,S})
\]

forces \(T_S=0\).

Therefore no contractive direct intertwiner can identify the external Green endpoint row with an internal bulk row in this manner.

## Correct transverse structure

On the common spectral carrier, define

\[
X=M_t,
\]

\[
D_S=-i\partial_t+V_{loc,S}(t).
\]

The endpoint shifted Laplacian belongs to the multiplication direction:

\[
\mathcal F
(\partial_x^2-	frac14)
\mathcal F^{-1}
=-(X^2+	frac14).
\]

The Euler current belongs to the derivative direction. Their exact relation is

\[
[D_S,X]=-iI.
\]

Thus the mixed endpoint--bulk object is a Heisenberg/Clifford bicomplex, not a one-dimensional chain map.

## Four-component carrier

A source-typed total operator has the form

\[
\mathbb D_S
=
\gamma_1(-i\partial_t)
+
\gamma_2V_{loc,S}
+
\gamma_3X
+
\frac12\gamma_4.
\]

Its Clifford square contains separate channels for:

1. the gamma--prime current;
2. the endpoint Heisenberg orientation;
3. the positive oscillator bulk;
4. endpoint parity.

This carrier packages the two directions without falsely identifying them.

## External endpoint realization

The endpoint evaluation at \(t=\pm i/2\) is a boundary trace of the closed bicomplex. It is not an ordinary interior matrix coefficient.

Accordingly, comparison with the Green/Wronskian endpoint feature must be formulated through a boundary triple

\[
(\mathcal H_{bulk},\Gamma_0,\Gamma_1)
\]

for a closed realization of \(\mathbb D_S\), together with its Weyl function.

The required theorem is that the boundary/Weyl function reproduces the reflected contour differential and its endpoint residues.

## Existing signed identification

Prior contour work already proves that the spectral and graph Green pullbacks agree on the common source core. Hence the signed coupling is constructed and extends boundedly to the augmented carrier.

What remains is not equality of signed pullbacks. It is positive control of the single odd endpoint square.

## Pontryagin index

The endpoint swap form has one positive and one negative parity line. Therefore the natural boundary kernel has negative index one.

It belongs to a generalized Nevanlinna class of type \(N_1\), not an ordinary positive Herglotz class.

The positive completion problem is the reduction

\[
N_1
\longrightarrow
N_0
\]

under the physical source compression.

At block level, this is still the Schur inequality

\[
C_k\succeq b_kb_k^*.
\]

The boundary-triple formulation supplies the correct analytic carrier for that inequality; it does not make the inequality automatic.

## Vertical endpoint obstruction

Prior research constructs a sequence that tends to zero in the unaugmented physical bulk norm while retaining unit endpoint value.

Therefore the endpoint row cannot be dominated by the unaugmented prolate or Plancherel bulk.

The carrier must first be enlarged by the independent endpoint graph sector. Only on this augmented graph is endpoint evaluation bounded.

This rules out interpreting the derived internal bulk face as already containing the external endpoint feature.

## Joint-graph alternative

Prior source architecture permits retaining the internal positive endpoint realization and the external Green/Wronskian realization as two legs of one complemented joint graph over their common labelled source.

This closes faithful multi-rung typing without proving that their positive metrics agree.

A single-metric identification remains strictly stronger. The Schur gate belongs to that stronger interface.

## Revised status of the positive H134 claim

The three internal positive faces determine a derived internal \(H_{134}\) presentation. This is valid as a source-labelled positive chart.

However, strict analytic coherence does not identify its metric with the external Green/Wronskian endpoint metric. The vertical endpoint obstruction shows that no such domination can hold on the unaugmented bulk.

Therefore the internal positive filler does not prove the external Schur inequality.

## Most precise remaining theorem

On the endpoint-augmented bicomplex carrier, construct a boundary triple whose Weyl function:

1. has the reflected contour differential as its signed boundary value;
2. has the source-fixed endpoint swap residues;
3. is natural under prime and seam successors;
4. has exactly one negative square before odd-parity cancellation;
5. satisfies the rank-one Schur leverage bound after physical compression.

This is the correctly typed external realization theorem.

## Disposition

Prior research closes the direct-intertwiner route and supplies the replacement architecture:

\[
\text{Heisenberg/Clifford bicomplex}
+
\text{boundary triple}
+
\text{index-one Nevanlinna kernel}.
\]

The signed Green comparison is already closed. The remaining positive statement is index-one cancellation on the augmented carrier. It is not derivable from the internal positive lattice chart alone.
