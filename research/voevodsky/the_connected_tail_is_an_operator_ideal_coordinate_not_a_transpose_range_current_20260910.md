# The connected tail is an operator-ideal coordinate, not a transpose-range current

## Question

Does the connected prime-power tail require another boundary-functional transpose-range theorem after primitive and square provenance?

## Claim boundary

No. The source filtration places grades at least three in a trace-class or nuclear operator ideal, while only the first two grades are boundary cumulants. Asking the connected tail to lie in a boundary transpose range is a type error. This does not prove the completed determinant comparison or an external G4 identity.

## Problem

The provisional provenance list treated primitive, square, connected, and archimedean objects as if all were arithmetic rows requiring realization by analytic boundary functionals. The three-grade completion theorem distinguishes their modalities.

## Bold conjecture

Every arithmetic grade must be represented by a functional in the transpose range of the four-port Green synthesis.

## Named rivals

1. Grades at least three are ordinary trace-class return operators and belong to the analytic bulk ideal directly.
2. The connected tail is only Hilbert--Schmidt, so an order-three determinant is not defined.
3. Source-coordinate retention closes the joint graph without converting operator coordinates into boundary rows.

## Exact grade threshold

For the \(k\)-th prime traversal, the relative transfer estimate is

\[
\|p^{-k/2}c_{p,it}\|
\le
p^{-k/2}\sqrt{\log p}\,\|\phi\|_2.
\]

Hilbert--Schmidt control is governed by

\[
\sum_p p^{-k}\log p,
\]

which converges for \(k\ge2\). Trace-class control is governed by

\[
\sum_p p^{-k/2}\sqrt{\log p},
\]

which converges precisely for \(k\ge3\).

Therefore:

- grade one is a distributional or renormalized boundary current;
- grade two is Hilbert--Schmidt but not trace class;
- grades at least three form a trace-class connected tail.

The threshold is sharp over the declared source class.

## Correct codomain

Let \(K^{(k)}\) be the grade-\(k\) return block. The connected coordinate is

\[
K_{\ge3}=\bigoplus_{p}\bigoplus_{k\ge3}K_{p}^{(k)}
\]

in its trace-class or nuclear ideal, with compact-local parameter topology where declared. Its admissible operations are ideal operations:

\[
K\longmapsto WKW^{-1},
\qquad
K\longmapsto \det_3(I+K),
\]

not pullback of a boundary covector by \(U_4'\).

Length-preserving sewing preserves the trace and nuclear ideal norms. Prime and grade cutoffs converge in the ideal topology under the existing summability estimates.

## Joint graph placement

The retained source graph is

\[
\mathcal Jx=(x,A_{m prim}x,A_{m sq}x,K_{\ge3}x,Cx).
\]

The connected coordinate is a continuous map into its operator ideal. Retaining the source coordinate makes this a closed embedding. No transpose-range membership is needed for \(K_{\ge3}\).

Boundary traces may subsequently pair with a response built from this operator, but that is a separate typed composition. Such a pairing does not turn the operator itself into a boundary current.

## Determinant modality

The source filtration selects

\[
\det_3(I+K),
\]

with primitive and square factors retained separately as the first two cumulants. At finite stages,

\[
\det(I+K)
=
\exp\left(\operatorname{Tr}K-
\frac12\operatorname{Tr}K^2\right)
\det_3(I+K).
\]

Thus the connected tail is exactly the determinant-class remainder after the two boundary cumulants are removed.

## Strongest falsification attempt

Rival 2 is rejected by the convergent trace-class majorant for every \(k\ge3\). The bold conjecture is rejected because it erases the distinction between a covector, a Hilbert--Schmidt quadratic coordinate, and a nuclear operator.

What remains unproved is not connected-tail provenance. It is the completed determinant compiler: holomorphic ideal convergence, source-normalized first two cumulants, inverse-gap control, and multiplicity preservation.

## Disposition

The connected-tail transpose-range task is closed as ill-typed. Grades at least three already inhabit the analytic trace-class/nuclear coordinate of the retained joint graph. Primitive and square retain their separate dual and relative-determinant roles. The only remaining boundary attachment in the current four-item audit is the archimedean current.
