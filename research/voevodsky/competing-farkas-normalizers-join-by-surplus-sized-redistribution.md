# Competing Farkas normalizers join by surplus-sized redistribution

On separately rooted `-x<=0`, upper-A `x<=1`, upper-B `x<=1`, let N_A consume surplus c using (1,1,0), and N_B using (1,0,1). The signed bound-zero redistribution R_theta moves theta of multiplier from A to B, provided `-v<=theta<=u`. Exact packet algebra gives `R_c N_A(p)=N_B(p)`. More generally `N_B R_theta(p)=R_(c+theta) N_A(p)`, while `N_i R_theta=R_theta N_i` for i=A,B on well-typed packets. After public target weakening adds delta surplus, the required bridge parameter grows to c+delta. Fresh `check_redistribution_normalization_interchange.py` checks 72 exact rational squares, including 60 with positive surplus, and refuses transfer overdraw.

This gives a strong LOCAL answer to the competing-syzygy comparison question: the different zero-surplus proofs have an explicit bounded source-row redistribution, and its *packet-level* interchange with both normalizers can be replayed. It does not equate proof-cell histories. In a proposed enriched category, N_A, N_B and R are labelled candidate 2-cells; the displayed packet equations would require specified higher comparison cells if those paths are to be identified. The earlier free critical trace paths still need a separately sourced 4-cell under a proof-history-replay contract.

Programme status: (1) endpoint certificates admit typed source-rooted normalization for chosen positive-bound kernel, with scoped strict presentation isomorphisms and lax surplus-adding maps; (2) multiple kernels defeat global uniqueness, but this duplicated-row case has a compositional redistribution candidate; (3) fine proof history, source authentication and the analytic S,A,R,C,G role/edge/base map remain independent gates. The next substantive test is to type the *path* boundaries of the displayed mixed square and locate its first missing higher constructor, not to treat packet equality as a coherence witness.
