# Redundant-row retraction is not a proof inverse or analytic quasi-isomorphism

For K=[0,1] let source presentation rows be `-x<=0`, `x<=1`. Append `x<=2` as a redundant row. The Farkas add-row arrow has multiplier matrix M with rows `(1,0),(0,1),(0,1)` and surplus `(0,0,1)`; the drop-row arrow has N with rows `(1,0,0),(0,1,0)` and zero surplus. `NM=I_2`, but `MN` has third row `(0,1,0)` rather than identity row `(0,0,1)`. The composite surplus also records the redundant upper-bound difference. Thus the proof-presentation retraction is one-sided although the defined sets are equal.

It is possible to quotient an auxiliary linear row module by the redundant direction to repair the dimensional cokernel mismatch, but the positive Farkas cone and nonnegative surplus must also descend. No canonical graded differential, analytic cofiber or reciprocal mate has been specified by this quotient. Labelling the one-sided retraction an analytic quasi-isomorphism would bypass exactly the missing comparison. Fresh `python research/voevodsky/checkers/check_polyhedral_residue_coherence.py` passed the finite Farkas category's ten triangle identities, ten natural squares and 70 five-arrow parenthesizations; it rejects negative surplus and untranslated fine bounds. These checks certify the PROOF category's composition, not a promotion to closed graph-domain operator packages.

If Nima supplies a role-labelled analytic assignment, a decisive local test is to apply it to M,N and demand both (i) preservation of source-domain/base/surplus meaning and (ii) the ten-interval graded defect profile of the historical Waldhausen fixture. A functor treating MN as identity without an explicit proof-path quotient fails (i); one treating row cokernels as analytic defects fails (ii) under redundant row addition. No owner response or new analytic map is claimed.
