# The retained joint graph already supplies the intermediate theta domain; the open gate is external idelic identification

The previous iteration correctly rejected the rapid test rung as the state
space of the adelic theta source, but overstated the remaining internal domain
problem.  The retained analytical architecture already constructs the jointly
closed graph

\[
D_{\rm ret}
=
\overline{\{(x,A_1x,\ldots,A_mx):x\in D_0\}},
\]

whose coordinates include both bilateral Volterra histories, endpoint traces,
wall/jump splitting, ordered principal-value response, and the labelled prime
and grade attachments.

Since

\[
S_{\rm ord}=V_+-V_-=-2H_{\rm jump},
\]

the ordered current is a continuous coordinate of `D_ret`.  Reciprocal
half-density transport, Fourier/Hardy naturality, prime diagonality, cutoff
naturality, and primitive/square/connected/archimedean attachment provenance
are already closed on this graph.

Thus `D_ret` is the required intermediate distribution/graph domain for the
retained theta side.  No second internally defined `D_theta` should be
invented.

The unresolved datum is external identification.  One must compare:

1. the idelic source trace and its Fourier operator `J_0`;
2. the retained sewing and source-derived loading on `D_ret`;
3. the external `C34/G4` determinant and polarized-current normalization.

The full adelic source still does not map into the rapid observer test rung,
but this no longer blocks internal Green products: those are defined on
`D_ret`.  It blocks only the source-retaining comparison that would identify
the idelic and retained presentations.

Accordingly, the next object is a comparison span or natural transformation
from the idelic trace presentation to `D_ret`, preserving all labelled
coordinates—not construction of another graph topology.
