# Correction: the arithmetic sewing core exists; the missing edge is the tail-to-idelic-trace interface

## Subsequent refinement

The direct arrow `L_tail→tau` below is superseded by the source-retaining span in `the-tail-to-idelic-interface-must-be-a-source-retaining-span-not-a-boundary-inverse.md`. A direct arrow would require an unproved inverse to the tail boundary record. The retained conclusion here is only that the arithmetic core `J0` already exists.

## Correction

The coordinate `a=0` in `a-canonical-pre-g4-tail-edge-and-positive-green-face.md` is too coarse if read as saying that no arithmetic sewing map exists. The source-derived arithmetic core already exists algebraically on the injective idelic trace range.

Let

\[
\tau:\mathcal S(\mathbb A)\to\operatorname{Ran}(\tau)
\]

be the idelic boundary trace. Source injectivity makes the formulas

\[
J_0(\tau\phi)=\tau(\mathcal F\phi),
\qquad
J_0^{-1}(\tau\phi)=\tau(\mathcal F^{-1}\phi)
\]

representative-independent. Thus `J0` and `J0_inverse` are source-derived before scalar Mellin readout.

## Actual missing interface

The tail edge has codomain

\[
\mathcal B_{\rm tail}
=\mathcal B_{\rm in}\oplus\mathcal B_{\rm out}
=(H\oplus H)\oplus(\mathbb C^2\oplus H\oplus H).
\]

The arithmetic sewing core has domain \(\operatorname{Ran}(\tau)\). No current theorem supplies a map

\[
L_{\rm tail\to\tau}:\mathcal B_{\rm tail}
\longrightarrow\operatorname{Ran}(\tau)
\]

that simultaneously identifies the primitive, prime-square, seam, endpoint, connected-tail, and archimedean coordinates and preserves their declared pairings.

Accordingly, the desired arithmetic extension would factor as

\[
S_{\rm ar}=Q_{G4}\circ J_0\circ L_{\rm tail\to\tau},
\]

where \(Q_{G4}\) is the still-required typed G4 realization/readout. Neither \(L_{\rm tail\to\tau}\) nor an authoritative \(Q_{G4}\) is currently exposed. The algebraic existence of \(J_0\) does not define either one.

## Refined coherence coordinates

Use coordinates

- \(t\): analytic doubled-tail graph edge;
- \(g\): positive polarized Green face;
- \(j\): source-derived idelic Fourier sewing core \(J_0\);
- \(\ell\): typed tail-to-idelic-trace interface;
- \(q\): authoritative G4 realization/readout;
- \(c\): comparison with `T_PB`.

The current materialized coordinate is

\[
(t,g,j,\ell,q,c)=(1,1,1,0,0,\bot).
\]

Its minimal failure antichain is

\[
\{L_{\rm tail\to\tau},Q_{G4}\}.
\]

A finite matrix presentation of \(J_0\) is also unavailable, but this is not an existence obstruction to the algebraic core. It becomes an executability obstruction when one requests a finite residual checker. Such a checker requires at least six explicitly typed source basis functions, their trace coordinates, exact Fourier images, the source Gram matrix, and a cutoff-compatible graph projection.

## Nondefinition warning

A convenient discrete Fourier matrix, or a bijection between the six names on each side, cannot define \(L_{\rm tail\to\tau}\). It would provide a model or instrument control, not the source-derived comparison edge.

## Disposition

The positive tail face and arithmetic Fourier sewing are both formed, but in different typed fibers. The first genuinely unformed coherence edge is their interface \(L_{\rm tail\to\tau}\); the subsequent G4 readout remains independently absent.
