# A row-bound edit invalidates only supported packets mathematically but all old-source grants

Edit the y-upper primitive inequality from y<=1 to y<=2. A row-support index points to Q=(0,1,1,1) as the only one of three selected x<=2 proof packets using that row. Fresh `check_incremental_square_bound_edit.py` finds its implied bound increases from 2 to 3; no NONNEGATIVE surplus can restore the old exact bound 2 with Q's fixed multipliers. The P=(1,2,0,0) and one-row x-upper-with-surplus packets retain their exact local target equations. The old signed Q-P comparison acquires bound difference 1, so its zero-bound certificate fails.

This allows incremental LOCAL mathematical recomputation of row-dependent packets. In contrast the complete ordered SOURCE manifest changes for everyone: an exact old-manifest publication grant, if one existed, could not automatically cover even the mathematically unaffected packets. There is currently no real Farkas issuer grant at all. Mathematical support index and source authority dependency index have different granularity.

Next test a compensating source edit of TWO row bounds where the signed comparison's net bound returns to zero, but endpoint proof packets' individual bounds may drift. Detect cancellation masquerading as path validity; evaluate every path segment, not just final signed delta. Analytic S,A,R,C,G correspondence deferred.
