# Deleting coverage, origin or archive breaks different typed edges

The checked local mixed-law square has separate predicates: (1) owner-verified migration binding, (2) complete full-segment coverage, (3) live generation, (4) source-lift context, and (5) independent fine-archive permission. No single positive capability bit replaces the conjunction. A fresh hostile run of `check_unified_local_laws.py` now deletes coverage weights or changes the migration root BEFORE attachment; both fail and leave the session's head/descriptor unchanged. A separate independently bootstrapped session retains lifting but omits archive; its re-exposure fails without changing its handle. On the admitted advanced session, old-generation lifting, excluded-domain lifting and a nonpublic operation also fail. The owner-verified public update still has exact section formula `(u,u)->(u,0,0)` on `[0,3/4]`, and only its fresh archived context carries the new zero-inserted public frame.

This disambiguates three obstruction kinds: missing mathematical coverage, wrong source identity and missing operational capability. A proof that two sections agree extensionally cannot repair any of them. A rejected candidate does not publish a replacement head. The live tests and checks use the owner's full-segment and migration verifiers; they do not establish adversarial process isolation or physical provenance. The receipt fields record only local encodings and are not a total cost result.

Command: `uv run --with sympy python research/voevodsky/checkers/check_unified_local_laws.py`; result `research/voevodsky/results/unified-local-laws.json`. Six refusals are recorded. Next bounded verification: an independent packet-level replay of the frozen source, coverage and public operation with a separate expected statement, while retaining the distinction that live authority itself cannot be recovered from serialized packets. The scoped cost audit remains independent.
