# Correction: the all-jet Laplace readout is constructed before G4 identification

## Question

Is `all_jet_compatible_laplace_readout` itself absent, or is only its identification with the frozen G4 incidence port absent?

## Claim boundary

The readout is constructed on the rapid radial response graph. Every parameter jet is continuous, entire, shell-additive, and compatible with the source Green identity. What remains missing is a comparison map to a declared G4 port and a canonical arithmetic loading.

## Problem

The prior G4 comparison table marked the all-jet readout “not verified.” Fresh source review found an exact rapid-topology theorem and a source-derived jet recurrence. That field must be strengthened without promoting it to G4 identification.

## Bold conjecture

Only finitely many Laplace derivatives are controlled on the completed radial graph, so multiplicity jets require separate assumptions.

## Named rivals

1. The rapid radial topology controls every Laplace jet locally uniformly.
2. A formal jet recurrence exists but lacks continuity on completion.
3. The readout is complete analytically but cannot be called the G4 incidence coordinate without a comparison arrow.

## Rapid radial source

Let \(\mathcal E_{\rm rad}\) carry seminorms

\[
q_{A,N}(f)
=
\int_0^\infty e^{At}(1+t)^N|f(t)|\,dt,
\qquad A,N\ge0.
\]

Completed-theta autocorrelation, endpoint, and Wronskian coordinates belong to this source because their decay is superexponential.

Define

\[
\mathcal L_zf
=
\int_0^\infty e^{-zt}f(t)\,dt.
\]

For every \(j\ge0\),

\[
\partial_z^j\mathcal L_zf
=(-1)^j\int_0^\infty t^je^{-zt}f(t)\,dt
\]

and

\[
\sup_{|\operatorname{Re}z|\le A}
|\partial_z^j\mathcal L_zf|
\le q_{A,j}(f).
\]

Thus the full jet family is continuous and entire on one completed source topology.

## Four-coordinate radial probe

For

\[
x=(\rho,e,w,\rho(0)),
\]

define

\[
\Pi_zx
=
\bigl(\rho(0),\mathcal L_ze,
\mathcal L_zw,\mathcal L_z\rho\bigr).
\]

The radial Stokes law becomes

\[
z\Pi_z^{(\rho)}x-\Pi_z^{(0)}x
=
\Pi_z^{(e)}x-
\frac12\Pi_z^{(w)}x.
\]

This is an identity of entire functions, so every multiplicity jet follows by differentiation. Shell concatenation is preserved because \(\Pi_z\) is linear and the intermediate endpoint cancels before readout.

## Gaussian-pair recurrence

For each ordered Gaussian pair, the source vacuum equation yields

\[
2\pi\kappa I^{(j+1)}(z)
-zI^{(j)}(z)
-jI^{(j-1)}(z)
=
\delta_{j0}\rho(0)+\alpha F^{(j)}(z).
\]

Therefore the parameter jets form a triangular source-derived recurrence. They are not independent fitted shell conditions.

## Strongest falsification attempt

The bold conjecture is rejected by the estimate using \(q_{A,j}\) for arbitrary \(j\). Rival 2 is rejected because the same estimate proves completion continuity, not merely a formal recurrence.

Rival 3 survives. The existing packets explicitly withhold identification of \(\Pi_z\), its parameter-derivative operator, and the ordered ratio-cocycle correction with a frozen G4 port.

## Oriented correction

Ratio translation of an ordered pair produces

\[
R_{nm}(z)
=(nm)^{-1/2}
\left(\frac mn\right)^z
\bigl(H(z)-J(z;d_{nm})\bigr).
\]

The finite oriented segment \(J\) is a genuine boundary cocycle of one-sided Laplace readout. A G4 comparison must retain it; a scalar Mellin character alone is insufficient.

## Disposition

The `all_jet_compatible_laplace_readout` capability is constructed on the rapid radial graph. The corrected G4 status is “constructed analytic source map; external port identification open,” not “unverified.” The remaining external blockers are the outdated three-port/Hilbert contract, the absent canonical arithmetic loading arrow, and the missing comparison that maps the rapid four-coordinate probe including its oriented ratio cocycle into G4.
