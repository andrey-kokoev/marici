# Characteristic-zero comparison synthesis correction

## Corrected result

Milnor residue, de Rham coefficient, Betti period, Tate pairing, Parshin flags, Cech monodromy, and character residue still detect the same normalized primitive class after orientation choices.

This agreement does not prove that `(Xi_log,-sigma123)` is nonexact in a comparison total complex. It instead motivates an explicit chain-level comparison: the two entries may represent the same class in different resolutions, and their difference may be a cone boundary.

The prior conclusion “no compatible horn exists” is withdrawn. What remains proved is that the rank-26 p-normal source has zero image and that `{u,v}` is degree two. A cone homotopy, if it exists, must be independently shown to arise from the admitted source rather than from a tautological abstract cone.

## Disposition

Define the graded flag-to-log comparison and distinguish abstract cone acyclicity from a sourced horn constructor.
