# Prior results construct the linear odd four-port route on a retained graph

## Question

Is the additive-Tate-to-boundary odd comparison still missing after the valuation-only no-go, or do later packets construct it through the required larger source carrier?

## Claim boundary

Later prior results construct the linear odd route through additive differentiation, a discrete valuation-shell Green observer, exact wall--jump comparison, and a unique normalized theta-to-window odd compression. The completion-stable quadratic object is the retained graph, not an isometry between output metrics. This review does not claim a completed inverse or identify every update lineage.

## Source route

The valuation-only route remains impossible. Later work bypasses it without reversing that result.

### Additive parity incidence

On logarithmic half-densities, additive differentiation becomes

\[
P=e^{-q}\left(\partial_q-\frac12\right),
\]

and maps the even additive Tate port to the odd port. It satisfies the exact Fourier square

\[
K_-P=2\pi i\,M_{e^q}K_+.
\]

Thus odd phase is generated before valuation projection.

### Discrete shell observer

For the valuation-shell backward shift \(B\), radialization \(\mathcal R_p\), and endpoint evaluation \(E_0\), the local Tate functional has the Green realization

\[
Z_p(f,s)=E_0(I-p^{-s}B)^{-1}\mathcal R_pf.
\]

The parity-changing source incidence gives the odd mixed return

\[
c_p(s)
=
E_0(I-p^{-s}B)^{-1}\mathcal R_pP_\eta f_\eta^{\rm odd}.
\]

This closes the odd Tate port to the discrete valuation-wall observer.

### Continuous boundary comparison

The later Hadamard wall--jump frame identifies the local and global boundary cylinders before scalar readout. Its even retract and odd jump coordinate are exact, prime-labelled, cutoff-natural, and zero-radical. This supersedes the earlier statement that the discrete-to-continuous wall comparison remained missing.

### Unique odd compression

On the theta endpoint plane, let

\[
e_{\rm wall}=\frac1{\sqrt2}(1,1),
\qquad
e_{\rm jump}=\frac1{\sqrt2}(1,-1),
\]

and

\[
j_\theta=\left(\frac14,-\frac14\right).
\]

The normalized odd covector \(\ell_{\rm jump}\) is fixed by

\[
\ell_{\rm jump}(j_\theta)=1,
\qquad
\ell_{\rm jump}(e_{\rm wall})=0.
\]

For the independently constructed Stieltjes disagreement \(d_p\), define

\[
K_p^{\rm odd}=d_p\otimes\ell_{\rm jump}.
\]

It is the unique linear map that intertwines reciprocal reflection, annihilates the even wall line, and sends \(j_\theta\) to \(d_p\). Causal evaluation fixes the same map with the forced normalization

\[
d_p=-2j_\theta b_p.
\]

The prime direct sum \(\bigoplus_pK_p^{\rm odd}\) is trace class in the constructed direction. Its inverse exists on each finite odd line but is not admitted after completion because its norm grows too rapidly.

## Correct quadratic object

Prior research explicitly rejects output-metric isometry. The induced covariance

\[
K_pG_{\theta,p}K_p^*
\]

is a positive rank-one shadow on the window carrier, but it need not equal the independently declared window Green metric. Conversely, raw pullback energy collapses with prime and cannot equal the fixed theta odd energy uniformly.

The completion-stable object is therefore

\[
\Gamma_p^{\rm odd}
=
\{(y,K_p^{\rm odd}y):y\in E_{p,\theta}^{\rm odd}\},
\]

with graph form

\[
G_{\Gamma,p}(y)
=
G_{\theta,p}(y)
+
G_{\rm win,p}(K_p^{\rm odd}y).
\]

This retains source energy and the trace-class output shadow simultaneously.

## Consequence for the four-port observer

The odd seam coordinate is reachable through a source-derived linear chain once the additive Tate carrier is retained before radialization. The relevant four-port incidence is not a direct map from bare valuation labels and not four identical copies of one Hilbert packet. It is a stratified retained graph combining:

- even valuation/theta incidence;
- additive parity conversion;
- discrete shell Green observation;
- continuous wall--jump comparison;
- unique odd endpoint compression.

The four-port kernel classifier \(h_4\) acts after this incidence. It classifies invisible boundary states but does not replace the incidence chain.

## Remaining limitation

The constructed odd route is one-way at completion. Its nonexistence of a bounded inverse forbids reconstruction of the theta source from the soft window output alone. Any observer theorem must retain the theta coordinate in the joint graph.

Update lineage is partially represented by the explicit source operation chain, but a complete coalgebra theorem would still need to show that every admitted constructor transports this graph and its four-port pairing naturally.

## Disposition

The earlier missing linear odd comparison is superseded. The source route exists and is completion-stable in the forward, retained-graph direction. The next nonredundant gate is functorial preservation of this four-port retained graph under the admitted constructor family, not construction of another output-only inverse or metric isometry.
