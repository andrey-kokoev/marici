# Integrated local architecture checked; analytic role identification withheld

## Two distinct conclusions

**Local, bounded success.** On one independently owner-verified rank-one migration, public evidence `u<=3/4`, full-domain section and restriction, live provider substitution, archived fine re-exposure, stale handle refusal, and typed answer/lift/archive services compose under one fixed source binding. Direct and staged whole-section comparisons agree on their specified scope. Independent semantic replay verifies the public optimum `3/4`, source attachment coverage and archived lifted frame; separate live sessions exercise authority and capability refusals. Three-provider subset enumeration gives a 3,658-byte minimum ONLY for the frozen receipt-field scalar. The independent cost ledger explicitly withholds total heap and work, and sensitivity reverses receipt preference when a normalized omitted differential exceeds 180.

**Analytic identification unsupported.** The independent 638-state acquisition/issuance graph is a DIFFERENT source carrier. The analytic `[S,A,R,C,G]` target has R as its fifth vertex and corrected base b_z as a retained base, not a vertex. Its historical closed-operator fixture has ten graded interval defect profiles; the first edge has one-dimensional cokernel, whereas a simplex-face cone is acyclic. No authoritative source-derived role/edge/base functor maps these data to the rank-one polyhedral/provider objects, respects reciprocal mates and retained base, and preserves those defects. Farkas row redundancy refutes naive raw-matrix cokernel transport. Nima was sent the exact role-map request; absent an owner-supplied mapping, analytic correspondence remains a typed reopening gate rather than an implied theorem.

Fresh verification in this turn reran the independent local unified-square, integrated-local, cost-ledger, finite-bundle and cost-sensitivity replay checkers, then `check_integrated_architecture_boundary.py` bound their current result artifacts by digest and checked compatible migration IDs and answer values. The boundary checker is a conjunction of previously admitted bounded checks, not a newly independent proof of all their kernels or a cross-sector functor. Result `results/integrated-architecture-boundary.json` records the positive local conjunction and negative analytic status separately.

The parent objective can be disposed only if its terminal semantics allow **scoped success plus typed obstruction**. If it requires an actual analytic identification, it remains open/deferred pending the requested map; calling the local packet a proof of the full five-role programme would be false. A next executable, nonredundant task is to freeze an explicit interface for the missing role-to-operator functor and test a small candidate against the first nonzero analytic defect without assuming that one exists. This cannot be solved by renaming vertices or summing encoding bytes.
