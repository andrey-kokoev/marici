# A source path can reverify on a different source without transporting authority

An 'irrelevant single-row edit' cannot be assumed for the P->Q->P loop: its signed edge vector involves all four primitive rows. Instead change bounds COUPLED so that their contributions cancel on BOTH P=(1,2,0,0) and Q=(0,1,1,1). Replace the unit-square bounds (0,1,0,1) by (-2,2,-1,1), leaving normals fixed. The new source is the singleton (x,y)=(2,1), not the old `[0,1]^2`. Nevertheless both Farkas packets still prove x<=2 and the signed P->Q vector still has normal and bound zero. Fresh `check_path_valid_across_source_identity_change.py` verifies these exact equations and the changed manifest digest.

Thus mathematical PATH re-verification can succeed on a substantially different source. An old-root record is still refused for new-source use (`REQUIRES_EXPLICIT_SOURCE_REBIND`); even after rebinding and arithmetic verification, live authority remains absent (`NO_LIVE_SOURCE_AUTHORITY`) until an independent owner admission. Matching proof vectors cannot establish source-set equality, actual history, or permission to operate. This is the opposite hostile from the y-high edit, where endpoint vectors survived but path arithmetic did not.

The local source-edit investigation now has both separating examples: (i) unchanged endpoints with broken path, (ii) valid path equations with changed source identity. Next synthesize a two-axis decision gate (mathematical proof/path validity versus source identity/authority) that refuses promotion in either direction, then proceed to a separate nonredundant programme branch. The analytic S,A,R,C,G comparison remains deferred.
