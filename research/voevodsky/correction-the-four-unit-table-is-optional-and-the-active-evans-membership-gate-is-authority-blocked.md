# Correction: the four-unit table is optional and the active Evans membership gate is authority-blocked

## Question

After adopting the faithful retained joint graph, is the four-unit single-metric table still an active obligation, and can the unchanged Evans membership condition be tested from current repository state?

## Claim boundary

The four-unit identity

\[
G_p^{\rm St}=Q_p^{\rm lin*}G_p^\theta Q_p^{\rm lin}
\]

is required only for optional source-forgetting descent to one metric. The adopted constructor retains both realizations on a common-source direct-sum graph. Consequently the packet

`research/voevodsky/the-four-polarized-prime-cell-tests-stop-at-the-missing-theta-history-mixed-form.md`

is a correct blocker for that optional descent but not the active G4 gate.

The active condition is

\[
\Delta^\top|_{\mathcal E_{\rm Ev}}=0,
\qquad
\Delta=U_{\rm G4}-T_{\rm PB}.
\]

## Fresh source audit

The repository constructs `T_PB` as a typed analytic pair-to-bordered crossing. It does not expose an independently authoritative `U_G4` on the same source and target pairings. Missing data are:

1. the owner-authorized G4 forward carrier;
2. exact codomain and basis order;
3. nondegenerate source and target pairings;
4. the arithmetic/radial forward incidence formula or matrix;
5. proof that the Evans lower row is the corresponding contragredient;
6. independent derivations of the map entries.

The existing transfer record explicitly forbids renaming the analytic surrogate `T_PB` as `U_G4`. Therefore \(\Delta\) cannot be formed from two independently defined maps, and its restriction cannot be evaluated.

## Exact surviving residual

The unchanged-Evans membership has a separate scalar falsifier. At the first critical-line zero, existing high-precision computation gives

\[
|\Xi(1/2+it_1)|\approx5.95\times10^{-44},
\]

while the unchanged-Evans adjoint Hilbert residual stabilizes near

\[
-0.1508512190587392007.
\]

This is not interval-certified, so it does not yet reject membership as a theorem. It is hostile evidence and supplies the exact residual to preserve while the source comparison is blocked.

## Acceptance test

The source-comparison branch reopens only when an independently exposed `U_G4` supplies the six typed items above. The first executable test is then

\[
\langle e,(U_{\rm G4}-T_{\rm PB})x\rangle_B=0
\]

for generators \(e\) of the Evans cyclic subspace and pair-source generators \(x\), followed by closure in the declared dual/graph topology.

The independent falsifying branch reopens when validated complex-ball evaluation can enclose the first-zero residual away from zero.

## Disposition

Freeze the active source-membership branch at the absent authoritative `U_G4`. Do not continue the obsolete single-metric table, identify `T_PB` with `U_G4`, or infer unchanged-Evans membership from maximal-isotropic sewing on the source-generated essential image. No RH implication is established.
