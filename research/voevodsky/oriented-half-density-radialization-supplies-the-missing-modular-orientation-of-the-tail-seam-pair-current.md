# Oriented half-density radialization supplies the missing modular orientation of the tail--seam pair current

## Question

Prior research constructs the pair-label current \(K=U^*U\) but leaves its modular orientation under simultaneous prime transport unresolved. Does the source-derived additive-to-log-radial half-density chart provide the missing comparison?

## Claim boundary

Yes on the labelled pair test/dual carrier. The unitary Jacobian factor \(r^{1/2}\) acts on both legs of the Gram current and produces the previously proposed weight \(\sqrt{nm}\) canonically. Simultaneous multiplication by \(p\) then has covariance factor \(p\). This closes the additive-Haar to multiplicative-Haar orientation of the pair current, but not owner identification of the resulting pair response with the G4 arithmetic adjoint.

## Existing pair current

Let \(u_n\) be the source tail--seam atom at logarithmic label \(\log n\), and let

$$
Sc=\sum_nc_nu_n.
$$

Its Gram current is

$$
K=S^*S,
\qquad
K(n,m)=\langle u_n,u_m\rangle.
$$

Translation covariance gives

$$
K(n,m)=A\left(\left|\log\frac nm\right|\right).
$$

Hence

$$
K(pn,pm)=K(n,m).
$$

This supplies pair observability but no modular factor.

## Source-derived half-density chart

The oriented radial unitary is

$$
(Cf)_\epsilon(u)
=e^{u/2}f(\epsilon e^u).
$$

The factor \(e^{u/2}=r^{1/2}\) is forced by

$$
dx=e^u du.
$$

It is therefore not an arbitrary arithmetic reweighting. On a positive labelled atom at radius \(n\), its amplitude is multiplied by \(n^{1/2}\).

Let

$$
D_{1/2}e_n=n^{1/2}e_n
$$

on the algebraic labelled test core, with contragredient action on the dual.

## Oriented pair current

Transport both Gram legs:

$$
K_{1/2}
=D_{1/2}KD_{1/2}.
$$

Its matrix coefficients are

$$
\boxed{
K_{1/2}(n,m)
=\sqrt{nm}\,
A\left(\left|\log\frac nm\right|\right).
}
$$

This is exactly the weighted kernel previously proposed but not source-authorized.

It is now authorized as the pullback of the additive \(L^2(dx)\) Gram through the unitary log-radial half-density chart, rather than as a new energy assigned by hand.

## Prime covariance

Under simultaneous prime transport,

$$
K_{1/2}(pn,pm)
=\sqrt{p^2nm}\,K(pn,pm)
=pK_{1/2}(n,m).
$$

Thus

$$
\boxed{
K_{1/2}(pn,pm)=pK_{1/2}(n,m).
}
$$

The missing modular orientation is exactly the Jacobian character of additive-to-multiplicative radialization.

## Orientation and Fourier transport

Keeping positive and negative radial channels separate retains slot reversal and the \(\pm i\) sectors. The oriented Hankel quarter turn and its Tate gamma matrix transport \(K_{1/2}\) contragrediently; absolute radialization would again collapse the odd linking orientation.

Hence the pair current is compatible with the already proved semilocal Fourier sewing rather than merely with the half-turn quotient.

## Topology

Although \(D_{1/2}\) is unbounded on an unweighted label Hilbert space, it is continuous between the declared projective exponential seminorms:

$$
q_\delta(D_{1/2}c)
\le q_{\delta+1/2}(c).
$$

Thus the comparison is valid on \(\mathcal A_{\exp}\) and extends contragrediently to its strong dual. No fixed-rung bounded equivalence is asserted.

## Pair-source consequence

The source chain now has a canonical linear pair-current route:

$$
\text{labelled Fock packet}
\xrightarrow{S}
\text{tail--seam synthesis}
\xrightarrow{S^*S}
K
\xrightarrow{D_{1/2}(\cdot)D_{1/2}}
K_{1/2}.
$$

Polarization makes this linear on the density/Fock-square carrier. Ordered pair, ratio, prime, grade, and shell labels remain intact.

## Remaining G4 crossing

This construction supplies the previously missing additive-to-multiplicative Haar comparison and modular orientation. To close the arithmetic return one must still show that the declared G4 pair incidence sends \(K_{1/2}\) to the bordered radial Stokes packet

$$
R+2E
=\frac{\rho(0)+E-W/2}{z}+2E
$$

with the frozen analytic-transpose sign and endpoint attachment. That is now a comparison of two explicit source constructions, not an unknown weighting.

## Disposition

The tail--seam pair current has a canonical modular orientation: oriented half-density radialization forces \(K_{1/2}(n,m)=\sqrt{nm}K(n,m)\) and the covariance \(K_{1/2}(pn,pm)=pK_{1/2}(n,m)\). The prior half-density gauge obstruction is closed on the projective test/dual carrier. Only the final pair-current-to-bordered-G4 response crossing remains.