# The completed theta orbit supplies the valuation-labelled reciprocal incidence map

## Question

Does an existing source constructor attach each primitive prime label to both reciprocal half-density history channels and complete in the seam-weighted arithmetic norm?

## Claim boundary

For each prime \(p\), put \(L_p=\log p\) and let \(\Phi\) be the completed bilateral theta forcing with \(R_H\Phi=\Phi\). Define the doubled prime signal fiber \(\mathcal S_p=\mathbb Ce_{p,+}\oplus\mathbb Ce_{p,-}\) and the local source incidence by \(\mathcal I_p(c_+,c_-)=p^{-1/2}(c_+\tau_{L_p}\Phi,c_-\tau_{-L_p}\Phi)\).

Every factor is source-located: \(L_p\) is the primitive valuation length, \(p^{-1/2}\) is the primitive Euler half-density, \(\tau_{\pm L_p}\) is Mellin translation, and the two signal lines come from reciprocal source doubling. Thus \(\mathcal I_p\) is the required primitive-label-to-doubled-history attachment.

Let \(R_Ue_{p,+}=e_{p,-}\) and \(R_Ue_{p,-}=e_{p,+}\). Since \(R_H\tau_{L_p}\Phi=\tau_{-L_p}\Phi\), the local source covariance is \(R_H\mathcal I_p=\mathcal I_pR_U\).

Use the completed arithmetic source norm \(\|x\|_U^2=\sum_p\log(p)(|x_{p,+}|^2+|x_{p,-}|^2)\). Translation is isometric in the object-indexed transported history topology, so the two columns at prime \(p\) each have squared norm \(p^{-1}\|\Phi\|_{\mathcal E_w}^2\). Relative to the normalized source basis, the completed direct sum satisfies \(\|\mathcal I\|_{\mathrm{HS}}^2=2\|\Phi\|_{\mathcal E_w}^2\sum_p1/(p\log p)<\infty\). Hence \(\mathcal I=\bigoplus_p\mathcal I_p\) is a bounded prime-diagonal map on the completed seam-weighted source.

Composing \(\mathcal I\) with the closed reciprocal Volterra/cut lift gives the positive and negative wall columns in \(H_{\mathrm{wall,rec}}\). Write the unoriented source columns as \(B_+^{\mathrm{src}}e_{p,+}=p^{-1/2}c_{\log p}\) and \(B_-^{\mathrm{src}}e_{p,-}=R_HB_+^{\mathrm{src}}e_{p,+}\). Define the physical negative orientation by \(B_-=-B_-^{\mathrm{src}}\). Then \(R_HB_+=-B_-R_U\) holds columnwise and on the completed source.

The exact endpoint trace of \(\mathcal I_p\) is \(V_p=\frac12\begin{pmatrix}1&p^{-1}\\p^{-1}&1\end{pmatrix}\), so the source attachment agrees with the uniformly faithful endpoint observer used by the central analytic-interface tetrahedron.

This construction attaches primitive valuation labels to reciprocal history channels. Euler cyclic feedback and the determinant-line return remain separate structures because feed-forward incidence does not create cyclic traces.

## Disposition

Gate 2.A and Gate 2.C are complete on the primitive seam-weighted source. The negative channel is independently generated by the translated completed-theta orbit, the completed direct sum is Hilbert–Schmidt, and the physical orientation convention yields the required signed covariance. APCT now has all analytic and source faces; its tetrahedral filler reduces to compatibility of the displayed local identities under direct-sum completion, which follows from prime diagonality and the convergent Hilbert–Schmidt sum.
