# Full declarative template manifest

Completed the remaining47 query/copy/eraser, update/union, cursor-preparation and scanner templates in `remaining_rule_templates.py`; the installed manifest now covers all57 admitted typed cases. This is hand transcription grouped by data-head cases, not runtime extraction of expected production certificates. It is not independent external authorship or proof of intended semantics.

`check_full_certificates.py` exercises every admitted principal pair with passive outside leaves, then composes exact allocation-slot template matching, typed/high-water checks and the separate pure replacement evaluator. All57 cases match exactly and agree with production output dictionaries. Unknown rules remain fail-closed. Existing mixed template test now reports its coverage subset rather than incorrectly demanding that its small corpus witnesses the entire manifest.

This closes the finite template transcription task, but not arbitrary context validation: these57 fixtures have separated outside leaves. Next run the composed certificate pipeline over whole reachable mixed executions, including cyclic contexts and splices, without skipping any replacement; add it and the pure-reference tests to the standard closure. Historical allocator authenticity and phase correctness remain separate premises, and the production mutator remains unchanged.
