# Strict source-enlargement factorization

## Question

What is the minimal extension adjoining the geometric `HomotopyLift` without altering rank-26 absorption?

## Claim boundary

Let `R` be the rank-26 p-normal relation complex. Let `A` be the two-term complex generated by `Gamma` in degree one and `z` in degree two, with

`d Gamma=z`.

Map `z` to the fully decorated pair `(Xi_rel,-sigma123)` and `Gamma` to `tau`. The geometric ambient star supplies this map independently.

The minimal enlargement is

`E=R direct_sum A`.

The summand `A` is contractible, so `H(E)=H(R)`. The differential, quotient, and absorption theorems on `R` are unchanged. Nevertheless, the pointed chain complex has a new specified nullhomotopy capability; no isomorphism fixing `R` can discard its geometric pointing and realization.

The boundary `z` is the signed comparison-pair datum, not the motivic class `{u,v}`. Adjoining `A` does not make the symbol exact.

## Disposition

The geometric horn is the minimal acyclic pointed enlargement of the absorbed source. The next leaf extends ambient-degree transports over this summand and tests functorial colimit coherence.

## Verification

- `research/voevodsky/check_cosmology_strict_source_enlargement_factorization.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
