# Three checked meshes compose on the whole section, not just at samples

The owner-local `check_whole_section_comparison.py` bootstraps three independent sessions from one verified forced-zero migration and attaches meshes `(0,1)`, `(0,1/2,1)`, `(0,1/4,3/4,1)`. Every attachment passes the source-bound full-segment verifier: vertex source admission and exact coverage Farkas implications; affine interpolation on every cell realizes `(u,u)->(u,0,0)` for ALL `0<=u<=1`. The direct comparison 0->2 equals the staged 0->1->2 comparison as an extensional whole-domain section with the same owning root and scope; this says nothing about equality of their section digests or execution histories.

Fresh run `uv run --with sympy python research/voevodsky/checkers/check_whole_section_comparison.py` passed. Missing coverage, negative coverage coefficients, foreign root and wrong source lift were rejected by the attachment verifier before comparison. After one owner-checked public advance, comparison through its captured old handle was rejected while comparison of unaffected live providers survived. Rejections do not publish a new head. The result is `results/whole-section-comparison.json`.

The full-segment proof uses the established source and coverage verifier rather than independent reimplementation; it is a controlled rank-one result and not a general triangulation transport theorem. Provider authority is checked, not transported. Restricted successor domain comparisons, arbitrary face-sharing meshes in higher dimension, and physical provenance remain open only as conditional generalizations. SCC classification: static full coverage and vertex/interval incidence passed; route/coherencer compatibility passed extensionally for this three-mesh triangle; authority transport unsupported.
