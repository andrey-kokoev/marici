# A single migration integrates public operations, live resources and checked sections

The owner-local `check_integrated_local_continuation.py` consumes a checked two-provider same-base session and tests the same real rank-one forced-zero migration through one public addition `u<=3/4`. The owning migration verifier checks projection certificates, each full-segment attachment checks source vertices and exact coverage, and the advance checks its independently expected public query. On the shared interval the old and new live section formulas remain `(u,u)->(u,0,0)`; the narrowed provider rejects u=1. The untouched live provider supplies a valid replacement lift. The advanced provider's OLD handle is refused for both lifting and archive, while its fresh archive re-exposes original fine evidence conjoined with u<=3/4. The untouched archive remains unrestricted: source equality does not move new history to it.

Fresh command `uv run --with sympy python research/voevodsky/checkers/check_integrated_local_continuation.py` passed, including excluded public point, nonpublic frame, stale-provider and stale-archive controls. It records both providers' receipt-level byte accounts; shared code, complete archive proof costs, approval process, and repair history remain outside those byte fields. The helper imports and executes the independently checked two-session fixture; it is NOT an independent verifier implementation. Typed source identity and public formula agree for this owner migration only, not between the finite coupled acquisition protocol and the polyhedral source.

This is a bounded LOCAL integration of operations, section coverage and resource/capability premises. It explicitly excludes any analytic S,A,R,C,G identification. The analytic handoff to Nima has been sent, and the correspondence remains deferred pending a source-derived role-to-operator map. Neither the pending owner answer nor a globally minimal retention algorithm is needed to verify this local target. Further integration should separate executable local success from unresolved analytic objectwise identification in the parent objective.
