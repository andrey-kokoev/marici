# Theta–Clark cross-sector admission audit

## Question

Does Kitaev's existing theta–Clark seam pro-Gram result define a second sector vertex in the coherence-pyramid nerve?

## Claim boundary

This audit preserves the realized constructor subset and distinguishes it from a full computad representation.

## Realized subset

The result constructs and controls:

- seam retention `S`;
- continuous forcing reservoir `R_f`;
- doubled Clark–Green operation `D`;
- source-derived q-flow graph features;
- a source-derived independent seam summand;
- exact braid residual zero on the authorized subsystem.

These are nontrivial local constructors. They are not erased by the failed full synthesis.

## Admission failure

The full sector vertex is blocked at its first source-incidence boundary. The missing maps are

\[
P_X:\text{valuation/Fock}\longrightarrow\text{boundary}
\]

and

\[
Q_X:\text{valuation/Fock}\longrightarrow\text{boundary}.
\]

The archimedean row, full mixed Green matrix, and common finite dynamics are also absent. Consequently no single common \(J_X\) matrix is constructed.

A list of constructors in one subsystem does not define their incidence into the common five-sort computad. The record is therefore retained as `constructor_subset_only`, not admitted as a sector vertex.

## Disposition

The expanded census has four candidates but still one admitted vertex, no cross-sector edges, cycle rank zero, and no mixed horns. The theta–Clark branch may reopen only when the two boundary incidence maps are source-derived; further local constructor elaboration does not alter admission.

## Verification

- `research/voevodsky/cross-sector-bridge-admission-registry-v2.json`
- `research/voevodsky/checkers/check_cross_sector_bridge_admission_v2.py`
- `research/voevodsky/results/cross_sector_bridge_admission_v2.json`
