# Repair rank-26 aggregation gate

## Question

Can the exact relation-family theorem be retained without promoting a diagnostic DNC gate into a geometric comparison theorem?

## Claim boundary

Yes. Aggregate schema v2 consumes only the exact IBP, K, and q dependencies. It retains the all-even-degree relation-presentation absorption result.

The aggregate now marks the supported DNC comparison `unverified` and names five required fields: supported target, chain map, support localization, differential commutation, and factorization. It emits no DNC-image or horn consequence.

The companion audit injects the former unsupported `DNC_consequence`; the repaired contract rejects that deliberate mutation.

This repair does not construct the missing comparison. Every downstream `Xi`, `omega`, or horn no-go claim depending on that map remains withdrawn or conditional.

## Disposition

The semantic aggregation defect is repaired and verified. The next leaf must construct the supported rank-26 comparison rather than infer it from relation absorption.

## Verification

- `research/voevodsky/check_cosmology_full_rank26_characteristic_zero_absorption.py` — exit 0
- `research/voevodsky/results/cosmology_full_rank26_characteristic_zero_absorption.json` — schema v2
- `research/voevodsky/check_cosmology_supported_comparison_to_rank26.py` — exit 0, deliberate mutation rejected
- `research/voevodsky/results/cosmology_supported_comparison_to_rank26.json` — schema v2
