# Two redundant rows compose with labelled surplus, not an unlabelled total

For the fixed interval `0<=x<=1`, append two labelled redundant rows `a:x<=1+k` and `b:x<=1+l`, k,l>=0. The actual Farkas arrow composition `(N M,N c+d)` gives four-row multiplier rows `(e1,e2,e2,e2)` and retained nonnegative surplus `(0,0,k,l)`. The opposite append order yields `(0,0,l,k)` with the `a,b` row positions exchanged. An EXPLICIT row-label permutation identifies the final proof presentations and their full surplus vectors; raw ordered packets are not equal. The direct and both staged routes agree after this declared permutation. Fresh exact-rational `check_two_row_surplus_composition.py` passes all 16 tested margin pairs and checks the matrices, not merely row lists.

An unlabelled total `k+l` is insufficient: `(k,l)=(0,2)` and `(1,1)` have the same total surplus and coarse interval but different labelled proof presentations. A retained TWO-coordinate labelled grade restores this specific append-only redundant family; ordinary coarse forgetting loses it. The control is fixed-chart, same-normal, nonnegative redundant additions. It proves no universal minimum-storage lower bound, nor analytic closed-cone or S/A/C/G correspondence. Deleting any retained margin can destroy precise re-exposure of the proof even while public answers are unchanged.

This closes the original syzygy question for its frozen one- and two-row fixtures: naive quotients fail, while an explicit retained-surplus decoration supports staged composition only with proof-row labels/permutation. A broader successor would have to specify how positivity, surplus and labels compose under arbitrary Farkas arrows or source chart changes, not extrapolate from these two repeated normals.
