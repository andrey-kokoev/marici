# Current retained-architecture terminal frontier

Date: 2026-09-08

## Closed or closure-candidate constructor layers

After reconciliation with the newest retained-graph packets:

- G1.1--G1.4 are coherent closure candidates on the labelled source-retained
  saturated graph;
- G2 is a closure candidate for the actually admitted operations: labelled
  direct sum and strict within-prime Adams reindexing;
- G3 has five positive uniform margins on the full-vector pre-scalar Green
  architecture.

None of these statements descends to an output-only codiagonal or terminal
scalar projection, and no ledger status is changed here.

## Exact G4 object

Let `tau` be the Xi determinant-line section, let `u_hat(z)` be the entire
split Evans history, and define the entire arithmetic residual

\[
r_U(z)=\mathcal B_G^\dagger\widehat u(z).
\]

A divisor-preserving chain promotion into the conservative Green pencil exists
locally exactly when

\[
r_U\in\tau\,\mathcal O(U_{\rm ar}).
\]

At a zero `z0` of order `m`, this is equivalent to

\[
r_U^{(j)}(z_0)=0,\qquad 0\le j<m.
\]

Thus multiplicity preservation is neither a determinant normalization nor a
zeroth-order kernel test.  It is the complete vector-valued residual-jet
family.

## Why G3 cannot close G4

The G3 margins control only the complement of the Xi defect.  Applying them to
the full G4 pencil would make it uniformly invertible and therefore unable to
carry the Xi divisor.  The required local normal form is

\[
C(s)\sim
\begin{pmatrix}a(s)\tau(s)&*\\0&C_Q(s)\end{pmatrix},
\]

with only `C_Q` coercive.

The triangular compiler already preserves the Xi divisor algebraically, but it
does not produce a conservative Green-domain state.  That requires a
source-derived chain map and a contractible complement.

## RH-strength boundary

For the natural nonzero history, full residual vanishing on every Xi zero
implies critical-line confinement by the Green identity.  Even the required
real forcing-pair cancellation is already RH-equivalent on represented zeros.
Therefore proving divisibility by imposing positivity only at the divisor, or
fitting a correction from the evaluated residual, would be circular.

The first genuinely new theorem must be parameterwise and source-derived: an
identity or chain homotopy valid before setting `tau=0` that forces
`r_U=tau h_U`, with all jets and completed bounds.  No current packet supplies
it.

## Search disposition

The retained constructor, coherence, and coercivity searches have reached the
RH-bearing boundary.  Further frame changes, Schur rearrangements, seam
localizations, or scalar determinant manipulations only restate the residual
condition.  Nonredundant progress now requires new mathematics constructing
the parameterwise source cancellation/chain map.
