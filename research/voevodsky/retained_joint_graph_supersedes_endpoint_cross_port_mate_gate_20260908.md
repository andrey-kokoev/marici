# Retained joint graph supersedes the endpoint cross-port mate gate

Date: 2026-09-08

## Correction

The preceding endpoint analysis identified the only possible map

\[
A_p=V_p^{-1}\operatorname{Tr}_{\rm end}T_{{\rm hist},p}J_P
\]

between the analytic Adams source port and the doubled endpoint signal port.
That calculation is valid, and its restriction to the four-front odd line is
faithful.  It is not, however, the earliest gate on the selected architecture.

The two ports are different source objects.  Requiring a direct identification
between them belongs to an output-only or cross-port presentation.  The
source-authorized G1 object instead retains their common labelled source:

\[
x\longmapsto(x,A_{p,12}x,C_{p,12}x).
\]

The window and cut/Wronskian legs are compared as shadows of the same
`E_{p,12}` coordinate.  They need not first be identified through `V_p^{-1}`.
The identity source projection makes this graph closed and zero-radical.

## Strict primitive/square cell

Prior research already constructs:

- `E_{p,12}=Ce_{p,1} direct-sum Ce_{p,2}`;
- source orientation `S_12=diag(-1,+1)` from the frozen four-front coefficients;
- window and cut/Wronskian maps retaining both grade labels;
- loadings `kappa_p^(k)=2(log p)p^(-k/2)Phi'(k log p)`;
- the forced diagonal mate `Lambda_{p,12}=S_12 D_{p,12}`;
- continuous polarized forms on the common retained graph;
- zero radical, cutoff naturality, and a uniform margin greater than `0.244`.

The apparent gradewise sign mismatch is therefore closed by a source-derived
orientation, not by the summed scalar normalization used in the earlier
Voevodsky note.

## Connected tail

The separate `k>=3` source summand is also constructed.  Its return

\[
R_{\ge3}e_{p,k}=\kappa_p^{(k)}j_p
\]

has nuclear completed synthesis, cutoff naturality, and a retained closed
zero-radical graph.  Hence the complete typed G1 carrier is

\[
\Gamma_{12}\oplus\Gamma_{\ge3}.
\]

## Current disposition

On the retained saturated architecture, G1.1--G1.4 are closure candidates.
The remaining action is a coherent cross-packet review of constructor order,
common domains, source idempotents, and the boundary excluding premature
terminal scalar evaluation.  Reopening the direct endpoint cross-port mate or
an output-only closed-range theorem would impose a stronger, nonselected
architecture.

This changes no ledger status and makes no RH claim.  G2--G4 remain open even
if G1 is accepted.

## Durable inputs

- `research/nima/rh-g1-retained-architecture-review-index.md`
- `research/nima/rh-g1-2-is-closed-on-the-retained-graph-but-not-on-an-output-only-pushforward.md`
- `research/nima/rh-g1-3-reduces-to-the-separate-connected-tail-return.md`
- `research/nima/the-first-adams-four-front-coefficients-supply-the-missing-grade-one-orientation-twist.md`
- `research/nima/the-connected-prime-power-tail-has-a-separate-nuclear-wronskian-return.md`
