# Unified local square: independent semantic replay, nontransportable authority

On the fixed forced-zero migration the declared typed square compares exact public restriction with the owner-verified public update. The source formula `(u,u)->(u,0,0)` is checked cellwise under independently certified two-cell coverage; after `u<=3/4`, its extensional restriction equals the checked successor section on the whole restricted interval. Fresh independent packet-level checker `verify_unified_local_laws.py` does NOT import the producer/session checker: it reconstructs the owning migration and old attachment through established verifiers, independently computes/refutes the expected refined public query (maximum `3/4`), checks the exact archived zero-inserted row, and rejects deleted coverage, foreign migration and a stale answer packet bound to the predecessor.

Fresh command `uv run --with sympy python research/voevodsky/checkers/verify_unified_local_laws.py` passed. The separate live-session run of `check_unified_local_laws.py` verifies stale-generation, excluded point, nonpublic frame and missing archive refusals. Serializing the packet cannot independently recheck process-local handle liveness or confer archive authority. The two checks supply different layers, not duplicate independent implementations of the same security gate.

This resolves a SCOPED cross-branch calculus on one fixed migration: source binding, operation, public domain, section coverage, fresh provider and archive premise are typed separately. An unrestricted fine-audit descent and an analytic S,A,R,C,G functor remain absent. Cost receipts remain incomplete by design; the separately open scoped cost audit must not be silently counted as done by the mixed-law result.
