#!/usr/bin/env python3
"""Consolidate shared-face, translation, dagger, and refinement evidence."""
import hashlib,json
from pathlib import Path
root=Path(__file__).parents[1];res=root/'results';names=['esd7_equivariant_pullback_overlay.json','esd7_equivariant_trace_class_completion.json','esd7_simultaneous_relative_completion.json','global_successor_mixed_naturality.json','two_polarity_quotient_contraquotient_coherencers.json','observer_family_refinement_descent.json'];D={n:json.loads((res/n).read_text()) for n in names}
def ok(x):return x.get('passed') is True or x.get('status')=='passed'
checks={'all_dependencies_pass':all(ok(x) for x in D.values()),'shared_faces_unique':D[names[0]]['checks']['shared_face_has_single_overlay_record'],'translation_covariant':D[names[1]]['checks']['translation_action_is_covariant'],'single_cofinal_refinement':D[names[2]]['checks']['single_cofinal_mode'],'dagger_successor_natural':D[names[3]]['checks']['dagger_commutes_after_parity_correction'],'finite_packet_two_polarity_coherence':D[names[4]]['checks']['joint_quotient_faithful'],'refinement_descent':D[names[5]]['telescoping_identity'] and D[names[5]]['sparse_principal_compressions']}
out={'schema':'marici.voevodsky.eight-lattice-compatibility-frontier.v1','checks':checks,'signed_relative_compatibility_certified':all(checks.values()),'positive_compatibility_certified':False,'limitations':['trace-class completion explicitly excludes ordinary positive-Hilbert terminal completion','two-polarity coherencer is an exact finite two-dimensional fixture only','refinement descent does not provide all-rank uniformity','successor result does not infer outer-regulator all-path convergence'],'artifacts':{n:hashlib.sha256((res/n).read_bytes()).hexdigest() for n in names},'rh_proved':False};p=res/'eight_lattice_compatibility_frontier.json';p.write_text(json.dumps(out,indent=2)+'\n');print(json.dumps(out,indent=2));assert out['signed_relative_compatibility_certified'] and not out['positive_compatibility_certified']
