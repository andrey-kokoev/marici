# Normalization calibrated; even independent four-cell weights fail

Fresh five-bracket calibration: `check_five_bracket_delta_frame.py` integrates the standard Gr(1,5) dlog cell C=(1,q1,q2,q3,q4) using its exact bosonic delta Jacobian. All625 four-flavor coefficients agree with the cyclic five-bracket at each of two rational inputs (1250 checks). Direct target-chart differentiation at four frame choices confirms J_B=J/(Ch)^4. Pairing J_B^-1 with normalized fermions C/(Ch) reproduces the invariant coefficient; raw C leaves an extra frame factor. This independently supports the unframed convention used in the previous comparison without modifying inherited barrier code.

A stronger comparison now permits arbitrary independent scalar weights on EACH of the four source contributions, discarding even the local pole-cancellation restrictions. `check_nine_point_four_cell_span.py` evaluates five Grassmann components at the same quotient kinematics e=1/2 and1. At each target the5x4 cell coefficient matrix has exact rational rank2; appending the full source-tree vector raises the rank to3. Matrices and tree vectors are retained in results/nine-point-four-cell-span.json. The full5x5 determinant vanishes because rank is only3; it is NOT the rank certificate. Rank is computed by exact rational elimination.

Consequently neither two family weights nor four independent cell weights can produce the full tree tensor at either sampled target. This conclusion is invariant under nonzero independent column rescalings, including orientation sign flips and extra det(Ch)^4 factors. Thus ambiguity in those factors cannot remove this span obstruction.

There is a simple support explanation for the decisive mixed test: zero2 cells have no chi2, and zero3 cells have no chi3. A monomial involving both labels is absent from every one of those four source tensors, irrespective of coefficients. The full tree mixed coefficient with flavor pairs(1,9),(2,8),(3,7),(4,6) provides a direct test of that missing support. No count of missing cells follows from this alone. The needed completion must contain contributions with simultaneous fermionic support at physical labels2 and3 (or otherwise change the asserted geometric target).

Next extract the nonzero authored histories contributing to this mixed component at the common targets. This gives a concrete sourced list of candidate missing sectors, instead of trying to resolve an impossible normalization fit. A history-to-positive-cell map and contour orientations are still needed for a geometric completion.
