# Two-vertex cross-sector census

## Question

How does the sector nerve change when Aspect's route-Gram construction is admitted as a finite-Green vertex but its Markov comparison fails identity provenance?

## Claim boundary

Aspect's vertex is mathematical and finite. It carries no optical detection or physical-readout claim.

## Admitted vertices

The sector nerve now has two vertices:

- the five-sort metric-transition Markov realization;
- the Aspect optical-route-frame realization into `finite_green` via the source-derived Gram construction.

A sector vertex needs a typed map into at least one computad sort. It need not realize every sort.

## Edge status

No edge joins the vertices. The matrix \(\operatorname{diag}(4,1)\) is realized on both sides, but no source-derived map identifies optical route labels with Markov site labels. Contract v2 therefore rejects the comparison.

## Recomputed nerve

The admitted graph has:

- two vertices;
- zero edges;
- two connected components;
- cycle rank zero;
- no triangles;
- no mixed horns.

The Aspect–Markov comparison is retained as one near-edge with a single typed reopening condition: source-derived route-to-site correspondence in faithful coordinates.

## Disposition

The cross-sector programme has advanced from one vertex to two without manufacturing an edge from matrix equality. The first cross-sector horn remains undefined because the edge boundary is absent.

## Verification

- `research/voevodsky/cross-sector-bridge-admission-registry-v4.json`
- `research/voevodsky/checkers/check_cross_sector_bridge_admission_v4.py`
- `research/voevodsky/results/cross_sector_bridge_admission_v4.json`
