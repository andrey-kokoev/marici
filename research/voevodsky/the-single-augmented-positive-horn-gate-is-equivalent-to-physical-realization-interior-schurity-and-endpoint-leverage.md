# The single augmented positive horn gate is equivalent to physical realization, interior Schurity, and endpoint leverage

## One coherence cell

The remaining coherence datum is one positive lift of the signed equation

\[
H_{124}
\circ
(H_{234}*\eta_{12})
=
H_{134}
\circ
(\eta_{34}*H_{123}).
\]

It is the central metric filler of the polarity cube.

The previously listed interior, physical-realization, and endpoint gates are not separate tetrahedral cells. They are restrictions of this one filler.

## Augmented feature

On the completed source graph \(\mathscr G_r\), let

\[
A_{S,r}:
\mathscr G_r
\to
\mathcal H_{S,r}
\]

be the positive Schur feature. Let

\[
A_{B,r}:
\mathscr G_r
\to
K_B
\]

be the interior Krein--Langer defect feature, and let

\[
b_r:
\mathscr G_r
\to
\mathscr H_{end}^{odd}
\]

be the physical odd endpoint row.

Define the augmented negative feature

\[
A_{def,r}
=
A_{B,r}
\oplus
b_r.
\]

Its Gram is

\[
A_{def,r}^*A_{def,r}
=
A_{B,r}^*A_{B,r}
+
b_r^*b_r.
\]

## Single acceptance condition

The positive horn has a source-labelled Hilbert filler exactly when

\[
A_{def,r}^*A_{def,r}
\preceq
A_{S,r}^*A_{S,r}.
\]

Equivalently,

\[
A_{B,r}^*A_{B,r}
+
b_r^*b_r
\preceq
A_{S,r}^*A_{S,r}.
\]

Douglas factorization then supplies the unique contraction on the generated Schur range:

\[
C_rA_{S,r}
=
A_{B,r}
\oplus
b_r.
\]

All graph, successor, aperture, and dagger coherences follow from the previously constructed canonical-extension results.

## Restriction to the interior defect

The endpoint term is positive. Therefore augmented acceptance implies

\[
A_{B,r}^*A_{B,r}
\preceq
A_{S,r}^*A_{S,r}.
\]

The translated-Gaussian source is Hardy-total. Hence this restricted inequality extends to the full Hardy carrier and forces

\[
K_B=\{0\}.
\]

Thus the augmented positive horn cannot exist while a nontrivial interior Krein--Langer defect survives.

## Restriction after interior Schurity

Once

\[
K_B=\{0\},
\]

one has

\[
A_{B,r}=0.
\]

The single augmented inequality reduces to

\[
b_r^*b_r
\preceq
A_{S,r}^*A_{S,r}.
\]

This is exactly odd endpoint leverage.

Therefore the two metric statements are successive restrictions of one operator inequality, not independent fillers.

## Physical realization component

The abstract generalized Nevanlinna model supplies an endpoint row

\[
b_r^{abs}.
\]

The physical Clifford/Green carrier supplies

\[
b_r^{phys}.
\]

The positive horn is physically typed only after the source-labelled joint graph identifies them:

\[
b_r^{phys}p
=
U_\partial b_r^{abs}p
\]

for a fixed endpoint isometry \(U_\partial\) and every source vector \(p\).

This is not another positivity condition. It determines which row occurs in the single augmented inequality.

## Exact equivalence

Subject to the completed source and bounded-feature hypotheses, the following are equivalent:

1. the combined \(H_{234}\)--\(H_{134}\) horn has an augmented physical positive filler;
2. the physical and abstract endpoint rows agree on the source-labelled joint graph, \(K_B=0\), and odd endpoint leverage holds;
3. there is a contraction \(C_r\) satisfying
   \[
   C_rA_{S,r}
   =
   A_{B,r}
   \oplus
   b_r^{phys};
   \]
4. the augmented Gram residual
   \[
   R_r^{aug}
   =
   A_{S,r}^*A_{S,r}
   -
   A_{B,r}^*A_{B,r}
   -
   (b_r^{phys})^*b_r^{phys}
   \]
   is positive semidefinite.

## Hostile audit

For

\[
\Theta=B^{-1}
\]

with nonconstant \(B\), one has

\[
A_S=0,
\qquad
A_B\ne0.
\]

The augmented residual is strictly negative on a detected source direction, independently of the endpoint row. Hence the signed tetrahedron closes while the positive horn has no filler.

This confirms that the combined gate is substantive rather than formal.

## What existing coherence contributes

Existing constructions provide:

1. the signed horn;
2. the completed source graph;
3. the full observed defect;
4. the partial Douglas constructor;
5. successor coherence after acceptance;
6. the opposite-polarity dagger;
7. the canonical joint-graph symmetry comparison.

None proves positivity of \(R_r^{aug}\).

## Irreducible remainder

The entire positive programme is now concentrated in one statement:

\[
R_r^{aug}\succeq0
\]

for the intended completed arithmetic multiplier and physical endpoint row.

Because its interior restriction forces \(K_B=0\), proving it requires the principal arithmetic zero-location input or an independently positive arithmetic representation strong enough to imply that input.

## Disposition

The three apparent gates collapse exactly to one augmented physical positive-horn gate. Its realization clause determines the endpoint feature, while its two orthogonal metric restrictions are interior Schurity and endpoint leverage.

No remaining categorical composition can prove this inequality. A proof must now supply genuinely new arithmetic positivity.
