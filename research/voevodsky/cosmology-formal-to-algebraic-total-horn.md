# Formal-to-algebraic total horn

## Question

When does the universal formal total horn algebraize?

## Claim boundary

Let completion map the algebraic total comparison mapping space to its formal counterpart. The formal horn algebraizes exactly when `tau_hat`, including its chosen nullhomotopy coherence, lies in the essential image. Its first class-level obstruction is the cokernel of completion on the relevant cohomology or `pi0`; higher effectivity remains mapping-space data.

Two strong certificates suffice:

1. an algebraic neighborhood `U` of `C` with a wall-preserving étale map to `Tot(N_C/X)` inducing the formal linearization;
2. a finite-type algebraic multiplicative Rees splitting with algebraic ordered wall coordinates.

Either certificate supplies algebraic ratios, symbol, incidence star, and regulator.

A formal isomorphism alone need not extend to either certificate. Finite-order approximation is not exact algebraization. Formal GAGA arguments require separately proved properness and coherence hypotheses for the actual comparison object.

The local polynomial chart is algebraic. No global effectivity certificate is materialized.

## Disposition

Formal total existence cannot be promoted silently. The next leaf proves the algebraic total theorem for étale-linearized carriers.

## Verification

- `research/voevodsky/check_cosmology_formal_to_algebraic_total_horn.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
