# Characteristic-zero horn frontier correction

## Corrected boundary

The rank-26 theorem remains valid: its complete characteristic-zero p-normal derivative module maps to zero, while `Xi_log` is a primitive nonzero logarithmic class. The Milnor symbol `{u,v}` remains a sourced degree-two class, not a degree-one higher-Chow precycle.

The former universal horn no-go is withdrawn because it assumed that projection from the unspecified `(Xi_log,-sigma123)` total complex to `H^2(U)` was a chain map. In a genuine comparison cone, the differential may contain the flag-to-log comparison; a cone homotopy can exist without making `Xi_log` exact in the logarithmic complex alone.

The arrangement, cut, twisted-character, and higher-dimensional branches remain conditional audits of their stated complexes. They do not decide exactness in an as-yet undefined flag-to-log comparison cone.

## Disposition

The live frontier is to construct the graded comparison object and test whether its homotopy is source-natural, integral, and compatible with the rank-26 carrier. No positive horn claim is made.
