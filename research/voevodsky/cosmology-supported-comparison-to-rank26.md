# Supported comparison to rank-26

## DPC conjecture

The verified rank-26 absorption theorem already includes a chain map into the exceptional-supported p-normal comparison complex.

Rivals are that the DNC gate is only a successful diagnostic, that later exact relation absorption does not supply geometric support typing, or that the DNC consequence is prose without a checked map.

Risky consequences are a serialized source and supported target, chain map, support localization, differential-commutation check, and dependencies whose `passed` status means the consumed theorem holds.

## Falsification

The aggregate checker tests only `passed=true`. Its DNC dependency explicitly states that the full characteristic-zero DNC absorption theorem is not established. The aggregate result nevertheless says the principal-wall DNC image is established. It contains no supported target, chain map, or localization contract.

The conjecture is rejected. Exact IBP, K, and q relation-family absorption may survive independently. The DNC consequence, supported `omega` obstruction, and comparison-based horn no-go are unverified until a typed map is constructed.

## Disposition

Withdraw unconditional downstream claims requiring the supported comparison. The next leaf repairs the aggregation gate so diagnostic completion cannot be consumed as theorem truth.

## Verification

- `research/voevodsky/check_cosmology_supported_comparison_to_rank26.py`
- Static audit completed; executable checker run remains pending because structured-command MCP is unavailable.
