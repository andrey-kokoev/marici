# The complete-response retract closes local seam--endpoint stability and radical descent for the resolved joint graph

## Question

After prior research selects the resolved saturated joint graph rather than the codiagonal causal-history Gram, can seam transport and endpoint attachment be shown to preserve that graph with closed range and radical compatibility?

## Claim boundary

Yes on the complete source-response essential image. The response map has a continuous left inverse given by all-seam flux, hence a continuous idempotent retract. Translation is transported through this retract, endpoint traces are continuous on the relative-history rungs, the image is closed, and its positive radical is zero. This closes the local response comparison cell, not the full global analytic--arithmetic pushout.

## Correction from prior research

The source-authorized quadratic object is the retained joint graph

$$
Rf=(f,Bf,M_\Phi f),
$$

with direct-sum Gram

$$
G_{\rm joint,res}
=(1+M_\Phi^2)I+B^*B.
$$

The cross term

$$
M_\Phi(B+B^*)
$$

belongs to the downstream codiagonal history synthesis

$$
H_\Phi f=M_\Phi f+Bf,
$$

not to the retained joint comparison graph. Thus the latest source ordering selects the resolved saturated branch without rescaling the wall.

## Complete response embedding

Use

$$
\mathscr Rg=(B_g,Q_g,A_g,C_g)
$$

with target

$$
\mathscr Y
=
\mathcal S\oplus\mathcal S
\oplus\mathcal H_{\rm rel}^2
\oplus\mathcal H_{\rm rel}^2.
$$

The all-seam flux projection

$$
\pi_B(B,Q,A,C)=B
$$

satisfies

$$
\pi_B\mathscr R=I.
$$

Therefore

$$
P_{\rm resp}=\mathscr R\pi_B
$$

is a continuous idempotent on \(\mathscr Y\), and

$$
\operatorname{ran}\mathscr R
=
\operatorname{ran}P_{\rm resp}
$$

is closed.

## Seam transport

Let \(U_a\) be source translation. Define on the response image

$$
\mathbb U_a
=\mathscr R U_a\pi_B.
$$

Then

$$
\mathbb U_a\mathscr R
=\mathscr R U_a,
$$

and

$$
\mathbb U_a\mathbb U_b
=\mathbb U_{a+b}
$$

on the image. Continuity follows from continuity of \(\mathscr R\), \(U_a\), and \(\pi_B\). Thus moving the seam cannot leave the resolved response graph.

## Endpoint attachment

The relative-history channels have continuous traces

$$
A_g(-\infty)=0,
\qquad
A_g(+\infty)=\int g,
$$

$$
C_g(-\infty)=+i\pi g(0),
\qquad
C_g(+\infty)=-i\pi g(0).
$$

Seam values and fluxes are evaluations of \(A_g\) and \(B_g\). Hence the complete endpoint-attachment map is continuous on \(\operatorname{ran}\mathscr R\), and its translated version is obtained by composition with \(\mathbb U_a\). No scalar seam quotient is used.

## Radical descent

Equip the retained joint response graph with any positive graph form containing the faithful source term

$$
\|\pi_Bx\|_2^2.
$$

If \(x=\mathscr Rg\) is in its radical, then

$$
0\ge\|g\|_2^2,
$$

so \(g=0\) and therefore \(x=0\). The radical is trivial. In particular seam transport carries radical to radical automatically.

## Resolved graph compatibility

The resolved wall, derivative-tail, and output-wall coordinates remain separately typed before codiagonalization. Their direct-sum form has zero wall--tail cross block and is stable under the transported seam action. The causal synthesis map may subsequently combine them and acquire its nonzero cross term; that downstream metric is not substituted back into the joint graph.

## Remaining global gate

This theorem proves local closed range and radical compatibility for the complete response comparison cell. It does not prove closed range of the full global pushout after adding:

- labelled arithmetic incidence;
- theta-label completion;
- connected grades;
- codiagonal scalar observation;
- the unchanged Evans state.

Nor does it supply any still-unlisted jump, Wronskian, or window Green coefficients.

## Disposition

The complete response retract closes seam transport composed with endpoint attachment on the resolved Fourier-saturated joint graph: the image is closed, endpoint maps are continuous, translations are exact, and the radical is zero. The remaining obstruction moves to assembly of this local cell with the labelled arithmetic/Green pushout and evaluation of the remaining typed Green entries.