# Strict D35/D04 spatial comparison

Date: 2026-09-08

## Result

Branch C Parts I and II pass in the strict kernel-valued model.

Use the complete 50-state dualizing complex

\[
\omega=\operatorname{Hom}_A(P_B,A\Omega_6)[6]
\]

and the strict truncation

\[
q(p_{E,O}^\vee)=-1,
\qquad q=0\text{ on the other 49 basis states}.
\]

For \(k=35,04\), let \(E_{\beta,k}\) have retained basis \(u,v\), zero coefficient differential, and

\[
\pi_k(u)=1,\qquad\pi_k(v)=0.
\]

Then

\[
D_k^n=\omega[2]^n\oplus(E_{\beta,k}\Pi^\vee[3])^n
\oplus(C\Pi^\vee[3])^{n-1}
\]

with

\[
d(z,e,h)=(d_\omega z,d_Ee,qz-\pi_ke-d_Ch)
\]

is a strict homotopy-pullback model. The only nonformal omega block is \(qd_\omega\); it vanishes on all fifty columns after the conductor quotient. The conormal projection is a chain map, so the remaining block vanishes as well.

For every \(T\in\{13,15,35,135\}\) and both endpoints, insert the fully framed Branch B map by

\[
b_{\sigma,T}(x)=(0,j_{k_\sigma}\kappa_{\sigma,T}(x),0).
\]

Its complete pullback defect is

\[
(0,j_k(d_N\kappa-\kappa d_G),-\pi_kj_k\kappa)=(0,0,0).
\]

The first term vanishes by the framed Branch B chain calculation and the second because the image is generated by \(v\in\ker\pi_k\). The support transition is the previously checked local-cohomology counit. Primitive coefficient is one in all eight frames.

The framed line and support factors are scalar relative to the normalized native operation factor. Hence operation compatibility is strict with zero new comparison homotopies in this tensor model; the graded antipode remains present. This transports the nine quadratic, 18 cubic, 15 quartic, six quintic and one sextic primitive classes and all 81 ordered quadratic products. Reflection exchanges `D35` and `D04`; it does not act inside one fixed marked target. The loaded determinant/polarity coefficient stays +1, and decomposable antipode corrections are retained.

## Reproduction

```sh
python research/voevodsky/check_marici_strict_d35_d04_spatial_comparison_20260908.py \
  --root . \
  --output research/voevodsky/marici_strict_d35_d04_spatial_comparison_certificate_20260908.json
```

The checker performs 86 new integration checks. Prior source, line, support and operation calculations are consumed through their certified interfaces rather than recounted.

## Boundary

This establishes the strict spatial `D35/D04` comparison. It does not construct a smooth or derived smooth manifold whose function algebra realizes this model. Branch C Part III—the physical Q-manifold, outer vector fields, trace and P24 deformation class—is the next gate.
