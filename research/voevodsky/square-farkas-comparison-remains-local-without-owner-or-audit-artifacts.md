# Square Farkas comparison remains local without owner or audit artifacts

Fresh `check_standalone_square_farkas_math.py` imports only Python rational arithmetic, path and JSON. It declares the four explicit unit-square inequalities directly, verifies two distinct NONNEGATIVE multiplier packets P=(1,2,0,0), Q=(0,1,1,1) with identical target normal (1,0), bound 2, and verifies the signed comparison Q-P=(-1,-1,1,1) has zero normal/bound. It reads no fictional handoff, issuer, directory, graph or audit fixture. Thus the LOCAL algebraic fact survives deletion of all hypothetical communication material.

This does not establish that an authoritative publisher issued those four primitive rows: the source ownership gate remains `BLOCKED_NO_SOURCE_ISSUER`, and analytic S,A,R,C,G correspondence remains deferred absent a role/edge/base map. Mathematical self-containment cannot be used to cross either authority boundary.

Next test the exact data-dependency frontier of this standalone proof: removing one primitive inequality may leave the endpoint inequality true but destroy a chosen proof path or signed comparison; calculate which row statements are necessary for this P/Q pair and which are dispensable from the chosen packets, without inferring canonical global minimality. Owner/handoff remains unassigned.
