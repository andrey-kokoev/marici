# Constructor conformance for relational continuations: two admitted slices and one refusal

The common compressed-successor criterion in `a-common-relational-contract-does-not-identify-source-structures.md` has a concrete constructor boundary. This audit inspects, but does not rerun, the current Python constructors. SCC classification: the proposed comparison is forward realization at fixed typed ports; comparing execution routes would additionally require route/coherencer compatibility. No SCC model or new verified run is asserted.

`checkers/check_observation_refinement_laws.py` computes `denote(obs,e)` as a subset of its fixed finite source grid, and `forget(C,obs)` as the full preimage of the observation image. Its explicit assertions establish equality of coarse/fine public evidence preimages, saturation idempotence, and a fine-audit counterexample. These are finite controls, not an interface that accepts arbitrary source actions or verified public formula certificates. Its `forget` operation is a closure of a *possibility set*, not the operation of dropping an arbitrary constraint row: the latter is explicitly refuted by the projected `{0,1}` versus row-deleted `{0,1,2}` control. The contract instantiation must use whole possibility sets as states.

`../grothendieck/checkers/moment_fine_rebase.py` has a different typed transition: `successor(state, operation)` appends an authorized fine row to a fixed-chart state. `RebaseSession.advance(handle,expected_before,expected_operation,authorization,candidate)` validates the current handle, reconstructs the successor, asks an external owner callback about that exact event and tuple, then checks `verify_rebase` before publishing. `propose` separately classifies retained columns, zero-extends existing flow certificates and chooses retained-optimum, inherited inconsistency or repair. The relation of *admitted source domains* shrinks, while the relation of *verified session heads* advances and replaces a capability. They cannot be conflated. A retained upper-bound proof need not remain attained, and an unchanged block can require new allocations. Neither public-image equality nor the abstract descent theorem certifies this transition, its runtime cost, or authority. The result `../grothendieck/fine-rebases-preserve-bounds-but-can-force-global-moment-repair.md` reports a global-repair hostile and a fixed-grid amortized event bound, not a per-repair locality theorem.

Therefore the tempting adapter from a public projection `q(C)=L(C)` to this fine-rebase `advance` is **refused**: source sets with equal public image can differ on the added hidden predicate, and session transition additionally requires event-specific owner authorization and verified certificate repair. A legal future comparison must declare the chart/grid and operation language, bind an exact authorized successor and its event, carry the retained fine relation or an adequate predicate-sufficient refinement of the public state, and separately verify candidate certificates. No restored vault reference is accepted by this constructor as-is. This is a precise missing typed map rather than an ownership-blocked request to change Grothendieck's code.

The selected source-contract branch now has an audited finite-set carrier and an audited fixed-chart successor. The coupled acquisition/issuance constructor and the actual source-action graph remain unaudited against the same signature. Next: inspect these two concrete ports and register a bounded owner-local SCC realization control; only then close the selected branch. This packet asserts no new checker run, arbitrary dimension extension, or authority over another owner's source.
