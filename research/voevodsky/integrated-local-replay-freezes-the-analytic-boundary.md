# Integrated local replay freezes the analytic boundary

`check_integrated_local_continuation.py` ran the joined service workload on one owner-admitted rank-one migration. A separate `verify_integrated_local_continuation.py` does NOT import that producer/session script: it reconstructs the independently expected owning migration, verifies two independently reconstructed full-domain attachments with the existing owner source/coverage kernels, checks a fresh refined public optimum `3/4` with the owner query verifier, and reconstructs the archived added fine-schema row `(1,0,0)<=3/4`. Fresh `uv run --with sympy python research/voevodsky/checkers/verify_integrated_local_continuation.py` passed. Its four refusal names are compared against the recorded producer result, NOT independently re-executed in this verifier; live-handle security is established only by the original live-session control and its process-local authority assumptions.

The scope is fixed: source migration binding, public operation `u<=3/4`, rank-one affine section `(u,u)->(u,0,0)`, checked full-domain coverage, owner-verified public query, two independently bootstrapped providers with live-handle requirements, one provider's fresh constrained archive and another's unrestricted archive. Byte receipts describe local payload fields; they exclude shared code, full proofs, owner grant infrastructure and cumulative work. No analytic S,A,R,C,G role map, source-authenticated acquisition, total-resource minimum, or universal operation equivalence follows.

A distinct remaining constructive target is unified typing of operation/service/coverage laws across the admitted LOCAL branches: ensure that route equality on fine sections is stated over precisely the same source binding, scope, authorizations and cost accounts. This is executable without Nima's analytic role assignment. Full analytic correspondence remains deferred with an exact requested reopening map. The locally verified result should be reported independently rather than promoted to the analytic parent by a dimension analogy.
