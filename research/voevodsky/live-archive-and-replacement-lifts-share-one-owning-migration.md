# Live archive and replacement lifts share one owning migration

A new owner-local consumer control bootstraps two independent `FullSegmentSession`s against the SAME verified Grothendieck migration plan/case, both with lift and archive context. Distinct full-coverage section meshes `(0,1)` and `(0,1/2,1)` attach through the existing vertex/source and coverage/Farkas gate. Both have source formula `(u,0,0)` for every public point `(u,u)` in the closed interval; five interpolation calls per provider sample the verified formula, rather than establish coverage themselves. Both archives initially return the same original fine plan.

One session is advanced through the owning public-only answer-verification gate to `u<=3/4`. Its old generation refuses both lift and re-exposure. The other still supplies the full-domain source lift at `u=1/2`. The fresh generation re-exposes the exact original fine plan with the new public upper bound; the untouched provider's archive remains the original plan. These are independently bootstrapped parallel sessions, not illegal forks of a single linear head. No archive grant is inferred from the section comparison: both archive capabilities were requested and independently verified at bootstrap. A provider without that retained context would not gain archive access by matching the source formula.

Fresh command: `uv run --with sympy python research/voevodsky/checkers/check_same_base_live_archive_services.py` passed, including owning five projection/compaction certificates, two live providers, full-domain covered lifts, stale lift/archive refusal, live replacement and constrained fresh re-exposure. Machine result `results/same-base-live-archive-services.json` records two receipts' local byte-account fields. These payloads omit shared code, owner verification work, Python object storage and full proof archives; they cannot be read as total cost. No physical source provenance, cryptographic token integrity, general-purpose optimizer, universal service coverage, or cross-base transport follows.

This meets the previously missing single-base *capability-coherence* control for the frozen answer/lift/re-exposure menu together with live provider substitution. It does not compute a globally minimal bundle, nor show that all three capabilities can be retained for arbitrary source changes. The service/dependency specification can now be disposed as a scoped result; an optimizer and broader relation-change adapters are conditional successors, not unearned implementation claims.
