# The flagged positive-plus-skew comparison is internally constructed, but its external CG identification is the confinement theorem

The retained-source analytical system already constructs the correct common
flagged package.  On the source-generated lattice range there is a boundedly
invertible transport

\[
K_{\rm src}:q_{k\log p}\longmapsto u_{p,k}
\]

between resolved-front and wall-extended theta-cut completions.  It commutes
with finite cutoffs and transports the positive forms by congruence,

\[
G_{\rm cut}=K_{\rm src}^{-*}G_{\rm front}K_{\rm src}^{-1}.
\]

The continuous Wronskian form supplies the compatible skew block

\[
K_{\rm link}=-J_{\rm link}/2.
\]

Thus the common positive-plus-skew refinement required in the previous
iteration is internally constructed on the essential source image.  Local
closedness, zero radical, cutoff naturality, positive-margin transport, and
ordered linking compatibility are available.

However, its balance is the internal radial Stokes identity

\[
-R-2E+(R+2E)=0.
\]

The final term is built from the same radial response as the first two.  This
closes an analytic tetrahedral filler but does not provide an independent
characteristic-to-Green law.  Reusing it as a second equation would count one
Stokes identity twice.

The genuinely external `CG` comparison must identify an independently sourced
Tate/Weil, determinant, or conservative spectral current with this canonical
Stokes return on the lifted Xi state.  Primewise, that missing equality sets

\[
(1-p^{-2\operatorname{Re}z})E_p(\Delta_\Xi(z))=0.
\]

Since the lifted pair state is noncollapsed and
`E_p(Delta_Xi(z))>0`, this forces

\[
\operatorname{Re}z=0.
\]

Therefore the remaining external polarization identity is already the
critical-line confinement theorem.  It cannot be filled by choosing a sign,
coefficient, or metric to make the internal Stokes cancellation recur.

The middle facet now has two distinct statuses:

1. internal source-generated flagged facet: constructed;
2. independent characteristic-to-Green facet: open and RH-strength.

This is the precise independence boundary exposed by the self-dual symmetry.
