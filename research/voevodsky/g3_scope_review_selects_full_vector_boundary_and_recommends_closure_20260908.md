# G3 scope review selects the full-vector boundary and recommends closure

Date: 2026-09-08

## Scope decision

The selected G1/G2 architecture fixes the three scope questions left in the G3
margin packet.

1. The analytic carrier is the closed doubled causal-history/cut graph.  Wall,
   endpoint, and Wronskian coordinates are continuous additions to that graph,
   not unobserved external summands.
2. The seam entering the Green boundary is the differentiated labelled
   front-to-cut comparison `J`, not the undifferentiated primitive window and
   not its scalar Wronskian projection.
3. Full Pauli outputs, both reciprocal history sheets, and the vector seam are
   retained until after the G3 Green form.  Terminal scalar evaluation occurs
   later.

These choices are not optimizations made to rescue a margin; they are the
constructor order already selected by the retained saturated graph and the G1
prime-idempotent review.

## Five margins

### Arithmetic

The retained Pauli pair has

\[
\delta_P=2m_\nu^2>0
\]

in squared-norm normalization.  Prime diagonality allows completion without
cross-prime degradation.

### Analytic

The doubled quarter-turn observer obeys

\[
\|Q_+x\|^2+\|Q_-x\|^2
=\|x\|^2+\|Hx\|^2,
\]

so `delta_A=1` in the history graph norm.

### Glue

For `D(x,y)=Jx-y`,

\[
DD^*=JJ^*+I\ge I,
\]

hence `delta_glue>=1` on the full labelled vector seam.

### Coherent diagonal

If `m_J` is the uniform lower bound of `J`, then

\[
\delta_{\rm diag}^2
\ge\frac{m_J^2}{1+m_J^2}>0.
\]

### Mixed

Set

\[
g_0=
\left[
\max\left\{
\frac2{m_J^2},
1+\frac2{m_J^2}
\right\}
\right]^{-1}
\]

using `c_A=1`.  If `M_B` is the declared uniform upper bound for the coherent
and disagreement diagonal blocks, then

\[
\delta_{\rm mix}=\frac{g_0}{M_B}>0.
\]

The connected grades are covered by the bi-bounded vector seam and analytic
observer; no false arithmetic coercivity of the nuclear tail is used.

## Anti-shortcuts retained

The conclusion does not descend to:

- a scalar Wronskian seam;
- a terminal arithmetic row;
- an output-only codiagonal;
- a norm augmented merely by the provenance identity coordinate.

Those compressed observers may have zero minimum modulus.  G3 closure belongs
to the full-vector pre-scalar Green architecture.

## Verdict

All five G3 margins are coherent closure candidates with cutoff-independent
positive constants on the selected architecture.  This review recommends G3
closure to the owning ledger authority but does not mutate status.  G4
spectral/divisor identification remains fully independent and open.
