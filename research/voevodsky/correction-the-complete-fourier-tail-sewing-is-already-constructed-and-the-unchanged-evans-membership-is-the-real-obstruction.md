# Correction: complete Fourier-tail sewing is already constructed, and unchanged Evans membership is the real obstruction

## Audit result

The proposed next step—constructing the complete reciprocal tail-port isometry—has already been carried out in prior research.

The complete response object retains

\[
\mathscr R(g)=(B_g,Q_g,A_g,C_g),
\]

including value, Fourier value, causal history, and principal-value history. Its Fourier graph is a canonical maximal-isotropic sewing relation on the common test, Hilbert, and strong-dual carriers.

The logarithmic Weil connection commutes with this sewing and is compatible with place cutoffs. Thus the unresolved issue is not Fourier normalization, missing reciprocal coherence, or failure to retain the complete tail ports.

Primary reference:

`research/voevodsky/source-derived-fourier-sewing-identification-closure-ledger.md`

## Membership is an additional statement

Let the constructed response graph be

\[
\Lambda_{\mathbb T}
=
\{(x,\mathbb T x):x\in\mathcal H_{\rm resp}\}.
\]

Every source-generated complete response lies in this graph.

For the unchanged Evans boundary data \((e_-,e_+)\), the required statement is

\[
e_+=\mathbb T e_-.
\]

Recovering a possible source from \(e_-\) does not prove the outgoing equality. It merely rewrites the same membership condition.

After projection to prime shells and differentiation through a Xi zero, membership becomes

\[
B_\Sigma^\dagger
\partial_z^j u(\cdot;z_0)=0.
\]

This is the RH-bearing residual family.

Reference:

`research/voevodsky/the-response-graph-does-not-discharge-evans-membership-without-the-missing-polarized-prime-cell.md`

## Numerical hostile evidence

The existing high-precision checker was rerun:

`research/voevodsky/checkers/check_first_xi_zero_evans_adjoint_hilbert_residual.py`

At the first critical-line zero ordinate

\[
14.13472514173469379045725198356247,
\]

it finds a root residual of approximately

\[
5.95\times10^{-44},
\]

while the unchanged Evans adjoint Hilbert residual stabilizes as the cutoff grows:

\[
\begin{array}{c|c}
20&-0.1508512191140906732\\
30&-0.1508512190587392048\\
40&-0.1508512190587392007\\
50&-0.1508512190587392007
\end{array}
\]

This is not interval certified, but it is strong evidence that the unchanged Evans trace is not the state admitted by the already constructed positive response graph.

The checker now locates the vendored `mpmath` automatically.

## Exact defect formulation

Let

\[
T_{\rm PB}:P\to B
\]

be the constructed pair-to-bordered crossing and let

\[
U_{\rm G4}:P\to B
\]

be the authoritative arithmetic forward map, once exposed on the same pairings. Define

\[
\Delta=U_{\rm G4}-T_{\rm PB}.
\]

If \(\mathcal E_{\rm Ev}\) is the closed cyclic subspace generated by Evans shell traces and their parameter jets, the exact required condition is

\[
\Delta^\top|_{\mathcal E_{\rm Ev}}=0.
\]

Full equality of the forward maps is sufficient but may be stronger than necessary.

Reference:

`research/voevodsky/the-final-evans-gate-is-the-annihilation-of-one-forward-crossing-defect-on-the-evans-cyclic-subspace.md`

## Missing source datum

A later audit supersedes the claim that an even scalar \(\alpha_p\) remains free. The completed theta wall and odd Stokes column uniquely determine the local linear map \(Q_p^{\rm lin}\).

The parameter-free open theorem is

\[
\mathfrak G_p^{\rm St}(e_j,e_k)
=
\mathfrak G_p^\theta
\left(Q_p^{\rm lin}e_j,Q_p^{\rm lin}e_k\right),
\qquad
j,k\in\{1,2\}.
\]

All four polarized matrix-unit identities remain unconstructed. The repository does not declare the joint sesquilinear coefficients and radical quotient defining \(\mathfrak G_p^\theta\) on these vectors. Fourier sewing and determinant equality cannot reconstruct those entries. Defining the theta metric as the pullback metric would be circular.

## Consequence for the phase-increment program

The nonlocal isometry requested by the phase-increment formulation is not absent. A complete Fourier-tail sewing relation exists.

What fails is the inference

\[
\text{source response graph exists}
\quad\Longrightarrow\quad
\text{unchanged Evans state belongs to it}.
\]

The phase-increment contraction cannot be concluded until the physical state is identified with the graph image through the missing polarized prime-cell comparison.

## Next admissible step

There are two honest continuations:

1. interval-certify the nonzero first-zero Evans residual, which would reject unchanged-Evans membership in the current positive horn;
2. expose an authoritative theta-history Green polarization and evaluate the four parameter-free matrix-unit identities before any Xi-zero specialization.

The first is executable now. The second needs source data not presently materialized in the contract.

## Disposition

The sewing construction is complete. The frontier is physical-state membership, not missing coherence:

\[
\Delta^\top|_{\mathcal E_{\rm Ev}}=0.
\]

Current high-precision evidence is hostile to that condition for the unchanged Evans state.
