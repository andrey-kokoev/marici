# The background-two vacuum acquisition is a nonsplit observer enlargement

## Result

Unlike the background-three and background-four additions, adjoining the background-TWO vacuum row produces a NONSPLIT source-bimodule restriction to the old observer.

The obstruction uses actual source elements: the old observer already determines the initial forgotten diamond, but does not detect its product with the remaining two forgotten diamonds. The enlarged observer detects that product. An equivariant section would have to preserve both facts and cannot do so.

Thus this acquisition genuinely couples new information to the old observer, rather than appending a source-independent direct summand.

The adjacent cubic class nevertheless remains filtered-nonzero and unfiltered-zero. The new nonsplit acquisition extension is a different degree-one class; these statements must not be conflated.

## 1. Compare before and after the already declared acquisition

Let E be the 270-row stage-three observer WITHOUT the background-two vacuum generator, retaining the original O_2 and the declared homogeneous degree-two cubic detector families. Set

    V=Sat(chi_2)^*,
    E^+=(Omega+Sat(chi_2))^*, rho:E^+->E,
    D=ker rho.

The comparison is meaningful even if chi_2 has already been acquired: it audits the generating sets immediately before and after that acquisition. It does not claim a new measurement has occurred.

Optional background-three/four vacuum blocks may be retained on both sides. Their corner support is disjoint from the old packet and does not alter the argument below.

All extra old six-event detector seeds covered here have retained degree two. None is an additional full-corner all-vacuum row at background two. O_2 remains unchanged.

## 2. The initial ideal corner is already fully observed

At the initial two-event corner 2->12, the entire source ideal has dimension two. It is generated by

    a_0=forgotten(2,3), a_1=mixed(2,3).

The independent source certificate verifies that the two-event terminal recorder has rank six on eight marked paths and that these two independent relations span its kernel.

The old first detector sees a_0 and kills a_1. The old second detector, after the forgotten right context

    b_0=forgotten(5,7),

kills a_0 and sees a_1 with value d_2!=0. After the existing normalization these observations form diag(1,d_2).

Thus E already has dimension two at this source corner, the maximum possible. Adding a new detector cannot enlarge it. Restriction rho is an isomorphism at this corner, and every hypothetical equivariant section must send

    epsilon(a_0) |-> epsilon^+(a_0).

There is no alternative lift in D at these endpoints.

## 3. A right ideal action contradicts a section

Let

    c_0=forgotten(11,13), k_2=a_0 b_0 c_0.

The source k_2 has eight all-forgotten terms and six events. Every old homogeneous degree-two cubic row kills it. No nonidentity context can fit a six-event source into a six-event detector, and the shorter old stages cannot see it. Optional vacuum rows at other backgrounds have different outer endpoints.

Therefore

    epsilon(k_2)=0.

The new unit-vacuum row, however, satisfies

    chi_2(k_2)=1,

so epsilon^+(k_2)!=0.

Suppose s:E->E^+ were an equivariant section. The forced initial lift and right equivariance would imply

    0=s(epsilon(a_0)b_0 c_0)
     =s(epsilon(a_0))b_0 c_0
     =epsilon^+(a_0)b_0 c_0
     =epsilon^+(k_2)!=0.

Contradiction. Hence

    0 -> D -> E^+ -> E -> 0

represents a nonzero ordinary source-bimodule Ext1 class. It cannot split in the more restrictive filtered category either.

This is a source-action obstruction, not merely overlap of supports or failure of a particular proposed section.

## 4. What is known about the increment

The previous interval calculation applies to Sat(chi_2): it has dimension 15, with basis the canonical contiguous subwords of the six-event path of length at least two. Let

    W=Omega intersect Sat(chi_2).

Finite duality gives

    D ~= (Sat(chi_2)/W)^*.

Unlike the background-three/four case, W is not zero. The old 270 private rows already supply at least these seven contextual vacuum functionals:

    (0,2), (1,3), (1,4), (2,4), (3,5), (3,6), (4,6),

where (i,j) denotes the contiguous interval in the canonical chain. A suitable private marked word places both retained features in the removed contexts, leaving precisely the desired forgotten subword coefficient.

These seven functionals have different corners and are independent. The full-corner chi_2 itself is not old. Consequently

    1 <= dim D <= 8.

This is a bound for the full declared detector union, NOT an asserted exact dimension. Additional calibrated detector families may supply further lower-filtration intersection directions. The subsequent audit `the-background-two-vacuum-increment-has-seven-states-and-a-nonintrinsic-filtration.md` resolves this contextual span for the declared signed common matched test: dim D=7 and the inherited filtration dimensions are (7,5,1,0). This refinement is unnecessary for the nonsplitting proof.

Exactly one new deepest ideal-layer direction is added, represented by k_2, because a six-event vacuum seed restricts to I^3 only through its uncontextualized full-corner row.

## 5. Keep the two extension questions separate

With O_2 fixed, define K^+=ker(E^+->O_2) and its inherited flag as before. The observed adjacent cubic class tau^+ pushes forward along K^+->K to the old nonzero filtered class tau. Therefore tau^+ is filtered-nonzero.

The old unfiltered nullhomotopy still works. Its chosen source

    x=path(2_forgotten,3_retained) b_0

has retained degree one. Every source context retains positive feature degree, so the new all-vacuum seed annihilates every context of x. The same calibrated lift into E^+ remains equivariant, and

    H^+=B_ev^+-j^+ w q

still extends the observed G_3 map into K^+. Thus the underlying adjacent class remains zero.

There is now NO source-equivariant decomposition E^+=E direct_sum D, so there is no canonical expression of tau^+ as an old class plus an independent D-component.

There is a separate restriction E^+->V to the complete vacuum observer. Under this projection, the adjacent class is filtered-zero: source evaluation A=J_2/J_4 -> I V extends the vacuum coefficient map on G_3. This projected vanishing does not supply a splitting over E and does not identify V with D.

In particular:

- the acquisition extension in Ext1(E,D) is nonzero even unfiltered;
- the adjacent source pushout into K^+ is nonzero filtered but zero unfiltered;
- its projection to the full vacuum block is filtered-zero.

Different coefficient modules and source extensions account for these different answers.

## 6. Structural meaning

The new reading is not an independent summand because some of its shorter contextual coordinates are already old observations. Those shared coordinates force the lift of a_0, and the new right action then exposes the missing cubic vacuum state.

This is the first audited acquisition in this sequence with genuine source-module coupling. It supplies a concrete contrast to the canonical split thirty-dimensional increment from backgrounds three and four.

It does not invalidate scalar acquisition certificates, assert a new higher Ext degree, or identify numerical aggregate constraints with module quotients.

## Verification

    python research/voevodsky/checkers/check_background_two_vacuum_coupling.py

The standard-library checker freshly verifies the independent old source obstruction certificate, the complete initial ideal corner, the old two-sector coefficients, the new eight-term cubic vacuum value, all 270 private-row vanishings, the seven definite shared contextual intervals and the retained nullhomotopy's vacuum invisibility.

The actual nonzero d_2 is the existing external calibrated input; symbolic scores are not substituted for its physical value.

Artifact: `results/background-two-vacuum-coupling.json`.
