# A numerical proof-target family remains bound to source generation

A FICTIONAL proof-use target family covers exact rational bounds b in [1,2] for normal (1,0) on the frozen square row manifest and fictional generation 7. Proofs with x-upper multiplier 1 and surplus b-1 work for b=1,3/2,2. Fresh `check_target_family_source_scope.py` refuses b=1/2 and 5/2 as outside the interval, a bad surplus inside it, generation 8 with the SAME row hash, and a changed UNUSED x-lower row that leaves these particular proofs mathematically valid. All accepted sample checks explicitly return `TEST_ONLY_INTERVAL_MATCH_NOT_AUTHORIZED`.

Numerical target-family syntax thus neither expands source identity nor transports an issuer grant across generations. Even a provable target inside an interval still requires the matching complete ordered source manifest and actual signed, live, action-scoped authorization, none available for the current Farkas row request.

Next test policy predicates under positive row PRESENTATION isomorphism: although mathematical proofs and feasible square transport exactly, an exact-manifest grant should refuse the rescaled manifest unless the owner separately authorizes a presentation rebind. This compares row-coordinate mathematics to publication scope, not analytic S,A,R,C,G.
