# Correction: U-G4 is authorized definitionally, but the independent comparison coordinate remains absent

## Fresh mutable-state audit

The current repository contains later packets than the previously reviewed v1/v2 candidate:

- `research/aspect/contracts/u-g4-owner-construction-packet.v4.json` constructs the completed analytic surrogate `T_pair_to_border`;
- `research/aspect/contracts/u-g4-owner-strict-authorization.v1.json` records direct operator adoption of that surrogate;
- `research/aspect/contracts/u-g4-owner-construction-packet.v5.json` defines
  \[
  U_{G4}:=T_{\rm pair\to border}.
  \]

Thus the claim that no packet-authorized `U_G4` exists is stale. For this adopted packet, its source, target, pairings, contragredient, completed domain, and all-jet holomorphic map are exposed.

## Two different coherence coordinates

The authorization closes a **definitional** coordinate:

\[
q_{\rm def}=1.
\]

It does not construct an independently derived physical G4 map. Introduce separately

\[
q_{\rm ind}=\begin{cases}
1,&\text{an independently sourced G4 forward map exists},\\
0,&\text{otherwise}.
\end{cases}
\]

The repository explicitly states that identification with an independently pre-existing physical G4 is not claimed, so

\[
q_{\rm ind}=0.
\]

## Effect on the comparison face

For the packet-defined map,

\[
\Delta_{\rm def}
=U_{G4}-T_{\rm pair\to border}=0
\]

by definition. Hence

\[
\Delta_{\rm def}^{\top}|_{\mathcal E_{\rm Ev}}=0
\]

is a reflexive/degenerate coherence face. It checks consistency of the adopted naming but contributes no independent evidence.

The scientifically nontrivial face would require

\[
\Delta_{\rm ind}
=U_{G4}^{\rm independent}-T_{\rm pair\to border}.
\]

This expression is still unformable because \(U_{G4}^{\rm independent}\) is absent.

## Correct lattice point

For coordinates

- \(t\): tail Green edge;
- \(g\): positive tail face;
- \(j\): idelic Fourier core;
- \(k\): common-source tail constructor;
- \(q_{\rm def}\): packet-authorized definitional G4;
- \(c_{\rm def}\): its comparison with the surrogate;
- \(q_{\rm ind}\): independent physical G4;
- \(c_{\rm ind}\): independent comparison face;

current state is

\[
(t,g,j,k,q_{\rm def},c_{\rm def},q_{\rm ind},c_{\rm ind})
=(1,1,1,0,1,1,0,\bot).
\]

The pair \((q_{\rm def},c_{\rm def})=(1,1)\) must not be projected onto the independent coordinates.

## Relation to the two-history proposal

The two-history graph target remains a legitimate source-faithful alternative architecture, but it is not the owner-selected v5 semantics. The owner instead selected the already completed polarized pair-to-bordered crossing. Therefore the two-history proposal must not be described as the current authoritative G4 target.

## Consequence of the numerical falsifier

The certified negative unchanged-Evans Hilbert residual does not contradict the definitional identity \(U_{G4}=T_{\rm pair\to border}\). It shows that this definitional forward-square closure does not supply the separate positive Hilbert membership required for unchanged-Evans promotion. Definitional coherence and positive-state membership are different predicates.

## Disposition

The definitional coherence edge and face are formed. The independent comparison edge and face are not. Any claim using the former as evidence for the latter is circular by construction.
