# Production concurrency invariant audit

`check_concurrency_invariant.py` now checks the phase proof's premises directly at production replacement calls over777 mixed fixtures. Across32347 rewrite states, all simultaneous redex sets have size<=2, disjoint principal pairs and one of the eight classified kind pairs. All eight families occur.

For7055 rewrites performed with a competing enabled redex, every exposed boundary slot connects to a fresh endpoint and no outside-to-outside splice occurs. For1211 splice-bearing replacements, there is no competitor. The test checks the actual rule's edges before mutation, not a table inferred from its kind, and also checks full external/fresh-port multiplicity. This is the exact representability premise used by the cyclic-safe local diamond argument.

These are seeded bounded trajectories, not exhaustive reachability. Together with the earlier all-schedule fixtures, explicit bijection diamonds and written phase classification they close the intended implementation/proof bridge at the programme's current assurance level. Fresh19-suite headless closure passes. Independent review and proof-assistant certification remain absent.

Critical-path reassessment: do not repeat this eight-family test as another micro-milestone. The next useful work is a durable review packet mapping each theorem claim to its assumptions, source rules, executable checks and known exclusions, and identifying the smallest independently checkable kernel (typed replacement plus fresh allocation and boundary matching). This makes external/formal review actionable without claiming a reviewer has already checked it. The parked source-reversion task remains unrelated and lower priority.
