# Correction: the theta prime-cell form is only partial until its Green mate is constructed

> **Retracted after broader source audit:** the bounded skew linking operator and its complete two-output polarized metric already construct the analytic Green-mate block. See `correction-the-green-mate-exists-as-the-bounded-skew-linking-operator-but-its-conservative-identification-is-open.md`. The open gate is quadratic identification with the independent conservative/Stieltjes cell.

## Fresh source audit

The joint analytical dossier writes the candidate theta matrix as

\[
\begin{aligned}
H_p^\theta={}&
F_{end,p}^*(2I)F_{end,p}
+F_{wall,p}^*F_{wall,p}
+F_{tail,p}^*F_{tail,p}\\
&+F_{jump,p}^*F_{jump,p}
+F_{odd,p}^*J_{odd,p}F_{odd,p}\\
&+F_{end,p}^*L_p^*F_{odd,p}
+F_{odd,p}^*L_pF_{end,p}.
\end{aligned}
\]

The diagonal legs and the odd constructor are described. But the dossier introduces \(L_p\) only by saying that its “Green mate enters” the matrix. It supplies no independent definition, domain, formula, continuity theorem, or source provenance for \(L_p\).

Repository search finds finite test-source Green-mate fixtures and abstract mate requirements, but no general prime-cell operator with the required type and completed-domain theorem.

Therefore \(H_p^\theta\) is a partially specified block form, not yet a fully evaluable analytic candidate.

## Exact missing type

With

\[
F_{end,p}:E_{p,12}\to H_{end,p},
\qquad
F_{odd,p}:E_{p,12}\to H_{odd,p},
\]

the mixed term requires a typed operator or bounded form

\[
L_p:H_{end,p}\longrightarrow H_{odd,p}^*
\]

(or the variance-equivalent reverse convention) such that

\[
\ell_p(x,y)
=\langle F_{odd,p}x,L_pF_{end,p}y\rangle

after the declared pairing conventions.
\]

It must be derived from the Green/Stokes source identity before comparing with \(G_p^{St}\). Defining \(L_p\) by the desired equality

\[
G_p^{St}=H_p^\theta
\]

would be circular.

## Relation to the two surviving defect coordinates

The diagonal part of \(H_p^\theta\) can address the positive normalization coordinate \(a\). The missing mixed operator \(L_p\) carries precisely the oriented imaginary coordinate \(y\). Consequently the antiunitary-reflection correction and the missing-mate audit agree:

- even-wall comparison is a diagonal-form gate;
- oriented-linking comparison is the Green-mate gate.

The latter cannot be replaced by the scalar coordinate \(-\kappa_p/(2s_p)\) alone. That scalar is a readout of a retained relation; a bilinear mate must specify both arguments, variance, and domain.

## Revised construction status

The accurate local status is:

- common source plane \(E_{p,12}\): candidate constructed;
- Stieltjes form \(G_p^{St}\): explicit candidate constructed;
- theta diagonal block form: candidate constructed;
- theta mixed Green mate \(L_p\): unconstructed;
- complete \(H_p^\theta\): not evaluable;
- owner-authorized independent comparison package: absent.

Thus the first mathematical constructor is \(L_p\), before the later authority and covariance checks.

## Acceptance conditions for L-p

A valid packet must provide:

1. source and target graph-norm domains;
2. analytic-transpose and Hermitian variance lanes;
3. a source formula independent of \(G_p^{St}=H_p^\theta\);
4. continuity or closability on the completed prime cell;
5. reciprocal orientation and antiunitary reflection law;
6. cutoff naturality;
7. its value on both ordered generators, not only one diagonal scalar;
8. compatibility with the separately closed external zero-trace flux without double counting it.

## Claim boundary

This audit does not disprove existence of \(L_p\). It retracts only the claim that the complete theta prime-cell form is already analytically available for comparison.
