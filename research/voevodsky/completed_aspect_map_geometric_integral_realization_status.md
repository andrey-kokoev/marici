# Completed Aspect map: geometric and integral realization status

## Question

Which parts of the completed Aspect map now have a source-derived geometric and integral realization, and which physical promotions remain blocked?

## Demonstrated construction

The worked relative-cohomology source contains a unique connection-stable active pairing

\[
C_{\rm act}=
\begin{pmatrix}
-1&1&0\\
1&0&1\\
0&1&-1
\end{pmatrix}
\]

between dual strata \((S_3,S_{23},S_{13})\) and canonical forms \((\vartheta_2,\vartheta_3,\vartheta_4)\). It has determinant two, Smith type \((1,1,2)\), and parity character \((1,1,1)\). The alternative determinant-two minor is rejected by Gauss–Manin leakage.

Explicit unimodular matrices satisfy

\[
UC_{\rm act}V=J_1.
\]

The same row change transports the active-boundary parity to the conductor character \((1,0,1)\). Site exchange gives \(J_2\) and \((0,1,1)\). Independently, conjugation by \(J_i\) removes exactly the corresponding conductor half-connection term. Thus each elementary conductor factor has an exact integral pairing model and a matching wallwise connection role.

On the two oriented Kummer covers \(Y_i(E)^2=\Delta_i(E)\), the maps

\[
(Y_{\rm rel},X_{1,\rm rel},X_{2,\rm rel})
=
(Y_i(E),Y_i(E)+E,-Y_i(E)+E)
\]

pull the three active relative factors back to \((E,E,2E)\). Both wall lifts have the same equal-order normal residue and meet on the total-energy special fiber, while their Kummer roots retain the required split first jets.

The positive generator of the punctured complex \(E\)-normal disk has winding vector

\[
(0,1,1,0,1)
\]

against the five relative divisors. Its integral \(d\log\) pairing gives the equal-order residue. The residue eigenlattice has index two and parity \((1,1,1)\).

## Conditional monodromy result

At the analytic quarter twist, the linked normal monodromy is an integral involution unimodularly conjugate to \(M_1M_2\). This product detects only diagonal index-two parity. Recovering the conductor's two independent parity classes requires retaining the two wall-labelled nearby operators separately.

The quarter twist does not extend as a rank-three integral local system over independent endpoint loops: those loops have \(i\)-monodromy and require Gaussian coefficients or rank doubling.

## Physical nonpromotion

The source-listed FRW exponents are \(0,-1,-2,-3\); they give trivial linked monodromy. Ordinary ramified pullback only multiplies these residues and cannot generate a quarter exponent. No source-derived fractional local system is present.

The contour above is mathematically canonical after complex orientation, but no apparatus authorizes complex total-energy continuation. The same-parity record lattice repairs the abstract linear readout, but Aspect's detector packets do not identify it with raw physical records.

Therefore the following promotions are refused:

- linked normal monodromy to physical Kummer monodromy;
- integral relative cycles to detector or laboratory paths;
- same-parity algebraic codomain to a physical joint-record lattice;
- support parity classes to physical observables.

## Disposition

The geometric support strata, integral relative pairing, wall-labelled parameter lifts, site exchange, normal contour, and arithmetic parity realization are constructed. A full physical realization is blocked at two independent typed objects:

1. a source-derived fractional local system or stacky character producing the quarter exponent while preserving both wall-labelled integral pairings;
2. a calibrated physical record map from trial data to the completed readout, including evidence that its image is the same-parity lattice.

Neither missing object can be inferred from the existing matrix, contour, or detector simulations.
