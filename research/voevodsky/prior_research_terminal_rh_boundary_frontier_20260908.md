# Prior-research terminal RH boundary frontier

Date: 2026-09-08

## Search result

The ordered bulk polarization is already defined in prior research:

\[
\mathcal C_z(K)=\langle DK,zK\rangle.
\]

The reciprocal face energies satisfy the exact identities

\[
x_\pm=(D\pm z)K,
\]

\[
\|x_+\|^2+\|x_-\|^2
=2\|DK\|^2+2|z|^2\|K\|^2,
\]

and

\[
\|x_+\|^2-\|x_-\|^2
=4\operatorname{Re}\langle DK,zK\rangle.
\]

Thus the ordered instrument exists algebraically; its unresolved status is not construction but source transport from the completed theta zero-state.

## Conditional positive cone

For a positive exponential resolvent packet, prior research records

\[
\Delta_{\mathrm{face}}
=2a\bigl(|K_z(0)|^2-4t^2S_z\bigr),
\qquad z=a+it,
\]

with

\[
|K_z(0)|^2-4t^2S_z=X_z^2+Y_z^2.
\]

For `t≠0`, positivity of the source modes gives `Y_z≠0`; therefore vanishing oriented flux forces `a=0`.  This is an RH-shaped factorization, but it is conditional on applying the positive-mode representation to the differentiated theta source.

## Exhaustive successor search

Repository search found no later packet or checker proving either required bridge:

1. completed scalar/theta zero implies vanishing total oriented face flux;
2. differentiated theta forcing admits the positive exponential-mode representation used by the factorization.

No separate occurrence of the exact `K_z(0)`, `S_z`, `X_z`, `Y_z` factorization was found outside the frontier packet.  Consequently the formula currently has no independently located constructor packet or executable checker.

## Final boundary

The localization, crystal, resolved-graph, seam transport, endpoint attachment, external-flux cancellation, Pauli linking, and mixed-tail calculations all terminate before the two arrows above.  Neither DAG localization nor prior quadratic machinery supplies them automatically.

The first missing typed object is a source-derived intertwiner from the completed theta zero-section into the ordered face-flux instrument.  Its acceptance test is:

\[
\Theta(z)=0
\Longrightarrow
\Delta_{\mathrm{face}}(K_z)=0
\]

for the same source-constructed `K_z` appearing in the positive-mode sum-of-squares factorization.  Without that common-object identification, combining the two implications would be a type error.

## Evidence

- `research/nima/anti-diagonal-zero-trace-cancels-boundary-flux-but-does-not-orient-the-cross-face-bulk.md`
- `research/nima/anti-diagonal-trace-already-closes-external-flux-not-the-resolved-bulk-polarization.md`
- `research/nima/the-source-authorized-joint-graph-selects-the-resolved-saturated-order.md`
