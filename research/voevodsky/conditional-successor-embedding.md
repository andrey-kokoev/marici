# Conditional completion as a strict continuation

Fresh `check_conditional_successor.py` prewires a GM successor at the conditional's RET/ACK interfaces. No runtime host decision starts the successor. Across 1,134 bounded executions with opposite workload priorities, all 1,134 successor releases occur only after conditional COPY/query/insertion/EA/WAIT/PICK/JOIN/JOINR work is absent. Both answers and the updated support are correct, and the previous Boolean node stays unchanged during successor execution. Total 29,754 rewrites agree with conditional cost plus one gate and the acknowledged successor query cost.

Final component audit partitions every live agent by graph reachability and requires exactly four rooted components: RET plus word/NIL, original OUT plus Boolean, successor OUT plus Boolean, ACK plus DONE. Their expected sizes and total n_final+8 exclude detached budgets, stray DONEs and unrooted garbage. At gate eligibility, total agents are n_final+j+9: word/NIL, old OUT/Boolean, successor OUT, RET, ACK, GM, budget/NIL and incoming DONE. The first test assertion mistakenly omitted that last DONE; corrected from +8 to +9. This was an accounting assertion error, not a rewrite-rule counterexample.

## Context bridge

Cut successor GM.s and GM.p into passive RET/ACK leaves. The conditional region has exactly the original constructor interfaces and its own OUT. GM's budget and output have no active consumer; GM.p faces WAIT.c, PICK.c, JOIN.c or JOINR.c until the final DONE is emitted. Every conditional rule is local and reconnects external port peers, so it commutes with this cut, even when both peers belong to the same GM agent. The two return sources inside JOIN remain distinct and are consumed separately. Thus successor gate eligibility inherits the full conditional postcondition, including rejection cleanup. Its release is the existing acknowledged-query constructor on the updated complete word. This yields a written composition theorem for this one conditional followed by a query, not arbitrary nested branches.

Next critical milestone: integrate a finite list of these conditional-insertion instructions into the strict programme compiler. Use an instruction boundary that releases the initial query and allocates the WAIT with two completed budgets, preserving one shared RET and ordered OUT identities. Extend the existing program induction using this proven conditional postcondition, rather than designing unrestricted branch AST erasure prematurely. Continue to exclude loops and arbitrary branch bodies.
