# Correction: the forward Euler-to-radial loading arrow is already constructed internally

## Question

Is the source-derived forward loading arrow itself absent, or is only G4's adoption of it absent?

## Claim boundary

The internal source arrow is constructed. Prime-power Euler-to-theta incidence has an exact coefficient, strict sign, labelwise diagonal action, and convergent composition with the local radial packet. The missing item is a G4 declaration identifying its source injection with this composite. This correction does not authorize that external identification.

## Problem

Earlier packets correctly distinguished the forward coefficient from the mixed determinant coefficient but continued to describe the forward loading arrow as missing. Fresh source review found the exact diagonal incidence map and its analytic continuation.

## Bold conjecture

Only the scalar formula for the Euler-to-theta coefficient is known; no completed source morphism exists.

## Named rivals

1. The diagonal incidence defines a compact but exact completed source morphism.
2. Compactness prevents the map from being a legitimate source arrow.
3. The mixed primitive--square coefficient remains an equally valid forward map.

## Exact source incidence

For each prime,

\[
c_p
=
2(\log p)
\sum_{k\ge1}p^{-k/2}\Phi'(k\log p).
\]

Every sampled derivative is strictly negative:

\[
\Phi'(k\log p)<0
\]

for \(p\ge2\) and \(k\ge1\). Hence

\[
c_p<0.
\]

The Euler-relative normalized coefficient \(\eta_p\) is likewise strictly negative. On the labelled Euler odd source,

\[
S_\theta e_p=\eta_pe_p.
\]

This is a prime-diagonal source morphism, not an inferred scalar aggregation.

## Completion

The coefficients satisfy

\[
\eta_p\to0,
\]

so \(S_\theta\) is compact on the infinite Hilbert odd source. Compactness excludes a uniform lower bound but does not invalidate the bounded forward map.

The local theta-to-radial maps retain prime, grade, shell, and orientation data. Their loaded direct sum is

\[
L_{\rm rad}
=
\bigoplus_p c_pU_p^{\theta\to {\rm rad}}.
\]

Superexponential source decay makes this sum convergent in every declared finite-order Green graph and rapid radial seminorm. Cutoff projections commute because the map is label diagonal.

Thus the internal forward chain is

\[
H_E^{\rm odd}
\xrightarrow{S_\theta}
H_\theta
\xrightarrow{U^{\theta\to {\rm rad}}}
\mathcal G_{\rm rad}.
\]

## Codiagonal compatibility

Absolute convergence and label diagonality give

\[
C\left(\sum_pc_pX_p\right)
=
\sum_pc_pC_pX_p,
\]

with the endpoint-minus-Wronskian coefficient retained. All compact-local Laplace jets commute with the same sum.

## Strongest falsification attempt

Rival 2 confuses coercivity with existence. The map is compact and not bounded below, but remains exact, continuous, and source-labelled. The full observer retains a complementary discrete coefficient port precisely because this forward analytic map is compact.

Rival 3 fails by constructor role: the mixed coefficient is derived from primitive--square determinant counterterms, while \(c_p\) is the Euler-to-theta incidence entering forward shell realization.

The bold conjecture is rejected: both the formula and the completed morphism are already present.

## Remaining external square

What remains is not construction of \(L_{\rm rad}\). It is owner-authorized identification

\[
U_X^{G4}=P_XL_{\rm rad}
\]

inside a declared G4 carrier, followed by readback that the G4 return and codiagonal reproduce the source radial response.

Together with the canonical fold

\[
C_uF^2=W_uC_u,
\]

all internal ingredients of the two previously open G4 interface cells are now constructed.

## Disposition

The internal loading-arrow blocker is withdrawn. The forward Euler-to-radial loading is the compact exact composite of diagonal Euler-to-theta incidence with local radial synthesis. Only external G4 adoption, carrier declaration, and metric/return readback remain. No claim that the current v2 contract supplies those declarations is made.
