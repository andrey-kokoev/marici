# Combined runtime signature and dispatch audit

Freshly inspected the mixed compiler and added `check_combined_signature.py`, with an explicit independently listed finite signature and allowed principal-pair table. The resulting JSON records the full table and actual method-resolution order: ScanningSetProgram, ConditionalSetProgram, ConditionalInsertion, FuelScanner, StrictSetProgram, AcknowledgedMembership, RetainedMembership.

For every declared active kind and each of eight possible data/token heads, construct a principal pair with distinct passive outside leaves. Exactly57 legal pairs enable and each executes exactly one production replacement;199 illegal combinations do not enable. Every replacement uses its external/fresh ports exactly once and creates only declared kinds with exactly their declared port sets. This exercises inherited dispatch rather than a second implementation of the rules. Port tuple order is not semantic; names and arity are checked.

All16 length-four GC/GS combinations compile with exact adjacent support/acknowledgment peers and ordered OUT roots, with no stale GM placeholder identities in those interfaces. Every resulting agent matches the signature. This explicitly covers adjacent conditional/scan placeholder replacement, independently of eventual final values.

Scope: the pair fixture is a symbolic interface representative, not an arbitrary graph simulation or a proof of operational denotation. Distinct passive leaves test slot use; contextual cyclic safety still rests on cut invariants. The audit does not certify allocator recovery, transactions or imported malformed states, and does not protect against future changes unless rerun.

Next consolidate the mixed recurrent programme theorem and create a single headless verification entrypoint for this closure, retaining separately reported rule-interface, observation and semantic regressions. Stop adding expressivity until this combined assurance boundary is explicit; the existing theorem components now suffice for induction across all five instruction forms.
