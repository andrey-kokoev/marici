# Split-residue bookkeeping sequence

## Correction

The previously stated sequence `0 -> Z/2 -> O -> Z(-2) -> 0` is not a motivic-cohomology exact sequence: it combined the degree-two free class with `H*(-1)` in motivic degree three.

There is still a useful chain-level bookkeeping distinction between a free secondary coefficient and constant tame-sign representatives, and the latter have a parity quotient after diagonal boundaries. Any displayed `Z plus Z/2` splitting belongs only to that bookkeeping quotient. It is not a canonical or noncanonical splitting of the horn cohomology.

The exact tame boundary of `{u,v}` remains `v^-1`, `u`, and `-v/u`. Its sign cannot be separated from the nonconstant units as an independent cocycle without producing the degree-three class `H*(-1)`.

## Disposition

All cohomological conclusions formerly drawn from the artificial exact sequence are withdrawn. The next graded-total-complex audit determines the valid interface.
