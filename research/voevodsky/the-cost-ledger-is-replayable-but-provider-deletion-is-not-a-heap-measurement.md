# Cost ledger replay: provider deletion is not heap measurement

A second checker, `verify_scoped_cost_ledger.py`, independently recomputes the two serialized receipt-field totals (1,921 and 1,917 bytes), the 3,838-byte arithmetic subtotal, the separately canonicalized 7,580-byte owning proof case and SHA-256/byte lengths of five shared source files. It checks that full heap/archive costs and verification time/memory remain explicitly unavailable. Four mutated-ledger controls fail: altered receipt sum, omitted unknown heap account, false claim of measured total memory and foreign migration binding. Fresh `python research/voevodsky/checkers/verify_scoped_cost_ledger.py` passed.

Removing provider 0 from the two-receipt SNAPSHOT leaves provider 1's recorded 1,917 receipt bytes; the prior live resource checker establishes that this provider can supply a same-scope replacement lift under its own authority. Neither arithmetic subtraction nor stale-handle rejection measures bytes freed by process deletion, full proof retention, or transfer of archive access. The original archived fine relation remains independently bound to its owner session; no removal of one session confers its history on the other.

The current cost result is a reproducible ledger of measured payload encodings, file fingerprints and explicitly unavailable accounts, not a total-runtime or total-storage bound. Time depends on interpreter, warm caches and owner verification work; arbitrary redundant future refinements can grow event history without changing this fixed snapshot. A useful final bounded test is to measure an actual archive-free versus archive-enabled provider receipt under the SAME migration and document exactly which field differs, while keeping shared lift context and verifier work distinct. A zero archive field is not evidence that its fine relation was destroyed from the shared process or proof context.
