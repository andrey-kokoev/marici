# Comparison path versus horn cell

## Question

Does graph inclusion followed by cone contraction produce anything sourced, and is it a source-chain horn?

## Claim boundary

For the primitive source cycle `k={u,v}`, the signed graph map is

`j(k)=(Xi_log,-sigma123)`.

The comparison cone contains `tau` with `d tau=j(k)`. Its contraction therefore gives a source-indexed target homotopy `H(k)=tau`. This is the cone presentation of the strict identity `Res R_log=R_flag`.

It is not an `ElementLift`. The source has no degree-one `h` with `d h=k`, and `k` remains nonzero in source cohomology. In the common flag codomain the comparison homotopy is zero; in the signed cone the same strict equality is represented by the formal path `tau`.

Thus a formal `HomotopyLift` exists after admitting the cone, while a source-chain horn does not. Cone admission alone does not make `tau` a geometric incidence cell, higher-Chow precycle, rank-26 relation, or physical process.

## Disposition

The path/element distinction is complete. The next leaf states which independent geometric data would be required to promote the formal target path to an admitted scientific horn.

## Verification

- `research/voevodsky/check_cosmology_comparison_path_vs_horn_cell.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
