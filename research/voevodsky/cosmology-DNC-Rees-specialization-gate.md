# Deformation-to-normal/Rees specialization gate

## Result

For the principal ideal `I=(p)`, with `p=x+y+3z`, the conormal module `I/I^2` is free of rank one. The Koszul/Rees construction therefore supplies a genuine degree-one normal generator `epsilon` with `d(epsilon)=p` before specialization. The deformation coordinate is not physical time.

This resolves source existence for the normal generator but not target survival. Its rank-26 p-normal derivative image is zero modulo `S+T` at degrees 8 through 18 over both tested primes. On the marked K-residual sector, the exact characteristic-zero absorption class extends to every even ambient degree at least 12.

## Claim boundary

The full unbounded characteristic-zero absorption theorem remains restricted to the marked K residual. Finite two-prime evidence for the other source families cannot be promoted to that theorem.

No chain map sends the DNC normal generator to `(Xi_log,-sigma123)` with column `(1,1)`. The available comparison instead points toward absorption.

## Next gate

Compute the DNC generator image in the full characteristic-zero non-marked source families, or derive a uniform factorization of those families through the absorption quotient. Only a nonzero residual outside that factorization could reopen the horn branch.

## Verification

- `research/voevodsky/check_cosmology_DNC_Rees_specialization_gate.py`
- `research/voevodsky/results/cosmology_DNC_Rees_specialization_gate.json`
