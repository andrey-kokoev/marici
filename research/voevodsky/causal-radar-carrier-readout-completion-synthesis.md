# Scoped causal radar carrier, readout and completion theorem

## Result

A concrete ideal gravitational observation now has an explicit chain

    declared vacuum source + observer/apparatus
      -> causal clock records
      -> complete finite display-map section
      -> independently computed protocol-indexed reading
      -> continuous extension on a certified record completion.

The chain is physically nonconstant, coherent under passive constant transverse
frame changes, and quantitatively stable on its stated apparatus domain.
It does not identify finite radar-shape acceleration with instantaneous curvature,
derive Einstein dynamics from a carrier, or establish a common foundation for
all physical sectors.

Fresh resume selected `causal-radar-carrier-completion-synthesis:v1`.
All six radar checkers were rerun headlessly during this turn: **167 exact
controls passed**, including the physical null-ray enclosure computation.

## 1. Keep the five roles distinct

| Role | Retained object | What is not inferred |
|---|---|---|
| Physical selection | Smooth vacuum Rosen metric, detector curves, initial preparation, clock, ideal reflection, ray-domain witness | Field equations or wave shape from regrouping |
| Carrier | Labelled nine-clock-pair record; a selected row over every time/direction slot | Arbitrary malformed tables or an unspecified global graph calculus |
| Coefficients and operations | Real clock data; exact rational controls/enclosures; per-row squaring, calibrated linear sums and probe-design inversion | A universal coefficient object selected by the carrier |
| Readout | Protocol-indexed symmetric finite radar-shape fit Y | Apparatus-independent curvature, noise law or hardware response |
| Completion | Closure of admitted record/apparatus data inside uniform positivity, range and conditioning margins | Classical curvature of every C0 metric or physical realization of every candidate record |

The source references in executable packets are provenance labels. They do not
constitute proof of a physical realization. The supplied vacuum solutions and
analytic/enclosure arguments provide the particular physical witnesses used here.
Graph admission likewise records coordination, not physical truth.

## 2. The observation and its independent finite carrier reading

Perlick arXiv:0708.0170v1, section 1 equations (1)-(2), defines radar distance as
half the emitting clock's emission-to-reception interval, with null propagation
and a reflection event. Section 2 requires the appropriate unique ray pair.
The retained source specialization is

    ds^2=-2du dV+gamma_ab(u)dX^a dX^b,
    central curve (u,u,0), reflector curves (u,u,b), tau=sqrt(2)u.

For a nonzero baseline, each future leg is determined by

    2h=b^T [integral_t^(t+h) gamma^-1(s) ds]^-1 b.

Positive metric bounds give a root bracket and residual derivative >=2.
The return starts at the actual reflection event. Proper-clock readings retain
both legs; neither spacelike Fermi separation nor equal outbound/return duration
is substituted.

For the frozen protocol, epsilon=1/32, the vectors are (3,0),(0,4),(3,4), and
proper emission times are 3/4,1,5/4. For the variable-apparatus form, retain the
actual three vectors v_d=(x_d,y_d), scale epsilon and time step h. Define

    q_jd=(R_jd/epsilon)^2,
    L(V)_d=(x_d^2,2*x_d*y_d,y_d^2),
    G_j=L(V)^-1 q_j,
    Y=-(G_+-2G_0+G_-)/(2h^2).

G and Y are represented by entries (11,12,22). They are fitted protocol outputs,
not presumed metric or curvature components.

Let A map an admitted physical source/apparatus packet to its causal clock
records. Let eta assign those rows to their time/direction fibers, retaining
their selected values and membership indices. Let rho be the time-first reading
and rho_section the independently implemented direct reading of selected rows.
Then the finite comparison law is

    rho_section(eta(record))=rho(record).

It follows by equality of the calibrated row values and finite distributivity
of the temporal and directional linear maps. Per-row squaring precedes both.
The native reading does not call a decoder or reconstruct the old packet.
Missing rows, wrong memberships and erased clock/protocol metadata are rejected.
This is the finite display-map interface, not yet an instantiated proof for
all of the owner's recursive native packages and retained rules.

## 3. Comparison and certified completion

For constant orthogonal Q, passive relabelling of the SAME physical probes gives

    v'_d=Q^T v_d, gamma'=Q^T gamma Q, Y'=Q^T Y Q.

The exact null scalar is invariant, and uniqueness of the quadratic fit gives
the output congruence law. These comparisons compose and invert coherently.
They do not authorize reusing unchanged records for an actively changed apparatus
in an anisotropic source. Time-dependent observer frames are outside this theorem.

On a record domain with

    ||L^-1|| <= K, epsilon>=e0>0, h>=h0>0,
    0<r0<=R_jd<=M,

write delta_R for maximum distance discrepancy, delta_L for induced-infinity
design discrepancy and delta_e,delta_h for scale/step discrepancies. Then

    ||delta Y||_max <=
      4KM delta_R/(e0^2 h0^2)
      +2K^2 M^2 delta_L/(e0^2 h0^2)
      +4KM^2 delta_e/(e0^3 h0^2)
      +4KM^2 delta_h/(e0^2 h0^3).

This is the joint apparatus/record stability theorem, not an inverse-curvature
estimate. The common inverse bound implies that a convergent sequence of designs
has an invertible limit. Resolution/range margins and clock-grid equations are
also closed. Thus rho extends uniquely to the closure of any admitted subset
of that domain, including the actual source image. Since eta simply reorganizes
retained finite coordinates, the comparison with rho_section extends too.
Bounded passive comparisons commute with this extension; the component-norm
certificate may be transported conservatively from K to 2K.

The theorem does not say the source-image closure is the entire record domain.
It also does not say every C0 source limit carries a classical connection or
curvature tensor. Only the declared observation is extended.

## 4. Physical noncollapse and the interpretation hostile

Two exact vacuum sources with the SAME initially-resting preparation give
unequal readings under the frozen protocol:

- static Minkowski source: Y=0;
- p=cosh(u/4), q=cos(u/4), gamma=diag(p^2,q^2): certified Y_11<0 and Y_22>0.

All 18 null legs of the latter were enclosed with rational outward arithmetic,
Taylor remainder bounds and monotone root brackets. Rounded clock packets carry
explicit error bounds and satisfy the direct-section comparison. Their nonzero
reading survives the certified completion because the unequal finite readout
values remain unequal in its real codomain.

A second Minkowski source, p=q=1+u/4, has moving reflectors and E=0 but nonzero Y.
Its changed initial relative velocity is explicit. This establishes that finite
shape acceleration includes kinematic/apparatus effects and cannot be renamed
pointwise curvature. It does not weaken the noncollapse comparison above, whose
wave and static control share the initially-resting preparation.

Noncollapse here is existence of physically distinguished readings. It is not
injectivity on every physical source or recovery of every discarded datum.

## 5. Two independent boundaries on stronger claims

**Pointwise curvature inverse.** Complete calibrated radar data at arbitrarily
small baselines recover gamma within the source ansatz. Nevertheless the prior
exact vacuum hostile has normalized radar gap <=8/n^2 and fixed corner tidal
gap 1/2. The inverse from uniform raw radar data to that curvature is discontinuous.
Additional temporal regularity and calibration/noise assumptions are substantive
inputs, not consequences of the finite carrier equivalence.

**Degenerating apparatus.** Probes (1,0),(0,1),(1,1/n) have invertible designs at
each finite n, but inverse norm n+1/(2n). Synthetic perturbations of positive
clock records can tend to zero while Y_12 changes by 4. This is a record/noise
hostile, not an additional vacuum realization. It shows why the apparatus
certificate is needed for the broader candidate-record completion.

These boundaries are different: one concerns trying to recover a stronger
physical readout; the other concerns conditioning of the finite declared fit.

## 6. Fresh owner-interface status: an important correction

This turn freshly read the current `research/nima/native-table-equivalence.md`
and all of `research/nima/agda/NativeTidalTableReadout.agda`.

The current native-table work is not limited to the old annotated-Code bridge.
The owner's published construction covers native operations for the eight
constructors and twelve retained rules. Its `readout-commutes` interface requires
an independently specified native readout and its comparison equation.

The direct tidal source now contains thirteen labelled integer jet rows,
independently defined jet arithmetic and an actual row-projection tensor reading.
It supplies component comparisons to `potentialTotal8`, `geometric8`, the actual
packages and retained history, plus a first-jet-only refusal theorem. Therefore
the earlier request for an independent finite Newtonian tidal reading is no
longer accurately described as an absent construction. Communication
acknowledgement and implementation evidence are separate questions.

This audit read that source and recorded its hashes; it did NOT freshly compile
the owner's Agda modules, modify owner artifacts, or certify their whole bridge.
Nor does the existing Newtonian instance automatically instantiate this radar
protocol. The radar-specific full native-package/retained-rule comparison remains
a genuine next task, rather than another request for the already supplied jet
construction. No owner adoption of the new radar readout is inferred.

## 7. Verification and evidence map

Run:

    python research/voevodsky/check_radar_completion_synthesis.py

Fresh receipt: `research/voevodsky/radar-completion-synthesis.json`.
The audit reruns six checkers, records exit codes, exact control counts, script
and receipt hashes, logs and the owner sources read. Total: 167 passing controls.
The 512 noise cases are covered within the finite-protocol controls, not counted
as 512 additional theorem proofs.

| Obligation | Primary own artifact |
|---|---|
| Source-backed causal response and C0 forward stability | `causal-radar-readout-is-c0-stable-in-the-plane-wave-sector.md` |
| Exact identification versus unstable curvature inverse | `radar-determines-curvature-but-not-stably-in-raw-completion.md` |
| Finite protocol and direct selected-row reading | `finite-radar-contract-retains-clocks-and-has-a-direct-section-readout.md` |
| Physical nonzero witnesses and rational ray enclosures | `physical-radar-protocol-is-noncollapsed-but-not-a-curvature-meter.md` |
| Frame versus apparatus comparison | `radar-frame-covariance-requires-retained-apparatus.md` |
| Joint conditioning and completion certificate | `radar-apparatus-completion-needs-uniform-conditioning.md` |

The universal mathematical statements are written proofs. The controls and
rational enclosures supplement them; they are not empirical validation or a
proof-assistant certification of continuum analysis.

## Disposition and nonredundant successor

The synthesis leaf is resolved: the conditional finite causal-observation
transport/completion theorem and its boundaries are now explicit and freshly
audited. The overall common-foundation programme remains open.

Next: instantiate this radar readout against the owner's ACTUAL current native
package and retained-resolution interfaces, preserving clock/probe/source data
and independently computing the target reading. Require a fresh formal closure
check of the comparison in an authorized integration artifact. Do not replace
that obligation with another Python regrouping test or generic readout-after-
decode statement, and do not treat the integration as new physical-law selection.
