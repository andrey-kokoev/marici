# Cone admission and source authority

## Question

What independent data would promote the formal path `tau` to a geometric scientific horn?

## Claim boundary

Promotion requires seven fields:

1. an independently defined geometric source `Y`;
2. a genuine degree-one cell `gamma` in its admitted chain complex;
3. `d gamma` computed inside `Y` before comparison;
4. a realization map from that chain complex to the residue comparison cone;
5. verification that the complete image is `(Xi_log,-sigma123)` with no residual tame, sign, flag, or support components;
6. naturality under declared source morphisms and orientations;
7. a sourced comparison from `Y` to the original characteristic-zero carrier.

Only then may `Phi(gamma)=tau` be called a geometric realization. The formal cone fails because it has no independent `Y` or `gamma`. The exceptional divisor alone has no face, but the ambient semistable boundary of the ordinary blowup contains a cone over that link and must be audited separately. `{u,v}` has degree two; logarithmic branches fail descent; rank-26 images vanish.

Geometric promotion would still not imply a physical process or readout.

## Disposition

Current authority reaches a formal source-indexed comparison path. The ambient semistable blowup is the next candidate for geometric promotion.

## Verification

- `research/voevodsky/check_cosmology_cone_admission_source_authority.py`
- Execution pending: structured-command MCP is unavailable in this turn, so no results JSON is claimed.
