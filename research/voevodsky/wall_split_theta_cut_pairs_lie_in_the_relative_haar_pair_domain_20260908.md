# Wall-split theta cut pairs lie in the relative-Haar pair domain

Date: 2026-09-08

## Coordinate correction

The singular endpoint of `L2(dx/x)` is `x=0`, corresponding in the interior
logarithmic chart `x=e^{-q}` to `q=+infinity`.  It is not the moving seam
`q=0`, which is `x=1`.

Every source cut atom `u_a` reassembles unitarily to the same completed theta
profile:

\[
R_au_a=\Phi.
\]

The completed theta profile and all derivatives decay superexponentially as
`q -> +infinity`.  Consequently

\[
\int_0^\infty|\Phi(q)|^2dq<\infty,
\qquad
\int_0^\infty|\Phi(q)|^2e^{-q}dq<\infty,
\]

and also

\[
\int_0^\infty|\Phi(q)|^2e^{q}dq<\infty.
\]

Thus the cut atom is admitted in both interior and exterior additive-Haar
charts and in the multiplicative-Haar chart.  The bounds are independent of
the cut location because moving-seam reassembly is unitary and returns `Phi`.

## Wall split

The endpoint constant or jump is retained as a separate finite-dimensional
wall coordinate.  It is not extended as a constant function toward `x=0`.
Therefore the non-normalizable constant-channel hostile does not apply to the
wall-split cut atom itself.  It remains relevant only if a constructor embeds
the wall coordinate into the relative-Haar bulk as a literal constant state.

## Pair-domain consequence

For every ordered pair of source cut atoms,

\[
u_a\otimes\overline{u_b}
\in
\operatorname{Dom}J\widehat\otimes
\overline{\mathcal H_m}
=
\operatorname{Dom}J_{\rm pair}.
\]

Hence the one-leg relative-Haar pair colligation is defined on every finite
prime/grade packet and every localized theta-label pair shell.  Moving-seam
transport preserves this domain through the common reassembled source vector.

The weighted cut-atom incidence is nuclear for connected grades and
cutoff-compatible on the retained projective source.  Finite pair
polarizations therefore have a common graph core.  Promotion of the complete
unrestricted Fock-square sum still requires the declared pair-source topology;
no claim is made for arbitrary Hilbert tensor vectors.

## Revised frontier

Relative-Haar domain admission is closed on the source-generated wall-split
pair core.  The remaining G4 comparison is no longer a local domain question.
It is the global source identification:

1. localize the tail--seam Gram current to the radial shell graph;
2. transport it through `J_pair` with ordered orientation;
3. prove that the resulting function-valued response is the adjoint response
   of the fixed conservative G4 column;
4. preserve the Evans Xi divisor and all completion jets.

No chain promotion or RH conclusion follows from domain admission alone.
