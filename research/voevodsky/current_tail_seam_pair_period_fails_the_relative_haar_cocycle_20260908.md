# The current tail-seam pair period fails the relative-Haar cocycle

Date: 2026-09-08

## Comparison

The source pair current is

\[
K(n,m)=A\!\left(\left|\log\frac nm\right|\right).
\]

Under simultaneous prime transport,

\[
K(pn,pm)=K(n,m).
\]

Thus its quadratic seam period, in the existing logarithmic-history metric
`dq`, has transport multiplier one before Mellin coefficients.

The independently typed Haar sectors have relative dilation multiplier

\[
\frac{\|T_pf\|_{L^2(dx)}^2}
     {\|T_pf\|_{L^2(dx/x)}^2}
=p.
\]

Therefore the current tail--seam period does **not** transform by the relative-
Haar cocycle.  The mate requested by the prior orientation programme fails for
the present single-sector metric:

\[
1\ne p
\qquad(p>1).
\]

This is exactly the gauge-audit distinction.  The unweighted pair Gram lives in
the multiplicative/logarithmic Haar sector.  Multiplying it by `sqrt(nm)` would
force the factor `p`, but without a two-sector comparison that operation merely
changes the source presentation or shifts the Mellin exponent.

## Consequence

The relative-Haar theorem closes the origin of the half-density seam, but it
does not orient the existing pair current.  A direct identification of the two
would be false.  The G4 crossing needs a genuinely relative pair form, not the
norm of `U*U` after diagonal reweighting.

The minimal new object has two independently typed legs,

\[
\mathcal H_{\rm pair,add}
\quad\text{and}\quad
\mathcal H_{\rm pair,mult},
\]

and a source comparison `J_pair` whose mixed form is invariant under separate
unitary frame changes while carrying the residual modular character `p`.
It must also localize the global ratio kernel to finite shells and preserve the
ordered analytic-transpose Wronskian orientation.

## Falsifier

Any claimed orientation map from the present seam Gram alone should compare
its prime-transport multiplier with the relative-Haar multiplier.  The former
is one and the latter is `p`; equality fails at every prime.  Scalar functional
equation agreement cannot repair this metric mismatch.

## Revised frontier

Prior research supplies both ingredients separately:

- a faithful pair current with multiplier one;
- a gauge-invariant relative-Haar cocycle with multiplier `p`.

It does not supply the relative pair colligation joining them.  Constructing
that colligation, rather than applying `sqrt(nm)` to one Gram, is the next
nonredundant source problem.
