# The four-port observer is source-derived analytically but not from valuation alone

## Question

Can the retained arithmetic grade source be mapped directly into the complete four-port observer packet now that the bilateral value--flux pair is known?

## Claim boundary

Prior research answers this negatively for valuation-only input. The real bilateral analytic packet supplies four boundary coordinates and canonical finite Fourier-character projectors, but the multiplicative valuation source cannot populate the odd additive Tate port after radialization. This packet identifies the exact factorization and first missing typed map; it does not pursue an RH conclusion.

## Two source facts that must coexist

### Analytic boundary packet

The bilateral completed theta source supplies the independent even/odd pair \((\Phi,\Phi')\), hence the seam value--flux plane \((M,J)\). On the finite boundary quotient, the source Fourier operator obeys

\[
\mathcal F^4=1.
\]

Its four character projectors

\[
P_\lambda=\frac14\sum_{a=0}^3\lambda^{-a}\mathcal F^a,
\qquad
\lambda\in\{1,-1,i,-i\},
\]

are orthogonal, sum to the identity, and have multiplicity-one images on the tested finite boundary packet. Thus the four finite observer lines are generated by source transport rather than fitted as a basis.

### Valuation limitation

The multiplicative valuation label \((p,k)\) records radial shell data. It does not retain an additive character. Translation by an element inside one valuation shell can change a later valuation output, so valuation projection does not intertwine all additive translations. Equivalently,

\[
\ker\pi_{\rm val}
\not\subseteq
\ker(\pi_{\rm val}T_h).
\]

Therefore no map defined only from \((p,k)\mapsto k\log p\) can reconstruct the odd Weyl phase. The weighted valuation-to-theta cut is genuine but parity-even.

## Correct factorization

The source-native route is

\[
\mathcal S(\mathbb Q_p)
\xrightarrow{\text{Weyl/Fourier orbit}}
\mathcal H_{\mathrm{Tate},p}
\xrightarrow{\pi_{\rm val}}
\mathcal V_p
\xrightarrow{\text{Euler/theta incidence}}
\mathcal H_{\theta}
\xrightarrow{V_\partial^*}
\mathcal B_4.
\]

At the local additive level one must retain both translations and characters. A radial shell lift

\[
e_{p,k}\longmapsto\mathbf1_{S_k}
\]

is canonical for spherical Fourier transport, but it is not closed under the full Weyl action and cannot by itself supply the odd port.

The fixed four-port object \(\mathcal B_4\) is consequently a boundary compression of a larger, generally growing additive Tate carrier. It is not the full local dynamic state.

## Consequence for the four-port residue classifier

The exact classifier

\[
h_4(r,j)=(r,-r,0,j)
\]

correctly describes the kernel once a state has reached the common boundary packet \(\mathcal B_4\). It does not construct the upstream odd coordinate \(j\) from valuation labels.

Hence two questions must remain separate:

1. **boundary classification:** which four-port states are invisible to the selected even readout? This is solved finitely by \(h_4\);
2. **source reachability:** which of those states arise from the additive/multiplicative source constructor? This remains open at the odd local Tate incidence.

A pure seam-flux kernel vector is an algebraically valid hostile even when it is not yet proved reachable. It refutes an unqualified kernel theorem but cannot be promoted to a source state without the missing incidence.

## Forced constructor order

Prior audits force:

1. local additive Tate carrier;
2. translation--character Weyl pair;
3. local Fourier transform;
4. comparison with the real even--odd derivative square;
5. valuation projection;
6. Euler half-density and grade typing;
7. theta tail--seam cut;
8. four-port boundary return.

Performing valuation projection before steps two through four irreversibly removes the odd information. No downstream residue classifier can restore it.

## First missing typed map

The next unconstructed arrow is not a generic map from all arithmetic strata into four identical copies of one packet. It is the source comparison

\[
\mathcal H_{\mathrm{Tate},p}^{\mathrm{odd}}
\longrightarrow
\mathcal B_4^{\mathrm{odd}}
\]

compatible with:

- additive Weyl covariance before radialization;
- the real bilateral derivative coordinate \(\Phi'\);
- valuation projection on the even shell sector;
- prime and grade labels;
- cutoff/conductor bonding maps;
- the four-port boundary pairing.

## Acceptance test

A valid construction must exhibit a growing finite-conductor additive carrier and maps \(U_{\partial,n},V_{\partial,n}^*\) such that:

1. Weyl relations hold before compression;
2. conductor bonding squares commute;
3. the compressed four-port returns converge in the declared topology;
4. the odd character projector remains observable;
5. valuation projection agrees with the established even shell incidence;
6. discarded nonspherical modes are annihilated or routed by a typed residual map.

## Disposition

The four-port observer is source-derived on the bilateral analytic boundary and its finite kernel classifier is exact. Direct cross-stratum incidence from valuation grades is impossible because valuation has already forgotten the additive Weyl phase. The branch stops at the missing additive-Tate-to-boundary odd comparison; fitting a downstream odd coefficient would violate source identity.
