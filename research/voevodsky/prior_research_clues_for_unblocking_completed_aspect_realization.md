# Prior-research clues for unblocking the completed Aspect realization

## Question

Do existing Marici packets contain a source-derived replacement for the missing fractional total-energy monodromy or physical readout calibration?

## Total-energy cusp clue

Prior cosmology work already constructs a source-defined integral local system at total energy. Ledger Entry 1750 identifies the homogeneous elliptic subsystem as

\[
\mathcal K_{B^{-1/2}}\otimes m^*\mathbb H_{\rm Leg}
\]

with native integral Legendre homology. At \(E_T=0\), the Legendre and Kummer signs cancel, leaving

\[
T_{\rm src}=
\begin{pmatrix}1&2\\0&1\end{pmatrix}.
\]

Thus the source supplies nontrivial integral total-energy monodromy without a quarter twist. Its coinvariants contain a width-two class.

This monodromy is unipotent, not conjugate to the semisimple involution produced by the quarter-twisted relative residue. It cannot validate that construction. Instead it indicates that the quarter-twist branch should be abandoned if the goal is the source physical cusp.

## Two-bit extension clue

Ledger Entry 1152 proves that gluing the elliptic width-two coinvariant into the full rank-nine lattice is controlled by

\[
\operatorname{Ext}^1_{\mathbb Z}
(\mathbb Z/2,\mathbb Z\langle e_6,v_{\rm alg}\rangle)
\cong(\mathbb Z/2)^2.
\]

This has the same abstract two-bit size as the completed conductor defect. The match does not identify the bits: the cusp class lives in the algebraic plane \((e_6,v_{\rm alg})\), while the conductor classes are the two wall parities. A source-labelled comparison between those planes is absent.

Entries 1153 and 1751 identify the exact missing constructor: an integral Picard–Lefschetz thimble and lift through the primitive infinity-Gysin sequence. The known physical Cut pairing maps to zero in the elliptic coinvariants, so repeating that pairing cannot select the extension class.

Hence prior work replaces an open-ended fractional-local-system search by one finite integral test: compute

\[
2m=a e_6+bv_{\rm alg}
\pmod{2\mathcal A_{--}},
\qquad(a,b)\in(\mathbb Z/2)^2,
\]

for a source-normalized elliptic coinvariant lift \(m\), then test whether a labelled isomorphism sends \((a,b)\) to the two conductor wall characters.

## Physical-readout clue

Aspect's existing apparatus work provides typed acquisition plans, calibration identities, compiler provenance, and synthetic deterministic means. It explicitly supplies no physical packet. The hardware-bound acquisition manifest lists missing constructors:

- physical raw packet;
- hardware acquisition service binding;
- spacelike certificate.

Therefore prior research contains the schema for an empirical estimator test but no observations capable of executing it. No filesystem-only calculation can unblock physical calibration.

## Disposition

Prior research gives one substantive redirection:

- abandon the unsourced quarter-twist route for physical total-energy monodromy;
- use the source-defined unipotent width-two cusp and its unresolved two-bit integral extension;
- compute the local integral Lefschetz thimble as the next finite geometric test.

The readout side remains externally blocked by absent raw acquisition and hardware authority.
