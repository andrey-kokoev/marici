# SCC-corrected RH observer frontier

Date: 2026-09-08

## Integrated rank ledger

| Layer | Result |
|---|---:|
| six-normal marked target incidence | rank 6 of 6 |
| current static theta source incidence | rank 4 of 6 |
| theta odd residual kernel | dimension 2 |
| action-generated theta incidence | finite rank 6 |
| action-generated completion margin | absent |

The target family is complete. The reported static rank four is produced by a synthetic SCC negative fixture containing the first four rows of the identity matrix; it is not derived from an RH source packet. It therefore demonstrates compiler behavior but does not establish that the RH source misses exactly two directions. Action generation likewise tests finite-rank versus topology and is not an RH incidence derivation.

## Four active residuals

1. **Candidate cokernel residual:** the synthetic fixture omits two directions; an RH cokernel cannot be asserted until its source-derived matrix exists.
2. **Candidate kernel residual:** the checker assigns dimension two in its verdict rather than computing it from a source-derived RH matrix; it cannot yet seed an RH child tower.
3. **Topological residual:** the action-generated rank-six family has no uniform lower frame bound after completion.
4. **Authority residual:** no common-source transport currently identifies the theta source with the six-normal endpoint source.

These residuals require different dispositions and cannot cancel one another.

## Corrected order before higher coherence

1. Freeze the common global-source packet, observer theory version, coefficients, and evidence digests.
2. Derive the actual level-zero incidence matrix from cited RH source maps, without assuming which directions are absent.
3. Compute its kernel and cokernel. Only if it has a rank defect may a direct extension or boxed-residual promotion be considered; any promotion then needs source authority, signature compatibility, independence, support preservation, and coherence certificates.
4. Type the six normal ports on that same source and configuration stratum.
5. Construct one common closed domain for Fourier and normal observers.
6. Prove observer continuity and one uniform lower frame margin.
7. Only then construct relative-null/endpoint level-2 coherence and its boundary-work promotion.

## SCC additions to retain

Every subsequent packet must include:

- configuration-domain downward closure;
- restriction functoriality;
- typed matching maps;
- continuation-interface faithfulness;
- kernel/cokernel/topological/authority residual separation;
- a child-tower termination measure;
- hostile preregistration;
- critical-pair joins;
- global coherence under completion, authority, coefficients, and parent/child transport;
- positivity as a separate admission output.

## Verification

```sh
python research/voevodsky/check_marici_rh_scc_gap_integration_20260908.py \
  --root . \
  --output research/voevodsky/marici_rh_scc_gap_integration_certificate_20260908.json
```

The checker performs 18 cross-certificate assertions and reports `typed_blocked`, as intended.
