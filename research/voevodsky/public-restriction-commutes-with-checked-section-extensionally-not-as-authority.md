# Public restriction commutes with checked section extensionally, not as authority

On the frozen forced-zero-tail migration, let D={(u,u):0<=u<=1} and s(u,u)=(u,0,0). The owner-checked full-segment attachment certifies D by Farkas coverage and s by source checks at its three knots; affine interpolation gives s on every point, not merely the checked vertices. For F={u<=3/4}, the public image law and the section law form a typed square:

`(D,s) -- restrict to F --> (D intersect F,s|F)`;
`(D,s) -- owner-verified advance --> (D_F,s_F)`, with `D_F=D intersect F` and `s_F=s|F` extensionally.

The top route is a mathematical restriction of a proved formula, whereas the bottom route changes the live owner session generation. A comparison between them is valid only on the shared source binding and restricted interval, with retained attachment data and a FRESH handle. It does not equate handles or prove interchangeable proof histories. The archived re-exposure after the bottom route includes the zero-inserted public row `(1,0,0)<=3/4`; applying only the top mathematical route does not manufacture archive capability.

Fresh `uv run --with sympy python research/voevodsky/checkers/check_unified_local_laws.py` passed against the owning migration and verified section. It refused old-generation lifting, lifting outside the restricted public domain and a nonpublic frame. The live receipt lists archive, witness-cache, full-segment, lift-context, descriptor and section encoding accounts; it excludes shared code, owner proof records and total work. The checker relies on the owner's coverage/query verifier; it is not an independent proof assistant or analytic source realization.

Thus this one mixed law is supported on a precisely frozen stratum, with the resource and authority boundary explicitly part of the typed diagram. An unrestricted law for all fine updates is false: equal public images can have different hidden-fine successors. A universal unified calculus cannot infer the analytic S,A,R,C,G roles or the coupled acquisition state from this rank-one square.
