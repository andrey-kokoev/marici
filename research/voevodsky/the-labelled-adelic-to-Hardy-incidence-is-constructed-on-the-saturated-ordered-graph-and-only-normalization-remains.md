# The labelled adelic-to-Hardy incidence is constructed on the saturated ordered graph and only normalization remains

The labelled incidence required after the Hardy-anomaly correction already
exists on the rapid labelled source carrier.  For a coefficient row `b=(b_n)`
define

\[
I_b(c)=\sum_{n\ge1}b_nc_n\,
\Gamma_S(\tau_{\log n}g),
\qquad
\Gamma_S(h)=(h,Sh).
\]

Translation by `log n` costs only polynomial and logarithmic factors in the
chosen rapid-label seminorms.  Therefore every polynomial-logarithmic row
extends continuously

\[
I_b:\mathcal S_{\rm lab}\to\mathfrak B_S.
\]

In particular the independently labelled rows

\[
b_n^{(P)}=\Lambda(n)1_{n\ {m prime}},
\qquad
b_n^{(2)}=\Lambda(n)1_{n=p^2},
\qquad
b_n^{(Q)}=\Lambda(n)1_{n=p^k,k\ge2}
\]

and the full prime-power row are continuous on one saturated ordered graph.
Their labels remain distinct before scalar aggregation.

Fourier transport commutes with this arithmetic synthesis because both are
continuous coordinates of the saturated graph:

\[
\mathcal F I_b(c)
=\sum_nb_nc_n\,
\mathcal F\Gamma_S(\tau_{\log n}g).
\]

Each ordered coordinate carries the universal multiplier

\[
2i\,\operatorname{pv}(1/\xi),
\]

while prime and grade labels are preserved rather than generated by that
multiplier.  The polarized pair current also extends continuously and is
independent of prime ordering and joint versus iterated cutoffs.

Thus the previously named map

\[
A_{\rm ad}:U_{\rm labelled}\to\mathcal H_{\rm Hardy/PV}
\]

is constructed at the carrier and continuity level.  The remaining gate is
normalization: prove that the incidence rows extracted independently from the
completed Tate trace equal these graph-incidence rows, including endpoint,
zero-mode, and archimedean completion terms.

This is stronger than scalar functional-equation agreement and narrower than
constructing another carrier.  The external `CG` comparison is now localized
to equality of two independently sourced labelled current families on a dense
generator set, followed by continuity.
