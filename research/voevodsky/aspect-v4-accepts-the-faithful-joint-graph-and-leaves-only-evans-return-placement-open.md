# Aspect v4 accepts the faithful joint graph and leaves only Evans return placement open

## Fresh mutable-state readback

Aspect has materialized:

- `research/aspect/contracts/theta-rh-interaction-net-state.v4.json`;
- `research/aspect/contracts/polarized-prime-cell-open-theorem.v2.json`.

## Accepted semantic choice

Version 4 selects

`faithful_source_retaining_multi_rung_joint_graph`

and declares the global carrier

$$
\operatorname{Graph}(T)
=\{(x,Tx):x\in E\}
\subset E\oplus\prod_\alpha Y_\alpha.
$$

It records:

$$
\pi_EJ_T=I,
\qquad
P_T=J_T\pi_E,
$$

closed complemented range, zero radical under a source-separating positive family, and no output lower-bound requirement.

## First-Adams status

The contract now declares the first-Adams constructor **constructed** with local object

$$
\operatorname{Graph}(A_{p,12},C_{p,12})
$$

over the common source \(E_{p,12}\), carrying the direct-sum form.

The four parameter-free identities

$$
G_{\rm St}=Q^*G_\theta Q
$$

are reclassified as optional single-metric descent and are not required by the selected G4 semantics.

## New analytic crossing awaiting readback

Subsequent analytic work constructs

$$
\mathcal T_{\rm pair\to border}
=
\operatorname{Laplace}
\circ(\rho_0,e,w,\rho)
\circ\operatorname{Loc}_{[a,b]}
\circ(C_{1/2}\widehat\otimes C_{1/2})
$$

in the analytic-transpose lane, with the Hermitian conjugate lane separately typed.

Its image satisfies

$$
zR-\rho(0)=E-\frac12W
$$

and its combined response readout is

$$
R+2E
=\frac{\rho(0)+E-W/2}{z}+2E.
$$

The construction preserves modular orientation, ordered pair labels, shells, cutoffs, and every parameter jet.

## Exact remaining contract cell

Version 4 still declares:

- `evans_membership = open`;
- `external_G4_identification = false`.

The only remaining placement statement is that the arithmetic G4 reciprocal/linking adjoint equals the constructed bordered return on the unchanged Evans source:

$$
I^{({\rm recip})}+I^{({\rm link})}
=
\mathcal J_{\rm RL}
\mathcal T_{\rm pair\to border}.
$$

If admitted with the frozen contragredient sign, then the source identity gives

$$
I^{(0)}+I^{({\rm end})}
+I^{({\rm recip})}+I^{({\rm link})}=0
$$

for every shell and every parameter jet, hence the declared Evans membership residual vanishes. Precisely because this implication is RH-bearing, it cannot be promoted from analytic type compatibility alone.

## Disposition

Aspect v4 authoritatively closes the faithful joint-graph and first-Adams semantic questions. All analytic data for the bordered pair response are now explicit. The sole remaining step is Aspect/G4-owner source identification of that constructed crossing with the arithmetic reciprocal/linking return; this is exactly the Evans/RH-bearing gate.