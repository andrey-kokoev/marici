# A source-rooted trace diamond exposes the first missing four-cell

On the fixed source rows `-x<=0` and `x<=1`, take surplus-consumption trace `(1/2,1/2,1)` under budget 2. The proposed 3-cell `merge` on its first two steps reaches `(1,1)`. Another path applies `swap` on the last two, `swap` on the first two, then `merge` on the last two; it also reaches `(1,1)`. Every elementary cell preserves total source-derived surplus consumption and hence the same Farkas proof tip `(2,3;0)`. But the two sequences of 3-cells are different. The first genuine branching diamond therefore asks for a **4-cell comparing the 3-cell paths**, not merely a proof that the final traces join.

Fresh `check_source_rooted_trace_cells.py` checks both boundaries, dependence on the two primitive source rows, and refuses a missing bound or cyclic authority support. It refuses a self-asserted critical filler: no source-derived 4-cell has been constructed. Its output `results/source-rooted-trace-cells.json` records the exact left/right paths and the residual `missing_source_4cell`. Labelling the merge/swap generators as source-rooted candidate cells does not itself admit them as genuine operative or analytic higher cells.

This is the promised diagnostic separation. Normalized endpoint certificates, normal traces, and equality of 3-cell paths are THREE different continuation contracts. The first two have bounded checks; the third requires an explicit higher interchange constructor with source-rooted support, or a declared quotient of histories that deliberately stops promising it. Nima's finite proof-DAG principle closes authority regress only after the constructor and its type exist. The analytic S,A,R,C,G correspondence is even stronger and remains separately deferred.
