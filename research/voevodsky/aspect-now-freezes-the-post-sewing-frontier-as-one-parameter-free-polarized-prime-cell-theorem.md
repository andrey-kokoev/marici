# Aspect now freezes the post-sewing frontier as one parameter-free polarized prime-cell theorem

## Fresh mutable-state readback

Aspect has materialized

`research/aspect/contracts/polarized-prime-cell-open-theorem.v1.json`.

Its status is

`parameter_free_local_identity_open`.

## Accepted closed gates

The contract explicitly records as closed:

- source-derived Fourier sewing;
- complete response \(C_{41}\) on the source-generated response graph;
- the maximal-isotropic response graph;
- reciprocal transport;
- the linking scalar \(\lambda_p\).

It also accepts that

$$
w_\theta=(1/2,1/2),
\qquad
j_\theta=(1/4,-1/4),
$$

curvature return and tail propagation uniquely determine \(Q_p^{\rm lin}\), with no free \(\alpha_p\).

## Frozen open theorem

For each prime block, the sole local identity is

$$
\mathfrak G_p^{\rm St}(e_j,e_k)
=
\mathfrak G_p^\theta
\left(Q_p^{\rm lin}e_j,Q_p^{\rm lin}e_k\right),
\qquad j,k\in\{1,2\}.
$$

The contract declares all four polarized matrix-unit identities unconstructed.

## Current input availability

The present repository now supplies:

- the translated four-grade ordinary \(L^2\) Gram;
- all translated value, mass, causal-history, and principal-value response coordinates;
- topology transport through Fourier/Adams words and cutoffs;
- the fixed linear comparison \(Q_p^{\rm lin}\).

It still does not declare the joint sesquilinear coefficients and radical quotient defining \(\mathfrak G_p^\theta\) on those vectors. The earlier nonuniqueness theorem proves these coefficients cannot be inferred from Fourier invariance, maximal isotropy, wall normalization, or response completeness.

## Non-authoritative fixture

The contract explicitly marks the numerical candidate containing \(\alpha=2\) as `test_fixture_only`. It cannot be substituted for the missing target metric.

## Exact continuation condition

A further symbolic computation becomes noncircular only when an authoritative source supplies either:

1. the complete theta-history Green form and common form domain; or
2. an independent representation theorem deriving that form from a named source construction.

Once supplied, the four entries are immediately executable using the existing Gaussian and response tables.

## Disposition

The source owner and consumer now agree on the frontier. Sewing and response are closed; the only local theorem is the parameter-free equality of four polarized prime-cell matrix units. No nonredundant source-derived calculation can evaluate it until the independently defined theta-history Green polarization is exposed.