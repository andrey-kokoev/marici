# Logarithmic tangent normalization audit

## Question

Once the complete Archimedean ordered field is granted, does a global scale ambiguity remain in the logarithm used by Gaussian and Mellin transport?

## Exact normalization

Let `R_+^x` be the positive multiplicative group of the completed ordered field. Its tangent at the unit is canonically identified with the additive field by the ambient coordinate `x=1+t`. For a differentiable homomorphism

`ell:R_+^x -> (R,+)`,

multiplicativity implies that its derivative is

`d ell_x(h)=c h/x`

for the constant `c=d ell_1(1)`. Requiring the tangent map at the multiplicative unit to be the identity forces `c=1`. Integration and `ell(1)=0` then give the ordinary logarithm uniquely.

Equivalently, the Euler dilation vector field `x d/dx` is canonical on the positive multiplicative group. The coordinate `u=ell(x)` is uniquely characterized by sending that field to `d/du` with unit coefficient. This removes the global rescaling `ell_c=c log` without invoking zeta zeros or the gamma factor.

## Gaussian compatibility

For the Gaussian scale orbit `exp(-pi m^2 e^(2v))`, setting `x=e^v` identifies translation in `v` with multiplicative dilation in `x`. The tangent-normalized logarithm gives the jump displacement `v=log(p^k)=k log p`. The half-density follows from the positive square root of the relative Haar modulus.

Thus, conditional on the Archimedean smooth realization and its multiplicative group, the completed-zeta grade and half-density normalization are canonical.

## Remaining source gate

The normalization theorem does not construct that realization from the coefficient-neutral Carrier. Existing Carrier data provide a discrete ordered initial ring; tangent spaces, differentiability, the complete ordered field, locally compact topology, and Haar measures enter only after analytic completion. No admitted comparison proves that this completion and its smooth multiplicative group are functorial for the occurrence-resolved Carrier or compatible with sector selection.

## Disposition

The alleged residual global logarithmic constant is removed by the identity-tangent condition. The sole first missing arrow in this chain is now the Carrier-compatible Archimedean smooth completion functor. Once supplied, internal height, logarithmic grade, positive half-power, Gaussian scale, and relative-Haar seam follow canonically; Frobenius and global Euler/Poisson completion remain separate arithmetic-geometric gates.
