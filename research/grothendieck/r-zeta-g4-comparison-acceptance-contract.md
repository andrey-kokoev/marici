# Labelled completed-zeta to radial-G4 comparison acceptance contract

## Question

What exact owner-supplied datum would reconnect the conditionally derived arithmetic lens to the radial-G4 objective?

## Required map

The missing comparison is

`R_zeta:S_zeta^labels -> U_G4`,

where `U_G4` is an owner-authorized projective oriented-edge source, not a synthetic basis renamed as G4.

A valid finite witness must provide:

1. ordered source and target bases with provenance;
2. an exact matrix for `R_zeta`;
3. preservation maps for prime, power, logarithmic grade, shell, ordered-pair, theta, and reciprocal-pole labels;
4. a commuting square with the radial feature map;
5. the full polarized Green matrix, quotient map, radical basis, and rank witness;
6. exact recovery, reciprocal covariance, kernel-sequence exactness, quotient-projection compatibility, and finite projective continuity equations.

The source side is frozen: 198 weighted prime-power labels through `q=1024`, coefficient `-2`, reciprocal pole annihilation at `2` and `1/2`, archimedean shift `n+1/4`, and positivity for one finite test only.

## Hostile tests

The witness must fail after each of these independent mutations:

- permute weights while retaining label count;
- erase prime-power exponent or shell;
- identify ordered-pair reversals;
- replace reciprocal-pole labels by generic basis vectors;
- perturb one entry of the radial commuting square;
- enlarge the declared radical or make the quotient Green form rank-deficient;
- retain schema literals while deleting executable matrices.

Equal dimensions, a positive scalar, matching signs, and a synthetic six-coordinate fixture are insufficient.

## Disposition

Conditional Carrier arithmetic now supplies a canonical scalar component-count lens, intrinsic primes, formal Euler product, Archimedean completion, theta/Poisson completion, logarithmic grades, and half-density normalization. None determines `R_zeta`. The branch is owner-blocked precisely at the G4 carrier and comparison matrix above; delivery of those objects reopens executable conformance and hostile testing.
