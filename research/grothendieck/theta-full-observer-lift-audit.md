# Audit of the full Fourier--Tate boundary observer lift

## Decision

A substantial algebraic observer lift exists placewise and on the restricted
tensor product, and the real-place Fourier action is explicit. The completed
full observer naturality cell is still absent. Its first missing component is
the Fourier action on the relative primitive/square anomaly increments and the
coherent comparison cell transporting them.

## Constructed part

For every finite place, restriction gives a test-space isomorphism
`J_p:S(Q_p)->T_p` retaining multiplicative data and the valuation-tail endpoint
constant. At infinity, `J_infinity:S(R)->T_infinity` retains matched full jets.
These assemble algebraically to

`J_0:S(A)->T_0(A^x)`.

Thus the source-to-multiplicative trace is not wholly unconstructed, and
Fourier--Tate covariance is meaningful before scalarization.

At the real place the conjugated operator is explicit. On the two-sign
logarithmic trace it is a Hankel integral with kernel depending on `q+r`, and
it satisfies exact Mellin reversal `K V_a=V_-a K`. Its endpoint action produces
the entire global moment tower. Therefore a determinant line or any finite jet
packet is not a faithful archimedean operator port; the completed observer must
retain the full moment rigging.

## Completion correction

The primitive readout has no cutoff-independent scalar limit. The correct
target is a relative vessel carrying transition increments
`Delta C_1`, `Delta C_2`, the connected and archimedean coordinates, and their
triangle cocycles. Naturality must therefore be a line-valued higher cell

`eta:J_rel F => F_Tate J_rel`,

not literal equality after choosing a scalar anomaly origin.

## Missing part

No approved packet constructs the Fourier action on
`(Delta C_1,Delta C_2,C_>=3,C_infinity^mom)` together with cutoff-triangle
coherence. In particular, the first two completed grade maps cannot yet be
used to compute a naturality residual. The real Hankel operator and finite
sheet swap are target components, not a proof that the assembled adelic
relative vessel commutes.

The exact next calculation is cutoffwise: derive how Fourier--Tate transport
maps each primitive and square transition increment; compute the comparison
residual; and accept it only if it is a named central cocycle satisfying the
triangle law. Any residual cancelled only after summing grades fails source
naturality.

## Confinement boundary

The partial lift explains scalar modular reflection and supplies the faithful
archimedean port, but it does not yet create the complete Green-current row or
cancel the forcing overlap. No scalar-null confinement follows until the
relative naturality cell and its compatibility with the doubled Green boundary
form are constructed.
