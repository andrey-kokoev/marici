# The six-prime shifted attachment has a function-valued paired comparison

## Strength and scope

**Finite-cutoff analytical comparison theorem, relative to the prescribed tensor Green assembly.** The two groupings of the six-prime shifted product attachment give the same function-valued paired observation, retaining the root state, intermediate vertices, and every spectral pair. The ambient paired observations detect all 720 product directions.

This concerns the shifted-product component of the common derived block complex. Its 2160 ordinary degree-zero coefficient coordinates remain present and are not declared radical. Equality or isometry of the complete coarse-block Green forms on that ordinary sector is not proved here.

No relation metric is fitted. No finite spectral matrix is used as a surrogate for analytical faithfulness. No completed prime-packet limit is taken.

## 1. Source operation and carrier

The source operation is the already constructed multiplication comparison between the groupings (ab)c and a(bc). Its common complex H retains the ordinary middle-block quotient and its shifted relation term. Its shifted part is P_3[1], where P_3=I^3 has dimension 720.

Let J_3 be the labelled tensor of three local seam complexes. The source-derived cycle map is j_3:P_3[3]->J_3. Its attachment target is J_3[-2], with the product cycles in degree -1.

At a fixed packet and common capacity N>=6, retain the prepared root carrier X_s and put

V=X_s tensor P_3,

Y=X_s tensor J_3[-2].

The cycle inclusion in degree -1 is

j_X=identity_(X_s) tensor j_3.

The two corrected grouping maps, restricted along the common comparison cell, are equal as actual chain maps. Their common shifted component is j_X after the connecting projection. This equality precedes any scalar pairing.

The root state is not replaced by the vacuum. All formulas below are sesquilinear in the actual root states, and extend by linearity to nondecomposable finite receiver vectors.

## 2. Function-valued features, not spectral samples

Use the fixed shell forcing, canonical four raw Clark ports, fixed normalized sewing matrix A_Cl, and the existing upper-half-plane region Omega. Write h_v for the raw four-tail vector and hhat_v=A_Cl h_v for its two-sheet amplitude. The one-letter feature used for the nondegenerate paired carrier is

Lhat(v)(z,t)=hhat_v(z) exp(i z t).

Here J=diag(1,-1) acts on the two-sheet carrier. The raw four-port coefficient is C_raw=A_Cl^* J A_Cl; it has rank two and is NOT an invertible four-dimensional signature. This distinction corrects the original notation of this note.

The carrier is the declared L2 space over Omega times the positive t-axis. The spectral region is bounded away from the real axis, and the existing weighted forcing bound makes these features integrable.

The admitted function-valued normalized feature map Lhat is injective on every finite chamber source. The positive-support four-trace span argument in `the-ordinary-middle-block-has-a-nonzero-cut-dependent-green-anomaly.md` also verifies that the fixed two-sheet sewing does not destroy this injectivity. Tensor products and typed direct sums preserve that injectivity. Consequently the actual three-seam inclusion is injective on all 720 coefficient directions, regardless of the rank of any finite point-sampling probe.

Use the two-sheet signature J and take the existing signature-closed feature envelope

W=span Lhat(V_chamber) + J span Lhat(V_chamber).

It is finite dimensional at this packet, J-invariant, and carries the inherited positive Hilbert norm and signed form. Its inclusion is not a declaration that the arithmetic source image alone has nondegenerate restricted Green form.

## 3. Keep both spectral indices

For two source letters u,v, the independently established slot Green identity is

K_uv(w,z)=hhat_u(w)^* J hhat_v(z) / [-i(z-conjugate(w))]

          =h_u(w)^* C_raw h_v(z) / [-i(z-conjugate(w))]

          =[B_uv(w,z)+R_uv(w,z)] / [-i(z-conjugate(w))].

Here B and R are the admitted shell-resolved boundary and forcing contributions. Their numerator channels remain distinguished. The denominator is attached to THIS spectral pair.

For finite coefficient packets the expression is

sum_(i,j) conjugate(c_i) d_j K_uv(w_i,z_j).

There is no common denominator that may replace all the pairwise denominators. This is checked by an exact hostile, not treated as a harmless implementation convention.

The full observer envelope also contains signature-transformed features. For epsilon,delta in {0,1}, its prescribed slot kernel is

K_uv^(epsilon,delta)(w,z)
 =hhat_u(w)^* J^(epsilon+delta+1) hhat_v(z)
   / [-i(z-conjugate(w))].

Thus equal signature parities use the signed numerator; opposite parities use the positive Hilbert numerator. The latter is fixed by the existing carrier, not a newly fitted relation metric. Restricting observers to the signed source-image self-pairing would omit these needed channels.

## 4. Spectral packets and the Hilbert beta map are distinct levels

The cross-spectral identity is a pairing of the t-dependent evaluated features at w and z. It is available on the finite-dimensional analytic feature envelope; arbitrary L2 functions are not being point-evaluated.

The actual Hilbert carrier pairing instead integrates the diagonal w=z over Omega. Tensor carrier pairings integrate the corresponding diagonal independently in each retained slot. The two constructions must not be silently identified with an arbitrary finite spectral sampling rule.

The full function-valued receiver retains the cross-spectral kernel family as well as the integrated pairing. Finite-dimensionality of the feature envelope, the spectral gap from the real axis, and the admitted forcing bound justify these finite-slot integrals. No uniform estimate over growing prime packets is inferred.

## 5. The sewn packet identity

Expand a seam vector in its actual typed edge and record-shape blocks. A block records its three seam positions, middle vertices, memory lengths, vacuum versus feature seam letters, and ordered retained slots. Distinct orthogonal blocks are not merged.

For matching shapes, the prescribed pairing is the root pairing times the product of the slot kernels, multiplied by the already declared memory weights. In schematic form,

Q_s(x,y) times omega_shape times product_l K_(u_l,v_l)^(epsilon_l,delta_l)(w_l,z_l).

The root factor keeps its own state dependence and, at the spectral packet level, its own spectral indices. Vacuum factors contribute the prescribed vacuum pairing. Memory weights such as tau^(2r) belong to the actual memory factors; no additional weight on seam letters is inferred from a total retained degree.

Extend this formula sesquilinearly over all actual derivative terms. It retains cross-shell, cross-orientation, and cross-spectral entries. Inserting B_uv+R_uv at each signed slot yields the tensor sewing of the independently proved arithmetic slot kernels. These labelled terms are retained before any final scalar integration.

Now apply the two corrected grouping maps to a vector in the common derived cell. The source comparison says their coefficients and typed output slots are identical. Hence the above paired packets agree for EVERY ambient observation, before spectral aggregation. Integrating the valid diagonal identity gives equality for the actual Hilbert Green pairing as well.

This is a comparison for the already specified tensor assembly. It is not a derivation of a new global arithmetic boundary operator whose metric is assumed, without proof, to equal that tensor assembly.

## 6. The paired observation and noncollapse

On the ambient bottom term Y^(-1), let

beta_Y(y)(z)=q_Y(z,y).

The root and seam carriers have the prescribed nondegenerate ambient pairings. Define

O=j_X^vee beta_Y : Y^(-1) -> V^h.

Equivalently, O(y)(x tensor p)=q_Y(x tensor j_3(p),y).

The map is surjective. Indeed j_X is injective by function-valued source faithfulness and the exact product construction; its finite-dimensional conjugate dual is surjective; beta_Y is an isomorphism. Thus every shifted product functional is realized by an ambient paired observation. The number of detected coefficient channels is 720, with the full root carrier retained.

This does not require j_X^vee beta_Y j_X to be invertible. The primal image can be isotropic while its ambient paired observation is surjective. The checker includes exactly that hostile.

Since d j_X=0, O annihilates the corresponding Green-dual boundaries. There is no lower primal degree before -1, so the dual observation descends to the corresponding degree-plus-one dual homology. This supplies the shifted observation, not merely a collection of scalar evaluations on chosen basis columns.

## 7. The comparison square and variance

Let Theta_L and Theta_R be the corrected grouped joint observations, and f_L,f_R the maps from the common block complex. The source equality is

Theta_L f_L = Theta_R f_R.

Pulling an ambient paired observation back gives

f_L^vee Theta_L^vee beta_Y
 = f_R^vee Theta_R^vee beta_Y.

On the shifted component this is the dual connecting inclusion followed by the same surjective observation O. Conjugate duality sends the degree-minus-one projection to an inclusion in degree +1 and reverses composition order.

For the full source objects, the corresponding statement uses perfect-module duality and the existing finite-projective tensor-Hom beta comparison. It does not identify the dual of a source ideal with the opposite-history ideal.

Opposite history uses Nima's separately normalized three-seam reversal, including its Koszul phase. Green contragredience uses the prescribed mate and cochain dual differential. Creation is not identified with contraction. Likewise the suspension correction needed for regrouping is retained; omitting it negates the paired observation on every nonzero product channel.

## 8. What remains outside this theorem

The result closes the function-valued paired square for the six-prime SHIFTED PRODUCT attachment, relative to the admitted tensor carrier and slot Green identity.

It does not establish:

- isometry between complete coarse-block Green forms;
- the ordinary degree-zero paired sewing on all 2160 common coefficient coordinates;
- positivity or nondegeneracy of the primal relation self-pairing;
- descent through arbitrary nonminimal product factorizations;
- a uniform completed prime-packet receiver or exclusion of phantom compatible families.

The ordinary sector remains a separate retained summand of the common complex. Its disappearance under the particular connecting projection is not a claim that it is radical or may be removed from the full packet.

## Verification

`uv run --with sympy python research/grothendieck/checkers/check_function_valued_paired_associativity.py`

Exact fixtures verify the slot polarization formulas, signature-closed observer channels, independent spectral denominators, tensor regrouping with a nontrivial root pairing, the missing-sign hostile, complex conjugate-dual order, and surjective ambient observation of an isotropic image. The artifact pins the exact source comparison certificate by hash.

The fixtures are not evaluations of the actual Clark traces. Analytical transfer uses the established function-valued injectivity and slot Green identity; no sampled spectral rank or new numerical forcing approximation is used.

References:

- `the-six-prime-associativity-cell-retains-the-derived-middle-block.md`;
- `research/voevodsky/function-valued-clark-faithfulness-supersedes-finite-spectral-rank-probes.md`;
- `research/voevodsky/a-weighted-clark-feature-history-receiver-with-convergent-comparisons.md`;
- `research/nima/the-attachment-opposite-comparison-needs-a-koszul-correction-and-a-distinct-green-dual.md`.
