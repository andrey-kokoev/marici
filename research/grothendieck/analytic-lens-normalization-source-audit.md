# Source audit of the analytic lens and half-density normalization

## Question

Are the logarithmic grade and critical half-density merely imported from completed zeta, or do approved source constructions select them?

## Exact conditional constructions

Two independent source packets sharpen the answer.

First, on the positive-integer label module the Gaussian scale operator

`Q e_m = m^2 e_m`

has positive logarithmic degree `(1/2) log Q`. The complete prime-power divisibility tower reconstructs the same operator by the von Mangoldt divisor identity. Sampling the Gaussian orbit at `k log p` implements `n -> n p^k`. Thus, once the integer-labelled Gaussian analytic module and prime-power incidences are admitted, the grade `k log p` is not arbitrary.

Second, the same dilation acts with norm ratio `p` between additive-Haar `L^2(dx)` and multiplicative-Haar `L^2(dx/x)`. The positive square root of their Radon--Nikodym ratio gives `p^(1/2)`, and Mellin weighting is isometric in the additive sector exactly at real part `1/2`. Reciprocal Todd-chart sewing independently forces the same half-density locally.

Hence the logarithmic grade and half-density are source-rigid inside the declared arithmetic--Gaussian--Haar sector. They are not selected from zeros.

## Remaining emergence gap

Neither construction is coefficient-neutral Carrier arithmetic. Both require an analytic realization functor sending the conditional initial-semiring class `m` to:

- a basis vector `e_m` in a positive Hilbert module;
- the real eigenvalue `m^2` of `Q`;
- dilation by the positive real scalar `p`;
- additive and multiplicative Haar measures.

The initial semiring has a unique unital map into any chosen commutative semiring, but it does not choose the ordered real target, topology, exponential/logarithm, Haar sectors, or Gaussian vacuum. Different sector fibres have no established common selection-compatible arithmetic action.

The weighted cut-atom incidence likewise starts from authorized prime-power labels and Euler half-density weights; it constructs a continuous arithmetic-to-analytic map on that typed source core but does not derive those inputs from the coefficient-neutral Carrier.

## Hostile test

Keep the intrinsic semiring fixed and realize it in two ordered analytic coefficient systems with different norms or exponential scale calibrations. Each retains multiplication and factorization; after its own logarithm it supports a Gaussian degree and relative-Haar half-density, but the numerical cut locations and weights differ. No initial-semiring law selects between the realizations.

## Disposition

The half-density and logarithmic grade gates pass conditionally after the arithmetic Gaussian/Haar lens is declared. The first missing arrow is the realization functor from the coefficient-neutral Carrier `pi_0` semiring to that specific ordered analytic Hilbert module, including its Gaussian vacuum and Haar normalization. Completed-zeta transport may use this lens; it does not prove the lens is forced by the shared Carrier.
