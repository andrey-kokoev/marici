# Symmetric powers survive conditionally but collapse on pi_0

Epistemic-graph event: 1381.

## Conditional symmetric-power construction

Nima's exact algebraic candidate replaces the full noncommutative `D4`
source symmetry by `A=D4_ab` and forms the rig groupoid of finite component
sets with `A`-labels on component automorphisms.  On this finite-set surface,
exterior and symmetric powers are canonical:

- `Lambda^r(I)` is the set of `r`-element subsets of `I`;
- `Sym^r(I)` is the set of size-`r` multisets in `I`.

They are functorial under labelled component bijections.  For an object with
`m` components their decategorified generating series are

`lambda_t(m)=(1+t)^m`,

`sigma_t(m)=(1-t)^(-m)`.

Thus the conditional rig carries the binomial lambda structure after passing
to `pi_0`.

## Adams collapse

The Adams operations associated to the binomial lambda structure satisfy

`psi^n(m)=m`

for every `n>=1`.  Hence all prime-indexed Adams candidates coincide with
the identity on `pi_0`.  Symmetric powers add coherent combinatorics, but
decategorized component count still supplies no place-dependent Frobenius
spectrum.

## Where information remains

Before decategorization, `n` indistinguishable copies of the connected
generator have automorphism group

`A^n semidirect S_n`.

These wreath-product stabilizers retain cycle-type information erased by
`pi_0`.  Therefore they are the smallest located reservoir from which
nontrivial ghost, power, or Burnside--Witt operations might be derived.
This packet does not assert that the required operations or trace maps have
already been constructed.

Two authorization boundaries remain:

1. the `D4 -> D4_ab` quotient is the universal algebraic unit-compatible
   quotient but is not established as a shared physical quotient; and
2. forming a Burnside or Witt completion from the wreath sectors is a new
   construction, not a consequence of component counts alone.

## Verdict

The symmetric-power gate is conditionally positive at the labelled rig
groupoid and negative after `pi_0` decategorization.  The next hostile test
must retain wreath-product cycle types and ask whether their canonical trace
operations satisfy Frobenius composition without importing prime labels.

## Scope

No full-Carrier lift, physical authorization, Witt vector object, Frobenius
trace, or Euler product is claimed.
