# A two-atom source falsifies universal null-implies-exchange-intertwining

## Exact positive source

Take the positive half-line source with atoms at \(u=1\) and \(u=2\), with
weights three and one. Reciprocal evenization gives the completed readout

\[
F(z)=3\cosh z+\cosh 2z.
\]

Put \(y=\cosh z\). Since

\[
\cosh 2z=2y^2-1,
\]

the zero equation is

\[
2y^2+3y-1=0.
\]

Its negative root is

\[
y_-=-\frac{3+\sqrt{17}}4<-1.
\]

Therefore

\[
z_*=a+i\pi,
\qquad
a=\operatorname{arcosh}
\left(\frac{3+\sqrt{17}}4\right)>0
\]

satisfies \(F(z_*)=0\). This is an exact off-seam zero of a positive,
reciprocally even finite source.

## Exchange defect remains bright

The source-weighted transport–exchange energy is

\[
\mathcal E_{\mathrm{ex}}(a)
=8\left(3\sinh^2a+\sinh^2(2a)\right).
\]

Since \(a>0\),

\[
\mathcal E_{\mathrm{ex}}(a)>0.
\]

Thus

\[
F(z)=0
\not\Longrightarrow
\mathcal E_{\mathrm{ex}}(\operatorname{Re}z)=0
\]

for the category of positive reciprocally even sources.

## Consequence for the control tower

Aspect's exchange detector is faithful: it correctly reports that the hostile
zero is off the self-dual seam. But the source state is still allowed to have
a dark scalar channel while that detector is bright.

Therefore no universal Green or control law using only

- positive source weights;
- reciprocal evenization;
- scalar nullity; and
- the transport–exchange commutator

can authorize the desired action. Any such law is falsified by this two-atom
packet.

The missing implication must consume a theta-specific capability absent from
the hostile source. The moving integer-incidence connection is currently the
only explicit candidate that has survived the earlier tests.

## Sharpened conditional target

Let \(R_{\mathrm{inc}}\) be the residual against the canonical moving-incidence
connection. The smallest source-specific statement is

\[
R_{\mathrm{inc}}=0
\quad\land\quad
F(a+it)=0
\quad\Longrightarrow\quad
\mathcal E_{\mathrm{ex}}(a)=0.
\]

The two-atom source fails the first antecedent. The denominator-three hostile
also fails it, by a residual already visible in the first unit cell. The
canonical theta source satisfies it by construction.

This statement remains RH-strength for the canonical source. Its value is
typing: it identifies the exact source capability that a noncircular Green
law must use, and rejects every proof that invokes only aggregate positivity
and reciprocal symmetry.

## Seven-axis classification

For the two-atom witness:

- scalar: dark at \(z_*\);
- packet: bright;
- incidence: mismatched with the integer constructor;
- history: finite atomic history retained;
- path: opposite Mellin transport present;
- coefficient: native for this hostile source;
- action: scalar null does not trigger exchange intertwining.

The tester therefore rejects a universal action law while leaving the
canonical-incidence conditional programme open.

## Operator stimulus

The operator instructed us to continue after applying Aspect's tester. The
updated action axis suggested the immediate hostile question: can a positive
reciprocal source be scalar-dark while the exchange detector is bright? The
two-atom calculation answers yes exactly and forces canonical incidence into
the antecedent of any surviving theorem.
