# Paired Hadamard bridge now needs authoritative citations, not more algebra

## Question

Does the repository already contain authoritative bibliographic support for the paired `Xi` product, zero count, low-ordinate zero-free bound, and inverse-Laplace uniqueness used by the heat bridge?

## Repository search

A bounded search across research Markdown for named classical references and explicit zero-count citations found no source locator supporting the required theorem chain. Existing packets derive or invoke the identities but do not provide publication, theorem number, page, or stable external locator.

The algebraic normalization and convergence mechanism are now recorded and symbolically checked. Repeating them does not replace source authority.

## Required citation contract

A source-complete theorem needs four independently checkable entries:

1. **Centered Hadamard product:** a source proving that `Xi(y)=xi(1/2+y)` is entire of order one, even, and admits the paired product over `a~-a` with multiplicities.
2. **Zero count:** a source proving a bound sufficient for
   \[
   \sum m_\rho/|\rho-1/2|^2<\infty.
   \]
3. **Low-ordinate exclusion:** a rigorous source establishing no nontrivial zero with `|Im rho|<=1/2`, or another explicit bound yielding `Re lambda_rho>=delta>0`.
4. **Laplace uniqueness/interchange:** a source theorem whose hypotheses match the normally convergent complex exponential sum and the Euler-product inverse transform.

Each entry must include the exact convention for counting zeros. A citation to a positive-ordinate formula is insufficient unless it explains how off-line functional and conjugate orbits are counted.

## Acceptance test

The citation packet passes only if a fresh reader can verify:

- one `rho,1-rho` orbit contributes one factor `1-y^2/a^2` with original multiplicity;
- off-line conjugates remain distinct paired orbits;
- the product may be differentiated normally away from zeros;
- absolute Laplace interchange holds in a declared half-plane;
- the resulting transform equals the endpoint--gamma--prime `B'(x)` with no factor-of-two ambiguity.

## Authority boundary

No external-search MCP is admitted on this Site, and shell or network fallback is not authorized. I did not invent page numbers or use an ungoverned network lookup.

## Disposition

The analytic bridge is citation-blocked, not algebra-blocked. Request source locators from the operator or a team packet with publication-level references. Do not claim source-complete equivalence until this contract is satisfied.