# The negative spline packet is a criterion-level alarm, not an RH conclusion

## Strength audit

The corrected computation supplies a compact cubic packet `h` whose autocorrelation is an even compactly supported septic spline with nonnegative Fourier transform, double pole annihilation, and negative value under the declared executable explicit-formula functional.

If all of the following comparison arrows are proved, the result would contradict Weil positivity and therefore imply failure of RH:

1. the checker functional equals the standard completed-zeta Weil functional with the same global sign;
2. the cubic autocorrelation convention equals the criterion involution `g*tilde(conjugate g)`;
3. the physical scaling `u=x/2` and spectral test `h(t)=integral f(2x)e^(itx)dx` preserve the positivity orientation;
4. pole deletion is exactly justified by the retained double annihilator;
5. the archimedean signed-atomic enclosure and prime enumeration independently reproduce the linear functional;
6. the zero-side contour theorem applies to this packet with no omitted boundary term.

No current external source packet proves this full chain. The internally derived convention map and two implementations share formulas and therefore are not independent authority for the criterion orientation.

## Required hostile checks

Before any RH interpretation, perform three independent checks:

1. **Zero-side low-zero sum.** Evaluate the packet transform on a certified list of critical-line zeros. Every line-zero contribution to an autocorrelation criterion must be nonnegative. Compare the partial sum and a rigorous tail bound against the source-side negative interval.
2. **Direct quadrature.** Independently evaluate the archimedean integral from the digamma multiplier, avoiding the Euler--Maclaurin/atomic-tail implementation.
3. **Published-theorem derivation.** Map a cited Weil criterion theorem line by line to the checker, including whether positivity is `L(f)>=0` or its negative and every factor of two.

A mismatch in any check is a computation/normalization defect. Agreement would elevate the result to a potential counterexample requiring external mathematical verification.

## Disposition

The current strength is a robust negative theorem about a declared executable form. It must not be reported as evidence against RH. The next action is independent zero-side and direct-quadrature reproduction, not further matrix enlargement or interpretation.
