# Half-Density Retyping Pushes Every Local Euler Zero Outside the Critical Stability Disk

## The source Hilbert coordinate

Give the valuation basis the half-density norm

\[
\|e_k\|^2=p^k.
\]

Then

\[
f_k=p^{-k/2}e_k
\]

is orthonormal. If (T_pf_k=f_{k+1}\), the original valuation shift satisfies

\[
S_p=p^{1/2}T_p.
\]

Therefore

\[
I-p^{-s}S_p
=I-bT_p,
\qquad
b=p^{1/2-s}.
\]

The local Hilbert stability disk \(|b|<1\) is exactly

\[
\Re s>\frac12.
\]

## The observation changes with the basis

The source augmentation obeys \(\varepsilon(e_k)=1\). In the orthonormal
half-density basis,

\[
\varepsilon(f_k)=p^{-k/2}.
\]

Consequently the truncated transfer is

\[
g_{p,N}(b)
=\sum_{k=0}^{N}p^{-k/2}b^k
=\sum_{k=0}^{N}p^{-ks}.
\]

Its zeros satisfy

\[
p^{-1/2}b
\in
\{\omega:\omega^{N+1}=1,\ \omega\ne1\}.
\]

Hence every local transmission zero obeys

\[
|b|=\sqrt p>1.
\]

Equivalently, every zero lies on \(\Re s=0\), outside the closed source-Hilbert
stability region \(\Re s\ge1/2\).

## Meaning

The half-density does two things inseparably:

1. it moves the ordinary-state stability boundary to the critical line;
2. it weights the augmentation port so that prime-local transfer zeros remain
   beyond that boundary.

Thus no individual prime supplies a transmission zero on or inside the RH
half-sector. The critical-line zero phenomenon cannot be a repeated local
root-of-unity mechanism. It must be created by completed interaction among
prime labels, reciprocal sewing, and the archimedean port.

## Source-authority warning

Keeping the orthonormalized shift while leaving augmentation coefficients all
equal would manufacture the polynomial (1+b+\cdots+b^N\) and place zeros on
the critical wall. That mixes coordinates from two different presentations.
The transported observation weights (p^{-k/2}\) are mandatory.

This is the smallest finite falsifier for any proposed local RH explanation
that changes the state metric without transporting the boundary readout.

## Revised global target

The global completed Rosenbrock system must retain both:

- the half-density state metric that creates the two Hilbert sectors;
- the transported augmentation maps that reproduce the Euler coefficients.

Only cross-place coupling can then move transmission zeros from the prime-
local line \(\Re s=0\) to the completed symmetry wall \(\Re s=1/2\). Deriving
that coupling is the remaining explanatory task.
