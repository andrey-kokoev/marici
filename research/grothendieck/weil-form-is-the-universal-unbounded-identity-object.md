# The Weil form is the universal unbounded identity object

## Question

Can the unbounded-positivity proposal identify a canonical comparison object before an external radial-G4 carrier is supplied?

## Source-native object

Let `D` be the authorized logarithmic test algebra and define

`<f,g>_W = W(f*g*)`,

where `W` is the complete prime, gamma, endpoint, and mixed explicit-formula distribution. This Hermitian form exists unconditionally. Quotienting by its radical gives the canonical source space

`K_W = D / rad(W)`.

Without RH, `K_W` is an indefinite Hermitian/Krein-form object. Under unbounded Weil positivity, its closure is a Hilbert space. Thus positivity is not an attribute imported from a proposed G4 metric; it is exactly the transition from the unconditional source form to its positive completion.

## Universal identity property

Suppose a candidate radial carrier `(V,G)` receives a dense source map `R:D->V` satisfying

`G(Rf,Rg)=<f,g>_W`

and `ker R=rad(W)`. Then `R` factors uniquely through an injective form-isometry

`K_W -> V`.

If the quotient range is dense and both forms are closed, the completion is determined up to unique unitary or Krein-unitary equivalence. Therefore `K_W`, not a finite coordinate fixture, is the universal identity object for every faithful radial realization of the complete explicit form.

This reframes `R_zeta`: its mathematical content is not a basis coincidence but a representation of the universal Weil-form quotient preserving labels, reciprocal transport, and closure.

## Consequence for G4 authority

An external owner-authorized G4 carrier is still required to claim that SCC's `conservative_green_complex` realizes this universal object. But the arithmetic side no longer waits for that declaration. It already has the canonical comparison source and can state an exact conformance theorem:

- construct the G4 Green form independently;
- show its source synthesis pulls back to the complete Weil form;
- prove kernel equals the Weil radical;
- prove dense quotient range and closed-form compatibility.

Any carrier satisfying these conditions is the same completed form object up to the relevant isometry.

## RH-bearing theorem boundary

Unbounded positivity of `W` on all convolution squares is equivalent to RH under the standard Weil criterion. The identity theorem does not prove positivity. It prevents circularity by keeping the source quotient indefinite until positivity is established.

The viable proof target is now one complete source inequality rather than a search for a fitted operator:

`W(f*f*) >= 0 for every authorized f`.

A G4 realization helps only if its independently positive Green form and source synthesis prove this pullback identity without assuming the divisor is on the critical line.

## Next exact test

Audit the complete assembled boundary synthesis against the known joint Green-form domination criterion. The first required inequality is radical annihilation and uniform domination of the full prime, seam, endpoint, and archimedean synthesis by one independently positive Green form. Channelwise positivity is insufficient because cross-channel radicals can survive.
