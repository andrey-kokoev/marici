# Primitive and Square Currents Belong to the Euler Determinant Chart, Not as Additive Theta Seam Cells

## The proposed attachment was ill-typed

The moving-seam prefix current is constructed from the completed theta source
\(\Phi\). It already contains the full integer-labelled source before any
Euler factorization. The primitive and prime-square currents arise in a
different presentation.

Let the primitive Euler loop be

\[
L_s e_p=p^{-s}e_p.
\]

Then

\[
-\log\det(I-L_s)
=\sum_{k\ge1}\frac1k\operatorname{Tr}(L_s^k).
\]

The primitive and square currents are the \(k=1\) and \(k=2\) cyclic
cumulants of this one loop. They are not independent state channels or
additional pieces absent from the completed theta source.

## Correct comparison architecture

The additive theta boundary line and the multiplicative Euler determinant line
must be compared as complete presentations:

\[
\mathcal L_\theta(s)
\longrightarrow
\mathcal L_\infty(s)
\otimes
\operatorname{Det}_{\rm rel}(I-L_s).
\]

The relative determinant retains the first two cumulants as typed boundary
data while the \(k\ge3\) tail is trace class. This typing is necessary in the
Euler chart, but it does not authorize adding those cumulants to the theta
moving-seam energy.

## Double-counting diagnosis

Appending separate \(k=1\) and \(k=2\) ports to the theta current would do
three wrong things:

1. count arithmetic already present in the integer-labelled theta source a
   second time;
2. replace cyclic determinant reconstruction by linear operator addition;
3. mix distributional, Hilbert non-trace-class, and trace-class grades inside
   one boundary norm.

The low cumulants are indispensable for changing charts, not for completing
the already undecomposed theta chart.

## Revised RH frontier

Within the theta/Green lane, the local forcing current and its mixed-prime
gluing are complete at source level. The remaining direct theorem is now
singular:

> Prove that a completed scalar zero makes the total oriented theta endpoint
> flux vanish, while the faithful affine/relative energy remains nonzero.

Separately, the Euler comparison must prove that its relative determinant line
is isomorphic to the theta boundary line with the primitive and square
cumulants retained. That is a presentation-equivalence theorem, not an
additional term in the Green identity.

## Meaning

This is another case of one object appearing as several after a change of
coordinates. Theta sees one completed additive source. Euler sees one primitive
feedback loop through infinitely many cyclic returns. The first two returns
look exceptional only because the determinant completion changes regularity at
those orders.

Keeping meanings primary requires preserving both presentations without
combining their coordinates as though they were independent physical degrees
of freedom.

## Falsifier

This correction fails if one derives, before Euler factorization, a source-
native theta boundary observable whose value is not already contained in the
moving-seam/affine packet and whose Euler image is specifically the primitive
or square cumulant. No such independent theta observable is presently known.

