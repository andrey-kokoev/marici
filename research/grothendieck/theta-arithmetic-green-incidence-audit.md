# Audit of primitive/square incidence into the theta Green carrier

## Decision

Finite primitive and square incidence maps do exist, but their codomain is the
Euler scale-current rigging, not the additive theta Green state module. Adding
them as independent theta Green rows is source-incompatible and would double
count the completed theta source.

## What is constructible

`theta-primitive-and-square-incidence-are-atomic-scale-currents.md` gives exact
finite-cutoff maps

`I_k(p,k)=(1/k)p^(-k/2) delta_(k log p)`, with `P=I_1`, `Q=I_2`.

These recover the primitive and prime-square Tate countercurrents. Their
completion types differ: `I_1` requires an exponential/Laplace test rigging,
whereas `I_2` is tempered but non-finite. Neither admits the undamped critical
sine readout separately without relative archimedean completion.

## Why these are not the missing Green rows

`primitive-and-square-currents-belong-to-the-euler-determinant-chart-not-as-additive-theta-seam-cells.md`
proves the typing correction. The currents are the first two cyclic cumulants
of the Euler loop and are already represented inside the undecomposed,
integer-labelled theta source. Appending them to the theta moving-seam energy
would double count arithmetic, replace determinant reconstruction by linear
addition, and mix incompatible regularity grades in one norm.

The authorized Euler-to-Green path in
`the-euler-to-green-incidence-has-rank-one.md` factors through scalar source
evaluation:

`nu -> nu(f) -> nu(f) G_s`.

It has rank at most one and annihilates nonzero labelled packets whose four
evaluated coefficients sum to zero. Thus it preserves a scalar control history,
not separate primitive and square Green response rows.

The common-trace arithmetic aggregation is likewise a transported line torsor
requiring one source frame; it creates no extra finite-cutoff kernel after the
trace but no independent response direction either.

## Consequence

The requested primitive/square rows cannot honestly complete the proposed
five-row theta pro-Gram. The source-compatible architecture keeps:

1. additive theta/Green incidence on its native carrier; and
2. primitive/square data in the relative Euler determinant chart,

joined only by a separately proved determinant-line comparison. Atomic-current
incidence is therefore real but does not instantiate `S_X`, the Green residual
`Q_X`, or a comparison to `D_theta`.

For confinement, the earliest direct theta gate is unchanged: prove that a
completed scalar zero closes the total oriented theta endpoint flux while the
source-derived affine/relative bulk remains nonzero. Arithmetic row attachment
is not a valid shortcut.
