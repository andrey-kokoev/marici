# The five-site boundary commutator is specified but not yet evaluable

Epistemic-graph event: 1326.

## Availability theorem

For a proposed branch quotient `q`, the forced Betti map of Ledger 1312 can
be tested as a physical chain map from a finite packet consisting of:

1. source-derived based relative chain groups `C_n(G)` and `C_n(H)`;
2. their signed boundary matrices `D_G^n` and `D_H^n` in those bases;
3. the coefficient pullback matrix in the dual bases, or equivalently its
   pairing-adjoint matrices `S_q^n`; and
4. the basis comparison fixing orientation and endpoint/regulator
   normalization.

The decision is finite and exact in every degree:

`Omega_q^n = D_H^n S_q^n - S_q^(n-1) D_G^n`.

The proposed physical pushforward exists on the supplied complexes exactly
when every `Omega_q^n` vanishes.  A single nonzero matrix entry is a complete
falsifier for the forced candidate.  Conversely, vanishing is meaningful
only when all matrices and bases are source-derived; formal sheet-label
matrices cannot substitute for the relative complexes.

## Five-site inventory audit

The currently frozen five-site materials do not contain this packet.

- The physical sheet-orbit certificate supplies a 32-to-16 set-level branch
  census, but no relative boundary operator.
- The branch-chain lift gate exhibits multiplicity ambiguity and explicitly
  lists a boundary-compatible specialization as missing.
- The physical deck-transfer gate works in the formal delta-paired sheet
  basis and explicitly requires a source-derived relative-cycle trace/Gysin
  map.
- The Cayley--Menger contour artifact supplies a semialgebraic boundary locus
  and normalization before specialization, but explicitly leaves the
  exceptional boundary map and deck trace unavailable.
- The marked `A2` specialization supplies a primitive coefficient image but
  explicitly excludes physical relative-chain normalization.
- The three-site residue link supplies an integrand-level cycle whose
  boundary vanishes, but explicitly leaves its pairing with the source
  relative chain open; it is not the five-site quotient complex.

Thus `Omega_q` is presently **undefined from the frozen physical evidence**,
not known to be zero and not known to be nonzero.  The algebraic Mackey
theorems survive unchanged.  No physical relative-chain pushforward follows.

## Smallest decisive acquisition

For the hostile one-bit quotient `(C2)^5 -> (C2)^4`, it is enough to provide
the two adjacent degrees containing the chamber chain and its boundary:

`D_G^n`, `D_H^n`, `S_q^n`, and `S_q^(n-1)`,

with row and column labels tied to the source relative strata.  Computing
the displayed commutator then either closes the physical chain-map gate or
produces an explicit boundary stratum and coefficient witnessing failure.
