# The canonical quarter-density lift recovers the rigging, not orientation

## Bounded question

Is the quarter-density selected by positive staircase curvature supplied by a
canonical source construction, and does that construction add RH force?

## Finite-cutoff square-root construction

For logarithmic-derivative normalization, the positive prime-power measure has
weights

\[
w_{p,k}^{\rm LD}=(\log p)p^{-k/2}.
\]

On a finite prime-power packet, its canonical square-root vector has
coordinates

\[
\eta_{p,k}^{\rm LD}
=\sqrt{\log p}\,p^{-k/4}.
\]

Thus the quarter-density selected by the commutator audit is exactly the GNS
or Hellinger square root of the positive arithmetic measure. No fitted exponent
is required once the positive measure is declared.

For Euler-log normalization,

\[
w_{p,k}^{\log}=\frac1k p^{-k/2},
\]

and the square-root coordinates are

\[
\eta_{p,k}^{\log}
=\frac1{\sqrt{k}}p^{-k/4}.
\]

The connected-depth factor remains visible rather than being absorbed into
prime transport.

## Exact positive realization

Let \(U_p=p^{-1/4}T_p\). Then

\[
[U_p^{*k},U_p^k]
=p^{-k/2}P_{v_p(n)<k}.
\]

Multiplying this curvature by \(\log p\) or \(1/k\) reproduces the two
authorized staircase weight systems exactly at every finite cutoff.

## Global completion reproduces the known filtration

The squared norm of the logarithmic-derivative square-root packet is

\[
\sum_{p,k}(\log p)p^{-k/2}.
\]

Its primitive and square parts diverge, while the \(k\geq3\) part converges.
The Euler-log packet has the same three-level division. Therefore the
quarter-density lift does not remove the need for the primitive and square
boundary currents; it reconstructs their rigged completion classes.

## Hostile-source audit

The square-root construction depends on the positive arithmetic weight packet,
not on the theta source values sampled at the prime-power locations. Any
hostile archimedean source coupled to the same positive staircase inherits the
same quarter-density Hilbertization and the same exclusion curvature.

Hence the lift supplies:

- a canonical amplitude-to-energy type conversion;
- exact finite-cutoff coefficients;
- the primitive, square, and trace-class completion anatomy.

It does not supply:

- a zero-state boundary condition;
- reciprocal adjointness off seam;
- a sign for the oscillatory scalar compression;
- rejection of hostiles sharing the arithmetic measure.

## Result

The quarter-density constructor exists canonically as the square root of the
positive prime-power measure. It validates the coefficient architecture but
adds no zero orientation. The missing information is not the half-form itself;
it is the coupling of that half-form to the theta-labelled boundary state.

## Next gate

Form the feature packet

\[
\eta_{p,k}B_{p,k}[A](z)
\]

before summation and derive its reciprocal Gram kernel. The decisive test is
whether theta sampling creates a source-specific cross-term absent for hostile
sources. Diagonal norms alone are already classified and cannot confine zeros.
