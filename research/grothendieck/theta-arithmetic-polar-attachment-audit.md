# Arithmetic anomaly torsor--polar Poisson attachment audit

## Exact reciprocity closure

The archimedean transition

`gamma_infinity(s)=chi_infinity(1-s)/chi_infinity(s)`

satisfies `gamma_infinity(1-s)=gamma_infinity(s)^(-1)`, matching finite-prime
line inversion exactly.

Before aggregation, the Poisson continuation packet is

`I_f(s)=(A_f(s),A_fhat(1-s),fhat(0)/(s-1),-f(0)/s)`.

Fourier source transport and reciprocal parameter reflection act by the
pairwise swap `(1 2)(3 4)`. There is no extra polar term. Combined with the
grade-wise inversion of the arithmetic anomaly torsor, the entire reciprocal
naturality layer closes without a typed residual, provided the relative line
and full-moment archimedean value types are retained.

## Why closure has no confinement force

The scalar Tate readout is the fixed row `(1,1,1,1)`. Its kernel on the
four-channel carrier has dimension three and meets both reciprocal parity
sectors. Thus a nonzero boundary packet can satisfy the scalar zero condition
while every channel remains defined.

An exact degree-twelve Fourier-fixed hostile source has positive nonzero
integer samples and off-seam Mellin zeros. It satisfies the universal Poisson
and polar reciprocity architecture, proving that no current depending only on
the four channels, their swap, and scalar aggregation can confine zeros.

Likewise, canonical heat finite-part extraction is not positivity preserving:
a positive regulated diagonal maps to `zeta(0)=-1/2`. The signed boundary wall
must remain explicit rather than being quotiented into a positive scalar form.

## Remaining gate

Arithmetic--polar naturality is complete; the missing rung is not another
coherence correction. It is a source-specific Green or relative quadratic
object on a refined theta boundary state that:

1. precedes scalar zero testing;
2. retains prime-power provenance beyond the universal four-channel lift;
3. promotes scalar nullity into its boundary domain;
4. couples value and an independent flux/comparison row; and
5. has strictly positive norm on every nonzero admitted zero-state.

Without that refined object, reciprocity merely transports the
three-dimensional cancellation kernel.
