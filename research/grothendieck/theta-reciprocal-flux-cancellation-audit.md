# Audit of reciprocal-sheet cancellation of complete theta flux

## Decision

Opposite reciprocal orientation cancels a finite-model bivector endpoint norm,
but no approved theorem cancels the complete theta Green flux. The forcing
current transforms with a normalization anomaly and survives reciprocal
subtraction.

## Exact finite result

`opposite-oriented-reciprocal-sewing-cancels-the-faithful-bivector-boundary-flux.md`
uses normalized `F_4` and exterior functoriality to identify the two boundary
relationship states isometrically. Their oppositely oriented squared norms
cancel in the relative boundary expression. This preserves both states and is
a valid finite cancellation mechanism.

That packet explicitly leaves unproved the reciprocal signs in the full Green
identity, rigged Fourier--Tate isometry, arithmetic-current retention, cutoff
completion, and positive completed bulk.

## Complete-pairing obstruction

`reciprocal-bivector-doubling-has-a-normalization-anomaly.md` computes both
Green laws for an exact exponential source. Rescaling can make reciprocal
state energies and endpoint norms equal, but transports the forcing terms as
well. Subtraction gives

`2(2s-1) E_tilde = -2(F_tilde_s-F_tilde_(1-s))`,

with both sides nonzero off the seam. Therefore state isometry is strictly
weaker than isometry of the complete source-typed Green pairing.

The source-native parity rotation confirms the same residual. The reciprocal
tails form one forced Dirac system, and scalar nullity gives a Dirichlet
condition only in the symmetric channel `P`. Its bulk identity is

`2 Re(z)(||P||^2+||Q||^2) = -2 sqrt(2) Re<f,Q>`.

The antisymmetric forcing overlap remains uncontrolled. Fourier quarter-turn
reconstruction likewise transports the faithful defect rather than
annihilating it.

## Consequence

The finite bivector cancellation cannot be promoted to total endpoint/work
closure. Such promotion would silently discard the forcing coefficient and
would contradict the exact normalization-anomaly fixture.

The first missing object is a reciprocal mate on the complete current packet,
constructed before zero restriction, that transports states, endpoint forms,
and forcing incidence simultaneously. It must prove either exact cancellation
of the antisymmetric overlap or its absorption by independently typed seam,
archimedean, and Euler determinant currents, with completion-stable
normalization. No such mate is currently source-derived.
