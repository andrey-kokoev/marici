# The Single Adjoint Response Is a Codiagonal Shadow of Two Sector-Local Responses

## Aspect-tester diagnosis

Aspect's updated tester distinguishes two locally completed witness systems
followed by an outer relational mate from one tower whose final operation is a
local totalization. The distinction applies exactly to the proposed theta
response port.

The common source amplitude first duplicates into the reciprocal sectors:

\[
\mathcal U
\xrightarrow{\Delta}
\mathcal U_+\oplus\mathcal U_-,
\qquad
\Delta c=(c,c).
\]

The local forcing incidences then act separately:

\[
B_+c=-fc,
\qquad
B_-c=-fc.
\]

Their local adjoint responses on a reciprocal state \((u,v)\) are

\[
r_+=-\overline f\,u,
\qquad
r_-=-\overline f\,v.
\]

## The apparent single response

Taking the adjoint only after the common input has been diagonalized applies
the codiagonal

\[
\Delta^*(r_+,r_-)=r_++r_-.
\]

Thus the apparent one-port response is

\[
r_\Sigma=-\overline f\,(u+v).
\]

On the completed scalar-zero domain \(u+v=0\), this response vanishes
identically. It is blind to the very zero-state relationship that the
completion must retain.

But before codiagonal aggregation, the relative response is

\[
r_\Delta=r_+-r_-=-\overline f\,(u-v).
\]

For a nontrivial zero-state \(v=-u\),

\[
r_\Delta=-2\overline f\,u,
\]

which need not vanish.

## Correct factorization

The response object is not one port. It is a labelled pair retained until an
outer mate acts:

```text
sector +: source input -> tail state -> local response
                                                    \
                                                     outer common/relative mate
                                                    /
sector -: source input -> tail state -> local response
```

Equivalently, the natural output basis is the pair

\[
(r_\Sigma,r_\Delta).
\]

The scalar completed readout uses the common coordinate. The missing
relationship witness lives in the relative coordinate. Aggregating to
\(r_\Sigma\) before the outer comparison repeats exactly the kernel failure in
Aspect's `2(2+1)+1` test.

## Relation to the multi-tower language

The prior input--state--output chain was still overcompressed. It contains two
sector-local input--state--response towers plus a relational mate. The common
source looks like one thing only after diagonal duplication, and the response
looks like one thing only after codiagonal projection. Those two projections
erase different fibers.

This is a target-relative correction, not a claim of final ontology. Aspect's
fourth rung forbids treating the resulting finite carrier as absolutely
complete. For the declared zero-state distinction, however, the two local
responses are minimally necessary: the one-port quotient provably kills the
relative witness.

## Next source gate

Derive the two response rows separately from the labelled Fourier--Tate
boundary packet and retain their common instance key. Then determine whether
the doubled Green defect couples to \(r_\Delta\), while the completed scalar
section couples to \(r_\Sigma\). If both facts hold with source-derived signs,
the missing variance constructor has been located rather than invented.

