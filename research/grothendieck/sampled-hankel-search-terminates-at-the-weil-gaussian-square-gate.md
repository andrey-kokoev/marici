# Sampled Hankel search terminates at the Weil Gaussian-square gate

## Question

Did the prior-research search produce a noncircular source mechanism proving the gamma-plus-prime remainder cone?

## Exact surviving theorem

For every `t,h>0` and polynomial `p`, define the explicit Schwartz amplitude

\[
G_{t,h,p}(u)
=e^{-tu^2/2}q_h(u)p(e^{-hu^2}),
\qquad
|q_h(u)|^2=1-e^{-hu^2}.
\]

The sampled completed localizer is the Weil convolution-square value

\[
W(g_{t,h,p}*g_{t,h,p}^*),
\]

where `g` is the inverse Fourier transform of `G`. Remainder Hankel positivity plus unconditional completed-heat decay forces the exact superunit endpoint atom; subtracting it yields completed localizer positivity. Generic-mesh pole rigidity then forces RH.

Thus the remainder Hankel family is a concrete RH-equivalent source gate, not a weaker surrogate.

## Rejected mechanisms

The search supplied explicit falsifiers for:

- sectorwise gamma and prime Gram factors;
- PNT-main-density versus prime-fluctuation block factors;
- pointwise positive densities for the signed Weil distribution;
- unnamed nonlocal kernels, which polarization makes circular on a faithful core;
- fixed finite recurrences, rational transforms, finite transfer operators, terminating J-fractions, and stabilized finite Jacobi matrices;
- Gaussian smoothing-threshold and first-contact arguments, which control diagonals but not polynomial superpositions;
- quarter normalization as endpoint removal, because the sharp support edge restores the same rank-one defect;
- determinant condensation without independently signed source pivots;
- raw large-heat numerical extrapolation, whose margin lies at first-zero scale.

## What finite computation established

Uncertified scans found remainder localizers positive through rank four on three low/intermediate boxes. Rank-two positivity is genuinely completed: gamma/prime roles reverse under common centering, and neither raw sectors nor the PNT split stay positive.

The hardest low/intermediate rank-two box needs only `10^-6` source-value enclosures. Prime and gamma analytic tails are far below that budget. The current admitted Python interval special-function backend is defective, but a rational elementary enclosure remains implementable. Such a certificate would verify isolated points only.

## Asymptotic correction

The endpoint--first-zero formula is conditional on RH and serves only to explain numerical conditioning. It is not an unconditional positive asymptotic. Without RH, a least-real-part conjugate shell produces oscillatory leading terms. Rank one of the remainder is dominated by the growing endpoint atom. No renormalized positive limit may be assumed before spectral reality is proved.

## First missing mathematical object

A proof now requires one of the following, with explicit source maps:

1. a direct inequality proving the completed Weil form nonnegative on every `G_(t,h,p)` convolution square;
2. a proper non-surjective compression `B*WB` whose image is exactly the heat-polynomial family and whose positivity is established independently of the target cone;
3. a nonterminating Jacobi or continued-fraction source formula with independently proved positive coefficients and the forced endpoint residue.

No prior packet supplies any of these objects. Naming a mixed factor, applying GNS, or invoking Cauchy--Schwarz after defining the target form is circular.

## Disposition

The prior-research search is exhausted at a typed mathematical blocker: no source-derived positivity constructor for the RH-equivalent Gaussian-square Weil subcone is available. Do not generate further coherence, density, finite-scan, or generic-factor packets without a new endpoint--gamma--prime identity. Reopen only when such an identity, an admitted rigorous interval backend for a bounded diagnostic, or new source literature is supplied.