# Correction: the negative two-translate direction is the disagreement cubic packet

## Exact diagonalization

The certified two-translate matrix has the form

`G=[[d,c],[c,d]]`,

where

`d=L(f)>0`.

A corrected `N=3` evaluation gives a strictly positive cross value `c` much larger than `d`. Its eigenvectors are the coherent and disagreement combinations

`v_+=(1,1)/sqrt(2)`, `v_-=(1,-1)/sqrt(2)`,

with eigenvalues `d+c` and `d-c`.

The corrected interval certificate gives

`c` in `[0.004366227496,0.004366339865]`.

Thus `d+c` is positive while

`d-c` lies in approximately `[-0.00435455,-0.00435432]`.

The negative direction is the disagreement, not the coherent sum. The earlier `N=1` generalized-tail execution failed baseline regression and is retracted.

## Explicit source packet

Let `g` be the compact cubic packet whose autocorrelation is the checked septic spline, and let

`delta=4 log 2`.

Then

`h=g-tau_delta g`

is compactly supported and piecewise cubic. Its Weil energy in the declared convention is

`q(h,h)=2(d-c)`, which is strictly negative. The checker records the exact rational interval.

This is an explicit negative packet, not merely a negative determinant or an existential eigenvector.

## Interpretation

The baseline packet is almost null and positive, but a positive cross-correlation much larger than the diagonal makes the disagreement packet negative. Sampling positive diagonal values therefore concealed an immediate interference obstruction. This is structurally analogous to the reciprocal swap-block lesson: off-diagonal cells decide signature.

The result does not identify a zeta zero or prove any statement beyond the declared executable form. Under a source-authorized Weil convention, a negative test would be compatible with an off-seam zero but would not localize which zero caused it.

## Disposition

The positive-spline promotion branch is falsified in its first nontrivial Gram cell. Independent reproduction and source normalization remain required before criterion-level interpretation.
