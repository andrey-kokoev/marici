# Aspect's full machinery extracts an admitted primitive-remainder port

## Full-stack execution

The complete current Aspect stack was executed, not only its top-level tower:

- ontology falsifier and four-rung aggregation;
- all 64 admissibility profiles and five-rung aggregation;
- quotient-aware prototype portfolio;
- environment-port tomography analyzer;
- sealed-controller replay analyzer;
- comb-referenced comparison analyzer;
- four-atom cross-scale hostile;
- acquisition-authoritative laboratory portfolio;
- six-rung aggregation.

All eleven executables returned success.

## Applying all eight ontology mutations

The eight mutation types do not receive one common disposition.

| Mutation | Theta/recursion realization | Disposition |
|---|---|---|
| nonlinear degree lift | Squaring the scalar readout adds no distinction because it is computed from the old readout | reject as alias |
| cross-coordinate composition | A joint law relating primitive remainder and reciprocal transport is typed but not constructed | defer |
| probe-dependent adversary | Choosing the source after seeing the probe has no theta-source provenance | reject |
| port creation | The primitive remainder is extracted exactly as `R=(1-cw)F` | admit |
| support refinement | Separate primitive and geometric-pole support is exactly observable | admit |
| arity lift | The proposed seed/forward/backward sewing cell is typed but lacks an authorized constructor | defer |
| singular support | Positive atomic hostile sources and their transforms are explicit exact witnesses | admit |
| fresh predicate | A sterile new name carries no source provenance or target | reject |

This yields three admissions, two deferrals, and three rejections.

## The new admitted object

Positive recursion had been treated as one completed object:

\[
F=\frac{R}{1-cw}.
\]

Aspect's port-creation mutation forces the lossless split

\[
F
\longmapsto
\bigl(R,,1-cw\bigr),
\qquad
R=(1-cw)F.
\]

This is not a fitted factorization. The recursion itself supplies the
extraction map. It exposes whether a hostile divisor was already present in
the primitive seed before geometric scale transport.

For reciprocal completion the corresponding split is

\[
R_{\rm rec}
=
(1-cw)(1-cw^{-1})F_{\rm rec}.
\]

The four-atom and two-atom hostiles therefore fail at the primitive-remainder
port, not at the transport port. This is the earliest currently available
source-level discriminator.

## What remains deferred

Extraction does not classify which primitive remainders are authorized by the
completed theta source. The missing object is still a joint constructor among

1. the primitive-remainder port;
2. forward scale transport;
3. backward reciprocal transport.

Its proposed arity-three relation has provenance, typing, a discriminating
target, and nonredundancy, but no derived operation or bounded constructor
test. Aspect's governor therefore correctly defers it.

## Portfolio consequence

Rung-five admission is not yet rung-six scheduling authority. The new
primitive-remainder diagnostic needs its own dependency and acquisition-load
contract before entering the portfolio. It must not inherit the cost or gain
coordinate of `mixed_prime_incidence` merely because both are arithmetic.

The existing laboratory controller is correct to:

- keep the four-atom cross-scale invariant deferred;
- retain mixed-prime incidence only for prefix gluing;
- avoid counting prime-local chart aliases as independent gains;
- keep apparatus availability outside its authority.

## New attack direction

The next finite target is no longer another zero scan. Derive the actual theta
primitive remainder at one prime-scale recursion, retain it as an independent
port, and ask for the smallest modular identity that distinguishes it from the
positive reciprocal hostile remainder. That identity is the missing
operational witness needed to promote joint seed sewing from `defer` to
`admit`.

