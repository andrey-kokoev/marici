# The fixed real theta integral bypasses scalar Euler completion

## Reconciliation

The weighted `B_epsilon` theorem is a valid completion result for scalar height evaluations. It is not the minimal completion route for the physical theta period.

The source packet `theta-modular-sewing-derivative-square-audit.md` already retains the fixed real representation

\[
Z(z)=\int_{\mathbb R}e^{zu}\Phi(u)\,du,
\]

where the completed theta source has superexponential decay at both real ends. On every compact spectral set and for every derivative order, the integrand has an integrable majorant. Dominated convergence therefore gives an entire function and permits all parameter derivatives on the same fixed contour.

## Consequence

No scalar embedding of unsmoothed Euler coefficients into `B_epsilon` is needed to construct or complete this period. The failure of `c_n=1` to lie in positive weighted sequence spaces blocks only that particular Euler-cutoff realization.

Conversely, replacing the fixed theta integral by the smoothed scalar series

\[
\sum_n 2e^{-\pi n^2u}n^{-z}
\]

changes the object unless a Mellin comparison theorem integrates out `u` and restores the completed normalization. Thus scalar cutoff completion is an optional presentation, not authority for the physical period.

## Remaining comparisons

Two questions must remain distinct:

1. **Existence and entire completion of the theta period:** supplied directly by the fixed real integral and its source decay.
2. **Identification with the classical completed zeta function and its Euler presentation:** requires the externally normalized Mellin--Poisson comparison, including gamma and polar terms.

Neither question proves positivity or excludes off-seam zeros. Once the first comparison is fixed, the surviving RH gate is the real-source positivity current already isolated in the source packet, not filler existence.

## Disposition

Close affine finite homology and physical completion as obstructions for the fixed real source. Retain the Mellin comparison square as an arithmetic normalization audit and the positivity current as the unresolved RH-strength theorem. Do not use failure of scalar Euler weighted decay to reopen existence of the fixed theta period.
