# Prior RH programme review selects the continuum value--flux gate

## Question

Which prior research branch remains nonredundantly executable after the finite translate-Gram certificates reached rank seven and the published explicit-formula normalization remained unmaterialized?

## Surviving and rejected ideas

The spline translate-Gram family is now a verified conditional diagnostic. Higher rank does not address its first missing object: a published convention fixing the complete Weil functional.

The Gaussian source-GNS kernel gives a clean all-label positivity criterion, but its converse still requires explicit-formula normalization, distributional uniqueness, and nonlinear extraction of the Xi divisor. Finite Gaussian Grams repeat the finite-rank limitation.

The transport--exchange commutator is a faithful detector of horizontal displacement. It cannot supply the action law from scalar nullity: the exact positive two-atom source has an off-seam zero with bright exchange defect. Any surviving implication must use theta-specific structure.

Autocorrelation, positive-real reconstruction, and the derived current cannot choose a spectral factor. A polynomial and its reciprocal reverse have identical boundary modulus and opposite radial zero locations. This branch is RH-equivalent unless an independent phase-bearing port is constructed.

Arithmetic reciprocal incidence matches the rigged transpose on every prime-power atom, but those atoms are not a core for the continuum seam: every test function supported in `(0, log 2)` lies in their annihilator. Density cannot bridge the gap.

## Selected executable gate

The smallest prior target not eliminated by these audits is the continuum value--flux identity. On a finite cut, construct reflected restriction between `H^1(0,L)` and `H^1(-L,0)` and compute the Green boundary form with value and flux traces at zero and at the moving endpoint. Test whether the pole-subtracted completed-theta precursor supplies either:

1. two independent source observers whose joint vanishing is equivalent to even Fourier-graph incidence; or
2. a source-derived relative determinant encoding the same rank-two defect.

This target consumes continuum seam information absent from the prime atoms and can use theta-specific moving incidence absent from the hostile two-atom source.

## Falsification test

At a finite source cutoff, form the two rows of the Fourier-graph defect and the proposed value--flux observer matrix. Reject the construction if its exact rank is less than two, if the flux row is obtained only by differentiating or dividing an assumed scalar zero without a source identity, or if the same implication holds for the hostile two-atom source.

## Disposition

Freeze higher translate ranks, autocorrelation polarization, and universal exchange-action arguments. Advance only the finite-cut continuum value--flux calculation. It remains conditional until the completed-theta source and the classical Xi normalization are connected by a directly inspected published formula.
