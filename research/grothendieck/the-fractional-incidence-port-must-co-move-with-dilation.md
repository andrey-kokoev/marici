# The fractional incidence port must co-move with dilation

## Transfer from the other sectors

Three independent results identify the same typing rule.

Aspect proves that a return incidence must move with its Green frame; freezing
the detector erases the endpoint current. Benincasa proves that a source port
specified at one fiber must be transported as a horizontal germ rather than
treated as a constant coordinate vector. Strominger proves that a coherent
horizontal port cannot be collapsed into the protected core without either
losing coherence or importing a new authority-bearing constructor.

The Haar-deficit fractional coordinate provides an exact theta realization of
all three rules.

## The co-moving fractional register

In the logarithmic-scale representation of the Haar deficit, define

\[
q_u(r)=\{e^{-u}r\},
\]

where (u) is the dilation coordinate and (r) is measured in the fixed
Gaussian frame. The cell boundaries occur at

\[
r=ne^u.
\]

Thus the integer-incidence detector moves under dilation. Replacing (q_u)
by the frozen register \(\{r\}\) changes the source constructor.

## Distributional connection law

The fractional-part distribution satisfies

\[
\frac{d}{dx}\{x\}
=1-\sum_{n\in\mathbb Z}\delta(x-n).
\]

Since (x=e^{-u}r), the chain rule gives

\[
\partial_uq_u(r)
=-e^{-u}r
+e^{-u}r\sum_{n\in\mathbb Z}
\delta(e^{-u}r-n).
\]

On the positive ray this may equivalently be written

\[
\partial_uq_u(r)
=-e^{-u}r
+e^u\sum_{n\geq1}n\,\delta(r-ne^u).
\]

The two terms have distinct types:

- the first is the smooth Haar current;
- the second is the moving weighted lattice-boundary current.

Their difference is the infinitesimal integrality-defect current. The
arithmetic comb is therefore generated by horizontal transport of the
fractional incidence port relative to the dilation frame.

## Why freezing the port is fatal

If the fractional register is frozen while the Gaussian frame dilates, its
derivative is zero and the moving endpoint term disappears. One can still
evaluate a scalar deficit at isolated scales, but the constructor no longer
retains how Haar cells cross integer boundaries.

This is precisely Aspect's frozen-detector failure in the theta setting. The
endpoint current is not a property of a fixed sawtooth function alone. It is
carried by co-transport of the sawtooth incidence with the scale frame.

It is also Benincasa's static-versus-horizontal distinction. The value of the
fractional register at one scale is merely initial data; its jumps must be
continued by the source connection.

## Transfer from the monitor and protection results

The complete moving incidence packet can be faithful relative to the declared
cell ontology, while the scalar Haar deficit is only one monitor of it.
Strominger's ubermonitor theorem therefore transfers with a scope boundary:
faithfulness must be proved for the full moving register and endpoint packet,
not inferred from its scalar output.

The same port should remain nonrepresentable inside the scalar completed
line. Collapsing it into that line either erases the boundary jumps or grants
the scalar readout authority to reconstruct incidence data it does not carry.
This is Strominger's coherent-port trilemma in source form.

Kitaev's all-order cancellation theorem supplies the renormalization warning.
Cancelling the smooth Haar response in the scalar line is lossless only while
the moving fractional register and its endpoint history remain explicit.
Otherwise the cancellation erases the arithmetic cocycle it was meant to
normalize.

## Consequence for the three-coset hostile

The positive Fourier-fixed three-coset witness passes aggregate positivity,
self-duality, density, and origin normalization. It fails this connection
law: its cell boundaries and residue-dependent endpoint weights require a
different moving incidence distribution.

Therefore the first source-level discriminator is not its scalar Mellin
transform. It is the residual obtained by substituting its incidence packet
into the connection law above. A nonzero typed residual rejects the hostile
source before locating its zeros.

## Next finite test

Construct the denominator-three moving register explicitly and compute

\[
R_3(u,r)
=\partial_uq_u^{(3)}(r)
-\left(
-e^{-u}r
+e^u\sum_{n\geq1}n\delta(r-ne^u)
\right).
\]

The residual must expose the additional fractional endpoints and mod-three
weights. Then test whether any finite auxiliary control port can absorb
\(R_3\) without changing the original one-cell incidence ontology.

If not, the moving-incidence connection is a source-rigidity theorem stronger
than positivity, Fourier symmetry, and scalar boundary normalization.

## Operator stimulus

The operator asked us to catch transfers rather than merely inventory other
work. The decisive transfer is the horizontal-germ principle: the hidden
fractional wall is not a static extra coordinate. It is a co-moving detector
whose derivative generates the arithmetic boundary current.
