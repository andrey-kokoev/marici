# Paired polarization has a conditional metaplectic eighth phase

Epistemic-graph event: 1401.

## Canonical polarization exchange

For a finite regular fiber `X`, let

`B=Z[X]`, `C=Fun(X,Z)`.

The delta basis gives a bijection-natural isomorphism

`iota:B -> C`, `[x] -> delta_x`.

On the symplectic double of Ledger 1369 define

`J(c,b)=(-iota(b),iota^(-1)(c))`.

Then

`J^2=-1`

and `J` preserves the canonical symplectic form.  It exchanges the coefficient
and Betti Lagrangian polarizations and is nontrivial even though mapping-class
symmetry acts trivially on rank-one `pi_0`.

## Archimedean half-rotation

After real extension, the positive-path square root

`R=exp(pi J/4)=(1/sqrt(2))(1+J)`

is a symplectic rotation through `pi/4`.  The continuous path from the
identity selects one of its two metaplectic lifts.  In one effective
symplectic mode, the half-form/oscillator vacuum acquires phase

`exp(-i pi/8)`.

Dividing its argument by `pi` gives exactly the missing smooth-count
correction

`(-pi/8)/pi=-1/8`.

Thus the paired coefficient--Betti polarization has a conditional mechanism
with the correct archimedean eighth phase, without reading `Gamma`, `xi`, or
zero ordinates.

## Selection gap

The numerical and representation-theoretic match is not yet a boundary
theorem.  The paired system canonically supplies `J`, but no admitted Carrier
map currently requires the dual-cutoff boundary to use its positive square
root `R` rather than `J`, the identity, or another symplectic path.  Moreover:

- `R` does not preserve the integral lattice;
- its construction uses the real exponential and `sqrt(2)`;
- a physical coefficient--Betti chain realization remains unavailable; and
- no determinant calculation links this metaplectic phase to `xi`.

The exact next gate is therefore a source-derived corner or polarization
comparison whose Maslov path is `exp(theta J)` for `0<=theta<=pi/4`.

## Scope

This proves a canonical algebraic `J` and a conditional archimedean phase
match.  It does not prove that the Carrier selects the half-rotation or that
zeta zeros are eigenvalues.
